openapi: 3.1.0
info:
  title: go_trumpcards API
  description: |
    REST API for the go_trumpcards card game server.

    Each endpoint manages game state on the server using a session identifier
    (`sessionId`) supplied by the client.  Sessions expire after 1 hour of
    inactivity and the server supports up to 10 000 concurrent sessions.
  version: 1.0.0

servers:
  - url: /
    description: Current server (resolves to the host serving the Swagger UI page)

tags:
  - name: blackjack
    description: BlackJack game
  - name: andarbahar
    description: Andar Bahar (アンダーバハール) game
  - name: botifarra
    description: Botifarra (ボティファラ) game
  - name: rikken
    description: Rikken (リッケン) game
  - name: colourwhist
    description: Colour Whist (カラーホイスト) game
  - name: chemindefer
    description: Chemin de Fer (シュマン・ド・フェール) game
  - name: crazyfourpoker
    description: Crazy 4 Poker (クレイジー4ポーカー) game
  - name: poker
    description: 5-card Draw Poker game
  - name: oldmaid
    description: Old Maid (Babanuki) game
  - name: daifugo
    description: Daifugo (大富豪) game
  - name: bigtwo
    description: Big Two (大老二) game
  - name: sevens
    description: Sevens (7並べ) game
  - name: doubt
    description: Doubt (ダウト) game
  - name: holdem
    description: Texas Hold'em game
  - name: omaha
    description: Omaha Hold'em game
  - name: omahahilo
    description: Omaha Hi-Lo / 8 or Better (split-pot variant of Omaha)
  - name: bigo
    description: 5 Card Omaha (Big O) game (Omaha dealt 5 hole cards)
  - name: bigohilo
    description: 5 Card Omaha Hi-Lo (Big O) / 8 or Better (split-pot variant)
  - name: shortdeck
    description: Short Deck (6+ Hold'em) game
  - name: hearts
    description: Hearts game
  - name: memory
    description: Memory (神経衰弱) game
  - name: klondike
    description: Klondike (ソリティア) game
  - name: freecell
    description: FreeCell (フリーセル) game
  - name: seahaventowers
    description: Seahaven Towers (シーヘイブンタワーズ) game
  - name: baccarat
    description: Baccarat (バカラ) game
  - name: spades
    description: Spades (スペード) game
  - name: twotenjack
    description: Two Ten Jack (ツーテンジャック) game
  - name: crazyeights
    description: Crazy Eights (クレイジーエイト) game
  - name: pageone
    description: Page One (ページワン) game
  - name: ginrummy
    description: Gin Rummy (ジンラミー) game
  - name: conquian
    description: Conquian (コンキャン) game
  - name: chinchon
    description: Chinchon (チンチョン) game
  - name: threethirteen
    description: Three Thirteen (スリー・サーティーン) game
  - name: sevenbridge
    description: Seven Bridge (セブンブリッジ) game
  - name: president
    description: President / Scum (プレジデント) game
  - name: cassino
    description: Cassino (カッシーノ) game
  - name: scopa
    description: Scopa (スコパ) game
  - name: scopone
    description: Scopone (スコポーネ) game
  - name: escoba
    description: Escoba (エスコバ) — Spanish 4-player free-for-all sum-to-15 capture game
  - name: barbu
    description: Barbu (バルブ) — French compendium trick-taking game (7 contracts, 28 deals)
  - name: macau
    description: Macau (マカオ) — European Crazy Eights variant with magic cards (2/7/8/J) and "Macau!" declaration
  - name: mao
    description: Mao (マオ) — Crazy Eights shedding game with magic cards (2/A/8) plus a secret hidden rule
  - name: spanish21
    description: Spanish 21 (スパニッシュ21) — BlackJack variant with 48-card deck and bonus payouts
  - name: canasta
    description: Canasta (カナスタ) game
  - name: handandfoot
    description: Hand and Foot (ハンド・アンド・フット) — 4-player team Canasta variant
  - name: burraco
    description: Burraco (ブラーコ) — Canasta-derived rummy with pozzetto reserve hands
  - name: pinochle
    description: Pinochle (ピノクル) game
  - name: spider
    description: Spider Solitaire (スパイダーソリティア) game
  - name: napoleon
    description: Napoleon (ナポレオン) game
  - name: indianpoker
    description: Indian Poker (インディアンポーカー) game
  - name: videopoker
    description: "Video Poker (Jacks or Better)"
  - name: deuceswild
    description: "Deuces Wild (デューシーズワイルド) - Video Poker variant"
  - name: jokerpoker
    description: "Joker Poker (ジョーカーポーカー) - Video Poker variant"
  - name: euchre
    description: Euchre (ユーカー) game
  - name: pyramid
    description: Pyramid (ピラミッド) game
  - name: tripeaks
    description: TriPeaks (トリピークス) game
  - name: golf
    description: Golf Solitaire (ゴルフ) game
  - name: cribbage
    description: Cribbage (クリベッジ) game
  - name: threecard
    description: Three Card Poker (スリーカードポーカー) game
  - name: caribbeanstud
    description: Caribbean Stud Poker (カリビアンスタッドポーカー) game
  - name: texasholdembonus
    description: Texas Hold'em Bonus Poker (テキサスホールデムボーナスポーカー) game
  - name: ohhell
    description: Oh Hell (オー・ヘル) game
  - name: ninetynine
    description: Ninety-Nine (ナインティナイン) game
  - name: bridge
    description: Contract Bridge (コントラクトブリッジ) game
  - name: pineapple
    description: Pineapple Poker (パイナップルポーカー) game
  - name: crazypineapple
    description: Crazy Pineapple Poker (クレイジーパイナップル) game — Pineapple variant where the discard happens after the flop betting round
  - name: irishpoker
    description: Irish Poker (アイリッシュポーカー) game — 4 hole cards dealt, discard 2 after flop betting, play with 2
  - name: speed
    description: Speed (スピード) game
  - name: gofish
    description: Go Fish (ゴーフィッシュ) game
  - name: pigtail
    description: Pig's Tail (ぶたのしっぽ) game
  - name: sevencardstud
    description: Seven Card Stud (セブンカード・スタッド) game
  - name: fivecardstud
    description: Five Card Stud (ファイブカード・スタッド) game
  - name: soko
    description: Soko / Canadian Stud (ソッコ) game - Five Card Stud with four-card straight and flush
  - name: fourseasons
    description: Four Seasons / Corners (フォーシーズンズ) game - cross tableau with wraparound foundations
  - name: colorado
    description: Colorado (コロラド) game - two-deck solitaire with four ascending and four descending foundations
  - name: cribbagesquares
    description: Cribbage Squares (クリベッジ・スクエアズ) game - 4x4 grid scored as eight cribbage hands against a hidden starter
  - name: diplomat
    description: Diplomat (ディプロマット) game - two-deck solitaire whose tableau builds down ignoring suit
  - name: royalcotillion
    description: Royal Cotillion (ロイヤルコティヨン) game - eight foundations built by twos that wrap through all thirteen ranks
  - name: crazyquilt
    description: Crazy Quilt (クレイジーキルト) game - an 8x8 quilt where only cards with an exposed short side can be taken
  - name: germanwhist
    description: German Whist (ジャーマンホイスト) game endpoints -- 26 tricks in two halves, only the second half scores
  - name: slobberhannes
    description: Slobberhannes (スロバーハンネス) game endpoints -- avoid the first trick, the last trick, and the Q of clubs
  - name: polignac
    description: Polignac (ポリニャック) game endpoints -- avoid the jacks; the J of spades costs double
  - name: reversis
    description: Reversis (レヴェルシ) game endpoints -- 48-card avoidance with a chip pool; the Quinola and the A of diamonds cost extra
  - name: rams
    description: Rams (ラムス) game endpoints -- a 3-5 player pot game; play or drop, and taking no trick costs extra
  - name: tarabish
    description: Tarabish (タラビッシュ) game endpoints -- 36-card Jass-family partnership game; trump J and 9 outrank the ace
  - name: baloot
    description: Baloot (バルート) game endpoints -- 32-card Gulf partnership game; the declared mode (Sun or Hokom) swaps the rank order itself
  - name: estimation
    description: Estimation (エスティメーション) game endpoints -- 4-player Oh Hell from the Gulf; only an exact call scores, and Dash/Risk calls swing wider
  - name: israeliwhist
    description: Israeli Whist (イスラエリホイスト) game endpoints -- 4-player two-stage bidder; an auction settles trump and a quota, then everyone calls separately
  - name: hokm
    description: Hokm (ホクム) game endpoints -- Iran's 2v2 trick-taker; a hand ends the moment one side takes seven tricks, and a shut-out (Kot) counts double
  - name: shelem
    description: Shelem (シェレム) game endpoints -- Iran's bridge-shaped bidder; what is bid is the score itself, with a widow exchange and an all-tricks Shelem call
  - name: mendikot
    description: Mendikot (メンディコット) game endpoints -- India's 2v2 trick-taker decided by the four tens, with no trump-choosing phase
  - name: bhabhi
    description: Bhabhi (バービー) game endpoints -- India and Pakistan's 3-7 player avoidance game; it names a loser, not a winner
  - name: teendopaanch
    description: 3-2-5 (ティーン・ドー・パーンチ) game endpoints -- northern India's three-handed trick-taker where the 3/2/5 targets are assigned, never bid
  - name: hasenpfeffer
    description: Hasenpfeffer (ハーゼンプフェファー) game endpoints -- the German-American euchre variant where the joker is the highest trump and bidding is compulsory
  - name: sergeantmajor
    description: Sergeant Major / 8-5-3 (サージェントメジャー) game endpoints -- the British army's three-hander where the 8/5/3 targets are fixed by seat, never bid
  - name: honeymoonbridge
    description: Honeymoon Bridge (ハネムーンブリッジ) game endpoints -- bridge for two, where the first thirteen tricks score nothing and only draw you cards
  - name: minibridge
    description: Minibridge (ミニブリッジ) game endpoints -- bridge with the auction replaced by an open high-card-point count
  - name: pasur
    description: Pasur (パスール) game endpoints -- Iran's fishing game, where captures add to eleven and emptying the table doubles what you took
  - name: snap
    description: Snap (スナップ) game endpoints -- England's reflex game, where the trigger moves with every card
  - name: rollingstone
    description: Rolling Stone (ローリングストーン) game endpoints -- the inside-out trick game where failing to follow suit hands you the trick
  - name: lingerlonger
    description: Linger Longer (リンガーロンガー) game endpoints -- an American elimination trick game where a trick wins you one card from the stock

  - name: pig
    description: Pig (ピッグ) game endpoints -- an American children's party game where four of a kind is signalled in silence

  - name: stealingbundles
    description: Stealing Bundles (スティーリングバンドル) game endpoints -- an American fishing game where a rival's won pile can be taken whole

  - name: cucumber
    description: Cucumber (キューカンバー) game endpoints -- a Swedish trick game with a suitless comparison follow

  - name: goofspiel
    description: Goofspiel (ゴフスピール) game endpoints -- a simultaneous-bidding mathematical game, also known as GOPS

  - name: clocksolitaire
    description: Clock Solitaire (クロックソリティア) game
  - name: durak
    description: Durak (ドゥラーク) game
  - name: fortythieves
    description: Forty Thieves (フォーティシーブス) game
  - name: paigow
    description: Pai Gow Poker (パイゴウポーカー) game
  - name: chinesepoker
    description: Chinese Poker (チャイニーズポーカー) game
  - name: war
    description: War (戦争) game
  - name: fiftyone
    description: Fifty-one (フィフティワン) game
  - name: yukon
    description: Yukon (ユーコン) game
  - name: cruel
  - name: russiansolitaire
    description: Russian Solitaire (ロシアンソリティア) game - Yukon variant requiring same-suit descending tableau moves
  - name: letitride
    description: Let It Ride (レットイットライド) game
  - name: pokersquares
    description: Poker Squares (ポーカー・スクエア) game
  - name: reddog
    description: Red Dog (レッドドッグ) game
  - name: sevencardstudhilo
    description: Seven Card Stud Hi-Lo
  - name: razz
    description: Razz (ラズ) game - lowball variant of Seven Card Stud
  - name: badugi
    description: Badugi (バドゥーギ) game - Asian 4-card draw lowball poker
  - name: deucetoseven
    description: 2-7 Triple Draw (デューストゥセブン) game - Deuce to Seven lowball draw poker
  - name: scorpion
    description: Scorpion (スコーピオン) game - variant of solitaire combining Yukon and Spider mechanics
  - name: wasp
    description: Wasp (ワスプ) game - easier Scorpion variant where empty columns accept any card
  - name: accordion
    description: Accordion (アコーディオン) game - compression solitaire where piles merge onto the 1st or 3rd pile to the left
  - name: trash
    description: Trash (トラッシュ / ガベージ) game - 2-player matching game where each player flips 10 face-down cards into position 1..10 using wilds
  - name: calculation
    description: Calculation (カルキュレーション) game - arithmetic-progression solitaire where 4 foundations (A,2,3,4) build up mod-13 with step sizes +1/+2/+3/+4
  - name: sirtommy
    description: Sir Tommy game
  - name: auldlangsyne
    description: Auld Lang Syne (オールド・ラング・サイン) game - forced four-way deal patience
  - name: bisley
    description: Bisley (ビズリー) game - fully open solitaire with two foundation sets, four ascending (A→K, dealt with the Aces) and four descending (K→A, opened by playing a King)
  - name: napoleonssquare
    description: Napoleon's Square game - two-deck square solitaire; eight foundations seeded with the Aces, twelve columns of four, and same-suit runs that move as one unit
  - name: grandfathersclock
    description: Grandfather's Clock game - twelve clock faces, each finishing on the rank of the hour it occupies; no stock, all 52 cards dealt at the start
  - name: missmilligan
    description: Miss Milligan game - two-deck solitaire dealt eight at a time onto every column, with a "waiving" rescue that holds one run aside once the stock is gone
  - name: duchess
    description: Duchess (Glenwood) game - Canfield-family solitaire where the player picks the foundations' starting rank off a reserve fan, and empty columns stay locked to the reserve
  - name: windmill
    description: Windmill (Propeller) game - two-deck cross solitaire whose centre climbs A-K four times over while four corner foundations run King down to Ace
  - name: americantoad
    description: American Toad game - the two-deck Canfield, with a 20-card reserve that auto-fills empty columns and foundations that wrap round from the deal-chosen base rank
  - name: congress
    description: Congress game - eight single-card piles flanking eight foundations; the tableau builds down ignoring suit one card at a time, and an empty pile takes only a stock or waste card
  - name: braid
    description: Braid game - a two-deck solitaire whose eight foundations build in ONE player-chosen direction, with a 20-card braid that only feeds them
  - name: pontoon
    description: Pontoon game - the British ancestor of blackjack; the banker's hand is face down, a five-card trick beats 21, and the banker keeps the bank while winning
  - name: settemezzo
    description: Sette e Mezzo game - the Italian 7.5 game on a 40-card deck; totals are carried in HALVES so the half-point cards compare exactly
  - name: niuniu
    description: Niu Niu game - the Chinese bull game where three of five cards must sum to a multiple of ten, and the remaining pair's last digit is the rank
  - name: chineseten
    description: Chinese Ten game - a fishing game where A-9 capture by summing to ten and 10-K by rank, and only the red cards score
  - name: toepen
    description: Toepen game - a Dutch bluffing trick game whose ranking is the standard order inverted (10 high, jack low), where only the winner of the final trick escapes the penalty
  - name: mushi
    description: Mushi game - hanafuda on 40 cards with June and July removed; a hana-awase game with no koi-koi stop, and November's lightning card is wild
  - name: bura
    description: Bura game - a 36-card Russian trick game where up to three cards of one suit are led together and 31 card points must be CLAIMED, not merely reached
  - name: terrace
    description: Terrace (Queen of Italy) game - the foundations build up in alternating colour from a player-chosen base rank, and the 11-card terrace feeds them and nothing else
  - name: spiteandmalice
    description: Spite and Malice (スパイト・アンド・マリス) game - 2-player race (1 human vs 1 CPU) to empty a goal pile by playing A→Q onto shared foundations; predecessor of Skip-Bo
  - name: skat
    description: Skat (スカート) game - Germany's national 3-player trick-taking game with a 32-card deck, ladder auction, and three game types (Suit / Grand / Null)
  - name: skitgubbe
    description: Skitgubbe
  - name: laughandliedown
    description: Laugh and Lie Down
  - name: sjavs
    description: Sjavs
  - name: trex
    description: Trex
  - name: loba
    description: Loba
  - name: desmoche
    description: Desmoche
  - name: zwicker
    description: Zwicker
  - name: poch
    description: Poch
  - name: popejoan
    description: Pope Joan
  - name: nainjaune
    description: Le Nain Jaune
  - name: kille
    description: Kille
  - name: klaberjass
    description: Klaberjass
  - name: kaiser
    description: Kaiser
  - name: boston
    description: Boston
  - name: vint
    description: Vint
  - name: bideuchre
    description: Bid Euchre
  - name: sixbidsolo
    description: Six-Bid Solo
  - name: karnoffel
    description: Karnöffel
  - name: literature
    description: Literature
  - name: guandan
    description: Guandan
  - name: shengji
    description: Sheng Ji / Tractor
  - name: shithead
    description: Shithead / Karma (シットヘッド / カーマ) game - 4-player shedding game with three card layers (face-down / face-up / hand) and configurable magic cards (2/7/8/10 + four-of-a-kind burn)
  - name: nertz
    description: Nertz / Pounce (ナーツ / パウンス) game - real-time multi-player competitive solitaire (2-6 players) racing to empty a 13-card Nertz pile onto shared Klondike-style foundations
  - name: slapjack
    description: Slapjack (スラップジャック) game - real-time reflex game where 1 human races a CPU to slap the centre pile when a Jack appears on top
  - name: egyptianratscrew
    description: Egyptian Ratscrew (エジプシャン・ラットスクリュー) game - real-time reflex game where 1 human races a CPU to slap pairs/sandwiches; face-card chance battles add a strategic layer
  - name: bakersdozen
    description: Baker's Dozen (ベーカーズ・ダズン) game - single-player open-information solitaire with 13 tableau columns of 4 cards each; kings auto-move to the bottom of each column at deal time
  - name: tonk
    description: Tonk (トンク) game - 2-player rummy variant with 5-card hands; instant Tonk win on a 49 or 50 point initial deal, knock when deadwood is 5 or less
  - name: thirtyone
    description: Thirty-One (サーティワン / Scat) game - 4-player draw-and-discard survival game; build the highest single-suit total (A=11, J/Q/K=10), knock to stand pat, lowest score loses a life, hitting exactly 31 wins the round instantly
  - name: yaniv
    description: Yaniv (ヤニブ) game - 4-player Israeli draw-and-discard hand-reduction game on a 54-card deck; discard singles/sets/runs, draw from stock or a discard end, declare Yaniv at hand total <= 5, beware the Asaf undercut (30-point penalty)
  - name: gongzhu
    description: Gong Zhu (拱猪 / Chinese Hearts) game - 4-player trick-taking game with positive and negative point cards (♠Q=-100, ♦J=+100), a doubling ♣10, graded hearts (total -200), an all-hearts +200 bonus, and a pre-round exposure phase that doubles a card's stakes
  - name: tienlen
    description: Tien Len (ティエンレン / Vietnamese Big Two) game - 4-player shedding game; suit order ♠<♣<♦<♥, holder of ♠3 leads, chops (three consecutive pairs / four of a kind) cut a lone 2
  - name: osmosis
    description: Osmosis (オズモシス / 浸透) solitaire - foundation-only single-player game; 4 reserve columns of 4 cards, 4 vertical foundation rows (one suit each), the top row builds any order while lower rows accept only ranks already present in the row directly above ("seep")
  - name: bristol
    description: Bristol (ブリストル) solitaire - single-player tableau-and-reserve game; 8 tableau columns (build down any suit, top card only, emptied columns are dead), 3 fans dealt from a stock (one card each per draw), and 4 foundations built up Ace-to-King regardless of suit
  - name: bidwhist
    description: Bid Whist (ビッド・ホイスト) - 4-player partnership trick-taking on a 54-card deck (52 + 2 jokers); 12-card hands with a 6-card kitty, auction bidding (target tricks 1-7 over the book of 6 plus Uptown/Downtown/No-Trump direction), trump declaration, kitty exchange, big/little joker as top trumps, first team to the target score (default 7) wins
  - name: tressette
    description: Tressette (トレセッテ) - 4-player no-trump must-follow trick-taking team game on the 40-card Briscola deck (8/9/10 removed), 10-card hands; trick strength 3>2>A>K>Q>J>7>6>5>4, scoring in thirds (A=1pt, 2/3/K/Q/J=1/3, last trick +1/3), first team to the target (default 21) wins
  - name: easthaven
    description: Easthaven (イーストヘイブン) - single-player Klondike/Spider hybrid solitaire; 7 tableau columns of 3 (2 face-down + 1 face-up), alternating-color descending tableau and A-to-K suit foundations (Klondike), with a Spider-style stock that deals one card to every column at once (no waste pile)
  - name: tichu
    description: Tichu (ティチュー) - 4-player 2-vs-2 partnership shedding game on a 56-card deck (52 + Dragon/Phoenix/Dog/Mahjong); 14-card hands, optional Tichu/Grand Tichu declarations (±100/±200), Daifugo-style combinations with four-of-a-kind and straight-flush bombs, special-card behaviours (Mahjong leads, Dog passes lead to partner, Phoenix wild, Dragon strongest single but trick goes to an opponent), card points (5/10/K, Dragon +25, Phoenix -25), one-two double victory (+200); higher team score wins the deal
  - name: bourre
    description: Bourré (ブーレ) - 5-player gambling trick-taking game fusing poker-style ante/draw betting with must-follow trick play on a 52-card deck; each player antes into the pot and is dealt 5 cards with the dealer's last card setting the trump suit, then decides to play or fold and may discard/draw (draw poker); five tricks under must-follow/must-trump-when-void/must-win-when-able rules, the most tricks takes the pot, and a player who plays but wins zero tricks is "bourréd" and pays a penalty equal to the pot
  - name: fivehundred
    description: 500 (Five Hundred / ファイブハンドレッド) game - 4-player partnership trick-taking with a 43-card deck (joker + red 4-A + black 5-A); auction bidding (6-10 tricks per suit/NT plus Misère/Open Misère), kitty exchange, Euchre-style bowers + joker as top trumps, first team to 500 wins
  - name: casinowar
    description: Casino War (カジノウォー) game - simple player-vs-dealer table game; high card wins (Aces high), tie offers Surrender (forfeit half ante) or War (match ante and re-draw)
  - name: pitch
    description: Pitch / Setback (ピッチ / セットバック) game - 4-player single-handed Auction Pitch trick-taking; bid 0/2/3/4 then compete for High/Low/Jack/Game points (4 max); set back on a missed bid
  - name: dragontiger
    description: Dragon Tiger (ドラゴンタイガー) game - simple 1-on-1 high-card showdown; Dragon vs Tiger (Aces low, Kings high), 1:1 on win, half refund on tie for Dragon/Tiger bets, 8:1 on tie for the Tie bet
  - name: blackjackswitch
    description: Blackjack Switch (ブラックジャック・スイッチ) game - blackjack variant where the player plays two hands and may swap the second card between them; in return, naturals pay 1:1 and a dealer 22 is a push (not a bust)
  - name: montecarlo
    description: Monte Carlo Solitaire (モンテカルロ・ソリティア) game - 5x5 grid solitaire; remove 8-direction adjacent same-rank pairs, then deal to compress the board top-left and refill from the stock; clear all 52 cards to win
  - name: contractrummy
    description: Contract Rummy (コントラクトラミー) game - 3-player rummy (1 human + 2 CPU) over 7 progressive rounds; each round has a fixed contract (sets / runs) the player must lay before going out; lowest cumulative penalty across rounds wins
  - name: kalooki
    description: Kalooki (カルーキ) game - Contract Rummy-style table-meld rummy for 2-4 players (1 human + 1-3 CPU) using a 106-card 2-deck plus 2 jokers, 13 cards dealt each; first meld must total at least the opening threshold (default 51), jokers are wild (joker melds score 1.5x), lay off onto any player's melds after opening, go out to win the round, lowest cumulative score wins
  - name: ultimatetexasholdem
    description: Ultimate Texas Hold'em (アルティメット・テキサスホールデム) game - single-player vs dealer Texas Hold'em variant with Ante + equal Blind wagers, optional Trips side bet, and one-shot Play raise at Pre-flop (3x / 4x), Flop (2x) or River (1x); dealer must qualify with a pair or better
  - name: crescent
    description: Crescent Solitaire (クレセント・ソリティア) game - 2-deck (104-card) solitaire with 16 tableau piles arranged around 8 foundations (4 ascending from A, 4 descending from K); same-suit ±1 moves with A↔K wrap, top card only; up to 3 redeals reverse every tableau pile
  - name: mississippistud
    description: Mississippi Stud (ミシシッピ・スタッド) game - single-player vs paytable casino poker; 2 hole cards plus 3 community cards revealed across 3rd / 4th / 5th Street, with 1x / 2x / 3x bet or fold at each street; winning hands paid per fixed paytable (pair of Jacks+ = 1:1, pair of 6s-10s = push)
  - name: belote
    description: Belote (ベロート) game - French 4-player partnership trick-taking on a 32-card deck (7-A × 4 suits); two-round bidding (take face-up suit / call other suit), strict obligation to follow / over-trump, last-trick bonus (Dix de Der +10) and K+Q-of-trumps bonus (Belote/Rebelote +20)
  - name: spiderette
    description: Spiderette (スパイダレット) game - single-player solitaire combining Klondike's 7-column staircase deal with Spider's same-suit descending move rules on a single 52-card deck; clear all 4 suits to win
  - name: mighty
    description: Mighty (マイティ) game - Korean 5-player trick-taking card game with bidding for point cards (10/J/Q/K/A, 20 max) and a separate No-Trump axis; declarer names a trump suit and a secret partner card; special cards Mighty (♠A normally; ♦A under spade trump), Joker (second-highest when not led, with a Joker-lead demand-suit rule and a Joker Call card ♣3/♠3 forcing the Joker)
  - name: oasispoker
    description: Oasis Poker (オアシスポーカー) game - Caribbean Stud variant adding a card-exchange phase between Bet and Action; exchange 1..5 cards for a fee equal to ante per card, then Call or Fold against the dealer
  - name: russianpoker
    description: Russian Poker (ロシアンポーカー) game - Oasis Poker evolution with 6th card purchase and force-qualify options; exchange cards, buy a 6th card, or force dealer's highest card exchange when dealer doesn't qualify
  - name: beleagueredcastle
    description: Beleaguered Castle (包囲された城) game - single-player open-information solitaire with 8 tableau columns of 6 cards each and 4 aces pre-seeded on the foundations; empty columns accept any card
  - name: piquet
    description: Piquet (ピケ) game - 2-player classical trick-taking using a 32-card deck (7-A); each deal has Exchange (Elder swaps 1-5, Younger 0-3), Declaration (Point/Sequence/Set auto-compared, honest claims), and 12-trick Play; full traditional bonuses (carte blanche, repique, pique, cards, capot) plus 6-deal partie with rubicon scoring
  - name: casinoholdem
    description: Casino Hold'em (カジノホールデム) game - single-player casino Texas Hold'em variant with Ante and optional AA Bonus side bet; flop revealed with hole cards, then Call (2x Ante) or Fold; dealer qualifies on Pair of Fours or better
  - name: callbreak
    description: Call Break (コールブレイク) game - 4-player Spades-style trick-taking game from South Asia; spades are permanent trump, must-trump-if-void rule, int×10 scoring (bid hit = bid*10 + overtricks, miss = -bid*10); 5 rounds fixed, highest cumulative score wins
  - name: tarneeb
    description: Tarneeb (ターニーブ) game - 4-player Middle-Eastern partnership trick-taking. Bid 7..13 or pass; bid winner declares trump suit; bidder team scores +tricks on success / -bid on failure, defender team always scores +tricks; first team to PointLimit wins.
  - name: highcardflush
    description: High Card Flush (ハイカードフラッシュ) game - single-player casino table game. Both player and dealer get 7 cards; longest same-suit subset (flush length) decides the winner (ties broken by high card within the flush). Player places an Ante and optional Flush Bonus / Straight Flush Bonus side bets, then chooses Raise (1x/2x/3x capped by flush length) or Fold. Dealer qualifies on 3+ card flush with 9-high or better.
  - name: briscola
    description: Briscola (ブリスコラ) game - 2-player Italian trick-taking classic. 40-card deck (A,2-7,J,Q,K x 4 suits — no 8/9/10), 3-card hands replenished from the stock, face-up trump (briscola) decides the trump suit. No must-follow — any card is legal at any time. Intra-suit rank order A>3>K>Q>J>7>6>5>4>2; trump beats non-trump. Card points A=11, 3=10, K=4, Q=3, J=2 (total 120); >60 points wins, 60-60 is a tie.
  - name: schnapsen
    description: Schnapsen / Sixty-Six (シュナプセン / 66) game - 2-player Austrian/German trick-taking classic. 20-card deck (A,10,K,Q,J x 4 suits), 5-card hands replenished from the stock, face-up trump upcard decides the trump suit. Phase 1 (stock present) has no must-follow; phase 2 (stock exhausted) requires following suit, winning if able, else trumping. A marriage (K+Q of one suit, declared on lead) scores 20 (40 for trump). Intra-suit rank A>10>K>Q>J; card points A=11, 10=10, K=4, Q=3, J=2 (total 120). First to 66 wins the round; otherwise the last trick taker wins.
  - name: gaps
    description: 'Gaps / Montana (ギャップス) - single-player puzzle solitaire on a 4x13 grid. All four Aces are removed leaving 4 gaps. A gap accepts the card whose suit matches its left neighbour and whose rank is +1. A gap in column 0 accepts any 2. A gap right-of-King is dead. Up to 3 redeals are allowed; the locked prefix of each row (2 then same-suit +1 successors) stays put and the rest is shuffled. Win when each row holds 2..K of one suit.'
  - name: fourcardpoker
    description: Four Card Poker (フォーカードポーカー) - single-player casino poker. Player is dealt 5 cards and dealer 6 (one upcard); both sides use their best 4-card hand. No dealer qualification. Player chooses Play (1x/2x/3x Ante) or Fold after seeing the upcard. 4-card ranks (ascending) - High Card < Pair < Two Pair < Straight < Flush < Three of a Kind < Straight Flush < Four of a Kind. Auto Ante Bonus pays for 3-of-a-Kind or better. Optional Aces Up sidebet pays on Pair of Aces or better.
  - name: rummy500
    description: Rummy 500 (500ラム / ラミー500) - 2-player rummy where the discard pile is an ordered stack and a player can draw any card from it (taking all cards above the chosen index). Sets (3+ same rank) and runs (3+ same-suit consecutive; Aces low or high but no K-A-2 wrap) are melded and may be extended by layoff. Scoring per round = laid-meld points (positive) minus remaining hand points (negative); A=1 (or 15 in a Q-K-A run), J/Q/K=10, others face value. First to the configured point limit (default 500) wins.
  - name: eightoff
    description: Eight Off (エイトオフ) - single-player open-information solitaire and a stricter FreeCell variant. 8 tableau columns x 6 face-up cards (48 cards) plus the remaining 4 cards pre-placed in the first 4 of 8 free cells. 4 foundations build up by suit Ace to King. Tableau builds same-suit descending only; empty columns accept only Kings. Supermove limit (1 + emptyFreeCells) x 2^(emptyTableauCols). A* solver gates stalemate detection and auto-complete.
  - name: penguin
    description: Penguin (ペンギン) - single-player FreeCell variant solitaire. 7 tableau columns x 7 face-up cards (49 cards) plus 3 cards of the same rank as the first dealt card (baseRank) pre-placed in 3 of 7 free cells. 4 foundations build up by suit from baseRank with K-A wraparound. Tableau builds same-suit descending with K-A wraparound; empty columns accept only prevRank(baseRank) cards. Supermove supported.
  - name: doudizhu
    description: Dou Dizhu (斗地主) - 3-player asymmetric shedding game with a 54-card deck (52 + 2 jokers). 17 cards each plus a 3-card kitty. Players bid 1-3 to become the Landlord (takes the kitty, 20 cards) vs two cooperating Peasants. Card strength 3<...<2<SmallJoker<BigJoker. Combos include singles, pairs, trios, straights, consecutive pairs, airplanes, bombs (4-of-a-kind), and rockets (both jokers). Bombs/rockets beat any non-bomb play. Score = baseBid x 2^bombCount.
  - name: truco
    description: Truco (トゥルコ) - 2-player (1 human + 1 CPU) South-American trick-taking + bluffing game with a 40-card deck (8/9/10 removed). Deal 3 cards per hand (mano); win 2 of 3 bazas (tricks, no must-follow) to win the mano. Card strength (matadores) 1-spades > 1-clubs > 7-spades > 7-coins > all 3 > 2 > 1 > K > Q > J > 7 > 6 > 5 > 4; equal-strength bazas tie (parda). Declare Truco to raise the stake (Truco=2, Retruco=3, Vale Cuatro=4); opponent accepts (Quiero), declines (No Quiero = caller wins at the prior stake), or re-raises. First to the match target (default 15) wins.
  - name: acesup
    description: Aces Up (四つ葉のクローバー) - single-player solitaire with a standard 52-card deck. 4 tableau columns dealt 1 card each (48 to stock). Aces rank highest. Remove a column's top card when another column's top is the same suit and higher rank; deal one card onto each column; move a top card into an empty column. Win by clearing the board down to the four Aces.
  - name: whist
    description: Whist (ホイスト) - 4-player partnership trick-taking on a standard 52-card deck; 13-card hands, the last dealt card fixes the trump suit, must-follow trick play, the highest trump (or highest lead-suit card if none) wins each trick, and the team taking more than 6 tricks scores 1 point per trick over the book of 6.
  - name: catchten
    description: Catch the Ten (Scotch Whist / キャッチ・ザ・テン) - 4-player 2v2 partnership trick-taking on a Scottish 36-card deck (ranks 6-A × 4 suits); 9-card hands, the last dealt card fixes the trump suit, must-follow trick play. In the trump suit the rank order is special - J highest, then A, K, Q, 10, 9, 8, 7, 6; non-trump suits are A high down to 6. Honor capture - the trick winner scores trump J=11, 10=10, A=4, K=3, Q=2 (non-trump cards score nothing). First side to the point limit (default 41) wins; an exact tie when both reach the target in the same deal is a draw.
  - name: canfield
    description: Canfield (キャンフィールド) - single-player solitaire with a standard 52-card deck; 4 tableau columns, a 13-card reserve, a stock dealt 3 at a time, and 4 foundations built up by suit from a randomly chosen base rank with K-A wraparound. Tableau builds down alternating colors with wraparound; the reserve feeds the tableau.
  - name: sixcardgolf
    description: Six Card Golf (シックスカードゴルフ) - 2-4 player golf-style game on a standard 52-card deck; each player holds a 2x3 grid of six cards (two face-up at start). On a turn, draw from the stock or discard pile and either swap a grid card or discard. The round ends when a grid is fully face-up; lowest total score wins, with vertical pairs of equal rank scoring zero.
  - name: bakersgame
    description: Baker's Game (ベーカーズゲーム) - single-player FreeCell variant on a standard 52-card deck; identical layout to FreeCell (8 tableau columns, 4 free cells, 4 foundations Ace-to-King by suit) except tableau builds **same-suit descending** instead of alternating colors, making it harder. Only Kings move to an empty column; supermove limit = (1 + emptyFreeCells) x 2^(emptyTableauCols).
  - name: sheepshead
    description: Sheepshead (シープスヘッド) - 5-player Wisconsin/German trick-taking game on a 32-card deck (A,7,8,9,10,J,Q,K × 4 suits); 6 cards each plus a 2-card blind; 14 fixed trumps (Q♣>Q♠>Q♥>Q♦>J♣>J♠>J♥>J♦>A♦>10♦>K♦>9♦>8♦>7♦); picker takes blind and buries 2, calls a fail-suit Ace to name a secret partner; picker team needs 61+ of 120 card points to win; Schneider (≤30) doubles, Schwarz (0 tricks) triples the chip stake
  - name: doppelkopf
    description: Doppelkopf (ドッペルコップ) - 4-player German trick-taking game on a 48-card doubled deck (9,10,J,Q,K,A × 4 suits × 2 copies); 12 cards each; 26 fixed trumps (♥10/Dulle>Q♣>Q♠>Q♥>Q♦>J♣>J♠>J♥>J♦>A♦>10♦>K♦>9♦, each ×2); two Q♣ holders secretly form the Re team; identical cards played in same trick resolved by play order (first wins); Re team needs 121+ of 240 card points to win; Re/Kontra announcements in trick 1 double the stake
  - name: mus
    description: Mus (ムス) - 4-player Basque/Spanish vying game on a 40-card Latin deck (A,2-7,Sota,Caballo,Rey × 4 suits); 4 cards each; 2 teams (seats 0&2 vs 1&3); Mus phase (discard/redraw up to 3 times) then four betting rounds Grande (highest hand)>Chica (lowest hand)>Pares (pairs)>Juego (31+ points); bets via Paso/Envido/Ordago with Quiero/No Quiero responses; first team to target amarrakos (default 40) wins
  - name: tute
    description: Tute (トゥーテ) - 4-player Spanish trick-taking game on a 40-card Latin deck (A,2-7,Sota,Caballo,Rey × 4 suits); 10 cards each; 2 teams (seats 0&2 vs 1&3); trump set by last dealt card; must-follow trick play, rank order A>3>Rey>Caballo>Sota>7>6>5>4>2; card points A=11, 3=10, Rey=4, Caballo=3, Sota=2 (total 120 + last trick +10); cante (marriage) declaration on lead scores +20 (non-trump) or +40 (trump); holding all 4 Reys or all 4 Caballos allows a Tute declaration for an instant game win; first team to 121 wins
  - name: sueca
    description: Sueca (スエカ) - 4-player Portuguese/Brazilian trick-taking game on a 40-card Latin deck (A,2-7,Sota/Caballo/Rei × 4 suits); 10 cards each; 2 teams (seats 0&2 vs 1&3); trump set by last dealt card; must-follow trick play, distinctive rank order A>7>Rei>Valete/Sota>Dama/Caballo>6>5>4>3>2; card points A=11, 7=10, Rei=4, Valete/Sota=3, Dama/Caballo=2 (total 120); no marriages/declarations — pure 10-trick play; round winner earns 1 game point (61-90 pts), 2 (91-119 pts), or 4 (120 pts sweep); 60-60 draw earns no points; first team to target (default 4) game points wins the match
  - name: klaverjas
    description: Klaverjas (クラヴァヤス) - 4-player Dutch Jass-family trick-taking game on a 32-card deck (7,8,9,10,J,Q,K,A × 4 suits); 8 cards each; 2 teams (seats 0&2 vs 1&3); trump set by last dealt card; Jass ranking — trump J(Jas)>9(Nel)>A>10>K>Q>8>7, non-trump A>10>K>Q>J>9>8>7; trump card points J=20, 9=14, A=11, 10=10, K=4, Q=3, 8=0, 7=0; non-trump A=11, 10=10, K=4, Q=3, J=2, others=0 (total 152); last trick +10 bonus; Klaverjas obligation — must follow led suit, if void must trump, if trump on table must overtrump when able; Roem (melds) auto-detected at round start (3-card run=20, 4-card run=50, four-of-a-kind excl 7s/8s=100); scores accumulate; first team to target (default 1501) wins
  - name: manille
    description: Manille (マニーユ) - 4-player Belgian/French trick-taking game on a 32-card deck (7,8,9,10,J,Q,K,A × 4 suits); 8 cards each; 2 teams (seats 0&2 vs 1&3); trump set by last dealt card; reversed rank — 10(Manille)>A(Manillon)>K>Q>J>9>8>7 within every suit; card points 10=5, A=4, K=3, Q=2, J=1, 9/8/7=0 (total 60); must-follow + trump-conservation exemption when partner is already winning the trick; no overtrump obligation; no melds, no bidding, no last-trick bonus; card points accumulate; first team to target (default 101) wins
  - name: marias
    description: Mariáš (マリアーシュ) - 3-player Czech/Slovak trick-taking game on a 32-card German-suited deck (7,8,9,10,J,Q,K,A × 4 suits); 10 cards each (2-card talon unused in simplified impl); no fixed teams — each round one Soloist (forehand, rotates) vs two Defenders; trump = Soloist's longest suit (auto-declared); rank A>10>K>Q>J>9>8>7 for all suits; card points A=11, 10=10, K=4, Q=3, J=2, 9/8/7=0 (total 120); marriages — K+Q same suit at deal scores 20 (40 for trump); must-follow + forced trump when void; last-trick +10 bonus; Soloist wins round (gains 2 game pts) if total > Defenders' combined total, else each Defender gains 2 game pts; first to target (default 10 game pts) wins
  - name: sedma
    description: Sedma (セドマ) - 4-player Czech/Slovak capture trick-taking game on a 32-card deck (7,8,9,10,J,Q,K,A × 4 suits); 8 cards each; 2 teams (seats 0&2 vs 1&3); no trump suit; no follow obligation — any card may be played freely; a card captures the trick if its rank matches the lead card's rank OR it is a 7 (wild); the last player in play order to capture wins the trick; if nobody after the lead captures, the lead player wins; card points A=10, 10=10, others=0 (total 80); last-trick +10 bonus; card points accumulate; first team to target (default 101) wins
  - name: solowhist
    description: Solo Whist (ソロ・ホイスト) - 4-player British trick-taking game on a standard 52-card deck (A,2-10,J,Q,K × 4 suits); 13 cards each, 13 tricks per round; bidding phase first (Pass/Solo/Misère/Abundance, order lowest-to-highest); highest bidder becomes the Declarer and plays alone vs 3 Defenders; trump = Declarer's longest suit for Solo/Abundance (auto-declared); Misère has no trump; rank A>K>Q>J>10>9>8>7>6>5>4>3>2; must-follow rule — follow led suit if able; Declarer wins contract value (Solo=2, Misère=3, Abundance=4 game pts) on success; each Defender wins that value on failure; first to target (default 21 game pts) wins
  - name: knockoutwhist
    description: Knockout Whist (ノックアウト・ホイスト) — 4-player British survival trick-taking game on a standard 52-card deck (A,2-10,J,Q,K × 4 suits); hand size shrinks each round (round 1 = 7 cards, round 2 = 6 cards, …, round 7 = 1 card; handSize = 8 − roundNumber); rank A>K>Q>J>10>9>8>7>6>5>4>3>2; trump = previous round winner's longest suit (auto-selected; round-1 leader is player to dealer's left); must-follow — follow led suit if able, else any card; highest trump wins, else highest led-suit card wins; Dogbone rule — a player who wins zero tricks spends 1 Dogbone token (each player starts with 1) to survive the round; if a player has no Dogbone left and wins zero tricks they are eliminated; last player remaining wins (or the winner of the final 1-card round)
  - name: nap
    description: Nap / Napoleon (ナップ) — 4-player British trick-taking gambling game on a standard 52-card deck (A,2-10,J,Q,K × 4 suits); 5 cards each, 5 tricks per round; rank A>K>Q>J>10>9>8>7>6>5>4>3>2; bidding phase first (Pass/Two/Three/Four/Nap, order lowest-to-highest); highest bidder becomes the Declarer and plays alone vs 3 Defenders; trump = Declarer's longest suit (auto-declared); Declarer leads the first trick; must-follow rule — follow led suit if able; chip scoring — Two/Three/Four pay face value on success and face value each to Defenders on failure; Nap pays 10 on success and 5 each to Defenders on failure; first to target (default 20 chips) wins
  - name: preference
    description: Préférence (プレフェランス) — 3-player Central/Eastern European trick-taking game on a 32-card deck (7,8,9,10,J,Q,K,A × 4 suits); 10 cards each, 10 tricks per round; rank A>K>Q>J>10>9>8>7; bidding phase first (Pass/Six/Misère/Seven/Eight, order lowest-to-highest); highest bidder becomes the Declarer and plays alone vs 2 Defenders; trump = Declarer's longest suit for Six/Seven/Eight (auto-declared); Misère has no trump; must-follow rule — follow led suit if able; Declarer gains contract value (Six=6, Seven=7, Eight=8, Misère=10 pts) on success; each Defender gains that value on failure; talon exchange omitted and pool bookkeeping replaced by simple cumulative points (simplified); first to target (default 30 pts) wins
  - name: spoilfive
    description: Spoil Five / Maw (スポイル・ファイブ) — 5-player Irish traditional trick-taking game on a standard 52-card deck (A,2-10,J,Q,K × 4 suits); 5 cards each, 5 tricks per round; trump determined by turn-up card from remainder of deck; fixed top trumps in order — trump 5 (Five Fingers) > trump J > ♥A (Ace of Hearts, always top trump regardless of trump suit) > trump A > trump K > trump Q > other trumps > led-suit cards; reneging — the three top trumps are exempt from must-follow and may be held back even when trump is led; must-follow otherwise; race to 3 — first player to win 3 tricks wins the round and takes the pot (each player antes each round); spoil — if no player reaches 3 tricks the pot carries over to the next round; first to target (default 30 pts) wins; pip-order red/black rank reversal and Jink bonus omitted (simplified)
  - name: fortyfives
    description: Auction Forty-Fives (フォーティファイブズ) — 4-player 2-team Irish/Nova Scotian trick-taking game on a standard 52-card deck (A,2-10,J,Q,K × 4 suits); 5 cards each, 5 tricks per round; teams are seats 0&2 vs seats 1&3; bidding phase first (Pass/15/20/25, order lowest-to-highest); highest bidder's team becomes the bid team and their longest suit is auto-declared trump (card-exchange omitted); fixed top trumps (Spoil Five family) — trump 5 > trump J > ♥A (always top trump) > trump A > trump K > trump Q > other trumps > led-suit cards; reneging — the three top trumps are exempt from must-follow; must-follow otherwise; each trick scores 5 pts to the winning team; bid team adds its trick points if it meets the bid, else loses the bid value; non-bid team always adds trick points won; Jink — bidding 25 and winning all 5 tricks is an instant match win; first team to reach 45 pts wins; honor-card bonuses and red/black pip-order rank reversal omitted (simplified)
  - name: twentynine
    description: Twenty-Nine / 29 (トゥエンティナイン) — 4-player 2-team South Asian trick-taking game on a 32-card deck (7,8,9,10,J,Q,K,A × 4 suits); 8 cards each, 8 tricks per round; teams are seats 0&2 vs seats 1&3; bidding phase first (Pass/16/20/24/28, order lowest-to-highest); highest bidder's team becomes the bid team and their longest suit is auto-declared trump; hidden trump — trump suit is not revealed until a player cannot follow the led suit and plays an off-suit card; card rank within a suit J > 9 > A > 10 > K > Q > 8 > 7 (trump beats all non-trump); must-follow otherwise; card points J=3, 9=2, A=1, 10=1 (28 total); last trick awards +1 point (29 total contested); bid team scores 1 game point if it meets its bid, else non-bid team scores 1 game point; first team to reach 6 game points wins
  - name: courtpiece
    description: Court Piece / Rang (コートピース) — 4-player 2-team Pakistani/Iranian trick-taking game on a standard 52-card deck (2-10,J,Q,K,A × 4 suits); 13 cards each, 13 tricks per round; teams are seats 0&2 vs seats 1&3; the caller (Hakim) is dealt the first 5 cards, peeks, and immediately declares a trump suit (1=♠ 2=♣ 3=♥ 4=♦) before the remaining cards are dealt; the caller leads the first trick; card rank within a suit A > K > Q > J > 10 > … > 2 (trump beats all non-trump); must-follow otherwise; highest trump wins the trick, else highest card of the led suit; a team taking 7 or more of the 13 tricks wins the round (Sar) and scores 1 point; winning consecutive rounds or sweeping all 13 tricks earns a Court bonus (+2 points); the winning team keeps the caller role, on a loss the caller passes to the next seat; first team to reach the point limit (default 7) wins
  - name: bezique
    description: Bezique (ベジーク) — 2-player French declaration trick-taking game, the direct ancestor of Pinochle, on a 64-card deck (A,7,8,9,10,J,Q,K × 4 suits × 2 copies); 8 cards each; the next card off the stock is turned up to set the trump suit; trick rank A > 10 > K > Q > J > 9 > 8 > 7 (trump beats all non-trump); Phase 1 (while the stock lasts) — no follow requirement, the trick winner may declare one meld for points or decline, then both draw 1 card (winner first); melds — Marriage (K+Q same suit)=20, Royal Marriage (trump K+Q)=40, Bezique (♠Q+♦J)=40, four Aces=100, four Kings=80, four Queens=60, four Jacks=40; Phase 2 (after the stock is exhausted) — strict must-follow (follow led suit, win if able, else trump if void), no melds, last-trick winner +10; captured card points A=11, 10=10, K=4, Q=3, J=2, 9/8/7=0; meld points, card points, and the last-trick bonus accumulate across deals; first player to reach the target (default 1000) wins
  - name: ecarte
    description: Écarté (エカルテ) — 2-player French trick-taking game on a 32-card deck (A,7,8,9,10,J,Q,K × 4 suits); 5 cards each; the next card off the stock is turned up to set the trump suit (if it is a King the dealer scores +1); trick rank K > Q > J > A > 10 > 9 > 8 > 7 (trump beats all non-trump); Exchange phase — the elder (non-dealer) proposes an exchange or stands, and if proposed the dealer accepts or refuses; on accept both players discard any number of cards and draw replacements, then the elder again proposes or stands until the elder stands, the dealer refuses, or the stock is exhausted; if the dealer refuses and then fails to take 3 tricks the elder gains +1 (refusal penalty); Play phase — 5 tricks with strict must-follow under trump (follow the led suit, win if able, else trump if void); holding the trump King in hand scores +1 (declared automatically); scoring — 3 or 4 tricks = 1 point, sweeping all 5 (the Vole) = 2 points; match scores accumulate across deals; first player to reach the target (default 5) wins
  - name: threecardbrag
    description: Three Card Brag (スリーカード・ブラグ) — 4-player English vying (betting) game, one of the ancestors of poker, on a standard 52-card deck; each player antes into the pot and is dealt 3 cards; every player is either Blind (has not looked at their hand) or Seen (has looked), and a Blind player may see their hand at any time to become Seen; Blind players stake at half the Seen rate (implementation Blind = stake, Seen = 2 * stake); on your turn you may bet (call the current stake), raise (increase the stake), or fold (drop out, forfeiting your contribution); when only two players remain a Seen player may pay 2 * stake to call a show (showdown) — the stronger hand takes the pot and on a tie the player who called the show loses; if everyone but one player folds the last remaining player wins the pot without a showdown; hand ranking (strongest to weakest) Prial (three of a kind) > Running Flush (straight flush) > Run (straight) > Flush > Pair > High Card; special cases — 3-3-3 is the highest Prial and A-2-3 is the highest Run; a player whose chips run out is eliminated, and the last player still holding chips is the match winner
  - name: teenpatti
    description: Teen Patti (ティーンパッティ) — the Indian version of Three Card Brag, a 4-player vying (betting) game on a standard 52-card deck; each player antes the boot into the pot and is dealt 3 cards; every player is either Blind (has not looked at their hand) or Seen (has looked), and a Blind player may see their hand at any time to become Seen; Blind players stake at half the Seen rate (implementation Blind = stake, Seen = 2 * stake); on your turn you may bet (call the current stake), raise (increase the stake), or fold (drop out, forfeiting your contribution); side show (Teen Patti's distinguishing mechanic) — when three or more players remain a Seen player may request a side show against the previous Seen player, who may accept (the weaker of the two folds; on a tie the requester folds) or decline (play continues); when only two players remain a Seen player may pay 2 * stake to call a show (showdown) — the stronger hand takes the pot and on a tie the player who called the show loses; if everyone but one player folds the last remaining player wins the pot without a showdown; hand ranking (strongest to weakest) Trail/Trio (three of a kind) > Pure Sequence (straight flush) > Sequence (straight) > Color (flush) > Pair > High Card, sharing Three Card Brag's hand evaluation; a player whose chips run out is eliminated, and the last player still holding chips is the match winner
  - name: spoons
    description: Spoons (スプーン) — a 4-player American pass-and-grab speed/party game on a standard 52-card deck; each player is dealt 4 cards and one fewer spoon than the number of players is placed in the centre (3 spoons for 4 players); during the Pass phase a feeder draws from the draw pile and every player passes one card to the next player, trying to collect four of a kind; the instant any player completes four of a kind they grab a spoon and the grab window opens, and everyone races to grab one of the remaining spoons so that exactly one player is left without one; the player who misses out gains a letter in the sequence S-P-O-O-N-S, and collecting all six letters eliminates that player (removing one spoon); the last player standing wins; CPU difficulties Easy (slow, often misses the grab), Normal (standard), Hard (fast, rarely misses); commands pass (p), grab (g), next (n), setdifficulty (sd), log (l), reset (r); phases Pass (0), Grab (1), RoundEnd (2), GameEnd (3)
  - name: kemps
    description: Kemps (ケムプス) — a 4-player, 2-team matching game on a standard 52-card deck built around secret signals; each player holds 4 cards and four cards are placed face-up on a shared field; during the Exchange phase a player swaps one hand card for a field card (or passes), racing to collect four of a kind; the human sets a secret signal type (0 Sound, 1 Blink); when a team completes four of a kind the Declare window opens and the team may declare Kemps for a point, while the opposing team may call Counter-Kemps against a seat to steal it (plus 1 on a correct read, minus 1 if wrong); teams are even seats (Team A, with the human at seat 0) versus odd seats (Team B), and the human partner is seat 2; the first team to the target score (default 5) wins; CPU difficulties Easy (slow, rarely reads signals), Normal (standard), Hard (fast, often reads signals); commands swap (s), pass (p), signal (sig), kemps (k), counter (c), next (n), setdifficulty (sd), log (l), reset (r); phases Exchange (0), Declare (1), RoundEnd (2), GameEnd (3)
  - name: cuckoo
    description: Cuckoo (カッコー) — a 4-player life-survival game (also known as Chase the Ace or Ranter-Go-Round) on a standard 52-card deck; each player holds exactly one card and starts with 3 lives (default); during the Turn phase a player either keeps their card or swaps it with a neighbour, while the dealer swaps with the stock instead of a neighbour; during the Refuse phase a player holding a King may refuse an incoming swap by revealing the King, blocking the exchange; after every player has acted the holder(s) of the lowest card lose a life (ties mean all tied holders lose one); a player reduced to 0 lives is eliminated and the last player standing wins; CPU difficulties Easy, Normal, Hard; commands keep (k), swap (s), refuse (rf), accept (ac), nextround (nr), setdifficulty (sd), log (l), reset (r); phases Turn (0), Refuse (1), RoundEnd (2), GameEnd (3); config cpuDifficulty (0 Easy, 1 Normal, 2 Hard) and initialLives (default 3)
  - name: pishti
    description: Pişti (ピシュティ) — a Turkish 2-4 player capture (fishing) game on a standard 52-card deck; each player is dealt 4 cards and plays one onto a central pile in turn; playing a card that matches the rank of the pile's top card, or playing a Jack (wild), captures the whole pile; capturing a lone card scores a Pişti (10 points; 20 for a Jack played onto a single Jack); hands are re-dealt 4 at a time until the stock runs out; final scoring most cards 3, each Ace 1, 2 of clubs 2, 10 of diamonds 3, each Jack 1, plus Pişti bonuses, and the highest total wins; CPU difficulties Easy, Normal, Hard; commands play (p, with handIndex), next (n), setdifficulty (sd), setplayers (sp), log (l), reset (r); phases play, roundEnd, gameEnd; config playerCnt (2-4, default 4) and cpuDifficulty (0 Easy, 1 Normal, 2 Hard)
  - name: cuarenta
    description: Cuarenta (クアレンタ) — a 4-player, 2-team Ecuadorian capture (fishing) game on a 40-card Spanish-style deck (the 8s, 9s and 10s are removed); teams are seats 0 and 2 (Team A) versus seats 1 and 3 (Team B); each player is dealt 5 cards and 4 cards are placed on the central table; on your turn you play one card, and if it matches the rank of any table card you capture all same-rank table cards (and may chain via ronda when 3 or more of a rank appear), otherwise the card is laid on the table; bonuses (all additive) are caída plus 2 (immediately capturing the very card the previous player just laid), limpia plus 1 (clearing the entire table), and ronda plus 1 per extra card beyond 2 (capturing a run of 3 or more of a rank); hands are re-dealt 5 at a time until the stock runs out, then the team that captured more than 20 of the 40 cards (the majority) gets a plus 6 most cards bonus; the first team to reach the target score (default 40) wins; CPU difficulties Easy, Normal, Hard; commands play (p, with handIndex), next (n), setdifficulty (sd), log (l), reset (r); phases play, roundEnd, gameEnd; config targetScore (default 40) and cpuDifficulty (0 Easy, 1 Normal, 2 Hard)
  - name: faro
    description: Faro (ファロ) — a 19th-century American single-player-vs-bank banking game on a standard 52-card deck with a 13-rank layout (A is 1 to K is 13); in the Betting phase the player places chips on ranks and a copper bet wagers a rank to lose rather than win; the bank burns the soda (first card) and deals turns of two cards, where the first (losing) card rank is collected by the bank and the second (winning) card rank is paid 1 to 1; when both cards share a rank (a split) the bank takes half of that rank stake; the final three cards may be called by predicting their order, paying 4 to 1 for a correct call; play continues until the player runs out of chips; commands bet (b, with rank, amount and copper), clearBet (cb, with rank), clearAll (ca), deal (d), call (with order), next (n), log (l), reset (r); phases 1 Betting, 2 Turn, 3 Call, 4 RoundEnd, 5 GameEnd
  - name: openfacechinese
    description: Open Face Chinese Poker / OFC (オープンフェイス・チャイニーズポーカー) — a solo-vs-CPU 2 to 4 player game on a standard 52-card deck blending solitaire and poker; cards are dealt one at a time and each must be committed immediately to one of three rows, Top (front, 3 cards), Middle (5 cards) or Bottom (back, 5 cards), and once placed a card cannot be moved; the three rows must rank in non-decreasing strength from Top to Bottom or the hand is fouled and scores nothing for the round; correctly placed strong rows earn royalty bonuses and a premium Top row (a pair of Queens or better) qualifies for Fantasyland; each round the rows are compared row by row against the other players, scoring points for every row won plus royalties, and the highest cumulative total after the target number of rounds wins; commands place (p, with row where 0 is front, 1 is middle, 2 is back; aliases front/f, middle/m, back/b), nextround (nr), setdifficulty (sd), setplayers (sp), log (l), reset (r); phases 0 Placing, 1 RoundEnd, 2 GameEnd
  - name: russianbank
    description: Russian Bank / Crapette (ロシアンバンク/クラペット) — a 2-player competitive solitaire on two 52-card decks (104 cards) played 1 human vs 1 CPU; each player owns a 13-card reserve (stock) and a hand (talon), and the board has 8 shared foundations (built up A to K by suit) plus 4 shared tableau columns (built down in alternating colour; an empty column accepts any card); on your turn you make as many legal moves as you like from your reserve or waste tops, a tableau column top, or the opponent's reserve or waste tops onto the shared foundations and tableau, and when out of moves you discard one hand card to your waste to end the turn; if the CPU ends its turn leaving a forced foundation move you may call stop to catch the lapse; the first player to empty their reserve wins (if both are stuck the player with fewer reserve cards wins); commands pf with source to a foundation, mt with source and column to a tableau, d discard, s stop, u undo, sd setdifficulty, hint, log (l), reset (r), where source is r own reserve, w own waste, or opponent reserve, ow opponent waste, t0 to t3 tableau columns; phases 0 Idle, 1 Playing, 2 GameEnd
  - name: labellelucie
    description: La Belle Lucie (ラ・ベル・ルーシー) — a classic French single-player fan solitaire on a standard 52-card deck; the cards are dealt into 17 fans of three plus one single card, and only the top card of each fan can move; a fan top stacks onto another fan whose top is the next-higher card of the same suit (built down in suit), and an exposed Ace starts one of the 4 foundations which then build up in suit from Ace to King; when no legal move remains the player may gather all non-foundation cards, shuffle and redeal (up to 3 redeals); clearing all 52 cards to the foundations wins, while being stuck after the third redeal loses; commands mf with from and to fan indices, ff with from fan index to a foundation, rd redeal, ac autocomplete, u undo, g giveup, hint, log (l), reset (r); phases 0 Playing, 1 GameClear, 2 GameOver
  - name: simplesimon
    description: Simple Simon (シンプル・サイモン) — an easier Spider-family single-player solitaire on a standard 52-card deck; all 52 cards are dealt face-up into 10 columns (sizes 8,8,8,7,6,5,4,3,2,1) with no stock and no free cells; a column top, or a same-suit descending run, can be moved, and a card may be placed on any card one rank higher of any suit (only same-suit runs move as a group) while an empty column accepts anything; a complete King-to-Ace same-suit run is removed automatically and counts as a finished suit; clearing all four suits wins and running out of legal moves loses; commands m with fromCol, cardIndex and toCol, u undo, undo_n, g giveup, hint, log (l), reset (r); phases 0 Playing, 1 GameClear, 2 GameOver
  - name: doubleklondike
    description: Double Klondike (ダブル・クロンダイク) — a two-deck (104-card) Klondike single-player solitaire; cards are dealt into 9 tableau columns (1..9 cards, only the top face-up) with the rest forming a three-card-draw stock, plus 8 foundations (two per suit); the tableau builds down in alternating colours (an empty column accepts only a King) and foundations build up by suit from Ace to King; the stock turns over three cards at a time and recycles the waste when empty, and moving a tableau top auto-flips the newly exposed card; moving all 104 cards to the foundations wins while running out of legal moves loses; commands d draw, mwt with col (waste to tableau), mwf (waste to foundation), mtt with fromCol, cardIndex and toCol (tableau to tableau), mtf with col (tableau to foundation), ac autocomplete, u undo, undo_n, g giveup, hint, log (l), reset (r); phases 0 Playing, 1 GameClear, 2 GameOver
  - name: blackhole
    description: Black Hole (ブラックホール) — a single-player logic-puzzle patience by David Parlett on a standard 52-card deck; the Ace of Spades seeds a single central foundation (the black hole) and the other 51 cards are dealt face-up into 17 fans of three; only a fan top can move, and a card may be played onto the black hole when its rank is one away from the current top (any suit, no King-Ace wrap); cards never leave the black hole, absorbing all 52 cards wins and running out of legal moves loses; commands m with fan, u undo, undo_n, g giveup, hint, log (l), reset (r); phases 0 Playing, 1 GameClear, 2 GameOver
  - name: beggarmyneighbour
    description: Beggar-My-Neighbour (ビガー・マイ・ネイバー) — a two-player (human vs CPU) war-style card game played with a standard 52-card deck split evenly 26-26 face-down; players alternate turning top cards onto a central pile; penalty cards (J=1, Q=2, K=3, A=4) force the opponent to pay that many cards; if a penalty card appears during payment the obligation flips; if the count is exhausted with no penalty card the last penalty player collects the entire central pile; collecting all 52 cards wins; command step (s) advances one action, autoplay runs to completion or round cap; phases 0 Play, 1 PayPenalty, 2 Collect, 3 GameEnd
  - name: allfours
    description: All Fours / Seven Up (オールフォーズ/セブンアップ) — a two-player (human elder hand vs CPU dealer) trick-taking game on a standard 52-card deck; each deal gives 6 cards each and flips a turn-up whose suit is the provisional trump; the non-dealer stands or begs, and on a beg the dealer gifts 1 point or runs the cards (3 more each plus a fresh trump, re-dealing if the deck is exhausted); must-follow trick play (trump always legal) scores High, Low, Jack and Game per deal; first to PointLimit (default 7) wins, with a Gift→High→Low→Jack→Game peg-out order breaking same-deal ties; commands stand (st), beg (bg), gift (g), run, play (p), next (n), nextround (nr); phases 0 Beg, 1 Gift, 2 Play, 3 TrickEnd, 4 RoundEnd, 5 GameEnd
  - name: prsi
    description: Prší (プルシー / Czech Crazy Eights / Mau Mau) — a Czech shedding game for 4 players (1 human + 3 CPU) on a 32-card pack (7,8,9,10,J,Q,K,A × 4 suits); each player is dealt 5 cards and one card starts the discard pile; play a card matching the discard top by suit or rank, otherwise draw 1 and end your turn; action cards — 7 forces the next player to draw 2 and 7s stack (a player facing a pending 7-penalty may stack another 7 or draw the accumulated count), Ace skips the next player (skips stack), and Under (Jack) also skips the next player; the first player to empty their hand wins immediately (no rounds or scoring); commands play (p) with cardIndex, draw (d), setdifficulty (sd), log (l), reset (r), quit (q); phases 0 Play, 1 GameEnd
  - name: calabresella
    description: Calabresella (カラブレセッラ / Terziglio) — an Italian (Calabrian) no-trump bidding trick-taker in the Tressette family for 3 players (1 human + 2 CPU) where a single soloist plays against a 2-player coalition, on a 40-card Italian pack (A,2,3,4,5,6,7,J,Q,K × 4 suits — 8/9/10 removed; same deck as Tressette/Briscola); each player is dealt 12 cards and the remaining 4 form the face-down widow (monte); bidding — each player in turn passes, declares chiamo (call, stake 1), or solo (stake 2), and the highest declaration becomes the soloist (if all pass, the forehand takes chiamo); the soloist then picks up the 4-card monte (16 cards) and discards 4 back down to 12; no trump — must-follow the led suit if able, otherwise play anything; trick rank 3>2>A>K>Q>J>7>6>5>4; card points in thirds A=3, 2/3/J/Q/K=1, others 0, winning the last trick (ultima) +1, total 33 thirds = 11 points per deal; the soloist must take more than half (≥18 of 33 thirds) to win the round and stake-based points transfer between the soloist and the coalition; first player to reach the target score (default 21) wins the match; commands reset (r), bid (b pass|chiamo|solo), discard (d), play (p), next (n), nextround (nr), setdifficulty (sd), hint (h), log (l); phases 0 Bid, 1 Monte, 2 Play, 3 TrickEnd, 4 RoundEnd, 5 GameEnd
  - name: king
    description: King (キング) — a Greek/Brazilian trick-avoidance compendium game for 4 players (1 human + 3 CPU, each scoring individually) on a standard 52-card pack; the match runs for exactly 7 deals, and each deal the dealer (親) chooses one of the 7 contracts not yet played this match, then a full round of 13 must-follow tricks is played and scored per the chosen contract; contracts 0 No Tricks (−2 per trick taken), 1 No Hearts (−2 per heart taken), 2 No Queens (−6 per queen taken), 3 No King of Hearts (−20 for capturing K♥), 4 No Last Two Tricks (−10 for each of the last two tricks taken), 5 No Men (−3 per Jack or King taken), 6 King/Trump — the dealer also chooses a trump suit and each trick taken scores +5 (the only positive contract); the six negative contracts are played no-trump; must follow the led suit if able, otherwise play anything (no overtrump obligation); trick strength A>K>Q>J>10>9>…>2 (trump beats non-trump only in the King contract); after all 7 contracts have each been played once the player with the highest total (least penalty) wins; commands reset (r), contract (c 0-6 [trump 1-4]), play (p), next (n), setdifficulty (sd), hint (h), log (l); phases 0 SelectContract, 1 Play, 2 TrickEnd, 3 RoundEnd, 4 GameEnd
  - name: cinch
    description: Cinch (チンチ / Double Pedro / High Five) — a 4-player (1 human + 3 CPU, each scoring individually) All-Fours/Pitch-family bidding trick-taker on a standard 52-card pack; each player is dealt 9 cards and bids in turn either 0 (pass) or 1-14, and the high bidder names trump and leads; a deal is worth 14 points — High (A of trump)=1, King of trump=1, Ten of trump ("Game")=1, Jack of trump=1, Right Pedro (5 of trump)=5, and Left Pedro (the 5 of the same color as trump)=5, where the Left Pedro ranks just below the trump 5 and counts as trump for following and winning; nine tricks are played per deal and must-follow the led suit if able, otherwise play anything; the bidder side must make its bid (capture at least the bid amount in points) or is set back (loses the bid amount), while non-bidders score the points they capture; the first player to reach the target score (default 21) wins; commands reset (r), bid (b 0-14), trump (t 1-4), play (p), next (n), nextround (nr), setdifficulty (sd), hint (h), log (l); phases 0 Bid, 1 NameTrump, 2 Play, 3 TrickEnd, 4 RoundEnd, 5 GameEnd
  - name: loo
    description: Loo (ルー / Lanterloo) — a classic English pot/gambling trick-taker (Five-card Loo) for 4 players (1 human + 3 CPU, each holding an individual chip stack) on a standard 52-card pack; each deal every player antes (default 3) into a carried-over pot, then a face-up turn-up card sets the trump suit; each player looks at their 5 cards and decides to play or pass; players who play compete over 5 tricks with must-follow-and-head (follow suit if able and, if you can beat the current best card, you must; when void of the led suit you must trump if you hold one, again heading if possible); each trick captured wins 1/5 of the pot, and a player who plays but takes no trick is "looed" and pays a penalty (the deal's starting pot) into the next deal's pot; if only one player plays they take the whole pot uncontested, and if nobody plays the pot carries over; chips accumulate over repeated deals (no target-score race — use nextround to settle and start the next deal); commands reset (r), decide (d 0-1 / play / pass), play (p), next (n), nextround (nr), setdifficulty (sd), hint (h), log (l); phases 0 Decide, 1 Play, 2 TrickEnd, 3 RoundEnd
  - name: basra
    description: Basra (バスラ / Bastra) — an Egyptian/Levantine fishing (capture) card game for 4 players (1 human + 3 CPU, each scoring individually) on a standard 52-card pack; each player is dealt 4 cards and 4 cards are placed face-up on the table; on your turn you play one hand card — a number card (A=1..10) captures table cards of the same rank and/or any table subset whose values sum to the played card's value (multiple groups at once), Q/K capture only by matching rank, and a Jack sweeps all table cards except other Jacks; clearing the whole table with a single non-Jack card scores a "Basra" (+10 bonus), while a Jack sweep is not a Basra, and if nothing is captured the card is left on the table (trail); when all hands are empty fresh 4-card hands are dealt (no new table cards) until the stock is exhausted, and leftover table cards go to the last player who captured; scoring at game end — most captured cards (unique) +3, 7♦ +3, 10♦ +2, each Ace +1, each Basra +10 — highest total wins (ties = multiple winners), with no target-score race (nextround starts a fresh game); commands reset (r), play (p h [t...]), next (n), nextround (nr), setdifficulty (sd), hint (h), log (l); phases 0 Play, 1 GameEnd
  - name: tablanet
    description: Tablanet (タブラネット / Tablić) — a Balkan (Serbian/Macedonian) fishing (capture) card game for 4 players (1 human + 3 CPU, each scoring individually) on a standard 52-card pack; each player is dealt 4 cards and 4 cards are placed face-up on the table; on your turn you play one hand card — a number card (A=1..10) captures table cards of the same rank and/or any table subset whose values sum to the played card's value (multiple groups at once), Q/K capture only by matching rank, and a Jack sweeps all table cards except other Jacks; clearing the whole table with a single non-Jack card scores a "Tabla" bonus, while a Jack sweep is not a Tabla, and if nothing is captured the card is left on the table (trail); when all hands are empty fresh 4-card hands are dealt (no new table cards) until the stock is exhausted, and leftover table cards go to the last player who captured; scoring at game end — most captured cards (unique) +3, 10♦ +2, each Ace +1, each Jack +1, 2♣ +1, each Tabla +1 — highest total wins (ties = multiple winners), with no target-score race (nextround starts a fresh game); commands reset (r), play (p h [t...]), next (n), nextround (nr), setdifficulty (sd), hint (h), log (l); phases 0 Play, 1 GameEnd
  - name: trenteetquarante
    description: Trente et Quarante (トラント・エ・カラント / Rouge et Noir) — a classic Monte-Carlo banking game for 1 player vs the bank on a standard 52-card pack with no player card decisions; the player picks one of four even-money bets — Noir (0), Rouge (1), Couleur (2), Inverse (3) — plus a stake, then the dealer deals two rows (Noir/black first, then Rouge/red), summing each (face cards = 10, Ace = 1) until the total reaches 31 or more; the row with the lower total wins; Noir/Rouge bet on which row wins, while Couleur/Inverse bet on whether the color of the first card dealt matches the winning color; winning bets pay 1:1, and a tie at exactly 31 in both rows is a "Refait" that costs the player half the stake; chips carry over across rounds (reset deals the next round); commands reset (r), bet (b amount noir|rouge|couleur|inverse), log (l); phases 1 Bet, 2 Dealt, 3 End
  - name: agnes
    description: Agnes Sorel (アグネス・ソレル) game
  - name: anaconda
    description: Anaconda (アナコンダ) game
  - name: bouillotte
    description: Bouillotte (ブイヨット) game
  - name: carioca
    description: Carioca (カリオカ) game
  - name: cego
    description: Cego (チェゴ) game
  - name: flowergarden
    description: Flower Garden (フラワーガーデン) game
  - name: fortyandeight
    description: Forty and Eight (フォーティ・アンド・エイト) game
  - name: frenchtarot
    description: French Tarot (フレンチタロット) game
  - name: gaigel
    description: Gaigel (ガイゲル) game
  - name: gostop
    description: Go-Stop (ゴーストップ) game
  - name: guts
    description: Guts (ガッツ) game
  - name: hachihachi
    description: Hachi-Hachi (八八) game
  - name: indianrummy
    description: Indian Rummy (インドラミー) game
  - name: jass
    description: Jass (ヤス／シーバー) game
  - name: kingalbert
    description: King Albert (キング・アルバート) game
  - name: koenigrufen
    description: Königrufen (ケーニッヒルーフェン) game
  - name: koikoi
    description: Koi-Koi (こいこい) game
  - name: machiavelli
    description: Machiavelli (マキャヴェッリ) game
  - name: michigan
    description: Michigan (ミシガン) game
  - name: oichokabu
    description: Oicho-Kabu (おいちょかぶ) game
  - name: ombre
    description: Ombre / Hombre (オンブル) game
  - name: pan
    description: Panguingue / Pan (パングインゲ) game
  - name: primero
    description: Primero (プリメロ) game
  - name: rook
    description: Rook (ルーク) game
  - name: samba
    description: Samba (サンバ) game
  - name: scarto
    description: Scarto (スカルト) game
  - name: streetsandalleys
    description: Streets and Alleys (ストリート・アンド・アレイズ) game
  - name: sultan
    description: Sultan (スルタン) game
  - name: tysiac
    description: Thousand / Tysiąc (トゥシオンツ) game
  - name: ulti
    description: Ulti / Ultimó (ウルティ) game
  - name: watten
    description: Watten (ヴァッテン) game
  - name: wizard
    description: Wizard (ウィザード) game
  - name: zheng
    description: Zheng Shangyou (争上游) game
  - name: aluette
    description: Aluette (アリュエット) game
  - name: ganjifa
    description: Ganjifa (ガンジファ) game
  - name: minchiate
    description: Minchiate (ミンキアーテ) game
  - name: tarocchini
    description: Tarocchini (タロッキーニ) game
  - name: vira
    description: Vira (ヴィーラ) game

paths:
  /blackjack/exec:
    post:
      tags:
        - blackjack
      summary: Execute a BlackJack game command
      description: |
        Send a command to advance the BlackJack game session identified by
        `sessionId`.

        The game proceeds through five phases:
        1. **Bet** (`phase=1`) — Player places an initial bet
        2. **Deal** (`phase=2`) — Two cards dealt to each; check if dealer shows Ace
        3. **Insurance** (`phase=3`) — Player accepts/declines insurance (only if dealer shows Ace)
        4. **Action** (`phase=4`) — Hit / Stand / Double Down / Split / Surrender
        5. **End** (`phase=5`) — Dealer plays, judgment, payout
        6. **Early Surrender** (`phase=6`) — Player decides whether to surrender before dealer BJ check (only when surrender rule = Early)

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `bet`   | `b`     | Place initial bet (requires `amount`) |
        | `hit`   | `h`     | Draw an additional card |
        | `stand` | `s`     | End the player turn; trigger dealer play and judgment |
        | `doubledown` | `d` | Double bet, draw one card, auto-stand |
        | `split` | `sp`    | Split a pair into two hands |
        | `insurance` | `i` | Accept insurance (costs half of original bet) |
        | `declineinsurance` | `di` | Decline insurance |
        | `surrender` | `sur` | Surrender first 2 cards and receive half bet back |
        | `togglehint` | — | Toggle basic strategy hint on/off |
        | `setdeckcount` | `sd` | Set shoe deck count (requires `amount`: 1/2/4/6/8) |
        | `earlysurrender` | `es` | Surrender before dealer BJ check (early surrender phase only) |
        | `declineearlysurrender` | `des` | Decline early surrender and continue normal play |
        | `setsurrenderrule` | `ssr` | Set surrender rule (requires `amount`: 0=Late/1=Early/2=No surrender) |
        | `togglesoft17` | `soft17` | Toggle dealer soft-17 rule (H17 ↔ S17) |
        | `togglecounting` | `counting` | Toggle card counting display on/off |
        | `toggledas` | `das` | Toggle double-after-split (DAS) on/off |
        | `setcountingsystem` | `scs` | Set counting system (requires `amount`: 0=Hi-Lo/1=KO/2=Zen Count/3=Omega II) |
        | `quit`  | `q`     | End the session |
      operationId: blackjackExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BlackJackRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Place a bet
                value:
                  command: bet
                  amount: 100
                  sessionId: my-session-001
              hit:
                summary: Draw a card
                value:
                  command: hit
                  sessionId: my-session-001
              stand:
                summary: Stand and end turn
                value:
                  command: stand
                  sessionId: my-session-001
              doubledown:
                summary: Double down
                value:
                  command: doubledown
                  sessionId: my-session-001
              split:
                summary: Split a pair
                value:
                  command: split
                  sessionId: my-session-001
              insurance:
                summary: Accept insurance
                value:
                  command: insurance
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BlackJackResponse'
              examples:
                inProgress:
                  summary: Game in progress (dealer hole card is hidden)
                  value:
                    dealer:
                      score: 0
                      cards:
                        - design: SPADE
                          value: 10
                      chips: 1000
                    player:
                      chips: 900
                    hands:
                      - score: 18
                        cards:
                          - design: CLOVER
                            value: 8
                          - design: DIAMOND
                            value: 10
                        bet: 100
                        stood: false
                        doubled: false
                        busted: false
                        isBlackJack: false
                        canSplit: false
                    phase: 4
                    currentHandIdx: 0
                    insuranceBet: 0
                    insuranceAvailable: false
                    message: ""
                playerWins:
                  summary: Game over — player wins (all dealer cards revealed)
                  value:
                    dealer:
                      score: 22
                      cards:
                        - design: SPADE
                          value: 10
                        - design: HEART
                          value: 5
                        - design: CLOVER
                          value: 7
                      chips: 900
                    player:
                      chips: 1200
                    hands:
                      - score: 20
                        cards:
                          - design: DIAMOND
                            value: 10
                          - design: HEART
                            value: 10
                        bet: 100
                        stood: true
                        doubled: false
                        busted: false
                        isBlackJack: false
                        canSplit: false
                    phase: 5
                    currentHandIdx: 0
                    insuranceBet: 0
                    insuranceAvailable: false
                    message: You are the winner.
                quit:
                  summary: Quit command response
                  value:
                    dealer:
                      chips: 0
                    player:
                      chips: 0
                    phase: 1
                    currentHandIdx: 0
                    insuranceBet: 0
                    insuranceAvailable: false
                    message: bye.
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BlackJackResponse'
              examples:
                paramError:
                  summary: Missing required parameter
                  value:
                    dealer:
                      chips: 0
                    player:
                      chips: 0
                    phase: 1
                    currentHandIdx: 0
                    insuranceBet: 0
                    insuranceAvailable: false
                    message: param error.

  /poker/exec:
    post:
      tags:
        - poker
      summary: Execute a 5-card Draw Poker game command
      description: |
        Send a command to advance the Poker game session identified by
        `sessionId`.  The game supports 1 human + 1-3 CPU players.

        The game proceeds through five phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Init       | `0`   | Before any cards are dealt |
        | Deal       | `1`   | Cards dealt; first betting round |
        | Exchange   | `2`   | Player may exchange cards |
        | SecondBet  | `3`   | Second betting round (after exchange) |
        | End        | `4`   | Showdown; final hands revealed |

        | Command    | Aliases | Description |
        |------------|---------|-------------|
        | `reset`    | `r`     | Start / restart a game |
        | `bet`      | `b`     | Place a bet (use `amount`) |
        | `call`     | `c`     | Match the current bet |
        | `raise`    | `ra`    | Raise (use `amount`) |
        | `check`    | `ck`    | Pass without betting (only when no outstanding bet) |
        | `fold`     | `f`     | Forfeit the pot |
        | `allin`    | `a`     | Go all-in (bet all remaining chips) |
        | `exchange` | `e`     | Exchange selected cards (use `indices` to specify which) |
        | `stand`    | `s`     | Keep current hand (no exchange) |
        | `odds`     | `o`     | Calculate draw odds for selected cards (read-only, exchange phase only) |
        | `quit`     | `q`     | End the session |
      operationId: pokerExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PokerRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              resetWithConfig:
                summary: Start a new game with 2 CPUs and 1 joker
                value:
                  command: reset
                  cpuCount: 2
                  jokerCount: 1
                  sessionId: my-session-001
              bet:
                summary: Place a bet of 20
                value:
                  command: bet
                  amount: 20
                  sessionId: my-session-001
              call:
                summary: Call the current bet
                value:
                  command: call
                  sessionId: my-session-001
              allin:
                summary: Go all-in
                value:
                  command: allin
                  sessionId: my-session-001
              exchange:
                summary: Exchange cards at positions 0 and 2
                value:
                  command: exchange
                  indices: [0, 2]
                  sessionId: my-session-001
              stand:
                summary: Stand (no exchange)
                value:
                  command: stand
                  sessionId: my-session-001
              odds:
                summary: Calculate draw odds for cards at positions 0 and 2
                value:
                  command: odds
                  indices: [0, 2]
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PokerResponse'
              examples:
                dealPhase:
                  summary: Cards dealt — first betting round (phase 1)
                  value:
                    players:
                      - id: 0
                        isHuman: true
                        cards:
                          - design: SPADE
                            value: 5
                          - design: HEART
                            value: 5
                          - design: CLOVER
                            value: 9
                          - design: DIAMOND
                            value: 9
                          - design: SPADE
                            value: 2
                        chips: 990
                        currentBet: 0
                        folded: false
                        allIn: false
                        handRank: 2
                        handName: Two Pair
                        exchangeCount: -1
                        playStyleName: ""
                      - id: 1
                        isHuman: false
                        cards: []
                        chips: 990
                        currentBet: 0
                        folded: false
                        allIn: false
                        handRank: 0
                        handName: ""
                        exchangeCount: -1
                        playStyleName: Balanced
                    pot: 20
                    sidePots: []
                    dealerIdx: 0
                    currentTurn: 0
                    phase: 1
                    gameEndFlag: false
                    lastBet: 0
                    minRaise: 10
                    ante: 10
                    jokerCount: 0
                    roundResults: []
                    cpuActions: []
                    cpuExchanges: []
                    message: ""
                endPhase:
                  summary: Showdown (phase 4)
                  value:
                    players:
                      - id: 0
                        isHuman: true
                        cards:
                          - design: SPADE
                            value: 5
                          - design: HEART
                            value: 5
                          - design: CLOVER
                            value: 9
                          - design: DIAMOND
                            value: 9
                          - design: SPADE
                            value: 2
                        chips: 1040
                        currentBet: 0
                        folded: false
                        allIn: false
                        handRank: 2
                        handName: Two Pair
                        exchangeCount: 0
                        playStyleName: ""
                      - id: 1
                        isHuman: false
                        cards:
                          - design: SPADE
                            value: 3
                          - design: HEART
                            value: 3
                          - design: CLOVER
                            value: 7
                          - design: DIAMOND
                            value: 10
                          - design: SPADE
                            value: 1
                        chips: 960
                        currentBet: 0
                        folded: false
                        allIn: false
                        handRank: 1
                        handName: One Pair
                        exchangeCount: 3
                        playStyleName: Balanced
                    pot: 0
                    sidePots: []
                    dealerIdx: 0
                    currentTurn: 0
                    phase: 4
                    gameEndFlag: false
                    lastBet: 0
                    minRaise: 10
                    ante: 10
                    jokerCount: 0
                    roundResults:
                      - playerIdx: 0
                        handRank: 2
                        handName: Two Pair
                        wonAmount: 40
                    cpuActions: []
                    cpuExchanges: []
                    message: You are the winner.
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PokerResponse'
              examples:
                paramError:
                  value:
                    players:
                      - id: 0
                        isHuman: true
                        cards: []
                        chips: 0
                        currentBet: 0
                        folded: false
                        allIn: false
                        handRank: 0
                        handName: ""
                        exchangeCount: -1
                        playStyleName: ""
                    pot: 0
                    sidePots: []
                    dealerIdx: 0
                    currentTurn: 0
                    phase: 0
                    gameEndFlag: false
                    lastBet: 0
                    minRaise: 0
                    ante: 0
                    jokerCount: 0
                    roundResults: []
                    cpuActions: []
                    cpuExchanges: []
                    message: param error.

  /oldmaid/exec:
    post:
      tags:
        - oldmaid
      summary: Execute an Old Maid (Babanuki) game command
      description: |
        Send a command to advance the Old Maid game session identified by
        `sessionId`.  The game has 4 players (player 0 = human, players 1–3 =
        CPU).

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `draw`  | `d`     | Draw a card from the next target player; use `drawIdx` to specify which card slot to draw from (omit for random) |
        | `quit`  | `q`     | End the session |
      operationId: oldmaidExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OldMaidRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              draw:
                summary: Draw the card at index 1 from the target player
                value:
                  command: draw
                  drawIdx: 1
                  sessionId: my-session-001
              drawRandom:
                summary: Draw a random card from the target player
                value:
                  command: draw
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OldMaidResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OldMaidResponse'
              examples:
                paramError:
                  value:
                    players: []
                    currentTurn: 0
                    nextDrawTargetIdx: 0
                    gameEndFlag: false
                    loserIdx: -1
                    lastDrawPlayerIdx: 0
                    lastDrawFromIdx: 0
                    lastDrawCard: null
                    lastDiscardedPairs: 0
                    hasDrawn: false
                    cpuActions: []
                    message: param error.

  /daifugo/exec:
    post:
      tags:
        - daifugo
      summary: Execute a Daifugo (大富豪) game command
      description: |
        Send a command to advance the Daifugo game session identified by
        `sessionId`.  The game has 4 players (player 0 = human, players 1–3 =
        CPU).

        Final player ranks: 1 = 大富豪, 2 = 富豪, 3 = 平民, 4 = 大貧民.

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `play`  | `p`     | Play cards specified by `indices`; pass with an empty array |
        | `sort`  |         | Sort human player's hand (optional `sortMode` param) |
        | `quit`  | `q`     | End the session |
      operationId: daifugoExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DaifugoRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play cards at hand positions 0 and 2
                value:
                  command: play
                  indices: [0, 2]
                  sessionId: my-session-001
              pass:
                summary: Pass (empty indices)
                value:
                  command: play
                  indices: []
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DaifugoResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DaifugoResponse'
              examples:
                paramError:
                  value:
                    players: []
                    currentTurn: 0
                    tableCards: []
                    lastPlayPlayerIdx: 0
                    gameEndFlag: false
                    cpuActions: []
                    humanAction: null
                    message: param error.

  /bigtwo/exec:
    post:
      tags:
        - bigtwo
      summary: Execute a Big Two (大老二) game command
      description: |
        Send a command to advance the Big Two game session identified by
        `sessionId`.  The game has 4 players (player 0 = human, players 1–3 =
        CPU).

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `play`  | `p`     | Play cards specified by `indices`; pass with an empty array |
      operationId: bigtwoExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BigTwoRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play cards at indices 0 and 1
                value:
                  command: play
                  indices: [0, 1]
                  sessionId: my-session-001
              pass:
                summary: Pass (empty indices)
                value:
                  command: play
                  indices: []
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BigTwoResponse'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BigTwoResponse'

  /tienlen/exec:
    post:
      tags:
        - tienlen
      summary: Execute a Tien Len (ティエンレン) game command
      description: |
        Send a command to advance the Tien Len game session identified by
        `sessionId`.  The game has 4 players (player 0 = human, players 1–3 =
        CPU). Card strength is `3 < 4 < … < A < 2`, ties broken by suit
        `♠ < ♣ < ♦ < ♥`. The holder of ♠3 leads first and must include it.
        Chops (three consecutive pairs / four of a kind) can cut a lone 2.

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `play`  | `p`     | Play cards specified by `indices`; pass with an empty array |
      operationId: tienlenExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TienLenRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play cards at indices 0 and 1
                value:
                  command: play
                  indices: [0, 1]
                  sessionId: my-session-001
              pass:
                summary: Pass (empty indices)
                value:
                  command: play
                  indices: []
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TienLenResponse'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TienLenResponse'

  /sevens/exec:
    post:
      tags:
        - sevens
      summary: Execute a Sevens (7並べ) game command
      description: |
        Send a command to advance the Sevens game session identified by
        `sessionId`.  The game has 4 players (player 0 = human, players 1–3 =
        CPU).

        Each suit builds outward from 7.  The board state is represented by
        `tableMinVals` and `tableMaxVals` — arrays of 5 integers indexed by suit
        (0 = unused, 1 = SPADE, 2 = CLOVER, 3 = HEART, 4 = DIAMOND).

        Players have a configurable number of passes (`maxPasses`, default 5, 0 = unlimited).

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `play`  | `p`     | Play the card at hand position `index`; pass with `index: -1` |
        | `quit`  | `q`     | End the session |
      operationId: sevensExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SevensRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              playCard:
                summary: Play the card at hand index 3
                value:
                  command: play
                  index: 3
                  sessionId: my-session-001
              pass:
                summary: Pass (index -1)
                value:
                  command: play
                  index: -1
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SevensResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SevensResponse'
              examples:
                paramError:
                  value:
                    players: []
                    currentTurn: 0
                    tableMinVals: [0, 0, 0, 0, 0]
                    tableMaxVals: [0, 0, 0, 0, 0]
                    gameEndFlag: false
                    cpuActions: []
                    humanAction: null
                    message: param error.

  /doubt/exec:
    post:
      tags:
        - doubt
      summary: Execute a Doubt (ダウト) game command
      description: |
        Send a command to advance the Doubt game session identified by
        `sessionId`.  The game has 4 players (player 0 = human, players 1–3 =
        CPU).

        Each turn, the active player places cards face-down and declares a card
        value (which may be a bluff).  After a card is played, other players can
        declare "doubt" (`d`/`doubt`) or pass (`s`/`skip`).

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `play`  | `p`     | Play `cardIndices` cards declaring `claimedValue` |
        | `doubt` | `d`     | Declare doubt; supply `doubterIndices` (human = 0 + any CPU doubters) |
        | `skip`  | `s`     | Pass on doubting; `doubterIndices` is ignored (server uses its own state) |
        | `quit`  | `q`     | End the session |
      operationId: doubtExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DoubtRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play cards at hand indices 0 and 2, declare value 3
                value:
                  command: play
                  cardIndices: [0, 2]
                  claimedValue: 3
                  sessionId: my-session-001
              doubt:
                summary: Human doubts (with CPU 2 also doubting)
                value:
                  command: doubt
                  doubterIndices: [0, 2]
                  sessionId: my-session-001
              skip:
                summary: Human passes (doubterIndices is not required for skip)
                value:
                  command: skip
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DoubtResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DoubtResponse'
              examples:
                paramError:
                  value:
                    players: []
                    currentTurn: 0
                    phase: 0
                    tableCardCount: 0
                    lastAction: null
                    cpuDoubters: []
                    cpuActions: []
                    humanAction: null
                    lastDoubtResult: null
                    gameEndFlag: false
                    winnerIdx: -1
                    message: param error.

  /holdem/exec:
    post:
      tags:
        - holdem
      summary: Execute a Texas Hold'em game command
      description: |
        Send a command to advance the Texas Hold'em game session identified by
        `sessionId`.  The game supports configurable table sizes: 4-max
        (default, 1 human + 3 CPU), 6-max (1 human + 5 CPU), or 9-max
        (1 human + 8 CPU).  Each CPU has a distinct play style.

        The game proceeds through eight phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Init       | `0`   | Before any cards are dealt |
        | PreFlop    | `1`   | Two hole cards dealt; first betting round |
        | Flop       | `2`   | Three community cards revealed |
        | Turn       | `3`   | Fourth community card revealed |
        | River      | `4`   | Fifth community card revealed |
        | Showdown   | `5`   | Hands evaluated; winners determined |
        | End        | `6`   | Round complete; results displayed |
        | Rebuy      | `7`   | Rebuy/addon decision phase |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `fold`  | `f`     | Fold and forfeit the pot |
        | `check` | `ck`    | Pass without betting (only when no outstanding bet) |
        | `call`  | `c`     | Match the current bet |
        | `bet`   | `b`     | Place a bet (use `amount`) |
        | `raise` | `ra`    | Raise by a specified amount on top of calling (use `amount`) |
        | `allin` | `a`     | Go all-in with all remaining chips |
        | `rebuy` | `rb`    | Buy back chips during rebuy period |
        | `skiprebuy` | `sr` | Skip rebuy opportunity |
        | `addon` | `ad`    | Add chips (one-time addon purchase) |
        | `skipaddon` | `sa` | Skip addon opportunity |
        | `quit`  | `q`     | End the session |
      operationId: holdemExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HoldemRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              resetCustomBlinds:
                summary: Start with custom blinds
                value:
                  command: reset
                  smallBlind: 10
                  bigBlind: 20
                  sessionId: my-session-001
              fold:
                summary: Fold
                value:
                  command: fold
                  sessionId: my-session-001
              call:
                summary: Call
                value:
                  command: call
                  sessionId: my-session-001
              bet:
                summary: Place a bet of 40
                value:
                  command: bet
                  amount: 40
                  sessionId: my-session-001
              raise:
                summary: Raise by 80 on top of calling
                value:
                  command: raise
                  amount: 80
                  sessionId: my-session-001
              allin:
                summary: Go all-in
                value:
                  command: allin
                  sessionId: my-session-001
              rebuy:
                summary: Accept rebuy
                value:
                  command: rebuy
                  sessionId: my-session-001
              skipaddon:
                summary: Skip addon
                value:
                  command: skipaddon
                  sessionId: my-session-001
              resetWithRebuy:
                summary: Start with rebuy/addon enabled
                value:
                  command: reset
                  rebuyEnabled: true
                  addonEnabled: true
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HoldemResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HoldemResponse'
              examples:
                paramError:
                  value:
                    players: []
                    communityCards: []
                    pot: 0
                    sidePots: []
                    dealerIdx: 0
                    currentTurn: 0
                    phase: 0
                    gameEndFlag: false
                    lastBet: 0
                    minRaise: 0
                    roundResults: []
                    cpuActions: []
                    message: param error.

  /omaha/exec:
    post:
      tags:
        - omaha
      summary: Execute an Omaha Hold'em game command
      description: |
        Send a command to advance the Omaha Hold'em game session identified by
        `sessionId`.  Omaha is a variant of Texas Hold'em where each player
        receives 4 hole cards and must use exactly 2 hole cards + 3 community
        cards to form the best 5-card hand.

        The game uses the same phases, commands, and configuration options as
        Texas Hold'em.  See the `/holdem/exec` endpoint for full documentation.
      operationId: omahaExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HoldemRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              fold:
                summary: Fold
                value:
                  command: fold
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HoldemResponse'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HoldemResponse'

  /omahahilo/exec:
    post:
      tags:
        - omahahilo
      summary: Execute an Omaha Hi-Lo (8 or Better) game command
      description: |
        Send a command to advance the Omaha Hi-Lo (8 or Better) game session
        identified by `sessionId`. Hi-Lo is a split-pot variant of Omaha:
        the high half follows the standard Omaha rules; the low half is
        awarded to the best A-5 lowball hand (5 distinct ranks all ≤ 8,
        Ace = 1; straights and flushes ignored). Pots are split 50:50;
        odd chips go to the high side. If no player qualifies for a low,
        the high winner takes the full pot.

        Reuses the Omaha request/response shape. Lo-hand cards and split-
        pot amounts surface via the optional `lowQualifies`, `lowBestHand`,
        `hiWonAmount`, and `lowWonAmount` fields in `HoldemResult`, plus
        `isHiLo` on the top-level response. Commands and configuration
        options are identical to `/omaha/exec`.
      operationId: omahaHiLoExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HoldemRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              fold:
                summary: Fold
                value:
                  command: fold
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HoldemResponse'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HoldemResponse'

  /bigo/exec:
    post:
      tags:
        - bigo
      summary: Execute a 5 Card Omaha (Big O) game command
      description: |
        Send a command to advance the 5 Card Omaha (Big O) game session
        identified by `sessionId`. Big O is Omaha dealt 5 hole cards (instead
        of 4); players must still use exactly 2 hole cards + 3 community cards
        to form the best 5-card hand. Phases, commands, configuration options,
        and the request/response shape are identical to `/omaha/exec`.
      operationId: bigOExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HoldemRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              fold:
                summary: Fold
                value:
                  command: fold
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HoldemResponse'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HoldemResponse'

  /bigohilo/exec:
    post:
      tags:
        - bigohilo
      summary: Execute a 5 Card Omaha Hi-Lo (Big O) game command
      description: |
        Send a command to advance the 5 Card Omaha Hi-Lo (Big O) game session
        identified by `sessionId`. This is the 8-or-Better split-pot variant of
        Big O: the high half follows standard Omaha rules over 5 hole cards;
        the low half is awarded to the best A-5 lowball hand (5 distinct ranks
        all ≤ 8, Ace = 1). Pots split 50:50 (odd chips to the high side); if no
        player qualifies for a low, the high winner takes the full pot. Reuses
        the Omaha request/response shape (Hi-Lo fields surfaced via
        `lowQualifies`, `lowBestHand`, `hiWonAmount`, `lowWonAmount`, `isHiLo`).
        Commands and configuration are identical to `/bigo/exec`.
      operationId: bigOHiLoExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HoldemRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              fold:
                summary: Fold
                value:
                  command: fold
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HoldemResponse'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HoldemResponse'

  /shortdeck/exec:
    post:
      tags:
        - shortdeck
      summary: Execute a Short Deck (6+ Hold'em) game command
      description: |
        Send a command to advance the Short Deck Hold'em game session identified by
        `sessionId`.  Short Deck (6+ Hold'em) is a variant of Texas Hold'em that uses
        a 36-card deck (2–5 of each suit removed).  Flush beats Full House, and
        the lowest straight is A-6-7-8-9.

        The game uses the same phases, commands, and configuration options as
        Texas Hold'em.  See the `/holdem/exec` endpoint for full documentation.
      operationId: shortdeckExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HoldemRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              fold:
                summary: Fold
                value:
                  command: fold
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HoldemResponse'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HoldemResponse'

  /hearts/exec:
    post:
      tags:
        - hearts
      summary: Execute a Hearts game command
      description: |
        Send a command to advance the Hearts game session identified by
        `sessionId`.  Hearts is a 4-player trick-taking card game
        (1 human + 3 CPU).  Players avoid taking hearts (1 point each)
        and Q♠ (13 points).  Game ends when any player reaches 100+
        points; lowest score wins.

        The game proceeds through these phases:

        | Phase       | Value | Description |
        |-------------|-------|-------------|
        | Pass        | `0`   | Pass 3 cards to another player |
        | Play        | `1`   | Play a card to the current trick |
        | TrickEnd    | `2`   | Trick complete; view result |
        | RoundEnd    | `3`   | Round complete; view scores |
        | GameEnd     | `4`   | Game over; final standings |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `pass`  | —       | Pass 3 cards (requires `cardIndices`) |
        | `play`  | `p`     | Play a card (requires `cardIndex`) |
        | `next`  | `n`     | Advance to next trick |
        | `nextround` | `nr` | Advance to next round |
        | `setdifficulty` | `sd` | Set CPU difficulty (requires `config.difficulty`: 0=Easy, 1=Normal, 2=Hard) |
        | `setlimit` | `sl`  | Set point limit (requires `config.pointLimit`) |
        | `log`   | `l`     | View action log |
        | `quit`  | `q`     | End the session |
      operationId: heartsExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HeartsRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              pass:
                summary: Pass 3 cards
                value:
                  command: pass
                  cardIndices: [0, 3, 7]
                  sessionId: my-session-001
              play:
                summary: Play a card
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              next:
                summary: Next trick
                value:
                  command: next
                  sessionId: my-session-001
              nextround:
                summary: Next round
                value:
                  command: nextround
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HeartsResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HeartsResponse'
              examples:
                paramError:
                  value:
                    players: []
                    phase: 0
                    roundNumber: 0
                    trickNumber: 0
                    currentPlayerIdx: 0
                    currentTrick: []
                    heartsBroken: false
                    passDirection: 0
                    gameEndFlag: false
                    winnerIdx: -1
                    message: param error.

  /memory/exec:
    post:
      tags:
        - memory
      summary: Execute a Memory game command
      description: |
        Send a command to advance the Memory (神経衰弱) game session identified by
        `sessionId`.  Memory is a 4-player concentration card game
        (1 human + 3 CPU).  52 cards are laid face-down; each turn a player
        flips 2 cards.  If they share the same rank the player takes the pair
        and gets another turn; otherwise the cards are flipped back and play
        passes to the next player.  Game ends when all 26 pairs are taken;
        most pairs wins.

        The game proceeds through these phases:

        | Phase       | Value | Description |
        |-------------|-------|-------------|
        | Flip1       | `0`   | First card flip |
        | Flip2       | `1`   | Second card flip |
        | Result      | `2`   | Flip result (match or mismatch) |
        | GameEnd     | `3`   | Game over; final standings |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game (accepts optional `config.cpuDifficulty`) |
        | `flip`  | `f`     | Flip a card (requires `position`) |
        | `next`  | `n`     | Advance after viewing result |
        | `log`   | `l`     | View action log |
      operationId: memoryExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MemoryRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              flip:
                summary: Flip a card
                value:
                  command: flip
                  position: 5
                  sessionId: my-session-001
              next:
                summary: Advance after result
                value:
                  command: next
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemoryResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MemoryResponse'
              examples:
                paramError:
                  value:
                    players: []
                    phase: 0
                    board: []
                    currentPlayerIdx: 0
                    firstFlipPos: -1
                    secondFlipPos: -1
                    lastMatchResult: false
                    turnNumber: 0
                    gameEndFlag: false
                    winnerIdx: -1
                    message: param error.
                    config:
                      cpuDifficulty: 1

  /klondike/exec:
    post:
      tags:
        - klondike
      summary: Execute a Klondike game command
      description: |
        Send a command to advance the Klondike (ソリティア) game session identified by
        `sessionId`.  Klondike is a single-player solitaire card game using a
        standard 52-card deck.  7 tableau columns with cascading face-down/face-up
        cards, a stock pile, a waste pile, and 4 foundation piles (one per suit).
        Build foundations from Ace to King by suit; build tableau columns in
        descending rank with alternating colors.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | All foundations completed; player wins |
        | GameOver   | `2`   | No more moves available; game over |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset`        | `r`  | Start / restart a game (optionally with `config`) |
        | `draw`         | `d`  | Draw card(s) from stock to waste (1 or 3 depending on draw mode) |
        | `move`         | `m`  | Move card(s) between zones (requires `from` and `to`) |
        | `giveup`       | `g`  | Give up the current game |
        | `hint`         | `h`  | Get a hint for the next move |
        | `autocomplete` | `ac` | Auto-complete remaining moves to foundation |
        | `undo`         | `u`  | Undo the last action |
        | `undo_n`       |      | Undo N actions (requires `n` parameter) |
        | `log`          | `l`  | View action log |
      operationId: klondikeExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/KlondikeRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              draw:
                summary: Draw from stock
                value:
                  command: draw
                  sessionId: my-session-001
              move:
                summary: Move a card
                value:
                  command: move
                  from:
                    zone: waste
                  to:
                    zone: tableau
                    col: 3
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KlondikeResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KlondikeResponse'
              examples:
                paramError:
                  value:
                    tableau: []
                    stockCount: 0
                    waste: []
                    foundation: []
                    phase: 0
                    moveCount: 0
                    message: param error.

  /freecell/exec:
    post:
      tags:
        - freecell
      summary: Execute a FreeCell game command
      description: |
        Send a command to advance the FreeCell (フリーセル) game session identified by
        `sessionId`.  FreeCell is a single-player solitaire card game using a
        standard 52-card deck.  8 tableau columns (first 4 have 7 cards, last 4
        have 6 cards), all dealt face-up.  4 free cells for temporary single-card
        storage.  4 foundation piles: build up by suit Ace to King.  Tableau:
        descending rank, alternating colors; only King on empty column.
        Supermove: max movable cards = (1 + emptyFreeCells) x 2^(emptyTableauCols).

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | All foundations completed; player wins |
        | GameOver   | `2`   | No more moves available; game over |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset`        | `r`  | Start / restart a game |
        | `move`         | `m`  | Move card(s) between zones (requires `from` and `to`) |
        | `giveup`       | `g`  | Give up the current game |
        | `hint`         | `h`  | Get a hint for the next move |
        | `autocomplete` | `ac` | Auto-complete remaining moves to foundation |
        | `undo`         | `u`  | Undo the last action |
        | `undo_n`       |      | Undo N actions (requires `n` parameter) |
        | `log`          | `l`  | View action log |
      operationId: freecellExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FreeCellRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              move:
                summary: Move a card
                value:
                  command: move
                  from:
                    zone: tableau
                    col: 0
                    cardIndex: 6
                  to:
                    zone: freecell
                    cell: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FreeCellResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FreeCellResponse'
              examples:
                paramError:
                  value:
                    tableau: []
                    freeCells: []
                    foundation: []
                    phase: 0
                    moveCount: 0
                    canUndo: false
                    message: param error.

  /seahaventowers/exec:
    post:
      tags:
        - seahaventowers
      summary: Execute a Seahaven Towers game command
      description: |
        Send a command to advance the Seahaven Towers (シーヘイブンタワーズ) game session
        identified by `sessionId`. Seahaven Towers is a single-player solitaire
        card game using a standard 52-card deck — a higher-difficulty FreeCell
        variant. 10 tableau columns × 5 cards each (50 cards), all dealt face-up;
        the remaining 2 cards are pre-placed in 2 upper reserved cells (towers).
        4 foundation piles build up by suit Ace to King. Tableau builds
        **same-suit descending**; only Kings may move to an empty column.
        Supermove limit: max movable cards = 1 + emptyReservedCells.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | All foundations completed; player wins |
        | GameOver   | `2`   | No more moves available; game over |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset`        | `r`  | Start / restart a game |
        | `move`         | `m`  | Move card(s) between zones (requires `from` and `to`) |
        | `giveup`       | `g`  | Give up the current game |
        | `hint`         | `h`  | Get a hint for the next move |
        | `autocomplete` | `ac` | Auto-complete remaining moves to foundation |
        | `undo`         | `u`  | Undo the last action |
        | `undo_n`       |      | Undo N actions (requires `n` parameter) |
        | `log`          | `l`  | View action log |
      operationId: seahaventowersExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SeahavenTowersRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              move:
                summary: Move a card
                value:
                  command: move
                  from:
                    zone: tableau
                    col: 0
                    cardIndex: 4
                  to:
                    zone: reserved
                    cell: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SeahavenTowersResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SeahavenTowersResponse'
              examples:
                paramError:
                  value:
                    tableau: []
                    reservedCells: []
                    foundation: []
                    phase: 0
                    moveCount: 0
                    canUndo: false
                    message: param error.

  /baccarat/exec:
    post:
      tags:
        - baccarat
      summary: Execute a Baccarat game command
      description: |
        Send a command to advance the Baccarat (バカラ) game session identified by
        `sessionId`.  Baccarat is a casino card game using a standard 52-card deck.
        Player bets on Player, Banker, or Tie, then cards are dealt automatically
        following baccarat third-card rules.  Card points: A=1, 2-9=face value,
        10/J/Q/K=0.  Hand value is the ones digit of the sum.

        The game proceeds through these phases:

        | Phase | Value | Description |
        |-------|-------|-------------|
        | Bet   | `1`   | Place a bet (amount and type) |
        | End   | `2`   | Round complete; shows result and payout |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `bet`   | `b`     | Place a bet (requires `amount` and `betType`) |
        | `log`   | `l`     | View action log |
      operationId: baccaratExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BaccaratRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              betPlayer:
                summary: Bet 100 on Player
                value:
                  command: bet
                  amount: 100
                  betType: 0
                  sessionId: my-session-001
              betBanker:
                summary: Bet 200 on Banker
                value:
                  command: bet
                  amount: 200
                  betType: 1
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BaccaratResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BaccaratResponse'
              examples:
                paramError:
                  value:
                    playerHand: []
                    bankerHand: []
                    playerHandValue: 0
                    bankerHandValue: 0
                    phase: 0
                    chips: 0
                    betAmount: 0
                    betType: 0
                    result: 0
                    payout: 0
                    message: param error.

  /spades/exec:
    post:
      tags:
        - spades
      summary: Execute a Spades game command
      description: |
        Send a command to advance the Spades (スペード) game session identified by
        `sessionId`.  Spades is a 4-player trick-taking game where spades are always
        trump.  Players bid how many tricks they expect to win, then play 13 tricks.
        Scoring: made bid = bid×10 + overtricks (bags); failed = -bid×10; nil bid
        (0) success = +100, fail = -100; every 10 bags = -100 penalty.  First to 500
        wins; drop below -200 and lose.

        The game proceeds through these phases:

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Bid       | `0`   | Each player declares expected trick wins |
        | Play      | `1`   | Trick play (follow suit; spades are trump) |
        | TrickEnd  | `2`   | Trick resolved; advance with `next` |
        | RoundEnd  | `3`   | All 13 tricks played; advance with `nextround` |
        | GameEnd   | `4`   | A player reached the point limit or fell below -200 |

        | Command     | Aliases | Description |
        |-------------|---------|-------------|
        | `reset`     | `r`     | Start / restart a game |
        | `bid`       | `b`     | Place a bid (requires `bid` field, 0-13) |
        | `play`      | `p`     | Play a card (requires `cardIndex`) |
        | `next`      | `n`     | Advance to the next trick |
        | `nextround` | `nr`    | Score the round and start the next |
        | `log`       | `l`     | View action log |
      operationId: spadesExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SpadesRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Bid 3 tricks
                value:
                  command: bid
                  bid: 3
                  sessionId: my-session-001
              play:
                summary: Play card at index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpadesResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpadesResponse'

  /twotenjack/exec:
    post:
      tags:
        - twotenjack
      summary: Execute a Two Ten Jack game command
      description: |
        Send a command to advance the Two Ten Jack (ツーテンジャック) game session
        identified by `sessionId`.  Two Ten Jack is a 4-player team trick-taking
        game (seats 0,2 vs 1,3).  Each player is dealt 13 cards and the declarer
        chooses a trump suit.  Point cards: A and J = 1 pt each, 10 = 10 pt; the
        last trick wins a +2 bonus (50 pts per round total).  The declaring team
        must take at least 26 points to make the contract.

        The game proceeds through these phases:

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Declare   | `0`   | Declarer picks the trump suit |
        | Play      | `1`   | Trick play (follow suit if possible, trump cuts) |
        | TrickEnd  | `2`   | Trick resolved; advance with `next` |
        | RoundEnd  | `3`   | Round scored; advance with `nextround` |
        | GameEnd   | `4`   | A team reached the cumulative point limit |

        | Command     | Aliases | Description |
        |-------------|---------|-------------|
        | `reset`     | `r`     | Start / restart a game |
        | `declare`   | `d`     | Declare trump suit (requires `trumpSuit`, 1=♠ 2=♣ 3=♥ 4=♦) |
        | `play`      | `p`     | Play a card (requires `cardIndex`) |
        | `next`      | `n`     | Advance to the next trick |
        | `nextround` | `nr`    | Score the round and start the next |
        | `log`       | `l`     | View action log |
      operationId: twoTenJackExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TwoTenJackRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              declare:
                summary: Declare spades as trump
                value:
                  command: declare
                  trumpSuit: 1
                  sessionId: my-session-001
              play:
                summary: Play card at index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TwoTenJackResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TwoTenJackResponse'

  /crazyeights/exec:
    post:
      tags:
        - crazyeights
      summary: Execute a Crazy Eights game command
      description: |
        Send a command to advance the Crazy Eights (クレイジーエイト) game session
        identified by `sessionId`.  Crazy Eights is a 4-player shedding card game
        using a standard 52-card deck.  Players match the top discard by suit or rank,
        or play an 8 (wild) and choose the next suit.  Cannot play? Draw 1 card.
        Round ends when a player empties their hand; scoring: 8=50, J/Q/K=10, A=1,
        others=face value.  Match ends at the point limit (default 200).

        The game proceeds through these phases:

        | Phase        | Value | Description |
        |--------------|-------|-------------|
        | Play         | `0`   | Play a card or draw |
        | ChooseSuit   | `1`   | Choose suit after playing an 8 |
        | RoundEnd     | `2`   | A player emptied their hand; advance with `nextround` |
        | GameEnd      | `3`   | A player reached the point limit |

        | Command     | Aliases | Description |
        |-------------|---------|-------------|
        | `reset`     | `r`     | Start / restart a game |
        | `play`      | `p`     | Play a card (requires `cardIndex`) |
        | `draw`      | `d`     | Draw a card from the draw pile |
        | `suit`      | `s`     | Choose next suit after playing 8 (requires `suit`: 1-4) |
        | `nextround` | `nr`    | Score the round and start the next |
        | `log`       | `l`     | View action log |
      operationId: crazyeightsExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CrazyEightsRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play card at index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              draw:
                summary: Draw a card
                value:
                  command: draw
                  sessionId: my-session-001
              suit:
                summary: Choose hearts as next suit
                value:
                  command: suit
                  suit: 3
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CrazyEightsResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CrazyEightsResponse'

  /macau/exec:
    post:
      tags:
        - macau
      summary: Execute a Macau game command
      description: |
        Send a command to advance the Macau (マカオ) game session identified by
        `sessionId`.  Macau is a 4-player European Crazy Eights variant on a
        standard 52-card deck with "magic cards" and a last-card declaration.
        Match the top discard by suit or rank.  Magic cards: **2** forces the
        next player to draw two (stackable — play another 2 to pass on the
        accumulated penalty), **7** skips the next player, **8** is wild (choose
        the next suit), **J** reverses the play direction.  On reaching 1 card a
        player must `declare` "Macau!" or `skipdeclare` to take a 2-card penalty.
        Round ends when a player empties their hand; scoring: 8=50, J/Q/K=10,
        A=1, others=face value.  Match ends at the point limit (default 200).

        | Phase        | Value | Description |
        |--------------|-------|-------------|
        | Play         | `0`   | Play a card, draw, or take the penalty stack |
        | ChooseSuit   | `1`   | Choose suit after playing an 8 |
        | MustDeclare  | `2`   | Declare "Macau!" or take the penalty |
        | RoundEnd     | `3`   | A player emptied their hand; advance with `nextround` |
        | GameEnd      | `4`   | A player reached the point limit |

        | Command       | Aliases | Description |
        |---------------|---------|-------------|
        | `reset`       | `r`     | Start / restart a game |
        | `play`        | `p`     | Play a card (requires `cardIndex`) |
        | `draw`        | `d`     | Draw a card / take the penalty stack |
        | `suit`        | `s`     | Choose next suit after playing 8 (requires `suit`: 1-4) |
        | `declare`     | `dc`    | Declare "Macau!" (one card left) |
        | `skipdeclare` | `sk`    | Skip the declaration and take the penalty |
        | `nextround`   | `nr`    | Score the round and start the next |
        | `log`         | `l`     | View action log |
      operationId: macauExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MacauRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play card at index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              draw:
                summary: Draw a card or take the penalty stack
                value:
                  command: draw
                  sessionId: my-session-001
              suit:
                summary: Choose hearts as next suit
                value:
                  command: suit
                  suit: 3
                  sessionId: my-session-001
              declare:
                summary: Declare "Macau!"
                value:
                  command: declare
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MacauResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MacauResponse'

  /mao/exec:
    post:
      tags:
        - mao
      summary: Execute a Mao game command
      description: |
        Send a command to advance the Mao (マオ) game session identified by
        `sessionId`.  Mao is a 4-player Crazy Eights-style shedding game on a
        standard 52-card deck with magic cards and a **secret hidden rule**.
        Match the top discard by suit or rank.  Magic cards: **2** forces the
        next player to draw two (stackable — play another 2 to pass on the
        accumulated penalty), **A** skips the next player, **8** is wild (choose
        the next suit).  On reaching 1 card a player must `declare` "Mao!" or
        `skipdeclare` to take a 2-card penalty.

        The hidden rule is never revealed to the client.  Breaking it incurs a
        silent penalty (`rulePenalty`); the player must guess when to speak the
        required word via `declareword`.  The server only signals `awaitingWord`,
        increments `correctCount` on compliance, and exposes a vague `ruleHint`
        (`hintUnlocked` true) after 3 correct compliances.

        | Phase        | Value | Description |
        |--------------|-------|-------------|
        | Play         | `0`   | Play a card, draw, or take the penalty stack |
        | ChooseSuit   | `1`   | Choose suit after playing an 8 |
        | MustDeclare  | `2`   | Declare "Mao!" or take the penalty |
        | RoundEnd     | `3`   | A player emptied their hand; advance with `nextround` |
        | GameEnd      | `4`   | A player reached the point limit |

        | Command       | Aliases | Description |
        |---------------|---------|-------------|
        | `reset`       | `r`     | Start / restart a game |
        | `play`        | `p`     | Play a card (requires `cardIndex`) |
        | `draw`        | `d`     | Draw a card / take the penalty stack |
        | `suit`        | `s`     | Choose next suit after playing 8 (requires `suit`: 1-4) |
        | `declare`     | `dc`    | Declare "Mao!" (one card left) |
        | `skipdeclare` | `sk`    | Skip the declaration and take the penalty |
        | `declareword` | `dw`    | Say a word (requires `word`); the hidden rule may require it |
        | `nextround`   | `nr`    | Score the round and start the next |
        | `log`         | `l`     | View action log |
      operationId: maoExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MaoRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play card at index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              draw:
                summary: Draw a card or take the penalty stack
                value:
                  command: draw
                  sessionId: my-session-001
              suit:
                summary: Choose hearts as next suit
                value:
                  command: suit
                  suit: 3
                  sessionId: my-session-001
              declareword:
                summary: Say a word required by the hidden rule
                value:
                  command: declareword
                  word: mao
                  sessionId: my-session-001
              declare:
                summary: Declare "Mao!"
                value:
                  command: declare
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MaoResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MaoResponse'

  /pageone/exec:
    post:
      tags:
        - pageone
      summary: Execute a Page One game command
      description: |
        Send a command to advance the Page One (ページワン) game session
        identified by `sessionId`.  Page One is a 4-player Japanese shedding
        card game similar to the UNO predecessor.  Match the top discard's
        suit or rank.  When your hand drops to 1 card you must `declare`
        "Page One!" or `skip` to take a 2-card penalty.

        Phases: 0 = Play, 1 = MustDeclare, 2 = RoundEnd, 3 = GameEnd
      operationId: pageoneExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PageOneRequest'
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PageOneResponse'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PageOneResponse'

  /ginrummy/exec:
    post:
      tags:
        - ginrummy
      summary: Execute a Gin Rummy game command
      description: |
        Send a command to advance the Gin Rummy (ジンラミー) game session
        identified by `sessionId`.  Gin Rummy is a 2-player rummy card game
        using a standard 52-card deck.  10 cards are dealt to each player.
        Draw from stock or discard pile, then discard or knock.  Melds are
        sets (3-4 same rank) and runs (3+ consecutive same suit).  Knock when
        deadwood ≤ 10.  Gin = 0 deadwood (25 bonus).  Undercut = opponent has
        ≤ knocker's deadwood (25 bonus to opponent).  First to reach point
        limit wins.

        The game proceeds through these phases:

        | Phase        | Value | Description |
        |--------------|-------|-------------|
        | Draw         | `0`   | Draw a card from stock or discard pile |
        | Discard      | `1`   | Discard a card or knock |
        | Layoff       | `2`   | Opponent lays off cards on knocker's melds |
        | RoundEnd     | `3`   | Round scored; advance with `nextround` |
        | GameEnd      | `4`   | A player reached the point limit |

        | Command        | Aliases | Description |
        |----------------|---------|-------------|
        | `reset`        | `r`     | Start / restart a game |
        | `drawstock`    | `ds`    | Draw a card from the stock pile |
        | `drawdiscard`  | `dd`    | Draw the top card from the discard pile |
        | `discard`      | `d`     | Discard a card (requires `cardIndex`) |
        | `knock`        | `k`     | Knock (deadwood ≤ 10) (requires `cardIndex`) |
        | `layoff`       | `lo`    | Lay off cards on knocker's melds (requires `cardIndices`) |
        | `nextround`    | `nr`    | Score the round and start the next |
        | `log`          | `l`     | View action log |
      operationId: ginrummyExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GinRummyRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              drawstock:
                summary: Draw from stock pile
                value:
                  command: drawstock
                  sessionId: my-session-001
              drawdiscard:
                summary: Draw from discard pile
                value:
                  command: drawdiscard
                  sessionId: my-session-001
              discard:
                summary: Discard card at index 2
                value:
                  command: discard
                  cardIndex: 2
                  sessionId: my-session-001
              knock:
                summary: Knock (discard card at index 2)
                value:
                  command: knock
                  cardIndex: 2
                  sessionId: my-session-001
              layoff:
                summary: Lay off cards at indices 0 and 3
                value:
                  command: layoff
                  cardIndices: [0, 3]
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GinRummyResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GinRummyResponse'

  /chinchon/exec:
    post:
      tags:
        - chinchon
      summary: Execute a Chinchon game command
      description: |
        Send a command to advance the Chinchon (チンチョン) game session
        identified by `sessionId`. Chinchon is a Spanish Gin Rummy relative
        played by 2 to 4 players with a 40-card Latin deck (each suit minus
        8, 9, 10). 7 cards are dealt to each player. Draw from stock or
        discard, then discard a card, or discard a card and knock. Melds are
        sets (3+ same rank) and runs (3+ consecutive same suit, where 7 and J
        are adjacent). Knock when post-discard deadwood is at or below the
        knock threshold (default 5); opponents then lay off onto the knocker's
        melds. Lowest deadwood wins the round. A player whose cumulative score
        exceeds the elimination limit (default 100) is out; the last player
        standing wins. A same-suit run of all 7 cards wins the match instantly.

        The game proceeds through these phases:

        | Phase        | Value | Description |
        |--------------|-------|-------------|
        | Draw         | `0`   | Draw a card from stock or discard pile |
        | Discard      | `1`   | Discard a card or knock |
        | Layoff       | `2`   | Opponents lay off cards on knocker's melds |
        | RoundEnd     | `3`   | Round scored; advance with `nextround` |
        | GameEnd      | `4`   | One player remains, or a Chinchon wins |

        | Command        | Aliases | Description |
        |----------------|---------|-------------|
        | `reset`        | `r`     | Start / restart a game |
        | `drawstock`    | `ds`    | Draw a card from the stock pile |
        | `drawdiscard`  | `dd`    | Draw the top card from the discard pile |
        | `discard`      | `d`     | Discard a card (requires `cardIndex`) |
        | `knock`        | `k`     | Discard a card and knock (requires `cardIndex`) |
        | `layoff`       | `lo`    | Lay off cards on knocker's melds (requires `cardIndices`) |
        | `nextround`    | `nr`    | Score the round and start the next |
        | `log`          | `l`     | View action log |
      operationId: chinchonExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChinchonRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              drawstock:
                summary: Draw from stock pile
                value:
                  command: drawstock
                  sessionId: my-session-001
              drawdiscard:
                summary: Draw from discard pile
                value:
                  command: drawdiscard
                  sessionId: my-session-001
              discard:
                summary: Discard card at index 2
                value:
                  command: discard
                  cardIndex: 2
                  sessionId: my-session-001
              knock:
                summary: Knock (discard card at index 2)
                value:
                  command: knock
                  cardIndex: 2
                  sessionId: my-session-001
              layoff:
                summary: Lay off cards at indices 0 and 3
                value:
                  command: layoff
                  cardIndices: [0, 3]
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChinchonResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChinchonResponse'

  /threethirteen/exec:
    post:
      tags:
        - threethirteen
      summary: Execute a Three Thirteen game command
      description: |
        Send a command to advance the Three Thirteen (スリー・サーティーン) game
        session identified by `sessionId`. Three Thirteen is a draw-and-discard
        knock Rummy played by 2 to 4 players over 11 rounds with two standard
        52-card decks (104 cards). In round R (1 to 11) each player is dealt
        R+2 cards, and rank R+2 is wild for that round. Draw from the stock or
        discard, then discard one card; if your remaining hand fully melds
        (deadwood 0) you may discard and knock. Melds are sets (3+ same rank)
        and runs (3+ consecutive same suit); the round's wild rank substitutes
        for any card. After a knock the other players take one final turn, then
        deadwood is scored and added to each player's cumulative total. After
        round 11 the lowest cumulative score wins.

        The game proceeds through these phases:

        | Phase        | Value | Description |
        |--------------|-------|-------------|
        | Draw         | `0`   | Draw a card from stock or discard pile |
        | Discard      | `1`   | Discard a card or knock |
        | RoundEnd     | `2`   | Round scored; advance with `nextround` |
        | GameEnd      | `3`   | Round 11 complete; lowest score wins |

        | Command        | Aliases | Description |
        |----------------|---------|-------------|
        | `reset`        | `r`     | Start / restart a game |
        | `drawstock`    | `ds`    | Draw a card from the stock pile |
        | `drawdiscard`  | `dd`    | Draw the top card from the discard pile |
        | `discard`      | `d`     | Discard a card (requires `cardIndex`) |
        | `knock`        | `k`     | Discard a card and knock (requires `cardIndex`) |
        | `nextround`    | `nr`    | Score the round and start the next |
        | `log`          | `l`     | View action log |
      operationId: threeThirteenExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ThreeThirteenRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              drawstock:
                summary: Draw from stock pile
                value:
                  command: drawstock
                  sessionId: my-session-001
              drawdiscard:
                summary: Draw from discard pile
                value:
                  command: drawdiscard
                  sessionId: my-session-001
              discard:
                summary: Discard card at index 2
                value:
                  command: discard
                  cardIndex: 2
                  sessionId: my-session-001
              knock:
                summary: Knock (discard card at index 2)
                value:
                  command: knock
                  cardIndex: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThreeThirteenResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThreeThirteenResponse'

  /conquian/exec:
    post:
      tags:
        - conquian
      summary: Execute a Conquian game command
      description: |
        Send a command to advance the Conquian (コンキャン) game session
        identified by `sessionId`. Conquian is the Mexican ancestor of Rummy
        played by 2 players with a 40-card Latin deck (each suit minus 8, 9, 10).
        10 cards are dealt to each player. On your turn draw from the stock or
        discard, then lay down table melds and discard one card. A card taken
        from the discard pile must be used in a meld immediately. Melds are sets
        (3+ same rank) and runs (3+ consecutive same suit, where 7 and J are
        adjacent). The first player to meld out their whole hand wins the round;
        first to the target wins (default 3) wins the match.

        The game proceeds through these phases:

        | Phase        | Value | Description |
        |--------------|-------|-------------|
        | Draw         | `0`   | Draw a card from stock or discard pile |
        | Meld         | `1`   | Lay down melds then discard a card |
        | RoundEnd     | `2`   | Round scored; advance with `nextround` |
        | GameEnd      | `3`   | A player reached the target wins |

        | Command        | Aliases | Description |
        |----------------|---------|-------------|
        | `reset`        | `r`     | Start / restart a game |
        | `drawstock`    | `ds`    | Draw a card from the stock pile |
        | `drawdiscard`  | `dd`    | Draw the top card from the discard pile |
        | `meld`         | `m`     | Lay down melds (requires `meldGroups`) |
        | `discard`      | `d`     | Discard a card (requires `cardIndex`) |
        | `nextround`    | `nr`    | Score the round and start the next |
        | `log`          | `l`     | View action log |
      operationId: conquianExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ConquianRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              drawstock:
                summary: Draw from stock pile
                value:
                  command: drawstock
                  sessionId: my-session-001
              drawdiscard:
                summary: Draw from discard pile
                value:
                  command: drawdiscard
                  sessionId: my-session-001
              meld:
                summary: Lay down a meld of cards at indices 0, 1, 2
                value:
                  command: meld
                  meldGroups: [[0, 1, 2]]
                  sessionId: my-session-001
              discard:
                summary: Discard card at index 2
                value:
                  command: discard
                  cardIndex: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConquianResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ConquianResponse'

  /sevenbridge/exec:
    post:
      tags:
        - sevenbridge
      summary: Execute a Seven Bridge game command
      description: |
        Send a command to advance the Seven Bridge (セブンブリッジ) game session
        identified by `sessionId`. Seven Bridge is a 2-player rummy-family game
        using a standard 52-card deck. Each player is dealt 7 cards; the first
        `7` in the remaining deck starts the discard pile. A turn consists of a
        Draw phase (stock, Pon, or Chi) followed by a Play phase (meld, layoff,
        discard). The discard rule allows only a `7`, the same rank as the top
        discard, or a rank ±1 away. First to the configured point limit wins.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Draw      | `0`   | Draw from stock or claim discard (Pon/Chi) |
        | Play      | `1`   | Meld, layoff, and discard |
        | RoundEnd  | `2`   | Round scored; advance with `nextround` |
        | GameEnd   | `3`   | A player reached the point limit |

        | Command       | Aliases | Description |
        |---------------|---------|-------------|
        | `reset`       | `r`     | Start / restart a game |
        | `drawstock`   | `ds`    | Draw one card from the stock |
        | `pon`         |         | Claim discard to form a set (`cardIndices` = 2) |
        | `chi`         |         | Claim discard to form a run (`cardIndices` = 2) |
        | `meld`        | `m`     | Lay down a meld (`cardIndices` ≥ 3) |
        | `layoff`      | `lo`    | Add one card to an existing meld (`targetPlayerIdx`, `meldIdx`, `cardIndex`) |
        | `discard`     | `d`     | Discard a card (`cardIndex`) |
        | `nextround`   | `nr`    | Start the next round |
        | `log`         | `l`     | View action log |
      operationId: sevenbridgeExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SevenBridgeRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              drawstock:
                summary: Draw from stock
                value:
                  command: drawstock
                  sessionId: my-session-001
              pon:
                summary: Claim discard with two same-rank cards
                value:
                  command: pon
                  cardIndices: [0, 1]
                  sessionId: my-session-001
              meld:
                summary: Meld a set of three
                value:
                  command: meld
                  cardIndices: [0, 1, 2]
                  sessionId: my-session-001
              layoff:
                summary: Add a card to opponent's meld
                value:
                  command: layoff
                  targetPlayerIdx: 1
                  meldIdx: 0
                  cardIndex: 2
                  sessionId: my-session-001
              discard:
                summary: Discard card at index 3
                value:
                  command: discard
                  cardIndex: 3
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SevenBridgeResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SevenBridgeResponse'

  /canasta/exec:
    post:
      tags:
        - canasta
      summary: Execute a Canasta game command
      description: |
        Send a command to advance the Canasta (カナスタ) game session
        identified by `sessionId`.  Canasta is a 2-player rummy card game
        using 2 decks + 4 jokers (108 cards).  15 cards are dealt to each
        player.  Wild cards (2s and Jokers) can substitute in melds.
        Canasta = 7+ card meld.  First to reach point limit wins.

        The game proceeds through these phases:

        | Phase        | Value | Description |
        |--------------|-------|-------------|
        | Draw         | `0`   | Draw from stock or pick up discard pile |
        | Meld         | `1`   | Meld cards or skip |
        | Discard      | `2`   | Discard a card or go out |
        | RoundEnd     | `3`   | Round scored; advance with `nextround` |
        | GameEnd      | `4`   | A player reached the point limit |

        | Command        | Aliases | Description |
        |----------------|---------|-------------|
        | `reset`        | `r`     | Start / restart a game |
        | `drawstock`    | `ds`    | Draw a card from the stock pile |
        | `drawdiscard`  | `dd`    | Pick up the discard pile (requires `naturalPairIndices`) |
        | `meld`         | `m`     | Meld cards (requires `meldGroups`) |
        | `skipmeld`     | `sm`    | Skip meld phase |
        | `discard`      | `d`     | Discard a card (requires `cardIndex`) |
        | `goout`        | `go`    | Go out (requires at least 1 canasta) |
        | `nextround`    | `nr`    | Score the round and start the next |
        | `log`          | `l`     | View action log |
      operationId: canastaExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CanastaRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              drawstock:
                summary: Draw from stock pile
                value:
                  command: drawstock
                  sessionId: my-session-001
              drawdiscard:
                summary: Pick up discard pile with natural pair at indices 0 and 1
                value:
                  command: drawdiscard
                  naturalPairIndices: [0, 1]
                  sessionId: my-session-001
              meld:
                summary: Meld cards at indices 0, 1, 2
                value:
                  command: meld
                  meldGroups: [[0, 1, 2]]
                  sessionId: my-session-001
              discard:
                summary: Discard card at index 2
                value:
                  command: discard
                  cardIndex: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CanastaResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CanastaResponse'

  /handandfoot/exec:
    post:
      tags:
        - handandfoot
      summary: Execute a Hand and Foot game command
      description: |
        Send a command to advance the Hand and Foot (ハンド・アンド・フット) game
        session identified by `sessionId`.  Hand and Foot is a 4-player /
        2-team Canasta variant using 4 decks + 8 jokers (216 cards).  Each
        player is dealt a 22-card hand plus a face-down 13-card foot picked up
        after the hand is exhausted.  Wild cards (2s and Jokers) can substitute
        in melds.  Melds are shared per team.  Canasta = 7+ card meld (red /
        clean without wilds, black / dirty with wilds).  Going out requires the
        team to hold at least 1 red canasta and 1 black canasta.  First team to
        reach the point limit wins.

        The game proceeds through these phases:

        | Phase        | Value | Description |
        |--------------|-------|-------------|
        | Draw         | `0`   | Draw from stock or pick up discard pile |
        | Meld         | `1`   | Meld cards or skip |
        | Discard      | `2`   | Discard a card or go out |
        | RoundEnd     | `3`   | Round scored; advance with `nextround` |
        | GameEnd      | `4`   | A team reached the point limit |

        | Command        | Aliases | Description |
        |----------------|---------|-------------|
        | `reset`        | `r`     | Start / restart a game |
        | `drawstock`    | `ds`    | Draw a card from the stock pile |
        | `drawdiscard`  | `dd`    | Pick up the discard pile (requires `naturalPairIndices`) |
        | `meld`         | `m`     | Meld cards (requires `meldGroups`) |
        | `skipmeld`     | `sm`    | Skip meld phase |
        | `discard`      | `d`     | Discard a card (requires `cardIndex`) |
        | `goout`        | `go`    | Go out (requires 1 red and 1 black canasta) |
        | `nextround`    | `nr`    | Score the round and start the next |
        | `log`          | `l`     | View action log |
      operationId: handAndFootExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/HandAndFootRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              drawstock:
                summary: Draw from stock pile
                value:
                  command: drawstock
                  sessionId: my-session-001
              drawdiscard:
                summary: Pick up discard pile with natural pair at indices 0 and 1
                value:
                  command: drawdiscard
                  naturalPairIndices: [0, 1]
                  sessionId: my-session-001
              meld:
                summary: Meld cards at indices 0, 1, 2
                value:
                  command: meld
                  meldGroups: [[0, 1, 2]]
                  sessionId: my-session-001
              discard:
                summary: Discard card at index 2
                value:
                  command: discard
                  cardIndex: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HandAndFootResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HandAndFootResponse'

  /burraco/exec:
    post:
      tags:
        - burraco
      summary: Execute a Burraco game command
      description: |
        Send a command to advance the Burraco (ブラーコ) game session
        identified by `sessionId`.  Burraco is a Canasta-derived 2-player
        rummy card game using 2 decks + 4 jokers (108 cards).  11 cards are
        dealt to each player, and two 11-card "pozzetto" reserve piles are set
        aside.  Wild cards (2s and Jokers) can substitute in melds.  Burraco =
        7+ card meld.  The first player to empty their hand claims a pozzetto;
        going out requires the pozzetto plus at least one burraco.  First to
        reach the point limit (default 2005) wins.

        The game proceeds through these phases:

        | Phase        | Value | Description |
        |--------------|-------|-------------|
        | Draw         | `0`   | Draw from stock or pick up discard pile |
        | Meld         | `1`   | Meld cards or skip |
        | Discard      | `2`   | Discard a card or go out |
        | RoundEnd     | `3`   | Round scored; advance with `nextround` |
        | GameEnd      | `4`   | A player reached the point limit |

        | Command        | Aliases | Description |
        |----------------|---------|-------------|
        | `reset`        | `r`     | Start / restart a game |
        | `drawstock`    | `ds`    | Draw a card from the stock pile |
        | `drawdiscard`  | `dd`    | Pick up the discard pile (requires `naturalPairIndices`) |
        | `meld`         | `m`     | Meld cards (requires `meldGroups`) |
        | `skipmeld`     | `sm`    | Skip meld phase |
        | `discard`      | `d`     | Discard a card (requires `cardIndex`) |
        | `goout`        | `go`    | Go out (requires the pozzetto and at least 1 burraco) |
        | `nextround`    | `nr`    | Score the round and start the next |
        | `log`          | `l`     | View action log |
      operationId: burracoExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BurracoRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              drawstock:
                summary: Draw from stock pile
                value:
                  command: drawstock
                  sessionId: my-session-001
              drawdiscard:
                summary: Pick up discard pile with natural pair at indices 0 and 1
                value:
                  command: drawdiscard
                  naturalPairIndices: [0, 1]
                  sessionId: my-session-001
              meld:
                summary: Meld cards at indices 0, 1, 2
                value:
                  command: meld
                  meldGroups: [[0, 1, 2]]
                  sessionId: my-session-001
              discard:
                summary: Discard card at index 2
                value:
                  command: discard
                  cardIndex: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BurracoResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BurracoResponse'

  /pinochle/exec:
    post:
      tags:
        - pinochle
      summary: Execute a Pinochle game command
      description: |
        Send a command to advance the Pinochle (ピノクル) game session
        identified by `sessionId`.  Pinochle is a 4-player 2-team trick-taking
        card game with bidding and melding using a 48-card double deck
        (9,10,J,Q,K,A × 2 per suit).  12 cards dealt to each player.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Bid       | `0`   | Players bid expected total points |
        | Trump     | `1`   | Highest bidder declares trump suit |
        | Meld      | `2`   | All players display melds |
        | Play      | `3`   | 12 tricks with must-follow/trump/win rules |
        | TrickEnd  | `4`   | Trick resolved |
        | RoundEnd  | `5`   | Round scored |
        | GameEnd   | `6`   | A team reached the point limit |

        **Commands:**
        | Command     | Parameters              | Description |
        |-------------|-------------------------|-------------|
        | `reset`     | `config` (opt)          | Start a new game |
        | `bid`       | `bidAmount`             | Place a bid |
        | `pass`      |                         | Pass on bidding |
        | `trump`     | `suit`                  | Declare trump suit (1-4) |
        | `meld`      |                         | Confirm melds and proceed to play |
        | `play`      | `cardIndex`             | Play a card from hand |
        | `next`      |                         | Proceed to next trick |
        | `nextround` |                         | Proceed to next round |
        | `hint`      |                         | Get AI hint |
        | `log`       |                         | Get action log |
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PinochleRequest'
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PinochleResponse'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PinochleResponse'

  /spider/exec:
    post:
      tags:
        - spider
      summary: Execute a Spider Solitaire game command
      description: |
        Send a command to advance the Spider Solitaire game session
        identified by `sessionId`.  Spider Solitaire is a single-player
        solitaire game using 2 decks (104 cards) with 10 tableau columns.
        Move same-suit descending sequences between columns.  Complete
        K-through-A same-suit sequences are automatically removed.
        8 completed suits = game clear.  Difficulty: 1/2/4 suits.

        | Phase       | Value | Description |
        |-------------|-------|-------------|
        | Playing     | `0`   | Game in progress |
        | GameClear   | `1`   | All 8 suits completed |
        | GameOver    | `2`   | Player gave up |

        | Command        | Aliases | Description |
        |----------------|---------|-------------|
        | `reset`        | `r`     | Start / restart a game |
        | `deal`         | `d`     | Deal 1 card to each column from stock |
        | `move`         | `m`     | Move cards between tableau columns |
        | `giveup`       | `g`     | Give up |
        | `hint`         | `h`     | Get a move suggestion |
        | `autocomplete` | `ac`    | Auto-complete (when all face up) |
        | `undo`         | `u`     | Undo last move |
        | `undo_n`       |         | Undo N moves (requires `n` parameter) |
        | `log`          | `l`     | View action log |
      operationId: spiderExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SpiderRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              resetWithDifficulty:
                summary: Start a new game with 2-suit difficulty
                value:
                  command: reset
                  config:
                    difficulty: 2
                  sessionId: my-session-001
              deal:
                summary: Deal cards from stock
                value:
                  command: deal
                  sessionId: my-session-001
              move:
                summary: Move card(s) from column 3, index 2 to column 5
                value:
                  command: move
                  from:
                    zone: tableau
                    col: 3
                    cardIndex: 2
                  to:
                    zone: tableau
                    col: 5
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpiderResponse'
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpiderResponse'

  /napoleon/exec:
    post:
      tags:
        - napoleon
      summary: Execute a Napoleon game command
      description: |
        Send a command to advance the Napoleon (ナポレオン) game session identified by
        `sessionId`.  Napoleon is a 4-player trick-taking card game using 52 cards + 1
        Joker (53 total).  Players bid the number of picture cards (J/Q/K/A + Joker = 17
        max) they expect to capture.  The highest bidder becomes Napoleon, declares a trump
        suit, and names an adjutant card (hidden ally).  Hidden teams: Napoleon's Army
        (Napoleon + adjutant holder) vs Allied Forces.  Special cards: Joker (mighty --
        strongest card), Spade 3 (joker killer -- beats mighty).  Napoleon's team must
        capture >= bid number of picture cards to win.

        The game proceeds through these phases:

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Bid       | `0`   | Each player bids picture card count or passes |
        | Trump     | `1`   | Napoleon declares the trump suit |
        | Exchange  | `2`   | Napoleon exchanges cards with the kitty |
        | Play      | `3`   | Trick play (follow suit; trump is declared suit) |
        | TrickEnd  | `4`   | Trick resolved; advance with `next` |
        | RoundEnd  | `5`   | All 13 tricks played; advance with `nextround` |
        | GameEnd   | `6`   | A player reached the point limit |

        | Command        | Aliases | Description |
        |----------------|---------|-------------|
        | `reset`        | `r`     | Start / restart a game |
        | `bid`          | `b`     | Place a bid (requires `bid` field) or pass (bid = 0) |
        | `trump`        | `t`     | Declare trump suit (requires `suit` field: 1-4) |
        | `exchange`     | `e`     | Exchange cards with kitty (requires `cardIndices`) |
        | `play`         | `p`     | Play a card (requires `cardIndex`) |
        | `next`         | `n`     | Advance to the next trick |
        | `nextround`    | `nr`    | Score the round and start the next |
        | `hint`         | `h`     | Get a play suggestion |
        | `log`          | `l`     | View action log |
      operationId: napoleonExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NapoleonRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Bid 12 picture cards
                value:
                  command: bid
                  bid: 12
                  sessionId: my-session-001
              pass:
                summary: Pass on bidding
                value:
                  command: bid
                  bid: 0
                  sessionId: my-session-001
              trump:
                summary: Declare spades as trump
                value:
                  command: trump
                  suit: 1
                  sessionId: my-session-001
              exchange:
                summary: Exchange cards at indices 0 and 3 with kitty
                value:
                  command: exchange
                  cardIndices: [0, 3]
                  sessionId: my-session-001
              play:
                summary: Play card at index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NapoleonResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NapoleonResponse'

  /indianpoker/exec:
    post:
      tags:
        - indianpoker
      summary: Execute an Indian Poker game command
      description: |
        Send a command to advance the Indian Poker (インディアンポーカー) game session
        identified by `sessionId`.  Indian Poker is a 4-player betting card game where
        each player can see everyone else's card but not their own.  Each round, one card
        is dealt to each player and held on their forehead (visible to others).  Players
        bet based on the strength they infer from opponents' cards and their own betting
        behavior.

        The game proceeds through these phases:

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Init      | `0`   | Initial state before first reset |
        | Ante      | `1`   | Ante posted; cards dealt |
        | Betting   | `2`   | Betting round (fold/check/call/bet/raise/all-in) |
        | Showdown  | `3`   | Cards revealed; winner determined |
        | End       | `4`   | A player is eliminated (0 chips) |

        | Command   | Aliases | Description |
        |-----------|---------|-------------|
        | `reset`   | `r`     | Start / restart a game |
        | `fold`    | `f`     | Fold the current hand |
        | `check`   | `ck`    | Check (no bet, when no outstanding bet) |
        | `call`    | `c`     | Call the current bet |
        | `bet`     | `b`     | Place a bet (requires `amount`) |
        | `raise`   | `ra`    | Raise (requires `amount`) |
        | `allin`   | `a`     | Go all-in |
        | `log`     | `l`     | View action log |
      operationId: indianPokerExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/IndianPokerRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              fold:
                summary: Fold the current hand
                value:
                  command: fold
                  sessionId: my-session-001
              bet:
                summary: Place a bet of 50
                value:
                  command: bet
                  amount: 50
                  sessionId: my-session-001
              call:
                summary: Call the current bet
                value:
                  command: call
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IndianPokerResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IndianPokerResponse'

  /videopoker/exec:
    post:
      tags:
        - videopoker
      summary: Execute a Video Poker game command
      description: |
        Send a command to advance the Video Poker (Jacks or Better) game session
        identified by `sessionId`.  Video Poker is a single-player casino card game
        using a standard 52-card deck.  The player bets 1-5 coins, receives 5 cards,
        chooses which cards to hold, then draws replacements.  The final hand is
        evaluated and paid according to the Jacks or Better pay table.

        The game proceeds through these phases:

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Bet     | `1`   | Place a bet (1-5 coins) |
        | Draw    | `2`   | Choose cards to hold, then draw replacements |
        | Result  | `3`   | Final hand evaluated; payout displayed |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `bet`   | `b`     | Place a bet (requires `amount`: 1-5) |
        | `hold`  | `h`     | Hold cards and draw (requires `indices`: array of card positions 0-4) |
        | `log`   | `l`     | View action log |
      operationId: videoPokerExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VideoPokerRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Bet 3 coins
                value:
                  command: bet
                  amount: 3
                  sessionId: my-session-001
              hold:
                summary: Hold cards at positions 0, 2, and 4
                value:
                  command: hold
                  indices: [0, 2, 4]
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VideoPokerResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VideoPokerResponse'

  /deuceswild/exec:
    post:
      tags:
        - deuceswild
      summary: Execute a Deuces Wild game command
      description: |
        Send a command to advance the Deuces Wild game session identified by
        `sessionId`.  Deuces Wild is a Video Poker variant using a standard
        52-card deck where all 2s are wild cards.  The player bets 1-5 coins,
        receives 5 cards, chooses which cards to hold, then draws replacements.
        The final hand is evaluated using wild card substitution and paid
        according to the Deuces Wild pay table.

        The game proceeds through these phases:

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Bet     | `1`   | Place a bet (1-5 coins) |
        | Draw    | `2`   | Choose cards to hold, then draw replacements |
        | Result  | `3`   | Final hand evaluated; payout displayed |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `bet`   | `b`     | Place a bet (requires `amount`: 1-5) |
        | `hold`  | `h`     | Hold cards and draw (requires `indices`: array of card positions 0-4) |
        | `log`   | `l`     | View action log |
      operationId: deucesWildExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VideoPokerRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Bet 3 coins
                value:
                  command: bet
                  amount: 3
                  sessionId: my-session-001
              hold:
                summary: Hold cards at positions 0, 2, and 4
                value:
                  command: hold
                  indices: [0, 2, 4]
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/VideoPokerResponse'
                  - type: object
                    properties:
                      variantName:
                        type: string
                        description: Name of the Video Poker variant
                        example: deuceswild
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VideoPokerResponse'

  /jokerpoker/exec:
    post:
      tags:
        - jokerpoker
      summary: Execute a Joker Poker game command
      description: |
        Send a command to advance the Joker Poker game session identified by
        `sessionId`.  Joker Poker is a Video Poker variant using a 53-card deck
        (standard 52 + 1 joker).  The joker acts as a wild card.  The player
        bets 1-5 coins, receives 5 cards, chooses which cards to hold, then
        draws replacements.  The final hand is evaluated using wild card
        substitution and paid according to the Joker Poker pay table.

        The game proceeds through these phases:

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Bet     | `1`   | Place a bet (1-5 coins) |
        | Draw    | `2`   | Choose cards to hold, then draw replacements |
        | Result  | `3`   | Final hand evaluated; payout displayed |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `bet`   | `b`     | Place a bet (requires `amount`: 1-5) |
        | `hold`  | `h`     | Hold cards and draw (requires `indices`: array of card positions 0-4) |
        | `log`   | `l`     | View action log |
      operationId: jokerPokerExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VideoPokerRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Bet 3 coins
                value:
                  command: bet
                  amount: 3
                  sessionId: my-session-001
              hold:
                summary: Hold cards at positions 0, 2, and 4
                value:
                  command: hold
                  indices: [0, 2, 4]
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                allOf:
                  - $ref: '#/components/schemas/VideoPokerResponse'
                  - type: object
                    properties:
                      variantName:
                        type: string
                        description: Name of the Video Poker variant
                        example: jokerpoker
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VideoPokerResponse'

  /euchre/exec:
    post:
      tags:
        - euchre
      summary: Execute a Euchre game command
      description: |
        Send a command to advance the Euchre (ユーカー) game session identified by
        `sessionId`.  Euchre is a 4-player, 2-team trick-taking game with a 24-card
        deck (9-A).  Features Right Bower (Jack of trump) and Left Bower (Jack of
        same-color suit) as highest trump cards.  Trump suit determined via bidding.
        "Going alone" option for bonus points.  First team to 10 points wins.

        The game proceeds through these phases:

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | PickUp    | `0`   | Decide whether to order up the turned-up card |
        | CallTrump | `1`   | Call a trump suit (after all pass in PickUp) |
        | Discard   | `2`   | Dealer discards a card after picking up |
        | Play      | `3`   | Trick play (follow suit; bower hierarchy) |
        | TrickEnd  | `4`   | Trick resolved; advance with `next` |
        | RoundEnd  | `5`   | All 5 tricks played; advance with `nextround` |
        | GameEnd   | `6`   | A team reached 10 points |

        | Command     | Aliases | Description |
        |-------------|---------|-------------|
        | `reset`     | `r`     | Start / restart a game |
        | `orderup`   | `o`     | Order up the turned-up card |
        | `orderupalone` | `oa` | Order up and go alone |
        | `pass`      | `pa`    | Pass on ordering up / calling trump |
        | `calltrump` | `c`     | Call a trump suit (requires `suit`: 1-4) |
        | `calltrumpalone` | `ca` | Call a trump suit and go alone |
        | `discard`   | `d`     | Discard a card (requires `cardIndex`) |
        | `play`      | `p`     | Play a card (requires `cardIndex`) |
        | `next`      | `n`     | Advance to the next trick |
        | `nextround` | `nr`    | Score the round and start the next |
        | `hint`      | `h`     | Get a play/bid hint |
        | `log`       | `l`     | View action log |
      operationId: euchreExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EuchreRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              orderup:
                summary: Order up the turned-up card
                value:
                  command: orderup
                  sessionId: my-session-001
              pass:
                summary: Pass on ordering up
                value:
                  command: pass
                  sessionId: my-session-001
              calltrump:
                summary: Call hearts as trump
                value:
                  command: calltrump
                  suit: 3
                  sessionId: my-session-001
              play:
                summary: Play card at index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EuchreResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EuchreResponse'

  /pyramid/exec:
    post:
      tags:
        - pyramid
      summary: Execute a Pyramid game command
      description: |
        Send a command to advance the Pyramid (ピラミッド) game session identified by
        `sessionId`.  Pyramid is a single-player solitaire card game using a
        standard 52-card deck.  28 cards are dealt face-up in a 7-row pyramid.
        Remove exposed pairs of cards that sum to 13; Kings (value 13) are
        removed alone.  Remaining 24 cards form the stock pile.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | All pyramid cards removed; player wins |
        | GameOver   | `2`   | No more moves available; game over |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset`        | `r`  | Start / restart a game |
        | `draw`         | `d`  | Draw a card from stock to waste |
        | `remove`       | `rm` | Remove card(s) (pair summing to 13, king, or waste pair) |
        | `giveup`       | `g`  | Give up the current game |
        | `hint`         | `h`  | Get a hint for the next move |
        | `undo`         | `u`  | Undo the last action |
        | `undo_n`       |      | Undo N actions (requires `n` parameter) |
        | `log`          | `l`  | View action log |
      operationId: pyramidExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PyramidRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              draw:
                summary: Draw from stock
                value:
                  command: draw
                  sessionId: my-session-001
              removePair:
                summary: Remove a pair from pyramid
                value:
                  command: remove
                  card1:
                    zone: pyramid
                    row: 6
                    col: 0
                  card2:
                    zone: pyramid
                    row: 6
                    col: 1
                  sessionId: my-session-001
              removeKing:
                summary: Remove a King from pyramid
                value:
                  command: remove
                  card1:
                    zone: pyramid
                    row: 6
                    col: 3
                  sessionId: my-session-001
              removeWithWaste:
                summary: Remove waste top + pyramid card
                value:
                  command: remove
                  card1:
                    zone: waste
                  card2:
                    zone: pyramid
                    row: 5
                    col: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PyramidResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PyramidResponse'
              examples:
                paramError:
                  value:
                    pyramid: []
                    stockCount: 0
                    waste: []
                    phase: 0
                    moveCount: 0
                    message: param error.

  /tripeaks/exec:
    post:
      tags:
        - tripeaks
      summary: Execute a TriPeaks game command
      description: |
        Send a command to advance the TriPeaks (トリピークス) game session identified by
        `sessionId`.  TriPeaks is a single-player solitaire card game using a
        standard 52-card deck.  Three overlapping pyramids (peaks) form a 28-card
        tableau.  Remove exposed cards that are ±1 rank from the waste pile top
        card (K-A wrap).  Win by removing all 28 tableau cards.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | All tableau cards removed; player wins |
        | GameOver   | `2`   | No more moves available; game over |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset`        | `r`  | Start / restart a game |
        | `draw`         | `d`  | Draw a card from stock to waste |
        | `remove`       | `rm` | Remove an exposed card ±1 rank from waste top |
        | `giveup`       | `g`  | Give up the current game |
        | `hint`         | `h`  | Get a hint for the next move |
        | `undo`         | `u`  | Undo the last action |
        | `undo_n`       |      | Undo N actions (requires `n` parameter) |
        | `log`          | `l`  | View action log |
      operationId: tripeaksExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TriPeaksRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              draw:
                summary: Draw from stock
                value:
                  command: draw
                  sessionId: my-session-001
              remove:
                summary: Remove a tableau card
                value:
                  command: remove
                  row: 3
                  col: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TriPeaksResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TriPeaksResponse'
              examples:
                paramError:
                  value:
                    tableau: []
                    stockCount: 0
                    waste: []
                    phase: 0
                    moveCount: 0
                    message: param error.

  /cribbage/exec:
    post:
      tags:
        - cribbage
      summary: Execute a Cribbage game command
      description: |
        Send a command to advance the Cribbage (クリベッジ) game session identified by
        `sessionId`.  Cribbage is a classic 2-player pegging and scoring card game
        using a standard 52-card deck.  Each player is dealt 6 cards and discards 2
        to the crib.  A starter card is cut.  Players alternate playing cards in the
        pegging phase (target 31), then score hands and crib.  First to 121 points wins.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Discard    | `0`   | Each player discards 2 cards to the crib |
        | Cut        | `1`   | Starter card revealed |
        | Pegging    | `2`   | Alternate playing cards toward 31 |
        | Show       | `3`   | Score hands and crib |
        | RoundEnd   | `4`   | Round summary |
        | GameEnd    | `5`   | Game over; winner determined |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset`          | `r`   | Start / restart a game |
        | `discard`        | `d`   | Discard cards to the crib (indices) |
        | `peg`            | `p`   | Play a card during pegging (index) |
        | `go`             |       | Declare "Go" when unable to play |
        | `shownext`       | `sn`  | Advance to next show step |
        | `nextround`      | `nr`  | Start the next round |
        | `setdifficulty`  | `sd`  | Set CPU difficulty (0=Easy, 1=Normal, 2=Hard) |
        | `setlimit`       | `sl`  | Set point limit |
        | `log`            | `l`   | View action log |
      operationId: cribbageExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CribbageRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              discard:
                summary: Discard two cards to crib
                value:
                  command: discard
                  cardIndices: [4, 5]
                  sessionId: my-session-001
              peg:
                summary: Play a card during pegging
                value:
                  command: peg
                  cardIndex: 0
                  sessionId: my-session-001
              go:
                summary: Declare Go
                value:
                  command: go
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CribbageResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CribbageResponse'
              examples:
                paramError:
                  value:
                    players: []
                    phase: 0
                    roundNumber: 0
                    currentPlayerIdx: 0
                    dealerIdx: 0
                    crib: []
                    pegCount: 0
                    pegPlayedCards: []
                    showPhaseStep: 0
                    handScoreDetails: [null, null, null]
                    gameEndFlag: false
                    winnerIdx: -1
                    message: param error.

  /threecard/exec:
    post:
      tags:
        - threecard
      summary: Execute a Three Card Poker game command
      description: |
        Send a command to advance the Three Card Poker (スリーカードポーカー) game session
        identified by `sessionId`.  Three Card Poker is a single-player casino card game
        using a standard 52-card deck.  Player places an Ante bet and optional Pair Plus
        side bet, receives 3 cards, then decides to Play (match Ante) or Fold (forfeit Ante).
        Dealer must have Q-high or better to qualify.

        Hand rankings (highest to lowest): Straight Flush, Three of a Kind, Straight,
        Flush, Pair, High Card.

        The game proceeds through these phases:

        | Phase  | Value | Description |
        |--------|-------|-------------|
        | Bet    | `1`   | Place Ante and optional Pair Plus bet |
        | Action | `2`   | Decide to Play or Fold |
        | End    | `3`   | Round complete; shows result and payout |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `bet`   | `b`     | Place Ante bet (requires `amount`; optional `pairPlusBet`) |
        | `play`  | `p`     | Place Play bet (equal to Ante) and see dealer's hand |
        | `fold`  | `f`     | Forfeit Ante and Pair Plus bets |
        | `log`   | `l`     | View action log |
      operationId: threecardExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ThreeCardRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Bet 100 Ante with 50 Pair Plus
                value:
                  command: bet
                  amount: 100
                  pairPlusBet: 50
                  sessionId: my-session-001
              play:
                summary: Place Play bet
                value:
                  command: play
                  sessionId: my-session-001
              fold:
                summary: Fold hand
                value:
                  command: fold
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThreeCardResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThreeCardResponse'
              examples:
                paramError:
                  value:
                    playerHand: []
                    dealerHand: []
                    phase: 0
                    chips: 0
                    anteBet: 0
                    pairPlusBet: 0
                    playBet: 0
                    result: 0
                    antePayout: 0
                    playPayout: 0
                    anteBonusPayout: 0
                    pairPlusPayout: 0
                    totalPayout: 0
                    dealerQualified: false
                    playerHandRank: 0
                    dealerHandRank: 0
                    message: param error.

  /fourcardpoker/exec:
    post:
      tags:
        - fourcardpoker
      summary: Execute a Four Card Poker game command
      description: |
        Send a command to advance the Four Card Poker (フォーカードポーカー) game session
        identified by `sessionId`. Single-player casino poker — the player is dealt 5 cards
        and the dealer 6 (one face-up upcard); both sides build their best 4-card hand.
        No dealer qualification. After seeing the upcard the player chooses Play
        (1x / 2x / 3x Ante) or Fold.

        Hand rankings (ascending): High Card, Pair, Two Pair, Straight, Flush,
        Three of a Kind, Straight Flush, Four of a Kind.

        | Phase  | Value | Description |
        |--------|-------|-------------|
        | Bet    | `1`   | Place Ante and optional Aces Up sidebet |
        | Action | `2`   | Choose Play (1x-3x Ante) or Fold |
        | End    | `3`   | Round complete; shows result and payouts |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `bet`   | `b`     | Place Ante (and optional Aces Up sidebet) |
        | `play`  | `p`     | Place Play bet at `playMultiplier` × Ante and resolve |
        | `fold`  | `f`     | Forfeit the Ante (Aces Up is still evaluated) |
        | `log`   | `l`     | View action log |
      operationId: fourcardpokerExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FourCardPokerRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Bet 100 Ante with 50 Aces Up
                value:
                  command: bet
                  amount: 100
                  acesUpBet: 50
                  sessionId: my-session-001
              play:
                summary: Play at 3x multiplier
                value:
                  command: play
                  playMultiplier: 3
                  sessionId: my-session-001
              fold:
                summary: Fold hand
                value:
                  command: fold
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FourCardPokerResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FourCardPokerResponse'

  /rummy500/exec:
    post:
      tags:
        - rummy500
      summary: Execute a Rummy 500 game command
      description: |
        Send a command to advance the Rummy 500 (500ラム / ラミー500) game session
        identified by `sessionId`. 2-player rummy variant with a 52-card deck (13 cards
        per player). The discard pile is preserved in order — when drawing from the
        discard pile the player may pick any card and receives it together with every
        card stacked above it. After drawing the player may meld sets (3+ same rank)
        or runs (3+ same-suit consecutive; Aces low or high but no K-A-2 wrap) and
        lay off cards onto existing melds; the turn ends by discarding one card.

        Scoring per round = laid-meld points (positive) minus remaining hand points
        (negative). Card values: A=1 (or 15 inside a Q-K-A run), J/Q/K=10, others
        face value. First player to reach the configured point limit (default 500)
        wins. A round ends when a player goes out (hand empty) or the stock is
        exhausted.

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Draw     | `0`   | Draw a card from stock or discard pile |
        | Play     | `1`   | Meld / layoff / then discard to end turn |
        | RoundEnd | `2`   | Round complete; advance via `nextround` |
        | GameEnd  | `3`   | Game over (point limit reached) |

        | Command       | Aliases | Description |
        |---------------|---------|-------------|
        | `reset`       | `r`     | Start / restart a game |
        | `drawstock`   | `ds`    | Draw the top card from the stock |
        | `drawdiscard` | `dd`    | Pick a card from the discard pile (idx + all cards above) |
        | `meld`        | `m`     | Lay down a meld from hand (cardIndices) |
        | `layoff`      | `lo`    | Lay off a card onto an existing meld |
        | `discard`     | `d`     | Discard a card to end the turn |
        | `nextround`   | `nr`    | Advance to the next round after RoundEnd |
        | `log`         | `l`     | View action log |
      operationId: rummy500Exec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Rummy500Request'
      responses:
        '200':
          description: Game state after the command was executed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Rummy500Response'
        '400':
          description: Invalid request body or missing required parameters.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Rummy500Response'

  /eightoff/exec:
    post:
      tags:
        - eightoff
      summary: Execute an Eight Off solitaire command
      description: |
        Send a command to advance the Eight Off (エイトオフ) game session
        identified by `sessionId`. Eight Off is a stricter FreeCell variant:
        the 52-card deck is dealt into **8 tableau columns of 6 face-up cards
        (48 cards)** plus the remaining 4 cards in the first 4 of **8 free
        cells**. Foundations build up by suit Ace→King. The tableau builds
        **same-suit descending** only; **only Kings may be placed on an empty
        column**. Supermove uses the FreeCell formula
        `(1 + emptyFreeCells) × 2^(emptyTableauCols)`. An A* solver gates
        stalemate detection and auto-complete.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | `0`   | Game in progress |
        | GameClear | `1`   | All foundations are complete |
        | GameOver  | `2`   | Player gave up |

        | Command        | Aliases | Description |
        |----------------|---------|-------------|
        | `reset`        | `r`     | Start / restart a game |
        | `move`         | `m`     | Move card(s) between tableau / freecell / foundation |
        | `giveup`       | `g`     | Forfeit the round |
        | `hint`         | `h`     | Suggest the next move |
        | `autocomplete` | `ac`    | Auto-move every safely-promotable card to foundations |
        | `undo`         | `u`     | Undo the previous action |
        | `undo_n`       |         | Undo `n` actions in a row |
        | `log`          | `l`     | View action log |
      operationId: eightoffExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EightOffRequest'
      responses:
        '200':
          description: Game state after the command was executed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EightOffResponse'
        '400':
          description: Invalid request body or missing required parameters.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EightOffResponse'

  /penguin/exec:
    post:
      tags:
        - penguin
      summary: Execute a Penguin solitaire command
      description: |
        Send a command to advance the Penguin (ペンギン) game session
        identified by `sessionId`. Penguin is a FreeCell variant:
        the 52-card deck is dealt into **7 tableau columns of 7 face-up cards
        (49 cards)** plus the remaining 3 cards of the same rank as the first
        dealt card (baseRank) in 3 of **7 free cells**. Foundations build up
        by suit from baseRank with K-A wraparound. The tableau builds
        **same-suit descending** with K-A wraparound; **only prevRank(baseRank)
        cards may be placed on an empty column**. Supermove uses the FreeCell
        formula `(1 + emptyFreeCells) x 2^(emptyTableauCols)`.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | `0`   | Game in progress |
        | GameClear | `1`   | All foundations are complete |
        | GameOver  | `2`   | Player gave up |

        | Command        | Aliases | Description |
        |----------------|---------|-------------|
        | `reset`        | `r`     | Start / restart a game |
        | `move`         | `m`     | Move card(s) between tableau / freecell / foundation |
        | `giveup`       | `g`     | Forfeit the round |
        | `hint`         | `h`     | Suggest the next move |
        | `autocomplete` | `ac`    | Auto-move every safely-promotable card to foundations |
        | `undo`         | `u`     | Undo the previous action |
        | `undo_n`       |         | Undo `n` actions in a row |
        | `log`          | `l`     | View action log |
      operationId: penguinExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PenguinRequest'
      responses:
        '200':
          description: Game state after the command was executed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PenguinResponse'
        '400':
          description: Invalid request body or missing required parameters.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PenguinResponse'

  /caribbeanstud/exec:
    post:
      tags:
        - caribbeanstud
      summary: Execute a Caribbean Stud Poker game command
      description: |
        Send a command to advance the Caribbean Stud Poker (カリビアンスタッドポーカー)
        game session identified by `sessionId`. Caribbean Stud Poker is a single-player
        casino poker game using a standard 52-card deck. Player places an Ante bet and
        an optional Jackpot side bet, receives 5 cards, then decides to Call (place a
        Play bet equal to 2x Ante) or Fold (forfeit Ante). Dealer must have Ace-King
        high or any pair to qualify. If dealer doesn't qualify, Ante pays 1:1 and Play
        bet pushes. If dealer qualifies, the higher 5-card poker hand wins. Play bet
        on win pays per hand: Royal Flush 100:1, Straight Flush 50:1, Four of a Kind
        20:1, Full House 7:1, Flush 5:1, Straight 4:1, Three of a Kind 3:1, Two Pair
        2:1, Pair or less 1:1. The Jackpot side bet pays independently of the main
        result: Royal Flush 20000x, Straight Flush 5000x, Four of a Kind 500x, Full
        House 100x, Flush 50x.

        The game proceeds through these phases:

        | Phase  | Value | Description |
        |--------|-------|-------------|
        | Bet    | `1`   | Place Ante and optional Jackpot side bet |
        | Action | `2`   | Decide to Call or Fold |
        | End    | `3`   | Round complete; shows result and payout |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `bet`   | `b`     | Place Ante bet (requires `amount`; optional `jackpotBet`) |
        | `play`  | `p`     | Call: place Play bet (2x Ante) and reveal dealer hand |
        | `fold`  | `f`     | Forfeit Ante bet |
        | `log`   | `l`     | View action log |
      operationId: caribbeanstudExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CaribbeanStudRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Bet 100 Ante with 10 Jackpot
                value:
                  command: bet
                  amount: 100
                  jackpotBet: 10
                  sessionId: my-session-001
              play:
                summary: Call (place Play bet)
                value:
                  command: play
                  sessionId: my-session-001
              fold:
                summary: Fold hand
                value:
                  command: fold
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CaribbeanStudResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CaribbeanStudResponse'
              examples:
                paramError:
                  value:
                    playerHand: []
                    dealerHand: []
                    phase: 0
                    chips: 0
                    anteBet: 0
                    jackpotBet: 0
                    playBet: 0
                    result: 0
                    antePayout: 0
                    playPayout: 0
                    jackpotPayout: 0
                    totalPayout: 0
                    dealerQualified: false
                    playerHandRank: 0
                    dealerHandRank: 0
                    message: param error.

  /texasholdembonus/exec:
    post:
      tags:
        - texasholdembonus
      summary: Execute a Texas Hold'em Bonus Poker game command
      description: |
        Send a command to advance the Texas Hold'em Bonus Poker
        (テキサスホールデムボーナスポーカー) session identified by `sessionId`.
        Texas Hold'em Bonus Poker is a single-player casino Texas Hold'em variant
        using a standard 52-card deck. Player places an Ante bet (required) and an
        optional Bonus side bet, receives 2 hole cards, then must either bet 2x Ante
        (Flop bet) to see the flop or fold (forfeit Ante). After the flop and turn
        the player can either Check or Raise (1x Ante). After the river the showdown
        evaluates the best 5-card hand from each player's 2 hole cards plus the
        5-card board. Dealer always plays — no qualifier.

        Ante and play bets pay 1:1 on a player win, push on a tie, and lose on a
        dealer win. Ante Bonus pays on the player's hand regardless of outcome:
        Royal Flush 1000:1, Straight Flush 200:1, Four of a Kind 25:1, Full House
        5:1, Flush 4:1, Straight 1:1. Bonus side bet pays on the two hole cards: AA
        30:1, AK suited 25:1, AQ/AJ suited 20:1, AK off 15:1, KK/QQ/JJ 10:1, AQ/AJ
        off 5:1, pocket pair 22-TT 3:1.

        The game proceeds through these phases:

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Bet      | `1`   | Place Ante and optional Bonus side bet |
        | PreFlop  | `2`   | Decide to Play (2x Ante) or Fold |
        | Flop     | `3`   | Decide to Check or Raise (1x Ante) |
        | Turn     | `4`   | Decide to Check or Raise (1x Ante) |
        | End      | `5`   | Round complete; shows result and payout |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `bet`   | `b`     | Place Ante bet (requires `amount`; optional `bonusBet`) |
        | `play`  | `p`     | Pre-flop: bet 2x Ante and reveal the flop |
        | `fold`  | `f`     | Pre-flop: forfeit Ante (Bonus side bet still resolves) |
        | `check` | `c`     | Flop / Turn: advance without an additional bet |
        | `raise` | `ra`    | Flop / Turn: bet 1x Ante and advance |
        | `log`   | `l`     | View action log |
      operationId: texasholdembonusExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TexasHoldemBonusRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Bet 100 Ante with 10 Bonus
                value:
                  command: bet
                  amount: 100
                  bonusBet: 10
                  sessionId: my-session-001
              play:
                summary: Pre-flop bet (2x Ante)
                value:
                  command: play
                  sessionId: my-session-001
              fold:
                summary: Fold pre-flop
                value:
                  command: fold
                  sessionId: my-session-001
              check:
                summary: Check (flop / turn)
                value:
                  command: check
                  sessionId: my-session-001
              raise:
                summary: Raise (1x Ante)
                value:
                  command: raise
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TexasHoldemBonusResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TexasHoldemBonusResponse'

  /casinoholdem/exec:
    post:
      tags:
        - casinoholdem
      summary: Execute a Casino Hold'em game command
      description: |
        Send a command to advance the Casino Hold'em (カジノホールデム) session
        identified by `sessionId`. Casino Hold'em is a single-player casino variant
        of Texas Hold'em using a standard 52-card deck. Player places an Ante bet
        (required) and an optional AA Bonus side bet, receives 2 hole cards and the
        flop (3 community cards) is revealed immediately. After the flop the player
        must either Call (2x Ante) to see the turn and river, or Fold (forfeit Ante;
        the AA Bonus still resolves on the visible 5 cards).

        Dealer must qualify with **Pair of Fours or better**. When dealer does not
        qualify, the Ante still pays per the Ante pay table but the Call bet pushes.

        Ante pay table (player win or dealer no-qualify): Royal Flush 100:1,
        Straight Flush 20:1, Four of a Kind 10:1, Full House 3:1, Flush 2:1, all
        other wins 1:1. Call bet pays 1:1 only when dealer qualifies and player
        wins.

        AA Bonus pay table (uses player's 2 hole + 3 flop cards): Royal Flush
        100:1, Straight Flush 50:1, Four of a Kind 40:1, Full House 30:1, Flush
        20:1, Straight 10:1, Three of a Kind 8:1, Two Pair 7:1, Pair of Aces 7:1.
        Pair of Kings or lower forfeits the AA bet.

        The game proceeds through these phases:

        | Phase | Value | Description |
        |-------|-------|-------------|
        | Bet   | `1`   | Place Ante and optional AA Bonus side bet |
        | Flop  | `2`   | Decide to Call (2x Ante) or Fold |
        | End   | `3`   | Round complete; shows result and payout |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `bet`   | `b`     | Place Ante bet (requires `amount`; optional `bonusBet`) |
        | `call`  | `c`     | Bet 2x Ante and reveal turn/river |
        | `fold`  | `f`     | Forfeit Ante (AA Bonus still resolves) |
        | `log`   | `l`     | View action log |
      operationId: casinoholdemExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CasinoHoldemRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Bet 100 Ante with 10 AA Bonus
                value:
                  command: bet
                  amount: 100
                  bonusBet: 10
                  sessionId: my-session-001
              call:
                summary: Call (2x Ante)
                value:
                  command: call
                  sessionId: my-session-001
              fold:
                summary: Fold at flop
                value:
                  command: fold
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CasinoHoldemResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CasinoHoldemResponse'

  /ohhell/exec:
    post:
      tags:
        - ohhell
      summary: Execute an Oh Hell game command
      description: |
        Send a command to advance the Oh Hell (オー・ヘル) game session identified by
        `sessionId`.  Oh Hell is a 4-player trick-taking game with variable hand sizes
        per round.  Trump suit is determined by the top card of the remaining deck.
        Players bid the exact number of tricks they expect to win; the dealer is
        restricted so total bids cannot equal the number of tricks available ("hook"
        rule).  Scoring: exact bid = 10 + bid; miss = 0 (standard) or -|difference|
        (penalty variant).  The game ends after all rounds are played.

        The game proceeds through these phases:

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Bid       | `0`   | Each player declares expected trick wins |
        | Play      | `1`   | Trick play (follow suit; trump suit wins) |
        | TrickEnd  | `2`   | Trick resolved; advance with `next` |
        | RoundEnd  | `3`   | All tricks played; advance with `nextround` |
        | GameEnd   | `4`   | All rounds completed |

        | Command     | Aliases | Description |
        |-------------|---------|-------------|
        | `reset`     | `r`     | Start / restart a game |
        | `bid`       | `b`     | Place a bid (requires `bid` field, 0-handSize) |
        | `play`      | `p`     | Play a card (requires `cardIndex`) |
        | `next`      | `n`     | Advance to the next trick |
        | `nextround` | `nr`    | Score the round and start the next |
        | `hint`      | `h`     | Get recommended bid or play |
        | `log`       | `l`     | View action log |
      operationId: ohhellExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OhHellRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Bid 2 tricks
                value:
                  command: bid
                  bid: 2
                  sessionId: my-session-001
              play:
                summary: Play card at index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OhHellResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OhHellResponse'

  /ninetynine/exec:
    post:
      tags:
        - ninetynine
      summary: Execute a Ninety-Nine game command
      description: |
        Send a command to advance the Ninety-Nine (ナインティナイン) game session identified
        by `sessionId`.  Ninety-Nine (David Parlett) is a 3-player trick-taking game on a
        36-card pack (A,6,7,8,9,10,J,Q,K × 4 suits).  Each deal 12 cards are dealt to each
        player.  The signature mechanic is bidding by burying: each player sets aside
        exactly 3 cards, whose suits encode the exact trick declaration via ♦=0, ♠=1, ♥=2,
        ♣=3 summed to 0-9.  The buried cards are out of play, leaving 9 cards / 9 tricks
        (must-follow).  Trump is a fixed suit per deal, rotating ♠→♥→♣→♦ by deal number.
        Scoring: exact declaration = 10 + declared tricks plus a success bonus that
        escalates as fewer players succeed (1→+30, 2→+20, 3→+10); miss = 0.  First to the
        target score (default 100) wins.

        The game proceeds through these phases:

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Bid       | `0`   | Each player buries 3 cards to declare their bid |
        | Play      | `1`   | Trick play (follow suit; trump suit wins) |
        | TrickEnd  | `2`   | Trick resolved; advance with `next` |
        | RoundEnd  | `3`   | All 9 tricks played; advance with `nextround` |
        | GameEnd   | `4`   | A player reached the target score |

        | Command     | Aliases | Description |
        |-------------|---------|-------------|
        | `reset`     | `r`     | Start / restart a game |
        | `bid`       | `b`     | Bury 3 cards (requires `buryIndices`, 3 distinct card indices) |
        | `play`      | `p`     | Play a card (requires `cardIndex`) |
        | `next`      | `n`     | Advance to the next trick |
        | `nextround` | `nr`    | Score the deal and start the next |
        | `hint`      | `h`     | Get recommended bury or play |
        | `log`       | `l`     | View action log |
      operationId: ninetynineExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NinetyNineRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Bury cards at indices 0, 1, 2
                value:
                  command: bid
                  buryIndices: [0, 1, 2]
                  sessionId: my-session-001
              play:
                summary: Play card at index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NinetyNineResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NinetyNineResponse'

  /bridge/exec:
    post:
      tags:
        - bridge
      summary: Execute a Contract Bridge game command
      description: |
        Send a command to advance the Contract Bridge (コントラクトブリッジ) game session
        identified by `sessionId`.  Contract Bridge is a 4-player, 2-team trick-taking
        game with a standard 52-card deck (13 cards per player).  Features an auction
        (bidding) phase with Pass/Double/Redouble, a dummy hand (partner of declarer),
        and rubber bridge scoring.  First team to win 2 games wins the rubber.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Bid        | `0`   | Auction phase (players bid to set the contract) |
        | Play       | `1`   | Trick play (follow suit; trump suit wins) |
        | TrickEnd   | `2`   | Trick resolved; advance with `next` |
        | RoundEnd   | `3`   | All 13 tricks played; advance with `nextround` |
        | GameEnd    | `4`   | A team won the rubber (2 games) |

        | Command     | Aliases | Description |
        |-------------|---------|-------------|
        | `reset`     | `r`     | Start / restart a game |
        | `bid`       | `b`     | Place a bid (requires `bidType`, and `bidLevel`+`bidSuit` for normal bids) |
        | `play`      | `p`     | Play a card (requires `cardIndex`) |
        | `next`      | `n`     | Advance to the next trick |
        | `nextround` | `nr`    | Score the round and start the next |
        | `hint`      | `h`     | Get recommended bid or play |
        | `log`       | `l`     | View action log |
      operationId: bridgeExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BridgeRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bidPass:
                summary: Pass during auction
                value:
                  command: bid
                  bidType: 0
                  sessionId: my-session-001
              bidNormal:
                summary: Bid 1 NoTrump
                value:
                  command: bid
                  bidType: 1
                  bidLevel: 1
                  bidSuit: 5
                  sessionId: my-session-001
              play:
                summary: Play card at index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BridgeResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BridgeResponse'

  /pineapple/exec:
    post:
      tags:
        - pineapple
      summary: Execute a Pineapple Poker game command
      description: |
        Send a command to advance the Pineapple Poker (パイナップルポーカー) game
        session identified by `sessionId`.  Pineapple Poker is a Crazy Pineapple
        variant of Texas Hold'em where each player receives 3 hole cards and must
        discard 1 after the flop betting round.  Supports configurable table sizes:
        4-max (default), 6-max, or 9-max.

        The game proceeds through nine phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Init       | `0`   | Before any cards are dealt |
        | PreFlop    | `1`   | Three hole cards dealt; first betting round |
        | Discard    | `8`   | Each player discards 1 hole card (after flop is dealt) |
        | Flop       | `2`   | Three community cards revealed; betting round |
        | Turn       | `3`   | Fourth community card revealed; betting round |
        | River      | `4`   | Fifth community card revealed; betting round |
        | Showdown   | `5`   | Hands evaluated; winners determined |
        | End        | `6`   | Round complete; results displayed |
        | Rebuy      | `7`   | Rebuy/addon decision phase |

        | Command     | Aliases | Description |
        |-------------|---------|-------------|
        | `reset`     | `r`     | Start / restart a game |
        | `fold`      | `f`     | Fold and forfeit the pot |
        | `check`     | `ck`    | Pass without betting (only when no outstanding bet) |
        | `call`      | `c`     | Match the current bet |
        | `bet`       | `b`     | Place a bet (use `amount`) |
        | `raise`     | `ra`    | Raise by a specified amount on top of calling (use `amount`) |
        | `allin`     | `a`     | Go all-in with all remaining chips |
        | `discard`   | `d`     | Discard a hole card (use `cardIdx` during Discard phase) |
        | `rebuy`     | `rb`    | Buy back chips during rebuy period |
        | `skiprebuy` | `sr`    | Skip rebuy opportunity |
        | `addon`     | `ad`    | Add chips (one-time addon purchase) |
        | `skipaddon` | `sa`    | Skip addon opportunity |
        | `quit`      | `q`     | End the session |
      operationId: pineappleExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PineappleRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              resetCustomBlinds:
                summary: Start with custom blinds
                value:
                  command: reset
                  smallBlind: 10
                  bigBlind: 20
                  sessionId: my-session-001
              fold:
                summary: Fold
                value:
                  command: fold
                  sessionId: my-session-001
              call:
                summary: Call
                value:
                  command: call
                  sessionId: my-session-001
              bet:
                summary: Place a bet of 40
                value:
                  command: bet
                  amount: 40
                  sessionId: my-session-001
              discard:
                summary: Discard hole card at index 1
                value:
                  command: discard
                  cardIdx: 1
                  sessionId: my-session-001
              allin:
                summary: Go all-in
                value:
                  command: allin
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PineappleResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PineappleResponse'

  /crazypineapple/exec:
    post:
      tags:
        - crazypineapple
      summary: Execute a Crazy Pineapple Poker game command
      description: |
        Send a command to advance the Crazy Pineapple Poker
        (クレイジーパイナップル) game session identified by `sessionId`.
        Crazy Pineapple is a Pineapple variant that shares the same request
        and response schemas (`PineappleRequest` / `PineappleResponse`) and
        the same command set (fold/check/call/bet/raise/allin/discard/
        rebuy/skiprebuy/addon/skipaddon/quit).

        The key rule difference is **discard timing**: Crazy Pineapple
        performs the discard phase *after* the flop betting round (before
        the turn is dealt), whereas Pineapple discards immediately after
        the flop is dealt (before flop betting). Hand-strength evaluation,
        equity calculations, rebuy/addon, muck/show, table sizes (4/6/9-max)
        and CPU play styles (TAG/LAP/TAP/LAG/GTO) are identical to Pineapple.

        Phase, command and field semantics are otherwise identical to
        `/pineapple/exec` — see that endpoint's description for the full
        phase table and field reference.
      operationId: crazyPineappleExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PineappleRequest'
            examples:
              reset:
                summary: Start a new Crazy Pineapple game
                value:
                  command: reset
                  sessionId: my-session-001
              discardAfterFlopBetting:
                summary: Discard hole card at index 1 (allowed only after flop betting)
                value:
                  command: discard
                  cardIdx: 1
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PineappleResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PineappleResponse'

  /irishpoker/exec:
    post:
      tags:
        - irishpoker
      summary: Execute an Irish Poker game command
      description: |
        Send a command to advance the Irish Poker (アイリッシュポーカー)
        game session identified by `sessionId`.
        Irish Poker is a Pineapple variant that shares the same request
        and response schemas (`PineappleRequest` / `PineappleResponse`) and
        the same command set (fold/check/call/bet/raise/allin/discard/
        rebuy/skiprebuy/addon/skipaddon/quit).

        4 hole cards are dealt (instead of 3). After the flop betting round,
        players discard 2 cards (instead of 1), then continue with 2 hole
        cards like standard Hold'em.

        Phase, command and field semantics are otherwise identical to
        `/pineapple/exec` — see that endpoint's description for the full
        phase table and field reference.
      operationId: irishPokerExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PineappleRequest'
            examples:
              reset:
                summary: Start a new Irish Poker game
                value:
                  command: reset
                  sessionId: my-session-001
              discardAfterFlopBetting:
                summary: Discard hole card at index 2 (called twice to discard 2 cards)
                value:
                  command: discard
                  cardIdx: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PineappleResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PineappleResponse'

  /speed/exec:
    post:
      tags:
        - speed
      summary: Execute a Speed (スピード) game command
      description: |
        Send a command to advance the Speed game session identified by
        `sessionId`.  Speed is a 2-player (1 human vs 1 CPU) real-time card
        game.  Players play cards ±1 rank onto two shared center piles.
        When both players are stuck, they flip new cards onto the center piles.
        First player to empty all cards wins.

        The game proceeds through three phases:

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Play     | `0`   | Players play cards onto center piles |
        | Stuck    | `1`   | Both players are stuck; flip new center cards |
        | GameEnd  | `2`   | A player has emptied all cards |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `play`  | `p`     | Play a card onto a center pile (requires `cardIndex` and `pileIndex`) |
        | `flip`  | `f`     | Flip new cards onto center piles (only in Stuck phase) |
        | `hint`  | `h`     | Get a hint for the next move |
        | `log`   | `l`     | View action log |
      operationId: speedExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SpeedRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play card at hand index 2 onto center pile 0
                value:
                  command: play
                  cardIndex: 2
                  pileIndex: 0
                  sessionId: my-session-001
              flip:
                summary: Flip new center cards when stuck
                value:
                  command: flip
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpeedResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpeedResponse'

  /gofish/exec:
    post:
      tags:
        - gofish
      summary: Execute a Go Fish (ゴーフィッシュ) game command
      description: |
        Send a command to advance the Go Fish game session identified by
        `sessionId`.  Go Fish is a 4-player (1 human vs 3 CPU) card game.
        Players ask opponents for a specific rank; if the opponent holds it
        they surrender all matching cards, otherwise the asker draws from
        the stock pile ("Go Fish").  Collect all 4 cards of one rank to
        form a "book".  Most books wins.

        The game proceeds through two phases:

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Play     | `0`   | Players take turns asking for ranks |
        | GameEnd  | `1`   | All 13 books formed or stock exhausted |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `ask`   | `a`     | Ask an opponent for a rank (requires `target` and `rank`) |
        | `sd`    | —       | Set CPU difficulty (requires `amount`: 0=Easy/1=Normal/2=Hard) |
        | `log`   | `l`     | View action log |
      operationId: gofishExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GoFishRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              ask:
                summary: Ask player 1 for rank 7
                value:
                  command: ask
                  target: 1
                  rank: 7
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoFishResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoFishResponse'

  /golf/exec:
    post:
      tags:
        - golf
      summary: Execute a Golf Solitaire game command
      description: |
        Send a command to advance the Golf Solitaire (ゴルフ) game session identified by
        `sessionId`.  Golf is a single-player solitaire card game using a
        standard 52-card deck.  Seven columns of five face-up cards (35 total) form
        the tableau.  Remove exposed cards (bottom-most per column) that are ±1 rank
        from the waste pile top card (K-A wrap).  Win by removing all 35 tableau cards.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | All tableau cards removed; player wins |
        | GameOver   | `2`   | No more moves available; game over |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset`        | `r`  | Start / restart a game |
        | `draw`         | `d`  | Draw a card from stock to waste |
        | `remove`       | `rm` | Remove an exposed card ±1 rank from waste top |
        | `giveup`       | `g`  | Give up the current game |
        | `hint`         | `h`  | Get a hint for the next move |
        | `undo`         | `u`  | Undo the last action |
        | `undo_n`       |      | Undo N actions (requires `n` parameter) |
        | `log`          | `l`  | View action log |
      operationId: golfExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, draw, d, remove, rm, giveup, g, hint, h, undo, u, undo_n, log, l]
                col:
                  type: integer
                  description: Column index (0-6) for remove command
                n:
                  type: integer
                  description: Number of undos to perform (required when command is undo_n)
                  example: 3
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              draw:
                summary: Draw from stock
                value:
                  command: draw
                  sessionId: my-session-001
              remove:
                summary: Remove a tableau card
                value:
                  command: remove
                  col: 3
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  layout:
                    type: array
                    items:
                      type: array
                      items:
                        type: object
                        properties:
                          card:
                            $ref: '#/components/schemas/Card'
                          removed:
                            type: boolean
                          exposed:
                            type: boolean
                  stockCount:
                    type: integer
                  waste:
                    type: array
                    items:
                      $ref: '#/components/schemas/Card'
                  phase:
                    type: integer
                  moveCount:
                    type: integer
                  canUndo:
                    type: boolean
                  isStalemate:
                    type: boolean
                  undoToEscape:
                    type: integer
                    description: Number of undos needed to reach a non-stalemate state (0 if not stalemate, -1 if no escape)
                    example: 0
                  message:
                    type: string
                  messageCode:
                    type: string
                  messageParams:
                    type: object
                  hint:
                    type: object
                    properties:
                      type:
                        type: string
                      col:
                        type: integer
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  layout:
                    type: array
                    items:
                      type: array
                  stockCount:
                    type: integer
                  waste:
                    type: array
                    items:
                      $ref: '#/components/schemas/Card'
                  phase:
                    type: integer
                  moveCount:
                    type: integer
                  message:
                    type: string

  /pigtail/exec:
    post:
      tags:
        - pigtail
      summary: Execute a Pig's Tail game command
      description: |
        Send a command to advance the Pig's Tail (ぶたのしっぽ) game session
        identified by `sessionId`.  Pig's Tail is a matching card game for 4
        players (1 human + 3 CPU) using a standard 52-card deck.  Cards are
        placed face-down in a circle pile.  Players take turns drawing 1 card
        and placing it face-up on the center pile.  If the drawn card's suit
        matches the center top card's suit, the player takes all center cards
        as a penalty.  When the circle pile is empty, the player with the most
        cards loses.

        The game proceeds through these phases:

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Play     | `0`   | Game in progress |
        | GameEnd  | `1`   | Circle pile empty; game over |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `draw`  | `d`     | Draw a card from the circle pile |
        | `log`   | `l`     | View action log |
      operationId: pigtailExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, draw, d, log, l]
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              draw:
                summary: Draw a card from circle pile
                value:
                  command: draw
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  players:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: integer
                        isHuman:
                          type: boolean
                        cardCount:
                          type: integer
                        cards:
                          type: array
                          items:
                            $ref: '#/components/schemas/Card'
                  circleCount:
                    type: integer
                    description: Remaining cards in the circle pile
                  centerTop:
                    nullable: true
                    allOf:
                      - $ref: '#/components/schemas/Card'
                    description: Top card of the center pile (null if empty)
                  centerCount:
                    type: integer
                    description: Number of cards in the center pile
                  currentTurn:
                    type: integer
                  gameEndFlag:
                    type: boolean
                  loserIdx:
                    type: integer
                    description: Index of the losing player (-1 if game not ended)
                  lastDrawCard:
                    nullable: true
                    allOf:
                      - $ref: '#/components/schemas/Card'
                  lastPenalty:
                    type: boolean
                    description: Whether the last draw triggered a suit-match penalty
                  cpuActions:
                    type: array
                    items:
                      type: object
                      properties:
                        drawPlayerIdx:
                          type: integer
                        drawnCard:
                          nullable: true
                          allOf:
                            - $ref: '#/components/schemas/Card'
                        penaltyFlag:
                          type: boolean
                        penaltyCount:
                          type: integer
                        hesitationMs:
                          type: integer
                  humanAction:
                    nullable: true
                    type: object
                    properties:
                      drawPlayerIdx:
                        type: integer
                      drawnCard:
                        nullable: true
                        allOf:
                          - $ref: '#/components/schemas/Card'
                      penaltyFlag:
                        type: boolean
                      penaltyCount:
                        type: integer
                  message:
                    type: string
                  messageCode:
                    type: string
                  messageParams:
                    type: object
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  players:
                    type: array
                    items:
                      type: object
                  circleCount:
                    type: integer
                  centerCount:
                    type: integer
                  gameEndFlag:
                    type: boolean
                  loserIdx:
                    type: integer
                  message:
                    type: string

  /sevencardstud/exec:
    post:
      tags:
        - sevencardstud
      summary: Execute a Seven Card Stud game command
      description: |
        Send a command to advance the Seven Card Stud game session identified by
        `sessionId`.  Seven Card Stud is a classic stud poker variant with no
        community cards.  Each player receives 2 face-down hole cards and 1
        face-up door card on Third Street, then 1 face-up card each on Fourth
        through Sixth Street, and 1 final face-down card on Seventh Street.
        Uses antes and bring-in (lowest door card) instead of blinds.  Betting
        order changes each street based on best visible hand (door cards).
        Best 5 out of 7 cards wins.

        The game supports configurable table sizes: 2-max through 7-max
        (default 4, i.e., 1 human + 3 CPU).

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Init       | `0`   | Before any cards are dealt |
        | ThirdSt    | `1`   | 2 hole cards + 1 door card; first betting round (bring-in) |
        | FourthSt   | `2`   | 4th card (face-up); betting round |
        | FifthSt    | `3`   | 5th card (face-up); betting round |
        | SixthSt    | `4`   | 6th card (face-up); betting round |
        | SeventhSt  | `5`   | 7th card (face-down); final betting round |
        | Showdown   | `6`   | Hands evaluated; winners determined |
        | End        | `7`   | Round complete; results displayed |
        | Rebuy      | `8`   | Rebuy/addon decision phase |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `fold`  | `f`     | Fold and forfeit the pot |
        | `check` | `ck`    | Pass without betting (only when no outstanding bet) |
        | `call`  | `c`     | Match the current bet |
        | `bet`   | `b`     | Place a bet (use `amount`) |
        | `raise` | `ra`    | Raise by a specified amount on top of calling (use `amount`) |
        | `allin` | `a`     | Go all-in with all remaining chips |
        | `rebuy` | `rb`    | Buy back chips during rebuy period |
        | `skiprebuy` | `sr` | Skip rebuy opportunity |
        | `addon` | `ad`    | Add chips (one-time addon purchase) |
        | `skipaddon` | `sa` | Skip addon opportunity |
        | `muck`  | `m`     | Muck (hide) hand at showdown |
        | `show`  | `sh`    | Show hand at showdown |
        | `log`   | `l`     | View action log |
        | `quit`  | `q`     | End the session |
      operationId: sevencardstudExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SevenCardStudRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              resetCustomAnte:
                summary: Start with custom ante and bring-in
                value:
                  command: reset
                  ante: 5
                  bringIn: 10
                  smallBet: 20
                  bigBet: 40
                  sessionId: my-session-001
              fold:
                summary: Fold
                value:
                  command: fold
                  sessionId: my-session-001
              call:
                summary: Call
                value:
                  command: call
                  sessionId: my-session-001
              bet:
                summary: Place a bet of 40
                value:
                  command: bet
                  amount: 40
                  sessionId: my-session-001
              raise:
                summary: Raise by 80
                value:
                  command: raise
                  amount: 80
                  sessionId: my-session-001
              allin:
                summary: Go all-in
                value:
                  command: allin
                  sessionId: my-session-001
              rebuy:
                summary: Accept rebuy
                value:
                  command: rebuy
                  sessionId: my-session-001
              skipaddon:
                summary: Skip addon
                value:
                  command: skipaddon
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SevenCardStudResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SevenCardStudResponse'
              examples:
                paramError:
                  value:
                    players: []
                    pot: 0
                    sidePots: []
                    dealerIdx: 0
                    currentTurn: 0
                    phase: 0
                    gameEndFlag: false
                    lastBet: 0
                    minRaise: 0
                    roundResults: []
                    cpuActions: []
                    message: param error.


  /fivecardstud/exec:
    post:
      tags:
        - fivecardstud
      summary: Execute a Five Card Stud game command
      description: |
        Send a command to advance the Five Card Stud game session identified by
        `sessionId`.  Five Card Stud is poker's oldest stud variant with no
        community cards.  Each player receives 1 face-down hole card and 1
        face-up door card on Second Street, then 1 face-up card each on Third,
        Fourth, and Fifth Street (5 cards total, 1 down + 4 up).  Uses antes and
        bring-in (lowest door card) instead of blinds.  Betting order changes
        each street based on best visible hand (door cards).  Best 5-card hand
        wins.

        The game supports configurable table sizes: 2-max through 6-max
        (default 4, i.e., 1 human + 3 CPU).

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Init       | `0`   | Before any cards are dealt |
        | SecondSt   | `1`   | 1 hole card + 1 door card; first betting round (bring-in) |
        | ThirdSt    | `2`   | 3rd card (face-up); betting round |
        | FourthSt   | `3`   | 4th card (face-up); betting round |
        | FifthSt    | `4`   | 5th card (face-up); final betting round |
        | Showdown   | `5`   | Hands evaluated; winners determined |
        | End        | `6`   | Round complete; results displayed |
        | Rebuy      | `7`   | Rebuy/addon decision phase |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `fold`  | `f`     | Fold and forfeit the pot |
        | `check` | `ck`    | Pass without betting (only when no outstanding bet) |
        | `call`  | `c`     | Match the current bet |
        | `bet`   | `b`     | Place a bet (use `amount`) |
        | `raise` | `ra`    | Raise by a specified amount on top of calling (use `amount`) |
        | `allin` | `a`     | Go all-in with all remaining chips |
        | `rebuy` | `rb`    | Buy back chips during rebuy period |
        | `skiprebuy` | `sr` | Skip rebuy opportunity |
        | `addon` | `ad`    | Add chips (one-time addon purchase) |
        | `skipaddon` | `sa` | Skip addon opportunity |
        | `muck`  | `m`     | Muck (hide) hand at showdown |
        | `show`  | `sh`    | Show hand at showdown |
        | `log`   | `l`     | View action log |
        | `quit`  | `q`     | End the session |
      operationId: fivecardstudExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SevenCardStudRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              fold:
                summary: Fold
                value:
                  command: fold
                  sessionId: my-session-001
              call:
                summary: Call
                value:
                  command: call
                  sessionId: my-session-001
              bet:
                summary: Place a bet of 40
                value:
                  command: bet
                  amount: 40
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SevenCardStudResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SevenCardStudResponse'
              examples:
                paramError:
                  value:
                    players: []
                    pot: 0
                    sidePots: []
                    dealerIdx: 0
                    currentTurn: 0
                    phase: 0
                    gameEndFlag: false
                    lastBet: 0
                    minRaise: 0
                    roundResults: []
                    cpuActions: []
                    message: param error.


  /soko/exec:
    post:
      tags:
        - soko
      summary: Execute a Soko game command
      description: |
        Send a command to advance the Soko (ソッコ) game session identified by
        `sessionId`. Soko, also called Canadian Stud, is Five Card Stud with two
        extra hand ranks. The deal, the bring-in and the betting rounds are
        identical to Five Card Stud -- only the showdown ranking differs.

        Two hands are inserted between one pair and two pair:

        | Hand | Beats | Loses to |
        |------|-------|----------|
        | Four-card straight (exactly four cards in sequence) | One pair | Four-card flush |
        | Four-card flush (exactly four cards of one suit) | Four-card straight | Two pair |

        Both can occur alongside a pair, and Soko ranks them above it, so a hand
        such as `S2 S5 S9 SK HK` plays as a four-card flush rather than a pair of
        kings.

        `handRank` in the response is on **Soko's own scale**, not the shared
        poker scale the other games use: 0 high card, 1 one pair, 2 four-card
        straight, 3 four-card flush, 4 two pair, 5 three of a kind, 6 straight,
        7 flush, 8 full house, 9 four of a kind, 10 straight flush, 11 royal
        flush. Do not compare it against another game's `handRank`.

        The request and response payloads, the commands and the phases are shared
        with Five Card Stud.
      operationId: sokoExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SevenCardStudRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              call:
                summary: Call
                value:
                  command: call
                  sessionId: my-session-001
              bet:
                summary: Place a bet of 40
                value:
                  command: bet
                  amount: 40
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SevenCardStudResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SevenCardStudResponse'


  /fourseasons/exec:
    post:
      tags:
        - fourseasons
      summary: Execute a Four Seasons game command
      description: |
        Send a command to advance the Four Seasons (フォーシーズンズ) game
        session identified by `sessionId`. Also called Corners, it is a
        single-player patience played with a standard 52-card deck.

        Five cards are dealt in a **cross** (the tableau) and one more opens the
        first of four **corner foundations**. That card's rank becomes the
        deal's **base rank**, returned as `baseRank` — the foundations do not
        necessarily start at Ace, and every placement rule reads it.

        | Pile | Builds | Suit | Wraps |
        |------|--------|------|-------|
        | Foundation (4 corners) | up from the base rank | same suit | yes (A follows K) |
        | Cross (5 piles) | down | ignored | yes (K goes under A) |

        Only the **top** card of a cross pile moves, one at a time, and an
        emptied cross space accepts **any** card. The stock turns one card at a
        time to the waste.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | All 52 cards are on the foundations |
        | GameOver   | `2`   | Player gave up |

        | Command        | Aliases     | Description |
        |----------------|-------------|-------------|
        | `reset`        | `r`         | Start / restart a game |
        | `draw`         | `d`         | Turn one card from the stock to the waste |
        | `move`         | `m`         | Move a card (requires `from` and `to` zones) |
        | `giveup`       | `g`         | Give up the current game |
        | `hint`         | `h`         | Return a hint, if any |
        | `autocomplete` | `ac`        | Auto-play every legal move to the foundations |
        | `undo`         | `u`         | Undo the last action |
        | `undo_n`       | -           | Undo the last `n` actions |
        | `log`          | `l`         | View action log |

        Move zones. A foundation always needs `idx`: which corner opens is
        decided by the order they are started, so the target cannot be implied.
        * `from`: `{zone: "waste"}` or `{zone: "tableau", idx: 0..4}`
        * `to`: `{zone: "foundation", idx: 0..3}` or `{zone: "tableau", idx: 0..4}`

      operationId: fourseasonsExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, draw, d, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  $ref: '#/components/schemas/FourSeasonsMoveZone'
                to:
                  $ref: '#/components/schemas/FourSeasonsMoveZone'
                n:
                  type: integer
                  description: Number of moves to undo (for `undo_n` command)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              draw:
                summary: Turn one card from the stock
                value:
                  command: draw
                  sessionId: my-session-001
              moveTableauToFoundation:
                summary: Play the top of cross pile 2 to corner 0
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: tableau
                    idx: 2
                  to:
                    zone: foundation
                    idx: 0
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FourSeasonsResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FourSeasonsResponse'


  /colorado/exec:
    post:
      tags:
        - colorado
      summary: Execute a Colorado game command
      description: |
        Send a command to advance the Colorado (コロラド) game session
        identified by `sessionId`. A single-player patience played with
        **two 52-card decks (104 cards)**.

        Twenty tableau piles get one card each; the other 84 form the stock.
        There are **eight foundations**: indices 0-3 build **up in suit from
        the Ace**, indices 4-7 build **down in suit from the King**. Eight
        piles of thirteen is exactly 104 cards, which is why the count cannot
        be anything else.

        The stock turns one card at a time to the waste and there is **no
        redeal**. The waste top may go to a foundation, or onto **any** tableau
        pile whatever its suit or rank -- which buries whatever was under it.
        A tableau card can only go to a foundation; there are no
        tableau-to-tableau moves. An empty pile may be filled straight from
        the stock.

        Move zones:

        * `from`: `{zone: "waste"}`, `{zone: "tableau", idx: 0..19}` or `{zone: "stock"}`
        * `to`: `{zone: "foundation"}` (the destination pile is chosen by the
          server) or `{zone: "tableau", idx: 0..19}`

      operationId: coloradoExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, draw, d, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  $ref: '#/components/schemas/ColoradoMoveZone'
                to:
                  $ref: '#/components/schemas/ColoradoMoveZone'
                n:
                  type: integer
                  description: Number of moves to undo (for `undo_n` command)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              draw:
                summary: Turn one card from the stock
                value:
                  command: draw
                  sessionId: my-session-001
              moveTableauToFoundation:
                summary: Play the top of pile 2 to a foundation
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: tableau
                    idx: 2
                  to:
                    zone: foundation
              buryWasteOnAPile:
                summary: Drop the waste card onto pile 7, burying what was there
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: waste
                  to:
                    zone: tableau
                    idx: 7
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ColoradoResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ColoradoResponse'
  /cribbagesquares/exec:
    post:
      tags:
        - cribbagesquares
      summary: Execute a Cribbage Squares game command
      description: |
        Send a command to advance the Cribbage Squares (クリベッジ・スクエアズ)
        game session identified by `sessionId`. A single-player patience played
        with a standard 52-card deck, of which 17 cards are used.

        Cards are turned one at a time and placed into any empty cell of a
        **4x4 grid**; a placed card never moves. Once all 16 are down, a
        **17th card - the starter - is turned**, and each of the four rows and
        four columns is scored as a cribbage hand of those four cards plus the
        starter (fifteens, pairs, runs, flush, nobs). The eight hands are
        summed and **61 points clears the game**.

        The starter stays face down until the last placement is committed, so
        `starter` is null for the whole of the playing phase and every hand is
        built without knowing its own fifth card.

        `place` needs `row` and `col`, both 0-based in 0..3.
      operationId: cribbagesquaresExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, place, p, hint, h, undo, u, giveup, g, log, l]
                sessionId:
                  type: string
                row:
                  type: integer
                  description: Row of the target cell (0..3), required for `place`
                col:
                  type: integer
                  description: Column of the target cell (0..3), required for `place`
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              place:
                summary: Put the card in hand into the middle of the grid
                value:
                  command: place
                  sessionId: my-session-001
                  row: 1
                  col: 2
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CribbageSquaresResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CribbageSquaresResponse'
  /diplomat/exec:
    post:
      tags:
        - diplomat
      summary: Execute a Diplomat game command
      description: |
        Send a command to advance the Diplomat (ディプロマット) game session
        identified by `sessionId`. A single-player patience played with
        **two 52-card decks (104 cards)**.

        Eight columns get four cards each face up (32 in all) and the other 72
        form the stock. There are **eight foundations**, each building up in
        suit from the Ace.

        The tableau builds **down ignoring suit**, one card at a time - the
        concession that makes Diplomat the gentle member of the Forty Thieves
        family, which requires same-suit runs. An **empty column takes any
        single card** from another column or from the waste, but never straight
        from the stock. The stock turns one card at a time with **no redeal**.

        Move zones:

        * `from`: `{zone: "tableau", col: 0..7}` or `{zone: "waste"}`
        * `to`: `{zone: "foundation"}` (the server picks the pile) or
          `{zone: "tableau", col: 0..7}`
      operationId: diplomatExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, draw, d, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  $ref: '#/components/schemas/DiplomatMoveZone'
                to:
                  $ref: '#/components/schemas/DiplomatMoveZone'
                n:
                  type: integer
                  description: Number of moves to undo (for `undo_n` command)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              draw:
                summary: Turn one card from the stock
                value:
                  command: draw
                  sessionId: my-session-001
              moveBetweenColumns:
                summary: Drop the top of column 0 onto column 5
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: tableau
                    col: 0
                  to:
                    zone: tableau
                    col: 5
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiplomatResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DiplomatResponse'
  /royalcotillion/exec:
    post:
      tags:
        - royalcotillion
      summary: Execute a Royal Cotillion game command
      description: |
        Send a command to advance the Royal Cotillion (ロイヤルコティヨン) game
        session identified by `sessionId`. A single-player patience played with
        **two 52-card decks (104 cards)**.

        Sixteen single-card tableau slots and four reserve piles of three (28
        cards in all) are dealt face up; the other 76 form the stock.

        **The eight foundations build by twos and wrap.** The four Ace-start
        piles run A,3,5,7,9,J,K,2,4,6,8,10,Q and the four deuce-start piles run
        2,4,6,8,10,Q,A,3,5,7,9,J,K, so each pile passes through all thirteen
        ranks and eight piles absorb all 104 cards.

        A tableau slot holds exactly one card and refills from the stock or the
        waste. **An emptied reserve pile is never refilled.** The stock turns
        one card at a time with no redeal.

        Move zones:

        * `from`: `{zone: "tableau", col: 0..15}`, `{zone: "reserve", col: 0..3}`,
          `{zone: "waste"}` or `{zone: "stock"}`
        * `to`: `{zone: "foundation"}` (the server picks the pile) or
          `{zone: "tableau", col: 0..15}` for an empty slot
      operationId: royalcotillionExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, draw, d, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  $ref: '#/components/schemas/RoyalCotillionMoveZone'
                to:
                  $ref: '#/components/schemas/RoyalCotillionMoveZone'
                n:
                  type: integer
                  description: Number of moves to undo (for `undo_n` command)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              tableauToFoundation:
                summary: Send the card in slot 3 to a foundation
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: tableau
                    col: 3
                  to:
                    zone: foundation
              fillSlotFromStock:
                summary: Refill the emptied slot 3 straight from the stock
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: stock
                  to:
                    zone: tableau
                    col: 3
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoyalCotillionResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RoyalCotillionResponse'
  /crazyquilt/exec:
    post:
      tags:
        - crazyquilt
      summary: Execute a Crazy Quilt game command
      description: |
        Send a command to advance the Crazy Quilt (クレイジーキルト, also known as
        Indian Carpet) game session identified by `sessionId`. A single-player
        patience played with **two 52-card decks (104 cards)**.

        One Ace and one King of each suit are **removed before the deal to seed
        the eight foundations**, so 104 - 8 - 64 leaves **32 cards in the
        stock**. The remaining 64 form an 8x8 quilt, laid vertically and
        horizontally in alternation.

        **A quilt card is available only when one of its SHORT sides is
        exposed** - up or down for a vertically laid card, left or right for a
        horizontal one. An open long side frees nothing, so an intact quilt
        offers exactly sixteen cards. The server reports this per cell in
        `available`; do not re-derive it from the layout.

        The Ace foundations build up in suit and the King foundations build
        down. A quilt card may also be played onto the **waste** when it is one
        rank away in either direction, suit irrelevant - the main way the quilt
        comes apart. The stock turns one card at a time; when it empties, the
        waste is turned over **without shuffling** for a single redeal.

        Move zones:

        * `from`: `{zone: "quilt", col: 0..63}` (cell index, row*8+col) or `{zone: "waste"}`
        * `to`: `{zone: "foundation"}` (the server picks the pile) or `{zone: "waste"}`
      operationId: crazyquiltExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, draw, d, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  $ref: '#/components/schemas/CrazyQuiltMoveZone'
                to:
                  $ref: '#/components/schemas/CrazyQuiltMoveZone'
                n:
                  type: integer
                  description: Number of moves to undo (for `undo_n` command)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              quiltToFoundation:
                summary: Send the card in cell 0 to a foundation
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: quilt
                    col: 0
                  to:
                    zone: foundation
              quiltToWaste:
                summary: Drop the card in cell 7 onto the waste (one rank away)
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: quilt
                    col: 7
                  to:
                    zone: waste
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CrazyQuiltResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CrazyQuiltResponse'
  /germanwhist/exec:
    post:
      tags:
        - germanwhist
      summary: Execute a German Whist game command
      description: |
        Send a command to advance the German Whist (ジャーマンホイスト) game
        session identified by `sessionId`.

        The hand is 26 tricks in two halves. In the first 13 the winner takes
        the face-up card and the loser draws blind from the stock, and **those
        tricks do not score**; only the second 13 count. The suit of the first
        face-up card is trump for the whole hand.
      operationId: execGermanWhist
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `play`)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the third card in hand
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 2
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GermanWhistResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GermanWhistResponse'
  /slobberhannes/exec:
    post:
      tags:
        - slobberhannes
      summary: Execute a Slobberhannes game command
      description: |
        Send a command to advance the Slobberhannes (スロバーハンネス) game
        session identified by `sessionId`.

        An **avoidance** game on the 32-card piquet pack with **no trump**.
        Taking the first trick, the last trick, or the trick holding the Q of
        clubs costs 1 point each; escaping all three scores 1. The **highest**
        total after the configured number of rounds wins.
      operationId: execSlobberhannes
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, next, n, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `play`)
                config:
                  type: object
                  properties:
                    rounds:
                      type: integer
                      description: Rounds to play (1..12, default 4)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the third card in hand
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 2
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SlobberhannesResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SlobberhannesResponse'
  /polignac/exec:
    post:
      tags:
        - polignac
      summary: Execute a Polignac game command
      description: |
        Send a command to advance the Polignac (ポリニャック) game session
        identified by `sessionId`.

        An **avoidance** game on the 32-card piquet pack with **no trump**.
        Only the four jacks score: 1 penalty point each, except the **jack of
        spades (the "Polignac") which costs 2**, so exactly 5 points move each
        round. Before play a player may declare **capot**, undertaking to win
        **all eight tricks** (5 to everyone else on success, 5 to the declarer
        on failure). The **lowest** total wins.
      operationId: execPolignac
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, capot, c, pass, play, p, next, n, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `play`)
                config:
                  type: object
                  properties:
                    rounds:
                      type: integer
                      description: Rounds to play (1..12, default 4)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              capot:
                summary: Undertake to win all eight tricks
                value:
                  command: capot
                  sessionId: my-session-001
              play:
                summary: Play the third card in hand
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 2
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolignacResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolignacResponse'
  /reversis/exec:
    post:
      tags:
        - reversis
      summary: Execute a Reversis game command
      description: |
        Send a command to advance the Reversis (レヴェルシ) game session
        identified by `sessionId`.

        A 4-player **avoidance** game on a 48-card pack (a standard 52 with the
        four 10s removed), **no trump**. Taking a trick costs its card points
        (A=4, K=3, Q=2, J=1 -- 40 a round). The **Quinola (J of hearts)** and
        the **A of diamonds** cost 5 more each **and 5 chips into the pool**.
        Everyone antes each round; the fewest penalty points takes the pool
        whole, and a tie carries it into the next round. Most chips wins.
      operationId: execReversis
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, next, n, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `play`)
                config:
                  type: object
                  properties:
                    rounds:
                      type: integer
                      description: Rounds to play (1..12, default 4)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the third card in hand
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 2
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReversisResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReversisResponse'
  /rams/exec:
    post:
      tags:
        - rams
      summary: Execute a Rams game command
      description: |
        Send a command to advance the Rams (ラムス) game session identified by
        `sessionId`.

        A **pot game for 3-5 players** (the table size is configurable) on the
        32-card piquet pack. Five cards each, trump from the card turned after
        the deal. Everyone antes, then each player chooses to **play** (`in`) or
        **drop** (`out`); only those who played take part in the tricks.
        **Playing and taking no trick at all costs an extra payment into the
        pot**, while anyone who took tricks shares the pot in proportion. Most
        chips after the configured rounds wins.
      operationId: execRams
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, in, play, out, pass, card, c, next, n, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `card`)
                config:
                  type: object
                  properties:
                    playerCnt:
                      type: integer
                      description: Players at the table (3..5, default 4)
                    rounds:
                      type: integer
                      description: Rounds to play (1..12, default 4)
            examples:
              reset:
                summary: Start a new five-player game
                value:
                  command: reset
                  sessionId: my-session-001
                  config:
                    playerCnt: 5
              playIn:
                summary: Enter this round
                value:
                  command: in
                  sessionId: my-session-001
              card:
                summary: Play the third card in hand
                value:
                  command: card
                  sessionId: my-session-001
                  cardIndex: 2
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RamsResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RamsResponse'
  /tarabish/exec:
    post:
      tags:
        - tarabish
      summary: Execute a Tarabish game command
      description: |
        Send a command to advance the Tarabish (タラビッシュ) game session
        identified by `sessionId`.

        A **Jass-family** partnership game from Cape Breton: 36 cards, 4 players
        in two partnerships (seats 0+2 vs 1+3), nine cards each. In trump the
        **J (Jass) = 20 and the 9 (Menel) = 14 outrank the ace**, in strength as
        well as points.

        **The deal is in two stages**, because nine each is the whole deck: six
        are dealt, the next card is turned to propose trump, and once a player
        takes it the rest is dealt with **the turned card going to the dealer**.
        Each player in turn may `take` or `pass`; **the dealer cannot pass**.

        Melds are computed from the deal: a run of three scores 20, four or more
        scores 50, and bella (trump K+Q) scores 20.
      operationId: execTarabish
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, take, t, pass, play, p, next, n, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `play`)
                config:
                  type: object
                  properties:
                    target:
                      type: integer
                      description: Points needed to win (100..1000, default 500)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              take:
                summary: Take the turned suit as trump
                value:
                  command: take
                  sessionId: my-session-001
              play:
                summary: Play the third card in hand
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 2
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TarabishResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TarabishResponse'
  /baloot/exec:
    post:
      tags:
        - baloot
      summary: Execute a Baloot game command
      description: |
        Send a command to advance the Baloot (バルート) game session
        identified by `sessionId`.

        The Gulf's most-played trick-taking game, descended from Belote: 32
        cards, 4 players in two partnerships (seats 0+2 vs 1+3), eight cards
        each. **The declared mode swaps the rank order itself**, not merely
        whether a trump exists:

        - **Sun** -- no trump; every suit runs A=11 > 10 > K=4 > Q=3 > J=2 > 9 >
          8 > 7, for 120 a round.
        - **Hokom** -- the declared trump suit runs J=20 > 9=14 > A=11 > 10 >
          K=4 > Q=3 > 8 > 7, plain suits as in Sun, for 152 a round.

        Both add 10 for the last trick. **The deal is in two stages**, because
        eight each is the whole deck: five are dealt, the mode is declared
        around the table, and the remaining three each follow. Each player in
        turn may declare `sun`, declare `hokom` (with a `suit`), or `pass`;
        **the dealer cannot pass**.

        Baloot -- the trump K+Q in one hand, worth 20 -- is a **Hokom-only**
        bonus, since Sun has no trump for it to be in.
      operationId: execBaloot
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, sun, hokom, pass, play, p, next, n, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `play`)
                suit:
                  type: integer
                  description: Trump suit 1..4 (1=S 2=C 3=H 4=D); required for `hokom`
                config:
                  type: object
                  properties:
                    target:
                      type: integer
                      description: Points needed to win (50..500, default 152)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              sun:
                summary: Declare Sun (no trump)
                value:
                  command: sun
                  sessionId: my-session-001
              hokom:
                summary: Declare Hokom with hearts as trump
                value:
                  command: hokom
                  sessionId: my-session-001
                  suit: 3
              play:
                summary: Play the third card in hand
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 2
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BalootResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BalootResponse'
  /estimation/exec:
    post:
      tags:
        - estimation
      summary: Execute an Estimation game command
      description: |
        Send a command to advance the Estimation (エスティメーション) game
        session identified by `sessionId`.

        The Gulf's household member of the Oh Hell family: 52 cards, four
        players each for themselves, thirteen cards apiece. The dealer names
        trump, then each player calls the tricks they expect.

        **Only an exact call scores.** Making it is worth `10 + call` and
        missing it costs the same, so one short costs exactly as much as five
        over -- the size of the call is the size of the bet. Two calls swing
        wider:

        - **Dash Call** -- calling 0, i.e. taking no trick with thirteen
          cards. A flat ±23.
        - **Risk** -- the highest call at the table, resolved to the earliest
          caller on a tie. Doubles whatever the call was worth.

        **The last caller may not make the total 13**, which would let everyone
        make their call at once; `restrictedBid` carries that barred value so a
        client can disable it rather than have the server reject it. Note this
        is the same restriction `OhHell` already places on its dealer.
      operationId: execEstimation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, trump, t, bid, b, play, p, next, n, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `play`)
                suit:
                  type: integer
                  description: Trump suit 1..4 (1=S 2=C 3=H 4=D); required for `trump`
                bid:
                  type: integer
                  description: Tricks called, 0..13; required for `bid`. 0 is a Dash Call, not an omission.
                config:
                  type: object
                  properties:
                    rounds:
                      type: integer
                      description: Rounds before the game ends (1..18, default 5)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              trump:
                summary: Make hearts trump
                value:
                  command: trump
                  sessionId: my-session-001
                  suit: 3
              bid:
                summary: Call four tricks
                value:
                  command: bid
                  sessionId: my-session-001
                  bid: 4
              dash:
                summary: Make a Dash Call
                value:
                  command: bid
                  sessionId: my-session-001
                  bid: 0
              play:
                summary: Play the third card in hand
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 2
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EstimationResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EstimationResponse'
  /israeliwhist/exec:
    post:
      tags:
        - israeliwhist
      summary: Execute an Israeli Whist game command
      description: |
        Send a command to advance the Israeli Whist (イスラエリホイスト) game
        session identified by `sessionId`.

        52 cards, four players each for themselves, thirteen apiece.
        **Bidding happens twice**, which is what separates it from the
        one-auction games:

        1. **Auction** -- players call "n tricks + a suit" (minimum 5), each
           call beating the last by number or, at equal number, by suit rank
           (clubs < diamonds < hearts < spades -- *not* the order the
           `CardDesign` constants sit in). Dropping out is final, and the last
           player standing with nobody having bid must bid rather than pass, so
           a trump is always settled. The winner's suit becomes trump and their
           number becomes their personal quota.
        2. **Calling** -- everyone, the winner included, calls their own target.
           The winner must call at least their quota (`minimumBid`), and the
           last caller may not make the total 13 (`restrictedBid`).

        Scoring: an exact call of n scores `n^2 + 10`, an exact 0 scores 25, and
        missing costs 10 per trick of difference. If all four are exact, or all
        four miss, the round doubles either way.
      operationId: execIsraeliWhist
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, auction, a, pass, bid, b, play, p, next, n, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `play`)
                suit:
                  type: integer
                  description: Trump suit 1..4 (1=S 2=C 3=H 4=D); required for `auction`
                bid:
                  type: integer
                  description: Tricks -- 5..13 for `auction`, 0..13 for `bid`. Required for both; 0 is a call, not an omission.
                config:
                  type: object
                  properties:
                    rounds:
                      type: integer
                      description: Rounds before the game ends (1..20, default 4)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              auction:
                summary: Bid seven tricks in hearts
                value:
                  command: auction
                  sessionId: my-session-001
                  bid: 7
                  suit: 3
              pass:
                summary: Drop out of the auction
                value:
                  command: pass
                  sessionId: my-session-001
              bid:
                summary: Call four tricks
                value:
                  command: bid
                  sessionId: my-session-001
                  bid: 4
              play:
                summary: Play the third card in hand
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 2
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IsraeliWhistResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IsraeliWhistResponse'
  /hokm/exec:
    post:
      tags:
        - hokm
      summary: Execute a Hokm game command
      description: |
        Send a command to advance the Hokm (ホクム) game session identified by
        `sessionId`.

        Iran's most-played trick-taking game: 52 cards, four players in two
        partnerships (seats 0+2 vs 1+3), thirteen cards each.

        **A hand does not play out all thirteen tricks.** The first partnership
        to **seven** takes it and the remaining cards are never played, so
        `teamTricks` against `tricksToWin` -- not `trickNumber` -- is the
        progress of a hand. (7 + 7 = 14 > 13, so a hand always settles.)

        The **hakem** is dealt **five cards only** and names trump from those
        alone; the rest is dealt afterwards to bring everyone to thirteen. The
        hakem keeps the role while their team keeps winning and passes it to
        the left only on a loss.

        Taking a hand scores 1 hand point. Taking it while the losers get **no
        trick at all** is a **Kot**, worth 2 -- `lastHandKot` says which
        happened, because a jump of two points is otherwise unexplained.
      operationId: execHokm
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, trump, t, play, p, next, n, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `play`)
                suit:
                  type: integer
                  description: Trump suit 1..4 (1=S 2=C 3=H 4=D); required for `trump`
                config:
                  type: object
                  properties:
                    target:
                      type: integer
                      description: Hand points needed to win (1..13, default 7)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              trump:
                summary: Declare hearts as trump
                value:
                  command: trump
                  sessionId: my-session-001
                  suit: 3
              play:
                summary: Play the third card in hand
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 2
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HokmResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HokmResponse'
  /shelem/exec:
    post:
      tags:
        - shelem
      summary: Execute a Shelem game command
      description: |
        Send a command to advance the Shelem (シェレム) game session identified
        by `sessionId`.

        52 cards dealt as **12 each plus a four-card widow** (12x4 + 4 = 52
        exactly), four players in two partnerships, **12 tricks**.

        **What is bid is the score itself, not a number of tricks**: from 55 to
        100 in steps of 5 -- the ceiling being exactly the **100 card points**
        a hand holds -- and only three ranks score at all (A and 10 are 10, the 5
        is 5, everything else 0). A contract above 100 could never be made,
        which is what caps the bidding.

        Whoever wins the bidding takes the widow into hand (16 cards),
        **discards four** and names trump. Making the contract in card points
        adds it; missing subtracts the same amount, and the other side banks
        the card points it took.

        Declaring **Shelem** replaces the number with a claim on every trick:
        ±200, settled the instant one trick is lost rather than playing the
        hand out. The last bidder standing with nobody having bid may not pass.
      operationId: execShelem
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bid, b, shelem, pass, discard, d, play, p, next, n, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `play`)
                suit:
                  type: integer
                  description: Trump suit 1..4 (1=S 2=C 3=H 4=D); required for `discard`
                bid:
                  type: integer
                  description: Points bid, 55..100 in steps of 5; required for `bid`
                discards:
                  type: array
                  description: Exactly four hand indices to discard; required for `discard`
                  items:
                    type: integer
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Bid 130 points
                value:
                  command: bid
                  sessionId: my-session-001
                  bid: 90
              shelem:
                summary: Claim every trick
                value:
                  command: shelem
                  sessionId: my-session-001
              discard:
                summary: Discard four cards and make hearts trump
                value:
                  command: discard
                  sessionId: my-session-001
                  discards: [0, 3, 7, 11]
                  suit: 3
              play:
                summary: Play the third card in hand
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 2
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ShelemResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ShelemResponse'
  /mendikot/exec:
    post:
      tags:
        - mendikot
      summary: Execute a Mendikot game command
      description: |
        Send a command to advance the Mendikot (メンディコット) game session
        identified by `sessionId`.

        India's household partnership trick-taker: 52 cards, four players in
        two partnerships (seats 0+2 against 1+3), thirteen cards apiece.

        **What decides the hand is neither points nor tricks but the four
        tens.** Three of them takes it outright; only a two-apiece split falls
        back to the trick count. All four is a **Mendikot** (2 points) and
        every trick is a **Whitewash** (3 points). Since the tens either split
        two-two or land three-or-more on one side, no hand is ever undecided.

        **There is no trump command.** Trump is set by whatever card the first
        player who cannot follow suit chooses to play, so `trumpSuit` stays `0`
        — possibly for the whole hand, if everyone always follows — and then
        settles mid-trick. `trumpChooserIdx` names the seat that set it.

        Note the ten is only the fifth-highest rank (A > K > Q > J > 10), so
        the cards that decide the game are not the ones that win tricks.
      operationId: execMendikot
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, next, n, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `play`)
                config:
                  type: object
                  properties:
                    target:
                      type: integer
                      description: Hand points needed to win (1..20, default 3)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the third card in hand
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 2
              next:
                summary: Deal the next hand
                value:
                  command: next
                  sessionId: my-session-001
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MendikotResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MendikotResponse'
  /bhabhi/exec:
    post:
      tags:
        - bhabhi
      summary: Execute a Bhabhi game command
      description: |
        Send a command to advance the Bhabhi (バービー) game session identified
        by `sessionId`.

        India and Pakistan's household **avoidance** game: the whole 52-card
        pack dealt among three to seven players, no trumps and no scoring.
        52 does not divide evenly for 3/5/6/7 players, so hands differ by at
        most one card and no stock remains.

        **It names a loser, not a winner.** Players who empty their hand simply
        drop out; the last one still holding cards is the **Bhabhi**. `rank`
        records the order seats got out and carries no other meaning.

        Following the led suit is compulsory. A player who cannot follow plays
        anything and **takes every card on the table into hand** — including
        the one they just played. **A trick everyone followed is discarded
        instead**, which is the only way cards leave play.

        **The player who picks up does not lead next** — the seat after them
        does. Letting the picker lead sends a lone card of a suit nobody else
        holds back and forth between two hands indefinitely.

        Because cards only leave play on a fully-followed trick, a distribution
        where somebody is always void can circulate forever. At
        `stalemateTricks` tricks the game is called as deadlocked, `stalemate`
        is set, and the seat holding the most cards becomes the Bhabhi. Games
        that decide normally take far fewer tricks than that.

        There is **no `next` command** — the game has no hand boundaries.
      operationId: execBhabhi
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `play`)
                config:
                  type: object
                  properties:
                    playerCnt:
                      type: integer
                      description: How many seats play (3..7, default 4)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              resetSixPlayers:
                summary: Deal a six-player game
                value:
                  command: reset
                  sessionId: my-session-001
                  config:
                    playerCnt: 6
              play:
                summary: Play the third card in hand
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 2
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BhabhiResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BhabhiResponse'
  /teendopaanch/exec:
    post:
      tags:
        - teendopaanch
      summary: Execute a 3-2-5 game command
      description: |
        Send a command to advance the 3-2-5 (ティーン・ドー・パーンチ) game
        session identified by `sessionId`.

        Northern India's asymmetric three-handed trick-taker, played with a
        **30-card pack**: eights through aces in all four suits (28) plus the
        **♠7 and ♥7**. Ten cards each for ten tricks — 28 cards would be two
        short, which is why the real game adds those two sevens.

        **The targets are assigned, not bid.** The three seats owe **3, 2 and
        5** tricks (3 + 2 + 5 = 10, so tricks divide exactly) and there is no
        bidding phase at all. The roles rotate, so three rounds give every seat
        one turn at each.

        Only the five-target seat (`fivePlayerIdx`) names trump, and it does so
        from **its first five cards**, before the rest are dealt.

        Making your number *or more* counts as met. **Extra tricks score
        nothing**: the surplus instead entitles you, at the next deal, to take
        that many of a short player's **best cards** in exchange for your
        worst. `lastExchange` reports how many cards moved, because nothing
        else on the board would show it.

        Most targets met over the configured rounds wins; ties have no winner.
      operationId: execTeenDoPaanch
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, trump, t, play, p, next, n, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `play`)
                suit:
                  type: integer
                  description: Trump suit 1..4 (1=S 2=C 3=H 4=D); required for `trump`
                config:
                  type: object
                  properties:
                    rounds:
                      type: integer
                      description: Rounds to play (3..30, default 3 -- one turn each at 3, 2 and 5)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              trump:
                summary: Make hearts trump
                value:
                  command: trump
                  sessionId: my-session-001
                  suit: 3
              play:
                summary: Play the third card in hand
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 2
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeenDoPaanchResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeenDoPaanchResponse'
  /hasenpfeffer/exec:
    post:
      tags:
        - hasenpfeffer
      summary: Execute a Hasenpfeffer game command
      description: |
        Send a command to advance the Hasenpfeffer (ハーゼンプフェファー) game
        session identified by `sessionId`.

        The euchre variant German immigrants carried to America: a **25-card
        pack** (the 24-card euchre deck plus one joker) dealt **six each with
        the odd card face down**, for **six tricks** — not euchre's five.

        **The joker is the highest trump of all** — the Best Bower, above the
        right bower (jack of trumps) and the left bower (jack of the same
        colour). Like the left bower it counts as the trump suit, so a trump
        lead obliges you to follow with it.

        **Bidding is compulsory.** Bids run 3..6 and must beat the standing
        one; `minBid` carries the lowest that would, and is **`0` when the
        maximum is already standing** and nobody can outbid. Once three players
        pass, `mustBid` is true for the dealer, who **cannot pass** — so no
        hand is ever thrown in. Clients should hide the pass control rather
        than offer a move the server refuses.

        The declarer takes the face-down card, making seven, then **discards
        one and names trump in a single `discard` command**.

        Making the contract scores the tricks actually taken, so overshooting
        is never wasted; being euchred hands the bid amount to the other side.
      operationId: execHasenpfeffer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bid, b, discard, d, play, p, next, n, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `play` and `discard`)
                suit:
                  type: integer
                  description: Trump suit 1..4 (1=S 2=C 3=H 4=D); required for `discard`
                bid:
                  type: integer
                  description: >-
                    Tricks bid, 3..6, or 0 to pass. Required for `bid` -- a
                    missing value is rejected rather than defaulted, because
                    defaulting would pass for a player who never chose to.
                config:
                  type: object
                  properties:
                    target:
                      type: integer
                      description: Points needed to win (5..50, default 10)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Bid four tricks
                value:
                  command: bid
                  sessionId: my-session-001
                  bid: 4
              pass:
                summary: Pass
                value:
                  command: bid
                  sessionId: my-session-001
                  bid: 0
              discard:
                summary: Discard the third card and make hearts trump
                value:
                  command: discard
                  sessionId: my-session-001
                  cardIndex: 2
                  suit: 3
              play:
                summary: Play the first card in hand
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 0
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HasenpfefferResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HasenpfefferResponse'
  /sergeantmajor/exec:
    post:
      tags:
        - sergeantmajor
      summary: Execute a Sergeant Major (8-5-3) game command
      description: |
        Send a command to advance the Sergeant Major / 8-5-3
        (サージェントメジャー) game session identified by `sessionId`.

        The British army's asymmetric three-hander: the standard 52-card pack
        dealt **sixteen each, with the four left over going to the dealer as a
        kitty**. 52 does not divide by three, which is exactly why the kitty
        exists. Sixteen tricks per hand.

        **The targets are fixed by seat, never bid.** The dealer owes **8**,
        the seat to their left **5**, the next **3** — and 8 + 5 + 3 = 16
        matches the trick count exactly. The deal rotates, so three rounds give
        every seat one turn at each target. There is no bidding command.

        Only the dealer names trump, and does so before taking the kitty and
        **discarding `discardCount` cards**.

        Scoring is simply tricks minus target, so **overshooting is pure
        profit**. Falling short costs you that many of your **best cards** at
        the next deal, in exchange for the overshooting player's worst;
        `lastExchange` reports how many moved, because nothing else shows it.
      operationId: execSergeantMajor
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, trump, t, discard, d, play, p, next, n, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `play`)
                suit:
                  type: integer
                  description: Trump suit 1..4 (1=S 2=C 3=H 4=D); required for `trump`
                discards:
                  type: array
                  description: Exactly four hand indices; required for `discard`
                  items:
                    type: integer
                config:
                  type: object
                  properties:
                    rounds:
                      type: integer
                      description: >-
                        Rounds to play, 3..30 and always a multiple of 3 -- otherwise
                        one seat would take the 8 more often than the others. Values
                        that are not a multiple are rounded down. Default 3.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              trump:
                summary: Make hearts trump
                value:
                  command: trump
                  sessionId: my-session-001
                  suit: 3
              discard:
                summary: Discard four cards for the kitty
                value:
                  command: discard
                  sessionId: my-session-001
                  discards: [0, 2, 5, 7]
              play:
                summary: Play the third card in hand
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 2
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SergeantMajorResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SergeantMajorResponse'
  /honeymoonbridge/exec:
    post:
      tags:
        - honeymoonbridge
      summary: Execute a Honeymoon Bridge game command
      description: |
        Send a command to advance the Honeymoon Bridge (ハネムーンブリッジ) game
        session identified by `sessionId`.

        Bridge adapted for two, in two halves. Thirteen cards each leaves 26 in
        the stock, and **the first thirteen tricks do not score**: they are
        played without trumps purely so that the winner, then the loser, draws
        a card after each one. 13 x 2 = 26 empties the stock exactly and both
        hands are back to thirteen for the auction.

        The auction runs over levels 1..7 across five denominations, and
        **no-trump ranks above diamonds** (S < C < H < D < NT) — an order the
        1..4 suit codes cannot express. A bid must outbid the table, and two
        consecutive passes close the auction; if both pass immediately the deal
        is thrown in.

        A contract of level *n* needs `6 + n` tricks. Making it scores
        `n * 10` plus 5 per overtrick; going down gives the opponent 10 per
        trick short. `minBidLevel`/`minBidSuit` carry the lowest bid that still
        outbids the table so a client never has to rederive that order — `0`
        there means the contract is at 7NT and pass is the only move.
      operationId: execHoneymoonBridge
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bid, b, pass, play, p, next, n, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `play`)
                level:
                  type: integer
                  description: Contract level 1..7; required for `bid`. Level n needs 6 + n tricks.
                suit:
                  type: integer
                  description: >-
                    Denomination 0..4 (0=NT 1=S 2=C 3=H 4=D); required for
                    `bid`. **0 is no-trump, not an omission** — an omitted suit
                    is rejected rather than defaulted, because defaulting it
                    would buy a no-trump contract nobody named.
                config:
                  type: object
                  properties:
                    target:
                      type: integer
                      description: Points that end the game (50..500, default 100)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Bid three hearts
                value:
                  command: bid
                  sessionId: my-session-001
                  level: 3
                  suit: 3
              notrump:
                summary: Bid two no-trump
                value:
                  command: bid
                  sessionId: my-session-001
                  level: 2
                  suit: 0
              pass:
                summary: Drop out of the auction
                value:
                  command: pass
                  sessionId: my-session-001
              play:
                summary: Play the third card in hand
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 2
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HoneymoonBridgeResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HoneymoonBridgeResponse'
  /minibridge/exec:
    post:
      tags:
        - minibridge
      summary: Execute a Minibridge game command
      description: |
        Send a command to advance the Minibridge (ミニブリッジ) game session
        identified by `sessionId`.

        The teaching variant of bridge: four players in two pairs, thirteen
        cards each, and **no auction**. Every player announces their high-card
        points (A=4, K=3, Q=2, J=1) and the pair holding more declares, the
        seat holding more of it becoming declarer.

        **The four hands always total exactly 40**, so the count can split
        20-20 — that goes to the dealer's side — and the two seats of the
        declaring pair can tie, which goes to the seat nearest the dealer.

        The declarer then names a contract with nothing to outbid, their
        partner's hand goes face up as the **dummy**, and **the declarer plays
        both hands**: `validPlays` is reported for whichever seat is currently
        being controlled, not always seat 0.

        A contract of level *n* needs `6 + n` tricks. Making it scores the
        contract points (20 per trick in a minor, 30 in a major, 40 + 30 in
        no-trump) plus 300 when those reach 100 and 50 when they do not, plus
        500 or 1000 at levels 6 and 7. Going down gives the opponents 50 per
        trick short.
      operationId: execMinibridge
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, contract, c, play, p, next, n, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: >-
                    Index into the hand you are controlling (required for
                    `play`) -- the dummy's hand when it is the dummy's turn and
                    you are the declarer.
                level:
                  type: integer
                  description: Contract level 1..7; required for `contract`. Level n needs 6 + n tricks.
                suit:
                  type: integer
                  description: >-
                    Denomination 0..4 (0=NT 1=S 2=C 3=H 4=D); required for
                    `contract`. **0 is no-trump, not an omission** -- an
                    omitted suit is rejected rather than defaulted.
                config:
                  type: object
                  properties:
                    rounds:
                      type: integer
                      description: >-
                        Deals to play (4..20, default 4). Rounded down to a
                        multiple of four so every seat deals equally -- the
                        dealer's seat breaks HCP ties, so it has to come round
                        evenly.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              contract:
                summary: Choose three no-trump
                value:
                  command: contract
                  sessionId: my-session-001
                  level: 3
                  suit: 0
              major:
                summary: Choose four hearts
                value:
                  command: contract
                  sessionId: my-session-001
                  level: 4
                  suit: 3
              play:
                summary: Play the third card of the hand you control
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 2
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MinibridgeResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MinibridgeResponse'
  /pasur/exec:
    post:
      tags:
        - pasur
      summary: Execute a Pasur game command
      description: |
        Send a command to advance the Pasur (パスール) game session identified
        by `sessionId`.

        Iran's member of the fishing family, for two to four players. Four
        cards go to the table first, which is what makes the deal work for
        every table size: the remaining 48 divides evenly by 2, 3 and 4 players
        at four cards apiece, dealt again whenever every hand empties.

        **A numeral captures a set of table numerals that, with it, adds to
        11** (ace 1 through ten 10), so one card alone never captures — nothing
        is worth 11. **J, Q and K never join the sum**; they take only the same
        rank from the table.

        **Capturing is compulsory when a capture exists**, otherwise scoring
        cards could be pushed onto the table beyond an opponent's reach. An
        absent `table` therefore means "lay this card down", and the server
        rejects it when the card could have captured.

        **Emptying the table is a "soor" and those cards score double.**
        Whatever remains on the table at the end goes to whoever captured last.
        Scoring: each club 1, an extra 1 for 2♣, 3 for 10♦, 1 per ace — exactly
        21 points per deck before any soor doubling.
      operationId: execPasur
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `play`)
                table:
                  type: array
                  description: >-
                    Table indices to capture. **Omitting it lays the card down
                    rather than being a missing parameter** -- but the server
                    rejects that when the card could have captured.
                  items:
                    type: integer
                config:
                  type: object
                  properties:
                    playerCnt:
                      type: integer
                      description: Players at the table (2..4, default 4)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              capture:
                summary: Play your first card and take two table cards
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 0
                  table: [1, 2]
              trail:
                summary: Lay a card down (only legal when it cannot capture)
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 1
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PasurResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PasurResponse'
  /snap/exec:
    post:
      tags:
        - snap
      summary: Execute a Snap game command
      description: |
        Send a command to advance the Snap (スナップ) game session identified by
        `sessionId`.

        Players turn the top of their face-down stock over in turn, and
        whoever calls first when a card matches the rank of the one before it
        takes the whole pile.

        **The trigger moves.** Slapjack always looks for a jack; Snap compares
        against the previous card, so **a single card showing can never be
        snapped**, and only the top two matter — a match buried under a third
        card is not one. `snapAvailable` carries the answer so a client need
        not work it out.

        Calling wrongly costs a card from the caller's stock, face up onto the
        pile. A seat with no stock left is skipped, and collecting every card
        wins.

        **`snap` takes no seat.** The server always calls for seat 0 — letting
        a client name the seat would let it force a CPU into a wrong call and
        pay the penalty for it.

        Simultaneous reflexes are deterministic: each CPU books its call behind
        a deadline drawn from a normal distribution clamped at 80ms, and `tick`
        fires it only once the clock passes that deadline, so a human who calls
        first genuinely wins. Poll `tick` while `pendingKind` is non-zero —
        including during your own turn, since a CPU's call is booked then too.
      operationId: execSnap
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, step, s, snap, n, tick, t, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                config:
                  type: object
                  properties:
                    playerCnt:
                      type: integer
                      description: Players at the table (2..4, default 2)
                    cpuDifficulty:
                      type: integer
                      description: >-
                        0 easy, 1 normal, 2 hard. Moves the mean CPU reaction
                        time (1400 / 900 / 500 ms), floored at 80ms.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              step:
                summary: Turn your top card over
                value:
                  command: step
                  sessionId: my-session-001
              snap:
                summary: Call snap (always for seat 0)
                value:
                  command: snap
                  sessionId: my-session-001
              tick:
                summary: Let a booked CPU reaction fire
                value:
                  command: tick
                  sessionId: my-session-001
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SnapResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SnapResponse'
  /rollingstone/exec:
    post:
      tags:
        - rollingstone
      summary: Execute a Rolling Stone game command
      description: |
        Send a command to advance the Rolling Stone (ローリングストーン, also
        Enflé) game session identified by `sessionId`.

        Germany and France's inside-out trick game, for four to six players.
        **Winning a trick is worth nothing but the lead**, and **failing to
        follow suit puts the whole trick into your hand** — the penalty is
        being given cards rather than taking them. The winner is whoever runs
        out first.

        Everyone gets **eight cards**, with the deck sized to the table:
        4/5/6 players take 32/40/48, each splitting evenly across four suits
        once the low ranks are dropped and the aces kept.

        **`pickup` is its own command**, not `play` with `cardIndex` omitted:
        being unable to follow is a different action, and the server rejects a
        pickup while a legal card remains. `mustPickUp` says which situation
        you are in — read it before `validPlays`, since an empty list of legal
        plays also means "not your turn".

        **The game does not necessarily end.** Cards leave play only on a
        completed trick, so a table that keeps failing to follow recycles the
        same cards. Measured over 400 games per table size, half of
        four-player games did not finish within 300,000 plays, while the
        longest finishing game took 303 tricks — so play stops at **500
        tricks** and goes to the smallest hand.
      operationId: execRollingStone
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, pickup, u, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `play`)
                config:
                  type: object
                  properties:
                    playerCnt:
                      type: integer
                      description: Players at the table (4..6, default 4). The deck is sized to match.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Follow suit with your third card
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 2
              pickup:
                summary: Take the trick into hand (only legal when you cannot follow)
                value:
                  command: pickup
                  sessionId: my-session-001
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RollingStoneResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RollingStoneResponse'
  /lingerlonger/exec:
    post:
      tags:
        - lingerlonger
      summary: Execute a Linger Longer game command
      description: |
        Send a command to advance the Linger Longer (リンガーロンガー) game
        session identified by `sessionId`.

        An American elimination trick game for four to six players.
        **Winning a trick earns exactly one card from the stock and nothing
        else** -- so a player who keeps winning never loses hand size. Run out
        of cards and you are eliminated; the last player still holding any
        wins.

        **You are dealt as many cards as there are players** -- four each at a
        four-player table, six each at six -- and the remainder becomes the
        stock (36 / 27 / 16 cards for 4 / 5 / 6 players). Following suit is
        compulsory.

        **There is no draw command.** Refilling is automatic and belongs to the
        trick winner alone, so `play` is the only move the game accepts.

        Two invariants that look true and are not: hands + trick + stock never
        equals 52 (completed tricks leave play, reported as `discarded`), and
        an empty hand does not mean elimination -- playing your last card
        leaves you in until the trick resolves and the winner's draw settles.

        Unlike every other game here, **the human can be eliminated while play
        continues**; the session keeps running until one seat is left.
      operationId: execLingerLonger
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `play`)
                config:
                  type: object
                  properties:
                    playerCnt:
                      type: integer
                      description: Players at the table (4..6, default 4). You are dealt that many cards.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Follow suit with your third card
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 2
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LingerLongerResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LingerLongerResponse'

  /pig/exec:
    post:
      tags:
        - pig
      summary: Execute a Pig game command
      description: |
        Send a command to advance the Pig (ピッグ, also Donkey) game session
        identified by `sessionId`.

        An American children's party game for three to six players. **A player
        who collects four of a kind says nothing and quietly puts a hand to
        their nose**; everyone else simply copies once they notice, and **the
        last player to notice** takes one letter of P-I-G. Three letters put
        you out, and the last player left wins. Nothing is grabbed and nothing
        is scored -- being slow to react is the only way to lose.

        The deck is players x 4, which is **n ranks x 4 suits** -- a plain
        subset of the standard pack, not a new kind of card. Three to six
        players use 12 / 16 / 20 / 24 cards.

        **Everyone passes one card to the left simultaneously**, so hands stay
        at four cards for the whole game. The server models this as each seat
        choosing in turn: `hasChosenPass` says who has already committed, and
        nothing moves until every seat has.

        **`signal` is its own command**, not `pass` with `cardIndex` omitted:
        noticing is a different action, and the server rejects a signal when
        nobody has signalled. `next` deals the following round -- the game
        stops at RoundEnd so the letter award can be read, since nothing on the
        board records it.
      operationId: execPig
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, pass, p, signal, s, next, n, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `pass`)
                config:
                  type: object
                  properties:
                    playerCnt:
                      type: integer
                      description: Players at the table (3..6, default 4). The deck is sized to match.
                    cpuDifficulty:
                      type: integer
                      description: "0=easy, 1=normal, 2=hard. How quickly the CPU notices a signal."
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              pass:
                summary: Pass your third card to the left
                value:
                  command: pass
                  sessionId: my-session-001
                  cardIndex: 2
              signal:
                summary: Say you noticed the signal (only legal while one is out)
                value:
                  command: signal
                  sessionId: my-session-001
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PigResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PigResponse'

  /stealingbundles/exec:
    post:
      tags:
        - stealingbundles
      summary: Execute a Stealing Bundles game command
      description: |
        Send a command to advance the Stealing Bundles (スティーリングバンドル)
        game session identified by `sessionId`.

        An American children's fishing game for two to four players. Matching a
        rank on the table captures it -- but **a rival's won bundle has a weak
        spot: its top card**. Play a card of that rank and the whole bundle
        becomes yours. Whatever you play lands on top of your own bundle, so
        the card you just captured with is what gets aimed at next; no lead is
        ever safe.

        **Capture is compulsory.** You take from the table or you take a
        bundle; only when neither is possible may you trail. `canCapture` says
        which situation you are in, and the server rejects a trail while a
        capture exists.

        Four cards go to the table and four to each player, and the rest is the
        stock. **It divides evenly at every table size** -- 48/8, 36/12, 32/16 --
        so hands are redealt until the stock runs out exactly. Whatever is left
        on the table then goes to the seat in `lastCaptureIdx`.

        `tableMatches` and `stealTargets` map each of your hand positions to
        what it can take, so a client does not have to cross-reference the
        table's ranks against every seat's `bundleTop` itself. Every seat's
        `bundleTop` is public -- it is the one thing opponents aim at.
      operationId: execStealingBundles
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, take, t, steal, s, trail, d, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `take`, `steal` and `trail`)
                victimIdx:
                  type: integer
                  description: Seat whose bundle to take (required for `steal`)
                config:
                  type: object
                  properties:
                    playerCnt:
                      type: integer
                      description: Players at the table (2..4, default 4).
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              take:
                summary: Capture from the table with your third card
                value:
                  command: take
                  sessionId: my-session-001
                  cardIndex: 2
              steal:
                summary: Take seat 1's whole bundle (its top card must match)
                value:
                  command: steal
                  sessionId: my-session-001
                  cardIndex: 0
                  victimIdx: 1
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StealingBundlesResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StealingBundlesResponse'

  /cucumber/exec:
    post:
      tags:
        - cucumber
      summary: Execute a Cucumber game command
      description: |
        Send a command to advance the Cucumber (キューカンバー, also Gurka) game
        session identified by `sessionId`.

        A Swedish/Finnish trick game for three to six players with **no suits at
        all**. If you hold a card higher than the highest played so far you must
        play one of them; if you do not, you must play your lowest card. The only
        decision is *which* high card to spend.

        **Only the last trick scores.** Whoever takes the seventh trick gains
        penalty points equal to the rank of the card they took it with; every
        other trick is worth nothing. Fewest penalty points wins, and the game
        ends when any seat reaches `targetScore`.

        Hands are a fixed **seven cards each** — 52 does not divide evenly by 3,
        5 or 6, so the remainder is simply not dealt.

        **`validPlays.length == 1` does not mean the play is forced**: a hand can
        hold exactly one card that *does* beat the trick. Read the `forced` flag
        instead. `highestInTrick` is the rank to beat, or 0 while nobody has led.

        `next` deals the following round — the game stops at RoundEnd so the
        penalty can be read, since nothing on the board records it.
      operationId: execCucumber
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, next, n, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your hand (required for `play`)
                config:
                  type: object
                  properties:
                    playerCnt:
                      type: integer
                      description: Players at the table (3..6, default 4).
                    targetScore:
                      type: integer
                      description: Penalty total that ends the game (10..100, default 30).
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play your third card
                value:
                  command: play
                  sessionId: my-session-001
                  cardIndex: 2
              next:
                summary: Deal the next round after reading the penalty
                value:
                  command: next
                  sessionId: my-session-001
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CucumberResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CucumberResponse'

  /goofspiel/exec:
    post:
      tags:
        - goofspiel
      summary: Execute a Goofspiel game command
      description: |
        Send a command to advance the Goofspiel (ゴフスピール, also GOPS) game
        session identified by `sessionId`.

        An American mathematical game and a game-theory staple, for two or three
        players. **There is no turn order.** A diamond is turned up as the prize
        and every seat bids one card from its own suit, face down and at the same
        time. The highest bid takes the prize's rank in points; a tie means
        nobody takes it, and the prize is either discarded or added to the next
        round's pot depending on `tieRule`. Exactly thirteen rounds.

        Diamonds are the prizes and the remaining suits are the bid hands.
        **Three players need four suits** -- one for prizes and one per seat --
        which a standard deck has exactly.

        **`bid` resolves the entire round in one request.** Because bidding is
        simultaneous there is no CPU turn to wait for: the response comes back
        in the Reveal phase with every seat's `revealedBid` filled in. `next`
        then turns the following prize.

        **Nothing is hidden except the bid in flight.** Every seat's `cards`
        holds its remaining bid cards, CPUs included -- spent cards are public,
        so a remainder is just a suit minus what has been played. Before the
        reveal a seat exposes only `hasBid`.
      operationId: execGoofspiel
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bid, b, next, n, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Index into your bid hand (required for `bid`)
                config:
                  type: object
                  properties:
                    playerCnt:
                      type: integer
                      description: Players at the table (2..3, default 2).
                    tieRule:
                      type: integer
                      description: "0=a tied prize is discarded, 1=it carries into the next round."
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Bid your third card face down
                value:
                  command: bid
                  sessionId: my-session-001
                  cardIndex: 2
              next:
                summary: Turn the next prize after reading the reveal
                value:
                  command: next
                  sessionId: my-session-001
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoofspielResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GoofspielResponse'

  /andarbahar/exec:
    post:
      tags:
        - andarbahar
      summary: Execute an Andar Bahar game command
      description: |
        Send a command to advance the Andar Bahar (アンダーバハール) game
        session identified by `sessionId`.

        An Indian two-way betting game. A joker (reference card) is turned up,
        then cards are dealt alternately into the **Andar** and **Bahar**
        columns until one matches the joker's rank.

        **The joker's colour decides which column is dealt first** - black
        starts with Andar, red with Bahar. The first-dealt column gets one extra
        chance and so wins 429/833 = **51.50%** of the time, which is why it
        pays only **0.9:1** while the other column pays **1:1**. Paying both
        1:1 would give the player a +3.00% edge.

        Bets must be multiples of 10 so the 0.9:1 payout settles exactly in
        integer chips. An optional side bet on the number of cards dealt pays
        by band (`sideBand` 0=1 card, 1=2-5, 2=6-10, 3=11-15, 4=16-25,
        5=26-35, 6=36+); omit `sideAmount` to skip it.
      operationId: execAndarBahar
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: "reset (r), bet (b), clear, hint (h), log, quit (q)"
                amount:
                  type: integer
                  description: Main bet, a multiple of 10 between 10 and 10000.
                target:
                  type: integer
                  description: "0=Andar, 1=Bahar"
                sideAmount:
                  type: integer
                  description: Side bet stake. Omit or 0 for no side bet.
                sideBand:
                  type: integer
                  description: Side bet band, only read when sideAmount is above 0.
                sessionId:
                  type: string
              example:
                command: bet
                amount: 100
                target: 0
                sessionId: my-session-001
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AndarBaharResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AndarBaharResponse'

  /botifarra/exec:
    post:
      tags:
        - botifarra
      summary: Execute a Botifarra game command
      description: |
        Send a command to advance the Botifarra (ボティファラ) game session
        identified by `sessionId`.

        A Catalan four-player partnership trick-taker on the Spanish 48-card
        deck (no tens), twelve cards each, seats 0+2 against 1+3.

        **There is no auction.** The dealer either names trump or delegates to
        their partner, who must then name it - passing is not an option.
        No-trump is a legal call, sent as `suit: -1`.

        **You must win the trick if you can**: beyond following suit, a player
        holding a card that beats the trick must play one, unless their partner
        is already winning. `validPlays` reflects that, so it is often a strict
        subset of the hand.

        Card points are manilla (9) 5, ace 4, king 3, knight 2, jack 1 - 60 in
        all - plus one point per trick, so exactly **72 points** move each round
        and only the excess over 36 scores. contrar/recontrar multiply it.
      operationId: execBotifarra
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: "reset (r), declare, delegate, double, passdouble, play (p), next, giveup (g), hint (h), log, quit (q)"
                cardIndex:
                  type: integer
                  description: Hand index to play.
                suit:
                  type: integer
                  description: "Trump to declare: 1=spades, 2=clubs, 3=hearts, 4=diamonds, -1=no trump."
                sessionId:
                  type: string
              example:
                command: declare
                suit: 3
                sessionId: my-session-001
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BotifarraResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BotifarraResponse'

  /rikken/exec:
    post:
      tags:
        - rikken
      summary: Execute a Rikken game command
      description: |
        Send a command to advance the Rikken (リッケン) game session identified
        by `sessionId`.

        A Dutch auction-and-contract trick-taker for four players on a standard
        52-card deck, thirteen cards each.

        **Contracts of opposite kinds share one bidding ladder**: Rik (take 8
        with a called partner) < Misere (take none, alone) < Solo (take 6
        alone) < Open Misere (take none, hand exposed). Bids may only rise.
        `contract: 0` is a pass and must be sent explicitly - it is a value,
        not an omission.

        **Teams are not fixed by seat.** Only Rik is 2-v-2: its declarer names
        a trump, then calls an ace they do not hold; the holder becomes a
        partner who stays hidden (`partnerIdx: -1`) until that card is played.

        Scoring is zero-sum - the four scores always total 0.
      operationId: execRikken
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: "reset (r), bid (b), call (c), play (p), next, giveup (g), hint (h), log, quit (q)"
                cardIndex:
                  type: integer
                  description: Hand index to play.
                contract:
                  type: integer
                  description: "0=pass, 1=Rik, 2=Misere, 3=Solo, 4=Open Misere. Required for bid."
                suit:
                  type: integer
                  description: "Trump: 1=spades, 2=clubs, 3=hearts, 4=diamonds. Required for call."
                sessionId:
                  type: string
              example:
                command: bid
                contract: 1
                sessionId: my-session-001
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RikkenResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RikkenResponse'

  /colourwhist/exec:
    post:
      tags:
        - colourwhist
      summary: Execute a Colour Whist game command
      description: |
        Send a command to advance the Colour Whist (カラーホイスト) game session
        identified by `sessionId`.

        A Flemish contract whist for four players on a standard 52-card deck.

        **The shape of the hand can decide the contract.** If anyone is dealt
        three aces, `troel` is forced **without an auction** and the holder of
        the fourth ace becomes their partner automatically; `troelForced` says
        so, and `phase` will not be Bid. Troel is the one contract that cannot
        be bid - sending it is rejected.

        Otherwise the ladder is Samen (8 with a called partner) < Alleen (8
        alone) < Miserie (take none). `contract: 0` is a pass and must be sent
        explicitly - it is a value, not an omission.

        Scoring is zero-sum: the four scores always total 0.
      operationId: execColourWhist
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: "reset (r), bid (b), call (c), play (p), next, giveup (g), hint (h), log, quit (q)"
                cardIndex:
                  type: integer
                  description: Hand index to play.
                contract:
                  type: integer
                  description: "0=pass, 1=Samen, 2=Alleen, 3=Miserie. Troel (4) cannot be bid."
                suit:
                  type: integer
                  description: "Trump: 1=spades, 2=clubs, 3=hearts, 4=diamonds."
                sessionId:
                  type: string
              example:
                command: bid
                contract: 1
                sessionId: my-session-001
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ColourWhistResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ColourWhistResponse'

  /crazyfourpoker/exec:
    post:
      tags:
        - crazyfourpoker
      summary: Execute a Crazy 4 Poker game command
      description: |
        Send a command to advance the Crazy 4 Poker (クレイジー4ポーカー) game
        session identified by `sessionId`.

        One player against the dealer on a standard 52-card deck. Both sides are
        dealt five cards and play their **best four**.

        **The ante and the Super Bonus are equal and mandatory**; Queens Up is
        optional and settles on the player's own four regardless of the dealer or
        a fold. Omitting `queensUp` means "no side bet".

        **The play bet must equal the ante unless the player holds a pair of aces
        or better**, in which case it may go up to 3x. Being able to move the
        multiplier at all is the strong-hand privilege, so `maxMultiplier` is
        returned by the server and is either 1 or 3 — clients must not re-derive
        it. Sending a larger multiplier is rejected rather than clamped.

        The dealer qualifies at **king high**. If the dealer does not qualify the
        ante pays 1:1 and the play bet pushes.

        **The dealer's cards are not in the response until the showdown**;
        `dealerHand`, `dealerBest` and `dealerHandRank` stay empty/zero while the
        player is still deciding.
      operationId: execCrazyFourPoker
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: "reset (r), bet (b), play (p), fold (f), next, hint (h), log, quit (q)"
                ante:
                  type: integer
                  description: Ante in multiples of 10. An equal Super Bonus is placed with it.
                queensUp:
                  type: integer
                  description: Optional side bet. Omit or send 0 for none.
                multiplier:
                  type: integer
                  description: "Play bet multiplier: 1, or up to 3 with a pair of aces or better."
                chips:
                  type: integer
                  description: Starting chips, for reset.
                sessionId:
                  type: string
              example:
                command: bet
                ante: 50
                queensUp: 20
                sessionId: my-session-001
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CrazyFourPokerResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CrazyFourPokerResponse'

  /chemindefer/exec:
    post:
      tags:
        - chemindefer
      summary: Execute a Chemin de Fer game command
      description: |
        Send a command to advance the Chemin de Fer (シュマン・ド・フェール) game
        session identified by `sessionId`.

        The French ancestor of baccarat, played on a six-deck shoe by six seats.
        **One of the seats is the bank, not the house.** The banker stakes an
        amount, the other seats cover it between them, and the bank passes to
        the next seat whenever the punters win.

        **The third card is not on a table here.** That is the whole difference
        from punto banco. Only the punter side is constrained, and only partly:
        0-4 must draw, 6-7 must stand, and **5 is a free choice**. The banker is
        free at every total. `punterMayChoose` says whether the punter side has
        a decision right now; when it is false the domain applies the forced
        play itself rather than asking.

        Drawing and standing are separate commands per side (`pd`/`ps` for the
        punter, `bd`/`bs` for the banker) so a client cannot resolve the wrong
        side's decision.

        Chips are zero-sum: the six stacks always total their starting sum.
      operationId: execChemindeFer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: "reset (r), stake (s), bet (b), pd, ps, bd, bs, d, st, pb, next, giveup (g), hint (h), log, quit (q)"
                stake:
                  type: integer
                  description: Amount to bank, for the stake command.
                amount:
                  type: integer
                  description: "Amount to bet. 0 means pass and is a value, not an omission."
                rounds:
                  type: integer
                  description: Coups per session, for reset.
                chips:
                  type: integer
                  description: Starting chips per seat, for reset.
                sessionId:
                  type: string
              example:
                command: bet
                amount: 50
                sessionId: my-session-001
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChemindeFerResponse'
        '400':
          description: Invalid command or missing parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChemindeFerResponse'

  /clocksolitaire/exec:
    post:
      tags:
        - clocksolitaire
      summary: Execute a Clock Solitaire game command
      description: |
        Send a command to advance the Clock Solitaire (クロックソリティア) game session identified by
        `sessionId`.  Clock Solitaire is a fully automatic single-player solitaire card game using a
        standard 52-card deck.  52 cards are dealt face-down into 13 piles of 4 cards: 12 piles placed
        at clock positions 1-12 and 1 pile in the center (King position).  One card at a time is flipped
        face-up and placed at the bottom of the pile matching its rank.  The game clears when all cards
        are face-up before the 4th King is placed; the game is over when the 4th King is turned while
        face-down cards remain.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | All cards are face-up; player wins |
        | GameOver   | `2`   | 4th King turned while face-down cards remain |

        | Command    | Aliases | Description |
        |------------|---------|-------------|
        | `reset`    | `r`     | Start / restart a game |
        | `step`     | `s`     | Flip and place one card |
        | `autoplay` | `a`     | Play all remaining cards to completion |
        | `log`      | `l`     | View action log |
      operationId: clocksolitaireExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, step, s, autoplay, a, log, l]
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              step:
                summary: Flip and place one card
                value:
                  command: step
                  sessionId: my-session-001
              autoplay:
                summary: Play all remaining cards to completion
                value:
                  command: autoplay
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  piles:
                    type: array
                    description: |
                      13 piles indexed 0-12 (index 0 = position 1, index 11 = position 12, index 12 = center/King).
                      Each pile is an array of card objects.
                    items:
                      type: array
                      items:
                        type: object
                        properties:
                          card:
                            $ref: '#/components/schemas/Card'
                          faceUp:
                            type: boolean
                  faceUpCount:
                    type: integer
                    description: Total number of face-up cards across all piles
                  phase:
                    type: integer
                    description: "0=Playing, 1=GameClear, 2=GameOver"
                  stepCount:
                    type: integer
                    description: Number of cards flipped so far
                  currentCard:
                    description: The card most recently flipped (null before first step)
                    nullable: true
                    allOf:
                      - $ref: '#/components/schemas/Card'
                  message:
                    type: string
                  messageCode:
                    type: string
                  messageParams:
                    type: object
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  piles:
                    type: array
                    items:
                      type: array
                  faceUpCount:
                    type: integer
                  phase:
                    type: integer
                  stepCount:
                    type: integer
                  message:
                    type: string

  /durak/exec:
    post:
      tags:
        - durak
      summary: Execute a Durak game command
      description: |
        Send a command to advance the Durak (ドゥラーク) game session
        identified by `sessionId`.  Durak is a popular Eastern European card
        game for 2-6 players (1 human + 1-5 CPU) using a 36-card deck (6-A).
        Players take turns attacking and defending.  The attacker plays cards
        and the defender must beat each with a higher card of the same suit or
        a trump card.  The last player with cards is the loser ("durak" = fool).

        The game proceeds through these phases:

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Attack   | `0`   | Attacker plays cards |
        | Defend   | `1`   | Defender beats attacking cards |
        | BoutEnd  | `2`   | Bout resolved (cards discarded or picked up) |
        | GameEnd  | `3`   | Game over; loser determined |

        | Command  | Aliases | Description |
        |----------|---------|-------------|
        | `reset`  | `r`     | Start / restart a game |
        | `attack` | `a`     | Play a card to attack |
        | `defend` | `d`     | Play a card to defend against an attack |
        | `pickup` | `p`     | Defender picks up all table cards |
        | `done`   | `dn`    | Attacker ends the attack |
        | `pass`   | `ps`    | Pass the attack to the next player |
        | `log`    | `l`     | View action log |
      operationId: durakExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, attack, a, defend, d, pickup, p, done, dn, pass, ps, log, l]
                cardIndex:
                  type: integer
                  description: Index of the card to play (for attack/defend commands)
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              attack:
                summary: Attack with a card
                value:
                  command: attack
                  cardIndex: 0
                  sessionId: my-session-001
              defend:
                summary: Defend against an attack
                value:
                  command: defend
                  cardIndex: 2
                  sessionId: my-session-001
              pickup:
                summary: Pick up all table cards
                value:
                  command: pickup
                  sessionId: my-session-001
              done:
                summary: End the attack
                value:
                  command: done
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  players:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: integer
                        isHuman:
                          type: boolean
                        cardCount:
                          type: integer
                        cards:
                          type: array
                          items:
                            $ref: '#/components/schemas/Card'
                  tableAttack:
                    type: array
                    description: Cards played by the attacker on the table
                    items:
                      $ref: '#/components/schemas/Card'
                  tableDefense:
                    type: array
                    description: Cards played by the defender on the table
                    items:
                      nullable: true
                      allOf:
                        - $ref: '#/components/schemas/Card'
                  trumpCard:
                    nullable: true
                    allOf:
                      - $ref: '#/components/schemas/Card'
                    description: The trump card (bottom of the deck)
                  trumpSuit:
                    type: integer
                    description: Trump suit (0=spade, 1=heart, 2=diamond, 3=club)
                  deckCount:
                    type: integer
                    description: Remaining cards in the deck
                  attackerIdx:
                    type: integer
                  defenderIdx:
                    type: integer
                  currentTurn:
                    type: integer
                  phase:
                    type: integer
                    description: "0=Attack, 1=Defend, 2=BoutEnd, 3=GameEnd"
                  gameEndFlag:
                    type: boolean
                  loserIdx:
                    type: integer
                    description: Index of the losing player (-1 if game not ended)
                  cpuActions:
                    type: array
                    items:
                      type: object
                  message:
                    type: string
                  messageCode:
                    type: string
                  messageParams:
                    type: object
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  players:
                    type: array
                    items:
                      type: array
                  phase:
                    type: integer
                  message:
                    type: string

  /fortythieves/exec:
    post:
      tags:
        - fortythieves
      summary: Execute a Forty Thieves game command
      description: |
        Send a command to advance the Forty Thieves (フォーティシーブス) game
        session identified by `sessionId`.  Forty Thieves is a single-player
        solitaire card game using 2 standard decks (104 cards).  10 tableau
        columns with 4 face-up cards each, 8 foundation piles (A→K, same suit),
        same-suit descending tableau moves, single card moves only, and no
        stock recycling.

        The game proceeds through these phases:

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | `0`   | Game in progress |
        | GameClear | `1`   | All foundations complete |
        | GameOver  | `2`   | No more moves available |

        | Command        | Aliases | Description |
        |----------------|---------|-------------|
        | `reset`        | `r`     | Start / restart a game |
        | `draw`         | `d`     | Draw a card from the stock to the waste |
        | `move`         | `m`     | Move a card between tableau/waste/foundation |
        | `undo`         | `u`     | Undo the last action |
        | `giveup`       | `g`     | Give up the current game |
        | `hint`         | `h`     | Show a hint for the next move |
        | `autocomplete` | `ac`    | Auto-move cards to foundations |
        | `log`          | `l`     | View action log |
      operationId: fortyThievesExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, draw, d, move, m, undo, u, undo_n, giveup, g, hint, h, autocomplete, ac, log, l]
                from:
                  $ref: '#/components/schemas/FortyThievesMoveZone'
                to:
                  $ref: '#/components/schemas/FortyThievesMoveZone'
                n:
                  type: integer
                  description: Number of times to undo (for undo_n command)
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              draw:
                summary: Draw a card from the stock
                value:
                  command: draw
                  sessionId: my-session-001
              move:
                summary: Move a card from waste to tableau column 0
                value:
                  command: move
                  from:
                    zone: waste
                  to:
                    zone: tableau
                    col: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  tableau:
                    type: array
                    description: 10 tableau columns, each an array of face-up cards
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/Card'
                  foundations:
                    type: array
                    description: 8 foundation piles (top card of each)
                    items:
                      nullable: true
                      allOf:
                        - $ref: '#/components/schemas/Card'
                  waste:
                    nullable: true
                    allOf:
                      - $ref: '#/components/schemas/Card'
                    description: Top card of the waste pile
                  stockCount:
                    type: integer
                    description: Number of remaining cards in the stock
                  moveCount:
                    type: integer
                  phase:
                    type: integer
                    description: "0=Playing, 1=GameClear, 2=GameOver"
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  tableau:
                    type: array
                    items:
                      type: array
                  phase:
                    type: integer
                  message:
                    type: string

  /crescent/exec:
    post:
      tags:
        - crescent
      summary: Execute a Crescent Solitaire game command
      description: |
        Send a command to advance the Crescent Solitaire (クレセント・ソリティア)
        game session identified by `sessionId`. Crescent is a single-player
        solitaire using 2 standard decks (104 cards). 16 tableau piles of 6
        cards each surround 8 foundations: 4 ascending from A and 4 descending
        from K (one per suit). Tableau moves are restricted to the top card
        only, same suit, value ±1, with A↔K wrap. Empty tableau piles cannot
        be refilled. Up to 3 redeals per game reverse every tableau pile.

        The game proceeds through these phases:

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | `0`   | Game in progress |
        | GameClear | `1`   | All 104 cards on the foundations |
        | GameOver  | `2`   | Gave up or stalemated |

        | Command        | Aliases | Description |
        |----------------|---------|-------------|
        | `reset`        | `r`     | Start / restart a game |
        | `move`         | `m`     | Move tableau→tableau or tableau→foundation |
        | `redeal`       |         | Reverse every tableau pile (max 3 per game) |
        | `undo`         | `u`     | Undo the last action |
        | `giveup`       | `g`     | Give up the current game |
        | `hint`         | `h`     | Show a hint for the next move |
        | `autocomplete` | `ac`    | Drain tops to foundations |
        | `log`          | `l`     | View action log |
      operationId: crescentExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, move, m, redeal, undo, u, undo_n, giveup, g, hint, h, autocomplete, ac, log, l]
                from:
                  $ref: '#/components/schemas/CrescentMoveZone'
                to:
                  $ref: '#/components/schemas/CrescentMoveZone'
                n:
                  type: integer
                  description: Number of times to undo (for undo_n command)
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              redeal:
                summary: Reverse every tableau pile
                value:
                  command: redeal
                  sessionId: my-session-001
              moveTableauToFoundation:
                summary: Move col 3 top card onto ascending foundation 0
                value:
                  command: move
                  from:
                    zone: tableau
                    col: 3
                  to:
                    zone: foundation
                    col: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  tableau:
                    type: array
                    description: 16 tableau piles (each an array of face-up cards)
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/Card'
                  foundation:
                    type: array
                    description: 8 foundation piles (asc 0-3 build A→K, desc 4-7 build K→A)
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/Card'
                  redealsRemaining:
                    type: integer
                    description: Redeals left for this game (0..3)
                  moveCount:
                    type: integer
                  phase:
                    type: integer
                    description: "0=Playing, 1=GameClear, 2=GameOver"
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  tableau:
                    type: array
                    items:
                      type: array
                  phase:
                    type: integer
                  message:
                    type: string

  /paigow/exec:
    post:
      tags:
        - paigow
      summary: Execute a Pai Gow Poker game command
      description: |
        Send a command to advance the Pai Gow Poker (パイゴウポーカー) game session
        identified by `sessionId`.  Pai Gow Poker is a single-player casino card game
        using a 53-card deck (standard 52 + 1 joker).  Player and dealer each receive
        7 cards.  Player splits cards into a 5-card high hand and 2-card low hand.
        Both hands are compared against the dealer's hands.

        The joker can be used as an ace or to complete a straight, flush, or straight
        flush.  The high hand must outrank the low hand.  Player wins both hands to win
        (1:1 minus 5% commission), loses both to lose, or pushes on a split.  Ties
        (copy hands) go to the dealer.

        The game proceeds through these phases:

        | Phase | Value | Description |
        |-------|-------|-------------|
        | Bet   | `1`   | Place a bet |
        | Set   | `2`   | Arrange 7 cards into high hand (5) and low hand (2) |
        | End   | `3`   | Round complete; shows result and payout |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `bet`   | `b`     | Place bet (requires `amount`) |
        | `set`   | `s`     | Set low hand cards (requires `low0`, `low1` as card indices) |
        | `log`   | `l`     | View action log |
      operationId: paigowExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PaiGowRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Bet 100 chips
                value:
                  command: bet
                  amount: 100
                  sessionId: my-session-001
              set:
                summary: Set low hand (card indices 2 and 5)
                value:
                  command: set
                  low0: 2
                  low1: 5
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaiGowResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaiGowResponse'
              examples:
                paramError:
                  value:
                    playerCards: []
                    dealerCards: []
                    playerHighHand: []
                    playerLowHand: []
                    dealerHighHand: []
                    dealerLowHand: []
                    phase: 0
                    chips: 0
                    bet: 0
                    result: 0
                    message: "unsupported command"

  /chinesepoker/exec:
    post:
      tags:
        - chinesepoker
      summary: Execute a Chinese Poker game command
      description: |
        Send a command to advance the Chinese Poker (チャイニーズポーカー) game session
        identified by `sessionId`.  Chinese Poker is a single-player casino card game
        using a standard 52-card deck (no joker).  Player and dealer each receive
        13 cards.  Player splits cards into Front (3), Middle (5), and Back (5) hands.
        Hands must satisfy Back ≧ Middle ≧ Front or the arrangement is a foul.

        Front hand uses 3-card poker ranking; Middle and Back use standard 5-card poker
        ranking.  Each hand is compared separately; ties go to the dealer.  Win 2+ hands
        for a standard win (1:1), all 3 for a scoop (3:1).  Royalty bonuses apply to
        premium hands.

        The game proceeds through these phases:

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Bet      | `1`   | Place a bet |
        | SetHands | `2`   | Split 13 cards into Front (3), Middle (5), Back (5) |
        | End      | `3`   | Round complete; shows result and payout |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `bet`   | `b`     | Place bet (requires `amount`) |
        | `set`   | `s`     | Set hands (requires `frontIndices` [3] and `middleIndices` [5]) |
        | `log`   | `l`     | View action log |
      operationId: chinesepokerExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [command, sessionId]
              properties:
                command:
                  type: string
                sessionId:
                  type: string
                amount:
                  type: integer
                frontIndices:
                  type: array
                  items:
                    type: integer
                middleIndices:
                  type: array
                  items:
                    type: integer
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
        '400':
          description: Bad request
          content:
            application/json:
              schema:
                type: object

  /slapjack/exec:
    post:
      tags:
        - slapjack
      summary: Execute a Slapjack game command
      description: |
        Send a command to advance the Slapjack game session identified by
        `sessionId`. Slapjack is a real-time reflex game for a human vs CPU
        played with a standard 52-card deck split evenly (26 cards each).
        Players take turns flipping the top of their stock onto the centre
        pile; whoever slaps first when a Jack appears on top wins the pile.
        Wrong slaps cost the offender one card to the opponent.

        The game proceeds through these phases:

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Play     | `0`   | Game is running |
        | GameEnd  | `1`   | Game over |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game (optional `config.cpuDifficulty` 0-2) |
        | `step`  | `s`     | Current player flips top of stock onto pile |
        | `slap`  | `j`     | Slap the pile (J = win, otherwise penalty) |
        | `tick`  |         | Advance the CPU's pending step / slap action |
        | `log`   | `l`     | View action log |
      operationId: slapjackExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, step, s, slap, j, tick, log, l]
                playerIdx:
                  type: integer
                  description: 0 = human (slap only)
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: 0=easy, 1=normal, 2=hard
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              slap:
                summary: Human slaps when J is on top
                value:
                  command: slap
                  playerIdx: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  phase:
                    type: integer
                  gameEndFlag:
                    type: boolean
                  winnerIdx:
                    type: integer
                  currentTurnIdx:
                    type: integer
                  isHumanTurn:
                    type: boolean
                  isTopJack:
                    type: boolean
                  centerPileSize:
                    type: integer
                  topCard:
                    nullable: true
                    allOf:
                      - $ref: '#/components/schemas/Card'
                  players:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                        isHuman:
                          type: boolean
                        stockSize:
                          type: integer
                  cpuDifficulty:
                    type: integer
                  pendingKind:
                    type: integer
                  pendingDeadlineMs:
                    type: integer
                  lastEventKind:
                    type: integer
                  lastEventPlayerIdx:
                    type: integer
                  message:
                    type: string
                  messageCode:
                    type: string
                  messageParams:
                    type: object
                    additionalProperties:
                      type: string

  /egyptianratscrew/exec:
    post:
      tags:
        - egyptianratscrew
      summary: Execute an Egyptian Ratscrew game command
      description: |
        Send a command to advance the Egyptian Ratscrew game session identified
        by `sessionId`. Egyptian Ratscrew is a real-time reflex game for a
        human vs CPU played with a standard 52-card deck split evenly (26 cards
        each). Players take turns flipping the top of their stock onto the
        centre pile. The pile is **slappable** when the top two share a rank
        (pair) or the top three form a sandwich (X-Y-X); the first to slap a
        slappable pile wins it. Wrong slaps cost 1 card to the opponent. Face
        cards trigger a chance battle: when a face card lands on top, the next
        player must reveal a face card within N flips (J=1, Q=2, K=3, A=4) or
        the player who placed the face card wins the pile.

        The game proceeds through these phases:

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Play     | `0`   | Game is running |
        | GameEnd  | `1`   | Game over |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game (optional `config.cpuDifficulty` 0-2) |
        | `step`  | `s`     | Current player flips top of stock onto pile |
        | `slap`  | `j`     | Slap the pile (pair / sandwich = win, otherwise penalty) |
        | `tick`  |         | Advance the CPU's pending step / slap action |
        | `log`   | `l`     | View action log |
      operationId: egyptianRatscrewExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, step, s, slap, j, tick, log, l]
                playerIdx:
                  type: integer
                  description: 0 = human (slap only)
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: 0=easy, 1=normal, 2=hard
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              slap:
                summary: Human slaps when pair / sandwich is on top
                value:
                  command: slap
                  playerIdx: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  phase:
                    type: integer
                  gameEndFlag:
                    type: boolean
                  winnerIdx:
                    type: integer
                  currentTurnIdx:
                    type: integer
                  isHumanTurn:
                    type: boolean
                  isTopFaceCard:
                    type: boolean
                  isSlappable:
                    type: boolean
                  centerPileSize:
                    type: integer
                  topCard:
                    nullable: true
                    allOf:
                      - $ref: '#/components/schemas/Card'
                  players:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                        isHuman:
                          type: boolean
                        stockSize:
                          type: integer
                  cpuDifficulty:
                    type: integer
                  chanceRemaining:
                    type: integer
                  chanceFromIdx:
                    type: integer
                  pendingKind:
                    type: integer
                  pendingDeadlineMs:
                    type: integer
                  lastEventKind:
                    type: integer
                  lastEventPlayerIdx:
                    type: integer
                  lastSlapReason:
                    type: integer
                  message:
                    type: string
                  messageCode:
                    type: string
                  messageParams:
                    type: object
                    additionalProperties:
                      type: string

  /bakersdozen/exec:
    post:
      tags:
        - bakersdozen
      summary: Execute a Baker's Dozen game command
      description: |
        Send a command to advance the Baker's Dozen (ベーカーズ・ダズン) game
        session identified by `sessionId`. Baker's Dozen is a single-player
        open-information solitaire using a single 52-card deck. 13 tableau
        columns of 4 face-up cards each (52 cards total), with kings
        automatically moved to the bottom of each column at deal time. 4
        foundation piles built up by suit Ace→King. Tableau builds rank-only
        descending (suit doesn't matter); only single-card moves are allowed.
        Empty columns cannot be filled. No stock or waste pile.

        The game proceeds through these phases:

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | `0`   | Game in progress |
        | GameClear | `1`   | All foundations complete |
        | GameOver  | `2`   | Player gave up |

        | Command        | Aliases | Description |
        |----------------|---------|-------------|
        | `reset`        | `r`     | Start / restart a game |
        | `move`         | `m`     | Move a card between tableau/foundation |
        | `undo`         | `u`     | Undo the last action |
        | `giveup`       | `g`     | Give up the current game |
        | `hint`         | `h`     | Show a hint for the next move |
        | `autocomplete` | `ac`    | Auto-move cards to foundations |
        | `log`          | `l`     | View action log |
      operationId: bakersDozenExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, move, m, undo, u, undo_n, giveup, g, hint, h, autocomplete, ac, log, l]
                from:
                  $ref: '#/components/schemas/BakersDozenMoveZone'
                to:
                  $ref: '#/components/schemas/BakersDozenMoveZone'
                n:
                  type: integer
                  description: Number of times to undo (for undo_n command)
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              move:
                summary: Move tableau column 0's top card to column 5
                value:
                  command: move
                  from:
                    zone: tableau
                    col: 0
                  to:
                    zone: tableau
                    col: 5
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  tableau:
                    type: array
                    description: 13 tableau columns, each an array of cards (with face-up flag)
                    items:
                      type: array
                      items:
                        type: object
                        properties:
                          card:
                            nullable: true
                            allOf:
                              - $ref: '#/components/schemas/Card'
                          faceUp:
                            type: boolean
                  foundation:
                    type: array
                    description: 4 foundation piles (cards by suit, A→K)
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/Card'
                  moveCount:
                    type: integer
                  canUndo:
                    type: boolean
                  isStalemate:
                    type: boolean
                  undoToEscape:
                    type: integer
                  phase:
                    type: integer
                    description: "0=Playing, 1=GameClear, 2=GameOver"
                  hint:
                    type: object
                    nullable: true
                    properties:
                      fromCol:
                        type: integer
                      cardIndex:
                        type: integer
                      toZone:
                        type: string
                      toCol:
                        type: integer
                  message:
                    type: string
                  messageCode:
                    type: string
                  messageParams:
                    type: object
                    additionalProperties:
                      type: string

  /beleagueredcastle/exec:
    post:
      tags:
        - beleagueredcastle
      summary: Execute a Beleaguered Castle game command
      description: |
        Send a command to advance the Beleaguered Castle (包囲された城) game
        session identified by `sessionId`. Beleaguered Castle is a
        single-player open-information solitaire using a single 52-card deck.
        The 4 aces are pre-seeded onto the foundations at deal time and the
        remaining 48 cards are dealt face-up into 8 tableau columns of 6 cards
        each — visually arranged with 4 columns flanking the centred
        foundations. Foundations build up by suit Ace→King. Tableau builds
        rank-only descending (suit doesn't matter); only single-card moves are
        allowed. Empty columns accept any card. No stock or waste pile.

        The game proceeds through these phases:

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | `0`   | Game in progress |
        | GameClear | `1`   | All foundations complete |
        | GameOver  | `2`   | Player gave up |

        | Command        | Aliases | Description |
        |----------------|---------|-------------|
        | `reset`        | `r`     | Start / restart a game |
        | `move`         | `m`     | Move a card between tableau/foundation |
        | `undo`         | `u`     | Undo the last action |
        | `giveup`       | `g`     | Give up the current game |
        | `hint`         | `h`     | Show a hint for the next move |
        | `autocomplete` | `ac`    | Auto-move cards to foundations |
        | `log`          | `l`     | View action log |
      operationId: beleagueredCastleExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, move, m, undo, u, undo_n, giveup, g, hint, h, autocomplete, ac, log, l]
                from:
                  $ref: '#/components/schemas/BeleagueredCastleMoveZone'
                to:
                  $ref: '#/components/schemas/BeleagueredCastleMoveZone'
                n:
                  type: integer
                  description: Number of times to undo (for undo_n command)
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              move:
                summary: Move tableau column 0's top card to column 5
                value:
                  command: move
                  from:
                    zone: tableau
                    col: 0
                  to:
                    zone: tableau
                    col: 5
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  tableau:
                    type: array
                    description: 8 tableau columns, each an array of cards (with face-up flag)
                    items:
                      type: array
                      items:
                        type: object
                        properties:
                          card:
                            nullable: true
                            allOf:
                              - $ref: '#/components/schemas/Card'
                          faceUp:
                            type: boolean
                  foundation:
                    type: array
                    description: 4 foundation piles (cards by suit, A→K). Pre-seeded with the four aces at deal time.
                    items:
                      type: array
                      items:
                        $ref: '#/components/schemas/Card'
                  moveCount:
                    type: integer
                  canUndo:
                    type: boolean
                  isStalemate:
                    type: boolean
                  undoToEscape:
                    type: integer
                  phase:
                    type: integer
                    description: "0=Playing, 1=GameClear, 2=GameOver"
                  hint:
                    type: object
                    nullable: true
                    properties:
                      fromCol:
                        type: integer
                      cardIndex:
                        type: integer
                      toZone:
                        type: string
                      toCol:
                        type: integer
                  message:
                    type: string
                  messageCode:
                    type: string
                  messageParams:
                    type: object
                    additionalProperties:
                      type: string

  /piquet/exec:
    post:
      tags:
        - piquet
      summary: Execute a Piquet (ピケ) game command
      description: |
        Send a command to advance the Piquet game session identified by
        `sessionId`. Piquet is a 2-player trick-taking game using a 32-card
        deck (7..A). Each deal: 12 cards each + 8-card talon (top 5 / bottom 3).

        Phases:

        | Phase       | Value | Description |
        |-------------|-------|-------------|
        | Exchange    | `0`   | Elder swaps 1..5 cards then Younger swaps 0..3 |
        | Declaration | `1`   | Point → Sequence → Set, advanced one step per `d` |
        | Play        | `2`   | 12-trick play, follow suit when possible |
        | Score       | `3`   | Round ended; call `nd` for the next deal |
        | GameEnd     | `4`   | Partie completed |

        Commands:

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a partie (optional config) |
        | `e`     | `elder` | Elder exchange (1..5 discard indices) |
        | `y`     | `younger` | Younger exchange (0..3 discard indices; empty list = pass) |
        | `d`     | `declare` | Advance one declaration step |
        | `p`     | `play`  | Play card at index |
        | `nd`    | `nextdeal` | Advance to next deal after Score |
        | `h`     | `hint`  | Show suggested move |
        | `log`   | `l`     | View action log |
      operationId: piquetExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, e, elder, y, younger, d, declare, p, play, nd, nextdeal, h, hint, log, l]
                cardIndex:
                  type: integer
                  description: Card index for `p`/`play`
                discardIndices:
                  type: array
                  items:
                    type: integer
                  description: Discard indices for `e`/`y` exchanges
                config:
                  $ref: '#/components/schemas/PiquetConfig'
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new partie
                value:
                  command: reset
                  sessionId: my-session-001
              exchangeElder:
                summary: Elder swaps cards at indices 0,1,2
                value:
                  command: e
                  discardIndices: [0, 1, 2]
                  sessionId: my-session-001
              declare:
                summary: Advance to next declaration step
                value:
                  command: d
                  sessionId: my-session-001
              play:
                summary: Play hand[3]
                value:
                  command: p
                  cardIndex: 3
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PiquetResponse'

  /war/exec:
    post:
      tags:
        - war
      summary: Execute a War (戦争) game command
      description: |
        Send a command to advance the War (戦争) game session identified by
        `sessionId`.  War is a two-player matching card game for a human vs
        CPU, played with a standard 52-card deck split evenly (26 cards each,
        face-down).  Each round both players reveal the top card of their
        draw pile; the higher rank (Ace high) wins both cards into the
        winner's discard pile.  On a tie, "war" is triggered: each player
        buries up to 3 cards face-down and reveals a new face-up card, with
        the higher face-up winning the entire pot.  Wars can chain if the
        new face-ups also tie.  When a draw pile empties, the discard pile
        is shuffled and reused.

        The game proceeds through these phases:

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Reveal   | `0`   | Ready to flip the next pair of cards |
        | Resolved | `1`   | Round decided; next step awards the pot |
        | WarBury  | `2`   | Tie triggered; next step buries face-down and reveals new face-ups |
        | GameEnd  | `3`   | Game over |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game (optional `maxRounds` 10-10000) |
        | `step`  | `s`     | Advance one step of the state machine |
        | `log`   | `l`     | View action log |
      operationId: warExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, step, s, log, l]
                maxRounds:
                  type: integer
                  description: Round cap (10-10000). Used only with reset.
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              step:
                summary: Flip next card / resolve round / resolve war
                value:
                  command: step
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  players:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: integer
                        isHuman:
                          type: boolean
                        drawPileSize:
                          type: integer
                        discardPileSize:
                          type: integer
                        totalCards:
                          type: integer
                  phase:
                    type: integer
                  gameEndFlag:
                    type: boolean
                  winnerIdx:
                    type: integer
                  playerRevealed:
                    nullable: true
                    allOf:
                      - $ref: '#/components/schemas/Card'
                  cpuRevealed:
                    nullable: true
                    allOf:
                      - $ref: '#/components/schemas/Card'
                  warPotSize:
                    type: integer
                  lastWinnerIdx:
                    type: integer
                  lastBurialCount:
                    type: integer
                  roundsPlayed:
                    type: integer
                  config:
                    type: object
                    properties:
                      maxRounds:
                        type: integer
                  message:
                    type: string
                  messageCode:
                    type: string
                  messageParams:
                    type: object
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  players:
                    type: array
                    items:
                      type: object
                  phase:
                    type: integer
                  gameEndFlag:
                    type: boolean
                  message:
                    type: string

  /canfield/exec:
    post:
      tags:
        - canfield
      summary: Execute a Canfield (キャンフィールド) game command
      description: |
        Send a command to advance a Canfield solitaire session identified by `sessionId`.
        Canfield uses a 13-card reserve, 4 tableau columns, 4 foundations, and a stock
        that draws 3 cards at a time to the waste. The foundation base rank is
        determined by the first card dealt. Tableau sequences are built in alternating
        colors descending with wrap-around; foundations build in same suit ascending
        with wrap-around.
      operationId: canfieldExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, draw, d, move, m, giveup, g, hint, h, autocomplete, ac, log, l, undo, u, undo_n]
                sessionId:
                  type: string
                from:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [waste, reserve, tableau]
                    col:
                      type: integer
                    cardIndex:
                      type: integer
                to:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [tableau, foundation]
                    col:
                      type: integer
                n:
                  type: integer
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  tableau:
                    type: array
                  reserve:
                    type: array
                  stockCount:
                    type: integer
                  waste:
                    type: array
                  foundation:
                    type: array
                  baseRank:
                    type: integer
                  phase:
                    type: integer
                  moveCount:
                    type: integer
                  canUndo:
                    type: boolean
                  message:
                    type: string
                  messageCode:
                    type: string
        '400':
          description: Bad request

  /osmosis/exec:
    post:
      tags:
        - osmosis
      summary: Execute an Osmosis (オズモシス / 浸透) game command
      description: |
        Send a command to advance an Osmosis solitaire session identified by `sessionId`.
        Osmosis uses 4 reserve columns of 4 cards each, 4 vertical foundation rows, and a
        stock that draws 1 card at a time to the waste (recyclable). The foundation base
        rank is set by the first card dealt. Each foundation row holds a single suit: the
        top row builds in any order, while a lower row accepts a card only if a card of the
        same rank already appears in the row directly above ("osmosis"/seep). Moves are
        waste→foundation and reserve→foundation; the destination foundation row is given by
        `to.col`, and the reserve source column by `from.col`.
      operationId: osmosisExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, draw, d, move, m, giveup, g, hint, h, autocomplete, ac, log, l, undo, u, undo_n]
                sessionId:
                  type: string
                from:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [waste, reserve]
                    col:
                      type: integer
                to:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [foundation]
                    col:
                      type: integer
                n:
                  type: integer
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  reserve:
                    type: array
                  stockCount:
                    type: integer
                  waste:
                    type: array
                  foundation:
                    type: array
                  baseRank:
                    type: integer
                  phase:
                    type: integer
                  moveCount:
                    type: integer
                  canUndo:
                    type: boolean
                  message:
                    type: string
                  messageCode:
                    type: string
        '400':
          description: Bad request

  /bristol/exec:
    post:
      tags:
        - bristol
      summary: Execute a Bristol (ブリストル) game command
      description: |
        Send a command to advance a Bristol solitaire session identified by `sessionId`.
        Bristol uses 8 tableau columns dealt 3 cards each, 3 fans (reserve piles), a stock,
        and 4 foundations. `draw` deals one card from the stock to each of the three fans.
        Moves take a `from` zone (`tableau` with `from.col`, or `fan` with `from.col`) and a
        `to` zone (`tableau` with `to.col` to build down regardless of suit, or `foundation`
        whose target pile is auto-selected). Emptied tableau columns can no longer hold a
        card. Foundations build up Ace-to-King regardless of suit; clearing all four wins.
      operationId: bristolExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, draw, d, move, m, giveup, g, hint, h, autocomplete, ac, log, l, undo, u, undo_n]
                sessionId:
                  type: string
                from:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [tableau, fan]
                    col:
                      type: integer
                to:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [tableau, foundation]
                    col:
                      type: integer
                n:
                  type: integer
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  tableau:
                    type: array
                  fan:
                    type: array
                  stockCount:
                    type: integer
                  foundation:
                    type: array
                  phase:
                    type: integer
                  moveCount:
                    type: integer
                  canUndo:
                    type: boolean
                  message:
                    type: string
                  messageCode:
                    type: string
        '400':
          description: Bad request

  /bidwhist/exec:
    post:
      tags:
        - bidwhist
      summary: Execute a Bid Whist (ビッド・ホイスト) game command
      description: |
        Send a command to advance a Bid Whist session identified by `sessionId`.
        Bid Whist is a 4-player partnership trick-taking game on a 54-card deck
        (52 + 2 jokers). Bidding (`b`/`bid`) sets `bidTricks` (1-7, target tricks
        over the book of 6) and `bidDirection` (0=Uptown, 1=Downtown, 2=NoTrump);
        `pass` (`pa`) passes. The high bidder declares trump with `trump` (`t`,
        `trumpSuit` 1=♠ 2=♣ 3=♥ 4=♦) for Uptown/Downtown contracts, then exchanges
        the 6-card kitty via `exchange` (`discardIndices`, six indices). During play
        (`play`), `cardIndex` selects the card. `next` advances the trick,
        `nextround` scores and deals the next round.
      operationId: bidWhistExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bid, b, pass, pa, trump, t, exchange, e, play, p, next, n, nextround, nr, hint, log, l]
                sessionId:
                  type: string
                bidTricks:
                  type: integer
                  description: Target tricks over the book of 6 (1-7)
                bidDirection:
                  type: integer
                  description: 0=Uptown (Ace high), 1=Downtown (2 high), 2=No Trump
                trumpSuit:
                  type: integer
                  description: 1=♠ 2=♣ 3=♥ 4=♦ (trump declaration)
                discardIndices:
                  type: array
                  items:
                    type: integer
                cardIndex:
                  type: integer
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                    targetScore:
                      type: integer
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  players:
                    type: array
                  phase:
                    type: integer
                  roundNumber:
                    type: integer
                  trickNumber:
                    type: integer
                  currentPlayerIdx:
                    type: integer
                  bidPlayerIdx:
                    type: integer
                  dealerIdx:
                    type: integer
                  leadPlayerIdx:
                    type: integer
                  trumpSuit:
                    type: integer
                  contractTricks:
                    type: integer
                  contractDirection:
                    type: integer
                  declarerIdx:
                    type: integer
                  highestBidder:
                    type: integer
                  kittyCount:
                    type: integer
                  currentTrick:
                    type: array
                  teamScores:
                    type: array
                    items:
                      type: integer
                  gameEndFlag:
                    type: boolean
                  winnerTeam:
                    type: integer
                  message:
                    type: string
                  messageCode:
                    type: string
        '400':
          description: Bad request

  /fivehundred/exec:
    post:
      tags:
        - fivehundred
      summary: Execute a 500 (Five Hundred / ファイブハンドレッド) game command
      description: |
        Send a command to advance a 500 game session identified by `sessionId`.
        500 is a 4-player partnership trick-taking game. Bidding (`b`/`bid`) sets a
        contract via `bidKind` (1=Suit, 2=NoTrump, 3=Misère, 4=Open Misère),
        `bidTricks` (6-10 for Suit/NT), and `bidSuit` (1=♠ 2=♣ 3=♥ 4=♦ for Suit
        contracts). The high bidder takes the 3-card kitty and discards 3 via
        `exchange` (`discardIndices`). During play (`play`), `cardIndex` selects the
        card; `jokerSuit` nominates the suit when leading the joker in a no-trump
        contract. `next` advances the trick, `nextround` scores and deals the next round.
      operationId: fiveHundredExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bid, b, pass, pa, exchange, e, play, p, next, n, nextround, nr, hint, log, l]
                sessionId:
                  type: string
                bidKind:
                  type: integer
                  description: 1=Suit, 2=NoTrump, 3=Misère, 4=Open Misère
                bidTricks:
                  type: integer
                bidSuit:
                  type: integer
                discardIndices:
                  type: array
                  items:
                    type: integer
                cardIndex:
                  type: integer
                jokerSuit:
                  type: integer
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                    targetScore:
                      type: integer
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  players:
                    type: array
                  phase:
                    type: integer
                  roundNumber:
                    type: integer
                  trickNumber:
                    type: integer
                  currentPlayerIdx:
                    type: integer
                  bidPlayerIdx:
                    type: integer
                  trumpSuit:
                    type: integer
                  contractKind:
                    type: integer
                  contractValue:
                    type: integer
                  declarerIdx:
                    type: integer
                  currentTrick:
                    type: array
                  teamScores:
                    type: array
                    items:
                      type: integer
                  gameEndFlag:
                    type: boolean
                  winnerTeam:
                    type: integer
                  message:
                    type: string
                  messageCode:
                    type: string
        '400':
          description: Bad request

  /fiftyone/exec:
    post:
      tags:
        - fiftyone
      summary: Execute a Fifty-one (フィフティワン) game command
      description: |
        4-player card exchange game. Players swap cards between their hand and face-up table cards,
        aiming to maximize the sum of same-suit cards (A=11, J/Q/K=10, 2-10=face value, max=51).

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Play    | 0     | Active play |
        | GameEnd | 1     | Game over   |

        | Command     | Description                              |
        |-------------|------------------------------------------|
        | reset / r   | Start new game                           |
        | play        | Exchange 1 card (requires handIdx, tableIdx) |
        | exchangeall | Exchange all 5 cards                     |
        | stop        | Call stop                                |
        | log / l     | View action log                          |
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [command, sessionId]
              properties:
                command:
                  type: string
                  enum: [reset, r, play, exchangeall, stop, log, l]
                handIdx:
                  type: integer
                  description: Hand card index (0-4, required for play)
                tableIdx:
                  type: integer
                  description: Table card index (0-4, required for play)
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      enum: [0, 1, 2]
                      description: "0=Easy, 1=Normal, 2=Hard"
                sessionId:
                  type: string
      responses:
        '200':
          description: Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  players:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: integer
                        isHuman:
                          type: boolean
                        cardCount:
                          type: integer
                        cards:
                          type: array
                          items:
                            $ref: '#/components/schemas/Card'
                        score:
                          type: integer
                  tableCards:
                    type: array
                    items:
                      $ref: '#/components/schemas/Card'
                  phase:
                    type: integer
                  currentTurn:
                    type: integer
                  gameEndFlag:
                    type: boolean
                  winnerIdx:
                    type: integer
                  turnNumber:
                    type: integer
                  stopCallerIdx:
                    type: integer
                  lastAction:
                    type: string
                  message:
                    type: string
                  messageCode:
                    type: string
        '400':
          description: Bad request

  /yukon/exec:
    post:
      tags:
        - yukon
      summary: Execute a Yukon (ユーコン) game command
      description: |
        Send a command to advance a Yukon solitaire session identified by `sessionId`.
        Yukon is a Klondike variant with no stock/waste pile -- all 52 cards are dealt
        to 7 tableau columns. Cards can be moved in groups regardless of sequence order.
        Build foundations by suit Ace to King.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | 0     | Game in progress |
        | GameClear | 1     | All foundations complete |
        | GameOver  | 2     | Player gave up or stalemate |

        | Command          | Aliases | Description |
        |------------------|---------|-------------|
        | `reset`          | `r`     | Start / restart a game |
        | `move`           | `m`     | Move card(s) between zones (requires `from`, `to`) |
        | `giveup`         | `g`     | Give up the current game |
        | `hint`           | `h`     | Get a move suggestion |
        | `autocomplete`   | `ac`    | Auto-complete to foundations |
        | `undo`           | `u`     | Undo last move |
        | `undo_n`         | —       | Undo N moves (requires `n`) |
        | `log`            | `l`     | View action log |
      operationId: yukonExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [tableau]
                    col:
                      type: integer
                    cardIndex:
                      type: integer
                to:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [tableau, foundation]
                    col:
                      type: integer
                n:
                  type: integer
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  tableau:
                    type: array
                  foundation:
                    type: array
                  phase:
                    type: integer
                  moveCount:
                    type: integer
                  canUndo:
                    type: boolean
                  isStalemate:
                    type: boolean
                  undoToEscape:
                    type: integer
                  hint:
                    type: object
                    properties:
                      fromCol:
                        type: integer
                      cardIndex:
                        type: integer
                      toZone:
                        type: string
                      toCol:
                        type: integer
                  message:
                    type: string
                  messageCode:
                    type: string
        '400':
          description: Bad request

  /russiansolitaire/exec:
    post:
      tags:
        - russiansolitaire
      summary: Execute a Russian Solitaire (ロシアンソリティア) game command
      description: |
        Send a command to advance a Russian Solitaire session identified by `sessionId`.
        Russian Solitaire is a Yukon variant: identical deal and "move any face-up card with
        everything stacked on top" mechanic, but tableau-to-tableau moves require **same suit
        descending** instead of Yukon's alternating colour. Build foundations by suit Ace to King.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | 0     | Game in progress |
        | GameClear | 1     | All foundations complete |
        | GameOver  | 2     | Player gave up or stalemate |

        | Command          | Aliases | Description |
        |------------------|---------|-------------|
        | `reset`          | `r`     | Start / restart a game |
        | `move`           | `m`     | Move card(s) between zones (requires `from`, `to`) |
        | `giveup`         | `g`     | Give up the current game |
        | `hint`           | `h`     | Get a move suggestion |
        | `autocomplete`   | `ac`    | Auto-complete to foundations |
        | `undo`           | `u`     | Undo last move |
        | `undo_n`         | —       | Undo N moves (requires `n`) |
        | `log`            | `l`     | View action log |
      operationId: russianSolitaireExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [tableau]
                    col:
                      type: integer
                    cardIndex:
                      type: integer
                to:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [tableau, foundation]
                    col:
                      type: integer
                n:
                  type: integer
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  tableau:
                    type: array
                  foundation:
                    type: array
                  phase:
                    type: integer
                  moveCount:
                    type: integer
                  canUndo:
                    type: boolean
                  isStalemate:
                    type: boolean
                  undoToEscape:
                    type: integer
                  hint:
                    type: object
                    properties:
                      fromCol:
                        type: integer
                      cardIndex:
                        type: integer
                      toZone:
                        type: string
                      toCol:
                        type: integer
                  message:
                    type: string
                  messageCode:
                    type: string
        '400':
          description: Bad request

  /easthaven/exec:
    post:
      tags:
        - easthaven
      summary: Execute an Easthaven (イーストヘイブン) game command
      description: |
        Send a command to advance an Easthaven session identified by `sessionId`.
        Easthaven fuses Klondike and Spider: the 52-card deck is dealt as 7 columns of 3
        (2 face-down + 1 face-up), the tableau builds down in alternating colours, foundations
        build A-to-K by suit, and an empty column accepts only a King. The stock deals
        Spider-style — one face-up card to **every** column at once (`deal`), blocked while any
        column is empty. There is no waste pile.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | 0     | Game in progress |
        | GameClear | 1     | All foundations complete |
        | GameOver  | 2     | Player gave up or stalemate |

        | Command          | Aliases | Description |
        |------------------|---------|-------------|
        | `reset`          | `r`     | Start / restart a game |
        | `deal`           | `d`     | Deal one card to every tableau column |
        | `move`           | `m`     | Move card(s) between zones (requires `from`, `to`) |
        | `giveup`         | `g`     | Give up the current game |
        | `hint`           | `h`     | Get a move suggestion |
        | `autocomplete`   | `ac`    | Auto-complete to foundations |
        | `undo`           | `u`     | Undo last move |
        | `undo_n`         | —       | Undo N moves (requires `n`) |
        | `log`            | `l`     | View action log |
      operationId: easthavenExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, deal, d, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [tableau]
                    col:
                      type: integer
                    cardIndex:
                      type: integer
                to:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [tableau, foundation]
                    col:
                      type: integer
                n:
                  type: integer
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  tableau:
                    type: array
                  foundation:
                    type: array
                  stockCount:
                    type: integer
                  phase:
                    type: integer
                  moveCount:
                    type: integer
                  canUndo:
                    type: boolean
                  isStalemate:
                    type: boolean
                  undoToEscape:
                    type: integer
                  hint:
                    type: object
                    properties:
                      fromCol:
                        type: integer
                      cardIndex:
                        type: integer
                      toZone:
                        type: string
                      toCol:
                        type: integer
                  message:
                    type: string
                  messageCode:
                    type: string
        '400':
          description: Bad request

  /tichu/exec:
    post:
      tags:
        - tichu
      summary: Execute a Tichu (ティチュー) game command
      description: |
        Send a command to advance a Tichu session identified by `sessionId`.
        Tichu is a 4-player, 2-vs-2 partnership shedding game (teams {P0,P2} vs {P1,P3})
        on a 56-card deck (standard 52 + Dragon/Phoenix/Dog/Mahjong). Each player is dealt
        14 cards. Before play, players may declare Tichu (±100) or Grand Tichu (±200).

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Declare | 0     | Declaration phase |
        | Play    | 1     | Trick play |
        | End     | 2     | Deal over |

        | Command         | Aliases | Description |
        |-----------------|---------|-------------|
        | `reset`         | `r`     | Start / restart a deal (optional `config`) |
        | `declare`       | `d`     | Declare (`declType`: 0=none, 1=Tichu, 2=Grand) |
        | `play`          | `p`     | Play cards by `indices` (empty = pass) |
        | `setdifficulty` | `sd`    | Set CPU difficulty (reset) |
        | `log`           | `l`     | View action log |
      operationId: tichuExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, declare, d, play, p, log, l]
                sessionId:
                  type: string
                indices:
                  type: array
                  items:
                    type: integer
                declType:
                  type: integer
                  enum: [0, 1, 2]
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      enum: [0, 1, 2]
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  players:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: integer
                        isHuman:
                          type: boolean
                        isFinished:
                          type: boolean
                        team:
                          type: integer
                        rank:
                          type: integer
                        declType:
                          type: integer
                        cardCount:
                          type: integer
                        cards:
                          type: array
                  phase:
                    type: string
                  currentTurn:
                    type: integer
                  tableCards:
                    type: array
                  tableCombo:
                    type: string
                  lastPlayIdx:
                    type: integer
                  startLeader:
                    type: integer
                  finishOrder:
                    type: array
                    items:
                      type: integer
                  scores:
                    type: array
                    items:
                      type: integer
                  isOneTwo:
                    type: boolean
                  bombCount:
                    type: integer
                  gameEndFlag:
                    type: boolean
                  message:
                    type: string
                  messageCode:
                    type: string
        '400':
          description: Bad request

  /cruel/exec:
    post:
      tags:
        - cruel
      summary: Execute a Cruel (クルーエル) game command
      description: |
        Send a command to advance a Cruel solitaire session identified by `sessionId`.
        Cruel is a puzzle-style solitaire. At deal time the four Aces are auto-placed on
        the foundations and the remaining 48 cards are dealt face-up into 12 columns of 4.
        Only the top card of each column can be moved, and only onto a same-suit card one
        rank higher. Empty columns cannot receive cards. When stuck, **Shift** gathers the
        remaining tableau cards left-to-right (preserving column order and in-column order)
        and redeals them into 12×4 left-aligned columns.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | 0     | Game in progress |
        | GameClear | 1     | All foundations complete |
        | GameOver  | 2     | Player gave up |

        | Command          | Aliases | Description |
        |------------------|---------|-------------|
        | `reset`          | `r`     | Start / restart a game |
        | `move`           | `m`     | Move top card between zones (requires `from`, `to`) |
        | `shift`          | `s`     | Gather and re-deal the tableau |
        | `giveup`         | `g`     | Give up the current game |
        | `hint`           | `h`     | Get a move suggestion |
        | `autocomplete`   | `ac`    | Auto-promote any tableau top cards to foundations |
        | `undo`           | `u`     | Undo last move |
        | `undo_n`         | —       | Undo N moves (requires `n`) |
        | `log`            | `l`     | View action log |
      operationId: cruelExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, move, m, shift, s, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [tableau]
                    col:
                      type: integer
                to:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [tableau, foundation]
                    col:
                      type: integer
                n:
                  type: integer
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  tableau:
                    type: array
                  foundation:
                    type: array
                  phase:
                    type: integer
                  moveCount:
                    type: integer
                  canUndo:
                    type: boolean
                  isStalemate:
                    type: boolean
                  undoToEscape:
                    type: integer
                  hint:
                    type: object
                    properties:
                      fromCol:
                        type: integer
                      cardIndex:
                        type: integer
                      toZone:
                        type: string
                      toCol:
                        type: integer
                  message:
                    type: string
                  messageCode:
                    type: string
        '400':
          description: Bad request

  /scorpion/exec:
    post:
      tags:
        - scorpion
      summary: Execute a Scorpion (スコーピオン) game command
      description: |
        Send a command to advance a Scorpion solitaire session identified by `sessionId`.
        Scorpion combines Yukon's "move any face-up card (with everything above it) in a group"
        mechanic with Spider's "same-suit K-to-A sequence is removed from the board" mechanic.
        49 cards are dealt into 7 columns (columns 0-3 start with 3 face-down + 4 face-up;
        columns 4-6 are fully face-up). Remaining 3 cards form the stock.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | 0     | Game in progress |
        | GameClear | 1     | All 4 suits removed |
        | GameOver  | 2     | Player gave up |

        | Command          | Aliases | Description |
        |------------------|---------|-------------|
        | `reset`          | `r`     | Start / restart a game |
        | `deal`           | `d`     | Deal 3 stock cards to columns 0-2 (requires no empty column) |
        | `move`           | `m`     | Move card(s) between tableau columns (requires `from`, `to`) |
        | `giveup`         | `g`     | Give up the current game |
        | `hint`           | `h`     | Get a move suggestion |
        | `autocomplete`   | `ac`    | Auto-remove completed suits when all cards are face-up |
        | `undo`           | `u`     | Undo last move |
        | `undo_n`         | —       | Undo N moves (requires `n`) |
        | `log`            | `l`     | View action log |
      operationId: scorpionExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, deal, d, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [tableau]
                    col:
                      type: integer
                    cardIndex:
                      type: integer
                to:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [tableau]
                    col:
                      type: integer
                n:
                  type: integer
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  tableau:
                    type: array
                  stockCount:
                    type: integer
                  completedSuits:
                    type: integer
                  phase:
                    type: integer
                  moveCount:
                    type: integer
                  canUndo:
                    type: boolean
                  isStalemate:
                    type: boolean
                  undoToEscape:
                    type: integer
                  hint:
                    type: object
                    properties:
                      fromCol:
                        type: integer
                      cardIndex:
                        type: integer
                      toCol:
                        type: integer
                  message:
                    type: string
                  messageCode:
                    type: string
        '400':
          description: Bad request

  /wasp/exec:
    post:
      tags:
        - wasp
      summary: Execute a Wasp (ワスプ) game command
      description: |
        Send a command to advance a Wasp solitaire session identified by `sessionId`.
        Wasp is an easier variant of Scorpion: it shares every rule with Scorpion
        (Yukon-style group moves of same-suit descending runs, same-suit K-to-A removal,
        a 3-card stock, and a Deal command blocked while any column is empty) except
        that an empty column accepts **any** card, not only a King — making stalemates rare.
        49 cards are dealt into 7 columns (columns 0-3 start with 3 face-down + 4 face-up;
        columns 4-6 are fully face-up). Remaining 3 cards form the stock.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | 0     | Game in progress |
        | GameClear | 1     | All 4 suits removed |
        | GameOver  | 2     | Player gave up |

        | Command          | Aliases | Description |
        |------------------|---------|-------------|
        | `reset`          | `r`     | Start / restart a game |
        | `deal`           | `d`     | Deal 3 stock cards to columns 0-2 (requires no empty column) |
        | `move`           | `m`     | Move card(s) between tableau columns (requires `from`, `to`) |
        | `giveup`         | `g`     | Give up the current game |
        | `hint`           | `h`     | Get a move suggestion |
        | `autocomplete`   | `ac`    | Auto-remove completed suits when all cards are face-up |
        | `undo`           | `u`     | Undo last move |
        | `undo_n`         | —       | Undo N moves (requires `n`) |
        | `log`            | `l`     | View action log |
      operationId: waspExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, deal, d, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [tableau]
                    col:
                      type: integer
                    cardIndex:
                      type: integer
                to:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [tableau]
                    col:
                      type: integer
                n:
                  type: integer
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  tableau:
                    type: array
                  stockCount:
                    type: integer
                  completedSuits:
                    type: integer
                  phase:
                    type: integer
                  moveCount:
                    type: integer
                  canUndo:
                    type: boolean
                  isStalemate:
                    type: boolean
                  undoToEscape:
                    type: integer
                  hint:
                    type: object
                    properties:
                      fromCol:
                        type: integer
                      cardIndex:
                        type: integer
                      toCol:
                        type: integer
                  message:
                    type: string
                  messageCode:
                    type: string
        '400':
          description: Bad request

  /accordion/exec:
    post:
      tags:
        - accordion
      summary: Execute an Accordion (アコーディオン) game command
      description: |
        Send a command to advance an Accordion compression solitaire session identified by `sessionId`.
        A standard 52-card deck is dealt one pile per card in a single row. A pile may be moved onto
        the pile 1 or 3 positions to its left when the top cards match in suit or rank; the source pile
        is then absorbed and every pile to its right shifts left to close the gap. The game clears when
        all cards are compressed into a single pile.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | 0     | Game in progress |
        | GameClear | 1     | All cards in one pile |
        | GameOver  | 2     | Player gave up |

        | Command  | Aliases | Description |
        |----------|---------|-------------|
        | `reset`  | `r`     | Start / restart a game |
        | `move`   | `m`     | Merge pile `from.index` onto pile `to.index` (`to` must be `from-1` or `from-3`) |
        | `giveup` | `g`     | Give up the current game |
        | `hint`   | `h`     | Get a move suggestion |
        | `undo`   | `u`     | Undo last move |
        | `undo_n` | —       | Undo N moves (requires `n`) |
        | `log`    | `l`     | View action log |
      operationId: accordionExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, move, m, giveup, g, hint, h, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [pile]
                    index:
                      type: integer
                to:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [pile]
                    index:
                      type: integer
                n:
                  type: integer
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  piles:
                    type: array
                    items:
                      type: object
                      properties:
                        cards:
                          type: array
                          items:
                            $ref: '#/components/schemas/Card'
                        size:
                          type: integer
                  pileCount:
                    type: integer
                  phase:
                    type: integer
                  moveCount:
                    type: integer
                  canUndo:
                    type: boolean
                  isStalemate:
                    type: boolean
                  undoToEscape:
                    type: integer
                  hint:
                    type: object
                    properties:
                      fromIdx:
                        type: integer
                      toIdx:
                        type: integer
                  message:
                    type: string
                  messageCode:
                    type: string
        '400':
          description: Bad request

  /trash/exec:
    post:
      tags:
        - trash
      summary: Execute a Trash (トラッシュ) game command
      description: |
        Send a command to advance a Trash (Garbage) session identified by `sessionId`.
        Two players (1 human vs 1 CPU) each have 10 face-down slots. On a turn the
        player draws from the stock; Ace-10 auto-places at the matching position
        (continuing the chain), K/Joker becomes a wild the player places manually,
        J/Q ends the turn. First to flip all 10 slots wins. Uses a 54-card deck
        (52 + 2 jokers); the stock refills from the shuffled discard pile when
        empty.

        | Phase       | Value | Description |
        |-------------|-------|-------------|
        | PlayerTurn  | 0     | Active player may draw |
        | AwaitWild   | 1     | Active player must choose a position for the drawn wild |
        | GameOver    | 2     | A player flipped all 10 slots |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `draw`  | `d`     | Draw one card from the stock |
        | `place` | `p`     | Place the pending wild at `position` (1..10) |
        | `cpu`   | —       | Advance the CPU turn by one step |
        | `log`   | `l`     | View action log (available after game over) |
      operationId: trashExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [command, sessionId]
              properties:
                command:
                  type: string
                  enum: [reset, r, draw, d, place, p, cpu, log, l, quit, q]
                sessionId:
                  type: string
                position:
                  type: integer
                  description: Slot index (1..10) for wild placement
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  phase:
                    type: integer
                  current:
                    type: integer
                  players:
                    type: array
                    minItems: 2
                    maxItems: 2
                    items:
                      type: object
                      properties:
                        slots:
                          type: array
                          minItems: 10
                          maxItems: 10
                          items:
                            type: object
                            properties:
                              card:
                                $ref: '#/components/schemas/Card'
                              faceUp:
                                type: boolean
                        isCpu:
                          type: boolean
                  stockSize:
                    type: integer
                  discardSize:
                    type: integer
                  discardTop:
                    $ref: '#/components/schemas/Card'
                  pending:
                    $ref: '#/components/schemas/Card'
                  moveCount:
                    type: integer
                  winner:
                    type: integer
                    description: -1 until the game ends; 0 for the human, 1 for the CPU
                  message:
                    type: string
                  messageCode:
                    type: string
                  messageParams:
                    type: object
                    additionalProperties:
                      type: string
        '400':
          description: Bad request

  /whist/exec:
    post:
      tags:
        - whist
      summary: Execute a Whist (ホイスト) game command
      description: |
        4-player 2v2 partnership trick-taking game. No bidding phase.
        Trump suit is determined by the last card dealt to the dealer.
        Teams score 1 point per trick above 6. First team to point limit wins.
        Commands: reset, play, next, nextround, hint, log.
      operationId: whistExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [command, sessionId]
              properties:
                command:
                  type: string
                  enum: [reset, play, next, nextround, hint, log, quit]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      minimum: 0
                      maximum: 2
                    pointLimit:
                      type: integer
                      minimum: 1
      responses:
        '200':
          description: Game state after executing the command
          content:
            application/json:
              schema:
                type: object
                properties:
                  players:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: integer
                        isHuman:
                          type: boolean
                        cardCount:
                          type: integer
                        cards:
                          type: array
                          items:
                            type: object
                        roundScore:
                          type: integer
                        cumulativeScore:
                          type: integer
                        trickCount:
                          type: integer
                        team:
                          type: integer
                  phase:
                    type: integer
                  roundNumber:
                    type: integer
                  trickNumber:
                    type: integer
                  currentPlayerIdx:
                    type: integer
                  currentTrick:
                    type: array
                    items:
                      type: object
                  trumpSuit:
                    type: integer
                  dealerIdx:
                    type: integer
                  teamScores:
                    type: array
                    items:
                      type: integer
                  gameEndFlag:
                    type: boolean
                  winnerTeam:
                    type: integer
                  leadPlayerIdx:
                    type: integer
                  message:
                    type: string
                  messageCode:
                    type: string
                  config:
                    type: object
                    properties:
                      cpuDifficulty:
                        type: integer
                      pointLimit:
                        type: integer
        '400':
          description: Bad request

  /catchten/exec:
    post:
      tags:
        - catchten
      summary: Execute a Catch the Ten (Scotch Whist) game command
      description: |
        4-player 2v2 partnership trick-taking game on a 36-card deck (ranks 6-A).
        The trump suit is fixed by the last card dealt to the dealer. In the trump
        suit only, J is the highest card, then A, K, Q, 10, 9, 8, 7, 6; non-trump
        suits are A high down to 6. The trick winner scores the trump honors it
        contains (J=11, 10=10, A=4, K=3, Q=2). First side to the point limit
        (default 41) wins; an exact same-deal tie is a draw (winnerTeam = -2).
        Commands: reset, play, next, nextround, hint, log.
      operationId: catchTenExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [command, sessionId]
              properties:
                command:
                  type: string
                  enum: [reset, play, next, nextround, hint, log, quit]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      minimum: 0
                      maximum: 2
                    pointLimit:
                      type: integer
                      minimum: 1
      responses:
        '200':
          description: Game state after executing the command
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CatchTenResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CatchTenResponse'

  /letitride/exec:
    post:
      tags:
        - letitride
      summary: Execute a Let It Ride game command
      description: |
        Send a command to advance the Let It Ride (レットイットライド)
        game session identified by `sessionId`. Let It Ride is a single-player
        casino poker game using a standard 52-card deck. Player places 3 equal
        bets, receives 3 cards plus 2 face-down community cards. Before each
        community card is revealed, the player can Pull (withdraw) that bet or
        Let It Ride (keep it). Final 5-card hand is evaluated. Pays on a pair
        of 10s or better. Payout table: Royal Flush 1000:1, Straight Flush
        200:1, Four of a Kind 50:1, Full House 11:1, Flush 8:1, Straight 5:1,
        Three of a Kind 3:1, Two Pair 2:1, Pair of 10s or Better 1:1.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Bet        | `1`   | Place 3 equal bets |
        | Decision1  | `2`   | First community card decision (Pull or Let It Ride) |
        | Decision2  | `3`   | Second community card decision (Pull or Let It Ride) |
        | End        | `4`   | Round complete; shows result and payout |

        | Command     | Aliases | Description |
        |-------------|---------|-------------|
        | `reset`     | `r`     | Start / restart a game |
        | `bet`       | `b`     | Place 3 equal bets (requires `amount`) |
        | `pull`      | `p`     | Pull (withdraw) the current bet |
        | `letitride` | `l`     | Let It Ride (keep the current bet) |
        | `log`       | `lg`    | View action log |
      operationId: letitrideExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LetItRideRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Bet 100 on each of the 3 spots
                value:
                  command: bet
                  amount: 100
                  sessionId: my-session-001
              pull:
                summary: Pull (withdraw) the current bet
                value:
                  command: pull
                  sessionId: my-session-001
              letitride:
                summary: Let It Ride (keep the current bet)
                value:
                  command: letitride
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LetItRideResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LetItRideResponse'
              examples:
                paramError:
                  value:
                    playerHand: []
                    communityCards: []
                    phase: 0
                    chips: 0
                    betAmount: 0
                    bet1Active: false
                    bet2Active: false
                    bet3Active: false
                    result: 0
                    handRank: 0
                    bet1Payout: 0
                    bet2Payout: 0
                    bet3Payout: 0
                    totalPayout: 0
                    message: param error.

  /pokersquares/exec:
    post:
      tags:
        - pokersquares
      summary: Execute a Poker Squares game command
      description: |
        Send a command to advance the Poker Squares (ポーカー・スクエア)
        game session identified by `sessionId`. Poker Squares is a
        single-player solitaire puzzle using a standard 52-card deck.
        Cards are drawn one at a time from a shuffled deck and must be
        placed on an empty cell of a 5x5 grid (25 cells). Once placed, a
        card cannot be moved. After all 25 cells are filled, the 5 rows
        and 5 columns form 10 five-card poker hands, each scored via the
        American scoring system: Royal Flush 100, Straight Flush 75,
        Four of a Kind 50, Full House 25, Flush 20, Straight 15,
        Three of a Kind 10, Two Pair 5, One Pair 2, High Card 0.
        The total score is the sum of all 10 hand scores.

        The game proceeds through these phases:

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | `0`   | Game in progress (placing cards) |
        | Complete  | `1`   | All 25 cells filled; final score shown |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset`  | `r`  | Start / restart a game |
        | `place`  | `p`  | Place the current card on cell (`row`, `col`) (0-4) |
        | `undo`   | `u`  | Undo the last placement |
        | `giveup` | `g`  | Give up the current game |
        | `log`    | `l`  | View action log |
        | `hint`   | `h`  | Get a synergy-aware placement suggestion |
      operationId: pokersquaresExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PokerSquaresRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              place:
                summary: Place the current card on row 2, col 3
                value:
                  command: place
                  row: 2
                  col: 3
                  sessionId: my-session-001
              undo:
                summary: Undo the last placement
                value:
                  command: undo
                  sessionId: my-session-001
              giveup:
                summary: Give up the current game
                value:
                  command: giveup
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PokerSquaresResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PokerSquaresResponse'
              examples:
                paramError:
                  value:
                    board: []
                    placedCount: 0
                    phase: 0
                    canUndo: false
                    rowScores: []
                    colScores: []
                    totalScore: 0
                    message: "param error: row and col are required."

  /reddog/exec:
    post:
      tags:
        - reddog
      summary: Execute a Red Dog game command
      description: |
        Send a command to advance the Red Dog (レッドドッグ) game session
        identified by `sessionId`. Red Dog is a single-player casino card game
        using a standard 52-card deck. Player places an ante bet, then 2 cards
        are dealt. If ranks are consecutive → push; if equal (pair) → a third
        card is dealt (match = 11:1, else push). Otherwise a spread is shown
        and the player may raise (up to ante) or stay before the third card.

        | Phase           | Value | Description |
        |-----------------|-------|-------------|
        | Bet             | `1`   | Place ante bet |
        | InitialDealt    | `2`   | 2 cards dealt (auto-resolved) |
        | SpreadDecision  | `3`   | Raise or Stay |
        | PairThird       | `4`   | Third card for pair (auto) |
        | End             | `5`   | Round complete |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `bet`   | `b`     | Place ante (requires `amount`) |
        | `raise` | —       | Raise before third card (requires `amount`, max ante) |
        | `stay`  | `s`     | Stay (no raise) and draw third card |
        | `log`   | —       | View action log |
      operationId: reddogExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RedDogRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Bet 100 as ante
                value:
                  command: bet
                  amount: 100
                  sessionId: my-session-001
              raise:
                summary: Raise 50
                value:
                  command: raise
                  amount: 50
                  sessionId: my-session-001
              stay:
                summary: Stay (no raise)
                value:
                  command: stay
                  sessionId: my-session-001
      responses:
        '200':
          description: Updated game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RedDogResponse'

  /sevencardstudhilo/exec:
    post:
      tags:
        - sevencardstudhilo
      summary: Execute a Seven Card Stud Hi-Lo game command
      description: |
        Send a command to advance the Seven Card Stud Hi-Lo (8 or Better)
        session identified by `sessionId`. It shares the whole Seven Card Stud
        stack -- deal, streets, betting and the response schema -- and differs
        only at the showdown, where the pot splits.

        * The **high** half is ordinary stud poker.
        * The **low** half needs five ranks of **eight or lower with no pair**
          (straights and flushes are ignored, the ace is always low). The best
          low is A-2-3-4-5.
        * **If nobody makes a qualifying low, the high takes the whole pot.**
          That is the point of eight-or-better: chasing a low and missing it
          leaves nothing behind.
        * High and low are chosen independently from the same seven cards, so
          one player can scoop both halves.
        * When the pot does split, the **odd chip goes to the high side** (the
          usual poker convention).

        Each showdown result carries `lowQualifies`, `lowBestHand` and
        `wonLow`; `wonAmount` is the high and low winnings combined, so the
        chips on screen always add up. `isHiLo` is set on the response so the
        client does not have to infer the variant from the route.
      operationId: sevenCardStudHiLoExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SevenCardStudRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              fold:
                summary: Fold
                value:
                  command: fold
                  sessionId: my-session-001
              call:
                summary: Call
                value:
                  command: call
                  sessionId: my-session-001
              bet:
                summary: Place a bet of 40
                value:
                  command: bet
                  amount: 40
                  sessionId: my-session-001
              allin:
                summary: Go all-in
                value:
                  command: allin
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SevenCardStudResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SevenCardStudResponse'
              examples:
                paramError:
                  value:
                    players: []
                    pot: 0
                    sidePots: []
                    dealerIdx: 0
                    currentTurn: 0
                    phase: 0
                    gameEndFlag: false
                    lastBet: 0
                    minRaise: 0
                    roundResults: []
                    cpuActions: []
                    message: param error.

  /razz/exec:
    post:
      tags:
        - razz
      summary: Execute a Razz game command
      description: |
        Send a command to advance the Razz game session identified by
        `sessionId`.  Razz is a lowball variant of Seven Card Stud where
        the lowest hand wins using A-5 lowball rules.  Ace is always low,
        straights and flushes don't count against you.  The best possible
        hand is A-2-3-4-5 (the wheel).

        Each player receives 2 face-down hole cards and 1 face-up door card
        on Third Street, then 1 face-up card each on Fourth through Sixth
        Street, and 1 final face-down card on Seventh Street.  Uses antes
        and bring-in (highest door card brings in, unlike Seven Card Stud
        where lowest brings in).  Best low 5 out of 7 cards wins.

        The game supports configurable table sizes: 2-max through 7-max
        (default 4, i.e., 1 human + 3 CPU).

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Init       | `0`   | Before any cards are dealt |
        | ThirdSt    | `1`   | 2 hole cards + 1 door card; first betting round (bring-in) |
        | FourthSt   | `2`   | 4th card (face-up); betting round |
        | FifthSt    | `3`   | 5th card (face-up); betting round |
        | SixthSt    | `4`   | 6th card (face-up); betting round |
        | SeventhSt  | `5`   | 7th card (face-down); final betting round |
        | Showdown   | `6`   | Hands evaluated; lowest hand wins |
        | End        | `7`   | Round complete; results displayed |
        | Rebuy      | `8`   | Rebuy/addon decision phase |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `fold`  | `f`     | Fold and forfeit the pot |
        | `check` | `ck`    | Pass without betting (only when no outstanding bet) |
        | `call`  | `c`     | Match the current bet |
        | `bet`   | `b`     | Place a bet (use `amount`) |
        | `raise` | `ra`    | Raise by a specified amount on top of calling (use `amount`) |
        | `allin` | `a`     | Go all-in with all remaining chips |
        | `rebuy` | `rb`    | Buy back chips during rebuy period |
        | `skiprebuy` | `sr` | Skip rebuy opportunity |
        | `addon` | `ad`    | Add chips (one-time addon purchase) |
        | `skipaddon` | `sa` | Skip addon opportunity |
        | `muck`  | `m`     | Muck (hide) hand at showdown |
        | `show`  | `sh`    | Show hand at showdown |
        | `log`   | `l`     | View action log |
        | `quit`  | `q`     | End the session |
      operationId: razzExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SevenCardStudRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              fold:
                summary: Fold
                value:
                  command: fold
                  sessionId: my-session-001
              call:
                summary: Call
                value:
                  command: call
                  sessionId: my-session-001
              bet:
                summary: Place a bet of 40
                value:
                  command: bet
                  amount: 40
                  sessionId: my-session-001
              allin:
                summary: Go all-in
                value:
                  command: allin
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SevenCardStudResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SevenCardStudResponse'
              examples:
                paramError:
                  value:
                    players: []
                    pot: 0
                    sidePots: []
                    dealerIdx: 0
                    currentTurn: 0
                    phase: 0
                    gameEndFlag: false
                    lastBet: 0
                    minRaise: 0
                    roundResults: []
                    cpuActions: []
                    message: param error.

  /badugi/exec:
    post:
      tags:
        - badugi
      summary: Execute a Badugi game command
      description: |
        Send a command to advance the Badugi game session identified by
        `sessionId`. Badugi is an Asian-origin 4-card draw lowball poker
        variant where the best hand has four cards, each with a distinct
        rank and a distinct suit. Ace is always low (1). Straights and
        flushes do not affect the hand.

        Each player receives 4 cards and may exchange up to all four over
        three draws, with a betting round before the first draw and after
        each draw (four betting rounds total). At showdown, the largest
        subset whose cards have unique ranks and suits wins, lowest card
        first.

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Init       | `0`   | Pre-reset state |
        | Deal       | `1`   | Cards dealt; first betting round |
        | Bet        | `2`   | Betting round after a draw (drawIndex identifies which) |
        | Draw       | `3`   | Draw round in progress (drawIndex 1..3) |
        | Showdown   | `4`   | Hands evaluated |
        | End        | `5`   | Round complete |

        | Command  | Aliases | Description |
        |----------|---------|-------------|
        | `reset`  | `r`     | Start / restart a game |
        | `fold`   | `f`     | Fold and forfeit the pot |
        | `check`  | `ck`    | Pass without betting |
        | `call`   | `c`     | Match the current bet |
        | `bet`    | `b`     | Place a bet (use `amount`) |
        | `raise`  | `ra`    | Raise (use `amount`) |
        | `allin`  | `a`     | Go all-in |
        | `exchange` | `e`   | Draw the cards at `indices` (0..3) |
        | `stand`  | `s`     | Stand pat (no exchange) |
        | `log`    | `l`     | View action log |
      operationId: badugiExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [command]
              properties:
                command:
                  type: string
                  enum: [reset, r, fold, f, check, ck, call, c, bet, b, raise, ra, allin, a, exchange, e, stand, s, log, l]
                sessionId:
                  type: string
                amount:
                  type: integer
                humanPlayMs:
                  type: integer
                indices:
                  type: array
                  items:
                    type: integer
                cpuCount:
                  type: integer
                bettingLimit:
                  type: integer
                cpuMetaAI:
                  type: boolean
                profile:
                  type: object
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  players:
                    type: array
                    items: { type: object }
                  pot: { type: integer }
                  sidePots:
                    type: array
                    items: { type: object }
                  dealerIdx: { type: integer }
                  currentTurn: { type: integer }
                  phase: { type: integer }
                  drawIndex: { type: integer }
                  gameEndFlag: { type: boolean }
                  lastBet: { type: integer }
                  minRaise: { type: integer }
                  ante: { type: integer }
                  bettingLimit: { type: integer }
                  raiseCount: { type: integer }
                  maxBetAmount: { type: integer }
                  roundResults:
                    type: array
                    items: { type: object }
                  cpuActions:
                    type: array
                    items: { type: object }
                  cpuExchanges:
                    type: array
                    items: { type: object }
                  message: { type: string }
                  messageCode: { type: string }
                  messageParams:
                    type: object
                    additionalProperties: { type: string }

  /deucetoseven/exec:
    post:
      tags:
        - deucetoseven
      summary: Execute a 2-7 Triple Draw game command
      description: |
        Send a command to advance the 2-7 Triple Draw (デューストゥセブン)
        game session identified by `sessionId`. "Deuce to Seven" is a lowball
        draw poker variant where the hand is read as an ordinary 5-card poker
        hand and the LOWEST hand wins. The Ace is always high (so A-2-3-4-5 is
        merely Ace-high, not a straight) and straights/flushes count against
        you — the nut low is 7-5-4-3-2.

        Each player receives 5 cards and may exchange any of them over three
        draws, with a betting round before the first draw and after each draw
        (four betting rounds total).

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Init       | `0`   | Pre-reset state |
        | Deal       | `1`   | Cards dealt; first betting round |
        | Bet        | `2`   | Betting round after a draw (drawIndex identifies which) |
        | Draw       | `3`   | Draw round in progress (drawIndex 1..3) |
        | Showdown   | `4`   | Hands evaluated |
        | End        | `5`   | Round complete |

        | Command  | Aliases | Description |
        |----------|---------|-------------|
        | `reset`  | `r`     | Start / restart a game |
        | `fold`   | `f`     | Fold and forfeit the pot |
        | `check`  | `ck`    | Pass without betting |
        | `call`   | `c`     | Match the current bet |
        | `bet`    | `b`     | Place a bet (use `amount`) |
        | `raise`  | `ra`    | Raise (use `amount`) |
        | `allin`  | `a`     | Go all-in |
        | `exchange` | `e`   | Draw the cards at `indices` (0..4) |
        | `stand`  | `s`     | Stand pat (no exchange) |
        | `log`    | `l`     | View action log |
      operationId: deuceToSevenExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [command]
              properties:
                command:
                  type: string
                  enum: [reset, r, fold, f, check, ck, call, c, bet, b, raise, ra, allin, a, exchange, e, stand, s, log, l]
                sessionId:
                  type: string
                amount:
                  type: integer
                humanPlayMs:
                  type: integer
                indices:
                  type: array
                  items:
                    type: integer
                cpuCount:
                  type: integer
                bettingLimit:
                  type: integer
                cpuMetaAI:
                  type: boolean
                profile:
                  type: object
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  players:
                    type: array
                    items: { type: object }
                  pot: { type: integer }
                  sidePots:
                    type: array
                    items: { type: object }
                  dealerIdx: { type: integer }
                  currentTurn: { type: integer }
                  phase: { type: integer }
                  drawIndex: { type: integer }
                  gameEndFlag: { type: boolean }
                  lastBet: { type: integer }
                  minRaise: { type: integer }
                  ante: { type: integer }
                  bettingLimit: { type: integer }
                  raiseCount: { type: integer }
                  maxBetAmount: { type: integer }
                  roundResults:
                    type: array
                    items: { type: object }
                  cpuActions:
                    type: array
                    items: { type: object }
                  cpuExchanges:
                    type: array
                    items: { type: object }
                  message: { type: string }
                  messageCode: { type: string }
                  messageParams:
                    type: object
                    additionalProperties: { type: string }

  /president/exec:
    post:
      tags:
        - president
      summary: Execute a President (Scum) game command
      description: |
        Send a command to advance the President / Scum (プレジデント) game
        session identified by `sessionId`. President is a 4-player going-out
        game using a standard 52-card deck (no jokers). Card strength
        `3 < 4 < ... < K < A < 2` (2 strongest). Plays are singles, pairs,
        triples, or quads of the same rank and must beat the table play.
        Quads trigger revolution (strength inverts). At the start of each
        subsequent round, card exchange swaps the best cards from Scum to
        President (2 cards) and Vice Scum to Vice President (1 card).

        | Command  | Aliases | Description |
        |----------|---------|-------------|
        | `reset`  | `r`     | Start / restart a game |
        | `play`   | `p`     | Play cards at `indices` (empty = pass) |
        | `log`    | `l`     | View action log |
      operationId: presidentExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PresidentRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play two cards (pair)
                value:
                  command: play
                  indices: [0, 1]
                  sessionId: my-session-001
              pass:
                summary: Pass
                value:
                  command: play
                  indices: []
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PresidentResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PresidentResponse'

  /cassino/exec:
    post:
      tags:
        - cassino
      summary: Execute a Cassino game command
      description: |
        Send a command to advance the Cassino (カッシーノ) game session
        identified by `sessionId`. Cassino is a 4-player fishing-style game
        using a standard 52-card deck (no jokers). On each turn the player
        plays one hand card and either takes table cards (sum or rank match),
        creates / extends a build, or trails the card. First to 21 cumulative
        points wins.

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `take`  | `t`     | Take table cards / builds with the played hand card |
        | `build` | `b`     | Create or extend a build of declared value |
        | `trail` | `tr`    | Place the played hand card on the table without capture |
        | `next`  | `n`     | Start the next round |
        | `log`   | `l`     | View action log |
      operationId: cassinoExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CassinoRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              take:
                summary: Take table cards
                value:
                  command: take
                  handIndex: 0
                  tableIndices: [0, 1]
                  sessionId: my-session-001
              build:
                summary: Build value 8
                value:
                  command: build
                  handIndex: 0
                  tableIndices: [1]
                  declaredValue: 8
                  sessionId: my-session-001
              trail:
                summary: Trail a card
                value:
                  command: trail
                  handIndex: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CassinoResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CassinoResponse'

  /scopa/exec:
    post:
      tags:
        - scopa
      summary: Execute a Scopa game command
      description: |
        Send a command to advance the Scopa (スコパ) game session identified
        by `sessionId`. Scopa is an Italian national fishing card game played
        heads-up (1 human + 1 CPU) with a 40-card deck (the 8/9/10 are removed).
        Play one hand card to capture table cards of equal value or summing to
        it (a single exact match is forced when one exists), otherwise lay the
        card. Clearing the table earns a `scopa` bonus. Scoring runs to 11 via
        cards (carte), diamonds (denari), the 7♦ (settebello), most 7s, and
        scope.

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `next`  | `n`     | Start the next round |
        | `play`  | `p`     | Play a hand card to capture table cards or lay it |
        | `log`   | `l`     | View action log |
      operationId: scopaExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ScopaRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play hand card 0 to capture table cards 1 and 3
                value:
                  command: play
                  handIndex: 0
                  tableIndices: [1, 3]
                  sessionId: my-session-001
              lay:
                summary: Lay hand card 2 on the table (no capture)
                value:
                  command: play
                  handIndex: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScopaResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScopaResponse'

  /scopone/exec:
    post:
      tags:
        - scopone
      summary: Execute a Scopone game command
      description: |
        Send a command to advance the Scopone (スコポーネ) game session
        identified by `sessionId`. Scopone is the 4-player, 2-team partnership
        form of Scopa (1 human + 3 CPU) on the same 40-card deck (the 8/9/10
        are removed). Seats 0 and 2 form one team and seats 1 and 3 the other.
        All 40 cards are dealt out (10 each) with no initial table cards and no
        stock. Play one hand card to capture table cards of equal value or
        summing to it (a single exact match is forced when one exists),
        otherwise lay the card. Clearing the table earns a `scopa` bonus.
        Scoring is per team toward the target (default 11) via cards (carte),
        diamonds (denari), the 7♦ (settebello), most 7s, and scope.

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset`         | `r`  | Start / restart a game |
        | `next`          | `n`  | Start the next round |
        | `play`          | `p`  | Play a hand card to capture table cards or lay it |
        | `setdifficulty` | `sd` | Set CPU difficulty (0-2) |
        | `settarget`     | `st` | Set the target score |
        | `log`           | `l`  | View action log |
      operationId: scoponeExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ScoponeRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play hand card 0 to capture table cards 1 and 3
                value:
                  command: play
                  handIndex: 0
                  tableIndices: [1, 3]
                  sessionId: my-session-001
              lay:
                summary: Lay hand card 2 on the table (no capture)
                value:
                  command: play
                  handIndex: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScoponeResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ScoponeResponse'

  /escoba/exec:
    post:
      tags:
        - escoba
      summary: Execute an Escoba game command
      description: |
        Send a command to advance the Escoba (エスコバ) game session
        identified by `sessionId`. Escoba is the Spanish 4-player
        free-for-all (no teams) member of the Scopa family (1 human + 3 CPU)
        on a 40-card deck (the 8/9/10 are removed; J=8, Q=9, K=10). Each
        player is dealt 3 cards and 4 cards are dealt face-up to the table;
        further packs of 3 are dealt from the stock once all hands are empty.
        Play one hand card to capture the subset of table cards whose values
        plus the played card sum to exactly 15, otherwise lay the card.
        Clearing the table earns an `escoba` bonus. Scoring is per player
        toward the target (default 10) via most cards, most espadas (♠),
        most 7s, most oros (♦), each escoba, and capturing the A♠ and 7♠.

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset`         | `r`  | Start / restart a game |
        | `next`          | `n`  | Start the next round |
        | `play`          | `p`  | Play a hand card to capture table cards or lay it |
        | `setdifficulty` | `sd` | Set CPU difficulty (0-2) |
        | `settarget`     | `st` | Set the target score |
        | `log`           | `l`  | View action log |
      operationId: escobaExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EscobaRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play hand card 0 to capture table cards 1 and 3 (sum to 15)
                value:
                  command: play
                  handIndex: 0
                  tableIndices: [1, 3]
                  sessionId: my-session-001
              lay:
                summary: Lay hand card 2 on the table (no capture)
                value:
                  command: play
                  handIndex: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EscobaResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EscobaResponse'

  /barbu/exec:
    post:
      tags:
        - barbu
      summary: Execute a Barbu game command
      description: |
        Send a command to advance the Barbu (バルブ) game session identified by
        `sessionId`. Barbu is a French compendium trick-taking game for 4 players
        (1 human + 3 CPU) on a 52-card deck. Over 28 deals each player deals 7
        times; the dealer picks one of 7 contracts (No Tricks / No Hearts / No
        Queens / Barbu(K♥) / No Last Trick / Trumps / Dominoes), each usable once.

        | Command    | Aliases | Description |
        |------------|---------|-------------|
        | `reset`    | `r`     | Start / restart a game |
        | `next`     | `n`     | Start the next deal |
        | `contract` | `c`     | Dealer picks a contract (trumpSuit only for Trumps) |
        | `play`     | `p`     | Play a hand card (handIndex -1 to pass in Dominoes) |
        | `log`      | `l`     | View action log |
      operationId: barbuExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BarbuRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              contract:
                summary: Pick the Trumps contract with spades as trump
                value:
                  command: contract
                  contract: 5
                  trumpSuit: 1
                  sessionId: my-session-001
              play:
                summary: Play hand card 0
                value:
                  command: play
                  handIndex: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BarbuResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BarbuResponse'

  /spanish21/exec:
    post:
      tags:
        - spanish21
      summary: Execute a Spanish 21 game command
      description: |
        Send a command to advance the Spanish 21 (スパニッシュ21) game session
        identified by `sessionId`. Spanish 21 is a BlackJack variant that uses
        a 48-card Spanish deck (10s removed; J/Q/K stay), where the player's 21
        always wins (including over dealer 21 and dealer natural BJ), and where
        winning hands earn bonus payouts: 5-card 21 (3:2), 6-card 21 (2:1),
        7+ card 21 (3:1), 6-7-8 (3:2 mixed / 2:1 same suit / 3:1 all spades),
        and 7-7-7 (3:2 / 2:1 / 3:1 by suit). Bonuses are forfeit on doubled
        hands and on natural BlackJack (which still pays 3:2). All other
        commands and phases match `/blackjack/exec`.

        See the BlackJack endpoint for the full command reference; the request
        and response payloads are byte-for-byte compatible.
      operationId: spanish21Exec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BlackJackRequest'
            examples:
              reset:
                summary: Start a new Spanish 21 game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Place a bet
                value:
                  command: bet
                  amount: 100
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BlackJackResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BlackJackResponse'

  /calculation/exec:
    post:
      tags:
        - calculation
      summary: Execute a Calculation game command
      description: |
        Send a command to advance the Calculation (カルキュレーション) game session
        identified by `sessionId`. Calculation is a single-player arithmetic
        solitaire using a standard 52-card deck. Four foundations start with A,
        2, 3, 4 and must be built up to K following arithmetic-progression rules
        with step sizes `+1`, `+2`, `+3`, `+4` respectively (mod 13). The
        remaining 48 cards form a single stock. On each turn the player takes
        the stock top and either plays it onto a foundation (if its value
        matches the foundation's next-expected value) or places it onto any of
        four waste piles (no ordering constraints). Waste-pile tops can later
        be played to foundations.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | All 4 foundations have reached K |
        | GameOver   | `2`   | Player gave up or reached a stalemate |

        | Command        | Aliases     | Description |
        |----------------|-------------|-------------|
        | `reset`        | `r`         | Start / restart a game |
        | `move`         | `m`         | Move a card (requires `from` and `to` zones) |
        | `giveup`       | `g`         | Give up the current game |
        | `hint`         | `h`         | Return a hint (stock/waste → foundation), if any |
        | `autocomplete` | `ac`        | Auto-play all legal waste→foundation moves |
        | `undo`         | `u`         | Undo the last action |
        | `undo_n`       | —           | Undo the last `n` actions |
        | `log`          | `l`         | View action log |

        Move zones:
        * `from`: `{zone: "stock"}` or `{zone: "waste", idx: 0..3}`
        * `to`: `{zone: "foundation", idx: 0..3}` or `{zone: "waste", idx: 0..3}`
      operationId: calculationExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  $ref: '#/components/schemas/CalculationMoveZone'
                to:
                  $ref: '#/components/schemas/CalculationMoveZone'
                n:
                  type: integer
                  description: Number of moves to undo (for `undo_n` command)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              moveStockToFoundation:
                summary: Play the stock top to foundation 0
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: stock
                  to:
                    zone: foundation
                    idx: 0
              moveStockToWaste:
                summary: Park the stock top on waste pile 2
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: stock
                  to:
                    zone: waste
                    idx: 2
              moveWasteToFoundation:
                summary: Play waste 1 top to foundation 3
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: waste
                    idx: 1
                  to:
                    zone: foundation
                    idx: 3
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CalculationResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CalculationResponse'
  /sirtommy/exec:
    post:
      tags:
        - sirtommy
      summary: Execute a Sir Tommy game command
      description: |
        Send a command to advance the Sir Tommy (サー・トミー) game session
        identified by `sessionId`. Sir Tommy is one of the oldest recorded
        single-player patiences, played with a standard 52-card deck.

        All 52 cards start face-down in the stock and **no foundation is dealt**:
        a foundation is opened by playing an Ace onto it. Foundations then build
        up one rank at a time, A to K, **ignoring suit**. A card that cannot be
        played goes on top of one of four waste piles, and only a waste pile's
        **top** card may later move to a foundation - piles are never reordered
        and cards never move between wastes, so choosing which pile to bury a
        card on is the only real decision in the game.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | All 52 cards are on the foundations |
        | GameOver   | `2`   | Player gave up, or the stock ran out with no legal move |

        | Command        | Aliases     | Description |
        |----------------|-------------|-------------|
        | `reset`        | `r`         | Start / restart a game |
        | `move`         | `m`         | Move a card (requires `from` and `to` zones) |
        | `giveup`       | `g`         | Give up the current game |
        | `hint`         | `h`         | Return a hint (stock/waste to foundation), if any |
        | `autocomplete` | `ac`        | Auto-play every legal move to the foundations |
        | `undo`         | `u`         | Undo the last action |
        | `undo_n`       | -           | Undo the last `n` actions |
        | `log`          | `l`         | View action log |

        Move zones:
        * `from`: `{zone: "stock"}` or `{zone: "waste", idx: 0..3}`
        * `to`: `{zone: "foundation", idx: 0..3}` or `{zone: "waste", idx: 0..3}`

      operationId: sirtommyExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  $ref: '#/components/schemas/SirTommyMoveZone'
                to:
                  $ref: '#/components/schemas/SirTommyMoveZone'
                n:
                  type: integer
                  description: Number of moves to undo (for `undo_n` command)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              moveStockToFoundation:
                summary: Play the stock top to foundation 0
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: stock
                  to:
                    zone: foundation
                    idx: 0
              moveStockToWaste:
                summary: Park the stock top on waste pile 2
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: stock
                  to:
                    zone: waste
                    idx: 2
              moveWasteToFoundation:
                summary: Play waste 1 top to foundation 3
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: waste
                    idx: 1
                  to:
                    zone: foundation
                    idx: 3
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SirTommyResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SirTommyResponse'

  /auldlangsyne/exec:
    post:
      tags:
        - auldlangsyne
      summary: Execute an Auld Lang Syne game command
      description: |
        Send a command to advance the Auld Lang Syne (オールド・ラング・サイン)
        game session identified by `sessionId`. Auld Lang Syne is a classic
        English single-player patience played with a standard 52-card deck.

        The four Aces are **removed from the deck and set out as the
        foundations**; the remaining 48 cards form the stock. A `deal` puts one
        card onto each of the four waste piles at once - the player does **not**
        choose where they land, which is the whole difference from Sir Tommy.
        Foundations build up one rank at a time, A to K, **ignoring suit**. Only
        a waste pile's **top** card may move to a foundation; piles are never
        reordered and cards never move between wastes. 48 is a multiple of four,
        so there are exactly 12 deals in a game.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | All 52 cards are on the foundations |
        | GameOver   | `2`   | Player gave up |

        | Command        | Aliases     | Description |
        |----------------|-------------|-------------|
        | `reset`        | `r`         | Start / restart a game |
        | `deal`         | `d`         | Deal one card onto each of the four wastes |
        | `move`         | `m`         | Move a card (requires `from` and `to` zones) |
        | `giveup`       | `g`         | Give up the current game |
        | `hint`         | `h`         | Return a hint (waste to foundation), if any |
        | `autocomplete` | `ac`        | Auto-play every legal move once the stock is empty |
        | `undo`         | `u`         | Undo the last action |
        | `undo_n`       | -           | Undo the last `n` actions |
        | `log`          | `l`         | View action log |

        Move zones:
        * `from`: `{zone: "waste", idx: 0..3}`
        * `to`: `{zone: "foundation", idx: 0..3}`

      operationId: auldlangsyneExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, deal, d, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  $ref: '#/components/schemas/AuldLangSyneMoveZone'
                to:
                  $ref: '#/components/schemas/AuldLangSyneMoveZone'
                n:
                  type: integer
                  description: Number of moves to undo (for `undo_n` command)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              deal:
                summary: Deal one card onto each waste
                value:
                  command: deal
                  sessionId: my-session-001
              moveWasteToFoundation:
                summary: Play waste 1 top to foundation 3
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: waste
                    idx: 1
                  to:
                    zone: foundation
                    idx: 3
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuldLangSyneResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuldLangSyneResponse'


  /bisley/exec:
    post:
      tags:
        - bisley
      summary: Execute a Bisley game command
      description: |
        Send a command to advance the Bisley (ビズリー) game session identified
        by `sessionId`. Bisley is a fully open single-player solitaire played
        with a standard 52-card deck: every card is face-up from the deal, so
        the only randomness is the layout.

        The four Aces are lifted onto the **ascending** foundations, which build
        A→2→…→K **by suit**. The remaining 48 cards are dealt face-up into **13
        tableau columns** (four of three cards, nine of four). A second set of
        four **descending** foundations starts empty and opens when the player
        plays a King, building K→Q→…→A by suit.

        Only the **top (last) card** of a column may move. It can be stacked on
        another column's top card only when it is the **same suit and exactly
        one rank away**, up or down. An emptied column is **not** a free cell:
        allowing it would make every deal trivially solvable.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | All 52 cards are on the two foundation sets |
        | GameOver   | `2`   | Player gave up |

        | Command        | Aliases     | Description |
        |----------------|-------------|-------------|
        | `reset`        | `r`         | Start / restart a game |
        | `move`         | `m`         | Move a card (requires `from` and `to` zones) |
        | `giveup`       | `g`         | Give up the current game |
        | `hint`         | `h`         | Return a foundation move, if any |
        | `autocomplete` | `ac`        | Send every legal card to the foundations |
        | `undo`         | `u`         | Undo the last action |
        | `undo_n`       | -           | Undo the last `n` actions |
        | `log`          | `l`         | View action log |

        Move zones:
        * `from`: `{zone: "tableau", col: 0..12}`
        * `to`: `{zone: "tableau", col: 0..12}`, `{zone: "ace"}`, or `{zone: "king"}`

      operationId: bisleyExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  $ref: '#/components/schemas/BisleyMoveZone'
                to:
                  $ref: '#/components/schemas/BisleyMoveZone'
                n:
                  type: integer
                  description: Number of moves to undo (for `undo_n` command)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              moveTableauToAceFoundation:
                summary: Send column 3's top card to its ascending foundation
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: tableau
                    col: 3
                  to:
                    zone: ace
              moveTableauToKingFoundation:
                summary: Open or extend a descending foundation from column 7
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: tableau
                    col: 7
                  to:
                    zone: king
              moveTableauToTableau:
                summary: Stack column 0's top card onto column 11 (same suit, one rank away)
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: tableau
                    col: 0
                  to:
                    zone: tableau
                    col: 11
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BisleyResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BisleyResponse'

  /napoleonssquare/exec:
    post:
      tags:
        - napoleonssquare
      summary: Execute a Napoleon's Square game command
      description: |
        Send a command to advance the Napoleon's Square (ナポレオンズ・スクエア)
        game session identified by `sessionId`. It is a two-deck (104-card)
        solitaire laid out as a square.

        The eight Aces are lifted onto **eight foundations** (two per suit, each
        pinned to a suit so the layout is stable across deals). 48 cards are
        dealt into **twelve columns of four**, and the remaining 48 form the
        stock, turned one card at a time onto a waste with **no redeal**.

        Foundations build up A->K by suit; the tableau builds down by suit. The
        rule that distinguishes this game is the **group move**: a same-suit
        descending run is carried as one unit by naming the index of its head in
        `from.cardIndex`. An empty column accepts any card or run.

        Note that the issue this game was implemented from says the Aces are
        seeded *and* that "the remaining 96 cards" are dealt to a 12x4 tableau;
        12x4 is 48, so 48 go to the tableau and the other 48 to the stock.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | All eight foundations reached the King |
        | GameOver   | `2`   | Player gave up |

        | Command        | Aliases     | Description |
        |----------------|-------------|-------------|
        | `reset`        | `r`         | Start / restart a game |
        | `draw`         | `d`         | Turn one card from the stock onto the waste |
        | `move`         | `m`         | Move a card or run (requires `from` and `to`) |
        | `giveup`       | `g`         | Give up the current game |
        | `hint`         | `h`         | Foundation move, else tableau move, else "turn the stock" |
        | `autocomplete` | `ac`        | Send every legal card to the foundations |
        | `undo`         | `u`         | Undo the last action |
        | `undo_n`       | -           | Undo the last `n` actions |
        | `log`          | `l`         | View action log |

        Move zones:
        * `from`: `{zone: "waste"}` or `{zone: "tableau", col: 0..11, cardIndex?: n}`
        * `to`: `{zone: "foundation"}` or `{zone: "tableau", col: 0..11}`

      operationId: napoleonssquareExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, draw, d, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  $ref: '#/components/schemas/NapoleonsSquareMoveZone'
                to:
                  $ref: '#/components/schemas/NapoleonsSquareMoveZone'
                n:
                  type: integer
                  description: Number of moves to undo (for `undo_n` command)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              draw:
                summary: Turn one card from the stock
                value:
                  command: draw
                  sessionId: my-session-001
              moveWasteToFoundation:
                summary: Send the waste top to its foundation
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: waste
                  to:
                    zone: foundation
              moveTopCardBetweenColumns:
                summary: Move only the top card of column 0 onto column 5
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: tableau
                    col: 0
                  to:
                    zone: tableau
                    col: 5
              moveRunBetweenColumns:
                summary: Carry the run starting at index 2 of column 0 onto column 5
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: tableau
                    col: 0
                    cardIndex: 2
                  to:
                    zone: tableau
                    col: 5
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NapoleonsSquareResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NapoleonsSquareResponse'

  /grandfathersclock/exec:
    post:
      tags:
        - grandfathersclock
      summary: Execute a Grandfather's Clock game command
      description: |
        Send a command to advance the Grandfather's Clock (グランドファーザーズ・
        クロック) game session identified by `sessionId`.

        Twelve **fixed** cards are placed as a clock face and the remaining 40 are
        dealt face-up into **eight columns of five**. There is **no stock**:
        12 + 8x5 = 52 accounts for every card, so the whole game is ordering the
        moves already on the table. (#4399's rule 5 mentions a stock running out;
        that cannot happen because none exists.)

        Each face builds **up by suit, wrapping King round to Ace**, and must
        finish on the rank of the hour it occupies — one o'clock on an Ace through
        twelve o'clock on a Queen. A face that reaches its target is **complete and
        accepts nothing more**; without that rule the wraparound would let it lap
        the whole suit. The starters are fixed rather than dealt because each then
        needs exactly 3 or 4 further cards, totalling the 40 in the tableau.

        The tableau builds **down by one ignoring suit**, one card at a time, and
        an empty column accepts any card.

        **The destination face index is required** on a move to `foundation`.
        Twelve faces can hold the same suit, so unlike the one-per-suit solitaires
        it cannot be derived from the card.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | All twelve faces reached their hour |
        | GameOver   | `2`   | Player gave up |

        | Command        | Aliases     | Description |
        |----------------|-------------|-------------|
        | `reset`        | `r`         | Start / restart a game |
        | `move`         | `m`         | Move a card (requires `from` and `to`) |
        | `giveup`       | `g`         | Give up the current game |
        | `hint`         | `h`         | A clock-face move if there is one, else a tableau move |
        | `autocomplete` | `ac`        | Send every legal card to the clock faces |
        | `undo`         | `u`         | Undo the last action |
        | `undo_n`       | -           | Undo the last `n` actions |
        | `log`          | `l`         | View action log |

        Move zones:
        * `from`: `{zone: "tableau", col: 0..7}`
        * `to`: `{zone: "tableau", col: 0..7}` or `{zone: "foundation", col: 0..11}`

      operationId: grandfathersclockExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  $ref: '#/components/schemas/GrandfathersClockMoveZone'
                to:
                  $ref: '#/components/schemas/GrandfathersClockMoveZone'
                n:
                  type: integer
                  description: Number of moves to undo (for `undo_n` command)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              moveTableauToClockFace:
                summary: Send column 2's top card to the five o'clock face
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: tableau
                    col: 2
                  to:
                    zone: foundation
                    col: 4
              moveTableauToTableau:
                summary: Stack column 0's top card onto column 5
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: tableau
                    col: 0
                  to:
                    zone: tableau
                    col: 5
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GrandfathersClockResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GrandfathersClockResponse'

  /missmilligan/exec:
    post:
      tags:
        - missmilligan
      summary: Execute a Miss Milligan game command
      description: |
        Send a command to advance the Miss Milligan (ミス・ミリガン) game session
        identified by `sessionId`. It is a two-deck (104-card) solitaire, widely
        considered one of the hardest.

        One card is dealt to each of **eight columns** to start; the other 96 form
        the stock, which is dealt **eight at a time, one onto every column**.
        There is **no waste pile** -- #4407's rule 5 describes turning the stock a
        card at a time into one, which is not how this game works.

        Eight foundations (two per suit) build up A->K by suit. The tableau builds
        **down in alternating colour** and a run moves as a unit. **An empty column
        takes only a King** or a King-headed run; #4407 omits that rule, but
        without it an empty column becomes a universal parking space.

        **Waiving** is the rescue rule. Once the stock is exhausted the player may
        lift one run aside (`waive`), free the cards underneath, then put it back
        through the ordinary move API with `from: {zone: "waived"}`. Dealing and a
        second waive are both refused while cards are held, and only a lone held
        card may go to a foundation. Because waiving is a rescue, `isStalemate`
        stays false while one remains available.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | All eight foundations reached the King |
        | GameOver   | `2`   | Player gave up |

        | Command        | Aliases     | Description |
        |----------------|-------------|-------------|
        | `reset`        | `r`         | Start / restart a game |
        | `deal`         | `d`         | Deal one card onto every column |
        | `move`         | `m`         | Move a card or run (requires `from` and `to`) |
        | `waive`        | `wv`        | Lift a run aside (requires `from.col`) |
        | `giveup`       | `g`         | Give up the current game |
        | `hint`         | `h`         | Foundation, else tableau, else "deal a row" |
        | `autocomplete` | `ac`        | Send every legal card to the foundations |
        | `undo`         | `u`         | Undo the last action |
        | `undo_n`       | -           | Undo the last `n` actions |
        | `log`          | `l`         | View action log |

        Move zones:
        * `from`: `{zone: "tableau", col: 0..7, cardIndex?: n}` or `{zone: "waived"}`
        * `to`: `{zone: "tableau", col: 0..7}` or `{zone: "foundation"}`

      operationId: missmilliganExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, deal, d, move, m, waive, wv, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  $ref: '#/components/schemas/MissMilliganMoveZone'
                to:
                  $ref: '#/components/schemas/MissMilliganMoveZone'
                n:
                  type: integer
                  description: Number of moves to undo (for `undo_n` command)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              deal:
                summary: Deal one card onto every column
                value:
                  command: deal
                  sessionId: my-session-001
              moveRunBetweenColumns:
                summary: Carry the run starting at index 2 of column 0 onto column 5
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: tableau
                    col: 0
                    cardIndex: 2
                  to:
                    zone: tableau
                    col: 5
              waive:
                summary: Lift the run starting at index 1 of column 3 aside
                value:
                  command: waive
                  sessionId: my-session-001
                  from:
                    zone: tableau
                    col: 3
                    cardIndex: 1
              returnWaived:
                summary: Put the held cards back on column 6
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: waived
                  to:
                    zone: tableau
                    col: 6
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MissMilliganResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MissMilliganResponse'

  /duchess/exec:
    post:
      tags:
        - duchess
      summary: Execute a Duchess game command
      description: |
        Send a command to advance the Duchess (ダッチェス) game session
        identified by `sessionId`. It is a Canfield-family solitaire on a single
        52-card deck, also known as Glenwood.

        **Four reserve fans of three cards (twelve in total)** are dealt, one card
        onto each of **four tableau columns**, and the remaining 36 form the stock,
        turned one at a time to a waste with **no redeal**. Only the top card of
        each fan is available. #4408 deals "four cards per fan, sixteen in total";
        Duchess deals three per fan, so the real game was followed.

        The distinguishing rule is that **the player picks the starting rank**. The
        first action must be `base`, choosing the top card of one reserve fan; that
        rank opens all four suit-pinned foundations. Until then `awaitingBaseRank`
        is true and every other command is refused. Foundations then build up in
        suit, wrapping King round to Ace, until each has come full circle.

        The tableau builds **down in alternating colour**, also wrapping Ace round
        to King, and a run moves as a unit. **While any reserve card remains, an
        empty column may only be filled from the reserve** -- #4408 omits this, but
        it is the family's core rule and without it there is no reason to spend the
        reserve.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | All four foundations came full circle |
        | GameOver   | `2`   | Player gave up |

        | Command        | Aliases       | Description |
        |----------------|---------------|-------------|
        | `reset`        | `r`           | Start / restart a game |
        | `base`         | `choosebase`  | Fix the starting rank (requires `from.col`) |
        | `draw`         | `d`           | Turn one card from the stock |
        | `move`         | `m`           | Move a card or run (requires `from` and `to`) |
        | `giveup`       | `g`           | Give up the current game |
        | `hint`         | `h`           | Foundation, else tableau, else "turn a card" |
        | `autocomplete` | `ac`          | Send every legal card to the foundations |
        | `undo`         | `u`           | Undo the last action |
        | `undo_n`       | -             | Undo the last `n` actions |
        | `log`          | `l`           | View action log |

        Move zones:
        * `from`: `{zone: "reserve", col: 0..3}`, `{zone: "waste"}`, or `{zone: "tableau", col: 0..3, cardIndex?: n}`
        * `to`: `{zone: "tableau", col: 0..3}` or `{zone: "foundation"}`

      operationId: duchessExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, base, choosebase, draw, d, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  $ref: '#/components/schemas/DuchessMoveZone'
                to:
                  $ref: '#/components/schemas/DuchessMoveZone'
                n:
                  type: integer
                  description: Number of moves to undo (for `undo_n` command)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              chooseBaseRank:
                summary: Take the starting rank off reserve fan 2
                value:
                  command: base
                  sessionId: my-session-001
                  from:
                    zone: reserve
                    col: 2
              draw:
                summary: Turn one card from the stock
                value:
                  command: draw
                  sessionId: my-session-001
              reserveToTableau:
                summary: Move the top of reserve fan 1 onto column 3
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: reserve
                    col: 1
                  to:
                    zone: tableau
                    col: 3
              moveRunBetweenColumns:
                summary: Carry the run starting at index 2 of column 0 onto column 3
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: tableau
                    col: 0
                    cardIndex: 2
                  to:
                    zone: tableau
                    col: 3
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DuchessResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DuchessResponse'
  /windmill/exec:
    post:
      tags:
        - windmill
      summary: Execute a Windmill game command
      description: |
        Send a command to advance the Windmill (ウィンドミル) game session
        identified by `sessionId`. It is a two-deck (104-card) solitaire, also
        known as Propeller.

        An Ace opens the centre and **eight cards** are laid around it as a cross
        -- the "sails", two per arm. The remaining 95 form the stock, turned one
        at a time to a waste with **no redeal**. #4416 describes a four-card
        cross; the real layout is eight.

        The four corner slots **start empty** and take a King as one turns up.
        #4416 deals them at the start, which would open four foundations for
        free.

        The **centre foundation builds up ignoring suit, wrapping King round to
        Ace, for 52 cards** -- A-K four times through. #4416 describes a single
        A-K run, and its "up to 8 per rank" is also wrong: two decks give 8 of
        each rank, but 4 go to the centre and 4 across the corners. Each corner
        builds **down ignoring suit, King to Ace, 13 cards**, so 52 + 13*4 = 104
        exactly.

        A vacated sail is refilled **immediately** from the waste, or from the
        stock if the waste is empty, so a hole only persists once both are gone.

        The one rescue is **pulling a corner top back onto the centre**, and it
        runs one way only (#4416 calls it bidirectional). After using it, the
        next card played to the centre must come from a sail or the waste;
        `transferBlocked` reports that state so the client does not have to
        remember the previous move. Auto-complete deliberately never performs the
        transfer -- dismantling a corner is a strategic judgement.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | Centre reached 52 and every corner reached 13 |
        | GameOver   | `2`   | Player gave up |

        | Command        | Aliases     | Description |
        |----------------|-------------|-------------|
        | `reset`        | `r`         | Start / restart a game |
        | `draw`         | `d`         | Turn one card from the stock |
        | `move`         | `m`         | Move a card (requires `from` and `to`) |
        | `giveup`       | `g`         | Give up the current game |
        | `hint`         | `h`         | Foundation, else the pull-back, else "draw" |
        | `autocomplete` | `ac`        | Send every legal sail/waste card to the foundations |
        | `undo`         | `u`         | Undo the last action |
        | `undo_n`       | -           | Undo the last `n` actions |
        | `log`          | `l`         | View action log |

        Move zones:
        * `from`: `{zone: "sail", col: 0..7}`, `{zone: "waste"}`, or `{zone: "corner", col: 0..3}`
        * `to`: `{zone: "center"}` or `{zone: "corner", col: 0..3}`
        * A `corner` source is only valid with a `center` destination.

      operationId: windmillExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, draw, d, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  $ref: '#/components/schemas/WindmillMoveZone'
                to:
                  $ref: '#/components/schemas/WindmillMoveZone'
                n:
                  type: integer
                  description: Number of moves to undo (for `undo_n` command)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              draw:
                summary: Turn one card from the stock
                value:
                  command: draw
                  sessionId: my-session-001
              sailToCenter:
                summary: Send the card in sail 3 to the centre foundation
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: sail
                    col: 3
                  to:
                    zone: center
              sailToCorner:
                summary: Open corner 1 with a King from sail 0
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: sail
                    col: 0
                  to:
                    zone: corner
                    col: 1
              pullBack:
                summary: Pull the top of corner 2 back onto the centre
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: corner
                    col: 2
                  to:
                    zone: center
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WindmillResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/WindmillResponse'
  /americantoad/exec:
    post:
      tags:
        - americantoad
      summary: Execute an American Toad game command
      description: |
        Send a command to advance the American Toad (アメリカン・トード) game
        session identified by `sessionId`. It is the two-deck (104-card) Canfield.

        A **20-card reserve**, one card onto each of **eight tableau columns**,
        one starter card that fixes the base rank, and the remaining 75 form the
        stock, turned one at a time to a waste with **one redeal** -- two passes
        in all, which #4417 does not mention. Sending `draw` with an empty stock
        performs that redeal.

        **Eight foundations, two per suit, build up in suit from the base rank,
        wrapping King round to Ace, 13 cards each** -- 8 x 13 = 104. #4417 says
        "up to 8 per suit", which comes to 32 and cannot absorb the two decks.

        The tableau builds **down in suit, wrapping Ace round to King**, and runs
        move as a unit (both omitted by #4417).

        The empty column is the heart of the game and has two regimes:
        while the reserve holds cards a gap is **refilled from it automatically**,
        so a manual placement into an empty column is refused; once the reserve is
        exhausted, **only the waste** may fill a gap. A tableau card can never
        move into an empty column, which would let the player rearrange columns
        freely. #4417 calls empty columns reserve-only, which would strand them.

        Auto-complete only sends cards to the foundations and never rearranges the
        tableau. An available redeal counts as a move, so a board with a
        recyclable waste is not reported as a dead end.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | All eight foundations reached 13 cards |
        | GameOver   | `2`   | Player gave up |

        | Command        | Aliases     | Description |
        |----------------|-------------|-------------|
        | `reset`        | `r`         | Start / restart a game |
        | `draw`         | `d`         | Turn one card, or redeal the waste once |
        | `move`         | `m`         | Move a card or run (requires `from` and `to`) |
        | `giveup`       | `g`         | Give up the current game |
        | `hint`         | `h`         | Foundation, else tableau, else "draw" |
        | `autocomplete` | `ac`        | Send every legal card to the foundations |
        | `undo`         | `u`         | Undo the last action |
        | `undo_n`       | -           | Undo the last `n` actions |
        | `log`          | `l`         | View action log |

        Move zones:
        * `from`: `{zone: "reserve"}`, `{zone: "waste"}`, or `{zone: "tableau", col: 0..7, cardIndex?: n}`
        * `to`: `{zone: "tableau", col: 0..7}` or `{zone: "foundation"}`

      operationId: americantoadExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, draw, d, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  $ref: '#/components/schemas/AmericanToadMoveZone'
                to:
                  $ref: '#/components/schemas/AmericanToadMoveZone'
                n:
                  type: integer
                  description: Number of moves to undo (for `undo_n` command)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              draw:
                summary: Turn one card (or redeal when the stock is out)
                value:
                  command: draw
                  sessionId: my-session-001
              reserveToFoundation:
                summary: Send the reserve top to its foundation
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: reserve
                  to:
                    zone: foundation
              moveRunBetweenColumns:
                summary: Carry the run starting at index 2 of column 0 onto column 5
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: tableau
                    col: 0
                    cardIndex: 2
                  to:
                    zone: tableau
                    col: 5
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AmericanToadResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AmericanToadResponse'
  /congress/exec:
    post:
      tags:
        - congress
      summary: Execute a Congress game command
      description: |
        Send a command to advance the Congress (コングレス) game session
        identified by `sessionId`. It is a two-deck (104-card) solitaire from
        England.

        **Eight tableau piles are dealt ONE card each** -- four either side of
        the foundations -- and the remaining 96 form the stock, turned one at a
        time to a waste with **no redeal**. #4419 deals all 96 into the tableau,
        which leaves the stock empty and contradicts its own rule 5.

        The **eight foundations start empty** and take Aces as they turn up,
        building up in suit to the King, 13 cards each -- 8 x 13 = 104. #4419
        pre-loads all eight Aces, which would mean searching the deck for them,
        and says "up to 8 per suit", which comes to 32.

        The tableau builds **down ignoring suit and colour** and does not wrap,
        so an Ace ends its pile. **Cards move one at a time**; there is no
        run-carrying.

        An **empty pile may only be filled from the stock or the waste**, never
        from another pile, which would let the eight piles be reordered freely.
        #4419 omits the rule. A `stock` source with a `tableau` destination is a
        move in its own right because routing a gap-fill through the waste would
        spend a stock card early, and the single pass makes that expensive.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | All eight foundations reached the King |
        | GameOver   | `2`   | Player gave up |

        | Command        | Aliases     | Description |
        |----------------|-------------|-------------|
        | `reset`        | `r`         | Start / restart a game |
        | `draw`         | `d`         | Turn one card from the stock |
        | `move`         | `m`         | Move a card (requires `from` and `to`) |
        | `giveup`       | `g`         | Give up the current game |
        | `hint`         | `h`         | Foundation, else tableau, else "draw" |
        | `autocomplete` | `ac`        | Send every legal card to the foundations |
        | `undo`         | `u`         | Undo the last action |
        | `undo_n`       | -           | Undo the last `n` actions |
        | `log`          | `l`         | View action log |

        Move zones:
        * `from`: `{zone: "tableau", col: 0..7}`, `{zone: "waste"}`, or `{zone: "stock"}`
        * `to`: `{zone: "tableau", col: 0..7}` or `{zone: "foundation"}`
        * A `stock` source is only valid with a `tableau` destination.

      operationId: congressExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, draw, d, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  $ref: '#/components/schemas/CongressMoveZone'
                to:
                  $ref: '#/components/schemas/CongressMoveZone'
                n:
                  type: integer
                  description: Number of moves to undo (for `undo_n` command)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              draw:
                summary: Turn one card from the stock
                value:
                  command: draw
                  sessionId: my-session-001
              tableauToFoundation:
                summary: Send the top of pile 1 to its foundation
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: tableau
                    col: 1
                  to:
                    zone: foundation
              stockFillsAGap:
                summary: Fill empty pile 3 straight from the stock
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: stock
                  to:
                    zone: tableau
                    col: 3
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CongressResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CongressResponse'
  /braid/exec:
    post:
      tags:
        - braid
      summary: Execute a Braid game command
      description: |
        Send a command to advance the Braid (ブレイド) game session identified by
        `sessionId`. It is a two-deck (104-card) solitaire.

        A 20-card **braid** is laid out in a plait, eight **fields** and eight
        **helpers** surround it, and the rest forms the stock.

        The signature rule: **all eight foundations build in ONE direction**,
        chosen once by the player. `awaitingDirection` is true until that choice
        is made and no foundation can be touched before it; `direction` then
        reports 1 for ascending or 2 for descending. The braid itself only ever
        feeds a foundation.

        Two redeals are allowed; `redealsLeft` and `canRedeal` report them.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | `0`   | Game in progress |
        | GameClear | `1`   | All eight foundations complete |
        | GameOver  | `2`   | Player gave up |

        | Command        | Aliases | Description |
        |----------------|---------|-------------|
        | `reset`        | `r`     | Start / restart a game |
        | `draw`         | `d`     | Turn a card from the stock |
        | `direction`    | `dir`   | Fix the foundation direction (`ascending`) |
        | `move`         | `m`     | Move a card (requires `from` and `to`) |
        | `autocomplete` | `ac`    | Send every card that can go up |
        | `undo`         | `u`     | Undo one move |
        | `undo_n`       |         | Undo `n` moves |
        | `giveup`       | `g`     | Concede |
        | `hint`         | `h`     | Suggested move |
        | `log`          | `l`     | Action-log transcript |
      operationId: braidExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, draw, d, direction, dir, move, m, autocomplete, ac, undo, u, undo_n, giveup, g, hint, h, log, l]
                sessionId:
                  type: string
                from:
                  $ref: '#/components/schemas/BraidZone'
                to:
                  $ref: '#/components/schemas/BraidZone'
                ascending:
                  type: boolean
                  description: Direction for the `direction` command. True builds up.
                n:
                  type: integer
                  description: Number of moves to undo (for `undo_n`)
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BraidResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BraidResponse'
  /niuniu/exec:
    post:
      tags:
        - niuniu
      summary: Execute a Niu Niu game command
      description: |
        Send a command to advance the Niu Niu (闘牛) game session identified by
        `sessionId`. It is a Chinese banking game on a 52-card deck.

        Each seat gets five cards. **Three of them must sum to a multiple of
        ten**; the last digit of the remaining two is the rank, 0 through 9,
        with a last digit of 0 being the top hand (niu niu). `comboIdx` reports
        which three made the bull so the client need not redo the search.

        **There is no turn.** `bet` deals and settles in one call -- the game
        offers no decision after the stake.

        `maxMultiplier` matters for the client: a banker's niu niu takes THREE
        times the stake, so what may legally be bet is `chips / maxMultiplier`,
        not `chips`. Betting the whole stack would leave the player unable to
        pay.

        | Phase | Value | Description |
        |-------|-------|-------------|
        | Bet   | `1`   | Waiting for a stake |
        | End   | `2`   | Round settled |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Next round |
        | `bet`   | `b`     | Bet, deal and settle (`amount`) |
        | `log`   | `l`     | Action-log transcript |
      operationId: niuNiuExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bet, b, log, l]
                sessionId:
                  type: string
                amount:
                  type: integer
                  description: |
                    Stake. Must satisfy `amount * maxMultiplier <= chips`,
                    because the worst case loses that much.
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NiuNiuResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NiuNiuResponse'
  /pontoon/exec:
    post:
      tags:
        - pontoon
      summary: Execute a Pontoon game command
      description: |
        Send a command to advance the Pontoon (ポンツーン) game session identified
        by `sessionId`. It is the British ancestor of blackjack.

        What separates it from blackjack:

        * **The banker's hand stays face down** until the round settles. While
          `bankerHand.hidden` is true the server sends NO cards and a zero total
          -- only the count survives.
        * A **five-card trick** (five cards without busting) beats any 21, and a
          two-card 21 is a **pontoon**, which beats that.
        * **`buy`** doubles the stake for one extra card, and is only legal
          before any `twist`.
        * The banker **keeps the bank while winning**; `nextBanker` reports who
          holds it next, or -1 while that is undecided.

        `canStick` / `canTwist` / `canBuy` / `canSplit` are computed by the
        server rather than re-derived by the client, because the rules that gate
        them (no sticking under 15, no buying after a twist) are easy to get
        subtly wrong twice.

        | Phase       | Value | Description |
        |-------------|-------|-------------|
        | Bet         | `1`   | Waiting for a stake |
        | PlayerTurn  | `2`   | Player acting |
        | BankerTurn  | `3`   | Banker acting |
        | End         | `4`   | Round settled |

        | Command       | Aliases | Description |
        |---------------|---------|-------------|
        | `reset`       | `r`     | Next round |
        | `bet`         | `b`     | Place a stake (`amount`) |
        | `deal`        |         | Deal the round |
        | `stick`       | `s`     | Stand |
        | `twist`       | `t`     | Take a card at no extra stake |
        | `buy`         |         | Double the stake for one card (`amount`) |
        | `split`       | `sp`    | Split a pair |
        | `bankertwist` | `bt`    | Banker takes a card (human banker) |
        | `bankerstay`  | `bs`    | Banker stands (human banker) |
        | `log`         | `l`     | Action-log transcript |
      operationId: pontoonExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bet, b, deal, stick, s, twist, t, buy, split, sp, bankertwist, bt, bankerstay, bs, log, l]
                sessionId:
                  type: string
                amount:
                  type: integer
                  description: Stake for `bet`, extra stake for `buy`.
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PontoonResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PontoonResponse'
  /settemezzo/exec:
    post:
      tags:
        - settemezzo
      summary: Execute a Sette e Mezzo game command
      description: |
        Send a command to advance the Sette e Mezzo (セッテ・エ・メッツォ) game
        session identified by `sessionId`. It is the Italian 7.5 game on a
        40-card deck (8, 9 and 10 removed).

        **Totals travel in HALVES, never as decimals.** Court cards are worth
        half a point, so a total of 7.5 is sent as `totalHalves: 15` against
        `targetHalves: 15`. Exact equality decides the round, and a float would
        make that a rounding question.

        The **matta** (the king of diamonds) is a wild card whose value the
        player fixes, in halves, with the `matta` command.

        The banker's hand is face down until the round settles; while hidden the
        server sends no cards and a zero total.

        | Phase       | Value | Description |
        |-------------|-------|-------------|
        | Bet         | `1`   | Waiting for a stake |
        | PlayerTurn  | `2`   | Player acting |
        | BankerTurn  | `3`   | Banker acting |
        | End         | `4`   | Round settled |

        | Command       | Aliases | Description |
        |---------------|---------|-------------|
        | `reset`       | `r`     | Next round |
        | `bet`         | `b`     | Place a stake (`amount`) |
        | `deal`        |         | Deal the round |
        | `hit`         | `h`     | Take a card |
        | `stand`       | `s`     | Stand |
        | `matta`       |         | Fix the matta's value in HALVES (`amount`) |
        | `bankerhit`   | `bh`    | Banker takes a card (human banker) |
        | `bankerstand` | `bs`    | Banker stands (human banker) |
        | `log`         | `l`     | Action-log transcript |
      operationId: setteEMezzoExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bet, b, deal, hit, h, stand, s, matta, bankerhit, bh, bankerstand, bs, log, l]
                sessionId:
                  type: string
                amount:
                  type: integer
                  description: |
                    Stake for `bet`. For `matta` it is the chosen value in
                    HALVES, so 1 means half a point.
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SetteEMezzoResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SetteEMezzoResponse'
  /chineseten/exec:
    post:
      tags:
        - chineseten
      summary: Execute a Chinese Ten game command
      description: |
        Send a command to advance the Chinese Ten (撿紅點) game session
        identified by `sessionId`. It is a Chinese fishing game on 52 cards in
        which **only the red cards score**.

        Capture works two different ways, and they do not overlap:

        * **A through 9** capture a layout card whose value brings the pair to
          exactly **ten** (A+9, 3+7, 5+5 …). A nine cannot take another nine.
        * **10, J, Q and K** capture only a card of the **same rank**. A ten
          cannot take a numeral.

        One played card takes at most **one** layout card.

        **A turn has two steps**, and #4378 documents only the first:

        1. play a card from hand — it captures, or joins the layout;
        2. **then turn the top card of the stock**, which captures on the same
           terms or stays in the layout.

        That second step is why there is no redeal: one hand card spends one
        stock card, so hands and stock empty together. The issue's "deal again
        until the stock runs out" follows from omitting it.

        Scoring, red cards only: 2-8 face value, 9 through K ten each, ace
        twenty. That comes to **210** between them, and `tieScore` is 105 —
        exactly half, which is what verifies the table.

        Captured cards are sent for BOTH seats. Reading what has already gone
        is how a fishing game is played; only the CPU's HAND is withheld, and
        its `cardCount` still ships.

        This implementation is **two-player**. At three players the ace of
        spades also scores and at four both black aces, which changes the 210
        and the 105 with it.

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Play    | `0`   | Waiting for a card |
        | Select  | `1`   | The played card can take more than one; choose |
        | GameEnd | `2`   | All cards played |

        | Command  | Aliases | Description |
        |----------|---------|-------------|
        | `reset`  | `r`     | Start / restart a game |
        | `play`   | `p`     | Play the hand card at `cardIndex` |
        | `select` | `s`     | Take the layout card at `layoutIndex` |
        | `hint`   | `h`     | Suggested move for the human seat |
        | `log`    | `l`     | Action-log transcript |
      operationId: chineseTenExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, select, s, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Hand index. Required for `play`.
                layoutIndex:
                  type: integer
                  description: Layout index. Required for `select`.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChineseTenResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChineseTenResponse'
  /toepen/exec:
    post:
      tags:
        - toepen
      summary: Execute a Toepen game command
      description: |
        Send a command to advance the Toepen (トゥーペン) game session identified
        by `sessionId`. It is a Dutch pub trick game: 32 cards, four to a hand,
        no trumps.

        **The ranking is the game, and it is the standard order inverted:**

            10 > 9 > 8 > 7 > A > K > Q > J

        The pip cards beat the ace and the court cards are weakest. #4411 calls
        the jack "the second highest"; it is the LOWEST, and the ten is the
        highest. Getting this backwards makes the game unplayable rather than
        merely wrong.

        Two more points differ from #4411: the game takes **3-8 players** (not
        3-5), and a hand of nothing but A/K/Q/J -- the four weakest ranks -- may
        be thrown in for a redeal with the `redeal` command, before any card is
        played. `canRedeal` reports whether that is available right now.

        At the table a redeal may be challenged, and the loser of the challenge
        pays a life. **There is no challenge here.** It exists because players
        cannot check each other's claims; the server holds the hands and simply
        verifies, so a false claim is rejected rather than contested. Keeping
        the challenge would leave a button that can only ever be wrong.

        Following suit is compulsory when possible. `validPlayIndices` reports
        what the human may legally play, so the client does not re-implement a
        rule the server already enforces.

        **Only the winner of the final trick escapes the penalty**; everyone
        else still in the hand loses `stake` lives. `stake` starts at 1 and
        rises by one per toep. **Folding costs the stake BEFORE the raise being
        declined** -- one life to the first toep, two to the second -- so
        declining is cheaper than losing to it.

        Ten lives lost eliminates a player; the last one standing wins.

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Play     | `0`   | Waiting for a card |
        | Respond  | `1`   | A toep is outstanding; stay or fold |
        | HandEnd  | `2`   | Hand settled |
        | GameEnd  | `3`   | One player left |

        | Command  | Aliases | Description |
        |----------|---------|-------------|
        | `reset`  | `r`     | Start / restart a game |
        | `play`   | `p`     | Play the hand card at `cardIndex` |
        | `toep`   | `t`     | Raise the stake by one |
        | `answer` | `a`     | Answer a toep: `stay` true to stay in, false to fold |
        | `redeal` | `d`     | Throw in a poverty hand (A/K/Q/J only) before any card is played |
        | `next`   | `n`     | Start the next hand |
        | `hint`   | `h`     | Suggested move for the human seat |
        | `log`    | `l`     | Action-log transcript |
      operationId: toepenExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, toep, t, answer, a, redeal, d, next, n, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Hand index. Required for `play`.
                stay:
                  type: boolean
                  description: Required for `answer`. True stays in, false folds.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
                    playerCnt:
                      type: integer
                      description: Seats, 3-8.
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToepenResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ToepenResponse'
  /mushi/exec:
    post:
      tags:
        - mushi
      summary: Execute a Mushi game command
      description: |
        Send a command to advance the Mushi (虫) game session identified by
        `sessionId`. It is an Osaka hanafuda fishing game for two players.

        Four points differ from the description in #4418, which does not match
        the game as documented (Hanafuda Hub / Fuda Wiki). The implementation
        follows the rules:

        * The pack drops **June (peony) and July (bush clover)**, not August and
          September. The arithmetic settles it: the round score subtracts 115,
          which is half the pack only if the 40 cards are worth 230 — 5 brights
          (100) + 7 animals (70) + 8 ribbons (40) + 20 chaff (20). Dropping
          August would remove a 20-point bright and break that.
        * The deal is **8 to each hand and 4 to the field**, not 6 and 6.
        * **There is no koi-koi style stop decision.** Mushi is a hana-awase
          game: the round runs until the hands are empty.
        * **Two players.**

        November's lightning card (month 11, index 4) is **wild**: played from
        the hand it takes any single field card except another willow, and the
        dealer must claim it if it lands in the opening field.
        `selectableIndices` reports what may be taken, so the client does not
        re-implement that rule.

        Captured cards are public for BOTH seats — reading what the opponent is
        collecting is how the game is played. Only the CPU's HAND is withheld,
        and its `cardCount` still ships.

        Leftovers are unscored: 16 turns consume only 16 of the 20 stock cards,
        so a few stock and field cards go begging each round. The sources do not
        say what becomes of them, so nobody scores them.

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Play       | `0`   | Waiting for a card |
        | Select     | `1`   | Two same-month cards on the field; choose one |
        | WildSelect | `2`   | The lightning card was played; choose its target |
        | RoundEnd   | `3`   | Round settled |
        | GameEnd    | `4`   | Final round settled |

        | Command  | Aliases | Description |
        |----------|---------|-------------|
        | `reset`  | `r`     | Start / restart a game |
        | `play`   | `p`     | Play the hand card at `cardIndex` |
        | `select` | `s`     | Take the field card at `fieldIndex` |
        | `next`   | `n`     | Start the next round |
        | `hint`   | `h`     | Suggested move for the human seat |
        | `log`    | `l`     | Action-log transcript |
      operationId: mushiExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, select, s, next, n, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Hand index. Required for `play`.
                fieldIndex:
                  type: integer
                  description: Field index. Required for `select`.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
                    targetRounds:
                      type: integer
                      description: Rounds per game, 1-12. Mushi is normally 12.
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MushiResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MushiResponse'
  /bura/exec:
    post:
      tags:
        - bura
      summary: Execute a Bura game command
      description: |
        Send a command to advance the Bura (ブラ) game session identified by
        `sessionId`. It is a Russian ace-ten trick game on the 36-card short deck
        (A, 6-10, J, Q, K), played by one human against one CPU.

        Card points are A=11, 10=10, K=4, Q=3, J=2 and 0 for the rest, so the deck
        holds 120. Ranks run A > 10 > K > Q > J > 9 > 8 > 7 > 6 -- the ten
        outranking the court cards is the ace-ten family's defining quirk.

        Three rules differ from the description in #4402, which does not match the
        game as documented by pagat.com. The implementation follows the rules:

        * **There is no obligation to follow suit.** Any cards may be played; the
          "higher card of the led suit, or a trump" condition is what WINS a
          trick, not what makes a play legal. Discarding a card you cannot win
          with, to keep a strong one, is the centre of the game's bluffing.
        * **31 points is claimed, not detected.** Reaching it wins nothing. The
          `claim` command declares it -- and claiming while short forfeits the
          round to the opponent.
        * **An unclaimed round with an empty stock is a draw**, reported by
          `isDraw`, not a settlement on points.

        A lead is one to three cards of a SINGLE suit, sent in `cardIndices`. A
        response must contain the same NUMBER of cards, and takes the trick only
        if every led card is beaten by a distinct card of the response.

        The game proceeds through these phases:

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Play      | `0`   | Round in progress |
        | GameEnd   | `1`   | Someone claimed or declared, or the round was drawn |

        | Command   | Aliases | Description |
        |-----------|---------|-------------|
        | `reset`   | `r`     | Start / restart a round |
        | `play`    | `p`     | Play the cards in `cardIndices` |
        | `claim`   | `c`     | Claim 31 card points; claiming short forfeits |
        | `declare` | `d`     | Declare a hand combination (bura / Moscow / little Moscow / molodka) |
        | `hint`    | `h`     | Suggested play for the human seat |
        | `log`     | `l`     | Action-log transcript |
      operationId: buraExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, claim, c, declare, d, hint, h, log, l]
                sessionId:
                  type: string
                cardIndices:
                  type: array
                  description: |
                    Hand indices to play. A LEAD may hold one to three cards, all
                    of the same suit; a RESPONSE must hold exactly as many cards
                    as the lead. Required for `play`.
                  items:
                    type: integer
                  minItems: 1
                  maxItems: 3
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BuraResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BuraResponse'
  /terrace/exec:
    post:
      tags:
        - terrace
      summary: Execute a Terrace game command
      description: |
        Send a command to advance the Terrace (テラス) game session identified by
        `sessionId`. It is a two-deck (104-card) solitaire, also known as Queen of
        Italy.

        An **11-card terrace** is laid out face up, one card is dealt to each of
        **nine tableau piles**, and the remaining 84 form the stock, turned one at
        a time to a waste with **no redeal**.

        Two signature rules:

        * The **eight foundations build up in ALTERNATING COLOUR, not by suit**,
          13 cards each, wrapping King round to Ace. The base rank is not dealt:
          the first card the player sends to a foundation fixes it, and
          `awaitingBaseRank` reports that state. #4381 describes "same suit
          ascending", which would be a different game.
        * **The terrace top can only go to a foundation, never to the tableau.**
          It is a feed, not a parking space, and it is never refilled. #4381 says
          the top card of the terrace and the tableau may both move, which would
          defuse the central tension.

        The tableau also builds down in alternating colour, wrapping Ace round to
        King, **one card at a time**. An empty tableau pile refills
        **automatically** from the waste, else the stock, so there is no move that
        fills one by hand.

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | All eight foundations reached 13 cards |
        | GameOver   | `2`   | Player gave up |

        | Command        | Aliases     | Description |
        |----------------|-------------|-------------|
        | `reset`        | `r`         | Start / restart a game |
        | `draw`         | `d`         | Turn one card from the stock |
        | `move`         | `m`         | Move a card (requires `from` and `to`) |
        | `giveup`       | `g`         | Give up the current game |
        | `hint`         | `h`         | Terrace, else foundation, else tableau, else "draw" |
        | `autocomplete` | `ac`        | Send every legal card to the foundations |
        | `undo`         | `u`         | Undo the last action |
        | `undo_n`       | -           | Undo the last `n` actions |
        | `log`          | `l`         | View action log |

        Move zones:
        * `from`: `{zone: "reserve"}` (the terrace), `{zone: "waste"}`, or `{zone: "tableau", col: 0..8}`
        * `to`: `{zone: "foundation"}` or `{zone: "tableau", col: 0..8}`
        * A `reserve` source is only valid with a `foundation` destination.

      operationId: terraceExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, draw, d, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
                sessionId:
                  type: string
                from:
                  $ref: '#/components/schemas/TerraceMoveZone'
                to:
                  $ref: '#/components/schemas/TerraceMoveZone'
                n:
                  type: integer
                  description: Number of moves to undo (for `undo_n` command)
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              terraceToFoundation:
                summary: Send the terrace top up, fixing the base rank on the first move
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: reserve
                  to:
                    zone: foundation
              betweenPiles:
                summary: Move one card from pile 0 onto pile 5
                value:
                  command: move
                  sessionId: my-session-001
                  from:
                    zone: tableau
                    col: 0
                  to:
                    zone: tableau
                    col: 5
              draw:
                summary: Turn one card from the stock
                value:
                  command: draw
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TerraceResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TerraceResponse'
  /spiteandmalice/exec:
    post:
      tags:
        - spiteandmalice
      summary: Execute a Spite and Malice game command
      description: |
        Send a command to advance the Spite and Malice (スパイト・アンド・マリス) game
        session identified by `sessionId`. Spite and Malice is a 2-player race
        card game (1 human vs 1 CPU). Each player has a goal pile (default 20
        cards), a 5-card hand, and 4 personal side piles. Up to 4 shared
        foundations build A → 2 → … → Q; K is wild. The first player to empty
        their goal pile wins. A turn ends with a discard from hand to one side
        pile.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | `0`   | Game in progress |
        | GameOver  | `1`   | A player has emptied their goal pile |

        | Command   | Aliases | Description |
        |-----------|---------|-------------|
        | `reset`   | `r`     | Start / restart a game |
        | `move`    | `m`     | Play a card to a foundation (requires `from` and `to`) |
        | `discard` | `d`     | Discard from hand to side pile (ends turn) |
        | `cpu`     | —       | Advance the CPU's turn by one step |
        | `hint`    | `h`     | Return the recommended next move |
        | `log`     | `l`     | View action log |

        Move zones:
        * `from`: `{zone: "hand", idx: i}` | `{zone: "goal"}` | `{zone: "side", idx: 0..3}`
        * `to` (move): `{zone: "foundation", idx: 0..3}`
        * `from` (discard): `{zone: "hand", idx: i}`
        * `to` (discard): `{zone: "side", idx: 0..3}`
      operationId: spiteandmaliceExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, move, m, discard, d, cpu, hint, h, log, l]
                sessionId:
                  type: string
                from:
                  $ref: '#/components/schemas/SpiteAndMaliceMoveZone'
                to:
                  $ref: '#/components/schemas/SpiteAndMaliceMoveZone'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              playHand:
                summary: Play hand[0] to foundation 1
                value:
                  command: move
                  sessionId: my-session-001
                  from: { zone: hand, idx: 0 }
                  to: { zone: foundation, idx: 1 }
              playGoal:
                summary: Play the goal pile top to foundation 0
                value:
                  command: move
                  sessionId: my-session-001
                  from: { zone: goal }
                  to: { zone: foundation, idx: 0 }
              discard:
                summary: Discard hand[2] to side 1 (ends turn)
                value:
                  command: discard
                  sessionId: my-session-001
                  from: { zone: hand, idx: 2 }
                  to: { zone: side, idx: 1 }
              cpu:
                summary: Advance CPU turn by one step
                value:
                  command: cpu
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpiteAndMaliceResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpiteAndMaliceResponse'

  /skat/exec:
    post:
      tags:
        - skat
      summary: Execute a Skat game command
      description: |
        Send a command to advance the Skat game session identified by
        `sessionId`. Skat is Germany's national 3-player trick-taking game
        with a 32-card deck (7..A in four suits), a ladder auction to
        decide the declarer, and three game types (Suit / Grand / Null).

        The round proceeds through these phases:

        | Phase            | Value | Description |
        |------------------|-------|-------------|
        | Bid              | `0`   | Ladder auction |
        | SkatPickup       | `1`   | Declarer chooses to pick up the skat |
        | Discard          | `2`   | Declarer discards two cards back to the skat |
        | GameDeclaration  | `3`   | Declarer picks game type and trump |
        | Play             | `4`   | Trick play |
        | TrickEnd         | `5`   | Trick won; awaiting next |
        | RoundEnd         | `6`   | Round complete; scores updated |
        | GameEnd          | `7`   | Match over (target score reached) |

        | Command       | Aliases | Description |
        |---------------|---------|-------------|
        | `reset`       | `r`     | Start / restart a match (optional `config`) |
        | `bid`         | `b`     | Accept or pass the current bid (`accept`: true/false) |
        | `pickskat`    | `ps`    | Declarer picks up or skips the skat (`pickup`: true/false) |
        | `discard`     | `d`     | Declarer discards two cards (`discardA`, `discardB`) |
        | `game`        | `g`     | Declare game type (`gameType`) and optional `trumpSuit` |
        | `play`        | `p`     | Play a card from hand (`cardIndex`) |
        | `next`        | `n`     | Advance after trick end |
        | `nextround`   | `nr`    | Start next round after round end |
        | `hint`        |         | Get a CPU-style hint |
        | `log`         | `l`     | View action log |
      operationId: skatExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bid, b, pickskat, ps, discard, d, game, g, play, p, next, n, nextround, nr, hint, log, l]
                accept:
                  type: boolean
                  description: bid command — true to accept current bid, false to pass
                pickup:
                  type: boolean
                  description: pickskat command — true to pick up, false to play "Hand"
                discardA:
                  type: integer
                  description: discard command — index of first card to discard
                discardB:
                  type: integer
                  description: discard command — index of second card to discard
                gameType:
                  type: integer
                  description: game command — 0=Suit, 1=Grand, 2=Null
                trumpSuit:
                  type: integer
                  description: game command (Suit only) — 0=Diamonds, 1=Hearts, 2=Spades, 3=Clubs
                cardIndex:
                  type: integer
                  description: play command — index of card in hand to play
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: 0=easy, 1=normal, 2=hard
                    targetScore:
                      type: integer
                      description: Match target score (default 1000)
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new match
                value:
                  command: reset
                  sessionId: my-session-001
              bidAccept:
                summary: Accept the current bid
                value:
                  command: bid
                  accept: true
                  sessionId: my-session-001
              play:
                summary: Play card at hand index 0
                value:
                  command: play
                  cardIndex: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  phase:
                    type: integer
                  roundNumber:
                    type: integer
                  trickNumber:
                    type: integer
                  currentPlayerIdx:
                    type: integer
                  forehandIdx:
                    type: integer
                  middlehandIdx:
                    type: integer
                  rearhandIdx:
                    type: integer
                  dealerIdx:
                    type: integer
                  declarerIdx:
                    type: integer
                  currentBid:
                    type: integer
                  activeBidActorIdx:
                    type: integer
                  gameType:
                    type: integer
                  trumpSuit:
                    type: integer
                  pickedSkat:
                    type: boolean
                  declarerCardPoints:
                    type: integer
                  defendersCardPoints:
                    type: integer
                  winnerSide:
                    type: integer
                  gameValue:
                    type: integer
                  gameEndFlag:
                    type: boolean
                  leadPlayerIdx:
                    type: integer
                  players:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: integer
                        isHuman:
                          type: boolean
                        cardCount:
                          type: integer
                        cards:
                          type: array
                          items:
                            $ref: '#/components/schemas/Card'
                        bid:
                          type: integer
                        isDeclarer:
                          type: boolean
                        cardPoints:
                          type: integer
                        cumulativeScore:
                          type: integer
                  currentTrick:
                    type: array
                    items:
                      type: object
                      properties:
                        playerIdx:
                          type: integer
                        card:
                          $ref: '#/components/schemas/Card'
                  skat:
                    type: array
                    items:
                      $ref: '#/components/schemas/Card'
                  config:
                    type: object
                    properties:
                      cpuDifficulty:
                        type: integer
                      targetScore:
                        type: integer
                  message:
                    type: string
                  messageCode:
                    type: string
                  messageParams:
                    type: object
                    additionalProperties:
                      type: string
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)

  /skitgubbe/exec:
    post:
      tags:
        - skitgubbe
      summary: Execute a Skitgubbe game command
      description: |
        Send a command to advance the Skitgubbe game session identified by
        `sessionId`. It is a Swedish 3-player game on 52 cards made of **two
        phases that are different games**, and the last player left holding
        cards is the *Skitgubbe*.

        #4404 describes both phases wrongly, so pagat.com's rules are used.

        **Phase 1 (collect), `phase: 0`.** It is **always a two-player duel**
        between the leader and the seat to the leader's left, and **suit is
        irrelevant** -- the higher card simply takes both, and its owner leads
        next. The issue's "the other players respond with the same suit or
        higher" is neither the seating nor the rule.

        * **stunsa**, which the issue omits: on equal ranks the cards **stay on
          the table**, both players draw, and the **same player leads again**.
          Whoever eventually wins takes everything that piled up.
        * Both duellists then draw back up to three -- not "the winner draws
          one".
        * The **last card drawn from the stock fixes the trump suit**, which
          the issue never mentions and which phase two depends on. `trumpSuit`
          is `-1` until then.
        * The phase ends when the stock is exhausted and **either side of the
          next duel** has run out. Waiting for every hand to empty would hang
          forever: with three seats, the player who never entered a duel keeps
          their cards.

        **Phase 2 (shed), `phase: 1`.** The cards collected in phase one --
        plus anything still in hand -- become the hand. A play must **beat**
        the card on the pile: a higher card of the **same suit**, or any
        **trump**. It is never lawful to duck, so the issue's "play in
        ascending order" is a different game. A player who cannot beat the pile
        **picks it up** (`canPickUp`) rather than being eliminated.

        `validIndices` carries the beat rule and `canPickUp` its negation, so
        the client re-implements neither.

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Collect | `0`   | Two-player duels; collect cards |
        | Shed    | `1`   | Beat the pile or pick it up |
        | GameEnd | `2`   | One player is left holding cards |

        | Command  | Aliases  | Description |
        |----------|----------|-------------|
        | `reset`  | `r`      | Start / restart a game |
        | `play`   | `p`      | Play the hand card at `cardIndex` |
        | `pickup` | `u`      | Take the pile; refused while anything beats it |
        | `hint`   | `h`      | Suggested move for the human seat |
        | `log`    | `l`      | Action-log transcript |
      operationId: skitgubbeExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, pickup, u, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Hand index. Required for `play`.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SkitgubbeResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SkitgubbeResponse'

  /laughandliedown/exec:
    post:
      tags:
        - laughandliedown
      summary: Execute a Laugh and Lie Down game command
      description: |
        Send a command to advance the Laugh and Lie Down game session identified
        by `sessionId`. It is a 16th-17th century English fishing game for five
        players on 52 cards, following David Parlett's reconstruction (the one
        pagat.com points to).

        #4396 gets the fishing mechanism itself wrong, so the source is used.

        * The issue calls the undealt cards a **stock pile** in the middle.
          There is no face-down stock: eight cards go to each player and the
          remaining **twelve are spread face up**. Seeing how many of each rank
          are left is what makes the three-card take a decision.
        * The issue matches against **the top card of the pile**. A play matches
          **any** table card of the same rank.
        * The issue has no three-card take. One hand card captures **one or
          three** table cards of a rank -- never two, never four.
        * The issue says a player who cannot capture **draws from the stock and
          is then eliminated**. There is no draw. A player who cannot capture
          **adds their whole hand to the table**, where it becomes everyone
          else's prey, and is out. Reducing that to plain elimination removes
          what the game is about.
        * The issue says the last player to shed their cards **takes the whole
          pot**. In fact whoever was **last in** (still holding cards when
          everyone else has finished) takes 5, and the rest settles on each
          player's captured count against eight.

        **The pot arithmetic is what verifies the settlement.** The pot is
        `3 + 2*4 = 11`. All 52 cards end in somebody's won pile, so the
        over/short total is always `(52 - 8*5) / 2 = 6`, and `5 + 6` is exactly
        11. A winner-takes-the-pot reading produces no such agreement.

        At the end, the last player's hand and whatever is left on the table go
        to the **dealer** -- the dealer staked one more than everyone else, so
        this is the source's compensation, and it is also what guarantees the 6
        above.

        `validIndices` carries the match rule and `threeTakeIndices` the
        three-card option, so the client never recounts the table.

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Play    | `0`   | Waiting for a capture |
        | GameEnd | `1`   | Only one player was left holding cards |

        | Command  | Aliases | Description |
        |----------|---------|-------------|
        | `reset`  | `r`     | Start / restart a game |
        | `play`   | `p`     | Capture with the hand card at `cardIndex` |
        | `hint`   | `h`     | Suggested move for the human seat |
        | `log`    | `l`     | Action-log transcript |
      operationId: laughAndLieDownExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Hand index. Required for `play`.
                takeCount:
                  type: integer
                  description: |
                    Table cards to capture: 1 or 3. Optional, defaulting to 1 --
                    an ordinary single capture needs no extra field. Anything
                    else is rejected.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LaughAndLieDownResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LaughAndLieDownResponse'

  /sjavs/exec:
    post:
      tags:
        - sjavs
      summary: Execute a Sjavs game command
      description: |
        Send a command to advance the Sjavs session identified by `sessionId`.
        It is the Faroese national card game: 32 cards, four players in two
        fixed partnerships (seats 0+2 against 1+3), Schafkopf-style permanent
        trumps, and a rubber counted DOWN from 24.

        #4403 gets three things wrong, so pagat.com is followed.

        * The issue lists **two** permanent trumps. There are **six**, in a
          fixed order: `CQ > SQ > CJ > SJ > HJ > DJ`, followed by the trump
          suit's own `A, K, (Q), 10, 9, 8, 7`. **The count checks this**: a red
          trump suit yields 6+7 = 13 trumps and a black one 6+6 = 12, matching
          the source. Two permanent trumps would give 9 and 8.
        * The issue bids **a target score**. A bid is the **length of your
          longest trump suit**, minimum five; if nobody reaches five the hand is
          redealt. Longer beats shorter and, at equal length, clubs beats
          everything — so a player whose longest suits include clubs must bid it
          as clubs.
        * The issue erases **one cross per hand**. A rubber is **24 points
          counted down**; the side first reaching zero or below wins it, and a
          cross records that rubber win.

        Trumps form their own suit when following: with hearts trump the club
        jack is a trump, so a club lead does not oblige it.

        Card points: A 11, 10 ten, K 4, Q 3, J 2 — **120 in the pack**.
        Settlement, doubled when clubs are trumps EXCEPT for the slam:

        | Declarers took | Declarers | In clubs |
        |----------------|-----------|----------|
        | all tricks     | 12        | **16** (not 24) |
        | 90-120         | 4         | 8 |
        | 61-89          | 2         | 4 |
        | 60-60          | nothing; the next game is worth two more | same |
        | 31-59          | opponents 4 | opponents 8 |
        | 0-30           | opponents 8 | opponents 16 |
        | no tricks      | **opponents 16, whatever the suit** | 16 |

        `trumpCount` ships the 13-or-12 so the client never recounts, and
        `validIndices` carries the following rule including the permanent
        trumps.

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Bid     | `0`   | Bidding a trump-suit length |
        | Play    | `1`   | Playing tricks |
        | HandEnd | `2`   | The hand is settled; `next` deals again |
        | GameEnd | `3`   | The rubber is decided |

        | Command  | Aliases | Description |
        |----------|---------|-------------|
        | `reset`  | `r`     | Start / restart a rubber |
        | `bid`    | `b`     | Bid `bidLength` cards; 0 passes |
        | `play`   | `p`     | Play the hand card at `cardIndex` |
        | `next`   | `n`     | Deal the next hand |
        | `hint`   | `h`     | Suggested move for the human seat |
        | `log`    | `l`     | Action-log transcript |
      operationId: sjavsExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bid, b, play, p, next, n, hint, h, log, l]
                sessionId:
                  type: string
                bidLength:
                  type: integer
                  description: |
                    Trump-suit length to bid. Required for `bid`. **Zero passes**
                    -- it is a real value, not an omission.
                cardIndex:
                  type: integer
                  description: Hand index. Required for `play`.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SjavsResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SjavsResponse'

  /trex/exec:
    post:
      tags:
        - trex
      summary: Execute a Trex game command
      description: |
        Send a command to advance the Trex (Trix) session identified by
        `sessionId`. It is the Middle Eastern contract-selection game: 52 cards,
        four players scoring individually, **twenty deals** made of four
        kingdoms of five contracts each.

        #4410 gets two of the five contracts wrong, so pagat.com is followed.

        | Value | Contract | Scoring |
        |-------|----------|---------|
        | `0` | King of Hearts | **-75** to whoever takes the trick with it |
        | `1` | Diamonds | **-10** per diamond taken (the issue says *hearts*) |
        | `2` | Queens | **-25** per queen taken |
        | `3` | Tricks | **-15** per trick taken |
        | `4` | Dominoes | **+200 / +150 / +100 / +50** by finishing order |
        | `5` | *(none)* | No contract chosen yet |

        * The issue lists a **"no kings"** contract. Only the **king of hearts**
          costs anything; the other three kings are free.
        * The issue starts the dominoes **from the seven**. They build out from
          the **JACK**: a legal play is any jack, or a card one rank above or
          below one already played in that suit. Each suit is its own run, and
          the ace sits above the king.
        * The issue treats every contract as a penalty and awards the game to
          the **smallest** total. The dominoes contract **pays out**, so the
          winner is whoever finishes **highest** and scores can be positive.

        **The table is verified by its arithmetic**: the four penalty contracts
        total `75 + 130 + 100 + 195 = 500`, and the dominoes hand out
        `200 + 150 + 100 + 50 = 500`. One kingdom is exactly zero-sum, so all
        four scores sum to zero after twenty deals. Change any single value and
        that agreement disappears.

        The player dealt the **seven of hearts** owns the first kingdom, and a
        king may never choose the same contract twice — `availableContracts`
        carries what is left.

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Choose  | `0`   | The king picks a contract |
        | Play    | `1`   | Playing the contract |
        | DealEnd | `2`   | The deal is settled; `next` deals again |
        | GameEnd | `3`   | Twenty deals played |

        | Command  | Aliases | Description |
        |----------|---------|-------------|
        | `reset`  | `r`     | Start / restart a game |
        | `choose` | `c`     | Choose `contract` (king only) |
        | `play`   | `p`     | Play the hand card at `cardIndex` |
        | `pass`   | `s`     | Pass in the dominoes; refused with a legal play |
        | `next`   | `n`     | Deal the next hand |
        | `hint`   | `h`     | Suggested move for the human seat |
        | `log`    | `l`     | Action-log transcript |
      operationId: trexExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, choose, c, play, p, pass, s, next, n, hint, h, log, l]
                sessionId:
                  type: string
                contract:
                  type: integer
                  description: Contract to play, 0-4. Required for `choose`.
                cardIndex:
                  type: integer
                  description: Hand index. Required for `play`.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrexResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrexResponse'

  /loba/exec:
    post:
      tags:
        - loba
      summary: Execute a Loba game command
      description: |
        Send a command to advance the Loba session identified by `sessionId`.
        It is the Argentine rummy played with **108 cards** (two packs plus four
        jokers): nine each, and a player is knocked out on reaching **101**
        penalty points. This implements **Loba de Menos**, the negative-scoring
        form.

        **#4414's central premise does not hold: Loba is not a contract rummy.**
        The issue has "a contract per hand, played in order" and awards the game
        to the lowest total once every contract is done. pagat.com states
        plainly that there is *no fixed contract* — that structure belongs to
        Carioca / Contract Rummy. Loba knocks players out at 101 and the **last
        player standing wins**. The issue also says 3-6 players; it is 2-5.

        Rules the issue does not mention, which are what make it Loba:

        * A **pierna** is one rank in **three DIFFERENT suits**. With two packs
          the same suit turns up twice, and that does not make a meld. Cards
          laid off later must stay within those three suits.
        * **Jokers are not wild.** They go only in an *escalera* (a suit run),
          at most one per run, never in a pierna — and they normally **cannot be
          discarded** (only when a joker is the last card in hand).
        * In an escalera the ace is high **or** low, never both, so K-A-2 is not
          a run.
        * Going out **without having melded earlier in the round** takes **10
          off** your score.
        * Cards left in hand: numerals at face value, **joker, K, Q, J and A ten
          each**.
        * Laying off is allowed onto **any** player's meld once you have melded
          something of your own.

        Not implemented: the buy-back ("reengancharse"). The source has it as
        optional and does not say who decides or when, so against CPUs there is
        no owner for that decision.

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Draw     | `0`   | Take from the stock or the discard |
        | Act      | `1`   | Meld, lay off, then discard |
        | RoundEnd | `2`   | The round is settled; `next` deals again |
        | GameEnd  | `3`   | One player is left |

        | Command       | Aliases | Description |
        |---------------|---------|-------------|
        | `reset`       | `r`     | Start / restart a game |
        | `drawstock`   | `ds`    | Draw from the stock |
        | `drawdiscard` | `dd`    | Take the top discard |
        | `meld`        | `m`     | Put down `cardIndices` as a meld |
        | `layoff`      | `o`     | Add `cardIndex` to `meldIndex` |
        | `discard`     | `d`     | Discard `cardIndex` |
        | `next`        | `n`     | Deal the next round |
        | `hint`        | `h`     | Suggested move for the human seat |
        | `log`         | `l`     | Action-log transcript |
      operationId: lobaExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, drawstock, ds, drawdiscard, dd, meld, m, layoff, o, discard, d, next, n, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Hand index. Required for `discard` and `layoff`.
                cardIndices:
                  type: array
                  description: Hand indices forming the meld. Required for `meld`.
                  items:
                    type: integer
                meldIndex:
                  type: integer
                  description: Index into `melds`. Required for `layoff`.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LobaResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LobaResponse'

  /desmoche/exec:
    post:
      tags:
        - desmoche
      summary: Execute a Desmoche game command
      description: |
        Send a command to advance the Desmoche session identified by `sessionId`.
        Desmoche is a Nicaraguan rummy for **2-4** players: nine cards each, and
        the pot goes to whoever gets **exactly ten** cards (the nine dealt plus
        one drawn) down in melds of three or more.

        **Two of #4405's headline mechanics do not exist.**

        * The issue has players "compare hand strength using the **poker hand
          rankings**", with the strongest taking the pot. **Poker rankings play
          no part.** Desmoche is a rummy in the Conquian/gin family and the only
          question is who gets down first.
        * The issue treats **desmoche** as the declaration you make on going out.
          It is not a declaration at all — it is the move of *taking a card from
          one of your own face-up melds to use in another*, allowed as long as
          the meld you took from is still valid.
        * You put down **ten** cards, not nine, so the winning turn ends with no
          discard.
        * 2-4 players, not 2-5.

        The issue also omits the ending that gives the game its shape: **if the
        stock runs out with nobody down to ten, there is no winner** and the pot
        carries over into the next deal, on top of fresh antes.

        Aces are high **or** low in a run, never both, so K-A-2 is not a run.

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Draw     | `0`   | Take from the stock or the discard |
        | Act      | `1`   | Meld, lay off, rearrange, then discard |
        | RoundEnd | `2`   | The round is settled; `next` deals again |
        | GameEnd  | `3`   | Five rounds are done |

        | Command       | Aliases | Description |
        |---------------|---------|-------------|
        | `reset`       | `r`     | Start / restart a game |
        | `drawstock`   | `ds`    | Draw from the stock |
        | `drawdiscard` | `dd`    | Take the top discard |
        | `meld`        | `m`     | Put down `cardIndices` as a meld |
        | `layoff`      | `o`     | Add `cardIndex` to `meldIndex` |
        | `desmoche`    | `x`     | Move card `cardIndex` of `fromMeldIndex` into `toMeldIndex` |
        | `discard`     | `d`     | Discard `cardIndex` |
        | `next`        | `n`     | Deal the next round |
        | `hint`        | `h`     | Suggested move for the human seat |
        | `log`         | `l`     | Action-log transcript |
      operationId: desmocheExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, drawstock, ds, drawdiscard, dd, meld, m, layoff, o, desmoche, x, discard, d, next, n, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: |
                    Hand index for `discard` and `layoff`; for `desmoche` it is
                    the index *within* `fromMeldIndex`, not a hand index.
                cardIndices:
                  type: array
                  description: Hand indices forming the meld. Required for `meld`.
                  items:
                    type: integer
                meldIndex:
                  type: integer
                  description: Index into `melds`. Required for `layoff`.
                fromMeldIndex:
                  type: integer
                  description: Meld to take a card from. Required for `desmoche`.
                toMeldIndex:
                  type: integer
                  description: Meld to move the card into. Required for `desmoche`.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DesmocheResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DesmocheResponse'

  /zwicker/exec:
    post:
      tags:
        - zwicker
      summary: Execute a Zwicker game command
      description: |
        Send a command to advance the Zwicker session identified by `sessionId`.
        Zwicker is the north-German (Schleswig) fishing game of the Cassino
        family, played by **four in two fixed partnerships** on **55 cards**
        (52 plus three jokers).

        **Neither headline mechanic in #4387 exists.**

        * The issue has a player **declaring any value for a joker**, making it
          wild. Jokers are not wild: the three carry a **fixed 15, 20 and 25**.
          What does carry a choice is the **aces and court cards** — A is 1 *or*
          11, J is 2 *or* 12, Q is 3 *or* 13, K is 4 *or* 14, and the player
          decides which. They also take part in sums, which is precisely why
          this cannot reuse the Cassino engine: there a court card matches by
          rank alone.
        * The issue treats **"Zwick"** as the name for capturing several groups
          at once. It is the bonus for **clearing the table**, worth 1. Taking
          several groups in one play is ordinary Cassino-family capture.
        * Three jokers, not two. The deal — 3 to the table, then 4 + 4 + 5 each
          — exhausts the pack only at 55: 55 − 3 = 52 divides by four, 54 − 3 =
          51 does not.

        Scoring, which comes to **exactly 30** plus one per Zwick: large joker
        7, middle 6, small 5, ♦10 3, ♠10 1, ♠2 1, each ace 1, majority of cards
        3. A tie on card count leaves the majority three unawarded.

        Documented convention, not from the source: when the deal runs out, any
        cards left on the table go to the **last player who captured**. That
        moves the three-point majority, so it is stated rather than hidden.

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Play     | `0`   | Capture, build or trail |
        | RoundEnd | `1`   | The deal is scored; `next` deals again |
        | GameEnd  | `2`   | A team reached the target |

        | Command   | Aliases | Description |
        |-----------|---------|-------------|
        | `reset`   | `r`     | Start / restart a game |
        | `take`    | `t`     | Play `cardIndex` as `playedValue` and capture `tableIndices` / `buildIndices` |
        | `build`   | `b`     | Build `declaredValue` from `cardIndex` and `tableIndices` |
        | `trail`   | `tr`    | Put `cardIndex` on the table |
        | `next`    | `n`     | Deal the next hand |
        | `hint`    | `h`     | Suggested move for the human seat |
        | `log`     | `l`     | Action-log transcript |

        `trail` is aliased `tr`, not `l`: `l` is the action log.
      operationId: zwickerExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, take, t, build, b, trail, tr, next, n, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Hand index. Required for `take`, `build` and `trail`.
                playedValue:
                  type: integer
                  description: |
                    Which matching value the played card is used as. Required for
                    `take`. An ace or court card has two; a number card and a
                    joker have one.
                tableIndices:
                  type: array
                  description: Table-card indices to capture, or to fold into a build.
                  items:
                    type: integer
                buildIndices:
                  type: array
                  description: Indices into `builds` to capture. A build is taken only at its exact declared value.
                  items:
                    type: integer
                declaredValue:
                  type: integer
                  description: |
                    Value of the build being made. Required for `build`. You must
                    keep a card able to make that value, or the build is refused.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
                    targetScore:
                      type: integer
                      description: Points that win the game (default 61).
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ZwickerResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ZwickerResponse'

  /poch/exec:
    post:
      tags:
        - poch
      summary: Execute a Poch game command
      description: |
        Send a command to advance the Poch session identified by `sessionId`.
        Poch is a 15th-century German three-stage game for **3-6** players on a
        **32-card** pack (A-K-Q-J-10-9-8-7) and a board of **nine pools**.

        **The three-stage skeleton in #4415 is right; all three stages are not.**

        * The issue deals leftover cards **onto** the board compartments. No
          cards go on the board. Before every deal each player puts **one chip
          into each of the nine pools**, and the single leftover card is turned
          face up -- its suit is the **pay suit**.
        * The issue pays a pool to anyone holding "the relevant **rank**". Rank
          alone pays nothing: you need that card **in the pay suit**. Marriage
          needs the pay-suit K *and* Q in one hand; Sequence needs its 7-8-9.
        * The issue makes stage two a **bluff**: declare "I hold N of a rank",
          bet, and whoever's declaration stands takes the pot. There is no
          declaration and no bluff. It is a **comparison of same-rank sets** --
          four of a kind beats three, three beats a pair -- among those who did
          not fold. "Pocher" is one of the nine pools, not a move.
        * The issue ends stage three at "you cannot play, so your turn ends".
          The play is a **suit sequence upwards**: each card must be the next
          higher card of the same suit. When nobody holds the next one the run
          is **stopped**, and whoever played the last and highest card starts
          again with any card.

        Two things the issue omits, both load-bearing: going out collects the
        **centre pot plus one chip from every other player for each card they
        still hold**, and **pools nobody claims carry over** — everyone antes
        again next deal, so Marriage and Sequence build up.

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Staking  | `0`   | Stage 1; resolves itself the moment cards are dealt |
        | Pochen   | `1`   | Stage 2; bet or fold |
        | Stops    | `2`   | Stage 3; play up the suit |
        | DealEnd  | `3`   | The deal is settled; `next` deals again |
        | GameEnd  | `4`   | The last deal is done |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `bet`   | `b`     | Put in one unit (call or raise) |
        | `fold`  | `f`     | Drop out of the pochen |
        | `play`  | `p`     | Play `cardIndex` |
        | `next`  | `n`     | Deal the next hand |
        | `hint`  | `h`     | Suggested move for the human seat |
        | `log`   | `l`     | Action-log transcript |
      operationId: pochExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bet, b, fold, f, play, p, next, n, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Hand index. Required for `play`.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
                    targetDeals:
                      type: integer
                      description: Deals played before the game is decided (default 5).
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PochResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PochResponse'

  /popejoan/exec:
    post:
      tags:
        - popejoan
      summary: Execute a Pope Joan game command
      description: |
        Send a command to advance the Pope Joan session identified by `sessionId`.
        Pope Joan is the ancestor of the stops family, played by **3-8** on **51
        cards** (a standard pack with the **8 of diamonds removed**) over a board
        of **eight named compartments**.

        **#4389 gets the board, the deal and the settlement wrong, and omits
        three rules.**

        * The issue calls the compartments "the eight court cards plus the Pope".
          They are **Ace, King, Queen, Jack, Game, Pope (9D), Matrimony (the
          trump K-Q) and Intrigue (the trump Q-J)** — Game, Matrimony and
          Intrigue correspond to no single card.
        * The issue has players **distributing** an ante. The split is fixed and
          it is the **dealer's**: six to Pope, two each to Matrimony and
          Intrigue, one to each of the other five — **fifteen** in all.
        * The issue leads "the lowest card of a run". The lead is **any suit, so
          long as it is your lowest card**.
        * The issue pays a compartment to whoever plays "the matching K, Q, J or
          Pope". Only **trumps** pay. The Pope is the exception: it is the 9 of
          diamonds whatever the trump suit.
        * Omitted: a hand is dealt to a **dead hand** as well, and its last card
          is **turned for trump**. If that card is the Pope, an Ace, King, Queen
          or Jack, the **dealer takes that compartment outright**.
        * Omitted: a run also stops on a **King** — nothing outranks it — not
          only at the 7 of diamonds. Whoever played the stopping card leads
          again.
        * Omitted: at the end, the player holding the **Pope is excused** the
          one-chip-per-card payment.
        * Omitted: a compartment nobody claims **carries over** to the next deal.

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Play    | `0`   | Lead or follow |
        | DealEnd | `1`   | The deal is settled; `next` deals again |
        | GameEnd | `2`   | The last deal is done |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `play`  | `p`     | Play `cardIndex` |
        | `next`  | `n`     | Deal the next hand |
        | `hint`  | `h`     | Suggested move for the human seat |
        | `log`   | `l`     | Action-log transcript |
      operationId: popejoanExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, next, n, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Hand index. Required for `play`.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
                    targetDeals:
                      type: integer
                      description: Deals played before the game is decided (default 5).
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PopeJoanResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PopeJoanResponse'

  /nainjaune/exec:
    post:
      tags:
        - nainjaune
      summary: Execute a Le Nain Jaune game command
      description: |
        Send a command to advance the Le Nain Jaune session identified by
        `sessionId`. The French stops game for **3-8** on a **52-card** pack and
        a board of **five boxes**.

        **#4380 gets the boxes, the stake, the run and the settlement wrong.**

        * The issue lists the boxes as "♦7, **♠10**, **♥Q**, ♣J, **sequence**".
          They are **♦10, ♣J, ♠Q, ♥K and ♦7** (the yellow dwarf, in the centre).
          Two suits are wrong, the **♥K is missing**, and **there is no
          "sequence" box** at all.
        * The issue says players "place an ante on the five boxes". The amount is
          fixed per box: **1 on ♦10, 2 on ♣J, 3 on ♠Q, 4 on ♥K and 5 on ♦7** —
          fifteen from each player.
        * The issue assigns the nonexistent sequence box to "whoever plays the
          last card of a suit (a king)". A king simply **ends the run**, and its
          player leads again with anything.
        * The issue plays "consecutive cards starting from the lowest". The lead
          is **any card**, and from there the run climbs by rank **ignoring suit
          entirely** — the decisive difference from Pope Joan.
        * The issue pays the winner "the number of cards" left in each hand. It
          is the **points**: ace 1, pip cards face value, **J/Q/K ten each**.
        * Not mentioned: a **talon** (undealt remainder) is set aside, and boxes
          nobody claims **carry over**.

        A box pays only on its **exact card, suit included** — an off-suit seven
        does not claim the dwarf.

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Play    | `0`   | Lead or continue the run |
        | DealEnd | `1`   | The deal is settled; `next` deals again |
        | GameEnd | `2`   | The last deal is done |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `play`  | `p`     | Play `cardIndex` |
        | `next`  | `n`     | Deal the next hand |
        | `hint`  | `h`     | Suggested move for the human seat |
        | `log`   | `l`     | Action-log transcript |
      operationId: nainjauneExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, next, n, hint, h, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Hand index. Required for `play`.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
                    targetDeals:
                      type: integer
                      description: Deals played before the game is decided (default 5).
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NainJauneResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NainJauneResponse'

  /kille/exec:
    post:
      tags:
        - kille
      summary: Execute a Kille game command
      description: |
        Send a command to advance the Kille session identified by `sessionId`.
        Kille is the Swedish Cuckoo game played with its **own 42-card pack**:
        **21 denominations in two copies, a single suit**. Each player holds
        exactly one card and tries not to be left holding the lowest.

        **#4383 has both named picture-card effects backwards, and misses three
        more.**

        * The issue says the **Harlekin (Harlequin) may refuse an exchange**. It
          may not. The Harlequin is exchanged **face up**, and its strength
          **inverts with how it arrived**: dealt or drawn from the stock it is
          the **highest** card in the pack, received in an exchange it is the
          **lowest**.
        * The issue says the **Kuckuck (Cuckoo) forces an exchange**. The
          opposite -- *"no one swaps the Cuckoo"*. The exchange is refused and
          the **round ends on the spot** with everyone revealing.
        * Three more picture cards break the exchange and the issue names none
          of them: the **Hussar** cuts down the **challenger**, the **Pig**
          unwinds the swap and knocks out **its own holder**, and the
          **Cavalier** and **Inn** pass the challenge **along to the next
          seat**.
        * The issue calls it a **life system with 3 lives**. It is not. Players
          **ante a stake**, the loser of a round is out, and may **buy back in
          at most three times** -- one stake, then **half the pot**, then the
          **whole pot**.
        * The issue says the lowest card loses. It is the lowest **excluding a
          dealt Harlequin**, and anyone knocked out by the Hussar or the Pig
          goes with them.
        * The issue says 3-6 players. The original is **3-12**.

        The deck is not the French 52, so cards carry `glyph` / `label` /
        `color` / `deck` and are drawn procedurally (ADR-0033).

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Exchange | `0`   | Each seat in turn challenges or stands pat |
        | Showdown | `1`   | Hands are revealed and the round is settled |
        | GameEnd  | `2`   | One player is left standing |

        | Command     | Aliases | Description |
        |-------------|---------|-------------|
        | `reset`     | `r`     | Start / restart a game |
        | `exchange`  | `e`     | Challenge your left neighbour (the dealer swaps with the stock) |
        | `satisfied` | `s`     | Keep your card |
        | `reenter`   | `re`    | Pay to buy back in after going out |
        | `nextround` | `nr`    | Deal the next round |
        | `log`       | `l`     | Action-log transcript |
      operationId: killeExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, exchange, e, satisfied, s, reenter, re, nextround, nr, log, l]
                sessionId:
                  type: string
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
                    stake:
                      type: integer
                      description: Stake each player antes per round (1-100, default 1).
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KilleResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KilleResponse'

  /klaberjass/exec:
    post:
      tags:
        - klaberjass
      summary: Execute a Klaberjass game command
      description: |
        Send a command to advance the Klaberjass (Clobyosh / Bela) session
        identified by `sessionId`. Klaberjass is the **two-player ancestor** of
        the Jass family, played on a 32-card pack of which **only 18 cards are
        dealt**.

        **#4395 is wrong on five counts.**

        * The issue says "**163 points** are contested". Wrong twice over: the
          pack holds **162** (152 in cards plus 10 for the last trick), and
          since only 18 of 32 cards reach a hand there is **no fixed total** to
          contest — it varies with the deal.
        * The issue omits the **dix**: when the turn-up suit becomes trump, the
          holder of the **trump seven** may exchange it for the turn-up. No
          exchange is allowed once a different suit is named.
        * The issue omits the **schmeiss**: a player may offer to throw the deal
          in, and if the opponent **refuses**, the *thrower* becomes the maker
          with the turn-up suit as trump.
        * The issue gives no sequence values. Three cards score **20**, four or
          more score **50** — and **only the better holder scores, but scores
          all of their sequences**; an exact tie scores for nobody. Sequences
          run in **natural rank order** (7-8-9-10-J-Q-K-A), *not* point order.
        * The issue says the bela needs trump K and Q played **consecutively**.
          It does not — each is announced as it is played.
        * The issue names no target score. It is **501**.

        In trumps the **jack (Jass) scores 20** and the **nine (Menel) 14**,
        both outranking the ace. Following suit, trumping when void and
        **overtrumping a trump lead** are all compulsory, so the server sends
        `validPlays` rather than leaving the client to re-derive them.

        At the showdown the maker must score **strictly more** than the
        opponent. A tie is **bete**: the maker scores nothing and the opponent
        takes **both** totals.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | BidTurnUp | `0`   | Take the turn-up suit, pass, or schmeiss |
        | BidFree   | `1`   | Name any suit but the refused turn-up, or pass |
        | Schmeiss  | `2`   | Answer a pending throw |
        | Play      | `3`   | Nine tricks |
        | HandEnd   | `4`   | The hand is settled; `next` deals again |
        | GameEnd   | `5`   | Somebody reached the target |

        | Command          | Aliases | Description |
        |------------------|---------|-------------|
        | `reset`          | `r`     | Start / restart a game |
        | `accept`         | `a`     | Take the turn-up suit as trump |
        | `call`           | `c`     | Name `suit` as trump (1=S 2=C 3=H 4=D) |
        | `pass`           | `ps`    | Pass |
        | `schmeiss`       | `sm`    | Offer to throw the deal in |
        | `answerschmeiss` | `as`    | Answer with `accept` |
        | `play`           | `p`     | Play `cardIndex` |
        | `next`           | `n`     | Deal the next hand |
        | `log`            | `l`     | Action-log transcript |
      operationId: klaberjassExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, accept, a, call, c, pass, ps, schmeiss, sm, answerschmeiss, as, play, p, next, n, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Hand index. Required for `play`.
                suit:
                  type: integer
                  description: "Trump suit for `call`: 1=Spade, 2=Clover, 3=Heart, 4=Diamond."
                accept:
                  type: boolean
                  description: Required for `answerschmeiss`. `false` makes the thrower the maker.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
                    targetScore:
                      type: integer
                      description: Points needed to win (100-1000, default 501).
                    allowSchmeiss:
                      type: boolean
                      description: Whether the schmeiss option is offered (default true).
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KlaberjassResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KlaberjassResponse'

  /kaiser/exec:
    post:
      tags:
        - kaiser
      summary: Execute a Kaiser game command
      description: |
        Send a command to advance the Kaiser session identified by `sessionId`.
        Kaiser is the Saskatchewan partnership bidding game for **4 players in
        two fixed teams** (seats 0/2 against 1/3).

        **#4393 is self-contradictory about the deck, and omits four rules.**

        * The issue asks for **32 cards, "excluding ranks 2-6"** — but that
          removes the **five of hearts and three of spades**, the two cards the
          game is named for and scores on. The pack is **34 cards**:
          A-K-Q-J-10-9-8-7 in each suit **plus the heart five and spade three**.
          The arithmetic confirms it: 4 seats x 8 cards = 32, plus a **two-card
          kitty** = 34.
        * The issue omits the **kitty**. Two cards go face down; the winning
          bidder takes them into hand and discards two — but **may never
          discard the heart five or the spade three**.
        * The issue says players bid a **number of tricks**. They bid
          **points** — there are only 8 tricks but the heart five is worth 5.
          The range is **7-12**, and the floor is 7 rather than 6 precisely
          because the kitty is such an advantage.
        * The issue omits **no trump** and **low no trump**. In low no trump
          the ranking **reverses**, so the seven is the highest card. A bid of
          the same number ranks trump < no trump < low no trump.
        * The issue names no target. It is **52 points**, raised to **62** once
          any no-trump bid succeeds. A side on **45 or more cannot score
          further unless it bids**.

        The issue is right that one hand is worth **10 points** (8 tricks + 5
        for the heart five - 3 for the spade three) and that a failed bid comes
        straight off the declaring side's score.

        Following suit is compulsory; trumping is not. The server therefore
        sends `validPlays` rather than leaving the client to re-derive them.

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Bid     | `0`   | Bid points or pass |
        | Discard | `1`   | Declarer names trump, then discards two |
        | Play    | `2`   | Eight tricks |
        | HandEnd | `3`   | The hand is settled; `next` deals again |
        | GameEnd | `4`   | A team reached the target |

        | Command   | Aliases | Description |
        |-----------|---------|-------------|
        | `reset`   | `r`     | Start / restart a game |
        | `bid`     | `b`     | Bid `bid` points with optional `contract` |
        | `pass`    | `ps`    | Pass |
        | `trump`   | `t`     | Name `suit` as trump |
        | `discard` | `d`     | Discard the two hand `indices` |
        | `play`    | `p`     | Play `cardIndex` |
        | `next`    | `n`     | Deal the next hand |
        | `log`     | `l`     | Action-log transcript |
      operationId: kaiserExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bid, b, pass, ps, trump, t, discard, d, play, p, next, n, log, l]
                sessionId:
                  type: string
                bid:
                  type: integer
                  description: Points bid (7-12). Required for `bid`.
                contract:
                  type: integer
                  description: "0=with trump, 1=no trump, 2=low no trump. Defaults to 0."
                suit:
                  type: integer
                  description: "Trump suit for `trump`: 1=Spade, 2=Clover, 3=Heart, 4=Diamond."
                indices:
                  type: array
                  items:
                    type: integer
                  description: The two hand indices to discard. Required for `discard`.
                cardIndex:
                  type: integer
                  description: Hand index. Required for `play`.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
                    allowNoTrump:
                      type: boolean
                      description: Whether no-trump bids are offered (default true).
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KaiserResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KaiserResponse'

  /boston/exec:
    post:
      tags:
        - boston
      summary: Execute a Boston game command
      description: |
        Send a command to advance the Boston session identified by `sessionId`.
        Boston is the 18th-century Whist derivative for **4 players** on a
        52-card pack, dealt **4-4-5** to thirteen cards each.

        **#4394 gets the auction ladder structurally wrong and omits three rules.**

        * The issue reads as though the trick bids run first and the miseres
          follow. **They interleave.** Little Misere ranks **below seven
          tricks**; Grand Misere ranks **below nine**. Flattening the ladder
          changes what the auction means.
        * The issue omits **Piccolissimo** — a bid to take **exactly one**
          trick, sitting between seven and eight. **Zero tricks fails it**, so
          it is neither a trick bid nor a misere.
        * The issue calls the exposed hand an option ("open-hand in some
          cases"). **On the Table is a separate, higher bid**, and the hand is
          exposed after the **first trick**.
        * The issue says every player plays for themselves. **Trick bids (5-10)
          may call a partner**, making it two against two; only the miseres,
          Piccolissimo and the slams are played alone against three.
        * The issue omits the deal pattern: **4 cards, 4 cards, then 5**.

        The full ladder, lowest first, is: 5 tricks, 6 tricks, **Little
        Misere**, 7 tricks, **Piccolissimo**, 8 tricks, **Grand Misere**, 9
        tricks, **Little Misere on the Table**, 10 tricks, **Grand Misere on
        the Table**, 11 tricks, 12 tricks, **Chelem**, **Chelem on the Table**.
        `bidOptions` sends this table so the client never has to rebuild it.

        Following suit is compulsory; trumping is not. The lead belongs to the
        **dealer's left**, not the declarer.

        | Phase       | Value | Description |
        |-------------|-------|-------------|
        | Bid         | `0`   | Bid a ladder step or pass |
        | CallPartner | `1`   | Trick bids only: call a partner or go alone |
        | Play        | `2`   | Thirteen tricks |
        | HandEnd     | `3`   | The hand is settled; `next` deals again |
        | GameEnd     | `4`   | The target hand count is reached |

        | Command       | Aliases | Description |
        |---------------|---------|-------------|
        | `reset`       | `r`     | Start / restart a game |
        | `bid`         | `b`     | Bid ladder step `level` with optional `suit` |
        | `pass`        | `ps`    | Pass |
        | `callpartner` | `cp`    | Call `partner` (-1 to play alone) |
        | `play`        | `p`     | Play `cardIndex` |
        | `next`        | `n`     | Deal the next hand |
        | `log`         | `l`     | Action-log transcript |
      operationId: bostonExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bid, b, pass, ps, callpartner, cp, play, p, next, n, log, l]
                sessionId:
                  type: string
                level:
                  type: integer
                  description: |
                    Ladder step, 1-15. Required for `bid`. **Not a number of
                    tricks** -- the miseres occupy steps of their own.
                suit:
                  type: integer
                  description: "Trump suit for a trick bid: 1=Spade, 2=Clover, 3=Heart, 4=Diamond. Ignored by no-trump bids."
                partner:
                  type: integer
                  description: Seat to partner, or -1 to play alone. Required for `callpartner`.
                cardIndex:
                  type: integer
                  description: Hand index. Required for `play`.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
                    targetHands:
                      type: integer
                      description: Hands played before the game is decided (1-30, default 8).
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BostonResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BostonResponse'

  /vint/exec:
    post:
      tags:
        - vint
      summary: Execute a Vint game command
      description: |
        Send a command to advance the Vint session identified by `sessionId`.
        Vint is the Russian ancestor of contract bridge: 4 players in two
        partnerships on a 52-card pack, thirteen cards each, and **no dummy** —
        every hand stays concealed to the end.

        **#4385 gets the scoring fundamentally wrong and omits four rules.**

        * The issue says to total "**the declaring side's tricks** and the
          overtricks above seven". **Both teams score below the line for every
          trick they take, irrespective of whether the contract was made.** The
          defenders' tricks count too, and nothing is measured "above seven".
        * The issue gives no trick values. They depend on **both the
          denomination and the level**: spades 4, clubs 6, diamonds 8, hearts
          10, no trump 12 at level 1, **plus 10 for each level above**.
        * The issue gives no bidding order. It is **spades < clubs < diamonds <
          hearts < no trump** — the reverse of bridge, with **spades LOWEST**.
        * The issue says honours score "according to how many you hold". They
          score from **three or more**: 20x, 30x or 40x the trick value. Two or
          fewer score nothing, and a no-trump contract has no trump honours at
          all.
        * The issue omits the **aces** entirely. They are scored separately
          from honours: the side holding the majority takes **10x the trick
          value per ace**, and on a 2-2 split the side with more tricks takes
          all four.
        * The issue names no target. A game is **500 below the line**; the
          first game is worth 500 above it and the rubber 1000.
        * The issue omits the penalty: **undertricks x level x 500**.

        A bid of level N contracts for **6 + N tricks**. Following suit is
        compulsory, so the server sends `validPlays`.

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Bid     | `0`   | Bid a level and denomination, or pass |
        | Play    | `1`   | Thirteen tricks, no dummy |
        | HandEnd | `2`   | The hand is settled; `next` deals again |
        | GameEnd | `3`   | The rubber is over |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `bid`   | `b`     | Bid `level` of `denom` |
        | `pass`  | `ps`    | Pass |
        | `play`  | `p`     | Play `cardIndex` |
        | `next`  | `n`     | Deal the next hand |
        | `log`   | `l`     | Action-log transcript |
      operationId: vintExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bid, b, pass, ps, play, p, next, n, log, l]
                sessionId:
                  type: string
                level:
                  type: integer
                  description: Bid level 1-7, contracting for 6 + level tricks. Required for `bid`.
                denom:
                  type: integer
                  description: "0=Spade, 1=Club, 2=Diamond, 3=Heart, 4=NoTrump -- spades are LOWEST. Required for `bid`."
                cardIndex:
                  type: integer
                  description: Hand index. Required for `play`.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VintResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/VintResponse'

  /bideuchre/exec:
    post:
      tags:
        - bideuchre
      summary: Execute a Bid Euchre game command
      description: |
        Send a command to advance the Bid Euchre session identified by
        `sessionId`. Bid Euchre is a 24-card partnership euchre variant
        (A K Q J 10 9 in each suit) for 4 players in two partnerships.

        **#4386 is wrong on the deal and omits four rules.**

        * The issue says the remainder forms a kitty. **24 / 4 = 6 exactly, so
          the whole pack is dealt out and there is NO kitty** and no turn-up.
        * The issue gives no minimum bid. It is **three tricks**.
        * The issue says each bid must beat the last. It must — **except the
          DEALER, who may take the contract by equalling the standing bid**.
        * The issue names only suit trumps. There are **two no-trump forms**:
          no trump high (the usual ranking) and **no trump LOW, where the
          ranking reverses and the nine is highest**.
        * The issue names no target. The first side to **32 points** wins.

        Within a suit contract the trump jack (right bower) is highest, then the
        same-colour jack (left bower), which **counts as a trump for every
        purpose including following suit**. Following suit is compulsory, so the
        server sends `validPlays`.

        Scoring: a made contract scores **each side its own tricks**. A set
        costs the declaring side **its BID, not the tricks it took**, while the
        defenders still score theirs.

        | Phase       | Value | Description |
        |-------------|-------|-------------|
        | Bid         | `0`   | Bid a trick count, or pass |
        | ChooseTrump | `1`   | The declarer names a suit or a no-trump form |
        | Play        | `2`   | Six tricks |
        | HandEnd     | `3`   | The hand is settled; `next` deals again |
        | GameEnd     | `4`   | Someone reached 32 |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `bid`   | `b`     | Bid `value` tricks (3-6) |
        | `pass`  | `ps`    | Pass |
        | `trump` | `t`     | Name `trump` (0=S 1=C 2=D 3=H 4=NT high 5=NT low) |
        | `play`  | `p`     | Play `cardIndex` |
        | `next`  | `n`     | Deal the next hand |
        | `log`   | `l`     | Action-log transcript |
      operationId: bidEuchreExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bid, b, pass, ps, trump, t, play, p, next, n, log, l]
                sessionId:
                  type: string
                value:
                  type: integer
                  description: "Bid in tricks, 3-6. Three is the floor. Required for `bid`."
                trump:
                  type: integer
                  description: "0=Spade, 1=Club, 2=Diamond, 3=Heart, 4=NoTrump high, 5=NoTrump LOW (nine highest). Required for `trump`."
                cardIndex:
                  type: integer
                  description: Hand index. Required for `play`.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
                    allowNoTrump:
                      type: boolean
                      description: Whether the declarer may name a no-trump form.
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BidEuchreResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BidEuchreResponse'

  /sixbidsolo/exec:
    post:
      tags:
        - sixbidsolo
      summary: Execute a Six-Bid Solo game command
      description: |
        Send a command to advance the Six-Bid Solo session identified by
        `sessionId`. Six-Bid Solo is a 3-player American descendant of skat
        played with a 36-card pack (A-10-K-Q-J-9-8-7-6, so the **ten outranks
        the king**).

        **#4377 gets the deal wrong twice and the misere condition wrong.**

        * The issue says "12 cards each, no skat". It is **eleven each plus a
          three-card widow**, dealt 4 - 3 - widow 3 - 4. The arithmetic settles
          it: **11 x 3 + 3 = 36**, and 12 x 3 leaves no room for the widow.
        * The widow is not decoration: it is **credited to the declarer at the
          end**, except at either misere.
        * The issue says a plain bid wins at "60 points". The target is **more
          than 60, so 61**, and the settlement is the **difference from 60**
          times a multiplier rather than a flat win or loss.
        * The issue says a misere wins by taking **no tricks**. It wins by
          taking **no card POINTS** -- a trick of nines and lower is harmless.
        * The issue gives no card values (A 11, 10 10, K 4, Q 3, J 2; **120 in
          total**), no guarantee targets (**74 at hearts, 80 elsewhere**), no
          bid values, and omits the **call solo card exchange** entirely.

        The six bids ascend: Solo (difference x2), Heart Solo (difference x3),
        Misere (30), Guarantee Solo (40), Spread Misere (60), Call Solo (100, or
        150 at hearts). Only a **higher** bid stands; there is no equalling
        privilege. Following suit is compulsory, so the server sends
        `validPlays`.

        A call solo also **names a card, and whoever holds it must exchange it**
        for one of the declarer's -- unless it is in the widow, in which case no
        exchange happens.

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Bid     | `0`   | Bid one of six levels, or pass |
        | Declare | `1`   | The declarer names a suit (and a card at a call solo) |
        | Play    | `2`   | Eleven tricks |
        | HandEnd | `3`   | The hand is settled; `next` deals again |
        | GameEnd | `4`   | The configured number of hands is done |

        | Command   | Aliases | Description |
        |-----------|---------|-------------|
        | `reset`   | `r`     | Start / restart a game |
        | `bid`     | `b`     | Bid `bid` (1-6) |
        | `pass`    | `ps`    | Pass |
        | `declare` | `d`     | Name `suit`, plus `calledSuit`/`calledValue` at a call solo |
        | `play`    | `p`     | Play `cardIndex` |
        | `next`    | `n`     | Deal the next hand |
        | `log`     | `l`     | Action-log transcript |

        Note: pagat and Wikipedia disagree on the misere value (30 vs 40).
        Wikipedia also puts guarantee at 40, which would tie the two and break
        the ladder, so this implementation uses pagat's **30**.
      operationId: sixBidSoloExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bid, b, pass, ps, declare, d, play, p, next, n, log, l]
                sessionId:
                  type: string
                bid:
                  type: integer
                  description: "1=Solo, 2=Heart Solo, 3=Misere, 4=Guarantee Solo, 5=Spread Misere, 6=Call Solo. Required for `bid`."
                suit:
                  type: integer
                  description: "1=Spade, 2=Clover, 3=Heart, 4=Diamond. Required for `declare`."
                calledSuit:
                  type: integer
                  description: Suit of the card a call solo names. Must be given together with `calledValue`.
                calledValue:
                  type: integer
                  description: Rank of the card a call solo names. Must be given together with `calledSuit`.
                cardIndex:
                  type: integer
                  description: Hand index. Required for `play`.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
                    targetHands:
                      type: integer
                      description: Hands in a game (3-12).
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SixBidSoloResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SixBidSoloResponse'

  /karnoffel/exec:
    post:
      tags:
        - karnoffel
      summary: Execute a Karnöffel game command
      description: |
        Send a command to advance the Karnöffel session identified by
        `sessionId`. Karnöffel is the oldest card game known by name, attested
        in 1426: four players in two partnerships on a **48-card pack with the
        ACES removed**.

        **#4376 is wrong on the deal, the top card and the deck, and it
        contradicts itself.**

        * The issue says twelve cards each with the last one turned up. **48 / 4
          = 12 consumes the pack**, leaving nothing to turn up. It is **five
          each**, dealt with the FIRST card face up in front of each player, and
          **the lowest of those four face-up cards picks the chosen suit**. The
          issue's own "3 of 5 tricks wins" agrees with five, not twelve.
        * The issue says the **Karnöffel is the six**. It is the **JACK**; the
          six is the Pope.
        * The issue reduces the devil to "Devil/Pope etc. apply". The **seven is
          the devil and beats everything but the Karnöffel ONLY WHEN LED** —
          played in follow it loses to every card, and it **cannot lead the
          first trick** at all.
        * The issue omits the **partial trumps**: the **3 loses to kings**, the
          **4 to kings and queens**, and the **5 to every face card**, so "the
          chosen suit nullifies plain cards" is false for three of its own
          cards.
        * The issue says the pack removes the **eights**. It removes the
          **aces** — the plain ranking (K > Q > J > 10 > 9 > 8 > 7 > 6 > 5 > 4 >
          3 > 2) presumes there are none.

        The issue is right that there are four players in two partnerships,
        that **following suit is not required**, and that three of five tricks
        takes the hand.

        Full ranking in the chosen suit: **J > 7 (led only) > 6 > 2 > 3 > 4 > 5
        > K > Q > 10 > 9 > 8**.

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Play    | `0`   | Up to five tricks; three takes the hand |
        | HandEnd | `1`   | The hand is over; `next` deals again |
        | GameEnd | `2`   | A team reached the configured number of hands |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `play`  | `p`     | Play `cardIndex` |
        | `next`  | `n`     | Deal the next hand |
        | `log`   | `l`     | Action-log transcript |
      operationId: karnoffelExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, next, n, log, l]
                sessionId:
                  type: string
                cardIndex:
                  type: integer
                  description: Hand index (0-4). Required for `play`.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
                    targetHands:
                      type: integer
                      description: Hands needed to win the game (1-10).
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KarnoffelResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KarnoffelResponse'

  /literature/exec:
    post:
      tags:
        - literature
      summary: Execute a Literature game command
      description: |
        Send a command to advance the Literature session identified by
        `sessionId`. Literature is a deduction fishing game for **six players in
        two teams of three, seated alternately**, on a 48-card pack with the
        **eights removed**. It contests **eight half-suits** of six cards each:
        low (2-7) and high (9-A) in every suit.

        **#4401 is wrong about how the game is won and about a failed claim.**

        * The issue says the first team to **four half-suits (half)** wins.
          **Four decides nothing** — the other side can reach four too. A
          majority of eight is **FIVE**. It is also not a race: a claim can be
          **cancelled**, so the two totals need not add to eight, and when
          nobody reaches five the side with more takes it.
        * The issue says a wrong claim hands the half-suit to the opponents.
          There are **three** outcomes. If your team holds all six but you
          **name the wrong teammate**, the half-suit is **CANCELLED** — it goes
          to nobody. It goes to the opponents only if they actually hold one of
          the six. That distinction is the reason to hesitate before claiming.
        * The issue states only that you must hold the half-suit. You must also
          ask **an opponent, never a teammate**, ask for a card you do **not**
          hold, and the target must still have cards.
        * The issue omits the **alternating seating** (which is what makes
          "opponents only" meaningful), the rule that a player who runs out
          mid-turn **passes to a teammate**, and that a claim may also be made
          by laying all six down yourself.

        The issue is right about the deck, the half-suit shape, the eight cards
        each, and that a hit keeps the turn while a miss passes it.

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Play    | `0`   | Asking and claiming |
        | GameEnd | `1`   | A team reached five, or every half-suit is settled |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `ask`   | `a`     | Ask `target` for `suit`/`value` |
        | `claim` | `c`     | Claim `halfSuit`, placing all six in `holders` |
        | `log`   | `l`     | Action-log transcript |
      operationId: literatureExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, ask, a, claim, c, log, l]
                sessionId:
                  type: string
                target:
                  type: integer
                  description: "Seat to ask (0-5). MUST BE AN OPPONENT. Required for `ask`."
                suit:
                  type: integer
                  description: "1=Spade, 2=Clover, 3=Heart, 4=Diamond. Required for `ask`."
                value:
                  type: integer
                  description: Rank (1-13). Required for `ask`.
                halfSuit:
                  type: integer
                  description: Half-suit index (0-7). Required for `claim`.
                holders:
                  type: array
                  items:
                    type: integer
                  description: |
                    Where each of the six cards is, in `halfSuitCards` order.
                    ALL SIX are required -- naming the wrong teammate cancels
                    the half-suit rather than handing it over.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LiteratureResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LiteratureResponse'

  /guandan/exec:
    post:
      tags:
        - guandan
      summary: Execute a Guandan game command
      description: |
        Send a command to advance the Guandan session identified by `sessionId`.
        Guandan is a Chinese two-pack climbing game for **four players in two
        partnerships sitting opposite**, 27 cards each from **108** (52x2 + 4
        jokers).

        **#4375 omits the rule the whole game turns on, and gets the climb wrong.**

        * Every hand is played at a **LEVEL**. Cards of that rank are not left
          where they normally sit: they go **ABOVE THE ACE**, below the black
          joker. The issue does not mention this at all.
        * The wild is **not** "the level cards" -- it is the **two HEARTS**
          among them, and they can stand in for anything except a joker.
        * The climb is **1, 2 or 4** levels, not "1-3". Going out first and
          second is worth **FOUR** and is the point of partnership play; **no
          result is ever worth three**.
        * The **tribute** system is missing from the issue entirely. Between
          hands the last-place seat pays its highest card (**wilds excluded**)
          to the winner and gets one back; after a first-and-second hand, third
          pays second and fourth pays first. A payer holding **both red jokers
          cancels tribute outright**.

        The issue is right about the two packs, the four players in two
        partnerships, the 27-card hands, and that climbing past the ace wins.

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Tribute | `0`   | Paying and returning tribute |
        | Play    | `1`   | Climbing |
        | HandEnd | `2`   | The hand is settled; deal the next |
        | GameEnd | `3`   | A team climbed past the ace |

        | Command   | Aliases | Description |
        |-----------|---------|-------------|
        | `reset`   | `r`     | Start / restart a game |
        | `play`    | `p`     | Play `cardIndexes` as one combination |
        | `pass`    | `ps`    | Pass (not allowed while the table is clear) |
        | `tribute` | `t`     | Hand `cardIndex` back as the return tribute |
        | `next`    | `n`     | Deal the next hand |
        | `log`     | `l`     | Action-log transcript |
      operationId: guandanExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, pass, ps, tribute, t, next, n, log, l]
                sessionId:
                  type: string
                cardIndexes:
                  type: array
                  items:
                    type: integer
                  description: |
                    Hand indexes forming ONE combination -- a turn is a
                    combination, not a card. Required for `play`.
                cardIndex:
                  type: integer
                  description: The single card handed back. Required for `tribute`.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GuandanResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GuandanResponse'

  /shengji/exec:
    post:
      tags:
        - shengji
      summary: Execute a Sheng Ji game command
      description: |
        Send a command to advance the Sheng Ji session identified by
        `sessionId`. Sheng Ji (升级 / 拖拉机) is a Chinese two-pack point-trick
        game for **four players in two partnerships sitting opposite**, dealt
        **25 cards each** from 108 with an **eight-card kitty**.

        **#4384 loses the kitty, inverts who scores, and omits the trump group.**

        * The issue deals all 108 cards to the four players. 108 divides by four
          exactly, so that leaves **no kitty**. The real deal is 25 each plus an
          **8-card kitty**, and the kitty matters: the defenders capture it
          **with a multiplier** (twice the winning play's size) by taking the
          last trick.
        * The issue has a **bid** decide the trump suit and the declarer. There
          is no auction. Trump is **shown** — a player exposes a card of the
          level rank — and only a **stronger showing** (a pair over a single)
          overrides. If nobody shows, the hand is played with **no trump suit**.
          The declaring side is whoever's level is being played.
        * **Trumps are not just the trump suit.** Every card of the level rank,
          **in all four suits**, plus all four jokers, is a trump: red joker >
          black joker > trump-suit level card > off-suit level cards (equal
          among themselves) > plain trumps. The issue omits this entirely,
          though it is the whole point of the level mechanic.
        * The issue reads as though the **declarers** collect points. The
          **defenders** do. Points are 5 = 5, 10 = 10, K = 10, **200 in the
          pack**, and the declarers win by holding the defenders under **80**.
        * The climb is unstated: 3 levels at zero, 2 under 40, 1 under 80; at 80
          the deal changes hands with **no** climb, and every further 40 is one
          more level. The climb **stops at the ace**, which must then be held.

        The issue is right about the two packs, the four players in two
        partnerships, the 5/10/K point cards, the tractor being consecutive
        same-suit pairs, and climbing from 2 to the ace.

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Declare | `0`   | Showing a level card to name trump |
        | Kitty   | `1`   | The declarer buries eight cards |
        | Play    | `2`   | Tricks |
        | HandEnd | `3`   | The hand is settled; deal the next |
        | GameEnd | `4`   | A team held the deal at the ace |

        | Command   | Aliases | Description |
        |-----------|---------|-------------|
        | `reset`   | `r`     | Start / restart a game |
        | `declare` | `d`     | Declare `suit`; **0 passes** |
        | `bury`    | `b`     | Bury eight `cardIndexes` into the kitty |
        | `play`    | `p`     | Play `cardIndexes` as one combination |
        | `next`    | `n`     | Deal the next hand |
        | `log`     | `l`     | Action-log transcript |
      operationId: shengjiExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, declare, d, bury, b, play, p, next, n, log, l]
                sessionId:
                  type: string
                suit:
                  type: integer
                  description: |
                    1=Spade, 2=Clover, 3=Heart, 4=Diamond, and **0 PASSES**.
                    Required for `declare` -- zero is a real choice here, so it
                    cannot be omitted to mean the same thing.
                cardIndexes:
                  type: array
                  items:
                    type: integer
                  description: |
                    The eight cards to bury, or the cards to play as ONE
                    combination. Required for `bury` and `play`.
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      description: "0=Normal (the only level in v1)"
      responses:
        '200':
          description: Current game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ShengJiResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ShengJiResponse'

  /shithead/exec:
    post:
      tags:
        - shithead
      summary: Execute a Shithead / Karma game command
      description: |
        Send a command to advance the Shithead (Karma) session identified
        by `sessionId`. Shithead is a 4-player shedding game with three
        card layers per player — face-down (3), face-up (3), and the hand
        — and configurable magic cards (2 / 7 / 8 / 10 plus four-of-a-kind
        burn). The last player holding cards is the "shithead".

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game (optional `config`) |
        | `play`  | `p`     | Play one or more cards (`indices`); empty array = pickup |
        | `log`   | `l`     | View action log |
      operationId: shitheadExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, log, l]
                indices:
                  type: array
                  items:
                    type: integer
                  description: play command — indices of cards to play (empty array picks up the discard pile)
                config:
                  type: object
                  properties:
                    magicTwo:
                      type: boolean
                      description: 2 resets the pile rank
                    magicSeven:
                      type: boolean
                      description: 7 forces next play to be 7 or lower
                    magicEight:
                      type: boolean
                      description: 8 skips the next player
                    magicTen:
                      type: boolean
                      description: 10 burns the discard pile
                    fourOfAKindBurn:
                      type: boolean
                      description: Four cards of the same rank burn the pile
                    cpuDifficulty:
                      type: integer
                      description: 0=easy, 1=normal, 2=hard
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play card indices 0 and 1 from hand
                value:
                  command: play
                  indices: [0, 1]
                  sessionId: my-session-001
              pickup:
                summary: Pick up the discard pile
                value:
                  command: play
                  indices: []
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  currentTurn:
                    type: integer
                  currentSource:
                    type: string
                    description: "hand / faceUp / faceDown"
                  stockSize:
                    type: integer
                  skipNext:
                    type: boolean
                  sevenActive:
                    type: boolean
                  gameEndFlag:
                    type: boolean
                  discardPile:
                    type: array
                    items:
                      $ref: '#/components/schemas/Card'
                  players:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: integer
                        isHuman:
                          type: boolean
                        isFinished:
                          type: boolean
                        rank:
                          type: integer
                        handCount:
                          type: integer
                        handCards:
                          type: array
                          items:
                            $ref: '#/components/schemas/Card'
                        faceUpCards:
                          type: array
                          items:
                            $ref: '#/components/schemas/Card'
                        faceDownCount:
                          type: integer
                  config:
                    type: object
                    properties:
                      magicTwo:
                        type: boolean
                      magicSeven:
                        type: boolean
                      magicEight:
                        type: boolean
                      magicTen:
                        type: boolean
                      fourOfAKindBurn:
                        type: boolean
                      cpuDifficulty:
                        type: integer
                  cpuActions:
                    type: array
                    items:
                      type: object
                      properties:
                        playerIdx:
                          type: integer
                        source:
                          type: string
                        playedCards:
                          type: array
                          items:
                            $ref: '#/components/schemas/Card'
                        pickup:
                          type: boolean
                        burned:
                          type: boolean
                        skipped:
                          type: boolean
                  humanAction:
                    nullable: true
                    type: object
                  message:
                    type: string
                  messageCode:
                    type: string
                  messageParams:
                    type: object
                    additionalProperties:
                      type: string
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)

  /nertz/exec:
    post:
      tags:
        - nertz
      summary: Execute a Nertz / Pounce game command
      description: |
        Send a command to advance the Nertz (Pounce) session identified by
        `sessionId`. Nertz is a real-time competitive solitaire (2–6
        players) racing to empty a 13-card "Nertz pile" onto shared
        Klondike-style foundations. Each player has a private tableau
        (4 columns), waste, and stock; the foundations are shared.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Idle      | `0`   | Pre-reset |
        | Playing   | `1`   | Round in progress |
        | RoundEnd  | `2`   | Someone emptied their Nertz pile |
        | GameEnd   | `3`   | Match target score reached |

        | Command     | Aliases     | Description |
        |-------------|-------------|-------------|
        | `reset`     | `r`         | Start / restart a match (optional `config`) |
        | `nextround` | `nr`        | Start next round after RoundEnd |
        | `tick`      |             | Advance pending CPU actions one step |
        | `draw`      | `d`         | Draw `drawCount` cards from stock to waste (`playerIdx`) |
        | `move`      | `m`         | Move a card between zones (see body) |
        | `undo`      | `u`         | Undo the previous move (human only) |
        | `hint`      |             | Get a hint for the current human player |
        | `log`       | `l`         | View action log |

        Move zones — supported `from.zone` → `to.zone` pairs:

        | From      | To         | Required fields |
        |-----------|------------|-----------------|
        | nertz     | foundation | `to.idx` |
        | nertz     | tableau    | `to.col` |
        | waste     | foundation | `to.idx` |
        | waste     | tableau    | `to.col` |
        | tableau   | foundation | `from.col`, `to.idx` |
        | tableau   | tableau    | `from.col`, `from.cardIndex`, `to.col` |
      operationId: nertzExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, nextround, nr, tick, draw, d, move, m, undo, u, hint, log, l]
                playerIdx:
                  type: integer
                  description: 0 = human; CPUs are 1..N-1
                from:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [nertz, waste, tableau]
                    col:
                      type: integer
                    cardIndex:
                      type: integer
                to:
                  type: object
                  properties:
                    zone:
                      type: string
                      enum: [foundation, tableau]
                    col:
                      type: integer
                    idx:
                      type: integer
                config:
                  type: object
                  properties:
                    playerCount:
                      type: integer
                      description: 2..6 players
                    drawCount:
                      type: integer
                      description: Cards drawn from stock per draw (default 3)
                    targetScore:
                      type: integer
                    cpuDifficulty:
                      type: integer
                      description: 0=easy, 1=normal, 2=hard
                    cpuTickMoves:
                      type: integer
                      description: CPU actions resolved per tick
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new match
                value:
                  command: reset
                  sessionId: my-session-001
              draw:
                summary: Draw from stock to waste
                value:
                  command: draw
                  playerIdx: 0
                  sessionId: my-session-001
              moveNertzToFoundation:
                summary: Send Nertz top card to foundation 0
                value:
                  command: move
                  playerIdx: 0
                  from:
                    zone: nertz
                  to:
                    zone: foundation
                    idx: 0
                  sessionId: my-session-001
              moveTableauToTableau:
                summary: Move card stack within tableau
                value:
                  command: move
                  playerIdx: 0
                  from:
                    zone: tableau
                    col: 1
                    cardIndex: 0
                  to:
                    zone: tableau
                    col: 3
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  phase:
                    type: integer
                  roundNumber:
                    type: integer
                  winnerIdx:
                    type: integer
                  matchWinner:
                    type: integer
                  moveCount:
                    type: integer
                  canUndo:
                    type: boolean
                  playerCount:
                    type: integer
                  drawCount:
                    type: integer
                  targetScore:
                    type: integer
                  cpuDifficulty:
                    type: integer
                  cpuTickMoves:
                    type: integer
                  players:
                    type: array
                    items:
                      type: object
                      properties:
                        name:
                          type: string
                        isHuman:
                          type: boolean
                        deckIdx:
                          type: integer
                        score:
                          type: integer
                        nertzSize:
                          type: integer
                        nertzTop:
                          nullable: true
                          allOf:
                            - $ref: '#/components/schemas/Card'
                        tableau:
                          type: array
                          items:
                            type: array
                            items:
                              type: object
                              properties:
                                card:
                                  $ref: '#/components/schemas/Card'
                                faceUp:
                                  type: boolean
                        wasteTop:
                          nullable: true
                          allOf:
                            - $ref: '#/components/schemas/Card'
                        wasteSize:
                          type: integer
                        stockSize:
                          type: integer
                  foundations:
                    type: array
                    items:
                      type: object
                      properties:
                        top:
                          nullable: true
                          allOf:
                            - $ref: '#/components/schemas/Card'
                        suit:
                          type: integer
                        size:
                          type: integer
                  hint:
                    nullable: true
                    type: object
                    properties:
                      fromZone:
                        type: string
                      fromCol:
                        type: integer
                      cardIndex:
                        type: integer
                      toZone:
                        type: string
                      toCol:
                        type: integer
                  message:
                    type: string
                  messageCode:
                    type: string
                  messageParams:
                    type: object
                    additionalProperties:
                      type: string
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)

  /tonk/exec:
    post:
      tags:
        - tonk
      summary: Execute a Tonk game command
      description: |
        Send a command to advance the Tonk (トンク) game session identified by
        `sessionId`. Tonk is a 2-player rummy variant using a standard 52-card
        deck. Each player is dealt 5 cards. If a player's initial 5 cards total
        49 or 50 points, the player wins immediately ("Tonk on deal") and earns
        a 50-point bonus. Otherwise standard draw/discard with a knock threshold
        of 5 deadwood. On knock, deadwood difference is awarded; if the opponent
        has lower deadwood than the knocker, opponent gets the difference + a
        5-point undercut penalty. First to reach the point limit wins.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Draw      | `0`   | Draw a card from stock or discard pile |
        | Discard   | `1`   | Discard a card or knock |
        | RoundEnd  | `2`   | Round scored; advance with `nextround` |
        | GameEnd   | `3`   | A player reached the point limit |

        | Command       | Aliases | Description |
        |---------------|---------|-------------|
        | `reset`       | `r`     | Start / restart a game |
        | `drawstock`   | `ds`    | Draw a card from the stock pile |
        | `drawdiscard` | `dd`    | Draw the top card from the discard pile |
        | `discard`     | `d`     | Discard a card (requires `cardIndex`) |
        | `knock`       | `k`     | Knock (deadwood ≤ 5) (requires `cardIndex`) |
        | `nextround`   | `nr`    | Score the round and start the next |
        | `log`         | `l`     | View action log |
      operationId: tonkExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TonkRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              knock:
                summary: Knock and discard card at index 2
                value:
                  command: knock
                  cardIndex: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TonkResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TonkResponse'

  /thirtyone/exec:
    post:
      tags:
        - thirtyone
      summary: Execute a Thirty-One game command
      description: |
        Send a command to advance the Thirty-One (サーティワン / Scat) game session
        identified by `sessionId`. Thirty-One is a 4-player (1 human + 3 CPU)
        draw-and-discard survival game using a standard 52-card deck. Each player
        is dealt 3 cards and tries to maximise the sum of a single suit (A=11,
        J/Q/K=10, 2-10 face value), aiming for exactly 31. On your turn, draw from
        the stock or the discard pile and discard one card, or knock to stand pat.
        After a knock every other active player takes one more turn, then the
        lowest best-suit score loses a life (ties: all tied lose). Reaching exactly
        31 wins the round instantly and every other active player loses a life.
        Each player starts with a configurable number of lives (default 3) and is
        eliminated when lives drop below 0; the game ends when one player remains
        or the human is eliminated.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Draw      | `0`   | Draw from stock or discard, or knock |
        | Discard   | `1`   | Discard a card |
        | RoundEnd  | `2`   | Round scored; advance with `nextround` |
        | GameEnd   | `3`   | One player remains (or human eliminated) |

        | Command       | Aliases | Description |
        |---------------|---------|-------------|
        | `reset`       | `r`     | Start / restart a game |
        | `drawstock`   | `ds`    | Draw a card from the stock pile |
        | `drawdiscard` | `dd`    | Draw the top card from the discard pile |
        | `discard`     | `d`     | Discard a card (requires `cardIndex`) |
        | `knock`       | `k`     | Knock (stand pat) |
        | `nextround`   | `nr`    | Score the round and start the next |
        | `log`         | `l`     | View action log |
      operationId: thirtyoneExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ThirtyOneRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              knock:
                summary: Knock to stand pat
                value:
                  command: knock
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThirtyOneResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThirtyOneResponse'

  /yaniv/exec:
    post:
      tags:
        - yaniv
      summary: Execute a Yaniv game command
      description: |
        Send a command to advance the Yaniv (ヤニブ) game session identified by
        `sessionId`. Yaniv is a 4-player (1 human + 3 CPU) Israeli draw-and-discard
        hand-reduction game on a 54-card deck (2 jokers; Joker=0, A=1, J/Q/K=10,
        others face value). Each player is dealt 5 cards. On your turn you first
        discard a single card, a same-value set (2+), or a same-suit run (3+
        consecutive), then draw one card from the stock or from either end of the
        previous player's discard. When your hand total is 5 or less you may instead
        declare Yaniv to end the round. On reveal, if the caller is strictly lowest
        they score 0; otherwise it is Asaf and the caller takes a 30-point penalty
        while the lowest opponent scores 0. A player is eliminated when their
        cumulative score exceeds the limit (default 200).

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Discard   | `0`   | Discard a combo, or declare Yaniv |
        | Draw      | `1`   | Draw from stock or a discard end |
        | RoundEnd  | `2`   | Round scored; advance with `nextround` |
        | GameEnd   | `3`   | One player remains (or human eliminated) |

        | Command      | Aliases | Description |
        |--------------|---------|-------------|
        | `reset`      | `r`     | Start / restart a game |
        | `discard`    | `d`     | Discard a combo (requires `cardIndices`) |
        | `yaniv`      | `y`     | Declare Yaniv (hand total <= 5) |
        | `drawstock`  | `ds`    | Draw a card from the stock pile |
        | `drawpickup` | `dp`    | Take an end of the previous discard (requires `end`) |
        | `nextround`  | `nr`    | Score the round and start the next |
        | `log`        | `l`     | View action log |
      operationId: yanivExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/YanivRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              yaniv:
                summary: Declare Yaniv
                value:
                  command: yaniv
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/YanivResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/YanivResponse'

  /tressette/exec:
    post:
      tags:
        - tressette
      summary: Execute a Tressette game command
      description: |
        Send a command to advance the Tressette (トレセッテ) game session identified
        by `sessionId`. Tressette is a 4-player (1 human + 3 CPU) no-trump
        must-follow trick-taking team game (teams 0+2 vs 1+3) on the 40-card
        Briscola deck (8/9/10 removed), dealt 10 cards each. There is no trump:
        trick strength runs 3 > 2 > A > K > Q > J > 7 > 6 > 5 > 4 and the highest
        card of the led suit wins. Scoring is in thirds of a point: A = 3 thirds
        (1 point); 2/3/K/Q/J = 1 third each; 4-7 = 0; the last-trick winner gets
        +1 third (round total 33 thirds = 11 points). At round end each team's
        thirds are divided by 3 (remainder dropped) and added to its score; the
        first team to the target (default 21) while leading wins.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Play      | `0`   | Play a card (must follow the led suit) |
        | TrickEnd  | `1`   | Trick resolved; advance with `next` |
        | RoundEnd  | `2`   | Round scored; advance with `nextround` |
        | GameEnd   | `3`   | A team reached the target score |

        | Command      | Aliases | Description |
        |--------------|---------|-------------|
        | `reset`      | `r`     | Start / restart a game |
        | `play`       | `p`     | Play a card (requires `cardIndex`) |
        | `next`       | `n`     | Advance to the next trick |
        | `nextround`  | `nr`    | Score the round and start the next |
        | `hint`       | `h`     | Get a recommended play |
        | `log`        | `l`     | View action log |
      operationId: tressetteExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TressetteRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 0
                value:
                  command: play
                  cardIndex: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TressetteResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TressetteResponse'

  /gongzhu/exec:
    post:
      tags:
        - gongzhu
      summary: Execute a Gong Zhu game command
      description: |
        Send a command to advance the Gong Zhu (拱猪 / Chinese Hearts) game session
        identified by `sessionId`. Gong Zhu is a 4-player (1 human + 3 CPU)
        trick-taking game on a 52-card deck dealt 13 each. Trick play follows Hearts
        (follow suit; hearts cannot be led until broken; the ♣2 holder leads first;
        the highest led-suit card wins, Ace high) but with positive and negative
        point cards: ♠Q (pig) = -100, ♦J (sheep) = +100, ♣10 (transformer) doubles
        the holder's whole round total (or +50 if taken alone), and hearts A=-50,
        K=-40, Q=-30, J=-20, 10–5=-10 each, 4–2=0 (total -200). Collecting all 13
        hearts flips them to +200. Before each round a player may expose ♠Q/♦J/♥A/♣10
        to double that card's stakes for whoever captures it. The game ends when any
        player's cumulative score reaches -PointLimit (default 1000); the highest
        score wins.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Expose    | `0`   | Optionally expose point cards |
        | Play      | `1`   | Play a card to the trick |
        | TrickEnd  | `2`   | Trick won; advance with `next` |
        | RoundEnd  | `3`   | Round scored; advance with `nextround` |
        | GameEnd   | `4`   | A player reached the point limit |

        | Command     | Aliases | Description |
        |-------------|---------|-------------|
        | `reset`     | `r`     | Start / restart a game |
        | `expose`    |         | Expose point cards (`cardIndices`; empty = none) |
        | `play`      | `p`     | Play a card (requires `cardIndex`) |
        | `next`      | `n`     | Resolve the trick and start the next |
        | `nextround` | `nr`    | Score the round and start the next |
        | `hint`      | `h`     | Get a suggested move |
        | `log`       | `l`     | View action log |
      operationId: gongzhuExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GongZhuRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              expose:
                summary: Expose nothing and start play
                value:
                  command: expose
                  cardIndices: []
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GongZhuResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GongZhuResponse'

  /casinowar/exec:
    post:
      tags:
        - casinowar
      summary: Execute a Casino War game command
      description: |
        Send a command to advance the Casino War (カジノウォー) game session
        identified by `sessionId`. Casino War is a single-player vs dealer
        casino card game using a standard 52-card deck. Player places an ante
        bet, then 1 card is dealt face-up to each side. Higher rank wins
        (Aces are highest, suits irrelevant). On a tie, the player chooses
        Surrender (forfeit half the ante) or War (post a bet equal to the
        ante; three burn cards are drawn face-down, then one more card is
        dealt to each side — player wins or ties → war bet pays 1:1 and ante
        pushes; player loses → both bets are forfeit).

        | Phase         | Value | Description |
        |---------------|-------|-------------|
        | Bet           | `1`   | Place ante bet |
        | InitialDealt  | `2`   | 1 card each dealt (auto-resolved) |
        | TieDecision   | `3`   | Surrender or go to War |
        | WarDealt      | `4`   | War cards dealt (auto) |
        | End           | `5`   | Round complete |

        | Command     | Aliases | Description |
        |-------------|---------|-------------|
        | `reset`     | `r`     | Start / restart a game |
        | `bet`       | `b`     | Place ante (requires `amount`) |
        | `surrender` | —       | Tie: forfeit half ante and end the round |
        | `war`       | —       | Tie: post a war bet equal to ante and re-draw |
        | `log`       | —       | View action log |
      operationId: casinowarExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CasinoWarRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Bet 100 as ante
                value:
                  command: bet
                  amount: 100
                  sessionId: my-session-001
              surrender:
                summary: Surrender on a tie
                value:
                  command: surrender
                  sessionId: my-session-001
              war:
                summary: Go to war on a tie
                value:
                  command: war
                  sessionId: my-session-001
      responses:
        '200':
          description: Updated game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CasinoWarResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)

  /dragontiger/exec:
    post:
      tags:
        - dragontiger
      summary: Execute a Dragon Tiger game command
      description: |
        Send a command to advance the Dragon Tiger (ドラゴンタイガー) game session
        identified by `sessionId`. Dragon Tiger is the simplest of the casino
        lineup: 1 card is dealt to the **Dragon** slot and 1 to the **Tiger**
        slot, and the higher rank wins (Aces are **lowest**, Kings highest).
        Three bet types: Dragon (1:1 if Dragon wins), Tiger (1:1 if Tiger wins),
        Tie (8:1 if equal ranks). On a tie with a Dragon or Tiger bet, half the
        stake is refunded.

        | Phase | Value | Description |
        |-------|-------|-------------|
        | Bet   | `1`   | Place bet (auto-resolves to End) |
        | End   | `2`   | Round complete |

        | Bet type | Value | Pays |
        |----------|-------|------|
        | Dragon   | `0`   | 1:1 on Dragon win, half refund on tie |
        | Tiger    | `1`   | 1:1 on Tiger win, half refund on tie |
        | Tie      | `2`   | 8:1 on tie, otherwise lose stake |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `bet`   | `b`     | Place a bet (requires `amount` and `betType`) |
        | `clear` | —       | Clear Big Road (history) |
        | `log`   | —       | View action log |
      operationId: dragontigerExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DragonTigerRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              betDragon:
                summary: Bet 100 on Dragon
                value:
                  command: bet
                  amount: 100
                  betType: 0
                  sessionId: my-session-001
              betTie:
                summary: Bet 10 on Tie
                value:
                  command: bet
                  amount: 10
                  betType: 2
                  sessionId: my-session-001
              clear:
                summary: Clear the Big Road history
                value:
                  command: clear
                  sessionId: my-session-001
      responses:
        '200':
          description: Updated game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DragonTigerResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)

  /blackjackswitch/exec:
    post:
      tags:
        - blackjackswitch
      summary: Execute a Blackjack Switch game command
      description: |
        Send a command to advance the Blackjack Switch (ブラックジャック・スイッチ)
        game session identified by `sessionId`. The player is dealt two hands
        and may swap the second card between them. In return, naturals pay 1:1
        (not 3:2) and a dealer 22 is treated as a push for any non-natural hand.

        | Phase  | Value | Description |
        |--------|-------|-------------|
        | Bet    | `1`   | Place bet (per hand; total cost is 2x amount) |
        | Switch | `2`   | Choose to switch the second cards between hands |
        | Action | `3`   | Hit / stand / double down on each hand in turn |
        | End    | `4`   | Round complete |

        | Command       | Aliases | Description |
        |---------------|---------|-------------|
        | `reset`       | `r`     | Start / restart a game |
        | `bet`         | `b`     | Place a bet (requires `amount`; total cost = amount * 2) |
        | `switch`      | `sw`    | Swap second cards between hands |
        | `keep`        | `k`     | Keep current hands and proceed to action |
        | `hit`         | `h`     | Hit the current hand |
        | `stand`       | `s`     | Stand the current hand |
        | `doubledown`  | `dd`    | Double down on the current hand (requires 2 cards) |
        | `log`         | —       | View action log |
      operationId: blackjackswitchExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BlackJackSwitchRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Bet 100 per hand (total 200)
                value:
                  command: bet
                  amount: 100
                  sessionId: my-session-001
              switch:
                summary: Swap second cards
                value:
                  command: switch
                  sessionId: my-session-001
              hit:
                summary: Hit the current hand
                value:
                  command: hit
                  sessionId: my-session-001
      responses:
        '200':
          description: Updated game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BlackJackSwitchResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)

  /montecarlo/exec:
    post:
      tags:
        - montecarlo
      summary: Execute a Monte Carlo Solitaire game command
      description: |
        Send a command to advance the Monte Carlo Solitaire (モンテカルロ・ソリティア)
        game session identified by `sessionId`. The first 25 cards of a single
        52-card deck are dealt row-major into a 5x5 grid, leaving 27 in the
        stock. Pick two adjacent (orthogonal or diagonal) cells of the same
        rank to remove them; press `deal` to compress the board top-left and
        refill empty cells from the stock. Win by clearing all 52 cards.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | `0`   | Remove or deal |
        | GameClear | `1`   | All 52 cards removed |
        | GameOver  | `2`   | Stalemate or giveup |

        | Command  | Aliases       | Description |
        |----------|---------------|-------------|
        | `reset`  | `r`           | Start / restart a game |
        | `remove` | `m`, `move`   | Remove an adjacent same-rank pair (requires `fromR`, `fromC`, `toR`, `toC`) |
        | `deal`   | `d`           | Compress the board and refill from the stock |
        | `undo`   | `u`           | Undo the last remove or deal |
        | `giveup` | `g`           | End the game as a loss |
        | `hint`   | `h`           | Return a recommended remove pair, or `deal` when none exists |
        | `log`    | `l`           | View action log |
      operationId: montecarloExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MonteCarloRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              remove:
                summary: Remove the pair at (0,0) and (0,1)
                value:
                  command: remove
                  fromR: 0
                  fromC: 0
                  toR: 0
                  toC: 1
                  sessionId: my-session-001
              deal:
                summary: Compress + refill from stock
                value:
                  command: deal
                  sessionId: my-session-001
              hint:
                summary: Ask the server for a hint
                value:
                  command: hint
                  sessionId: my-session-001
      responses:
        '200':
          description: Updated game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MonteCarloResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)

  /contractrummy/exec:
    post:
      tags:
        - contractrummy
      summary: Execute a Contract Rummy game command
      description: |
        Send a command to advance the Contract Rummy (コントラクトラミー) game
        session identified by `sessionId`. 3 players (1 human + 2 CPU) play
        7 progressive rounds. Each round has a fixed contract — a list of
        slots (set of N or run of N) the player must lay down before being
        able to lay off, lay extra melds, or go out by emptying their hand.
        Lowest cumulative penalty across rounds wins.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Draw      | `0`   | Draw from stock or take the discard top |
        | Play      | `1`   | Optional meld / layoff / extra, then discard |
        | RoundEnd  | `2`   | Round finished, awaiting `nextround` |
        | GameEnd   | `3`   | Game finished after round 7 |

        | Command          | Aliases       | Description |
        |------------------|---------------|-------------|
        | `reset`          | `r`           | Start / restart a game |
        | `drawstock`      | `ds`          | Draw a card from the stock |
        | `drawdiscard`    | `dd`          | Take the top of the discard pile |
        | `meldcontract`   | `mc`          | Lay down the round contract (`indicesPerSlot` required) |
        | `meldextra`      | `me`          | Lay an extra meld after the contract is met (`cardIndices` required) |
        | `layoff`         | `lo`          | Add one card to an existing meld (`targetPlayerIdx`, `meldIdx`, `cardIndex` required) |
        | `discard`        | `d`           | Discard one card to end the turn (`cardIndex` required) |
        | `nextround`      | `nr`          | Advance to the next round |
        | `log`            | `l`           | View action log |
      operationId: contractrummyExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ContractRummyRequest'
      responses:
        '200':
          description: Updated game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContractRummyResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)

  /kalooki/exec:
    post:
      tags:
        - kalooki
      summary: Execute a Kalooki game command
      description: |
        Send a command to advance the Kalooki (カルーキ) game session
        identified by `sessionId`. 2-4 players (1 human + 1-3 CPU) play with a
        106-card 2-deck plus 2 jokers; 13 cards are dealt to each player. On a
        turn a player draws (stock or top-of-discard), optionally melds or lays
        off, then discards one card. A player's first meld(s) must total at
        least the opening threshold (default 51 points). After opening, a
        player may lay off onto any player's melds. Jokers are wild and a meld
        containing a joker scores 1.5x. Going out wins the round; remaining
        deadwood scores as penalty and the lowest cumulative score wins.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Draw      | `0`   | Draw from stock or take the discard top |
        | Meld      | `1`   | Optional meld / layoff, then discard |
        | RoundEnd  | `2`   | Round finished, awaiting `nextround` |
        | GameEnd   | `3`   | Game finished |

        | Command          | Aliases       | Description |
        |------------------|---------------|-------------|
        | `reset`          | `r`           | Start / restart a game |
        | `drawstock`      | `ds`          | Draw a card from the stock |
        | `drawdiscard`    | `dd`          | Take the top of the discard pile |
        | `meld`           | `m`           | Lay down one meld group (`meldGroups` required) |
        | `layoff`         | `lo`          | Add one card to any player's meld (`targetPlayerIdx`, `meldIdx`, `cardIndex` required) |
        | `discard`        | `d`           | Discard one card to end the turn (`cardIndex` required) |
        | `nextround`      | `nr`          | Advance to the next round |
        | `log`            | `log`         | View action log |
      operationId: kalookiExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/KalookiRequest'
      responses:
        '200':
          description: Updated game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KalookiResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)

  /pitch/exec:
    post:
      tags:
        - pitch
      summary: Execute a Pitch (Setback) game command
      description: |
        Send a command to advance the Pitch / Setback (ピッチ / セットバック) game
        session identified by `sessionId`. Pitch is a 4-player single-handed
        Auction Pitch trick-taking game using a standard 52-card deck (24 cards
        actually played). 6 cards are dealt to each player. Bidding: each player
        in turn declares pass (0) or 2/3/4. The highest bidder leads, and the
        first card played sets the trump suit. Each round awards up to 4 points:
        High (highest trump played), Low (lowest trump played), Jack (J of trump
        if dealt) and Game (single highest pip total: A=4, K=3, Q=2, J=1, 10=10).
        Bidder must reach the bid or be set back by the bid amount. First to
        PointLimit (default 7) wins.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Bid       | `0`   | Each player declares pass (0) or 2/3/4 |
        | Play      | `1`   | Trick play (must follow lead suit OR play trump) |
        | TrickEnd  | `2`   | Trick resolved; advance with `next` |
        | RoundEnd  | `3`   | All 6 tricks played; advance with `nextround` |
        | GameEnd   | `4`   | A player reached the point limit |

        | Command     | Aliases | Description |
        |-------------|---------|-------------|
        | `reset`     | `r`     | Start / restart a game |
        | `bid`       | `b`     | Place a bid (requires `bid` field, 0=pass, 2-4) |
        | `play`      | `p`     | Play a card (requires `cardIndex`) |
        | `next`      | `n`     | Advance to the next trick |
        | `nextround` | `nr`    | Score the round and start the next |
        | `hint`      | `h`     | Get a suggested action |
        | `log`       | `l`     | View action log |
      operationId: pitchExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PitchRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Bid 3
                value:
                  command: bid
                  bid: 3
                  sessionId: my-session-001
              pass:
                summary: Pass during bidding
                value:
                  command: bid
                  bid: 0
                  sessionId: my-session-001
              play:
                summary: Play card at index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Updated game state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PitchResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)

  /ultimatetexasholdem/exec:
    post:
      tags:
        - ultimatetexasholdem
      summary: Execute an Ultimate Texas Hold'em game command
      description: |
        Send a command to advance the Ultimate Texas Hold'em
        (アルティメット・テキサスホールデム) session identified by `sessionId`.
        Ultimate Texas Hold'em is a single-player casino Texas Hold'em variant
        using a standard 52-card deck. The player posts an Ante and an equal
        Blind (mandatory) plus an optional Trips side bet, then receives 2 hole
        cards. The dealer also receives 2 hole cards.

        There is exactly one place where the player commits to a Play bet:
        pre-flop (3x or 4x Ante), after the flop (2x Ante), or after the river
        (1x Ante). Choosing Check at pre-flop or flop defers the decision; at the
        river the player must either Play 1x or Fold (losing Ante + Blind).

        Dealer qualifies with a **pair or better**. If the dealer does not
        qualify, the Ante pushes (returned). The Play bet always pays 1:1 on
        win, pushes on tie, and loses on loss (regardless of qualifying).
        The Blind pays per its paytable only when the player wins with a
        Straight or better; otherwise it pushes on win / tie or loses on a
        loss. The optional Trips side bet pays on the player's 7-card best
        hand independent of the dealer and the round result.

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Bet      | `1`   | Place Ante + Blind (equal) and optional Trips |
        | PreFlop  | `2`   | Play 3x / 4x or Check |
        | Flop     | `3`   | Play 2x or Check (only when checked pre-flop) |
        | River    | `4`   | Play 1x or Fold (only when checked twice) |
        | End      | `5`   | Showdown complete; result and payouts shown |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `bet`   | `b`     | Place Ante (`amount`) and optional Trips (`tripsBet`); Blind is set automatically |
        | `play`  | `p`     | Place Play bet (`multiplier` ∈ {1, 2, 3, 4}) |
        | `check` | `c`     | Pre-flop / Flop: advance without an additional bet |
        | `fold`  | `f`     | River: forfeit Ante + Blind |
        | `log`   | `l`     | View action log |
      operationId: ultimatetexasholdemExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UltimateTexasHoldemRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Bet 100 Ante (Blind matches) + 20 Trips
                value:
                  command: bet
                  amount: 100
                  tripsBet: 20
                  sessionId: my-session-001
              play4x:
                summary: Pre-flop Play 4x
                value:
                  command: play
                  multiplier: 4
                  sessionId: my-session-001
              check:
                summary: Check (pre-flop / flop)
                value:
                  command: check
                  sessionId: my-session-001
              fold:
                summary: Fold at the river
                value:
                  command: fold
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UltimateTexasHoldemResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UltimateTexasHoldemResponse'

  /mississippistud/exec:
    post:
      tags:
        - mississippistud
      summary: Execute a Mississippi Stud game command
      description: |
        Send a command to advance the Mississippi Stud
        (ミシシッピ・スタッド) session identified by `sessionId`.
        Mississippi Stud is a single-player vs paytable casino poker game.
        After placing an Ante, the player receives 2 hole cards. Three
        community cards (3rd / 4th / 5th Street) are revealed one at a time;
        before each reveal the player chooses 1x / 2x / 3x bet or fold.
        Folding forfeits every bet on the table. After 5th Street, the best
        5-card hand is scored against a fixed paytable.

        | Phase        | Value | Description |
        |--------------|-------|-------------|
        | Ante         | `1`   | Place the Ante |
        | 3rd Street   | `2`   | Hole cards dealt; 1x / 2x / 3x or fold |
        | 4th Street   | `3`   | 1st community card revealed; 1x / 2x / 3x or fold |
        | 5th Street   | `4`   | 2nd community card revealed; 1x / 2x / 3x or fold |
        | End          | `5`   | 3rd community card revealed; payout |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `bet`   | `b`     | Place Ante (`amount`) |
        | `play`  | `p`     | Place street bet (`multiplier` ∈ {1, 2, 3}) |
        | `fold`  | `f`     | Forfeit all bets on the table |
        | `log`   | `l`     | View action log |
      operationId: mississippistudExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MississippiStudRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Place Ante of 100
                value:
                  command: bet
                  amount: 100
                  sessionId: my-session-001
              play3x:
                summary: Raise 3x at the current street
                value:
                  command: play
                  multiplier: 3
                  sessionId: my-session-001
              fold:
                summary: Fold and forfeit all bets
                value:
                  command: fold
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MississippiStudResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MississippiStudResponse'

  /belote/exec:
    post:
      tags:
        - belote
      summary: Execute a Belote game command
      description: |
        Send a command to advance the Belote (ベロート) session identified
        by `sessionId`. Belote is a French 4-player partnership trick-taking
        game played on a 32-card deck (7-A × 4 suits). Each round deals 5
        cards plus a face-up "turn-up" card; the human is player 0, partner
        with player 2.

        | Phase           | Value | Description |
        |-----------------|-------|-------------|
        | BidPickUp       | `0`   | Round 1: take the turn-up's suit as trump |
        | BidCallTrump    | `1`   | Round 2: name any of the other 3 suits |
        | Play            | `2`   | Trick play (follow-suit + obligation à couper / monter) |
        | TrickEnd        | `3`   | Trick complete, advance with `next` |
        | RoundEnd        | `4`   | 8 tricks complete, score and advance with `nextround` |
        | GameEnd         | `5`   | Target score reached |

        | Command      | Aliases | Description |
        |--------------|---------|-------------|
        | `reset`      | `r`     | Start / restart a game |
        | `orderup`    | `o`     | Take the face-up card's suit as trump |
        | `pass`       | `pa`    | Pass (PickUp or CallTrump) |
        | `calltrump`  | `c`     | Call trump (`suit` ∈ {1,2,3,4}, ≠ face-up's suit) |
        | `play`       | `p`     | Play card by `cardIndex` |
        | `next`       | `n`     | Advance to next trick |
        | `nextround`  | `nr`    | Score round and start the next |
        | `hint`       | `h`     | Suggest an action / card |
        | `log`        | `l`     | View action log |
      operationId: beloteExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BeloteRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              orderup:
                summary: Take the face-up suit as trump
                value:
                  command: orderup
                  sessionId: my-session-001
              calltrump:
                summary: Call clubs (suit=2) as trump
                value:
                  command: calltrump
                  suit: 2
                  sessionId: my-session-001
              play:
                summary: Play the card at index 0
                value:
                  command: play
                  cardIndex: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BeloteResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BeloteResponse'

  /spiderette/exec:
    post:
      tags:
        - spiderette
      summary: Execute a Spiderette game command
      description: |
        Send a command to advance the Spiderette session identified by
        `sessionId`. Spiderette is a single-deck (52 cards) variant of
        Spider Solitaire played on a Klondike-style 7-column staircase
        (column i holds i+1 cards; only the bottom card is face-up).
        Movement rules match Spider: a card may be placed on any card one
        rank higher (regardless of suit), but only same-suit descending
        runs move as a group. K-through-A same-suit sequences are
        auto-removed; clearing all 4 suits wins.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | `0`   | Game in progress |
        | GameClear | `1`   | All 4 suits completed |
        | GameOver  | `2`   | Player gave up |

        | Command        | Aliases | Description |
        |----------------|---------|-------------|
        | `reset`        | `r`     | Start / restart a game |
        | `deal`         | `d`     | Deal 1 card to each column from stock |
        | `move`         | `m`     | Move cards between tableau columns |
        | `giveup`       | `g`     | Give up |
        | `hint`         | `h`     | Get a move suggestion |
        | `autocomplete` | `ac`    | Auto-complete (when all face up) |
        | `undo`         | `u`     | Undo last move |
        | `undo_n`       |         | Undo N moves (requires `n` parameter) |
        | `log`          | `l`     | View action log |
      operationId: spideretteExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SpideretteRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              deal:
                summary: Deal cards from stock
                value:
                  command: deal
                  sessionId: my-session-001
              move:
                summary: Move card(s) from column 3, index 2 to column 5
                value:
                  command: move
                  from:
                    zone: tableau
                    col: 3
                    cardIndex: 2
                  to:
                    zone: tableau
                    col: 5
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpideretteResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpideretteResponse'

  /mighty/exec:
    post:
      tags:
        - mighty
      summary: Execute a Mighty game command
      description: |
        Send a command to advance the Mighty (マイティ) game session identified by
        `sessionId`. Mighty is a Korean 5-player trick-taking card game using 52 cards
        + 1 Joker (53 total). 10 cards are dealt to each player plus a 3-card kitty.
        Players bid the number of point cards (10/J/Q/K/A across 4 suits = 20 max) they
        expect to capture, with a separate No-Trump axis (bid floor +2, round score
        doubled ×2). The highest bidder becomes the declarer, declares the trump suit
        (−1 = No-Trump, 1−4 = Spade/Clover/Heart/Diamond) and names a partner card
        (suit + value); whoever holds the partner card becomes the secret partner. The
        declarer then exchanges 3 cards with the kitty. Special cards: **Mighty**
        (♠A normally; ♦A under spade trump) beats everything including the Joker;
        **Joker** is the second-highest card when not led, can be played any time;
        **Joker-lead rule** -- leading the Joker forces the holder to name a demand
        suit and the Joker loses the trick; **Joker Call** (♣3 normally; ♠3 under
        clubs trump) when led after the first trick forces the Joker holder to play
        the Joker.

        The game proceeds through these phases:

        | Phase           | Value | Description |
        |-----------------|-------|-------------|
        | Bid             | `0`   | Each player bids point-card count (or passes) |
        | TrumpAndFriend  | `1`   | Declarer declares trump suit and names partner card |
        | KittyExchange   | `2`   | Declarer exchanges 3 cards with the kitty |
        | Play            | `3`   | Trick play (follow-suit; trump as declared) |
        | TrickEnd        | `4`   | Trick resolved; advance with `next` |
        | RoundEnd        | `5`   | All 10 tricks played; advance with `nextround` |
        | GameEnd         | `6`   | A player reached the point limit |

        | Command        | Aliases | Description |
        |----------------|---------|-------------|
        | `reset`        | `r`     | Start / restart a game |
        | `bid`          | `b`     | Place a bid (requires `bid`; optionally `noTrump`) or pass (bid = 0) |
        | `trump`        | `t`     | Declare trump suit and partner card (requires `trumpSuit`, `partnerSuit`, `partnerValue`) |
        | `exchange`     | `e`     | Exchange 3 cards with the kitty (requires `discardIndices` of length 3) |
        | `play`         | `p`     | Play a card (requires `cardIndex`) |
        | `jokerlead`    | `jl`    | Lead the Joker and name a demand suit (requires `cardIndex` and `jokerLeadSuit`) |
        | `next`         | `n`     | Advance to the next trick |
        | `nextround`    | `nr`    | Score the round and start the next |
        | `hint`         | `h`     | Get a play suggestion |
        | `log`          | `l`     | View action log |
      operationId: mightyExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MightyRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Bid 14 point cards
                value:
                  command: bid
                  bid: 14
                  sessionId: my-session-001
              bidNoTrump:
                summary: Bid 15 under No-Trump
                value:
                  command: bid
                  bid: 15
                  noTrump: true
                  sessionId: my-session-001
              pass:
                summary: Pass on bidding
                value:
                  command: bid
                  bid: 0
                  sessionId: my-session-001
              trump:
                summary: Declare spades trump and ♥A partner
                value:
                  command: trump
                  trumpSuit: 1
                  partnerSuit: 3
                  partnerValue: 1
                  sessionId: my-session-001
              exchange:
                summary: Discard kitty-exchange cards at indices 0, 3, 9
                value:
                  command: exchange
                  discardIndices: [0, 3, 9]
                  sessionId: my-session-001
              play:
                summary: Play card at index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              jokerlead:
                summary: Lead the Joker at index 5 and demand hearts
                value:
                  command: jokerlead
                  cardIndex: 5
                  jokerLeadSuit: 3
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MightyResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MightyResponse'

  /oasispoker/exec:
    post:
      tags:
        - oasispoker
      summary: Execute an Oasis Poker game command
      description: |
        Send a command to advance the Oasis Poker (オアシスポーカー) game session
        identified by `sessionId`. Oasis Poker is a Caribbean Stud variant that
        inserts a card exchange phase between Bet and Action. After receiving 5
        cards, the player may exchange 1..5 cards (or stand) for a fee equal to
        the ante per card, then either Call (place a Play bet equal to 2x ante)
        or Fold (forfeit ante and exchange fee). Dealer must have Ace-King high
        or any pair to qualify. Pay tables and the optional Jackpot side bet are
        identical to Caribbean Stud.

        The game proceeds through these phases:

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Bet      | `1`   | Place Ante and optional Jackpot side bet |
        | Exchange | `2`   | Optionally exchange 1..5 cards (fee = ante × cards) |
        | Action   | `3`   | Decide to Call or Fold |
        | End      | `4`   | Round complete; shows result and payout |

        | Command    | Aliases | Description |
        |------------|---------|-------------|
        | `reset`    | `r`     | Start / restart a game |
        | `bet`      | `b`     | Place Ante bet (requires `amount`; optional `jackpotBet`) |
        | `exchange` | `e`     | Exchange cards at the given `indices` (0..4); fee = ante × len |
        | `stand`    | `s`     | Skip exchange, advance to action |
        | `play`     | `p`     | Call: place Play bet (2x Ante) and reveal dealer hand |
        | `fold`     | `f`     | Forfeit Ante bet and exchange fee |
        | `log`      | `l`     | View action log |
      operationId: oasispokerExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OasisPokerRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Bet 100 Ante with 10 Jackpot
                value:
                  command: bet
                  amount: 100
                  jackpotBet: 10
                  sessionId: my-session-001
              exchange:
                summary: Exchange cards at indices 0 and 2
                value:
                  command: exchange
                  indices: [0, 2]
                  sessionId: my-session-001
              stand:
                summary: Skip exchange
                value:
                  command: stand
                  sessionId: my-session-001
              play:
                summary: Call (place Play bet)
                value:
                  command: play
                  sessionId: my-session-001
              fold:
                summary: Fold hand
                value:
                  command: fold
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OasisPokerResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OasisPokerResponse'

  /russianpoker/exec:
    post:
      tags:
        - russianpoker
      summary: Execute a Russian Poker game command
      description: |
        Send a command to advance the Russian Poker (ロシアンポーカー) game session
        identified by `sessionId`. Russian Poker is an Oasis Poker evolution popular
        in Eastern European casinos. It offers four player options after the deal:
        Play, Fold, Exchange cards (fee = ante × count), or Buy a 6th card (fee = ante).
        When the dealer doesn't qualify, the player can pay ante to force exchange
        the dealer's highest card. No jackpot side bet.

        | Phase        | Value | Description |
        |--------------|-------|-------------|
        | Bet          | `1`   | Place Ante |
        | Action       | `2`   | Fold / Play / Exchange / Buy 6th |
        | Select       | `3`   | Discard 1 from 6 cards (after Buy 6th) |
        | PostAction   | `4`   | Fold / Play (after Exchange or Buy 6th) |
        | ForceQualify | `5`   | Force dealer exchange or decline |
        | End          | `6`   | Round complete |

        | Command   | Aliases | Description |
        |-----------|---------|-------------|
        | `reset`   | `r`     | Start / restart a game |
        | `bet`     | `b`     | Place Ante bet (requires `amount`) |
        | `exchange`| `e`     | Exchange cards at `indices` (0..4); fee = ante × len |
        | `buy6th`  | `6`     | Buy 6th card (fee = ante) |
        | `select`  | `sel`   | Discard card at `discardIndex` from 6-card hand |
        | `play`    | `p`     | Call: place Play bet (2x Ante) |
        | `fold`    | `f`     | Forfeit Ante and fees |
        | `force`   | `fe`    | Force exchange dealer's highest card (fee = ante) |
        | `decline` | `d`     | Decline force exchange |
        | `log`     | `l`     | View action log |
      operationId: russianpokerExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RussianPokerRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Bet 100 Ante
                value:
                  command: bet
                  amount: 100
                  sessionId: my-session-001
              exchange:
                summary: Exchange cards at indices 0 and 2
                value:
                  command: exchange
                  indices: [0, 2]
                  sessionId: my-session-001
              buy6th:
                summary: Buy 6th card
                value:
                  command: buy6th
                  sessionId: my-session-001
              select:
                summary: Discard card at index 3
                value:
                  command: select
                  discardIndex: 3
                  sessionId: my-session-001
              play:
                summary: Call (place Play bet)
                value:
                  command: play
                  sessionId: my-session-001
              fold:
                summary: Fold hand
                value:
                  command: fold
                  sessionId: my-session-001
              force:
                summary: Force dealer exchange
                value:
                  command: force
                  sessionId: my-session-001
              decline:
                summary: Decline force exchange
                value:
                  command: decline
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RussianPokerResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RussianPokerResponse'

  /callbreak/exec:
    post:
      tags:
        - callbreak
      summary: Execute a Call Break (コールブレイク) game command
      description: |
        Send a command to advance the Call Break game session identified by
        `sessionId`. Call Break is a 4-player trick-taking game from South
        Asia, structurally Spades with different scoring. Spades are the
        permanent trump. Bids are 1..13 (no Nil). If void in the led suit
        and you hold spades you must trump with a spade. Scoring is stored
        as int×10 (display "X.Y"): meeting the bid scores `bid*10 +
        overtricks`, missing scores `-bid*10`. The game runs for 5 rounds
        by default and the highest cumulative score wins.

        Phases:

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Bid       | `0`   | Each player declares 1..13 |
        | Play      | `1`   | 13-trick play (must follow / must trump if void) |
        | TrickEnd  | `2`   | Trick resolved; call `n`/`next` to continue |
        | RoundEnd  | `3`   | Round scored; call `nr`/`nextround` |
        | GameEnd   | `4`   | MaxRounds reached; highest cumulative score wins |

        Commands:

        | Command       | Aliases       | Description |
        |---------------|---------------|-------------|
        | `reset` / `r` |               | Restart with optional config |
        | `bid` / `b`   |               | Place bid (1..13) |
        | `play` / `p`  |               | Play card at index |
        | `next` / `n`  |               | Advance to next trick |
        | `nextround` / `nr` |          | Score round and start next |
        | `hint` / `h`  |               | Suggested move |
        | `log` / `l`   |               | View action log |
      operationId: callbreakExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bid, b, play, p, next, n, nextround, nr, hint, h, log, l]
                bid:
                  type: integer
                  description: Bid amount for `bid`/`b` (1..13)
                cardIndex:
                  type: integer
                  description: Card index for `play`/`p`
                config:
                  $ref: '#/components/schemas/CallBreakConfig'
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Declare bid 4
                value:
                  command: b
                  bid: 4
                  sessionId: my-session-001
              play:
                summary: Play hand[0]
                value:
                  command: p
                  cardIndex: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CallBreakResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CallBreakResponse'

  /tarneeb/exec:
    post:
      tags:
        - tarneeb
      summary: Execute a Tarneeb (ターニーブ) game command
      description: |
        Send a command to advance the Tarneeb game session identified by
        `sessionId`. Tarneeb is a 4-player Middle-Eastern partnership
        trick-taking game. Bids are 7..13 or 0 (pass); the highest bidder
        declares the trump suit in a separate phase, then plays out 13 tricks
        following the led suit when possible. Bidder team scores +tricks on
        success or -bid on failure; defender team always scores +tricks. First
        team to PointLimit wins.

        Phases:

        | Phase             | Value | Description |
        |-------------------|-------|-------------|
        | Bid               | `0`   | Each player bids 7..13 or 0 (pass); all-pass triggers redeal |
        | TrumpDeclaration  | `1`   | Bid winner picks the trump suit |
        | Play              | `2`   | 13-trick play (must follow / free choice when void) |
        | TrickEnd          | `3`   | Trick resolved; call `n`/`next` to continue |
        | RoundEnd          | `4`   | Round scored; call `nr`/`nextround` |
        | GameEnd           | `5`   | Game over once a team reaches PointLimit |

        Commands:

        | Command            | Aliases | Description |
        |--------------------|---------|-------------|
        | `reset` / `r`      |         | Restart with optional config |
        | `bid` / `b`        |         | Place bid (0=pass, 7..13) |
        | `trump` / `t`      |         | Declare trump suit (1=♠ 2=♣ 3=♥ 4=♦) |
        | `play` / `p`       |         | Play card at index |
        | `next` / `n`       |         | Advance to next trick |
        | `nextround` / `nr` |         | Score round and start next |
        | `hint` / `h`       |         | Suggested move |
        | `log` / `l`        |         | View action log |
      operationId: tarneebExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bid, b, trump, t, play, p, next, n, nextround, nr, hint, h, log, l]
                bid:
                  type: integer
                  description: Bid amount for `bid`/`b` (0=pass, 7..13)
                trumpSuit:
                  type: integer
                  description: Trump suit for `trump`/`t` (1=♠, 2=♣, 3=♥, 4=♦)
                  minimum: 1
                  maximum: 4
                cardIndex:
                  type: integer
                  description: Card index for `play`/`p`
                config:
                  $ref: '#/components/schemas/TarneebConfig'
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Declare bid 8
                value:
                  command: b
                  bid: 8
                  sessionId: my-session-001
              trump:
                summary: Declare spades trump
                value:
                  command: t
                  trumpSuit: 1
                  sessionId: my-session-001
              play:
                summary: Play hand[0]
                value:
                  command: p
                  cardIndex: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TarneebResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TarneebResponse'

  /briscola/exec:
    post:
      tags:
        - briscola
      summary: Execute a Briscola (ブリスコラ) game command
      description: |
        Send a command to advance the Briscola game session identified by
        `sessionId`. Briscola is a 2-player Italian trick-taking classic
        played with a 40-card deck (A,2-7,J,Q,K x 4 suits — no 8/9/10).
        Each player holds 3 cards, replenished from the stock after each
        trick. The face-up trump card at the bottom of the stock decides
        the trump suit. **No must-follow**: any card in hand is legal at
        any time. Trick winner gets the trump-beats-non-trump treatment
        and the Briscola intra-suit rank order (A>3>K>Q>J>7>6>5>4>2).
        Card points A=11, 3=10, K=4, Q=3, J=2 (total 120 per deal);
        >60 points wins, 60-60 is a tie.

        Phases:

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Play     | `0`   | Either player to act |
        | TrickEnd | `1`   | Trick resolved; call `n`/`next` to continue |
        | GameEnd  | `2`   | All 40 cards played; winnerIdx set |

        Commands:

        | Command       | Aliases | Description |
        |---------------|---------|-------------|
        | `reset` / `r` |         | Restart with optional config |
        | `play` / `p`  |         | Play card at index |
        | `next` / `n`  |         | Advance to next trick (draw replenish + game-end detect) |
        | `hint` / `h`  |         | Suggested move |
        | `log` / `l`   |         | View action log |
      operationId: briscolaExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, next, n, hint, h, log, l]
                cardIndex:
                  type: integer
                  description: Card index for `play`/`p`
                config:
                  $ref: '#/components/schemas/BriscolaConfig'
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play hand[0]
                value:
                  command: p
                  cardIndex: 0
                  sessionId: my-session-001
              next:
                summary: Advance to next trick
                value:
                  command: n
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BriscolaResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BriscolaResponse'

  /schnapsen/exec:
    post:
      tags:
        - schnapsen
      summary: Execute a Schnapsen / Sixty-Six (シュナプセン / 66) game command
      description: |
        Send a command to advance the Schnapsen game session identified by
        `sessionId`. Schnapsen is a 2-player Austrian/German trick-taking
        classic played with a 20-card deck (A,10,K,Q,J x 4 suits). Each
        player holds 5 cards, replenished from the stock after each trick.
        The face-up trump upcard at the bottom of the stock decides the trump
        suit. **Phase 1 (stock present): no must-follow** — any card is legal.
        **Phase 2 (stock exhausted): must-follow** — follow suit and win if
        able, otherwise trump if void. A **marriage** (K+Q of one suit,
        declared while on lead) scores 20 (40 for the trump suit) and leads
        one of those cards. Intra-suit rank A>10>K>Q>J; card points A=11,
        10=10, K=4, Q=3, J=2 (total 120). First to 66 wins the round; if
        nobody reaches 66, the last trick taker wins.

        Phases:

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Play     | `0`   | Either player to act |
        | TrickEnd | `1`   | Trick resolved; call `n`/`next` to continue |
        | GameEnd  | `2`   | Round over; winnerIdx set |

        Commands:

        | Command          | Aliases | Description |
        |------------------|---------|-------------|
        | `reset` / `r`    |         | Restart with optional config |
        | `play` / `p`     |         | Play card at index |
        | `marriage` / `m` |         | Declare marriage and lead the K/Q at index |
        | `next` / `n`     |         | Advance to next trick (draw replenish + game-end detect) |
        | `hint` / `h`     |         | Suggested move |
        | `log` / `l`      |         | View action log |
      operationId: schnapsenExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, marriage, m, next, n, hint, h, log, l]
                cardIndex:
                  type: integer
                  description: Card index for `play`/`p` and `marriage`/`m`
                config:
                  $ref: '#/components/schemas/SchnapsenConfig'
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play hand[0]
                value:
                  command: p
                  cardIndex: 0
                  sessionId: my-session-001
              marriage:
                summary: Declare a marriage and lead hand[2]
                value:
                  command: m
                  cardIndex: 2
                  sessionId: my-session-001
              next:
                summary: Advance to next trick
                value:
                  command: n
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SchnapsenResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SchnapsenResponse'

  /gaps/exec:
    post:
      tags:
        - gaps
      summary: Execute a Gaps / Montana (ギャップス) game command
      description: |
        Single-player puzzle solitaire on a 4-row by 13-column grid. All four
        Aces are removed, leaving 4 gaps. A gap accepts the card whose suit
        matches its left neighbour and whose rank is +1; a gap in column 0
        accepts any 2 (start-of-row anchor); a gap right of a King is dead.
        Up to 3 redeals are allowed: the locked prefix of each row (2 then
        same-suit successors) stays put, the rest is shuffled. Win when each
        row holds 2..K of one suit.

        Phases:

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | `0`   | Game in progress |
        | GameClear | `1`   | All rows sorted 2..K |
        | GameOver  | `2`   | Stalemate or give-up |

        Commands:

        | Command          | Aliases | Description |
        |------------------|---------|-------------|
        | `reset` / `r`    |         | Start a new game |
        | `move` / `m`     |         | Move card from `from` zone to `to` zone (gap) |
        | `redeal` / `rd`  |         | Redeal remaining cards (max 3 times) |
        | `undo` / `u`     |         | Undo last move |
        | `undo_n`         |         | Chain `n` undos |
        | `giveup` / `g`   |         | Give up and end the game |
        | `hint` / `h`     |         | Suggest the next move |
        | `log` / `l`      |         | View action log |
      operationId: gapsExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, move, m, redeal, rd, undo, u, undo_n, giveup, g, hint, h, log, l]
                from:
                  $ref: '#/components/schemas/GapsMoveZone'
                to:
                  $ref: '#/components/schemas/GapsMoveZone'
                n:
                  type: integer
                  description: Number of undos for `undo_n`.
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              move:
                summary: Move a card into a gap
                value:
                  command: move
                  from: { zone: grid, row: 2, col: 0 }
                  to: { zone: grid, row: 0, col: 0 }
                  sessionId: my-session-001
              redeal:
                summary: Redeal remaining cards
                value:
                  command: redeal
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GapsResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GapsResponse'

  /highcardflush/exec:
    post:
      tags:
        - highcardflush
      summary: Execute a High Card Flush (ハイカードフラッシュ) game command
      description: |
        Single-player casino table game. Each round both player and dealer get
        7 cards; the longest same-suit subset (flush length) decides the winner
        (ties broken by high card descending within the flush). The player
        places an Ante and optional **Flush Bonus** / **Straight Flush Bonus**
        side bets, then chooses **Raise** (1x / 2x / 3x of Ante, capped by
        their flush length: 2-4 cards = 1x, 5 cards = 2x, 6-7 cards = 3x) or
        **Fold**. Dealer qualifies on a 3+ card flush with 9-high or better.

        Phases:

        | Phase  | Value | Description |
        |--------|-------|-------------|
        | Bet    | `1`   | Place Ante and optional side bets |
        | Action | `2`   | Raise (1/2/3x) or Fold |
        | End    | `3`   | Result and payout displayed |

        Commands:

        | Command         | Aliases | Description |
        |-----------------|---------|-------------|
        | `reset` / `r`   |         | Restart the round |
        | `bet` / `b`     |         | Place Ante + optional Flush Bonus + Straight Flush Bonus |
        | `raise` / `ra`  |         | Raise with multiplier 1..MaxRaiseMultiplier |
        | `fold` / `f`    |         | Fold and forfeit the Ante (side bets still evaluate) |
        | `log` / `l`     |         | View action log |
      operationId: highcardflushExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bet, b, raise, ra, fold, f, log, l]
                amount:
                  type: integer
                  description: Ante amount for `bet`/`b`
                flushBonusBet:
                  type: integer
                  description: Optional Flush Bonus side bet for `bet`/`b` (0 = no bet)
                straightFlushBet:
                  type: integer
                  description: Optional Straight Flush Bonus side bet for `bet`/`b` (0 = no bet)
                multiplier:
                  type: integer
                  description: Raise multiplier for `raise`/`ra` (1..3, capped by flush length)
                  minimum: 1
                  maximum: 3
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Ante 100 + Flush Bonus 50 + Straight Flush Bonus 20
                value:
                  command: b
                  amount: 100
                  flushBonusBet: 50
                  straightFlushBet: 20
                  sessionId: my-session-001
              raise2x:
                summary: Raise 2x
                value:
                  command: ra
                  multiplier: 2
                  sessionId: my-session-001
              fold:
                summary: Fold and forfeit Ante
                value:
                  command: f
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HighCardFlushResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HighCardFlushResponse'

  /sixcardgolf/exec:
    post:
      tags:
        - sixcardgolf
      summary: Execute a Six Card Golf game command
      description: |
        Send a command to advance the Six Card Golf (シックスカードゴルフ) game session
        identified by `sessionId`. Six Card Golf is a multi-player card game (1 human plus
        CPU opponents) using a standard 52-card deck. Each player owns a 2x3 grid of six
        cards and aims for the lowest score over a configurable number of rounds. Aces
        count 1, 2 counts -2, number cards face value, J/Q count 10, K counts 0, and a
        matched vertical pair cancels to 0.

        The game proceeds through these phases:

        | Phase       | Value | Description |
        |-------------|-------|-------------|
        | Setup       | `0`   | Flip two initial cards in your grid |
        | PlayerTurn  | `1`   | Choose to draw from the stock or the discard pile |
        | DrawPending | `2`   | Swap the drawn card into the grid or discard it |
        | RoundOver   | `3`   | Round complete; scores shown |
        | GameOver    | `4`   | Match complete; final winner decided |

        | Command       | Aliases | Description |
        |---------------|---------|-------------|
        | `reset`       | `r`     | Start / restart a game (accepts optional `config`) |
        | `flipinitial` | `fi`    | Flip one of the two initial cards (requires `position`) |
        | `drawstock`   | `ds`    | Draw the top card from the stock pile |
        | `drawdiscard` | `dd`    | Draw the top card from the discard pile |
        | `swap`        | `sw`    | Swap the drawn card into a grid slot (requires `position`) |
        | `discard`     | `di`    | Discard the drawn stock card without swapping |
        | `flip`        | `fl`    | Flip a face-down grid card (requires `position`) |
        | `skipflip`    | `sf`    | Skip the optional flip |
        | `nextround`   | `nr`    | Advance to the next round |
        | `log`         | `l`     | View action log |
      operationId: sixcardgolfExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, flipinitial, fi, drawstock, ds, drawdiscard, dd, swap, sw, discard, di, flip, fl, skipflip, sf, nextround, nr, log, l]
                position:
                  type: integer
                  description: Grid slot index (0-5) for flipinitial, swap, and flip commands
                config:
                  type: object
                  description: Optional game configuration (used with reset)
                  properties:
                    playerCount:
                      type: integer
                      description: Number of players (2-4)
                    cpuDifficulty:
                      type: integer
                      description: CPU difficulty (0=Easy, 1=Normal, 2=Hard)
                    rounds:
                      type: integer
                      description: Number of rounds to play (1-18)
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              flipinitial:
                summary: Flip an initial card
                value:
                  command: flipinitial
                  position: 0
                  sessionId: my-session-001
              drawstock:
                summary: Draw from the stock pile
                value:
                  command: drawstock
                  sessionId: my-session-001
              swap:
                summary: Swap the drawn card into the grid
                value:
                  command: swap
                  position: 3
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  players:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: integer
                        isHuman:
                          type: boolean
                        grid:
                          type: array
                          items:
                            type: object
                            properties:
                              card:
                                $ref: '#/components/schemas/Card'
                              faceUp:
                                type: boolean
                        roundScore:
                          type: integer
                        cumulativeScore:
                          type: integer
                        allFaceUp:
                          type: boolean
                  phase:
                    type: integer
                  roundNumber:
                    type: integer
                  totalRounds:
                    type: integer
                  currentPlayerIdx:
                    type: integer
                  discardTop:
                    $ref: '#/components/schemas/Card'
                  drawPileCount:
                    type: integer
                  drawnCard:
                    $ref: '#/components/schemas/Card'
                  drawnFromDiscard:
                    type: boolean
                  canFlip:
                    type: boolean
                  finalTurnTrigger:
                    type: integer
                  gameEndFlag:
                    type: boolean
                  winnerIdx:
                    type: integer
                  config:
                    type: object
                    properties:
                      playerCount:
                        type: integer
                      cpuDifficulty:
                        type: integer
                      rounds:
                        type: integer
                  message:
                    type: string
                  messageCode:
                    type: string
                  messageParams:
                    type: object
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string

  /doudizhu/exec:
    post:
      tags:
        - doudizhu
      summary: Execute a Dou Dizhu (斗地主) game command
      description: |
        Send a command to advance the Dou Dizhu game session identified by
        `sessionId`. The game has 3 players (1 human + 2 CPU). One player
        becomes the Landlord via bidding and plays against two Peasants.

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset` | `r`     | Start / restart a game |
        | `bid`   |         | Bid 0-3 (0 = pass) during the bidding phase |
        | `play`  | `p`     | Play cards specified by `indices`; pass with an empty array |
        | `log`   | `l`     | Return the action log |
      operationId: doudizhuExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DoudizhuRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Bid 2 to become the Landlord
                value:
                  command: bid
                  bidValue: 2
                  sessionId: my-session-001
              play:
                summary: Play cards at hand positions 0 and 1
                value:
                  command: play
                  indices: [0, 1]
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DoudizhuResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DoudizhuResponse'

  /truco/exec:
    post:
      tags:
        - truco
      summary: Execute a Truco (トゥルコ) game command
      description: |
        Send a command to advance the Truco match identified by `sessionId`.
        The game has 2 players (1 human + 1 CPU). Each hand (mano) deals 3 cards;
        a player may raise the stake with `truco` and the opponent responds with
        `accept` / `decline` / `truco` (re-raise). First to the match target wins.

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset`   | `r` | Start / restart a match |
        | `play`    | `p` | Play the card at hand position `cardIndex` |
        | `truco`   | `t` | Declare Truco, or re-raise (Retruco / Vale Cuatro) |
        | `accept`  | `a` | Accept the pending call (Quiero) |
        | `decline` | `d` | Decline the pending call (No Quiero) |
        | `next`    | `n` | Advance to the next baza / hand |
        | `hint`    | `h` | Return a move hint |
        | `log`     | `l` | Return the action log |
      operationId: trucoExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TrucoRequest'
            examples:
              reset:
                summary: Start a new match
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the card at hand position 0
                value:
                  command: play
                  cardIndex: 0
                  sessionId: my-session-001
              truco:
                summary: Declare Truco
                value:
                  command: truco
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrucoResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrucoResponse'

  /acesup/exec:
    post:
      tags:
        - acesup
      summary: Execute an Aces Up game command
      description: |
        Send a command to advance the Aces Up (四つ葉のクローバー) game session identified by
        `sessionId`. Aces Up is a single-player solitaire using a standard 52-card deck.
        Four tableau columns are dealt 1 card each (48 cards to stock). Aces rank highest
        (A > K). The top card of a column may be removed when another column's top card is
        the same suit and higher rank; `draw` deals one card onto each column; the top card
        of a column may be moved into an empty column. Win by clearing the board down to the
        four Aces (one per column).

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | Only the four Aces remain; player wins |
        | GameOver   | `2`   | No more moves available; game over |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset`        | `r`  | Start / restart a game |
        | `draw`         | `d`  | Deal one card onto each column |
        | `remove`       | `rm` | Remove a column's top card (same-suit-higher rule) |
        | `move`         | `mv` | Move a column's top card into an empty column |
        | `giveup`       | `g`  | Give up the current game |
        | `hint`         | `h`  | Get a hint for the next move |
        | `undo`         | `u`  | Undo the last action |
        | `undo_n`       |      | Undo N actions (requires `n` parameter) |
        | `log`          | `l`  | View action log |
      operationId: acesupExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, draw, d, remove, rm, move, mv, giveup, g, hint, h, undo, u, undo_n, log, l]
                col:
                  type: integer
                  description: Column index (0-3) for remove and move commands
                n:
                  type: integer
                  description: Number of undos to perform (required when command is undo_n)
                  example: 3
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              draw:
                summary: Deal to each column
                value:
                  command: draw
                  sessionId: my-session-001
              remove:
                summary: Remove a column's top card
                value:
                  command: remove
                  col: 0
                  sessionId: my-session-001
              move:
                summary: Move a top card to an empty column
                value:
                  command: move
                  col: 1
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  columns:
                    type: array
                    items:
                      type: array
                      items:
                        type: object
                        properties:
                          card:
                            $ref: '#/components/schemas/Card'
                          top:
                            type: boolean
                          removable:
                            type: boolean
                          movable:
                            type: boolean
                  stockCount:
                    type: integer
                  discardCount:
                    type: integer
                  phase:
                    type: integer
                  moveCount:
                    type: integer
                  canUndo:
                    type: boolean
                  isStalemate:
                    type: boolean
                  undoToEscape:
                    type: integer
                    description: Number of undos needed to reach a non-stalemate state (0 if not stalemate, -1 if no escape)
                    example: 0
                  message:
                    type: string
                  messageCode:
                    type: string
                  messageParams:
                    type: object
                  hint:
                    type: object
                    properties:
                      type:
                        type: string
                      col:
                        type: integer
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  columns:
                    type: array
                    items:
                      type: array
                  stockCount:
                    type: integer
                  discardCount:
                    type: integer
                  phase:
                    type: integer
                  moveCount:
                    type: integer
                  message:
                    type: string

  /bakersgame/exec:
    post:
      tags:
        - bakersgame
      summary: Execute a Baker's Game command
      description: |
        Send a command to advance the Baker's Game (ベーカーズゲーム) session identified by
        `sessionId`. Baker's Game is a FreeCell variant on a standard 52-card deck with an
        identical layout (8 tableau columns, 4 free cells, 4 foundations built up by suit
        Ace to King) except the tableau builds **same-suit descending** instead of
        alternating colors, which makes it harder. Only Kings may move to an empty column.
        Supermove: max movable cards = (1 + emptyFreeCells) x 2^(emptyTableauCols).

        Baker's Game shares FreeCell's request/response schema
        (`FreeCellRequest` / `FreeCellResponse`).

        The game proceeds through these phases:

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Playing    | `0`   | Game in progress |
        | GameClear  | `1`   | All foundations completed; player wins |
        | GameOver   | `2`   | No more moves available; game over |

        | Command | Aliases | Description |
        |---------|---------|-------------|
        | `reset`        | `r`  | Start / restart a game |
        | `move`         | `m`  | Move card(s) between zones (requires `from` and `to`) |
        | `giveup`       | `g`  | Give up the current game |
        | `hint`         | `h`  | Get a hint for the next move |
        | `autocomplete` | `ac` | Auto-complete remaining moves to foundation |
        | `undo`         | `u`  | Undo the last action |
        | `undo_n`       |      | Undo N actions (requires `n` parameter) |
        | `log`          | `l`  | View action log |
      operationId: bakersgameExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FreeCellRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              move:
                summary: Move a card
                value:
                  command: move
                  from:
                    zone: tableau
                    col: 0
                    cardIndex: 6
                  to:
                    zone: freecell
                    cell: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FreeCellResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FreeCellResponse'

  /bourre/exec:
    post:
      tags:
        - bourre
      summary: Execute a Bourré (ブーレ) game command
      description: |
        Send a command to advance a Bourré session identified by `sessionId`.
        Bourré is a 5-player gambling trick-taking game fusing poker-style ante/draw
        betting with must-follow trick play on a standard 52-card deck. Each player antes
        into the pot and is dealt 5 cards; the dealer's last card sets the trump suit.
        Players decide to play or fold, may discard and draw replacements, then play five
        tricks under must-follow / must-trump-when-void / must-win-when-able rules. The
        most tricks takes the pot; a player who plays but wins zero tricks is "bourréd"
        and pays a penalty equal to the pot.

        | Phase    | Value      | Description |
        |----------|------------|-------------|
        | Decide   | `decide`   | Each player chooses to play or fold |
        | Draw     | `draw`     | Active players discard and draw |
        | Play     | `play`     | Five tricks are played |
        | RoundEnd | `roundEnd` | Hand resolved; pot distributed |
        | GameEnd  | `gameEnd`  | Match over |

        | Command  | Aliases | Description |
        |----------|---------|-------------|
        | `reset`  | `r`     | Start / restart the game (optional `config`) |
        | `decide` |         | Play or fold (`decide`: true=play, false=fold) |
        | `draw`   |         | Discard cards by `indices`, then draw replacements |
        | `play`   | `p`     | Play a card by `cardIndex` |
        | `next`   |         | Advance to the next hand |
        | `log`    | `l`     | View action log |
      operationId: bourreExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BourreRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              decide:
                summary: Choose to play this hand
                value:
                  command: decide
                  decide: true
                  sessionId: my-session-001
              play:
                summary: Play a card
                value:
                  command: play
                  cardIndex: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BourreResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BourreResponse'

  /sheepshead/exec:
    post:
      tags:
        - sheepshead
      summary: Execute a Sheepshead game command
      description: |
        Send a command to advance a Sheepshead session identified by `sessionId`.
        Sheepshead is a 5-player (1 human + 4 CPU) Wisconsin/German trick-taking game
        on a 32-card deck (A,7,8,9,10,J,Q,K × 4 suits). Each player receives 6 cards
        and the remaining 2 form the blind. Fixed trump (14 cards) strongest to weakest:
        Q♣ > Q♠ > Q♥ > Q♦ > J♣ > J♠ > J♥ > J♦ > A♦ > 10♦ > K♦ > 9♦ > 8♦ > 7♦.
        Fail suits (♣♠♥) keep only A,10,K,9,8,7. Card points: A=11, 10=10, K=4, Q=3,
        J=2, 9/8/7=0 (total 120). The picker buries 2 cards face-down (which count
        toward their team), calls a fail-suit Ace to name a secret partner, then plays
        6 tricks. Picker team wins with 61+ points. Schneider (loser ≤30) doubles,
        Schwarz (loser takes 0 tricks) triples the chip stake.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Pick      | `0`   | Each player picks up the blind or passes |
        | Bury      | `1`   | Picker takes blind and buries 2 cards |
        | Call      | `2`   | Picker calls a fail-suit Ace to name secret partner |
        | Play      | `3`   | Six tricks played under must-follow rules |
        | TrickEnd  | `4`   | Trick resolved; advance with `next` |
        | RoundEnd  | `5`   | Round scored; chips settled; advance with `nextround` |
        | GameEnd   | `6`   | A player reached the target chip count |

        | Command           | Aliases    | Description |
        |-------------------|------------|-------------|
        | `reset`           | `r`        | Start / restart the game (optional `config`) |
        | `pick`            | `p`        | Pick up the blind (Pick phase) |
        | `pass`            |            | Pass the blind (Pick phase) |
        | `bury`            | `b`        | Bury 2 cards by `cardIndices` (Bury phase) |
        | `call`            | `c`        | Call a fail-suit Ace by `suit` (Call phase; 1=♠ 2=♣ 3=♥) |
        | `play`            |            | Play a card by `cardIndex` (Play phase) |
        | `next`            | `n`        | Advance to the next trick |
        | `nextround`       | `nr`       | Settle chips and start the next round |
        | `setdifficulty`   | `sd`       | Set CPU difficulty (requires `difficulty` 0-2) |
        | `setbasechips`    | `sb`       | Set base chip stake (requires `amount`) |
        | `hint`            | `h`        | Get a suggested move |
        | `log`             | `l`        | View action log |
      operationId: sheepsheadExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SheepsheadRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              pick:
                summary: Pick up the blind
                value:
                  command: pick
                  sessionId: my-session-001
              bury:
                summary: Bury cards at hand indices 0 and 1
                value:
                  command: bury
                  cardIndices: [0, 1]
                  sessionId: my-session-001
              call:
                summary: Call spades (suit 1)
                value:
                  command: call
                  suit: 1
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 0
                value:
                  command: play
                  cardIndex: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SheepsheadResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SheepsheadResponse'

  /doppelkopf/exec:
    post:
      tags:
        - doppelkopf
      summary: Execute a Doppelkopf game command
      description: |
        Send a command to advance a Doppelkopf session identified by `sessionId`.
        Doppelkopf is a 4-player (1 human + 3 CPU) German trick-taking game
        on a 48-card doubled deck (9,10,J,Q,K,A × 4 suits × 2 copies each).
        Each player receives 12 cards. Fixed trump (26 cards) strongest to weakest:
        ♥10 (Dulle) > Q♣ > Q♠ > Q♥ > Q♦ > J♣ > J♠ > J♥ > J♦ > A♦ > 10♦ > K♦ > 9♦.
        Fail suits (♣♠♥) keep only A,10,K,9 (♥ keeps A,K,9 since ♥10 is trump).
        Card points: A=11, 10=10, K=4, Q=3, J=2, 9=0 (total 240). The two Q♣ holders
        secretly form the Re team; the other two are Kontra. Re team wins with 121+
        points. Re/Kontra may be announced during the 1st trick (×2 stake each).
        Identical cards played in the same trick are resolved by play order (first wins).

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Play      | `0`   | Twelve tricks played under must-follow rules |
        | TrickEnd  | `1`   | Trick resolved; advance with `next` |
        | RoundEnd  | `2`   | Round scored; chips settled; advance with `nextround` |
        | GameEnd   | `3`   | A player reached the target chip count |

        | Command           | Aliases    | Description |
        |-------------------|------------|-------------|
        | `reset`           | `r`        | Start / restart the game (optional `config`) |
        | `play`            |            | Play a card by `cardIndex` (Play phase) |
        | `announce`        | `a`        | Announce Re or Kontra during trick 1 (Play phase) |
        | `next`            | `n`        | Advance to the next trick |
        | `nextround`       | `nr`       | Settle chips and start the next round |
        | `setdifficulty`   | `sd`       | Set CPU difficulty (requires `difficulty` 0-2) |
        | `setbasechips`    | `sb`       | Set base chip stake (requires `amount`) |
        | `log`             | `l`        | View action log |
      operationId: doppelkopfExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DoppelkopfRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              announce:
                summary: Announce Re or Kontra during trick 1
                value:
                  command: announce
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DoppelkopfResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DoppelkopfResponse'

  /mus/exec:
    post:
      tags:
        - mus
      summary: Execute a Mus game command
      description: |
        Send a command to advance a Mus session identified by `sessionId`.
        Mus is a 4-player (1 human + 3 CPU) Basque/Spanish vying (betting) game on
        a 40-card Latin deck (A,2-7,Sota,Caballo,Rey × 4 suits). Players sit in two
        fixed teams: seats 0&2 (human is seat 0) vs seats 1&3. Each player holds 4 cards.

        **Mus phase**: each player declares Mus (wants to exchange cards) or Corte
        (start betting). All four agreeing triggers a discard-and-redraw cycle, repeated
        up to 3 times. One Corte ends the phase immediately.

        **Betting rounds** (in order): Grande (highest hand) → Chica (lowest hand) →
        Pares (pairs; Duples>Medias>Par) → Juego (31+ card points; if nobody qualifies
        it becomes Punto = closest to 30).

        **Hand values for Juego**: Sota/Caballo/Rey=10, A=1, others=face. Best Juego
        rank: 31 > 32 > 40 > 39 > ... > 33.

        **Betting actions**: Paso (pass), Envido N (bet N points), Ordago (all-in;
        accepted = immediate showdown deciding the whole game), Quiero (accept bet),
        No Quiero (decline; bettor gets +1 amarrako). Both teams Paso defers +1 to showdown.

        **Scoring**: amarrakos; first team to reach the target (default 40) wins.

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Mus        | `0`   | Mus/Corte declaration phase |
        | Discard    | `1`   | Each player discards and redraws |
        | Grande     | `2`   | Betting on highest hand |
        | Chica      | `3`   | Betting on lowest hand |
        | Pares      | `4`   | Betting on pair combinations |
        | Juego      | `5`   | Betting on 31+ point hands (or Punto) |
        | Showdown   | `6`   | Hands revealed and amarrakos settled |
        | GameEnd    | `7`   | A team reached the target amarrakos |

        | Command         | Aliases      | Description |
        |-----------------|--------------|-------------|
        | `reset`         | `r`          | Start / restart the game (optional `config`) |
        | `mus`           | `m`          | Declare Mus (Mus phase) |
        | `corte`         | `c`, `cut`   | Declare Corte — start betting (Mus phase) |
        | `discard`       | `d`          | Discard cards by `cardIndices` (Discard phase) |
        | `paso`          |              | Pass this betting round |
        | `envido`        | `e`          | Bet N points (requires `amount`) |
        | `ordago`        |              | Bet the whole game |
        | `quiero`        |              | Accept the current bet |
        | `noquiero`      | `nq`         | Decline the current bet |
        | `next`          | `n`          | Advance to the next phase |
        | `setdifficulty` | `sd`         | Set CPU difficulty (requires `difficulty` 0-2) |
        | `log`           | `l`          | View action log |
      operationId: musExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MusRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              mus:
                summary: Declare Mus (want to exchange cards)
                value:
                  command: mus
                  sessionId: my-session-001
              corte:
                summary: Declare Corte (start betting)
                value:
                  command: corte
                  sessionId: my-session-001
              discard:
                summary: Discard cards at hand indices 0 and 2
                value:
                  command: discard
                  cardIndices: [0, 2]
                  sessionId: my-session-001
              envido:
                summary: Bet 5 points
                value:
                  command: envido
                  amount: 5
                  sessionId: my-session-001
              ordago:
                summary: Bet the whole game
                value:
                  command: ordago
                  sessionId: my-session-001
              quiero:
                summary: Accept the current bet
                value:
                  command: quiero
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MusResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MusResponse'

  /tute/exec:
    post:
      tags:
        - tute
      summary: Execute a Tute game command
      description: |
        Send a command to advance a Tute session identified by `sessionId`.
        Tute is a 4-player (1 human + 3 CPU) Spanish trick-taking game on a
        40-card Latin deck (A,2–7,Sota,Caballo,Rey × 4 suits).
        Each player receives 10 cards. Players sit in two fixed teams:
        seats 0&2 (human is seat 0) vs seats 1&3.
        The trump suit is set by the suit of the last card dealt.
        Must-follow trick play: follow the led suit if able; otherwise any card.
        Trick winner = highest trump if any trump played, else highest led-suit card.
        Rank order within a suit (strongest to weakest): A > 3 > Rey(K) > Caballo(Q) > Sota(J) > 7 > 6 > 5 > 4 > 2.
        Card points: A=11, 3=10, Rey=4, Caballo=3, Sota=2, others=0 (total 120).
        The last (10th) trick awards an additional +10 points.
        **Cante (marriage)**: on lead, a player holding the Rey and Caballo of the same suit
        may declare a cante — +20 pts for a non-trump suit, +40 pts for the trump suit.
        Each suit can be declared once per round.
        **Tute**: holding all four Reys OR all four Caballos allows a player to declare
        "Tute" for an immediate game win.
        Cumulative scores accumulate across rounds; first team to the target (default 121) wins.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Play      | `0`   | Ten tricks played under must-follow rules |
        | TrickEnd  | `1`   | Trick resolved; advance with `next` |
        | RoundEnd  | `2`   | Round scored; advance with `nextround` |
        | GameEnd   | `3`   | A team reached the target score |

        | Command           | Aliases                | Description |
        |-------------------|------------------------|-------------|
        | `reset`           | `r`                    | Start / restart the game (optional `config`) |
        | `play`            |                        | Play a card by `cardIndex` (Play phase) |
        | `marriage`        | `m`                    | Declare cante by `suit` while on lead (Play phase) |
        | `tute`            |                        | Declare Tute for an instant game win |
        | `next`            | `n`                    | Advance to the next trick |
        | `nextround`       | `nr`                   | Score the round and start the next |
        | `setdifficulty`   | `sd`                   | Set CPU difficulty (requires `difficulty` 0-2) |
        | `log`             | `l`                    | View action log |
      operationId: tuteExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TuteRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 3
                value:
                  command: play
                  cardIndex: 3
                  sessionId: my-session-001
              marriage:
                summary: Declare cante (marriage) in the hearts suit
                value:
                  command: marriage
                  suit: 3
                  sessionId: my-session-001
              tute:
                summary: Declare Tute for an instant win
                value:
                  command: tute
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TuteResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TuteResponse'

  /sueca/exec:
    post:
      tags:
        - sueca
      summary: Execute a Sueca game command
      description: |
        Send a command to advance a Sueca session identified by `sessionId`.
        Sueca is a 4-player (1 human + 3 CPU) Portuguese/Brazilian trick-taking game on a
        40-card Latin deck (A,2–7,Sota/Caballo/Rei × 4 suits).
        Each player receives 10 cards. Players sit in two fixed teams:
        seats 0&2 (human is seat 0) vs seats 1&3.
        The trump suit is set by the suit of the last card dealt.
        Must-follow trick play: follow the led suit if able; otherwise any card.
        Trick winner = highest trump if any trump played, else highest led-suit card.
        Rank order within a suit (strongest to weakest): A > 7 > Rei(K) > Valete/Sota(J) > Dama/Caballo(Q) > 6 > 5 > 4 > 3 > 2.
        Card points: A=11, 7=10, Rei=4, Valete/Sota=3, Dama/Caballo=2, others=0 (total 120).
        No marriages or special declarations — pure trick play over 10 tricks.
        Round scoring: the team with more card points wins; 61-90 points earns 1 game point,
        91-119 earns 2, 120 (all tricks) earns 4; 60-60 is a draw and neither team scores.
        Game points accumulate across rounds; the first team to the target (default 4) wins the match.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Play      | `0`   | Ten tricks played under must-follow rules |
        | TrickEnd  | `1`   | Trick resolved; advance with `next` |
        | RoundEnd  | `2`   | Round scored; advance with `nextround` |
        | GameEnd   | `3`   | A team reached the target game points |

        | Command           | Aliases                | Description |
        |-------------------|------------------------|-------------|
        | `reset`           | `r`                    | Start / restart the game (optional `config`) |
        | `play`            | `p`                    | Play a card by `cardIndex` (Play phase) |
        | `next`            | `n`                    | Advance to the next trick |
        | `nextround`       | `nr`                   | Score the round and start the next |
        | `setdifficulty`   | `sd`                   | Set CPU difficulty (requires `difficulty` 0-2) |
        | `log`             | `l`                    | View action log |
      operationId: suecaExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SuecaRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              next:
                summary: Advance to the next trick
                value:
                  command: next
                  sessionId: my-session-001
              nextround:
                summary: Score the round and continue
                value:
                  command: nextround
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuecaResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SuecaResponse'

  /klaverjas/exec:
    post:
      tags:
        - klaverjas
      summary: Execute a Klaverjas game command
      description: |
        Send a command to advance a Klaverjas session identified by `sessionId`.
        Klaverjas is a 4-player (1 human + 3 CPU) Dutch Jass-family trick-taking game on a
        32-card deck (7,8,9,10,J,Q,K,A × 4 suits).
        Each player receives 8 cards. Players sit in two fixed teams:
        seats 0&2 (human is seat 0) vs seats 1&3.
        The trump suit is set by the suit of the last card dealt.
        Jass ranking — trump strength (strongest to weakest): J(Jas) > 9(Nel) > A > 10 > K > Q > 8 > 7.
        Non-trump strength (strongest to weakest): A > 10 > K > Q > J > 9 > 8 > 7.
        Card points (trump): J=20, 9=14, A=11, 10=10, K=4, Q=3, 8=0, 7=0.
        Card points (non-trump): A=11, 10=10, K=4, Q=3, J=2, 9=0, 8=0, 7=0 (total 152 card points).
        The last (8th) trick awards an additional +10 points to the winning team.
        Klaverjas obligation: must follow led suit if able; if void must play a trump if held;
        if a trump is on the table must overtrump when able; only discard freely if no trump is held.
        Roem (melds) are auto-detected from each player's initial hand and awarded to their team:
        3-card same-suit run = 20, 4-card run = 50, four of a kind (excl. 7s/8s) = 100.
        Scores accumulate across rounds; the first team to reach the target (default 1501) wins the match.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Play      | `0`   | Eight tricks played under Klaverjas obligation rules |
        | TrickEnd  | `1`   | Trick resolved; advance with `next` |
        | RoundEnd  | `2`   | Round scored (card points + roem + last-trick bonus); advance with `nextround` |
        | GameEnd   | `3`   | A team reached the target score |

        | Command           | Aliases                | Description |
        |-------------------|------------------------|-------------|
        | `reset`           | `r`                    | Start / restart the game (optional `config`) |
        | `play`            | `p`                    | Play a card by `cardIndex` (Play phase) |
        | `next`            | `n`                    | Advance to the next trick |
        | `nextround`       | `nr`                   | Score the round and start the next |
        | `setdifficulty`   | `sd`                   | Set CPU difficulty (requires `difficulty` 0-2) |
        | `log`             | `l`                    | View action log |
      operationId: klaverjasExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/KlaverjasRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              next:
                summary: Advance to the next trick
                value:
                  command: next
                  sessionId: my-session-001
              nextround:
                summary: Score the round and continue
                value:
                  command: nextround
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KlaverjasResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KlaverjasResponse'

  /manille/exec:
    post:
      tags:
        - manille
      summary: Execute a Manille game command
      description: |
        Send a command to advance a Manille session identified by `sessionId`.
        Manille is a 4-player (1 human + 3 CPU) Belgian/French trick-taking game on a
        32-card deck (7,8,9,10,J,Q,K,A × 4 suits).
        Each player receives 8 cards. Players sit in two fixed teams:
        seats 0&2 (human is seat 0) vs seats 1&3.
        The trump suit is set by the suit of the last card dealt.
        Reversed rank (signature of Manille): within every suit, the strength order
        (strongest to weakest) is 10(Manille) > A(Manillon) > K > Q > J > 9 > 8 > 7.
        The same order applies inside the trump suit and inside each non-trump suit.
        Card points: 10=5, A=4, K=3, Q=2, J=1, 9=0, 8=0, 7=0 (total 60 across all suits).
        Must-follow + trump-conservation exemption: players must follow the led suit if able;
        if void and the partner is not already winning the trick, they must play a trump if held;
        if void and the partner is already winning the trick, they may keep their trumps and discard freely.
        No overtrump obligation. No melds, no bidding, no last-trick bonus.
        Card points accumulate across rounds; the first team to reach the target (default 101) wins the match.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Play      | `0`   | Eight tricks played under Manille follow rules |
        | TrickEnd  | `1`   | Trick resolved; advance with `next` |
        | RoundEnd  | `2`   | Round scored (card points); advance with `nextround` |
        | GameEnd   | `3`   | A team reached the target score |

        | Command           | Aliases                | Description |
        |-------------------|------------------------|-------------|
        | `reset`           | `r`                    | Start / restart the game (optional `config`) |
        | `play`            | `p`                    | Play a card by `cardIndex` (Play phase) |
        | `next`            | `n`                    | Advance to the next trick |
        | `nextround`       | `nr`                   | Score the round and start the next |
        | `setdifficulty`   | `sd`                   | Set CPU difficulty (requires `difficulty` 0-2) |
        | `log`             | `l`                    | View action log |
      operationId: manilleExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ManilleRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              next:
                summary: Advance to the next trick
                value:
                  command: next
                  sessionId: my-session-001
              nextround:
                summary: Score the round and continue
                value:
                  command: nextround
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ManilleResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ManilleResponse'

  /marias/exec:
    post:
      tags:
        - marias
      summary: Execute a Mariáš game command
      description: |
        Send a command to advance a Mariáš session identified by `sessionId`.
        Mariáš is a 3-player (1 human + 2 CPU) Czech/Slovak trick-taking game on a
        32-card German-suited deck (7,8,9,10,J,Q,K,A × 4 suits).
        Each player receives 10 cards; the remaining 2 form an unused talon (simplified implementation).
        No fixed teams: each round one Soloist (the forehand = dealer's left neighbour,
        rotates right each round) plays alone against the two Defenders.
        Trump is the Soloist's longest suit, auto-declared at deal (no bidding phase).
        Rank (same for trump and plain suits, strongest to weakest): A > 10 > K > Q > J > 9 > 8 > 7.
        Trump beats all non-trump cards.
        Card points: A=11, 10=10, K=4, Q=3, J=2, 9/8/7=0 (total 120 across all suits).
        Marriages: holding K+Q of the same suit at deal auto-scores 20 points (40 for the trump suit);
        detected at deal — no play-time declaration required (simplified implementation).
        Must-follow + forced trump when void: follow led suit if able; if void play a trump if held;
        otherwise discard freely. No overtrump obligation.
        Last-trick bonus: winner of the 10th trick gets +10 bonus points.
        Round result: Soloist's total (card points won + marriages + last-trick bonus if taken)
        vs Defenders' combined total; Soloist wins (gains 2 game points) if their total is higher,
        otherwise each Defender gains 2 game points.
        Match: first player to reach the target (default 10 game points) wins.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Play      | `0`   | Ten tricks played under Mariáš follow rules |
        | TrickEnd  | `1`   | Trick resolved; advance with `next` |
        | RoundEnd  | `2`   | Round scored; advance with `nextround` |
        | GameEnd   | `3`   | A player reached the target game points |

        | Command           | Aliases                | Description |
        |-------------------|------------------------|-------------|
        | `reset`           | `r`                    | Start / restart the game (optional `config`) |
        | `play`            | —                      | Play a card by `cardIndex` (Play phase) |
        | `next`            | `n`                    | Advance to the next trick |
        | `nextround`       | `nr`                   | Score the round and start the next |
        | `setdifficulty`   | `sd`                   | Set CPU difficulty (requires `difficulty` 0-2) |
        | `log`             | `l`                    | View action log |
      operationId: mariasExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MariasRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              next:
                summary: Advance to the next trick
                value:
                  command: next
                  sessionId: my-session-001
              nextround:
                summary: Score the round and continue
                value:
                  command: nextround
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MariasResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MariasResponse'

  /sedma/exec:
    post:
      tags:
        - sedma
      summary: Execute a Sedma game command
      description: |
        Send a command to advance a Sedma session identified by `sessionId`.
        Sedma is a 4-player (1 human + 3 CPU) Czech/Slovak capture trick-taking game on a
        32-card deck (7,8,9,10,J,Q,K,A × 4 suits).
        Each player receives 8 cards. Players sit in two fixed teams:
        seats 0&2 (human is seat 0) vs seats 1&3.
        There is no trump suit.
        Any card may be played freely — there is no follow obligation.
        A card captures the trick if its rank matches the lead card's rank,
        OR it is a 7 (wild — captures any trick regardless of the lead rank).
        The last player in play order to capture wins the trick;
        if nobody after the lead captures, the lead player wins.
        The trick winner leads the next trick.
        Card points: A=10, 10=10, all others=0 (total 80 across all 4 suits).
        The winner of the 8th (final) trick earns a +10 bonus.
        Card points accumulate across rounds; the first team to reach the target (default 101) wins the match.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Play      | `0`   | Eight tricks played under Sedma capture rules |
        | TrickEnd  | `1`   | Trick resolved; advance with `next` |
        | RoundEnd  | `2`   | Round scored (card points + last-trick bonus); advance with `nextround` |
        | GameEnd   | `3`   | A team reached the target score |

        | Command           | Aliases                | Description |
        |-------------------|------------------------|-------------|
        | `reset`           | `r`                    | Start / restart the game (optional `config`) |
        | `play`            | `p`                    | Play a card by `cardIndex` (Play phase) |
        | `next`            | `n`                    | Advance to the next trick |
        | `nextround`       | `nr`                   | Score the round and start the next |
        | `setdifficulty`   | `sd`                   | Set CPU difficulty (requires `difficulty` 0-2) |
        | `log`             | `l`                    | View action log |
      operationId: sedmaExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SedmaRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              next:
                summary: Advance to the next trick
                value:
                  command: next
                  sessionId: my-session-001
              nextround:
                summary: Score the round and continue
                value:
                  command: nextround
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SedmaResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SedmaResponse'

  /solowhist/exec:
    post:
      tags:
        - solowhist
      summary: Execute a Solo Whist game command
      description: |
        Send a command to advance a Solo Whist session identified by `sessionId`.
        Solo Whist is a 4-player (1 human + 3 CPU) British trick-taking game on a standard
        52-card deck (A,2-10,J,Q,K × 4 suits). Each player is dealt 13 cards and 13 tricks
        are played per round.
        Rank (strongest to weakest): A > K > Q > J > 10 > 9 > 8 > 7 > 6 > 5 > 4 > 3 > 2.
        Bidding phase: starting from the dealer's left, each player bids once choosing from
        Pass / Solo (≥8 tricks, value 2) / Misère (0 tricks, no trump, value 3) /
        Abundance (≥9 tricks, value 4). A non-pass bid must outrank the current highest
        (Pass < Solo < Misère < Abundance). The highest bidder becomes the Declarer and
        plays alone against the three Defenders. If everyone passes, the round is void and
        redealt. The traditional Prop & Cop two-player alliance is omitted.
        Trump: for Solo and Abundance the Declarer's longest suit is auto-declared as trump
        (simplified). Misère has no trump.
        Must-follow: players must follow the led suit if able; otherwise any card may be played.
        Highest trump wins; if no trump, highest card of the led suit wins.
        Scoring: Declarer fulfils contract → gains contract value game points;
        Declarer fails → each Defender gains contract value game points.
        Match: first player to reach the target (default 21 game points) wins.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Bid       | `0`   | Bidding phase; each player bids once |
        | Play      | `1`   | Thirteen tricks played under must-follow rules |
        | TrickEnd  | `2`   | Trick resolved; advance with `next` |
        | RoundEnd  | `3`   | Round scored; advance with `nextround` |
        | GameEnd   | `4`   | A player reached the target game points |

        | Command           | Aliases                | Description |
        |-------------------|------------------------|-------------|
        | `reset`           | `r`                    | Start / restart the game (optional `config`) |
        | `bid`             | —                      | Place a bid (requires `bidType` 0=Pass 1=Solo 2=Misère 3=Abundance) |
        | `pass`            | —                      | Pass the bid (equivalent to `bid` with bidType 0) |
        | `play`            | —                      | Play a card by `cardIndex` (Play phase) |
        | `next`            | `n`                    | Advance to the next trick |
        | `nextround`       | `nr`                   | Score the round and start the next |
        | `setdifficulty`   | `sd`                   | Set CPU difficulty (requires `difficulty` 0-2) |
        | `log`             | `l`                    | View action log |
      operationId: soloWhistExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SoloWhistRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Bid Solo (bidType 1)
                value:
                  command: bid
                  bidType: 1
                  sessionId: my-session-001
              pass:
                summary: Pass the bid
                value:
                  command: pass
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              next:
                summary: Advance to the next trick
                value:
                  command: next
                  sessionId: my-session-001
              nextround:
                summary: Score the round and continue
                value:
                  command: nextround
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SoloWhistResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SoloWhistResponse'

  /knockoutwhist/exec:
    post:
      tags:
        - knockoutwhist
      summary: Execute a Knockout Whist game command
      description: |
        Send a command to advance a Knockout Whist session identified by `sessionId`.
        Knockout Whist is a 4-player (1 human + 3 CPU) British survival trick-taking game
        on a standard 52-card deck (A,2-10,J,Q,K × 4 suits).
        Hand size decreases each round: round 1 deals 7 cards, round 2 deals 6, …,
        round 7 deals 1 (handSize = 8 − roundNumber).
        Rank (highest to lowest): A > K > Q > J > 10 > 9 > 8 > 7 > 6 > 5 > 4 > 3 > 2.
        Trump is determined automatically from the previous round winner's longest suit;
        the round-1 leader is the player to the dealer's left.
        Must-follow: players must follow the led suit if able; otherwise any card may be played.
        Highest trump wins; if no trump was played, the highest card of the led suit wins.
        Dogbone rule: a player who wins zero tricks in a round must spend one Dogbone token
        (each player starts with 1) to survive; if no token remains the player is eliminated.
        The last player remaining wins (or the winner of the final 1-card round).

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Play       | `0`   | Tricks played under must-follow rules |
        | TrickEnd   | `1`   | Trick resolved; advance with `next` |
        | RoundEnd   | `2`   | Round scored; apply Dogbone/elimination; advance with `nextround` |
        | GameEnd    | `3`   | One player remains (or final round winner determined) |

        | Command           | Aliases                | Description |
        |-------------------|------------------------|-------------|
        | `reset`           | `r`                    | Start / restart the game (optional `config`) |
        | `play`            | —                      | Play a card by `cardIndex` (Play phase) |
        | `next`            | `n`                    | Advance to the next trick |
        | `nextround`       | `nr`                   | Score the round and start the next |
        | `setdifficulty`   | `sd`                   | Set CPU difficulty (requires `difficulty` 0-2) |
        | `log`             | `l`                    | View action log |
      operationId: knockoutWhistExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/KnockoutWhistRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              next:
                summary: Advance to the next trick
                value:
                  command: next
                  sessionId: my-session-001
              nextround:
                summary: Score the round and continue
                value:
                  command: nextround
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KnockoutWhistResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KnockoutWhistResponse'

  /nap/exec:
    post:
      tags:
        - nap
      summary: Execute a Nap (Napoleon) game command
      description: |
        Send a command to advance a Nap session identified by `sessionId`.
        Nap (Napoleon) is a 4-player (1 human + 3 CPU) British trick-taking gambling game on a
        standard 52-card deck (A,2-10,J,Q,K × 4 suits). Each player is dealt 5 cards and
        5 tricks are played per round.
        Rank (strongest to weakest): A > K > Q > J > 10 > 9 > 8 > 7 > 6 > 5 > 4 > 3 > 2.
        Bidding phase: starting from the dealer's left, each player bids once choosing from
        Pass / Two (≥2 tricks, value 2) / Three (≥3 tricks, value 3) /
        Four (≥4 tricks, value 4) / Nap (all 5 tricks, success value 10, failure value 5).
        A non-pass bid must outrank the current highest (Pass < Two < Three < Four < Nap).
        The highest bidder becomes the Declarer and plays alone against the three Defenders.
        If everyone passes, the round is void and redealt.
        Trump: the Declarer's longest suit is auto-declared as trump (simplified).
        The Declarer leads the first trick of each round.
        Must-follow: players must follow the led suit if able; otherwise any card may be played.
        Highest trump wins; if no trump, highest card of the led suit wins.
        Chip scoring: Declarer wins at least bid tricks → gains contract value chips
        (Two=2, Three=3, Four=4, Nap=10); Declarer fails → each Defender gains contract value
        (Two/Three/Four) or 5 chips (failed Nap).
        Match: first player to reach the target (default 20 chips) wins.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Bid       | `0`   | Bidding phase; each player bids once |
        | Play      | `1`   | Five tricks played under must-follow rules |
        | TrickEnd  | `2`   | Trick resolved; advance with `next` |
        | RoundEnd  | `3`   | Round scored; advance with `nextround` |
        | GameEnd   | `4`   | A player reached the target chip count |

        | Command           | Aliases                | Description |
        |-------------------|------------------------|-------------|
        | `reset`           | `r`                    | Start / restart the game (optional `config`) |
        | `bid`             | —                      | Place a bid (requires `bidType` 0=Pass 2=Two 3=Three 4=Four 5=Nap) |
        | `pass`            | —                      | Pass the bid (equivalent to `bid` with bidType 0) |
        | `play`            | —                      | Play a card by `cardIndex` (Play phase) |
        | `next`            | `n`                    | Advance to the next trick |
        | `nextround`       | `nr`                   | Score the round and start the next |
        | `setdifficulty`   | `sd`                   | Set CPU difficulty (requires `difficulty` 0-2) |
        | `log`             | `l`                    | View action log |
      operationId: napExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/NapRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Bid Three (bidType 3)
                value:
                  command: bid
                  bidType: 3
                  sessionId: my-session-001
              pass:
                summary: Pass the bid
                value:
                  command: pass
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              next:
                summary: Advance to the next trick
                value:
                  command: next
                  sessionId: my-session-001
              nextround:
                summary: Score the round and continue
                value:
                  command: nextround
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NapResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/NapResponse'

  /preference/exec:
    post:
      tags:
        - preference
      summary: Execute a Préférence game command
      description: |
        Send a command to advance a Préférence session identified by `sessionId`.
        Préférence is a 3-player (1 human + 2 CPU) Central/Eastern European trick-taking game
        on a 32-card deck (7,8,9,10,J,Q,K,A × 4 suits). Each player is dealt 10 cards and
        10 tricks are played per round.
        Rank (strongest to weakest): A > K > Q > J > 10 > 9 > 8 > 7.
        Bidding phase: starting from the dealer's left, each player bids once choosing from
        Pass / Six (≥6 tricks, value 6) / Misère (0 tricks, no trump, value 10) /
        Seven (≥7 tricks, value 7) / Eight (≥8 tricks, value 8).
        A non-pass bid must outrank the current highest (Pass < Six < Misère < Seven < Eight).
        The highest bidder becomes the Declarer and plays alone against the two Defenders.
        If everyone passes, the round is void and redealt.
        The traditional 2-card talon (widow) exchange is omitted (simplified).
        Trump: for Six, Seven, and Eight the Declarer's longest suit is auto-declared as trump
        (simplified). Misère has no trump.
        Must-follow: players must follow the led suit if able; otherwise any card may be played.
        Highest trump wins; if no trump, highest card of the led suit wins.
        Scoring: Declarer fulfils contract → gains contract value game points
        (Six=6, Seven=7, Eight=8, Misère=10); Declarer fails → each Defender gains that value.
        Traditional pool/penalty bookkeeping is replaced by simple cumulative points (simplified).
        Match: first player to reach the target (default 30 game points) wins.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Bid       | `0`   | Bidding phase; each player bids once |
        | Play      | `1`   | Ten tricks played under must-follow rules |
        | TrickEnd  | `2`   | Trick resolved; advance with `next` |
        | RoundEnd  | `3`   | Round scored; advance with `nextround` |
        | GameEnd   | `4`   | A player reached the target game points |

        | Command           | Aliases                | Description |
        |-------------------|------------------------|-------------|
        | `reset`           | `r`                    | Start / restart the game (optional `config`) |
        | `bid`             | —                      | Place a bid (requires `bidType` 0=Pass 1=Six 2=Misère 3=Seven 4=Eight) |
        | `pass`            | —                      | Pass the bid (equivalent to `bid` with bidType 0) |
        | `play`            | —                      | Play a card by `cardIndex` (Play phase) |
        | `next`            | `n`                    | Advance to the next trick |
        | `nextround`       | `nr`                   | Score the round and start the next |
        | `setdifficulty`   | `sd`                   | Set CPU difficulty (requires `difficulty` 0-2) |
        | `log`             | `l`                    | View action log |
      operationId: preferenceExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PreferenceRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Bid Six (bidType 1)
                value:
                  command: bid
                  bidType: 1
                  sessionId: my-session-001
              pass:
                summary: Pass the bid
                value:
                  command: pass
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              next:
                summary: Advance to the next trick
                value:
                  command: next
                  sessionId: my-session-001
              nextround:
                summary: Score the round and continue
                value:
                  command: nextround
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PreferenceResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PreferenceResponse'

  /ganjifa/exec:
    post:
      tags:
        - ganjifa
      summary: Execute a Ganjifa game command
      description: |
        Send a command to advance a Ganjifa session identified by `sessionId`.
        Ganjifa is a 3-player (1 human + 2 CPU) Mughal-era Indian trick-taking game on a
        **circular 96-card deck** of 8 suits x 12 ranks. Each player is dealt 32 cards and
        32 tricks are played per round; there is no bidding.
        **The rank order inverts between suit groups**, which is what separates this game
        from every other trick-taker here:
        strong suits (design 1-4) rank high-to-low from 12 down to 1, while weak suits
        (design 5-8) rank the other way — 1 is the strongest card and 12 the weakest.
        The same "3" is third-from-bottom in a strong suit and third-from-top in a weak one,
        so comparing raw values gives the opposite answer in half the deck.
        Trump: the dealer's longest suit is auto-declared each round, so **whether trump is a
        strong or a weak suit changes how the hand should be read**; the response carries the
        trump suit and the UI states which group it belongs to.
        Must-follow: players must follow the led suit if able; otherwise any card may be played.
        Highest trump wins; if no trump, the strongest card of the led suit wins — "strongest"
        always meaning the group-aware order above.
        Scoring: each trick won is one point. After the configured number of rounds
        (default 3) the highest total wins; a tie leaves no winner.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
                | Play      | `0`   | Thirty-two tricks played under must-follow rules |
        | TrickEnd  | `1`   | Trick resolved; advance with `next` |
        | RoundEnd  | `2`   | Round scored; advance with `nextround` |
        | GameEnd   | `3`   | The configured number of rounds has been played |

        | Command           | Aliases                | Description |
        |-------------------|------------------------|-------------|
        | `reset`           | `r`                    | Start / restart the game (optional `config`) |
                        | `play`            | —                      | Play a card by `cardIndex` (Play phase) |
        | `next`            | `n`                    | Advance to the next trick |
        | `nextround`       | `nr`                   | Score the round and start the next |
        | `setdifficulty`   | `sd`                   | Set CPU difficulty (requires `difficulty` 0-2) |
        | `log`             | `l`                    | View action log |
      operationId: ganjifaExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GanjifaRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              next:
                summary: Advance to the next trick
                value:
                  command: next
                  sessionId: my-session-001
              nextround:
                summary: Score the round and continue
                value:
                  command: nextround
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GanjifaResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GanjifaResponse'
  /vira/exec:
    post:
      tags:
        - vira
      summary: Execute a Vira game command
      description: |
        Send a command to advance a Vira session identified by `sessionId`.
        Vira is a 3-player (1 human + 2 CPU) Swedish auction trick-taking game on a
        standard 52-card deck. Each player is dealt 13 cards and 13 tricks are played per
        round; the remaining 13-card talon is unused (simplified).
        Rank (strongest to weakest): A > K > Q > J > 10 > 9 > … > 2.
        Bidding phase: each player bids once choosing from
        Pass / Gask (≥7 tricks, value 2) / Solo (≥8 tricks, value 4) /
        Misère (0 tricks, no trump, value 6) / Vira (≥10 tricks, value 8).
        A non-pass bid must outrank the current highest (Pass < Gask < Solo < Misère < Vira).
        The historical bid table runs to more than 30 contracts; this implementation keeps the
        four principal ones (simplified).
        The highest bidder becomes the Declarer and plays alone against the two Defenders.
        If everyone passes the round is void — **the pot carries forward rather than being
        refunded**, which is the point of the pot format.
        Trump: for Gask, Solo and Vira the Declarer's longest suit is auto-declared as trump
        (simplified). Misère has no trump.
        Must-follow: players must follow the led suit if able; otherwise any card may be played.
        Settlement is by **pot**, which is what separates Vira from Préférence: every player
        antes 1 into the pot at the start of each round; a Declarer who makes the contract takes
        the whole pot and collects the contract value from each Defender; one who fails adds the
        contract value to the pot and pays each Defender the same. The pot therefore grows while
        contracts keep failing, and the next Declarer stands to win more.
        Match: after the configured number of rounds (default 6, a multiple of the player count
        so each player deals equally often) the highest score wins; a tie leaves no winner.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Bid       | `0`   | Bidding phase; each player bids once |
        | Play      | `1`   | Thirteen tricks played under must-follow rules |
        | TrickEnd  | `2`   | Trick resolved; advance with `next` |
        | RoundEnd  | `3`   | Round scored; advance with `nextround` |
        | GameEnd   | `4`   | The configured number of rounds has been played |

        | Command           | Aliases                | Description |
        |-------------------|------------------------|-------------|
        | `reset`           | `r`                    | Start / restart the game (optional `config`) |
        | `bid`             | —                      | Place a bid (requires `bidType` 0=Pass 1=Gask 2=Solo 3=Misère 4=Vira) |
        | `pass`            | —                      | Pass the bid (equivalent to `bid` with bidType 0) |
        | `play`            | —                      | Play a card by `cardIndex` (Play phase) |
        | `next`            | `n`                    | Advance to the next trick |
        | `nextround`       | `nr`                   | Score the round and start the next |
        | `setdifficulty`   | `sd`                   | Set CPU difficulty (requires `difficulty` 0-2) |
        | `log`             | `l`                    | View action log |
      operationId: viraExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ViraRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Bid Gask (bidType 1)
                value:
                  command: bid
                  bidType: 1
                  sessionId: my-session-001
              pass:
                summary: Pass the bid
                value:
                  command: pass
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              next:
                summary: Advance to the next trick
                value:
                  command: next
                  sessionId: my-session-001
              nextround:
                summary: Score the round and continue
                value:
                  command: nextround
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ViraResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ViraResponse'
  /spoilfive/exec:
    post:
      tags:
        - spoilfive
      summary: Execute a Spoil Five game command
      description: |
        Send a command to advance a Spoil Five session identified by `sessionId`.
        Spoil Five is a 5-player (1 human + 4 CPU) Irish traditional trick-taking game
        on a standard 52-card deck (A,2-10,J,Q,K × 4 suits).
        Each player is dealt 5 cards and 5 tricks are played per round.
        Trump is determined by the turn-up card from the remainder of the deck after dealing.
        Fixed top trumps (strongest to weakest): trump 5 (Five Fingers) > trump J >
        ♥A (Ace of Hearts, always a top trump regardless of trump suit) >
        trump A > trump K > trump Q > remaining trumps > led-suit cards.
        Reneging: the three top trumps (trump 5, trump J, ♥A) are exempt from must-follow
        and may be held back even when trump is led.
        Must-follow otherwise: follow the led suit if able; if void follow with a trump if able.
        Highest trump wins the trick; if no trump was played, highest card of the led suit wins.
        Race to 3: the first player to win 3 tricks in a round takes the pot (all player antes).
        Spoil: if no player reaches 3 tricks the pot carries over to the next round.
        Match: first player to reach the target (default 30 points) wins.
        Pip-order red/black rank reversal and Jink all-5-tricks bonus are omitted (simplified).

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Play       | `0`   | Tricks played under must-follow and reneging rules |
        | TrickEnd   | `1`   | Trick resolved; advance with `next` |
        | RoundEnd   | `2`   | Round scored (pot awarded or carried over); advance with `nextround` |
        | GameEnd    | `3`   | A player reached the target score |

        | Command           | Aliases                | Description |
        |-------------------|------------------------|-------------|
        | `reset`           | `r`                    | Start / restart the game (optional `config`) |
        | `play`            | —                      | Play a card by `cardIndex` (Play phase) |
        | `next`            | `n`                    | Advance to the next trick |
        | `nextround`       | `nr`                   | Score the round and start the next |
        | `setdifficulty`   | `sd`                   | Set CPU difficulty (requires `difficulty` 0-2) |
        | `log`             | `l`                    | View action log |
      operationId: spoilFiveExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SpoilFiveRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              next:
                summary: Advance to the next trick
                value:
                  command: next
                  sessionId: my-session-001
              nextround:
                summary: Score the round and continue
                value:
                  command: nextround
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpoilFiveResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpoilFiveResponse'

  /fortyfives/exec:
    post:
      tags:
        - fortyfives
      summary: Execute an Auction Forty-Fives game command
      description: |
        Send a command to advance an Auction Forty-Fives session identified by `sessionId`.
        Auction Forty-Fives is a 4-player 2-team (1 human + 3 CPU) Irish/Nova Scotian trick-taking
        game on a standard 52-card deck (A,2-10,J,Q,K × 4 suits).
        Teams are seats 0&2 (human is seat 0) vs seats 1&3.
        Each player is dealt 5 cards and 5 tricks are played per round.
        Bidding phase: starting from the dealer's left, each player bids once choosing from
        Pass / 15 / 20 / 25. A non-pass bid must outrank the current highest (15 < 20 < 25).
        The highest bidder's team becomes the bid team; the bidder's longest suit is
        auto-declared as trump (card-exchange step omitted).
        If everyone passes the round is void and redealt.
        Fixed top trumps (Spoil Five family, strongest to weakest regardless of rank):
        trump 5 (Five Fingers) > trump J (Knave of Trump) >
        ♥A (Ace of Hearts, always a top trump regardless of trump suit) >
        trump A > trump K > trump Q > other trumps > led-suit cards.
        Reneging: the three top trumps (trump 5, trump J, ♥A) are exempt from must-follow
        and may be held back even when trump is led.
        Must-follow otherwise: follow the led suit if able; if void any card may be played.
        Highest trump wins the trick; if no trump was played, highest card of the led suit wins.
        Trick winner leads the next trick.
        Scoring: each trick scores 5 points to the winning team (maximum 25 per round).
        Bid team adds its trick points if it meets the bid, else loses the bid value.
        Non-bid team always adds trick points won.
        Jink: bidding 25 and winning all 5 tricks is an instant match win.
        Match: first team to reach 45 points wins.
        Honor-card bonuses for trump J / ♥A and red/black pip-order rank reversal are omitted (simplified).

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Bid        | `0`   | Bidding phase; each player bids once |
        | Play       | `1`   | Five tricks played under must-follow and reneging rules |
        | TrickEnd   | `2`   | Trick resolved; advance with `next` |
        | RoundEnd   | `3`   | Round scored; advance with `nextround` |
        | GameEnd    | `4`   | A team reached 45 points or scored a Jink |

        | Command           | Aliases                | Description |
        |-------------------|------------------------|-------------|
        | `reset`           | `r`                    | Start / restart the game (optional `config`) |
        | `bid`             | —                      | Place a bid (requires `bidValue` 0=Pass 15 20 25) |
        | `pass`            | —                      | Pass the bid (equivalent to `bid` with bidValue 0) |
        | `play`            | —                      | Play a card by `cardIndex` (Play phase) |
        | `next`            | `n`                    | Advance to the next trick |
        | `nextround`       | `nr`                   | Score the round and start the next |
        | `setdifficulty`   | `sd`                   | Set CPU difficulty (requires `difficulty` 0-2) |
        | `log`             | `l`                    | View action log |
      operationId: fortyFivesExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FortyFivesRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Bid 20 points
                value:
                  command: bid
                  bidValue: 20
                  sessionId: my-session-001
              pass:
                summary: Pass the bid
                value:
                  command: pass
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              next:
                summary: Advance to the next trick
                value:
                  command: next
                  sessionId: my-session-001
              nextround:
                summary: Score the round and continue
                value:
                  command: nextround
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FortyFivesResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FortyFivesResponse'

  /twentynine/exec:
    post:
      tags:
        - twentynine
      summary: Execute a Twenty-Nine game command
      description: |
        Send a command to advance a Twenty-Nine session identified by `sessionId`.
        Twenty-Nine is a 4-player 2-team (1 human + 3 CPU) South Asian trick-taking
        game on a 32-card deck (7,8,9,10,J,Q,K,A × 4 suits).
        Teams are seats 0&2 (human is seat 0) vs seats 1&3.
        Each player is dealt 8 cards and 8 tricks are played per round.
        Bidding phase: starting from the dealer's left, each player bids once choosing from
        Pass / 16 / 20 / 24 / 28. A non-pass bid must outrank the current highest (16 < 20 < 24 < 28).
        The highest bidder's team becomes the bid team; the bidder's longest suit is
        auto-declared as trump.
        If everyone passes the round is void and redealt.
        Hidden trump: the trump suit is not revealed at the start of the round.
        It is exposed automatically the moment any player cannot follow the led suit
        and plays an off-suit card.
        Card rank within a suit (strongest to weakest): J > 9 > A > 10 > K > Q > 8 > 7.
        Trump beats all non-trump; within a suit this rank order decides.
        Must-follow: follow the led suit if able; if void any card may be played.
        Highest trump wins the trick; if no trump was played, highest card of the led suit wins.
        Trick winner leads the next trick.
        Card points: J=3, 9=2, A=1, 10=1 (28 total across the deck).
        The team that wins the last trick (trick 8) gains an additional +1 point,
        making the total contested points per deal 29.
        Scoring: if the bid team captures at least its bid in card points it wins the deal
        and scores 1 game point; otherwise the non-bid team scores 1 game point.
        Match: first team to reach 6 game points wins.

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Bid        | `0`   | Bidding phase; each player bids once |
        | Play       | `1`   | Eight tricks played under must-follow rules |
        | TrickEnd   | `2`   | Trick resolved; advance with `next` |
        | RoundEnd   | `3`   | Round scored; advance with `nextround` |
        | GameEnd    | `4`   | A team reached 6 game points |

        | Command           | Aliases                | Description |
        |-------------------|------------------------|-------------|
        | `reset`           | `r`                    | Start / restart the game (optional `config`) |
        | `bid`             | —                      | Place a bid (requires `bidValue` 0=Pass 16 20 24 28) |
        | `pass`            | —                      | Pass the bid (equivalent to `bid` with bidValue 0) |
        | `play`            | —                      | Play a card by `cardIndex` (Play phase) |
        | `next`            | `n`                    | Advance to the next trick |
        | `nextround`       | `nr`                   | Score the round and start the next |
        | `setdifficulty`   | `sd`                   | Set CPU difficulty (requires `difficulty` 0-2) |
        | `log`             | `l`                    | View action log |
      operationId: twentyNineExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TwentyNineRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Bid 20 points
                value:
                  command: bid
                  bidValue: 20
                  sessionId: my-session-001
              pass:
                summary: Pass the bid
                value:
                  command: pass
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              next:
                summary: Advance to the next trick
                value:
                  command: next
                  sessionId: my-session-001
              nextround:
                summary: Score the round and continue
                value:
                  command: nextround
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TwentyNineResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TwentyNineResponse'

  /courtpiece/exec:
    post:
      tags:
        - courtpiece
      summary: Execute a Court Piece game command
      description: |
        Send a command to advance a Court Piece session identified by `sessionId`.
        Court Piece / Rang is a 4-player 2-team (1 human + 3 CPU) Pakistani/Iranian
        trick-taking game on a standard 52-card deck (2-10,J,Q,K,A × 4 suits).
        Teams are seats 0&2 (human is seat 0) vs seats 1&3.
        Each player is dealt 13 cards and 13 tricks are played per round.
        Trump declaration: the caller (Hakim) is dealt the first 5 cards, peeks at them,
        and immediately declares a trump suit (1=Spades, 2=Clubs, 3=Hearts, 4=Diamonds).
        The remaining cards are then dealt and the caller leads the first trick.
        Card rank within a suit (strongest to weakest): A > K > Q > J > 10 > … > 2.
        Trump beats all non-trump; within a suit this rank order decides.
        Must-follow: follow the led suit if able; if void any card may be played.
        Highest trump wins the trick; if no trump was played, highest card of the led suit wins.
        Trick winner leads the next trick.
        Scoring: a team that takes 7 or more of the 13 tricks wins the round (Sar) and scores 1 point.
        Winning consecutive rounds, or sweeping all 13 tricks, earns a Court bonus (+2 points).
        The winning team keeps the caller role; on a loss the caller passes to the next seat.
        Match: first team to reach the point limit (default 7) wins.

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Trump      | `0`   | Caller declares the trump suit |
        | Play       | `1`   | Thirteen tricks played under must-follow rules |
        | TrickEnd   | `2`   | Trick resolved; advance with `next` |
        | RoundEnd   | `3`   | Round scored; advance with `nextround` |
        | GameEnd    | `4`   | A team reached the point limit |

        | Command           | Aliases                | Description |
        |-------------------|------------------------|-------------|
        | `reset`           | `r`                    | Start / restart the game (optional `config`) |
        | `trump`           | `t`                    | Declare trump (requires `trumpSuit` 1-4; caller only) |
        | `play`            | `p`                    | Play a card by `cardIndex` (Play phase) |
        | `next`            | `n`                    | Advance to the next trick |
        | `nextround`       | `nr`                   | Score the round and start the next |
        | `hint`            | `h`                    | Request a play hint |
        | `setdifficulty`   | `sd`                   | Set CPU difficulty (requires `difficulty` 0-2) |
        | `setlimit`        | `sl`                   | Set the match point limit (requires `limit`) |
        | `log`             | `l`                    | View action log |
      operationId: courtPieceExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CourtPieceRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              trump:
                summary: Declare hearts as trump
                value:
                  command: trump
                  trumpSuit: 3
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              next:
                summary: Advance to the next trick
                value:
                  command: next
                  sessionId: my-session-001
              nextround:
                summary: Score the round and continue
                value:
                  command: nextround
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CourtPieceResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CourtPieceResponse'

  /bezique/exec:
    post:
      tags:
        - bezique
      summary: Execute a Bezique game command
      description: |
        Send a command to advance a Bezique session identified by `sessionId`.
        Bezique is a 2-player (1 human vs 1 CPU) French declaration trick-taking game,
        the direct ancestor of Pinochle, on a 64-card deck (A,7,8,9,10,J,Q,K × 4 suits × 2 copies).
        Each player is dealt 8 cards; the next card off the stock is turned up to set the trump suit,
        and the rest forms the stock.
        Trick rank within a suit (strongest to weakest): A > 10 > K > Q > J > 9 > 8 > 7.
        Trump beats all non-trump; within a suit this rank order decides.
        Phase 1 (while the stock lasts): tricks have no follow requirement; after winning a trick,
        the winner may declare one meld for points (or decline), then both players draw 1 card (winner first).
        Melds: Marriage (K+Q same suit)=20, Royal Marriage (trump K+Q)=40, Bezique (Queen of Spades + Jack of Diamonds)=40,
        four Aces=100, four Kings=80, four Queens=60, four Jacks=40.
        Phase 2 (after the stock is exhausted): strict must-follow (follow the led suit, win if able,
        otherwise trump if void); no melds; the winner of the last trick gains +10.
        Captured card points: A=11, 10=10, K=4, Q=3, J=2, 9/8/7=0.
        Scoring: meld points, card points, and the last-trick bonus accumulate across deals;
        the first player to reach the target (default 1000) wins.

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Play       | `0`   | Play a card to the current trick |
        | Meld       | `1`   | Trick winner declares one meld or skips (Phase 1 only) |
        | TrickEnd   | `2`   | Trick resolved; advance with `next` |
        | RoundEnd   | `3`   | Deal scored; advance with `nextround` |
        | GameEnd    | `4`   | A player reached the target score |

        | Command           | Aliases                | Description |
        |-------------------|------------------------|-------------|
        | `reset`           | `r`                    | Start / restart the game (optional `config`) |
        | `play`            | `p`                    | Play a card by `cardIndex` (Play phase) |
        | `meld`            | `m`                    | Declare a meld by `meldIndex` (Meld phase; winner only) |
        | `skip`            | `s`                    | Decline to declare a meld (Meld phase) |
        | `next`            | `n`, `nextround`       | Advance to the next trick or deal |
        | `hint`            | `h`                    | Request a play hint |
        | `setdifficulty`   | `sd`                   | Set CPU difficulty (requires `difficulty` 0-2) |
        | `settarget`       | `st`                   | Set the target score (requires `target`) |
        | `log`             | `l`                    | View action log |
      operationId: beziqueExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BeziqueRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 2
                value:
                  command: play
                  cardIndex: 2
                  sessionId: my-session-001
              meld:
                summary: Declare the meld at list index 0
                value:
                  command: meld
                  meldIndex: 0
                  sessionId: my-session-001
              skip:
                summary: Decline to declare a meld
                value:
                  command: skip
                  sessionId: my-session-001
              next:
                summary: Advance to the next trick or deal
                value:
                  command: next
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BeziqueResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BeziqueResponse'

  /ecarte/exec:
    post:
      tags:
        - ecarte
      summary: Execute an Écarté game command
      description: |
        Send a command to advance an Écarté session identified by `sessionId`.
        Écarté is a 2-player (1 human vs 1 CPU) French trick-taking game on a
        32-card deck (A,7,8,9,10,J,Q,K × 4 suits). Each player is dealt 5 cards;
        the next card off the stock is turned up to set the trump suit (if it is a
        King the dealer scores +1), and the rest forms the stock.
        Trick rank within a suit (strongest to weakest): K > Q > J > A > 10 > 9 > 8 > 7.
        Trump beats all non-trump; within a suit this rank order decides.
        Exchange phase: the elder (non-dealer) proposes an exchange or stands; if proposed,
        the dealer accepts or refuses. On accept, both players discard any number of cards and
        draw replacements from the stock, then the elder again proposes or stands; this repeats
        until the elder stands, the dealer refuses, or the stock is exhausted. If the dealer
        refuses and then fails to take 3 tricks that deal, the elder gains +1 (refusal penalty).
        Play phase: 5 tricks with strict must-follow under trump (follow the led suit, win if able,
        otherwise trump if void). Holding the trump King in hand scores +1 (declared automatically).
        Scoring: taking 3 or 4 tricks = 1 point; sweeping all 5 tricks (the Vole) = 2 points.
        Match scores accumulate across deals; the first player to reach the target (default 5) wins.

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Exchange   | `0`   | Elder proposes or stands; dealer accepts or refuses; discard and redraw |
        | Play       | `1`   | Play a card to the current trick |
        | RoundEnd   | `2`   | Deal scored; advance with `nextround` |
        | GameEnd    | `3`   | A player reached the target score |

        | Command           | Aliases                | Description |
        |-------------------|------------------------|-------------|
        | `reset`           | `r`                    | Start / restart the game (optional `config`) |
        | `propose`         | `pr`                   | Propose an exchange (elder; Exchange phase) |
        | `stand`           | `st`                   | Decline to exchange and play as dealt (elder; Exchange phase) |
        | `accept`          | `a`                    | Accept the proposed exchange (dealer; Exchange phase) |
        | `refuse`          | `rf`                   | Refuse the proposed exchange (dealer; Exchange phase) |
        | `discard`         | `d`                    | Discard cards by `discardIndices` and redraw (after accept) |
        | `play`            | `p`                    | Play a card by `cardIndex` (Play phase) |
        | `next`            | `n`, `nextround`       | Advance to the next trick or deal |
        | `hint`            | `h`                    | Request a play hint |
        | `setdifficulty`   | `sd`                   | Set CPU difficulty (requires `difficulty` 0-2) |
        | `settarget`       | `tg`                   | Set the target score (requires `target`) |
        | `log`             | `l`                    | View action log |
      operationId: ecarteExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EcarteRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              propose:
                summary: Propose an exchange (elder)
                value:
                  command: propose
                  sessionId: my-session-001
              accept:
                summary: Accept the proposed exchange (dealer)
                value:
                  command: accept
                  accept: true
                  sessionId: my-session-001
              discard:
                summary: Discard the cards at hand indices 3 and 4 and redraw
                value:
                  command: discard
                  discardIndices: [3, 4]
                  sessionId: my-session-001
              play:
                summary: Play the card at hand index 0
                value:
                  command: play
                  cardIndex: 0
                  sessionId: my-session-001
              next:
                summary: Advance to the next trick or deal
                value:
                  command: next
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EcarteResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/EcarteResponse'

  /threecardbrag/exec:
    post:
      tags:
        - threecardbrag
      summary: Execute a Three Card Brag game command
      description: |
        Send a command to advance a Three Card Brag session identified by `sessionId`.
        Three Card Brag is a 4-player (1 human + 3 CPU) English vying (betting) game on a
        standard 52-card deck and one of the ancestors of poker. Each player antes into the
        pot and is dealt 3 cards. Every player is either Blind (has not looked at their hand)
        or Seen (has looked); a Blind player may `see` their hand at any time to become Seen.
        Blind players stake at half the Seen rate (implementation: Blind = stake, Seen = 2 x stake).
        Betting phase: on your turn you may `bet` (call the current stake), `raise` (increase the
        stake), or `fold` (drop out, forfeiting your contribution). When only two players remain,
        a Seen player may pay 2 x stake to call a `show` (showdown): the stronger hand takes the pot
        and on a tie the player who called the show loses. If everyone but one player folds, the last
        remaining player wins the pot without a showdown.
        Hand ranking (strongest to weakest): Prial (three of a kind) > Running Flush (straight flush)
        > Run (straight) > Flush > Pair > High Card. Special cases: 3-3-3 is the highest Prial and
        A-2-3 is the highest Run. A player whose chips run out is eliminated; the last player still
        holding chips is the match winner.

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Betting    | `0`   | See, bet, raise, fold; or show when two players remain |
        | Showdown   | `1`   | Hands compared; the stronger hand takes the pot |
        | RoundEnd   | `2`   | Deal scored; advance with `nextround` |
        | GameEnd    | `3`   | Only one player still holds chips |

        | Command           | Aliases                | Description |
        |-------------------|------------------------|-------------|
        | `reset`           | `r`                    | Start / restart the game (optional `config`) |
        | `see`             | `s`                    | Look at your hand and become Seen (Blind only) |
        | `bet`             | `b`                    | Call the current stake |
        | `raise`           | `rs`                   | Raise the stake by `raiseStake`, then call |
        | `fold`            | `f`                    | Drop out of the current deal |
        | `show`            | `sh`                   | Call a showdown (Seen player, two players remaining) |
        | `next`            | `n`, `nextround`       | Advance to the next deal |
        | `hint`            | `h`                    | Request an action hint |
        | `setdifficulty`   | `sd`                   | Set CPU difficulty (requires `difficulty` 0-2) |
        | `setante`         | `sa`                   | Set the ante (requires `ante`) |
        | `setchips`        | `sc`                   | Set the starting chips (requires `startingChips`) |
        | `log`             | `l`                    | View action log |
      operationId: threeCardBragExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ThreeCardBragRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              see:
                summary: Look at your hand (become Seen)
                value:
                  command: see
                  sessionId: my-session-001
              bet:
                summary: Call the current stake
                value:
                  command: bet
                  sessionId: my-session-001
              raise:
                summary: Raise the stake by 10, then call
                value:
                  command: raise
                  raiseStake: 10
                  sessionId: my-session-001
              fold:
                summary: Drop out of the current deal
                value:
                  command: fold
                  sessionId: my-session-001
              show:
                summary: Call a showdown (two players remaining)
                value:
                  command: show
                  sessionId: my-session-001
              next:
                summary: Advance to the next deal
                value:
                  command: next
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThreeCardBragResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ThreeCardBragResponse'
  /teenpatti/exec:
    post:
      tags:
        - teenpatti
      summary: Execute a Teen Patti game command
      description: |
        Send a command to advance a Teen Patti session identified by `sessionId`.
        Teen Patti is the Indian version of Three Card Brag, a 4-player (1 human + 3 CPU)
        vying (betting) game on a standard 52-card deck. Each player antes the boot into the
        pot and is dealt 3 cards. Every player is either Blind (has not looked at their hand)
        or Seen (has looked); a Blind player may `see` their hand at any time to become Seen.
        Blind players stake at half the Seen rate (implementation: Blind = stake, Seen = 2 x stake).
        Betting phase: on your turn you may `bet` (call the current stake), `raise` (increase the
        stake), or `fold` (drop out, forfeiting your contribution). Side show (Teen Patti's
        distinguishing mechanic): when three or more players remain, a Seen player may `sideshow`
        (request a private hand comparison) against the previous Seen player, who may `accept`
        (the weaker of the two folds; on a tie the requester folds) or `decline` (play continues).
        When only two players remain, a Seen player may pay 2 x stake to call a `show` (showdown):
        the stronger hand takes the pot and on a tie the player who called the show loses. If
        everyone but one player folds, the last remaining player wins the pot without a showdown.
        Hand ranking (strongest to weakest): Trail/Trio (three of a kind) > Pure Sequence
        (straight flush) > Sequence (straight) > Color (flush) > Pair > High Card, sharing Three
        Card Brag's hand evaluation. A player whose chips run out is eliminated; the last player
        still holding chips is the match winner.

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Betting    | `0`   | See, bet, raise, fold; sideshow when three or more remain; show when two remain |
        | SideShow   | `1`   | A side show was requested; the target accepts or declines |
        | Showdown   | `2`   | Hands compared; the stronger hand takes the pot |
        | RoundEnd   | `3`   | Deal scored; advance with `nextround` |
        | GameEnd    | `4`   | Only one player still holds chips |

        | Command           | Aliases                | Description |
        |-------------------|------------------------|-------------|
        | `reset`           | `r`                    | Start / restart the game (optional `config`) |
        | `see`             | `s`                    | Look at your hand and become Seen (Blind only) |
        | `bet`             | `b`                    | Call the current stake |
        | `raise`           | `rs`                   | Raise the stake by `raiseStake`, then call |
        | `fold`            | `f`                    | Drop out of the current deal |
        | `sideshow`        | `ss`                   | Request a side show vs the previous Seen player (three or more remaining) |
        | `accept`          | `ac`                   | Accept a side show request |
        | `decline`         | `dc`                   | Decline a side show request |
        | `show`            | `sh`                   | Call a showdown (Seen player, two players remaining) |
        | `next`            | `n`, `nextround`       | Advance to the next deal |
        | `hint`            | `h`                    | Request an action hint |
        | `setdifficulty`   | `sd`                   | Set CPU difficulty (requires `difficulty` 0-2) |
        | `setante`         | `sa`                   | Set the ante / boot (requires `ante`) |
        | `setchips`        | `sc`                   | Set the starting chips (requires `startingChips`) |
        | `log`             | `l`                    | View action log |
      operationId: teenPattiExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/TeenPattiRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              see:
                summary: Look at your hand (become Seen)
                value:
                  command: see
                  sessionId: my-session-001
              bet:
                summary: Call the current stake
                value:
                  command: bet
                  sessionId: my-session-001
              raise:
                summary: Raise the stake by 10, then call
                value:
                  command: raise
                  raiseStake: 10
                  sessionId: my-session-001
              fold:
                summary: Drop out of the current deal
                value:
                  command: fold
                  sessionId: my-session-001
              sideshow:
                summary: Request a side show vs the previous Seen player
                value:
                  command: sideshow
                  sessionId: my-session-001
              accept:
                summary: Accept a side show request
                value:
                  command: accept
                  sessionId: my-session-001
              decline:
                summary: Decline a side show request
                value:
                  command: decline
                  sessionId: my-session-001
              show:
                summary: Call a showdown (two players remaining)
                value:
                  command: show
                  sessionId: my-session-001
              next:
                summary: Advance to the next deal
                value:
                  command: next
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeenPattiResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TeenPattiResponse'
  /spoons/exec:
    post:
      tags:
        - spoons
      summary: Execute a Spoons game command
      description: |
        Send a command to advance a Spoons session identified by `sessionId`.
        Spoons is a 4-player (1 human + 3 CPU) American pass-and-grab speed game on a
        standard 52-card deck. Each player is dealt 4 cards and one fewer spoon than the
        number of players is placed in the centre. During the Pass phase a feeder draws
        from the draw pile and every player passes one card to the next player, trying to
        collect four of a kind. The instant any player completes four of a kind they grab a
        spoon and the grab window opens; everyone races to grab one of the remaining spoons
        so exactly one player is left without one. The player who misses out gains a letter
        in the sequence S-P-O-O-N-S, and collecting all six letters eliminates that player.
        The last player standing wins.

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Pass     | `0`   | Players pass one card to the next player each turn |
        | Grab     | `1`   | Four of a kind was made; everyone races to grab a spoon |
        | RoundEnd | `2`   | A player missed out and gained a letter; advance with `next` |
        | GameEnd  | `3`   | Only one player remains |

        | Command         | Aliases | Description |
        |-----------------|---------|-------------|
        | `reset`         | `r`     | Start / restart the game (optional `config`) |
        | `pass`          | `p`     | Pass the card at `cardIndex` to the next player |
        | `grab`          | `g`     | Grab a spoon (while the grab window is open) |
        | `next`          | `n`     | Advance to the next round |
        | `log`           | `l`     | View action log |
      operationId: spoonsExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SpoonsRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              pass:
                summary: Pass the card at index 2
                value:
                  command: pass
                  cardIndex: 2
                  sessionId: my-session-001
              grab:
                summary: Grab a spoon
                value:
                  command: grab
                  sessionId: my-session-001
              next:
                summary: Advance to the next round
                value:
                  command: next
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpoonsResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SpoonsResponse'

  /kemps/exec:
    post:
      tags:
        - kemps
      summary: Execute a Kemps game command
      description: |
        Send a command to advance a Kemps session identified by `sessionId`.
        Kemps is a 4-player (1 human + 3 CPU), 2-team matching game on a standard
        52-card deck built around secret signals. Each player holds 4 cards and four
        cards sit face-up on a shared field. During the Exchange phase a player swaps
        one hand card for a field card (or passes), racing to collect four of a kind.
        The human sets a secret signal type (0=Sound, 1=Blink). When a team completes
        four of a kind the Declare window opens: the team may declare Kemps for a point,
        while the opposing team may call Counter-Kemps against a seat to steal it
        (+1 on a correct read, -1 if wrong). Even seats form Team A (with the human at
        seat 0); the human partner is seat 2. The first team to the target score
        (default 5) wins.

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Exchange | `0`   | Players swap a hand card for a field card each turn |
        | Declare  | `1`   | Four of a kind was made; declare Kemps or Counter-Kemps |
        | RoundEnd | `2`   | The round was scored; advance with `next` |
        | GameEnd  | `3`   | A team reached the target score |

        | Command         | Aliases | Description |
        |-----------------|---------|-------------|
        | `reset`         | `r`     | Start / restart the game (optional `config`) |
        | `swap`          | `s`     | Swap hand card `handIndex` with field card `fieldIndex` |
        | `pass`          | `p`     | Skip the swap (or decline in the declare window) |
        | `signal`        | `sig`   | Set the human secret signal type (`signalType` 0=Sound, 1=Blink) |
        | `kemps`         | `k`     | Declare Kemps |
        | `counter`       | `c`     | Declare Counter-Kemps against opponent `targetSeat` |
        | `next`          | `n`     | Advance to the next round |
        | `log`           | `l`     | View action log |
      operationId: kempsExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/KempsRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              swap:
                summary: Swap hand card 1 with field card 2
                value:
                  command: swap
                  handIndex: 1
                  fieldIndex: 2
                  sessionId: my-session-001
              signal:
                summary: Set the secret signal to Blink
                value:
                  command: signal
                  signalType: 1
                  sessionId: my-session-001
              kemps:
                summary: Declare Kemps
                value:
                  command: kemps
                  sessionId: my-session-001
              counter:
                summary: Counter-Kemps against seat 1
                value:
                  command: counter
                  targetSeat: 1
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KempsResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KempsResponse'

  /cuckoo/exec:
    post:
      tags:
        - cuckoo
      summary: Execute a Cuckoo game command
      description: |
        Send a command to advance a Cuckoo session identified by `sessionId`.
        Cuckoo (also known as Chase the Ace or Ranter-Go-Round) is a 4-player
        (1 human + 3 CPU) life-survival game on a standard 52-card deck. Each player
        holds exactly one card and starts with 3 lives (default). During the Turn
        phase a player either keeps their card or swaps it with a neighbour, while the
        dealer swaps with the stock instead of a neighbour. During the Refuse phase a
        player holding a King may refuse an incoming swap by revealing the King,
        blocking the exchange. After every player has acted, the holder(s) of the
        lowest card lose a life (ties mean all tied holders lose one). A player reduced
        to 0 lives is eliminated, and the last player standing wins.

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Turn     | `0`   | Each player keeps or swaps their single card |
        | Refuse   | `1`   | A King holder may refuse an incoming swap |
        | RoundEnd | `2`   | The round was scored and lives were deducted; advance with `nextround` |
        | GameEnd  | `3`   | Only one player remains alive |

        | Command         | Aliases | Description |
        |-----------------|---------|-------------|
        | `reset`         | `r`     | Start / restart the game (optional `config`) |
        | `keep`          | `k`     | Keep the current card |
        | `swap`          | `s`     | Swap with the neighbour (or the stock, for the dealer) |
        | `refuse`        | `rf`    | Refuse an incoming swap by revealing a King |
        | `accept`        | `ac`    | Accept an incoming swap |
        | `nextround`     | `nr`    | Advance to the next round |
        | `setdifficulty` | `sd`    | Set CPU difficulty (`config.cpuDifficulty` 0 Easy, 1 Normal, 2 Hard) |
        | `log`           | `l`     | View action log |
      operationId: cuckooExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CuckooRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              keep:
                summary: Keep the current card
                value:
                  command: keep
                  sessionId: my-session-001
              swap:
                summary: Swap with the neighbour
                value:
                  command: swap
                  sessionId: my-session-001
              refuse:
                summary: Refuse an incoming swap with a King
                value:
                  command: refuse
                  sessionId: my-session-001
              nextround:
                summary: Advance to the next round
                value:
                  command: nextround
                  sessionId: my-session-001
              setdifficulty:
                summary: Set CPU difficulty to Hard
                value:
                  command: setdifficulty
                  config:
                    cpuDifficulty: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CuckooResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CuckooResponse'

  /pishti/exec:
    post:
      tags:
        - pishti
      summary: Execute a Pişti game command
      description: |
        Send a command to advance a Pişti session identified by `sessionId`.
        Pişti is a Turkish 2-4 player (1 human + 1-3 CPU) capture (fishing) game on
        a standard 52-card deck. Each player is dealt 4 cards and plays one onto a
        central pile in turn. Playing a card that matches the rank of the pile's top
        card, or playing a Jack (wild), captures the whole pile. Capturing a lone card
        scores a Pişti (10 points; 20 for a Jack played onto a single Jack). Hands are
        re-dealt 4 at a time until the stock runs out, then the captured cards are
        scored and the highest total wins.

        | Phase    | Value      | Description |
        |----------|------------|-------------|
        | play     | `play`     | Players take turns playing a hand card onto the pile |
        | roundEnd | `roundEnd` | A deal was scored; the next deal is dealt automatically |
        | gameEnd  | `gameEnd`  | The stock is exhausted and final scores are computed |

        | Command         | Aliases | Description |
        |-----------------|---------|-------------|
        | `reset`         | `r`     | Start / restart the game (optional `config`) |
        | `play`          | `p`     | Play hand card `handIndex` onto the pile |
        | `next`          | `n`     | Start the next game after one ends |
        | `setdifficulty` | `sd`    | Set CPU difficulty (`config.cpuDifficulty` 0 Easy, 1 Normal, 2 Hard) |
        | `setplayers`    | `sp`    | Set player count (`config.playerCnt` 2-4) |
        | `log`           | `l`     | View action log |
      operationId: pishtiExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PishtiRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the first hand card
                value:
                  command: play
                  handIndex: 0
                  sessionId: my-session-001
              next:
                summary: Start the next game
                value:
                  command: next
                  sessionId: my-session-001
              setdifficulty:
                summary: Set CPU difficulty to Hard
                value:
                  command: setdifficulty
                  config:
                    cpuDifficulty: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PishtiResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PishtiResponse'

  /cuarenta/exec:
    post:
      tags:
        - cuarenta
      summary: Execute a Cuarenta game command
      description: |
        Send a command to advance a Cuarenta session identified by `sessionId`.
        Cuarenta is a 4-player, 2-team (1 human + 3 CPU) Ecuadorian capture (fishing)
        game on a 40-card Spanish-style deck (the 8s, 9s and 10s are removed). Teams are
        seats 0 and 2 (Team A) versus seats 1 and 3 (Team B). Each player is dealt 5
        cards and 4 cards are placed on the central table. On your turn you play one card,
        and if it matches the rank of any table card you capture all same-rank table cards
        (chaining via ronda when 3 or more of a rank appear), otherwise the card is laid
        on the table. Bonuses are caída (2 for capturing the card just laid), limpia (1
        for clearing the table), and ronda (1 per extra card beyond 2). Hands are re-dealt
        5 at a time until the stock runs out, then the team holding more than 20 of the 40
        cards earns a 6-point most cards bonus, and the first team to the target score
        (default 40) wins.

        | Phase    | Value      | Description |
        |----------|------------|-------------|
        | play     | `play`     | Players take turns playing a hand card onto the table |
        | roundEnd | `roundEnd` | A deal was scored; the next deal is dealt automatically |
        | gameEnd  | `gameEnd`  | A team reached the target score and final scores are computed |

        | Command         | Aliases | Description |
        |-----------------|---------|-------------|
        | `reset`         | `r`     | Start / restart the game (optional `config`) |
        | `play`          | `p`     | Play hand card `handIndex` onto the table |
        | `next`          | `n`     | Start the next round after one ends |
        | `setdifficulty` | `sd`    | Set CPU difficulty (`config.cpuDifficulty` 0 Easy, 1 Normal, 2 Hard) |
        | `log`           | `l`     | View action log |
      operationId: cuarentaExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CuarentaRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the first hand card
                value:
                  command: play
                  handIndex: 0
                  sessionId: my-session-001
              next:
                summary: Start the next round
                value:
                  command: next
                  sessionId: my-session-001
              setdifficulty:
                summary: Set CPU difficulty to Hard
                value:
                  command: setdifficulty
                  config:
                    cpuDifficulty: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CuarentaResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CuarentaResponse'

  /faro/exec:
    post:
      tags:
        - faro
      summary: Execute a Faro game command
      description: |
        Send a command to advance a Faro session identified by `sessionId`.
        Faro is a single-player-vs-bank banking game on a standard 52-card deck with a
        13-rank layout (A is 1 to K is 13). In the Betting phase the player places chips on
        ranks; a copper bet wagers a rank to lose rather than win. The bank burns the soda
        (first card) and deals turns of two cards, where the first (losing) card rank is
        collected by the bank and the second (winning) card rank is paid 1 to 1. When both
        cards share a rank (a split) the bank takes half of that rank stake. The final
        three cards may be called by predicting their order, paying 4 to 1 for a correct
        call. Play continues until the player runs out of chips.

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Betting  | `1`   | The player places chip bets on the rank layout |
        | Turn     | `2`   | The bank deals turns of two cards (loser then winner) |
        | Call     | `3`   | The final three cards may be called for a 4 to 1 payout |
        | RoundEnd | `4`   | The deal is scored; the next deal awaits |
        | GameEnd  | `5`   | The player ran out of chips |

        | Command    | Aliases | Description |
        |------------|---------|-------------|
        | `reset`    | `r`     | Start / restart the game |
        | `bet`      | `b`     | Place a chip bet (`rank`, `amount`, `copper`) |
        | `clearBet` | `cb`    | Clear the bet on a `rank` |
        | `clearAll` | `ca`    | Clear all bets |
        | `deal`     | `d`     | Deal one turn (two cards) |
        | `call`     |         | Predict the `order` of the final three cards |
        | `next`     | `n`     | Start the next deal after one ends |
        | `log`      | `l`     | View action log |
      operationId: faroExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FaroRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Bet 100 chips on rank 7 (Seven)
                value:
                  command: bet
                  rank: 7
                  amount: 100
                  copper: false
                  sessionId: my-session-001
              deal:
                summary: Deal one turn
                value:
                  command: deal
                  sessionId: my-session-001
              call:
                summary: Call the order of the final three cards
                value:
                  command: call
                  order: [3, 9, 12]
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FaroResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FaroResponse'

  /openfacechinese/exec:
    post:
      tags:
        - openfacechinese
      summary: Execute an Open Face Chinese Poker (OFC) game command
      description: |
        Send a command to advance an Open Face Chinese Poker (OFC) session identified by
        `sessionId`. OFC is a solo-vs-CPU game where each dealt card must be committed
        immediately to one of three rows, Top (0), Middle (1) or Bottom (2), and once
        placed a card cannot be moved. The rows must rank in non-decreasing strength from
        Top to Bottom or the hand is fouled. Strong rows earn royalty bonuses and a premium
        Top row qualifies for Fantasyland. Highest cumulative total after the target number
        of rounds wins.

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Placing  | `0`   | The player commits each dealt card to a row |
        | RoundEnd | `1`   | The round is scored; the next round awaits |
        | GameEnd  | `2`   | The target number of rounds has been played |

        | Command       | Aliases       | Description |
        |---------------|---------------|-------------|
        | `reset`       | `r`           | Start / restart the game |
        | `place`       | `p`           | Place the pending card into a `row` (0 front, 1 middle, 2 back) |
        | `nextround`   | `nr`          | Start the next round after one ends |
        | `setdifficulty` | `sd`        | Set the CPU difficulty (0 to 2) |
        | `setplayers`  | `sp`          | Set the player count (2 to 4) |
        | `log`         | `l`           | View action log |
      operationId: openFaceChineseExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OpenFaceChineseRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              place:
                summary: Place the pending card on the bottom row
                value:
                  command: place
                  row: 2
                  sessionId: my-session-001
              nextround:
                summary: Start the next round
                value:
                  command: nextround
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OpenFaceChineseResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OpenFaceChineseResponse'

  /russianbank/exec:
    post:
      tags:
        - russianbank
      summary: Execute a Russian Bank (Crapette) game command
      description: |
        Send a command to advance a Russian Bank (Crapette) session identified by
        `sessionId`. Russian Bank is a 2-player competitive solitaire on two decks where
        each player races to empty their 13-card reserve onto 8 shared foundations
        (built up A to K by suit) and 4 shared tableau columns (built down in alternating
        colour). Move sources are identified by `zone` (0 reserve, 1 waste, 2 tableau),
        `fromOpp` (true for the opponent's pile), and `col` (the tableau column). When out
        of moves, `d` discards a hand card to end the turn. Catch a CPU lapse with `s`.

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Idle    | `0`   | Before the first deal |
        | Playing | `1`   | Players take turns making moves |
        | GameEnd | `2`   | A player emptied their reserve (or stalemate) |

        | Command | Description |
        |---------|-------------|
        | `reset` | Start / restart the game |
        | `pf`    | Move `zone`/`fromOpp`/`col` source to any valid foundation |
        | `mt`    | Move source to tableau column `toCol` |
        | `d`     | Discard a hand card and end the turn |
        | `s`     | Call stop on the CPU's missed forced move |
        | `u`     | Undo the last move |
        | `hint`  | Request a move hint |
        | `log`   | View action log |
      operationId: russianBankExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RussianBankRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              toFoundation:
                summary: Move the reserve top to a foundation
                value:
                  command: pf
                  zone: 0
                  fromOpp: false
                  col: 0
                  sessionId: my-session-001
              toTableau:
                summary: Move the waste top to tableau column 2
                value:
                  command: mt
                  zone: 1
                  fromOpp: false
                  col: 0
                  toCol: 2
                  sessionId: my-session-001
              discard:
                summary: Discard a hand card to end the turn
                value:
                  command: d
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RussianBankResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RussianBankResponse'

  /labellelucie/exec:
    post:
      tags:
        - labellelucie
      summary: Execute a La Belle Lucie game command
      description: |
        Send a command to advance a La Belle Lucie session identified by `sessionId`.
        La Belle Lucie is a single-player fan solitaire. Only the top card of each fan
        moves: onto another fan whose top is the next-higher same-suit card (`mf`), or up
        from the Ace on a foundation (`ff`). When stuck, `rd` gathers, shuffles and redeals
        (up to 3 times). Clear all 52 cards to win.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | `0`   | Moving cards among fans and foundations |
        | GameClear | `1`   | All 52 cards reached the foundations |
        | GameOver  | `2`   | Stuck after the last redeal, or gave up |

        | Command | Description |
        |---------|-------------|
        | `reset` | Start / restart the game |
        | `mf`    | Move fan `from` top onto fan `to` |
        | `ff`    | Move fan `from` top onto a foundation |
        | `rd`    | Gather, shuffle and redeal |
        | `ac`    | Auto-play every available foundation move |
        | `u`     | Undo the last move |
        | `undo_n`| Undo `n` moves |
        | `g`     | Give up |
        | `hint`  | Request a move hint |
        | `log`   | View action log |
      operationId: laBelleLucieExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LaBelleLucieRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              fanToFan:
                summary: Move fan 1 top onto fan 0
                value:
                  command: mf
                  from: 1
                  to: 0
                  sessionId: my-session-001
              fanToFoundation:
                summary: Move fan 2 top onto a foundation
                value:
                  command: ff
                  from: 2
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LaBelleLucieResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LaBelleLucieResponse'

  /simplesimon/exec:
    post:
      tags:
        - simplesimon
      summary: Execute a Simple Simon game command
      description: |
        Send a command to advance a Simple Simon session identified by `sessionId`.
        Simple Simon is an easier Spider-family solitaire: all 52 cards are dealt
        face-up into 10 columns with no stock. Move a column top or a same-suit
        descending run with `m` (fromCol, cardIndex, toCol); a complete King-to-Ace
        same-suit run is removed automatically. Clear all four suits to win.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | `0`   | Moving cards among the 10 columns |
        | GameClear | `1`   | All four suits were removed |
        | GameOver  | `2`   | No legal moves remain, or gave up |

        | Command | Description |
        |---------|-------------|
        | `reset` | Start / restart the game |
        | `m`     | Move `fromCol` from `cardIndex` onto `toCol` |
        | `u`     | Undo the last move |
        | `undo_n`| Undo `n` moves |
        | `g`     | Give up |
        | `hint`  | Request a move hint |
        | `log`   | View action log |
      operationId: simpleSimonExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SimpleSimonRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              move:
                summary: Move column 1 (from its top card) onto column 0
                value:
                  command: m
                  fromCol: 1
                  cardIndex: 0
                  toCol: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimpleSimonResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SimpleSimonResponse'
  /doubleklondike/exec:
    post:
      tags:
        - doubleklondike
      summary: Execute a Double Klondike game command
      description: |
        Send a command to advance a Double Klondike session identified by `sessionId`.
        Double Klondike is a two-deck (104-card) Klondike solitaire with 9 tableau
        columns, 8 foundations (two per suit), and a three-card-draw stock. Build the
        tableau down in alternating colours and the foundations up by suit from Ace to
        King. Move all 104 cards to the foundations to win.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | `0`   | Building tableau and foundations |
        | GameClear | `1`   | All 104 cards reached the foundations |
        | GameOver  | `2`   | No legal moves remain, or gave up |

        | Command | Description |
        |---------|-------------|
        | `reset` | Start / restart the game |
        | `d`     | Draw three cards from the stock |
        | `mwt`   | Move the waste top onto tableau `col` |
        | `mwf`   | Move the waste top to a foundation |
        | `mtt`   | Move `fromCol` from `cardIndex` onto `toCol` |
        | `mtf`   | Move the top of tableau `col` to a foundation |
        | `ac`    | Auto-complete to the foundations |
        | `u`     | Undo the last move |
        | `undo_n`| Undo `n` moves |
        | `g`     | Give up |
        | `hint`  | Request a move hint |
        | `log`   | View action log |
      operationId: doubleKlondikeExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DoubleKlondikeRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              draw:
                summary: Turn over three cards from the stock
                value:
                  command: d
                  sessionId: my-session-001
              moveTableau:
                summary: Move column 1 (from its top card) onto column 0
                value:
                  command: mtt
                  fromCol: 1
                  cardIndex: 0
                  toCol: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DoubleKlondikeResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DoubleKlondikeResponse'
  /blackhole/exec:
    post:
      tags:
        - blackhole
      summary: Execute a Black Hole game command
      description: |
        Send a command to advance a Black Hole session identified by `sessionId`.
        Black Hole is a one-deck patience: the Ace of Spades seeds a central pile and
        the other 51 cards are dealt into 17 fans of three. Play a fan top whose rank
        is one away from the pile top (any suit, no King-Ace wrap). Absorb all 52
        cards to win.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Playing   | `0`   | Absorbing cards into the black hole |
        | GameClear | `1`   | All 52 cards were absorbed |
        | GameOver  | `2`   | No legal moves remain, or gave up |

        | Command | Description |
        |---------|-------------|
        | `reset` | Start / restart the game |
        | `m`     | Play fan `fan` top into the black hole |
        | `u`     | Undo the last move |
        | `undo_n`| Undo `n` moves |
        | `g`     | Give up |
        | `hint`  | Request a move hint |
        | `log`   | View action log |
      operationId: blackHoleExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BlackHoleRequest'
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the top of fan 0 into the black hole
                value:
                  command: m
                  fan: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BlackHoleResponse'
        '400':
          description: Bad request (unsupported command, missing/invalid parameters, or session error)
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BlackHoleResponse'

  /beggarmyneighbour/exec:
    post:
      tags:
        - beggarmyneighbour
      summary: Execute a Beggar-My-Neighbour (ビガー・マイ・ネイバー) game command
      description: |
        Send a command to advance the Beggar-My-Neighbour session identified by
        `sessionId`.  Beggar-My-Neighbour is a two-player (human vs CPU) war-style
        card game played with a standard 52-card deck split evenly 26 cards each,
        face-down.  Players alternate turning top cards onto a central pile.
        Penalty cards (J=1, Q=2, K=3, A=4) force the opponent to pay that many
        cards; if a penalty card appears during payment the obligation flips back.
        When the required count is exhausted with no penalty card the last
        penalty-card player collects the entire central pile to the bottom of their
        hand.  The player who collects all 52 cards wins.

        | Phase      | Value | Description |
        |------------|-------|-------------|
        | Play       | `0`   | Normal turn; current player turns top card |
        | PayPenalty | `1`   | Opponent paying penalty cards |
        | Collect    | `2`   | Central pile being collected |
        | GameEnd    | `3`   | Game over |

        | Command    | Aliases | Description |
        |------------|---------|-------------|
        | `reset`    | `r`     | Start / restart a game (optional `maxRounds` 10-50000) |
        | `step`     | `s`     | Advance one step of the state machine |
        | `autoplay` | `a`     | Run to completion (up to round cap) |
        | `log`      | `l`     | View action log |
      operationId: beggarMyNeighbourExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, step, s, autoplay, a, log, l]
                maxRounds:
                  type: integer
                  description: Round cap (10-50000, default 2000). Used only with reset.
                  minimum: 10
                  maximum: 50000
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              step:
                summary: Turn next card / pay penalty / collect pile
                value:
                  command: step
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  players:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: integer
                        isHuman:
                          type: boolean
                        drawPileSize:
                          type: integer
                        discardPileSize:
                          type: integer
                        totalCards:
                          type: integer
                  phase:
                    type: integer
                    description: "0 Play, 1 PayPenalty, 2 Collect, 3 GameEnd"
                  gameEndFlag:
                    type: boolean
                  winnerIdx:
                    type: integer
                  currentPlayerIdx:
                    type: integer
                  penaltyOwnerIdx:
                    type: integer
                  penaltyRemaining:
                    type: integer
                  centralPileSize:
                    type: integer
                  lastCardPlayed:
                    nullable: true
                    allOf:
                      - $ref: '#/components/schemas/Card'
                  roundsPlayed:
                    type: integer
                  config:
                    type: object
                    properties:
                      maxRounds:
                        type: integer
                  message:
                    type: string
                  messageCode:
                    type: string
                  messageParams:
                    type: object
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  players:
                    type: array
                    items:
                      type: object
                  phase:
                    type: integer
                  gameEndFlag:
                    type: boolean
                  message:
                    type: string

  /allfours/exec:
    post:
      tags:
        - allfours
      summary: Execute an All Fours (オールフォーズ) game command
      description: |
        Send a command to advance the All Fours (Seven Up) session identified by
        `sessionId`. All Fours is a two-player (human elder hand vs CPU dealer)
        trick-taking game on a standard 52-card deck. Each deal gives 6 cards
        each and flips a turn-up whose suit is the provisional trump. The
        non-dealer stands or begs; on a beg the dealer gifts 1 point or runs the
        cards (deals 3 more each and turns a fresh trump). Must-follow trick play
        (trump always legal) scores High, Low, Jack and Game per deal; first to
        PointLimit (default 7) wins.

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Beg      | `0`   | Non-dealer stands or begs |
        | Gift     | `1`   | Dealer gifts a point or runs the cards |
        | Play     | `2`   | Trick play |
        | TrickEnd | `3`   | Trick resolved; advance to next trick |
        | RoundEnd | `4`   | Deal scored; advance to next deal |
        | GameEnd  | `5`   | Game over |

        | Command     | Aliases | Description |
        |-------------|---------|-------------|
        | `reset`     | `r`     | Start / restart a game |
        | `beg`       |         | Stand (`beg=false`) or beg (`beg=true`) |
        | `respond`   |         | Gift (`run=false`) or run the cards (`run=true`) |
        | `play`      | `p`     | Play a card (requires `cardIndex`) |
        | `next`      | `n`     | Advance to next trick |
        | `nextround` | `nr`    | Score the deal and advance |
        | `hint`      | `h`     | Get a recommended action |
        | `log`       | `l`     | View action log |
      operationId: allFoursExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, beg, respond, play, p, next, n, nextround, nr, hint, h, log, l]
                beg:
                  type: boolean
                  description: For the `beg` command — true to beg, false to stand.
                run:
                  type: boolean
                  description: For the `respond` command — true to run the cards, false to gift.
                cardIndex:
                  type: integer
                  description: Hand index of the card to play (for `play`).
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      minimum: 0
                      maximum: 2
                    pointLimit:
                      type: integer
                      minimum: 1
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              stand:
                summary: Non-dealer stands
                value:
                  command: beg
                  beg: false
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AllFoursResponse'
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string

  /prsi/exec:
    post:
      tags:
        - prsi
      summary: Execute a Prší (プルシー) game command
      description: |
        Send a command to advance the Prší (Czech Crazy Eights / Mau Mau)
        session identified by `sessionId`. Prší is a 4-player shedding game
        (1 human + 3 CPU) on a 32-card pack (7,8,9,10,J,Q,K,A × 4 suits). Play
        a card matching the discard top by suit or rank; otherwise draw 1 and
        end your turn. Action cards: 7 forces the next player to draw 2 (7s
        stack), Ace skips the next player, Under (Jack) also skips. First to
        empty their hand wins.

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Play    | `0`   | Normal play |
        | GameEnd | `1`   | A player emptied their hand and won |

        | Command         | Aliases | Description |
        |-----------------|---------|-------------|
        | `reset`         | `r`     | Start / restart a game |
        | `play`          | `p`     | Play a card (requires `cardIndex`) |
        | `draw`          | `d`     | Draw (takes the whole 7-penalty stack when pending) |
        | `setdifficulty` | `sd`    | Set CPU difficulty (via config on reset) |
        | `log`           | `l`     | View action log |
      operationId: prsiExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, draw, d, log, l]
                cardIndex:
                  type: integer
                  description: Hand index of the card to play (for `play`).
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      minimum: 0
                      maximum: 2
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play the first card in hand
                value:
                  command: play
                  cardIndex: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PrsiResponse'
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /calabresella/exec:
    post:
      tags:
        - calabresella
      summary: Execute a Calabresella (カラブレセッラ) game command
      description: |
        Send a command to advance the Calabresella (Terziglio) session
        identified by `sessionId`. Calabresella is a 3-player no-trump bidding
        trick-taker (1 human + 2 CPU) where a single soloist plays against a
        2-player coalition, on a 40-card Italian pack (A,2-7,J,Q,K × 4 suits —
        no 8/9/10). Each player is dealt 12 cards; the remaining 4 form the
        face-down widow (monte). In bidding each player passes, declares chiamo
        (stake 1) or solo (stake 2); the highest declaration becomes the
        soloist, who picks up the 4-card monte (16 cards) and discards 4 back
        down to 12. No trump — must-follow the led suit. Trick rank
        3>2>A>K>Q>J>7>6>5>4. Card points in thirds: A=3, 2/3/J/Q/K=1, others 0,
        last trick +1 (total 33 thirds = 11 points). The soloist must take more
        than half (≥18 of 33 thirds) to win the round; stake-based points
        transfer between soloist and coalition. First to the target score
        (default 21) wins.

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Bid      | `0`   | Auction (pass / chiamo / solo) |
        | Monte    | `1`   | Soloist exchanges the 4-card monte |
        | Play     | `2`   | Trick play |
        | TrickEnd | `3`   | A trick has completed |
        | RoundEnd | `4`   | The deal is scored |
        | GameEnd  | `5`   | A player reached the target score |

        | Command         | Aliases | Description |
        |-----------------|---------|-------------|
        | `reset`         | `r`     | Start / restart a game |
        | `bid`           | `b`     | Declare a bid (requires `bid`: 0=pass, 1=chiamo, 2=solo) |
        | `discard`       | `d`     | Discard a monte card (requires `cardIndex`; 4 times) |
        | `play`          | `p`     | Play a card (requires `cardIndex`) |
        | `next`          | `n`     | Advance to the next trick |
        | `nextround`     | `nr`    | Advance to the next round |
        | `setdifficulty` | `sd`    | Set CPU difficulty (via config on reset) |
        | `hint`          | `h`     | Get a recommended action |
        | `log`           | `l`     | View action log |
      operationId: calabresellaExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bid, b, discard, d, play, p, next, n, nextround, nr, setdifficulty, sd, hint, h, log, l]
                bid:
                  type: integer
                  description: "Bid declaration for the `bid` command: 0=pass, 1=chiamo (stake 1), 2=solo (stake 2)."
                  enum: [0, 1, 2]
                cardIndex:
                  type: integer
                  description: Hand index of the card to play (for `play`) or discard (for `discard`).
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      minimum: 0
                      maximum: 2
                    targetPoints:
                      type: integer
                      minimum: 1
                      maximum: 1000000
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Declare chiamo
                value:
                  command: bid
                  bid: 1
                  sessionId: my-session-001
              play:
                summary: Play the first card in hand
                value:
                  command: play
                  cardIndex: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CalabresellaResponse'
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string

  /king/exec:
    post:
      tags:
        - king
      summary: Execute a King (キング) game command
      description: |
        Send a command to advance the King session identified by `sessionId`.
        King is a Greek/Brazilian trick-avoidance compendium game for 4 players
        (1 human + 3 CPU, each scoring individually) on a standard 52-card pack.
        The match runs for exactly 7 deals; each deal the dealer (親) chooses one
        of the 7 contracts not yet played this match, then a full round of 13
        must-follow tricks is played and scored per the chosen contract. The six
        negative contracts are played no-trump; the King contract is played with
        a dealer-chosen trump suit. Must follow the led suit if able, otherwise
        play anything (no overtrump obligation). Trick strength
        A>K>Q>J>10>9>…>2. After all 7 contracts have each been played once, the
        player with the highest total (least penalty) wins.

        | Contract | Value | Scoring |
        |----------|-------|---------|
        | No Tricks         | `0` | −2 per trick taken |
        | No Hearts         | `1` | −2 per heart taken |
        | No Queens         | `2` | −6 per queen taken |
        | No King of Hearts | `3` | −20 for capturing K♥ |
        | No Last Two Tricks| `4` | −10 for each of the last two tricks taken |
        | No Men            | `5` | −3 per Jack or King taken |
        | King (Trump)      | `6` | +5 per trick taken (dealer chooses trump) |

        | Phase          | Value | Description |
        |----------------|-------|-------------|
        | SelectContract | `0`   | Dealer picks an unplayed contract (and trump for King) |
        | Play           | `1`   | Trick play (13 tricks) |
        | TrickEnd       | `2`   | A trick has completed |
        | RoundEnd       | `3`   | The deal is scored |
        | GameEnd        | `4`   | All 7 contracts played — match decided |

        | Command         | Aliases | Description |
        |-----------------|---------|-------------|
        | `reset`         | `r`     | Start / restart a game |
        | `contract`      | `c`     | Choose a contract (requires `contract`: 0-6; for King also `trumpSuit`: 1-4) |
        | `play`          | `p`     | Play a card (requires `handIndex`) |
        | `next`          | `n`     | Advance to the next deal |
        | `setdifficulty` | `sd`    | Set CPU difficulty (via config on reset) |
        | `hint`          | `h`     | Get a recommended action |
        | `log`           | `l`     | View action log |
      operationId: kingExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, contract, c, play, p, next, n, setdifficulty, sd, hint, h, log, l]
                contract:
                  type: integer
                  description: "Contract selection for the `contract` command: 0=No Tricks, 1=No Hearts, 2=No Queens, 3=No King of Hearts, 4=No Last Two Tricks, 5=No Men, 6=King (Trump)."
                  enum: [0, 1, 2, 3, 4, 5, 6]
                trumpSuit:
                  type: integer
                  description: "Trump suit for the King contract (contract 6 only): 1=♠ Spades, 2=♥ Hearts, 3=♦ Diamonds, 4=♣ Clubs. Ignored for the six negative contracts."
                handIndex:
                  type: integer
                  description: Hand index of the card to play (for `play`).
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      minimum: 0
                      maximum: 2
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              contract:
                summary: Choose the King contract with hearts as trump
                value:
                  command: contract
                  contract: 6
                  trumpSuit: 2
                  sessionId: my-session-001
              play:
                summary: Play the first card in hand
                value:
                  command: play
                  handIndex: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/KingResponse'
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string

  /cinch/exec:
    post:
      tags:
        - cinch
      summary: Execute a Cinch (チンチ) game command
      description: |
        Send a command to advance the Cinch (Double Pedro / High Five) session
        identified by `sessionId`. Cinch is a 4-player All-Fours/Pitch-family
        bidding trick-taker (1 human + 3 CPU, each scoring individually) on a
        standard 52-card pack. Each player is dealt 9 cards and bids in turn
        either 0 (pass) or 1-14; the high bidder names trump and leads. A deal
        is worth 14 points: High (A of trump)=1, King of trump=1, Ten of trump
        ("Game")=1, Jack of trump=1, Right Pedro (5 of trump)=5, and Left Pedro
        (the 5 of the same color as trump)=5. The Left Pedro ranks just below
        the trump 5 and counts as trump for following and winning. Nine tricks
        are played per deal; must-follow the led suit if able, otherwise play
        anything. The bidder side must make its bid (capture at least the bid
        amount in points) or is set back (loses the bid amount), while
        non-bidders score the points they capture. First to the target score
        (default 21) wins. Trump suit encoding: 1=♠, 2=♣, 3=♥, 4=♦.

        | Phase     | Value | Description |
        |-----------|-------|-------------|
        | Bid       | `0`   | Auction (0=pass, 1-14) |
        | NameTrump | `1`   | High bidder names the trump suit |
        | Play      | `2`   | Trick play |
        | TrickEnd  | `3`   | A trick has completed |
        | RoundEnd  | `4`   | The deal is scored (make or set back) |
        | GameEnd   | `5`   | A player reached the target score |

        | Command         | Aliases | Description |
        |-----------------|---------|-------------|
        | `reset`         | `r`     | Start / restart a game |
        | `bid`           | `b`     | Declare a bid (requires `bid`: 0=pass, 1-14) |
        | `trump`         | `t`     | Name the trump suit (requires `trumpSuit`: 1-4) |
        | `play`          | `p`     | Play a card (requires `cardIndex`) |
        | `next`          | `n`     | Advance to the next trick / round |
        | `nextround`     | `nr`    | Advance to the next round |
        | `setdifficulty` | `sd`    | Set CPU difficulty (via config on reset) |
        | `hint`          | `h`     | Get a recommended action |
        | `log`           | `l`     | View action log |
      operationId: cinchExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bid, b, trump, t, play, p, next, n, nextround, nr, setdifficulty, sd, hint, h, log, l]
                bid:
                  type: integer
                  description: "Bid declaration for the `bid` command: 0=pass, 1-14."
                  minimum: 0
                  maximum: 14
                trumpSuit:
                  type: integer
                  description: "Trump suit for the `trump` command: 1=♠, 2=♣, 3=♥, 4=♦."
                  enum: [1, 2, 3, 4]
                cardIndex:
                  type: integer
                  description: Hand index of the card to play (for `play`).
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      minimum: 0
                      maximum: 2
                    pointLimit:
                      type: integer
                      minimum: 1
                      maximum: 1000000
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              bid:
                summary: Bid 6
                value:
                  command: bid
                  bid: 6
                  sessionId: my-session-001
              trump:
                summary: Name spades as trump
                value:
                  command: trump
                  trumpSuit: 1
                  sessionId: my-session-001
              play:
                summary: Play the first card in hand
                value:
                  command: play
                  cardIndex: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CinchResponse'
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string

  /loo/exec:
    post:
      tags:
        - loo
      summary: Execute a Loo (ルー) game command
      description: |
        Send a command to advance the Loo (Lanterloo) session identified by
        `sessionId`. Loo is a 4-player pot/gambling trick-taker (Five-card Loo,
        1 human + 3 CPU, each holding an individual chip stack) on a standard
        52-card pack. Each deal every player antes (default 3) into a
        carried-over pot, then a face-up turn-up card sets the trump suit. Each
        player looks at their 5 cards and decides to play or pass. Players who
        play compete over 5 tricks with must-follow-and-head: follow the led
        suit if able and, if you can beat the current best card, you must; when
        void of the led suit you must trump if you hold one (again heading if
        possible). Each trick captured wins 1/5 of the pot; a player who plays
        but takes no trick is "looed" and pays a penalty (the deal's starting
        pot) into the next deal's pot. If only one player plays they take the
        whole pot uncontested; if nobody plays the pot carries over. Chips
        accumulate over repeated deals (no target-score race). Trump suit
        encoding: 1=♠, 2=♣, 3=♥, 4=♦.

        | Phase    | Value | Description |
        |----------|-------|-------------|
        | Decide   | `0`   | Each player decides to play or pass |
        | Play     | `1`   | Trick play (active players only) |
        | TrickEnd | `2`   | A trick has completed |
        | RoundEnd | `3`   | The deal is settled (pot split, looed penalties) |

        | Command         | Aliases | Description |
        |-----------------|---------|-------------|
        | `reset`         | `r`     | Start / restart a game |
        | `decide`        | `d`     | Play (`d 1`/`play`) or pass (`d 0`/`pass`); web uses `play` boolean |
        | `play`          | `p`     | Play a card (requires `cardIndex`) |
        | `next`          | `n`     | Advance / settle to the next deal |
        | `nextround`     | `nr`    | Settle and advance to the next deal |
        | `setdifficulty` | `sd`    | Set CPU difficulty (via config on reset) |
        | `hint`          | `h`     | Get a recommended action |
        | `log`           | `l`     | View action log |
      operationId: looExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, decide, d, play, p, next, n, nextround, nr, setdifficulty, sd, hint, h, log, l]
                play:
                  type: boolean
                  description: "Participation decision for the `decide` command: true=play, false=pass."
                cardIndex:
                  type: integer
                  description: Hand index of the card to play (for `play`).
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      minimum: 0
                      maximum: 2
                    ante:
                      type: integer
                      minimum: 1
                      maximum: 1000000
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              decide:
                summary: Decide to play
                value:
                  command: decide
                  play: true
                  sessionId: my-session-001
              play:
                summary: Play the first card in hand
                value:
                  command: play
                  cardIndex: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LooResponse'
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string

  /basra/exec:
    post:
      tags:
        - basra
      summary: Execute a Basra (バスラ) game command
      description: |
        Send a command to advance the Basra (Bastra) session identified by
        `sessionId`. Basra is a 4-player Egyptian/Levantine fishing (capture)
        card game (1 human + 3 CPU, each scoring individually) on a standard
        52-card pack. Each player is dealt 4 cards and 4 cards are placed
        face-up on the table. On your turn you play one hand card: a number
        card (A=1..10) captures table cards of the same rank and/or any table
        subset whose values sum to the played card's value (multiple groups at
        once); Q/K capture only by matching rank; a Jack sweeps all table cards
        except other Jacks. Clearing the whole table with a single non-Jack
        card scores a "Basra" (+10 bonus) — a Jack sweep is not a Basra; if
        nothing is captured the card is left on the table (trail). When all
        hands are empty, fresh 4-card hands are dealt (no new table cards)
        until the stock is exhausted, and leftover table cards go to the last
        player who captured. Scoring at game end: most captured cards (unique)
        +3, 7♦ +3, 10♦ +2, each Ace +1, each Basra +10; highest total wins
        (ties = multiple winners). There is no target-score race.

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Play    | `0`   | Players play cards and capture table cards |
        | GameEnd | `1`   | The stock is exhausted and the game is scored |

        | Command         | Aliases | Description |
        |-----------------|---------|-------------|
        | `reset`         | `r`     | Start / restart a game |
        | `play`          | `p`     | Play a hand card (requires `cardIndex`; optional `tableIndices` to capture, omit to trail; a Jack always sweeps) |
        | `next`          | `n`     | Advance to the next game |
        | `nextround`     | `nr`    | Start a fresh game |
        | `setdifficulty` | `sd`    | Set CPU difficulty (via config on reset) |
        | `hint`          | `h`     | Get a recommended action |
        | `log`           | `l`     | View action log |
      operationId: basraExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, next, n, nextround, nr, setdifficulty, sd, hint, h, log, l]
                cardIndex:
                  type: integer
                  description: Hand index of the card to play (for `play`).
                tableIndices:
                  type: array
                  description: "Indices of the table cards to capture (for `play`); omit to trail. A Jack always sweeps regardless of this field."
                  items:
                    type: integer
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      minimum: 0
                      maximum: 2
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play hand card 0 and capture table cards 1 and 2
                value:
                  command: play
                  cardIndex: 0
                  tableIndices: [1, 2]
                  sessionId: my-session-001
              trail:
                summary: Play hand card 0 without capturing (trail)
                value:
                  command: play
                  cardIndex: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BasraResponse'
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string

  /tablanet/exec:
    post:
      tags:
        - tablanet
      summary: Execute a Tablanet (タブラネット) game command
      description: |
        Send a command to advance the Tablanet (Tablić) session identified by
        `sessionId`. Tablanet is a 4-player Balkan (Serbian/Macedonian) fishing
        (capture) card game (1 human + 3 CPU, each scoring individually) on a
        standard 52-card pack. Each player is dealt 4 cards and 4 cards are placed
        face-up on the table. On your turn you play one hand card: a number
        card (A=1..10) captures table cards of the same rank and/or any table
        subset whose values sum to the played card's value (multiple groups at
        once); Q/K capture only by matching rank; a Jack sweeps all table cards
        except other Jacks. Clearing the whole table with a single non-Jack
        card scores a "Tabla" bonus — a Jack sweep is not a Tabla; if
        nothing is captured the card is left on the table (trail). When all
        hands are empty, fresh 4-card hands are dealt (no new table cards)
        until the stock is exhausted, and leftover table cards go to the last
        player who captured. Scoring at game end: most captured cards (unique)
        +3, 10♦ +2, each Ace +1, each Jack +1, 2♣ +1, each Tabla +1; highest
        total wins (ties = multiple winners). There is no target-score race.

        | Phase   | Value | Description |
        |---------|-------|-------------|
        | Play    | `0`   | Players play cards and capture table cards |
        | GameEnd | `1`   | The stock is exhausted and the game is scored |

        | Command         | Aliases | Description |
        |-----------------|---------|-------------|
        | `reset`         | `r`     | Start / restart a game |
        | `play`          | `p`     | Play a hand card (requires `cardIndex`; optional `tableIndices` to capture, omit to trail; a Jack always sweeps) |
        | `next`          | `n`     | Advance to the next game |
        | `nextround`     | `nr`    | Start a fresh game |
        | `setdifficulty` | `sd`    | Set CPU difficulty (via config on reset) |
        | `hint`          | `h`     | Get a recommended action |
        | `log`           | `l`     | View action log |
      operationId: tablanetExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, play, p, next, n, nextround, nr, setdifficulty, sd, hint, h, log, l]
                cardIndex:
                  type: integer
                  description: Hand index of the card to play (for `play`).
                tableIndices:
                  type: array
                  description: "Indices of the table cards to capture (for `play`); omit to trail. A Jack always sweeps regardless of this field."
                  items:
                    type: integer
                config:
                  type: object
                  properties:
                    cpuDifficulty:
                      type: integer
                      minimum: 0
                      maximum: 2
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
              play:
                summary: Play hand card 0 and capture table cards 1 and 2
                value:
                  command: play
                  cardIndex: 0
                  tableIndices: [1, 2]
                  sessionId: my-session-001
              trail:
                summary: Play hand card 0 without capturing (trail)
                value:
                  command: play
                  cardIndex: 0
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TablanetResponse'
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string

  /trenteetquarante/exec:
    post:
      tags:
        - trenteetquarante
      summary: Execute a Trente et Quarante (トラント・エ・カラント) game command
      description: |
        Send a command to advance the Trente et Quarante (Rouge et Noir)
        session identified by `sessionId`. Trente et Quarante is a pure banking
        game (1 player vs the bank) on a standard 52-card pack with no player
        card decisions. The player picks one of four even-money bets — Noir (0),
        Rouge (1), Couleur (2), Inverse (3) — plus a stake, then the dealer deals
        two rows (Noir/black first, then Rouge/red), summing each (face cards =
        10, Ace = 1) until the total reaches 31 or more. The row with the lower
        total wins. Noir/Rouge bet on which row wins; Couleur/Inverse bet on
        whether the color of the first card dealt matches the winning color.
        Winning bets pay 1:1; a tie at exactly 31 in both rows is a "Refait" and
        the player loses half the stake. Chips carry over across rounds. Placing
        a bet immediately deals both rows and resolves the round, so there are
        only two phases.

        | Phase  | Value | Description |
        |--------|-------|-------------|
        | Bet    | `0`   | The player selects a bet and stake |
        | Result | `1`   | The round has been dealt, resolved, and scored |

        | Command     | Aliases      | Description |
        |-------------|--------------|-------------|
        | `reset`     | `r`          | Start a fresh game (chips persist) |
        | `bet`       | `b`          | Place a stake on a bet, deal, and resolve (requires `bet` and `stake`) |
        | `nextround` | `nr`,`n`,`next` | Start the next round |
        | `hint`      |              | Return an educational betting hint |
        | `log`       | `l`          | View action log |
      operationId: trenteetquaranteExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  enum: [reset, r, bet, b, nextround, nr, n, next, hint, log, l]
                bet:
                  type: integer
                  description: "Bet selection (for `bet`): 0=Noir, 1=Rouge, 2=Couleur, 3=Inverse."
                  enum: [0, 1, 2, 3]
                stake:
                  type: integer
                  description: Stake to wager (for `bet`).
                sessionId:
                  type: string
            examples:
              reset:
                summary: Start a fresh game
                value:
                  command: reset
                  sessionId: my-session-001
              bet:
                summary: Wager 100 on Noir
                value:
                  command: bet
                  bet: 0
                  stake: 100
                  sessionId: my-session-001
              nextround:
                summary: Start the next round
                value:
                  command: nextround
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrenteEtQuaranteResponse'
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string

  /agnes/exec:
    post:
      tags:
        - agnes
      summary: Execute a Agnes Sorel (アグネス・ソレル) game command
      description: |
        Send a command to advance the Agnes Sorel (アグネス・ソレル) game session identified by
        `sessionId`. Klondike-family single-deck solitaire in which the foundation base rank is set by the first card dealt and tableau builds down by alternating colour.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: agnesExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /anaconda/exec:
    post:
      tags:
        - anaconda
      summary: Execute a Anaconda (アナコンダ) game command
      description: |
        Send a command to advance the Anaconda (アナコンダ) game session identified by
        `sessionId`. Anaconda (pass-the-trash) poker variant: each player is dealt seven cards, passes unwanted cards to a neighbour over several rounds, then reveals the best five-card poker hand.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: anacondaExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /bouillotte/exec:
    post:
      tags:
        - bouillotte
      summary: Execute a Bouillotte (ブイヨット) game command
      description: |
        Send a command to advance the Bouillotte (ブイヨット) game session identified by
        `sessionId`. Bouillotte, an 18th-century French vying/gambling game related to Brelan, played with a stripped pack where players bet and bluff over the best combination.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: bouillotteExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /carioca/exec:
    post:
      tags:
        - carioca
      summary: Execute a Carioca (カリオカ) game command
      description: |
        Send a command to advance the Carioca (カリオカ) game session identified by
        `sessionId`. Carioca, a South American contract-rummy variant played over several deals in which each deal has a mandatory meld contract to fulfil before going out.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: cariocaExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /cego/exec:
    post:
      tags:
        - cego
      summary: Execute a Cego (チェゴ) game command
      description: |
        Send a command to advance the Cego (チェゴ) game session identified by
        `sessionId`. Cego, a German Baden tarock trick-taker on a 54-card tarot pack featuring the Cego blind-swap contract (declarer versus the field).

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: cegoExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /flowergarden/exec:
    post:
      tags:
        - flowergarden
      summary: Execute a Flower Garden (フラワーガーデン) game command
      description: |
        Send a command to advance the Flower Garden (フラワーガーデン) game session identified by
        `sessionId`. Flower Garden, a single-deck open solitaire with six tableau columns and a large 'flower bed' reserve of loose cards all available for play.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: flowergardenExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /fortyandeight/exec:
    post:
      tags:
        - fortyandeight
      summary: Execute a Forty and Eight (フォーティ・アンド・エイト) game command
      description: |
        Send a command to advance the Forty and Eight (フォーティ・アンド・エイト) game session identified by
        `sessionId`. Forty and Eight, a two-deck solitaire with eight tableau columns of five cards, building down by suit and dealing from a stock through the waste.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: fortyandeightExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /frenchtarot/exec:
    post:
      tags:
        - frenchtarot
      summary: Execute a French Tarot (フレンチタロット) game command
      description: |
        Send a command to advance the French Tarot (フレンチタロット) game session identified by
        `sessionId`. French Tarot, a 78-card tarot trick-taker with bidding, a chien (dog), and the declarer playing alone against three defenders; scoring depends on captured bouts.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: frenchtarotExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /gaigel/exec:
    post:
      tags:
        - gaigel
      summary: Execute a Gaigel (ガイゲル) game command
      description: |
        Send a command to advance the Gaigel (ガイゲル) game session identified by
        `sessionId`. Gaigel, a German Swabian point-trick game of the Sixty-Six family played with a doubled Piquet pack, featuring marriages and a race to a target score.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: gaigelExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /gostop/exec:
    post:
      tags:
        - gostop
      summary: Execute a Go-Stop (ゴーストップ) game command
      description: |
        Send a command to advance the Go-Stop (ゴーストップ) game session identified by
        `sessionId`. Go-Stop, a Korean flower-card (hanafuda) capture game in which players match cards by month to score sets and decide to 'Go' (continue) or 'Stop' (settle).

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: gostopExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /guts/exec:
    post:
      tags:
        - guts
      summary: Execute a Guts (ガッツ) game command
      description: |
        Send a command to advance the Guts (ガッツ) game session identified by
        `sessionId`. Guts, a fast gambling poker game where players simultaneously declare whether they are in or out and the losers of a showdown match the pot.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: gutsExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /hachihachi/exec:
    post:
      tags:
        - hachihachi
      summary: Execute a Hachi-Hachi (八八) game command
      description: |
        Send a command to advance the Hachi-Hachi (八八) game session identified by
        `sessionId`. Hachi-Hachi (88), a three-player hanafuda game where players capture matching flower cards to score yaku and settle points around a base of 88.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: hachihachiExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /indianrummy/exec:
    post:
      tags:
        - indianrummy
      summary: Execute a Indian Rummy (インドラミー) game command
      description: |
        Send a command to advance the Indian Rummy (インドラミー) game session identified by
        `sessionId`. Indian Rummy, a 13-card rummy variant using jokers/wild cards in which players form pure and impure sequences plus sets before declaring.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: indianrummyExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /jass/exec:
    post:
      tags:
        - jass
      summary: Execute a Jass (ヤス／シーバー) game command
      description: |
        Send a command to advance the Jass (ヤス／シーバー) game session identified by
        `sessionId`. Jass (Schieber), a Swiss trick-taking point game in which the Jack (Jass) and Nine (Nell) of trumps are the top cards and melds add bonus points.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: jassExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /kingalbert/exec:
    post:
      tags:
        - kingalbert
      summary: Execute a King Albert (キング・アルバート) game command
      description: |
        Send a command to advance the King Albert (キング・アルバート) game session identified by
        `sessionId`. King Albert, a single-deck open solitaire dealing a nine-column staircase tableau plus a seven-card reserve, building down by alternating colour.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: kingalbertExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /koenigrufen/exec:
    post:
      tags:
        - koenigrufen
      summary: Execute a Königrufen (ケーニッヒルーフェン) game command
      description: |
        Send a command to advance the Königrufen (ケーニッヒルーフェン) game session identified by
        `sessionId`. Königrufen, an Austrian tarock game on a 54-card pack featuring the 'call a king' secret-partnership mechanism and declarer-versus-field play.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: koenigrufenExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /koikoi/exec:
    post:
      tags:
        - koikoi
      summary: Execute a Koi-Koi (こいこい) game command
      description: |
        Send a command to advance the Koi-Koi (こいこい) game session identified by
        `sessionId`. Koi-Koi, a two-player hanafuda game where players capture matching flower cards to build yaku and choose to 'Koi-Koi' (continue) or stop for points.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: koikoiExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /machiavelli/exec:
    post:
      tags:
        - machiavelli
      summary: Execute a Machiavelli (マキャヴェッリ) game command
      description: |
        Send a command to advance the Machiavelli (マキャヴェッリ) game session identified by
        `sessionId`. Machiavelli, an Italian rummy played with two packs in which players may rearrange the cards already on the table to make room for their own melds.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: machiavelliExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /michigan/exec:
    post:
      tags:
        - michigan
      summary: Execute a Michigan (ミシガン) game command
      description: |
        Send a command to advance the Michigan (ミシガン) game session identified by
        `sessionId`. Michigan (Newmarket/stops family): players stake chips on boodle cards, then play cards up in suit sequence, claiming stakes as matching cards appear.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: michiganExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /oichokabu/exec:
    post:
      tags:
        - oichokabu
      summary: Execute a Oicho-Kabu (おいちょかぶ) game command
      description: |
        Send a command to advance the Oicho-Kabu (おいちょかぶ) game session identified by
        `sessionId`. Oicho-Kabu, a Japanese kabufuda gambling game similar to baccarat where players aim for a hand total whose last digit is closest to nine (kabu).

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: oichokabuExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /ombre/exec:
    post:
      tags:
        - ombre
      summary: Execute a Ombre / Hombre (オンブル) game command
      description: |
        Send a command to advance the Ombre / Hombre (オンブル) game session identified by
        `sessionId`. Ombre (Hombre), a classic three-player trick-taker on a 40-card Spanish-style pack with a bidding declarer (the Ombre) contending against two opponents.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: ombreExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /pan/exec:
    post:
      tags:
        - pan
      summary: Execute a Panguingue / Pan (パングインゲ) game command
      description: |
        Send a command to advance the Panguingue / Pan (パングインゲ) game session identified by
        `sessionId`. Panguingue (Pan), a multi-pack rummy game in which players draw and meld runs and sets, paying and collecting chips for scoring 'conditions'.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: panExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /primero/exec:
    post:
      tags:
        - primero
      summary: Execute a Primero (プリメロ) game command
      description: |
        Send a command to advance the Primero (プリメロ) game session identified by
        `sessionId`. Primero, a Renaissance gambling/vying game (an ancestor of poker) where players bet on four-card combinations such as flush, prime, and supremus.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: primeroExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /rook/exec:
    post:
      tags:
        - rook
      summary: Execute a Rook (ルーク) game command
      description: |
        Send a command to advance the Rook (ルーク) game session identified by
        `sessionId`. Rook, a trick-taking game played with the dedicated 57-card Rook deck (four coloured suits 1-14 plus the Rook bird card) featuring bidding and a nest/kitty.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: rookExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /samba/exec:
    post:
      tags:
        - samba
      summary: Execute a Samba (サンバ) game command
      description: |
        Send a command to advance the Samba (サンバ) game session identified by
        `sessionId`. Samba, a three-deck Canasta variant that additionally allows same-suit sequences (sambas) alongside standard rank melds and canastas.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: sambaExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /aluette/exec:
    post:
      tags:
        - aluette
      summary: Execute an Aluette (アリュエット) game command
      description: |
        Send a command to advance the Aluette (アリュエット) game session identified by
        `sessionId`. Aluette is a Breton four-player trick-taker in fixed 2v2 teams on a
        **48-card Spanish-suited pack** (4 suits x A-9, sota, caballo, rey — the 10 is absent).
        Three rules set it apart and they depend on each other:
        **there is no trump suit**, **there is no follow obligation** (any card in hand is
        always legal, so `playableIndices` is the whole hand on a human turn), and
        **strength is per CARD, not per rank** — six named cards, the *luettes*, outrank
        the entire deck: Monsieur (3 of coins) > Madame (3 of cups) > Borgne (2 of cups) >
        Vache (2 of coins) > Grand Neuf (9 of cups) > Petit Neuf (9 of coins). Every other
        card ranks 3 > 2 > A > rey > caballo > sota > 9 > 8 > 7 > 6 > 5 > 4 with suit ignored.
        Because the same value can be the best card in the deck in one suit and ordinary in
        another, responses carry the full `luettes` table (strongest first) so a client never
        has to keep its own copy. Equal ranks do not steal: the card played **first** wins.
        Five tricks are played per mène; three of them take the mène.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: aluetteExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /minchiate/exec:
    post:
      tags:
        - minchiate
      summary: Execute a Minchiate (ミンキアーテ) game command
      description: |
        Send a command to advance the Minchiate (ミンキアーテ) game session identified by
        `sessionId`. Minchiate, a 16th-century Florentine four-player tarot trick-taker in fixed 2v2
        teams on a **97-card pack**: 4 suits of 14 + **40 trumps** + the Matto.
        The 40-trump ladder is the largest here and is not a plain number line — the upper
        reaches carry the 12 zodiac signs, the four elements and the virtues — so responses
        name each trump rather than only numbering it.
        97 does not divide by 4: each player gets 21 and the dealer buries the remaining 13
        (scarto). Buried cards score for the dealer's team, so trumps and the Matto cannot
        be discarded.
        Must-follow, then must-trump when void; the Matto is exempt from both and never
        takes a trick. **A led Matto does not set the suit** — the next non-Matto card does,
        so a client must not treat the first card of a trick as the led suit unconditionally.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: minchiateExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /tarocchini/exec:
    post:
      tags:
        - tarocchini
      summary: Execute a Tarocchini (タロッキーニ) game command
      description: |
        Send a command to advance the Tarocchini (タロッキーニ) game session identified by
        `sessionId`. Tarocchini (Ottocento), a Bolognese four-player tarot trick-taker in fixed 2v2 teams on a
        62-card pack: 4 suits of 10 (the low pips 2-5 are absent) + 21 trumps + the Matto.
        15 cards each; 62 does not divide by 4, so the dealer buries the remaining 2 (scarto).
        **The four papi rank equal and the LATER-played one wins the trick** — a client that
        resolves with "strictly stronger card wins" gives the trick to whoever played their
        papa first. The papi are a middle group, not the top trumps: a higher ordinary trump
        still beats them. Must-follow, then must-trump when void; the Matto is exempt and
        never takes a trick.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: tarocchiniExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /scarto/exec:
    post:
      tags:
        - scarto
      summary: Execute a Scarto (スカルト) game command
      description: |
        Send a command to advance the Scarto (スカルト) game session identified by
        `sessionId`. Scarto, a Piedmontese three-player tarot trick-taker on a 78-card pack with no bidding; the dealer discards to the talon and then leads the tricks.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: scartoExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /streetsandalleys/exec:
    post:
      tags:
        - streetsandalleys
      summary: Execute a Streets and Alleys (ストリート・アンド・アレイズ) game command
      description: |
        Send a command to advance the Streets and Alleys (ストリート・アンド・アレイズ) game session identified by
        `sessionId`. Streets and Alleys, a single-deck open solitaire (a FreeCell relative without free cells) dealing all 52 cards into eight tableau columns.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: streetsandalleysExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /sultan/exec:
    post:
      tags:
        - sultan
      summary: Execute a Sultan (スルタン) game command
      description: |
        Send a command to advance the Sultan (スルタン) game session identified by
        `sessionId`. Sultan (Sultan of Turkey), a two-deck builder solitaire arranging eight Kings and a Queen into a decorative rosette built up in suit.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: sultanExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /tysiac/exec:
    post:
      tags:
        - tysiac
      summary: Execute a Thousand / Tysiąc (トゥシオンツ) game command
      description: |
        Send a command to advance the Thousand / Tysiąc (トゥシオンツ) game session identified by
        `sessionId`. Tysiąc (Thousand), a Polish point-trick game on a 24-card pack featuring marriage bonuses and bidding, with the first player to 1000 points winning.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: tysiacExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /ulti/exec:
    post:
      tags:
        - ulti
      summary: Execute a Ulti / Ultimó (ウルティ) game command
      description: |
        Send a command to advance the Ulti / Ultimó (ウルティ) game session identified by
        `sessionId`. Ulti (Ultimó), a Hungarian tarokk-influenced trick-taker on a 32-card Hungarian pack where the declarer bids compound contracts against two opponents.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: ultiExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /watten/exec:
    post:
      tags:
        - watten
      summary: Execute a Watten (ヴァッテン) game command
      description: |
        Send a command to advance the Watten (ヴァッテン) game session identified by
        `sessionId`. Watten, a Bavarian/Tyrolean trick game on a 33-card pack with a fixed set of top cards (the Critical cards) and bluff-driven raising over the deal's value.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: wattenExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /wizard/exec:
    post:
      tags:
        - wizard
      summary: Execute a Wizard (ウィザード) game command
      description: |
        Send a command to advance the Wizard (ウィザード) game session identified by
        `sessionId`. Wizard, a 60-card exact-bid trick-taker (a standard pack plus four Wizards and four Jesters) in which players bid the exact number of tricks they will win.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: wizardExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /zheng/exec:
    post:
      tags:
        - zheng
      summary: Execute a Zheng Shangyou (争上游) game command
      description: |
        Send a command to advance the Zheng Shangyou (争上游) game session identified by
        `sessionId`. Zheng Shangyou, a Chinese 4-player climbing/shedding game on a 54-card deck (52 cards plus two jokers), an ancestor of Big Two and Daifugo in which suits are irrelevant and bombs beat any normal play.

        Use `reset` (`r`) to start or restart a game and `quit` (`q`) to end
        the session. Consult the in-app manual for the full command set and
        phase progression specific to this game.
      operationId: zhengExec
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - command
                - sessionId
              properties:
                command:
                  type: string
                  description: The game command to execute (e.g. reset, or a game-specific action).
                amount:
                  type: integer
                  description: Optional numeric argument for commands that require one (e.g. a bet, index, or selection).
                sessionId:
                  type: string
                  description: Client-supplied session identifier.
            examples:
              reset:
                summary: Start a new game
                value:
                  command: reset
                  sessionId: my-session-001
      responses:
        '200':
          description: Command processed successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                description: Game state after the command was processed.
                properties:
                  message:
                    type: string
        '400':
          description: Bad request (unsupported command, invalid parameters, or session error)
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string

components:
  schemas:
    # -------------------------------------------------------------------------
    # Shared types
    # -------------------------------------------------------------------------
    FortyThievesMoveZone:
      type: object
      description: Source or target zone for a Forty Thieves card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: Zone identifier (waste, tableau, foundation)
          enum: [waste, tableau, foundation]
        col:
          type: integer
          description: Column index (required for tableau zone)
        cardIndex:
          type: integer
          description: Card index within the column (required when moving from tableau)
    CrescentMoveZone:
      type: object
      description: Source or target zone for a Crescent Solitaire card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: Zone identifier (tableau, foundation)
          enum: [tableau, foundation]
        col:
          type: integer
          description: Column index (tableau column 0..15, or foundation id 0..7)
    BakersDozenMoveZone:
      type: object
      description: Source or target zone for a Baker's Dozen card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: Zone identifier (tableau, foundation)
          enum: [tableau, foundation]
        col:
          type: integer
          description: Column index (required for tableau zone)
        cardIndex:
          type: integer
          description: Optional / ignored. The server always operates on the top card of the source column; clients may omit this field. Retained for parity with FortyThievesMoveZone.
    BeleagueredCastleMoveZone:
      type: object
      description: Source or target zone for a Beleaguered Castle card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: Zone identifier (tableau, foundation)
          enum: [tableau, foundation]
        col:
          type: integer
          description: Column index (required for tableau zone; tableau is 0..7)
        cardIndex:
          type: integer
          description: Optional / ignored. The server always operates on the top card of the source column; clients may omit this field. Retained for parity with BakersDozenMoveZone.
    BisleyMoveZone:
      type: object
      description: Source or target zone for a Bisley card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: |
            Zone identifier. `tableau` is a column, `ace` is an ascending
            (A→K) foundation, `king` is a descending (K→A) foundation.
          enum: [tableau, ace, king]
        col:
          type: integer
          description: |
            Column index. Required when `zone` is `tableau` (0..12). For the
            two foundation zones the server derives the pile from the card's
            suit, so this field is ignored.
    BisleyTableauCard:
      type: object
      description: A single tableau card in Bisley. Every card is face-up by rule, but the flag is surfaced rather than assumed.
      properties:
        card:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Card'
        faceUp:
          type: boolean
    BisleyHint:
      type: object
      description: Suggested next move for Bisley
      properties:
        fromCol:
          type: integer
          description: Source tableau column (0..12)
        toZone:
          type: string
          description: Destination zone
          enum: [ace, king, tableau]
        toIdx:
          type: integer
          description: Foundation index (0..3) when `toZone` is `ace`/`king`, otherwise the destination column
    BisleyResponse:
      type: object
      description: Response payload for /bisley/exec
      properties:
        tableau:
          type: array
          description: Thirteen tableau columns; four hold three cards and nine hold four at the deal.
          items:
            type: array
            items:
              $ref: '#/components/schemas/BisleyTableauCard'
        aceFoundations:
          type: array
          description: Four ascending foundations indexed by suit (spade, clover, heart, diamond), each seeded with its Ace.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        kingFoundations:
          type: array
          description: Four descending foundations indexed by suit; each starts empty and opens when its King is played.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "0=Playing, 1=GameClear, 2=GameOver"
        moveCount:
          type: integer
        canUndo:
          type: boolean
        isStalemate:
          type: boolean
        undoToEscape:
          type: integer
          description: Number of undos needed to escape stalemate (0 = not stalemate, -1 = unreachable)
        hint:
          $ref: '#/components/schemas/BisleyHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    NapoleonsSquareMoveZone:
      type: object
      description: Source or target zone for a Napoleon's Square card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: Zone identifier
          enum: [waste, tableau, foundation]
        col:
          type: integer
          description: |
            Tableau column index (0..11). Required when `zone` is `tableau`, and
            for a `tableau` destination. Ignored for `waste`; for a `foundation`
            destination the server picks the pile from the card's suit.
        cardIndex:
          type: integer
          description: |
            Head of the same-suit descending run to carry, for a `tableau` source
            moving to `tableau`. Omit to move only the top card (the server then
            uses -1, which it resolves to the top of the column).
    NapoleonsSquareTableauCard:
      type: object
      description: A single tableau card. Every card is face-up by rule, but the flag is surfaced rather than assumed.
      properties:
        card:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Card'
        faceUp:
          type: boolean
    NapoleonsSquareHint:
      type: object
      description: Suggested next move for Napoleon's Square
      properties:
        fromZone:
          type: string
          description: "`stock` means: turn a card."
          enum: [waste, tableau, stock]
        fromCol:
          type: integer
          description: Source tableau column, or -1 when the source is the waste or stock
        cardIndex:
          type: integer
          description: Head of the run to carry, or -1 when the source is not the tableau
        toZone:
          type: string
          enum: [foundation, tableau, waste]
        toCol:
          type: integer
          description: Destination foundation or column index, or -1 for the waste
    NapoleonsSquareResponse:
      type: object
      description: Response payload for /napoleonssquare/exec
      properties:
        tableau:
          type: array
          description: Twelve columns of four, arranged as a square around the foundations.
          items:
            type: array
            items:
              $ref: '#/components/schemas/NapoleonsSquareTableauCard'
        stockCount:
          type: integer
          description: Cards left in the stock. There is no redeal, so this only decreases.
        waste:
          type: array
          description: The waste pile; only its last card is playable.
          items:
            $ref: '#/components/schemas/Card'
        foundation:
          type: array
          description: Eight foundations, two per suit, each seeded with its Ace at deal time.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "0=Playing, 1=GameClear, 2=GameOver"
        moveCount:
          type: integer
        canUndo:
          type: boolean
        isStalemate:
          type: boolean
        undoToEscape:
          type: integer
          description: Number of undos needed to escape stalemate (0 = not stalemate, -1 = unreachable)
        hint:
          $ref: '#/components/schemas/NapoleonsSquareHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    GrandfathersClockMoveZone:
      type: object
      description: Source or target zone for a Grandfather's Clock card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: Zone identifier
          enum: [tableau, foundation]
        col:
          type: integer
          description: |
            Tableau column (0..7) for `tableau`, or the clock face index (0..11)
            for `foundation`. **Required in both cases.** Unlike the one-per-suit
            solitaires, twelve faces can hold the same suit, so the destination
            face cannot be derived from the card being moved.
    GrandfathersClockTableauCard:
      type: object
      description: A single tableau card. Every card is face-up by rule, but the flag is surfaced rather than assumed.
      properties:
        card:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Card'
        faceUp:
          type: boolean
    GrandfathersClockFoundation:
      type: object
      description: One clock face, with the rank it has to end on.
      properties:
        cards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        targetRank:
          type: integer
          description: |
            The rank this face must finish on: 1 (Ace) at one o'clock through 12
            (Queen) at twelve. Sent so the client never recomputes the clock
            ordering and drifts from the server.
          minimum: 1
          maximum: 12
        complete:
          type: boolean
          description: Whether the face has reached `targetRank`. A complete face accepts no further cards.
    GrandfathersClockHint:
      type: object
      description: Suggested next move for Grandfather's Clock
      properties:
        fromCol:
          type: integer
          description: Source tableau column (0..7)
        toZone:
          type: string
          enum: [foundation, tableau]
        toIdx:
          type: integer
          description: Destination clock face (0..11) or tableau column (0..7)
    GrandfathersClockResponse:
      type: object
      description: Response payload for /grandfathersclock/exec
      properties:
        tableau:
          type: array
          description: Eight columns, dealt five cards each.
          items:
            type: array
            items:
              $ref: '#/components/schemas/GrandfathersClockTableauCard'
        foundation:
          type: array
          description: Twelve clock faces, index 0 at one o'clock through index 11 at twelve.
          items:
            $ref: '#/components/schemas/GrandfathersClockFoundation'
        phase:
          type: integer
          description: "0=Playing, 1=GameClear, 2=GameOver"
        moveCount:
          type: integer
        canUndo:
          type: boolean
        isStalemate:
          type: boolean
        undoToEscape:
          type: integer
          description: Number of undos needed to escape stalemate (0 = not stalemate, -1 = unreachable)
        hint:
          $ref: '#/components/schemas/GrandfathersClockHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    TerraceMoveZone:
      type: object
      description: Source or target zone for a Terrace card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: "`reserve` is the terrace; it is only ever a source, and only for a foundation."
          enum: [reserve, waste, tableau, foundation]
        col:
          type: integer
          description: |
            Tableau pile (0..8). Required for a `tableau` source and destination;
            ignored for `reserve` and `waste`, and for a `foundation` destination
            the server picks the pile itself.
    TerraceHint:
      type: object
      description: Suggested next move for Terrace
      properties:
        fromZone:
          type: string
          description: "`reserve` is the terrace; `stock` means: turn a card to the waste."
          enum: [reserve, waste, tableau, stock]
        fromIdx:
          type: integer
          description: Source pile, or -1 when the source is not the tableau
        toZone:
          type: string
          enum: [foundation, tableau, waste]
        toIdx:
          type: integer
          description: Destination index, or -1 for a draw, which targets no single pile
    SirTommyHint:
      type: object
      description: |
        Suggested move for Sir Tommy. Defined here because the response schema
        already referenced it and nothing did -- see the dangling-reference
        guard in internal/infrastructure/games.
      properties:
        from:
          type: string
          description: Source zone, e.g. `stock` or `waste`.
        to:
          type: string
          description: Destination zone, e.g. `foundation` or `tableau`.
        col:
          type: integer
          description: Destination pile index where the zone has more than one.
        reason:
          type: string
          description: Reason identifier for the suggestion.
    BraidZone:
      type: object
      description: A source or destination for a Braid move.
      required:
        - zone
      properties:
        zone:
          type: string
          enum: [braid, field, helper, waste, foundation]
        col:
          type: integer
          description: |
            Slot number for `field` (0-3) and `helper` (0-7). Not used by
            `braid`, `waste` or `foundation`.
    BraidResponse:
      type: object
      description: Response payload for /braid/exec
      properties:
        braid:
          type: array
          description: The 20-card plait. Only the last card is playable, and only to a foundation.
          items:
            $ref: '#/components/schemas/Card'
        fields:
          type: array
          description: |
            Four field slots. An EMPTY slot is sent as null rather than omitted:
            closing the gaps would shift every later index and the hint's slot
            number would then point at the wrong card on screen.
          items:
            $ref: '#/components/schemas/Card'
            nullable: true
        helpers:
          type: array
          description: Eight helper slots, null for empty, on the same reasoning as `fields`.
          items:
            $ref: '#/components/schemas/Card'
            nullable: true
        foundation:
          type: array
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        stockCount:
          type: integer
        waste:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        baseRank:
          type: integer
          description: The rank all eight foundations start from, fixed by the deal.
        direction:
          type: integer
          description: "0=not chosen yet, 1=ascending, 2=descending"
        awaitingDirection:
          type: boolean
          description: |
            While true the player has not fixed the build direction, and no
            foundation can be touched. All eight then share that one direction.
        redealsLeft:
          type: integer
        canRedeal:
          type: boolean
        phase:
          type: integer
          description: "0=Playing, 1=GameClear, 2=GameOver"
        moveCount:
          type: integer
        canUndo:
          type: boolean
        hint:
          type: object
          properties:
            from:
              $ref: '#/components/schemas/BraidZone'
            to:
              $ref: '#/components/schemas/BraidZone'
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
    PontoonHand:
      type: object
      description: |
        One Pontoon hand. While `hidden` is true the server sends NO cards and a
        zero total -- only the count of nulls survives, so the client can draw
        the right number of backs without learning what they are.
      properties:
        cards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
            nullable: true
        bet:
          type: integer
        total:
          type: integer
          description: 0 while hidden.
        rank:
          type: integer
          description: "0=bust, 1=point total, 2=five-card trick, 3=pontoon. 0 while hidden."
        hidden:
          type: boolean
    PontoonResponse:
      type: object
      description: Response payload for /pontoon/exec
      properties:
        seats:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              isCpu:
                type: boolean
              hands:
                type: array
                description: More than one after a split.
                items:
                  $ref: '#/components/schemas/PontoonHand'
        bankerHand:
          $ref: '#/components/schemas/PontoonHand'
        bankerIdx:
          type: integer
        isHumanBanker:
          type: boolean
          description: |
            When true the human holds the bank, does not stake, and decides last.
        chips:
          type: integer
        activeSeat:
          type: integer
        activeHand:
          type: integer
        nextBanker:
          type: integer
          description: "Who holds the bank next; -1 while undecided."
        lastResult:
          type: string
        phase:
          type: integer
          description: "1=Bet, 2=PlayerTurn, 3=BankerTurn, 4=End"
        canStick:
          type: boolean
          description: |
            Decided by the server, not re-derived by the client. The gates are
            fiddly -- no sticking under 15, no buying after a twist -- and
            implementing them twice means getting them subtly different twice.
        canTwist:
          type: boolean
        canBuy:
          type: boolean
        canSplit:
          type: boolean
        message:
          type: string
        messageCode:
          type: string
    SetteEMezzoHand:
      type: object
      description: One Sette e Mezzo hand. Hidden hands carry null cards and a zero total.
      properties:
        cards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
            nullable: true
        bet:
          type: integer
        totalHalves:
          type: integer
          description: |
            The total in HALVES, never as a decimal. Court cards are worth half
            a point and exact equality decides the round, so 7.5 travels as 15
            rather than as a float whose comparison is a rounding question.
        hidden:
          type: boolean
    SetteEMezzoResponse:
      type: object
      description: Response payload for /settemezzo/exec
      properties:
        seats:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              isCpu:
                type: boolean
              hand:
                $ref: '#/components/schemas/SetteEMezzoHand'
        bankerHand:
          $ref: '#/components/schemas/SetteEMezzoHand'
        bankerIdx:
          type: integer
        isHumanBanker:
          type: boolean
        chips:
          type: integer
        activeSeat:
          type: integer
        nextBanker:
          type: integer
        lastResult:
          type: string
        phase:
          type: integer
          description: "1=Bet, 2=PlayerTurn, 3=BankerTurn, 4=End"
        targetHalves:
          type: integer
          description: The target (7.5) in halves, so 15. Sent so the figure is not written down twice.
        canHit:
          type: boolean
        canStand:
          type: boolean
        canSetMatta:
          type: boolean
          description: True while the king of diamonds is in hand and its value is unset.
        message:
          type: string
        messageCode:
          type: string
    NiuNiuHand:
      type: object
      description: One Niu Niu hand. Hidden hands carry null cards and no rank.
      properties:
        cards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
            nullable: true
        bet:
          type: integer
        comboIdx:
          type: array
          description: |
            Positions of the three cards that summed to a multiple of ten. Empty
            for a no-bull and while hidden. Sent so the client need not redo the
            combination search to highlight them.
          items:
            type: integer
        rank:
          type: integer
          description: "0=no bull, 1-9=niu 1 through niu 9, 10=niu niu. 0 while hidden."
        rankLabel:
          type: string
        multiplier:
          type: integer
        payout:
          type: integer
        hidden:
          type: boolean
    NiuNiuResponse:
      type: object
      description: Response payload for /niuniu/exec
      properties:
        seats:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              isCpu:
                type: boolean
              hand:
                $ref: '#/components/schemas/NiuNiuHand'
        bankerHand:
          $ref: '#/components/schemas/NiuNiuHand'
        bankerIdx:
          type: integer
        chips:
          type: integer
        maxMultiplier:
          type: integer
          description: |
            Largest payout multiplier in the table. The cap on a legal stake is
            `chips / maxMultiplier`, NOT `chips` -- a banker's niu niu takes
            three times the stake, so betting the whole stack leaves the player
            unable to pay. Sent so that division is not reinvented client-side.
        lastResult:
          type: string
        phase:
          type: integer
          description: "1=Bet, 2=End"
        message:
          type: string
        messageCode:
          type: string
    ChineseTenCard:
      allOf:
        - $ref: '#/components/schemas/Card'
        - type: object
          properties:
            points:
              type: integer
              description: |
                What this card is worth. Non-zero only for red cards. Sent so
                the 52-card scoring table exists in one place.
            isRed:
              type: boolean
              description: Whether the card is a scoring (heart or diamond) card.
    ChineseTenResponse:
      type: object
      description: Response payload for /chineseten/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
                description: Hand size. Always sent, including while hidden.
              cards:
                type: array
                description: Empty while `hidden` is true.
                items:
                  $ref: '#/components/schemas/ChineseTenCard'
              captured:
                type: array
                description: |
                  ALWAYS sent for both seats. Which cards have already gone is
                  public, and reading it is how a fishing game is played.
                items:
                  $ref: '#/components/schemas/ChineseTenCard'
              score:
                type: integer
              hidden:
                type: boolean
                description: Whether this seat's HAND is withheld.
        layout:
          type: array
          items:
            $ref: '#/components/schemas/ChineseTenCard'
        phase:
          type: integer
          description: "0=Play, 1=Select, 2=GameEnd"
        currentPlayerIdx:
          type: integer
        stockCount:
          type: integer
        pendingCard:
          $ref: '#/components/schemas/ChineseTenCard'
        selectableIndices:
          type: array
          description: |
            Layout indices the pending card may take. Carries both capture
            rules (sum-to-ten for A-9, same-rank for 10-K) so the client never
            re-derives them.
          items:
            type: integer
        tieScore:
          type: integer
          description: The score that draws the game -- half the red cards' 210.
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "-1 when the totals matched"
        hint:
          type: object
          description: Suggested move, sent on every response while it is the human's turn.
          properties:
            cardIndex:
              type: integer
            layoutIndex:
              type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    SkitgubbeResponse:
      type: object
      description: Response payload for /skitgubbe/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
                description: Hand size. Always sent, including while hidden.
              cards:
                type: array
                description: Empty while `hidden` is true.
                items:
                  $ref: '#/components/schemas/Card'
              collectedCount:
                type: integer
                description: |
                  Cards taken in phase one. Public, and worth reading: they
                  become this seat's phase-two hand, so collecting too much is
                  a liability.
              finished:
                type: boolean
                description: Out of cards in phase two.
              hidden:
                type: boolean
                description: Whether this seat's HAND is withheld.
        phase:
          type: integer
          description: "0=Collect, 1=Shed, 2=GameEnd"
        currentPlayerIdx:
          type: integer
        stockCount:
          type: integer
        trumpSuit:
          type: integer
          description: |
            Fixed by the LAST card drawn from the stock, so it is `-1` for most
            of phase one.
        duel:
          type: array
          description: |
            Phase-one cards on the table. Grows in pairs while stunsa (equal
            ranks) keeps bouncing.
          items:
            $ref: '#/components/schemas/Card'
        duelLeader:
          type: integer
          description: Seat leading the current duel; its opponent is the next seat.
        pile:
          type: array
          description: Phase-two cards on the table. The last one is what must be beaten.
          items:
            $ref: '#/components/schemas/Card'
        validIndices:
          type: array
          description: |
            Hand indices that may be played. Carries the beat rule (higher of
            the same suit, or a trump) so the client never re-derives it. Empty
            unless it is the human's turn.
          items:
            type: integer
        canPickUp:
          type: boolean
          description: |
            True only in phase two, with a non-empty pile and no playable card.
            Ducking is never lawful, so this is exactly the negation of
            `validIndices` being non-empty.
        gameEndFlag:
          type: boolean
        loserIdx:
          type: integer
          description: "The Skitgubbe -- the last seat holding cards; -1 while undecided"
        hint:
          type: object
          description: Suggested move, sent on every response while it is the human's turn.
          properties:
            cardIndex:
              type: integer
            pickUp:
              type: boolean
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    LaughAndLieDownResponse:
      type: object
      description: Response payload for /laughandliedown/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
                description: Hand size. Always sent, including while hidden.
              cards:
                type: array
                description: Empty while `hidden` is true.
                items:
                  $ref: '#/components/schemas/Card'
              wonCount:
                type: integer
                description: |
                  Cards captured. Public for every seat: the difference from
                  eight IS the settlement.
              laidDown:
                type: boolean
                description: Could not capture, so their whole hand went to the table.
              score:
                type: integer
                description: Net chips. Zero until the game ends.
              hidden:
                type: boolean
                description: Whether this seat's HAND is withheld.
        layout:
          type: array
          description: |
            The face-up table. Starts at twelve and grows with the hands of
            players who lay down. There is no face-down stock.
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "0=Play, 1=GameEnd"
        currentPlayerIdx:
          type: integer
        validIndices:
          type: array
          description: |
            Hand indices that match some table card by rank. Empty unless it is
            the human's turn.
          items:
            type: integer
        threeTakeIndices:
          type: array
          description: |
            Subset of `validIndices` whose rank has three or more cards on the
            table, so the three-card take is offered. Sent so the client never
            recounts the table.
          items:
            type: integer
        dealerIdx:
          type: integer
          description: Stakes one more than the others, and sweeps the leftovers.
        lastInIdx:
          type: integer
          description: "Last seat still holding cards; -1 while undecided"
        pot:
          type: integer
          description: |
            Total staked. Equals the last-in award plus the over/short total,
            which is what verifies the settlement table.
        gameEndFlag:
          type: boolean
        hint:
          type: object
          description: Suggested move, sent on every response while it is the human's turn.
          properties:
            cardIndex:
              type: integer
            takeCount:
              type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    SjavsResponse:
      type: object
      description: Response payload for /sjavs/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              team:
                type: integer
                description: "0 or 1; seats across the table are partners"
              cardCount:
                type: integer
                description: Hand size. Always sent, including while hidden.
              cards:
                type: array
                description: Empty while `hidden` is true.
                items:
                  $ref: '#/components/schemas/Card'
              bid:
                type: integer
                description: |
                  This seat's bid, 0 for a pass or no bid yet. Public -- a bid is
                  spoken at the table, and what your partner holds is the read.
              hidden:
                type: boolean
        phase:
          type: integer
          description: "0=Bid, 1=Play, 2=HandEnd, 3=GameEnd"
        currentPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        trumpSuit:
          type: integer
          description: "-1 until bidding ends"
        trumpCount:
          type: integer
          description: |
            Trumps in the declared suit: 13 for a red suit, 12 for a black one,
            counting the six permanent trumps. Zero while undecided. Sent so the
            client never recounts -- counting only the trump suit always falls
            short.
        bidderIdx:
          type: integer
          description: "Seat that declared trumps; -1 while undecided"
        bidLength:
          type: integer
        minBid:
          type: integer
          description: Shortest biddable trump suit. Below this you must pass.
        myLongest:
          type: integer
          description: The human's longest trump suit, which caps their bid.
        trick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        trickNo:
          type: integer
        validIndices:
          type: array
          description: |
            Hand indices that may be played. Carries the following rule,
            including that trumps form their own suit. Empty outside the human's
            turn in the play phase.
          items:
            type: integer
        teamPoints:
          type: array
          description: Card points this hand, per team. Always sums to 120.
          items:
            type: integer
        remaining:
          type: array
          description: |
            Each team's distance from winning the rubber, counted DOWN from 24.
            Zero or below wins.
          items:
            type: integer
        crosses:
          type: array
          description: |
            Rubbers won, per team. A cross records a rubber, not a hand.
          items:
            type: integer
        carryOver:
          type: integer
          description: Extra points a 60-60 hand added to the next game.
        handResult:
          type: object
          description: Present once a hand has been settled.
          properties:
            declarerTeam:
              type: integer
            declarerPoints:
              type: integer
            scoringTeam:
              type: integer
              description: "-1 when the hand tied 60-60 and nobody scored"
            amount:
              type: integer
            vol:
              type: boolean
              description: All eight tricks.
            trumpWasClubs:
              type: boolean
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: "-1 while the rubber is undecided"
        doubleVictory:
          type: boolean
          description: The losers never left 24.
        hint:
          type: object
          description: Suggested move, sent on every response while it is the human's turn.
          properties:
            cardIndex:
              type: integer
            bidLength:
              type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    TrexResponse:
      type: object
      description: Response payload for /trex/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
                description: Hand size. Always sent, including while hidden.
              cards:
                type: array
                description: Empty while `hidden` is true.
                items:
                  $ref: '#/components/schemas/Card'
              score:
                type: integer
                description: |
                  Running total. Public for every seat -- who is down and by how
                  much is what a king weighs when choosing. Can be positive,
                  because the dominoes pay out.
              dealScore:
                type: integer
              tricksWon:
                type: integer
              hidden:
                type: boolean
        phase:
          type: integer
          description: "0=Choose, 1=Play, 2=DealEnd, 3=GameEnd"
        currentPlayerIdx:
          type: integer
        kingIdx:
          type: integer
          description: Seat choosing the contract for this deal.
        contract:
          type: integer
          description: "0=KingOfHearts, 1=Diamonds, 2=Queens, 3=Tricks, 4=Dominoes, 5=none"
        availableContracts:
          type: array
          description: |
            Contracts this king has not played yet. A contract is played once
            per kingdom, so the client never has to track what is spent.
          items:
            type: integer
        isTrix:
          type: boolean
          description: The dominoes contract, which builds runs instead of tricks.
        dealNo:
          type: integer
        totalDeals:
          type: integer
          description: Twenty -- four kingdoms of five contracts.
        trick:
          type: array
          description: Empty during the dominoes contract.
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        trickNo:
          type: integer
        runs:
          type: array
          description: |
            The four dominoes runs, one per suit. `low`/`high` span what is on
            the table with the ace as 14, so the jack that starts a run is 11.
          items:
            type: object
            properties:
              suit:
                type: integer
              started:
                type: boolean
              low:
                type: integer
              high:
                type: integer
        finishOrder:
          type: array
          description: Seats in the order they went out during the dominoes.
          items:
            type: integer
        validIndices:
          type: array
          description: |
            Hand indices that may be played. Each contract follows a different
            rule, so this is computed once server-side.
          items:
            type: integer
        canPass:
          type: boolean
          description: |
            True only in the dominoes with no legal play. Trick contracts have
            no pass at all.
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "Highest final score; -1 before the twentieth deal"
        hint:
          type: object
          description: Suggested move, sent on every response while the game is live.
          properties:
            cardIndex:
              type: integer
            contract:
              type: integer
            pass:
              type: boolean
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    LobaResponse:
      type: object
      description: Response payload for /loba/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
                description: Hand size. Always sent, including while hidden.
              cards:
                type: array
                description: Empty while `hidden` is true.
                items:
                  $ref: '#/components/schemas/Card'
              score:
                type: integer
                description: |
                  Penalty total. Public for every seat: 101 knocks a player out,
                  so how close each one is drives every decision.
              eliminated:
                type: boolean
              hasMelded:
                type: boolean
                description: |
                  Whether this seat has melded this round, which is what allows
                  laying off.
              hidden:
                type: boolean
        phase:
          type: integer
          description: "0=Draw, 1=Act, 2=RoundEnd, 3=GameEnd"
        currentPlayerIdx:
          type: integer
        stockCount:
          type: integer
        discardTop:
          $ref: '#/components/schemas/Card'
        melds:
          type: array
          items:
            type: object
            properties:
              owner:
                type: integer
              kind:
                type: integer
                description: "0=pierna (one rank, three different suits), 1=escalera (a suit run)"
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
        roundNo:
          type: integer
        knockOut:
          type: integer
          description: Penalty total that eliminates a player (101).
        roundWinner:
          type: integer
          description: "Seat that went out last round; -1 when none"
        roundClean:
          type: boolean
          description: |
            That seat went out without having melded earlier in the round, which
            is what takes ten off.
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "Last player standing; -1 while the game is live"
        hint:
          type: object
          description: Suggested move, sent on every response while it is the human's turn.
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            cardIndex:
              type: integer
            drawStock:
              type: boolean
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    DesmocheResponse:
      type: object
      description: Response payload for /desmoche/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
                description: Hand size. Always sent, including while hidden.
              cards:
                type: array
                description: Empty while `hidden` is true.
                items:
                  $ref: '#/components/schemas/Card'
              score:
                type: integer
                description: Chip balance. Antes subtract, winning the pot adds.
              meldedCount:
                type: integer
                description: |
                  How many cards this seat has face up. Public for every seat --
                  melds are laid down face up, and this is the only progress
                  indicator toward the ten needed to go out.
              hidden:
                type: boolean
        phase:
          type: integer
          description: "0=Draw, 1=Act, 2=RoundEnd, 3=GameEnd"
        currentPlayerIdx:
          type: integer
        stockCount:
          type: integer
        discardTop:
          $ref: '#/components/schemas/Card'
        melds:
          type: array
          items:
            type: object
            properties:
              owner:
                type: integer
              kind:
                type: integer
                description: "0=set (one rank), 1=run (consecutive, one suit)"
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
        roundNo:
          type: integer
        pot:
          type: integer
          description: |
            Chips in the middle. A round that ends with no winner leaves this
            standing, so it grows across deals -- the client must not recompute
            it from the ante.
        goOutSize:
          type: integer
          description: Cards that must be melded to take the pot (10).
        roundWinner:
          type: integer
          description: "Seat that got down to ten last round; -1 when nobody did"
        roundExhausted:
          type: boolean
          description: |
            The stock ran out with nobody down to ten, which is why the pot
            carried over.
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "Seat with the most chips after the last round; -1 while live"
        hint:
          type: object
          description: Suggested move, sent on every response while it is the human's turn.
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            cardIndex:
              type: integer
            drawStock:
              type: boolean
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    ZwickerCard:
      type: object
      description: |
        A card together with every matching value it can take. Sent so the client
        never re-derives the value table: aces and court cards have two values
        each and the jokers are 15/20/25, and a client-side copy would drift from
        the server's capture check.
      allOf:
        - $ref: '#/components/schemas/Card'
        - type: object
          properties:
            values:
              type: array
              items:
                type: integer
    ZwickerResponse:
      type: object
      description: Response payload for /zwicker/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              team:
                type: integer
                description: "0 or 1. Seats across the table are partners."
              cardCount:
                type: integer
                description: Hand size. Always sent, including while hidden.
              cards:
                type: array
                description: Empty while `hidden` is true.
                items:
                  $ref: '#/components/schemas/ZwickerCard'
              capturedCount:
                type: integer
                description: |
                  Cards taken this deal. Public for every seat: the majority of
                  cards is worth three, so the count is what the decision is made
                  against.
              zwicks:
                type: integer
                description: Times this seat cleared the table. One point each.
              hidden:
                type: boolean
        phase:
          type: integer
          description: "0=Play, 1=RoundEnd, 2=GameEnd"
        currentPlayerIdx:
          type: integer
        stockCount:
          type: integer
        tableCards:
          type: array
          items:
            $ref: '#/components/schemas/ZwickerCard'
        builds:
          type: array
          items:
            type: object
            properties:
              owner:
                type: integer
              value:
                type: integer
                description: Declared value. A build is captured only at exactly this value.
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
        teamScores:
          type: array
          description: Running totals, indexed by team.
          items:
            type: integer
        targetScore:
          type: integer
          description: Points that win the game.
        lastRound:
          type: object
          description: Breakdown of the deal just scored; absent until one is.
          properties:
            cardPoints:
              type: array
              items:
                type: integer
            cards:
              type: array
              items:
                type: integer
            majorityTeam:
              type: integer
              description: "Team with more cards; -1 when the counts were level, in which case nobody takes the three"
            zwicks:
              type: array
              items:
                type: integer
            total:
              type: array
              items:
                type: integer
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: "Winning team; -1 while the game is live"
        hint:
          type: object
          description: Suggested move, sent on every response while it is the human's turn.
          properties:
            take:
              type: boolean
              description: True to capture, false to trail.
            cardIndex:
              type: integer
            value:
              type: integer
            tableIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    PochResponse:
      type: object
      description: Response payload for /poch/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
                description: |
                  Hand size. Public for every seat, including while hidden: whoever
                  goes out is paid one chip per card still held, so this is a
                  running liability.
              cards:
                type: array
                description: Empty while `hidden` is true.
                items:
                  $ref: '#/components/schemas/Card'
              chips:
                type: integer
              bet:
                type: integer
                description: Staked in the current pochen round.
              folded:
                type: boolean
              hidden:
                type: boolean
        phase:
          type: integer
          description: "0=Staking, 1=Pochen, 2=Stops, 3=DealEnd, 4=GameEnd"
        currentPlayerIdx:
          type: integer
        pools:
          type: array
          description: |
            All nine, always. Chips nobody claims stay put and everyone antes
            again next deal, so what is sitting on each pool is the motivation
            for the deal to come.
          items:
            type: object
            properties:
              name:
                type: string
                description: "ace | king | queen | jack | ten | marriage | sequence | pocher | centre"
              chips:
                type: integer
        paySuit:
          type: integer
          description: Suit of the turn-up. Stage one pays only on this suit.
        turnUp:
          $ref: '#/components/schemas/Card'
        stakingAwards:
          type: array
          description: |
            What stage one paid out. It resolves the instant the cards are dealt,
            so without this record the player cannot tell what happened.
          items:
            type: object
            properties:
              pool:
                type: string
              player:
                type: integer
              chips:
                type: integer
        betTarget:
          type: integer
          description: Amount each surviving player must have staked for the round to close.
        pochenWinner:
          type: integer
          description: "Seat whose set won the comparison; -1 until it is settled"
        pochenPot:
          type: integer
        playedPile:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        stopsSuit:
          type: integer
          description: |
            Suit of the run in progress. **-1 means the run is stopped** and any
            card may be led -- do not collapse it to 0, which reads as spades.
        stopsRank:
          type: integer
          description: Highest rank played in the current run (ace counts 14).
        dealNo:
          type: integer
        targetDeals:
          type: integer
        dealWinner:
          type: integer
          description: "Seat that went out; -1 when the deal is still live"
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "Seat with the most chips; -1 while the game is live"
        hint:
          type: object
          description: Suggested move, sent on every response while it is the human's turn.
          properties:
            action:
              type: string
              description: "bet | fold | play"
            cardIndex:
              type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    PopeJoanResponse:
      type: object
      description: Response payload for /popejoan/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
                description: |
                  Hand size. Public for every seat: going out is paid one chip per
                  card left in each other hand.
              cards:
                type: array
                description: Empty while `hidden` is true.
                items:
                  $ref: '#/components/schemas/Card'
              chips:
                type: integer
              holdsPope:
                type: boolean
                description: |
                  Public even while the hand is hidden — the Pope's holder is
                  excused the per-card payment, so hiding it would make the
                  settlement unreadable.
              hidden:
                type: boolean
        phase:
          type: integer
          description: "0=Play, 1=DealEnd, 2=GameEnd"
        currentPlayerIdx:
          type: integer
        compartments:
          type: array
          description: |
            All eight, always. Chips nobody claims stay put and the dealer dresses
            the board again next deal, so Pope and Matrimony build up.
          items:
            type: object
            properties:
              name:
                type: string
                description: "ace | king | queen | jack | game | pope | matrimony | intrigue"
              chips:
                type: integer
        trumpSuit:
          type: integer
          description: Set by the dead hand's last card. Compartments pay only on this suit.
        turnUp:
          $ref: '#/components/schemas/Card'
        awards:
          type: array
          description: What the board paid out this deal.
          items:
            type: object
            properties:
              compartment:
                type: string
              player:
                type: integer
              chips:
                type: integer
              byTurnUp:
                type: boolean
                description: The dealer took it outright because the turn-up matched.
        playedPile:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        runSuit:
          type: integer
          description: |
            Suit of the run in progress. **-1 means the run is stopped** and the
            next player leads their lowest card of any suit — do not collapse it
            to 0, which reads as spades.
        runRank:
          type: integer
          description: Highest rank played in the current run (ace counts 14).
        dealNo:
          type: integer
        targetDeals:
          type: integer
        dealWinner:
          type: integer
          description: "Seat that went out; -1 while the deal is still live"
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "Seat with the most chips; -1 while the game is live"
        hint:
          type: object
          description: Suggested move, sent on every response while it is the human's turn.
          properties:
            cardIndex:
              type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    NainJauneResponse:
      type: object
      description: Response payload for /nainjaune/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                description: Empty while `hidden` is true.
                items:
                  $ref: '#/components/schemas/Card'
              chips:
                type: integer
              points:
                type: integer
                description: |
                  What the hand is worth if the deal ends now. Public for every
                  seat, because settlement is in points rather than cards -- the
                  card count alone does not say what a hand owes.
              hidden:
                type: boolean
        phase:
          type: integer
          description: "0=Play, 1=DealEnd, 2=GameEnd"
        currentPlayerIdx:
          type: integer
        boxes:
          type: array
          description: |
            All five, always. Chips nobody claims stay put and everyone antes
            again next deal, so the dwarf builds up fastest.
          items:
            type: object
            properties:
              name:
                type: string
                description: "ten | jack | queen | king | dwarf"
              chips:
                type: integer
              card:
                allOf:
                  - $ref: '#/components/schemas/Card'
                description: |
                  The exact card that claims this box. Sent rather than derived,
                  because the suit is part of it -- an off-suit seven does not
                  claim the dwarf.
        talonCount:
          type: integer
          description: Cards set aside undealt. Nobody uses them.
        awards:
          type: array
          items:
            type: object
            properties:
              box:
                type: string
              player:
                type: integer
              chips:
                type: integer
        playedPile:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        runRank:
          type: integer
          description: |
            Highest rank played in the current run. **0 means the run is
            stopped** and any card may be led. There is no suit here: the run
            ignores suit entirely.
        dealNo:
          type: integer
        targetDeals:
          type: integer
        dealWinner:
          type: integer
          description: "Seat that went out; -1 while the deal is still live"
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "Seat with the most chips; -1 while the game is live"
        hint:
          type: object
          description: Suggested move, sent on every response while it is the human's turn.
          properties:
            cardIndex:
              type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    KilleResponse:
      type: object
      description: Response payload for /kille/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              card:
                allOf:
                  - $ref: '#/components/schemas/Card'
                description: |
                  The seat's single card, or null while it is face down. Carries
                  `glyph` / `label` / `color` / `deck` because the Kille pack is
                  not the French 52 (ADR-0033).
              strength:
                type: integer
                description: |
                  Effective rank, `0` while the card is face down. **Not the
                  same as the card's denomination**: a Harlequin received in an
                  exchange scores 0 rather than 21.
              chips:
                type: integer
              reentries:
                type: integer
                description: Buy-backs used so far, out of three.
              reentryCost:
                type: integer
                description: |
                  What the next buy-back costs -- one stake, then half the pot,
                  then the whole pot. `0` once all three are spent.
              canReenter:
                type: boolean
              isOut:
                type: boolean
              knockedBy:
                type: string
                description: '"hussar" | "pig" | "" (went out as the lowest)'
              isSatisfied:
                type: boolean
              isFinished:
                type: boolean
                description: Out for good -- eliminated and no buy-backs left.
              isCurrentTurn:
                type: boolean
        phase:
          type: integer
          description: "0=Exchange, 1=Showdown, 2=GameEnd"
        roundNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
          description: |
            The dealer acts last and swaps with the **stock** rather than a
            neighbour, so it is the one seat that cannot be challenged.
        stockCount:
          type: integer
        pot:
          type: integer
        events:
          type: array
          description: What happened in this round's exchanges, in order.
          items:
            type: object
            properties:
              kind:
                type: string
                description: '"swap" | "satisfied" | "cuckoo" | "hussar" | "pig" | "pass" | "stock"'
              actor:
                type: integer
              target:
                type: integer
                description: "Seat challenged; -1 for a swap with the stock"
        loserIdxs:
          type: array
          items:
            type: integer
          description: Seats that went out this round -- more than one when a Hussar or Pig fired.
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "Last player standing; -1 while the game is live"
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    KlaberjassResponse:
      type: object
      description: Response payload for /klaberjass/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                description: Your own hand only; the opponent's is empty until the showdown.
                items:
                  $ref: '#/components/schemas/Card'
              sequences:
                type: array
                description: |
                  Empty until the hand is settled -- seeing the opponent's runs
                  during play would give the contest away.
                items:
                  type: object
                  properties:
                    suit:
                      type: integer
                    topValue:
                      type: integer
                      description: Top card of the run, with the ace as 14.
                    length:
                      type: integer
                    points:
                      type: integer
                      description: 20 for three cards, 50 for four or more.
              handPoints:
                type: integer
                description: Points taken this deal, melds and bela included.
              score:
                type: integer
              isMaker:
                type: boolean
              isDealer:
                type: boolean
              isCurrentTurn:
                type: boolean
        phase:
          type: integer
          description: "0=BidTurnUp, 1=BidFree, 2=Schmeiss, 3=Play, 4=HandEnd, 5=GameEnd"
        dealNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        bidPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        trumpSuit:
          type: integer
          description: "1=Spade, 2=Clover, 3=Heart, 4=Diamond; 0 while undecided"
        turnUpCard:
          allOf:
            - $ref: '#/components/schemas/Card'
          description: |
            The card turned up to propose trump. It is **not dealt to anyone** --
            it only enters play if it is swapped for the trump seven.
        makerIdx:
          type: integer
          description: "Seat that fixed trump; -1 while undecided"
        trick:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        trickLeaderIdx:
          type: integer
        trickNumber:
          type: integer
        validPlays:
          type: array
          items:
            type: integer
          description: |
            Hand indices the human may legally play. Sent because following
            suit, trumping when void and **overtrumping a trump lead** are all
            compulsory -- re-deriving that on the client always drifts.
        sequenceWinner:
          type: integer
          description: "Seat that won the sequence contest; -1 when nobody scored"
        belaHolder:
          type: integer
          description: "Seat holding trump K+Q; -1 when neither does"
        belaScored:
          type: boolean
        dixUsed:
          type: boolean
          description: Whether the trump seven was exchanged for the turn-up.
        bete:
          type: boolean
          description: |
            Whether the maker failed. The maker must score **strictly more**
            than the opponent -- a tie is bete, and the opponent takes both
            totals.
        schmeissBy:
          type: integer
          description: "Seat offering to throw the deal in; -1 when none"
        targetScore:
          type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    KaiserResponse:
      type: object
      description: Response payload for /kaiser/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              team:
                type: integer
                description: "0 for seats 0 and 2, 1 for seats 1 and 3"
              cardCount:
                type: integer
              cards:
                type: array
                description: Your own hand only; the opponents' are empty until the settlement.
                items:
                  $ref: '#/components/schemas/Card'
              isDealer:
                type: boolean
              isDeclarer:
                type: boolean
              isCurrentTurn:
                type: boolean
        phase:
          type: integer
          description: "0=Bid, 1=Discard, 2=Play, 3=HandEnd, 4=GameEnd"
        handNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        bidPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        bids:
          type: array
          description: This hand's bidding, in order. A pass is recorded with `value` 0.
          items:
            type: object
            properties:
              player:
                type: integer
              value:
                type: integer
                description: Points bid -- NOT a number of tricks.
              contract:
                type: integer
                description: "0=with trump, 1=no trump, 2=low no trump"
        highBid:
          type: object
          description: The standing bid, or null while nobody has bid.
          properties:
            player:
              type: integer
            value:
              type: integer
            contract:
              type: integer
        declarerIdx:
          type: integer
          description: "Seat that won the auction; -1 while undecided"
        trumpSuit:
          type: integer
          description: "1=Spade, 2=Clover, 3=Heart, 4=Diamond; 0 for a no-trump contract or before it is named"
        contract:
          type: integer
          description: "0=with trump, 1=no trump, 2=low no trump (the ranking reverses, seven high)"
        kittySize:
          type: integer
          description: |
            Cards still face down in the kitty. Two at the deal, zero once the
            declarer takes them. A 32-card pack could not produce one at all.
        trick:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        trickLeaderIdx:
          type: integer
        trickNumber:
          type: integer
        validPlays:
          type: array
          items:
            type: integer
          description: |
            Hand indices the human may legally play. Sent because following suit
            is compulsory (trumping is not).
        teamHandPoints:
          type: array
          items:
            type: integer
          description: |
            Points each team took this hand, indexed by team. The two always sum
            to 10 -- 8 tricks plus 5 for the heart five minus 3 for the spade three.
        teamScores:
          type: array
          items:
            type: integer
          description: Running game scores by team.
        heartFiveBy:
          type: integer
          description: "Seat that took the +5 heart five; -1 while it is unplayed"
        spadeThreeBy:
          type: integer
          description: "Seat that took the -3 spade three; -1 while it is unplayed"
        bidMade:
          type: boolean
          description: |
            Whether the declaring side reached its bid. When false the side
            loses the **bid amount** rather than scoring what it took.
        targetScore:
          type: integer
          description: "52, raised to 62 once any no-trump bid succeeds"
        minBid:
          type: integer
          description: "7 -- the floor is raised from 6 because the kitty is such an advantage"
        maxBid:
          type: integer
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: "Winning team; -1 while the game is live"
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    BostonResponse:
      type: object
      description: Response payload for /boston/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                description: |
                  Your own hand; also the declarer's under an "on the Table"
                  contract once the first trick is done, and everyone's at the
                  settlement.
                items:
                  $ref: '#/components/schemas/Card'
              tricksWon:
                type: integer
              chips:
                type: integer
              isDealer:
                type: boolean
              isDeclarer:
                type: boolean
              isPartner:
                type: boolean
              isDeclarerSide:
                type: boolean
                description: True for the declarer and, when one was called, the partner.
              isCurrentTurn:
                type: boolean
        phase:
          type: integer
          description: "0=Bid, 1=CallPartner, 2=Play, 3=HandEnd, 4=GameEnd"
        handNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        bidPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        bids:
          type: array
          description: This hand's auction, in order. A pass is recorded with `level` 0.
          items:
            type: object
            properties:
              player:
                type: integer
              level:
                type: integer
              name:
                type: string
              suit:
                type: integer
        highBid:
          type: object
          description: The standing bid, or null while nobody has bid.
          properties:
            player:
              type: integer
            level:
              type: integer
            name:
              type: string
            suit:
              type: integer
        bidOptions:
          type: array
          description: |
            The whole ladder in rank order, sent by the server. The miseres sit
            BETWEEN the trick bids, so a client that rebuilds this list from
            trick counts alone will get the order wrong.
          items:
            type: object
            properties:
              level:
                type: integer
              name:
                type: string
              kind:
                type: integer
                description: "1=trick count, 2=misere, 3=piccolissimo"
              tricks:
                type: integer
                description: Target trick count -- 0 for a misere, 1 for Piccolissimo.
              needsTrump:
                type: boolean
              exposed:
                type: boolean
                description: Whether the declarer's hand is shown after the first trick.
              canCallPartner:
                type: boolean
              payout:
                type: integer
        declarerIdx:
          type: integer
          description: "Seat that won the auction; -1 while undecided"
        partnerIdx:
          type: integer
          description: "Seat called as partner; -1 when the declarer plays alone"
        trumpSuit:
          type: integer
          description: "1=Spade, 2=Clover, 3=Heart, 4=Diamond; 0 for a no-trump contract"
        exposed:
          type: boolean
        trick:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        validPlays:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (following suit is compulsory).
        trickLeaderIdx:
          type: integer
        trickNumber:
          type: integer
        declarerTricks:
          type: integer
          description: |
            Tricks the declaring side has taken -- the declarer's plus the
            partner's, since a called partner's tricks count toward the contract.
        bidMade:
          type: boolean
        handSize:
          type: integer
        targetHands:
          type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    VintResponse:
      type: object
      description: Response payload for /vint/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              team:
                type: integer
                description: "0 for seats 0 and 2, 1 for seats 1 and 3"
              cardCount:
                type: integer
              cards:
                type: array
                description: |
                  Your own hand only until the settlement. **There is no dummy**
                  -- unlike bridge, even your partner's hand stays concealed.
                items:
                  $ref: '#/components/schemas/Card'
              tricksWon:
                type: integer
              isDealer:
                type: boolean
              isDeclarer:
                type: boolean
              isCurrentTurn:
                type: boolean
        phase:
          type: integer
          description: "0=Bid, 1=Play, 2=HandEnd, 3=GameEnd"
        handNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        bidPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        bids:
          type: array
          description: This hand's auction, in order. A pass is recorded with `level` 0.
          items:
            type: object
            properties:
              player:
                type: integer
              level:
                type: integer
              denom:
                type: integer
                description: "0=Spade, 1=Club, 2=Diamond, 3=Heart, 4=NoTrump -- spades LOWEST"
              trickValue:
                type: integer
        highBid:
          type: object
          description: The standing bid, or null while nobody has bid.
          properties:
            player:
              type: integer
            level:
              type: integer
            denom:
              type: integer
            trickValue:
              type: integer
        declarerIdx:
          type: integer
          description: "Seat that won the auction; -1 while undecided"
        trumpSuit:
          type: integer
          description: "1=Spade, 2=Clover, 3=Heart, 4=Diamond; 0 at no trump"
        trick:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        validPlays:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (following suit is compulsory).
        trickLeaderIdx:
          type: integer
        trickNumber:
          type: integer
        teamTricks:
          type: array
          items:
            type: integer
          description: Tricks each team has taken this hand.
        below:
          type: array
          items:
            type: integer
          description: |
            Below-the-line totals by team. **Both teams score here for the
            tricks they took**, whether or not the contract was made. Reset to
            zero whenever a team reaches 500 and takes a game.
        above:
          type: array
          items:
            type: integer
          description: Above-the-line totals by team -- honours, aces, penalties and game bonuses.
        gamesWon:
          type: array
          items:
            type: integer
          description: Games each team has taken. Two takes the rubber.
        lastResult:
          type: object
          description: Breakdown of the most recent settlement, or null before the first one.
          properties:
            trickPoints:
              type: array
              items:
                type: integer
              description: What each team scored below the line for its own tricks.
            honourPoints:
              type: array
              items:
                type: integer
              description: |
                Trump A K Q J 10. Scores only from **three or more** (20x / 30x
                / 40x the trick value); a no-trump contract has none.
            acePoints:
              type: array
              items:
                type: integer
              description: |
                Scored separately from honours. The side with the majority takes
                10x the trick value per ace; a 2-2 split goes to the side with
                more tricks.
            penalty:
              type: array
              items:
                type: integer
              description: "Undertricks x level x 500, scored above the opponents' line"
            made:
              type: boolean
            declarerTricks:
              type: integer
            trickValue:
              type: integer
        trickValues:
          type: array
          items:
            type: integer
          description: |
            Level-1 trick value per denomination, indexed by denom (spades 4,
            clubs 6, diamonds 8, hearts 10, no trump 12). Each level above adds 10.
        gameTarget:
          type: integer
          description: Below-the-line points needed for a game (500).
        minLevel:
          type: integer
        maxLevel:
          type: integer
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: "Team that took the rubber; -1 while it is live"
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    BidEuchreResponse:
      type: object
      description: Response payload for /bideuchre/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              team:
                type: integer
                description: "0 for seats 0 and 2, 1 for seats 1 and 3"
              cardCount:
                type: integer
              cards:
                type: array
                description: |
                  Populated for the human seat, and for every seat once the hand
                  is settled. There is no kitty and no dummy, so no other hand is
                  ever visible during play.
                items:
                  $ref: '#/components/schemas/Card'
              tricksWon:
                type: integer
              isDealer:
                type: boolean
              isDeclarer:
                type: boolean
              isCurrentTurn:
                type: boolean
        phase:
          type: integer
          description: "0=Bid, 1=ChooseTrump, 2=Play, 3=HandEnd, 4=GameEnd"
        handNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        bidPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
          description: The dealer alone may take the contract by EQUALLING the standing bid.
        bids:
          type: array
          items:
            type: object
            properties:
              player:
                type: integer
              value:
                type: integer
                description: Tricks bid; 0 for a pass.
        highBid:
          type: object
          nullable: true
          properties:
            player:
              type: integer
            value:
              type: integer
        declarerIdx:
          type: integer
          description: "-1 until the auction settles"
        trump:
          type: integer
          description: |
            The declaration itself: 0=Spade, 1=Club, 2=Diamond, 3=Heart,
            4=NoTrump high, 5=NoTrump LOW. `trumpSuit` alone cannot tell the two
            no-trump forms apart.
        trumpSuit:
          type: integer
          description: "Trump suit, or 0 at either no trump"
        trumpChosen:
          type: boolean
        trick:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        validPlays:
          type: array
          items:
            type: integer
          description: |
            Hand indexes the human may play. Following suit is compulsory and the
            left bower counts as a trump, so the server decides this.
        trickLeaderIdx:
          type: integer
        trickNumber:
          type: integer
        teamTricks:
          type: array
          items:
            type: integer
        scores:
          type: array
          items:
            type: integer
        lastResult:
          type: object
          nullable: true
          properties:
            points:
              type: array
              items:
                type: integer
              description: |
                What each team scored. A set costs the declaring side its BID,
                not the tricks it took; the defenders score their own tricks
                either way.
            tricks:
              type: array
              items:
                type: integer
            made:
              type: boolean
            bid:
              type: integer
        gameTarget:
          type: integer
          description: Points needed to win (32).
        minBid:
          type: integer
          description: The floor, three tricks.
        maxBid:
          type: integer
        handSize:
          type: integer
          description: Six -- the whole 24-card pack is dealt out, leaving no kitty.
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: "Winning team; -1 while the game is live"
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            allowNoTrump:
              type: boolean
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    SixBidSoloResponse:
      type: object
      description: Response payload for /sixbidsolo/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                description: |
                  Populated for the human seat, for every seat once the hand is
                  settled, and for the declarer while a spread misere has its
                  hand laid down.
                items:
                  $ref: '#/components/schemas/Card'
              points:
                type: integer
                description: Card points taken this hand.
              tricksWon:
                type: integer
              score:
                type: integer
                description: Running total across hands.
              isDealer:
                type: boolean
              isDeclarer:
                type: boolean
              isCurrentTurn:
                type: boolean
        phase:
          type: integer
          description: "0=Bid, 1=Declare, 2=Play, 3=HandEnd, 4=GameEnd"
        handNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        bidPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        bids:
          type: array
          items:
            type: object
            properties:
              player:
                type: integer
              kind:
                type: integer
                description: "0=pass, then the six bids in ascending order"
        highBid:
          type: object
          nullable: true
          properties:
            player:
              type: integer
            kind:
              type: integer
        declarerIdx:
          type: integer
          description: "-1 until the auction settles"
        trumpSuit:
          type: integer
          description: "Trump suit, or 0 at either misere"
        declared:
          type: boolean
        calledCard:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Card'
          description: The card a call solo named; its holder had to exchange it.
        spreadOpen:
          type: boolean
          description: Whether a spread misere has laid the declarer's hand face up.
        widow:
          type: array
          description: |
            The three face-down cards, sent only once the hand settles. They are
            CREDITED TO THE DECLARER at the end, except at either misere.
          items:
            $ref: '#/components/schemas/Card'
        widowSize:
          type: integer
          description: How many cards are face down (three), sent even while concealed.
        trick:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        validPlays:
          type: array
          items:
            type: integer
          description: Hand indexes the human may play; following suit is compulsory.
        trickLeaderIdx:
          type: integer
        trickNumber:
          type: integer
        lastResult:
          type: object
          nullable: true
          properties:
            kind:
              type: integer
            declarer:
              type: integer
            declarerPoints:
              type: integer
              description: Card points the declarer took, INCLUDING the widow.
            widowPoints:
              type: integer
              description: What the widow added. ZERO at either misere.
            target:
              type: integer
            made:
              type: boolean
            value:
              type: integer
              description: What each opponent pays or receives.
            deltas:
              type: array
              items:
                type: integer
        bidTargets:
          type: array
          items:
            type: integer
          description: |
            Target card points per bid, indexed by kind. A plain bid needs 61,
            not 60, and guarantee solo depends on the trump suit (74 at hearts,
            80 elsewhere), so the resolved table is sent.
        totalPoints:
          type: integer
          description: Card points in play (120).
        baseTarget:
          type: integer
          description: The base a plain bid must EXCEED (60).
        handSize:
          type: integer
          description: Cards per hand -- eleven, with three more in the widow.
        targetHands:
          type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "Winning seat; -1 while the game is live"
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetHands:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    KarnoffelResponse:
      type: object
      description: Response payload for /karnoffel/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              team:
                type: integer
                description: "0 for seats 0 and 2, 1 for seats 1 and 3"
              cardCount:
                type: integer
              cards:
                type: array
                description: Populated for the human seat, and for every seat once the hand is over.
                items:
                  $ref: '#/components/schemas/Card'
              upCard:
                nullable: true
                allOf:
                  - $ref: '#/components/schemas/Card'
                description: |
                  The single card dealt face up in front of this seat. THE
                  LOWEST OF THE FOUR decides the chosen suit, so every seat's is
                  public from the start.
              tricksWon:
                type: integer
              isDealer:
                type: boolean
              isCurrentTurn:
                type: boolean
        phase:
          type: integer
          description: "0=Play, 1=HandEnd, 2=GameEnd"
        handNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        chosenSuit:
          type: integer
          description: "1=Spade, 2=Clover, 3=Heart, 4=Diamond"
        trick:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        validPlays:
          type: array
          items:
            type: integer
          description: |
            Hand indexes the human may play. There is NO obligation to follow
            suit; this narrows only to keep the devil out of the opening lead,
            which depends on the chosen suit.
        trickLeaderIdx:
          type: integer
        trickNumber:
          type: integer
        teamTricks:
          type: array
          items:
            type: integer
        handsWon:
          type: array
          items:
            type: integer
        lastResult:
          type: object
          nullable: true
          properties:
            winnerTeam:
              type: integer
              description: "The team that took the hand; -1 if neither reached three tricks"
            tricks:
              type: array
              items:
                type: integer
            chosenSuit:
              type: integer
        tricksToWin:
          type: integer
          description: Tricks that take a hand (three).
        handSize:
          type: integer
          description: Cards per hand -- FIVE, not twelve.
        targetHands:
          type: integer
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: "Winning team; -1 while the game is live"
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetHands:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    ShengJiResponse:
      type: object
      description: Response payload for /shengji/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              team:
                type: integer
                description: "0 for seats 0/2, 1 for seats 1/3 -- partners sit opposite"
              cardCount:
                type: integer
              cards:
                type: array
                description: The human's own hand only, until the hand is settled.
                items:
                  $ref: '#/components/schemas/Card'
              isDeclarer:
                type: boolean
                description: |
                  True for the side whose level is being played. THE DECLARERS
                  DO NOT COLLECT POINTS -- they win by holding the defenders
                  under the target.
              isCurrentTurn:
                type: boolean
        phase:
          type: integer
          description: "0=Declare, 1=Kitty, 2=Play, 3=HandEnd, 4=GameEnd"
        handNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        level:
          type: integer
          description: |
            This hand's level rank. EVERY CARD OF THIS RANK, IN ALL FOUR SUITS,
            IS A TRUMP -- not just the ones in the trump suit.
        teamLevels:
          type: array
          items:
            type: integer
        declarerTeam:
          type: integer
        trumpSuit:
          type: integer
          description: "1-4, or 0 for no trump suit (level cards and jokers are still trumps)"
        declaration:
          type: object
          nullable: true
          properties:
            seat:
              type: integer
            suit:
              type: integer
            strength:
              type: integer
              description: "1 for a single level card, 2 for a pair. ONLY A STRONGER SHOWING OVERRIDES."
        declarableSuits:
          type: object
          additionalProperties:
            type: integer
          description: Suits the human can declare right now, keyed by suit number, valued by strength.
        kittySize:
          type: integer
        kitty:
          type: array
          description: The kitty's cards -- EMPTY until the hand is settled.
          items:
            $ref: '#/components/schemas/Card'
        trick:
          type: array
          items:
            type: object
            properties:
              seat:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
        trickLeader:
          type: integer
        leadCombo:
          type: object
          nullable: true
          properties:
            kind:
              type: integer
              description: "0=none, 1=single, 2=pair, 3=tractor (consecutive pairs)"
            rank:
              type: integer
            size:
              type: integer
            trump:
              type: boolean
            suit:
              type: integer
        teamPoints:
          type: array
          items:
            type: integer
          description: Points each team collected this hand. ONLY THE DEFENDERS' TOTAL COUNTS.
        trickCount:
          type: integer
        lastTrickWinner:
          type: integer
        lastResult:
          type: object
          nullable: true
          properties:
            declarerTeam:
              type: integer
            defenderPoints:
              type: integer
              description: Points the DEFENDERS collected -- they are the side that scores.
            kittyPoints:
              type: integer
            kittyMultiplier:
              type: integer
              description: "Twice the winning play's size; 0 when the declarers took the last trick."
            declarerHeld:
              type: boolean
            advance:
              type: integer
            advancingTeam:
              type: integer
        minLevel:
          type: integer
        maxLevel:
          type: integer
        kittySizeMax:
          type: integer
        totalPoints:
          type: integer
          description: Points in the pack -- 200.
        defenderTarget:
          type: integer
          description: What the defenders need to take the deal -- 80, two fifths of the pack.
        advanceStep:
          type: integer
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
    GuandanResponse:
      type: object
      description: Response payload for /guandan/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              team:
                type: integer
                description: "0 for seats 0/2, 1 for seats 1/3 -- partners sit opposite"
              cardCount:
                type: integer
              cards:
                type: array
                description: The human's own hand only, until the hand is settled.
                items:
                  $ref: '#/components/schemas/Card'
              finishedRank:
                type: integer
                description: |
                  1-4 once this seat has gone out, 0 while it still holds cards.
                  The order sets BOTH the next hand's tribute and the climb.
              isCurrentTurn:
                type: boolean
        phase:
          type: integer
          description: "0=Tribute, 1=Play, 2=HandEnd, 3=GameEnd"
        handNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        level:
          type: integer
          description: |
            This hand's level rank. CARDS OF THIS RANK BEAT ACES and lose only
            to the jokers; the two hearts among them are wild.
        teamLevels:
          type: array
          items:
            type: integer
          description: Each team's current level, 2 through 14 (ace).
        declarerTeam:
          type: integer
        lastCombo:
          type: object
          nullable: true
          properties:
            kind:
              type: integer
              description: |
                0=none, 1=single, 2=pair, 3=triple, 4=full house, 5=straight,
                6=plate, 7=tube, 8=bomb, 9=straight flush, 10=joker bomb
            rank:
              type: integer
            size:
              type: integer
        lastPlayerIdx:
          type: integer
        finished:
          type: array
          items:
            type: integer
          description: Seats in the order they went out this hand.
        tributes:
          type: array
          items:
            type: object
            properties:
              from:
                type: integer
              to:
                type: integer
              card:
                nullable: true
                allOf:
                  - $ref: '#/components/schemas/Card'
              returned:
                nullable: true
                allOf:
                  - $ref: '#/components/schemas/Card'
        tributeCancelled:
          type: boolean
          description: True when a payer held both red jokers, which cancels tribute outright.
        lastResult:
          type: object
          nullable: true
          properties:
            order:
              type: array
              items:
                type: integer
            winnerTeam:
              type: integer
            advance:
              type: integer
              description: "Levels climbed. ONE OF 1, 2 OR 4 -- never 3."
            firstSecond:
              type: boolean
        minLevel:
          type: integer
        maxLevel:
          type: integer
        advanceFirstSecond:
          type: integer
          description: Levels gained by taking first and second -- FOUR.
        advanceFirstThird:
          type: integer
        advanceFirstFourth:
          type: integer
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: Winning team; -1 while the game is live.
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
    LiteratureResponse:
      type: object
      description: Response payload for /literature/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              team:
                type: integer
                description: "0 for seats 0/2/4, 1 for seats 1/3/5 -- seating alternates"
              cardCount:
                type: integer
              cards:
                type: array
                description: |
                  The human's own hand only, until the game ends. EVEN A
                  TEAMMATE'S HAND IS HIDDEN -- deducing where the cards are is
                  the game.
                items:
                  $ref: '#/components/schemas/Card'
              isCurrentTurn:
                type: boolean
        phase:
          type: integer
          description: "0=Play, 1=GameEnd"
        currentPlayerIdx:
          type: integer
        halfSuits:
          type: array
          items:
            type: integer
          description: "Per half-suit: 0=open, 1=team 0, 2=team 1, 3=CANCELLED"
        halfSuitCards:
          type: array
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
          description: The six cards of each half-suit, so the client can offer them.
        asks:
          type: array
          description: The ask history. PUBLIC INFORMATION -- this is what deduction runs on.
          items:
            type: object
            properties:
              from:
                type: integer
              to:
                type: integer
              card:
                nullable: true
                allOf:
                  - $ref: '#/components/schemas/Card'
              success:
                type: boolean
        claims:
          type: array
          items:
            type: object
            properties:
              player:
                type: integer
              halfSuit:
                type: integer
              outcome:
                type: integer
                description: "0=won, 1=CANCELLED, 2=lost to the opponents"
              awardedTeam:
                type: integer
                description: "The team that took it; -1 when cancelled"
        lastAsk:
          type: object
          nullable: true
        lastClaim:
          type: object
          nullable: true
        teamHalfSuits:
          type: array
          items:
            type: integer
        cancelledCount:
          type: integer
          description: |
            Cancelled half-suits belong to nobody, so the two team totals need
            not add to eight.
        openCount:
          type: integer
        winThreshold:
          type: integer
          description: Half-suits needed to win -- FIVE, a majority of eight, not four.
        halfSuitCnt:
          type: integer
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: "Winning team; -1 while live, and also -1 if it ends level"
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    ToepenResponse:
      type: object
      description: Response payload for /toepen/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
                description: Hand size. Always sent, including while hidden.
              cards:
                type: array
                description: Empty while `hidden` is true.
                items:
                  $ref: '#/components/schemas/Card'
              lives:
                type: integer
                description: Lives lost so far. `maxLives` ends the player's game.
              folded:
                type: boolean
                description: Out of THIS hand, having declined a toep.
              eliminated:
                type: boolean
                description: Out of the game.
              hidden:
                type: boolean
        phase:
          type: integer
          description: "0=Play, 1=Respond, 2=HandEnd, 3=GameEnd"
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        currentTrick:
          type: array
          description: Cards played into the trick so far, in play order.
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        leadSuit:
          type: integer
          description: "-1 before anything is led"
        trickNumber:
          type: integer
        handNumber:
          type: integer
        stake:
          type: integer
          description: |
            Lives this hand now costs. Starts at 1 and rises by one per toep.
            Folding costs `stake - 1`, the value BEFORE the raise being declined.
        knockerIdx:
          type: integer
          description: "Who toeped; -1 outside a response phase"
        pendingRespondent:
          type: integer
          description: "Whose answer is awaited; -1 when none"
        lastTrickWinner:
          type: integer
          description: The only seat that escapes the penalty when the hand settles.
        maxLives:
          type: integer
          description: Lives that eliminate a player. Sent so the figure is not written down twice.
        validPlayIndices:
          type: array
          description: |
            Hand indices the human may legally play. Carries the follow-suit
            obligation, so the client never re-derives it.
          items:
            type: integer
        canRedeal:
          type: boolean
          description: |
            Whether the human may throw in a poverty hand right now. Decided
            here so the client does not count card ranks a second time.
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "-1 while undecided"
        hint:
          type: object
          description: Suggested move, sent on every response while the round is live.
          properties:
            cardIndex:
              type: integer
            fold:
              type: boolean
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    MushiCard:
      allOf:
        - $ref: '#/components/schemas/Card'
        - type: object
          properties:
            category:
              type: integer
              description: "0=chaff, 1=ribbon, 2=animal, 3=bright"
            points:
              type: integer
              description: |
                Card value (20/10/5/1). Sent so the 40-card identity table is
                not carried a second time in the client.
            isWild:
              type: boolean
              description: True only for November's lightning card.
    MushiResponse:
      type: object
      description: Response payload for /mushi/exec
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
                description: Hand size. Always sent, including while hidden.
              cards:
                type: array
                description: Empty while `hidden` is true.
                items:
                  $ref: '#/components/schemas/MushiCard'
              captured:
                type: array
                description: |
                  ALWAYS sent for both seats. Captured cards are public in
                  hanafuda -- hiding them would remove the read on what the
                  opponent is collecting.
                items:
                  $ref: '#/components/schemas/MushiCard'
              capturedPoints:
                type: integer
              score:
                type: integer
                description: Cumulative score across rounds.
              roundResult:
                type: integer
                description: The change from the round just settled.
              hidden:
                type: boolean
                description: Whether this seat's HAND is withheld.
        field:
          type: array
          items:
            $ref: '#/components/schemas/MushiCard'
        phase:
          type: integer
          description: "0=Play, 1=Select, 2=WildSelect, 3=RoundEnd, 4=GameEnd"
        currentPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        roundNumber:
          type: integer
        targetRounds:
          type: integer
        stockCount:
          type: integer
        pendingCard:
          $ref: '#/components/schemas/MushiCard'
        selectableIndices:
          type: array
          description: |
            Field indices that may be taken right now. Carries the wild's
            "not another willow" restriction so the client need not re-derive it.
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "-1 when undecided or tied"
        hint:
          type: object
          description: Suggested move, sent on every response while it is the human's turn.
          properties:
            cardIndex:
              type: integer
            fieldIndex:
              type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    BuraResponse:
      type: object
      description: Response payload for /bura/exec
      properties:
        players:
          type: array
          description: |
            Both seats. A seat with `hidden` true carries NO cards -- only
            `cardCount`, because how many cards someone holds is public while
            which cards they are is not.
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
                description: Always sent, including while the hand is hidden.
              cards:
                type: array
                description: Empty while `hidden` is true.
                items:
                  $ref: '#/components/schemas/Card'
              points:
                type: integer
                description: Card points taken in tricks so far.
              hidden:
                type: boolean
        phase:
          type: integer
          description: "0=Play, 1=GameEnd"
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
          description: Seat to act; -1 once the round is over.
        leadPlayerIdx:
          type: integer
        currentLead:
          type: array
          description: Cards led this trick, one to three of a single suit. Empty between tricks.
          items:
            $ref: '#/components/schemas/Card'
        trumpSuit:
          type: integer
        trumpCard:
          $ref: '#/components/schemas/Card'
        stockRemaining:
          type: integer
          description: Excludes the face-up trump indicator, which is drawn last.
        winThreshold:
          type: integer
          description: |
            Card points needed to claim a win (31). Sent rather than left for the
            client to hardcode, so the figure is not written down twice.
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "-1 when undecided or drawn"
        isDraw:
          type: boolean
          description: |
            True when the stock and both hands emptied with nobody claiming.
            Nobody wins -- points alone never decide a Bura round.
        hint:
          type: object
          description: Suggested play for the human seat, sent on every response while it is their turn.
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
              description: "Reason identifier, e.g. bura.hint.lead"
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    TerraceResponse:
      type: object
      description: Response payload for /terrace/exec
      properties:
        reserve:
          type: array
          description: |
            The 11-card terrace. Only the last element is playable, its only
            destination is a foundation, and it is never refilled -- so its depth
            is the number that decides the game.
          items:
            $ref: '#/components/schemas/Card'
        tableau:
          type: array
          description: Nine piles, dealt one card each at the start. An empty pile refills itself.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        foundation:
          type: array
          description: Eight foundations built up in ALTERNATING COLOUR from `baseRank`, 13 cards each.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        stockCount:
          type: integer
          description: Cards left in the stock. Only ever decreases -- there is no redeal.
        waste:
          type: array
          description: Turned cards. Only the last is available.
          items:
            $ref: '#/components/schemas/Card'
        baseRank:
          type: integer
          description: The rank all eight foundations start from. 0 means it has not been fixed yet.
        awaitingBaseRank:
          type: boolean
          description: |
            While true, the next card sent to a foundation fixes the base rank.
            Sent alongside `baseRank` so the client never has to encode the
            "0 means unchosen" convention itself.
        phase:
          type: integer
          description: "0=Playing, 1=GameClear, 2=GameOver"
        moveCount:
          type: integer
        canUndo:
          type: boolean
        isStalemate:
          type: boolean
        undoToEscape:
          type: integer
          description: Number of undos needed to escape stalemate (0 = not stalemate, -1 = unreachable)
        hint:
          $ref: '#/components/schemas/TerraceHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    CongressMoveZone:
      type: object
      description: Source or target zone for a Congress card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: Zone identifier
          enum: [tableau, waste, stock, foundation]
        col:
          type: integer
          description: |
            Tableau pile (0..7). Required for a `tableau` source and destination;
            ignored for `waste`, `stock`, and for a `foundation` destination the
            server picks the pile from the card's suit.
    CongressHint:
      type: object
      description: Suggested next move for Congress
      properties:
        fromZone:
          type: string
          enum: [tableau, waste, stock]
        fromIdx:
          type: integer
          description: Source pile, or -1 when the source is the waste or the stock
        toZone:
          type: string
          description: "`waste` means: turn a card from the stock."
          enum: [foundation, tableau, waste]
        toIdx:
          type: integer
          description: Destination index, or -1 for a draw, which targets no single pile
    CongressResponse:
      type: object
      description: Response payload for /congress/exec
      properties:
        tableau:
          type: array
          description: |
            Eight piles, dealt one card each at the start. Only the top card of a
            pile is playable, and an empty pile can only be refilled from the
            stock or the waste.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        foundation:
          type: array
          description: Eight suit-pinned foundations, two per suit, opened by Aces and built up to Kings.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        stockCount:
          type: integer
          description: Cards left in the stock. Only ever decreases -- there is no redeal.
        waste:
          type: array
          description: Turned cards. Only the last is available.
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "0=Playing, 1=GameClear, 2=GameOver"
        moveCount:
          type: integer
        canUndo:
          type: boolean
        isStalemate:
          type: boolean
        undoToEscape:
          type: integer
          description: Number of undos needed to escape stalemate (0 = not stalemate, -1 = unreachable)
        hint:
          $ref: '#/components/schemas/CongressHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    AmericanToadMoveZone:
      type: object
      description: Source or target zone for an American Toad card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: Zone identifier
          enum: [reserve, waste, tableau, foundation]
        col:
          type: integer
          description: |
            Tableau column (0..7). Required for a `tableau` source and for a
            `tableau` destination; ignored for `reserve` and `waste`, and for a
            `foundation` destination the server picks the pile from the card's suit.
        cardIndex:
          type: integer
          description: |
            Head of the same-suit descending run to carry, for a `tableau` source.
            Omit to move only the top card (the server then uses -1, which it
            resolves to the top of the column).
    AmericanToadTableauCard:
      type: object
      description: A single tableau card. Every card is face-up by rule, but the flag is surfaced rather than assumed.
      properties:
        card:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Card'
        faceUp:
          type: boolean
    AmericanToadHint:
      type: object
      description: Suggested next move for American Toad
      properties:
        fromZone:
          type: string
          description: "`stock` means: turn a card to the waste (or redeal)."
          enum: [reserve, waste, tableau, stock]
        fromIdx:
          type: integer
          description: Source column, or -1 when the source is not the tableau
        cardIndex:
          type: integer
          description: Head of the run to carry, or -1 when the source is not the tableau
        toZone:
          type: string
          enum: [foundation, tableau, waste]
        toIdx:
          type: integer
          description: Destination index, or -1 for a draw, which targets no single pile
    AmericanToadResponse:
      type: object
      description: Response payload for /americantoad/exec
      properties:
        reserve:
          type: array
          description: |
            The 20-card reserve. Only the last element is playable, but the depth
            matters: while it is non-empty, empty columns refill from it
            automatically and cannot be filled by hand.
          items:
            $ref: '#/components/schemas/Card'
        tableau:
          type: array
          description: Eight columns, dealt one card each at the start.
          items:
            type: array
            items:
              $ref: '#/components/schemas/AmericanToadTableauCard'
        foundation:
          type: array
          description: Eight suit-pinned foundations, two per suit, all opened at `baseRank`.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        stockCount:
          type: integer
          description: Cards left in the stock.
        waste:
          type: array
          description: Turned cards. Only the last is available.
          items:
            $ref: '#/components/schemas/Card'
        baseRank:
          type: integer
          description: The rank all eight foundations start from, fixed by the deal.
        passesUsed:
          type: integer
          description: How many times the stock has been recycled. Two passes are allowed in all.
        canRedeal:
          type: boolean
          description: |
            Whether `draw` would recycle the waste into the stock: the stock is
            empty, the waste is not, and the single redeal is unspent. Sent so the
            client does not recompute the condition.
        phase:
          type: integer
          description: "0=Playing, 1=GameClear, 2=GameOver"
        moveCount:
          type: integer
        canUndo:
          type: boolean
        isStalemate:
          type: boolean
          description: False while a redeal is still available, since that is itself a move.
        undoToEscape:
          type: integer
          description: Number of undos needed to escape stalemate (0 = not stalemate, -1 = unreachable)
        hint:
          $ref: '#/components/schemas/AmericanToadHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    WindmillMoveZone:
      type: object
      description: Source or target zone for a Windmill card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: Zone identifier
          enum: [sail, waste, corner, center]
        col:
          type: integer
          description: |
            Sail index (0..7) for a `sail` source, or corner index (0..3) for a
            `corner` source and destination. Ignored for `waste` and `center`.
    WindmillHint:
      type: object
      description: Suggested next move for Windmill
      properties:
        fromZone:
          type: string
          description: "`stock` means: turn a card to the waste."
          enum: [sail, waste, corner, stock]
        fromIdx:
          type: integer
          description: Sail or corner index, or -1 when the source is the waste or the stock
        toZone:
          type: string
          enum: [center, corner, waste]
        toIdx:
          type: integer
          description: Corner index, or -1 when the destination is not a corner
    WindmillResponse:
      type: object
      description: Response payload for /windmill/exec
      properties:
        sails:
          type: array
          description: |
            Eight fixed slots. A slot is refilled the moment it empties, so a
            null only appears once both the waste and the stock are exhausted --
            it is sent as null rather than compacted away so the cross keeps its
            shape.
          items:
            nullable: true
            allOf:
              - $ref: '#/components/schemas/Card'
        center:
          type: array
          description: The centre foundation. 52 cards when complete (A-K four times through).
          items:
            $ref: '#/components/schemas/Card'
        corners:
          type: array
          description: Four corner foundations, each King down to Ace (13 cards).
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        stockCount:
          type: integer
          description: Cards left in the stock. Only ever decreases -- there is no redeal.
        waste:
          type: array
          description: Turned cards. Only the last is available.
          items:
            $ref: '#/components/schemas/Card'
        transferBlocked:
          type: boolean
          description: |
            True right after a corner card was pulled back onto the centre. While
            true another pull-back is refused; the next centre card must come from
            a sail or the waste.
        phase:
          type: integer
          description: "0=Playing, 1=GameClear, 2=GameOver"
        moveCount:
          type: integer
        canUndo:
          type: boolean
        isStalemate:
          type: boolean
        undoToEscape:
          type: integer
          description: Number of undos needed to escape stalemate (0 = not stalemate, -1 = unreachable)
        hint:
          $ref: '#/components/schemas/WindmillHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    DuchessMoveZone:
      type: object
      description: Source or target zone for a Duchess card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: Zone identifier
          enum: [reserve, waste, tableau, foundation]
        col:
          type: integer
          description: |
            Reserve fan (0..3) for a `reserve` source, or tableau column (0..3)
            for a `tableau` source and destination. Ignored for `waste`, and for
            a `foundation` destination the server picks the pile from the card's
            suit. Also carries the fan index for the `base` command.
        cardIndex:
          type: integer
          description: |
            Head of the alternating-colour run to carry, for a `tableau` source.
            Omit to move only the top card (the server then uses -1, which it
            resolves to the top of the column).
    DuchessTableauCard:
      type: object
      description: A single tableau card. Every card is face-up by rule, but the flag is surfaced rather than assumed.
      properties:
        card:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Card'
        faceUp:
          type: boolean
    DuchessHint:
      type: object
      description: Suggested next move for Duchess
      properties:
        fromZone:
          type: string
          description: "`stock` means: turn a card to the waste."
          enum: [reserve, waste, tableau, stock]
        fromIdx:
          type: integer
          description: Reserve fan or tableau column, or -1 when the source is the waste or the stock
        cardIndex:
          type: integer
          description: Head of the run to carry, or -1 when the source is not the tableau
        toZone:
          type: string
          enum: [foundation, tableau, waste]
        toIdx:
          type: integer
          description: Destination index, or -1 for a draw, which targets no single pile
    DuchessResponse:
      type: object
      description: Response payload for /duchess/exec
      properties:
        reserve:
          type: array
          description: Four fans of three cards. Only the last element of each is available.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        tableau:
          type: array
          description: Four columns, dealt one card each at the start.
          items:
            type: array
            items:
              $ref: '#/components/schemas/DuchessTableauCard'
        foundation:
          type: array
          description: Four suit-pinned foundations, all opened at `baseRank`.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        stockCount:
          type: integer
          description: Cards left in the stock. Only ever decreases -- there is no redeal.
        waste:
          type: array
          description: Turned cards. Only the last is available.
          items:
            $ref: '#/components/schemas/Card'
        baseRank:
          type: integer
          description: The rank all four foundations start from. 0 means it has not been chosen yet.
        awaitingBaseRank:
          type: boolean
          description: |
            While true, `base` is the only command that will be accepted. Sent
            alongside `baseRank` so the client never has to encode the
            "0 means unchosen" convention itself.
        phase:
          type: integer
          description: "0=Playing, 1=GameClear, 2=GameOver"
        moveCount:
          type: integer
        canUndo:
          type: boolean
        isStalemate:
          type: boolean
        undoToEscape:
          type: integer
          description: Number of undos needed to escape stalemate (0 = not stalemate, -1 = unreachable)
        hint:
          $ref: '#/components/schemas/DuchessHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    MissMilliganMoveZone:
      type: object
      description: Source or target zone for a Miss Milligan card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: Zone identifier
          enum: [tableau, waived, foundation]
        col:
          type: integer
          description: |
            Tableau column (0..7). Required for a `tableau` source and for a
            `tableau` destination; ignored for `waived`, and for a `foundation`
            destination the server picks the pile from the card's suit.
        cardIndex:
          type: integer
          description: |
            Head of the alternating-colour run to carry, for a `tableau` source.
            Omit to move only the top card (the server then uses -1, which it
            resolves to the top of the column). Also used by the `waive` command
            to choose how much of a column to lift aside.
    MissMilliganTableauCard:
      type: object
      description: A single tableau card. Every card is face-up by rule, but the flag is surfaced rather than assumed.
      properties:
        card:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Card'
        faceUp:
          type: boolean
    MissMilliganHint:
      type: object
      description: Suggested next move for Miss Milligan
      properties:
        fromZone:
          type: string
          description: "`stock` means: deal a row onto every column."
          enum: [tableau, waived, stock]
        fromCol:
          type: integer
          description: Source column, or -1 when the source is the waived set or the stock
        cardIndex:
          type: integer
          description: Head of the run to carry, or -1 when the source is not the tableau
        toZone:
          type: string
          enum: [foundation, tableau]
        toIdx:
          type: integer
          description: Destination index, or -1 for a deal, which targets no single column
    MissMilliganResponse:
      type: object
      description: Response payload for /missmilligan/exec
      properties:
        tableau:
          type: array
          description: Eight columns, dealt one card each at the start.
          items:
            type: array
            items:
              $ref: '#/components/schemas/MissMilliganTableauCard'
        stockCount:
          type: integer
          description: Cards left in the stock. Only ever decreases, eight at a time.
        foundation:
          type: array
          description: Eight foundations, two per suit, opened by Aces.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        waived:
          type: array
          description: Cards held aside by waiving. While this is non-empty, dealing and a second waive are both refused.
          items:
            $ref: '#/components/schemas/Card'
        canWaive:
          type: boolean
          description: |
            Whether waiving is available: the stock is exhausted and nothing is
            currently held. Sent so the client does not recompute the condition.
        phase:
          type: integer
          description: "0=Playing, 1=GameClear, 2=GameOver"
        moveCount:
          type: integer
        canUndo:
          type: boolean
        isStalemate:
          type: boolean
          description: |
            False while a waive is still available, even when no ordinary move
            exists -- waiving is a rescue, so the board is not yet dead.
        undoToEscape:
          type: integer
          description: Number of undos needed to escape stalemate (0 = not stalemate, -1 = unreachable)
        hint:
          $ref: '#/components/schemas/MissMilliganHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    Card:
      type: object
      description: A single playing card
      required:
        - design
        - value
      properties:
        design:
          type: string
          description: |
            Card suit.  `JOKER` is only present in games that use a Joker
            (Old Maid, Daifugo, Sevens).
          enum: [SPADE, CLOVER, HEART, DIAMOND, JOKER]
          example: HEART
        value:
          type: integer
          description: |
            Card rank.  1 = Ace, 2–10 = pip, 11 = Jack, 12 = Queen, 13 = King.
            0 indicates the Joker.
          minimum: 0
          maximum: 13
          example: 7

    # -------------------------------------------------------------------------
    # BlackJack
    # -------------------------------------------------------------------------
    BlackJackRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `bet` (`b`),
            `hit` (`h`), `stand` (`s`), `doubledown` (`d`), `split` (`sp`),
            `insurance` (`i`), `declineinsurance` (`di`), `togglesoft17` (`soft17`),
            `togglecounting` (`counting`), `toggledas` (`das`),
            `setcountingsystem` (`scs`), `setpenetration` (`pen`),
            `earlysurrender` (`es`), `declineearlysurrender` (`des`),
            `setsurrenderrule` (`ssr`), `quit` (`q`).
          enum: [r, reset, b, bet, h, hit, s, stand, d, doubledown, sp, split, i, insurance, di, declineinsurance, sur, surrender, togglehint, sd, setdeckcount, togglesoft17, soft17, togglecounting, counting, toggledas, das, scs, setcountingsystem, pen, setpenetration, es, earlysurrender, des, declineearlysurrender, ssr, setsurrenderrule, log, q, quit]
          example: hit
        amount:
          type: integer
          description: |
            Bet amount.  Required for `bet` command (minimum 10).
            Deck count for `setdeckcount` command (valid values: 1, 2, 4, 6, 8).
            Counting system for `setcountingsystem` command (0=Hi-Lo, 1=KO, 2=Zen Count, 3=Omega II).
            Penetration rate for `setpenetration` command (valid values: 50, 75).
            Surrender rule for `setsurrenderrule` command (0=Late, 1=Early, 2=No surrender).
            Ignored for other commands.
          minimum: 1
          example: 100
        dealerHitsSoft17:
          type: boolean
          description: |
            Whether the dealer hits on soft 17.  Only used with `reset` command.
            When true, dealer hits on soft 17 (H17); when false, dealer stands (S17).
            Defaults to false (S17) when omitted.
          example: false
        cpuPlayerCount:
          type: integer
          description: |
            Number of CPU players at the table (0–3).  Only used with `reset` command.
            CPU players use basic strategy.  Defaults to 0 when omitted.
          minimum: 0
          maximum: 3
          example: 0
        countingEnabled:
          type: boolean
          description: |
            Whether card counting display is enabled.  Only used with `reset` command.
            Defaults to false when omitted.
          example: false
        doubleAfterSplit:
          type: boolean
          description: |
            Whether double-after-split (DAS) is allowed.  Only used with `reset` command.
            Defaults to true when omitted.
          example: true
        countingSystem:
          type: integer
          description: |
            Card counting system (0=Hi-Lo, 1=KO, 2=Zen Count, 3=Omega II).
            Only used with `reset` command.  Defaults to 0 (Hi-Lo) when omitted.
          minimum: 0
          maximum: 3
          example: 0
        deckPenetration:
          type: integer
          description: |
            Deck penetration rate in percent (50 or 75).
            Only used with `reset` command.  Defaults to 75 when omitted.
            At 75%, the shoe is reshuffled when less than 25% of cards remain.
            At 50%, the shoe is reshuffled when less than 50% of cards remain.
          enum: [50, 75]
          example: 75
        perfectPairsBet:
          type: integer
          description: |
            Perfect Pairs side bet amount.  Only used with `bet` command.
            Must be 0 (no bet) or a positive multiple of 10.
            Defaults to 0 when omitted.
          minimum: 0
          example: 0
        twentyOnePlus3Bet:
          type: integer
          description: |
            Poker-hand side bet amount.  Only used with `bet` command.
            Must be 0 (no bet) or a positive multiple of 10.
            Defaults to 0 when omitted.
          minimum: 0
          example: 0
        handCount:
          type: integer
          description: |
            Number of initial hands to deal (1–3).  Only used with `bet` command.
            Each hand receives the same bet amount.  Side bets apply only to hand 0.
            Defaults to 1 when omitted.  0 is treated as 1.
          minimum: 0
          maximum: 3
          example: 1
        surrenderRule:
          type: integer
          description: |
            Surrender rule (0=Late surrender, 1=Early surrender, 2=No surrender).
            Only used with `reset` command.  Defaults to 0 (Late surrender) when omitted.
          minimum: 0
          maximum: 2
          example: 0
        sessionId:
          type: string
          description: Unique session identifier (max 256 characters)
          maxLength: 256
          example: my-session-001

    BlackJackPlayer:
      type: object
      required:
        - chips
      properties:
        score:
          type: integer
          description: |
            Hand score.  Present only for the **dealer**.  `0` while the game
            is in progress; the actual score once the game ends.  For the
            **player**, score is available via the `hands` array.
          example: 18
        cards:
          type: array
          description: |
            Cards currently held.  Present only for the **dealer**.  Only the
            face-up card (index 0) is returned while the game is in progress;
            all cards are returned once the game ends.  For the **player**,
            cards are available via the `hands` array.
          items:
            $ref: '#/components/schemas/Card'
        chips:
          type: integer
          description: Current chip count for this player.
          example: 1000

    BlackJackHand:
      type: object
      required:
        - score
        - cards
        - bet
        - stood
        - doubled
        - busted
        - isBlackJack
        - canSplit
        - surrendered
        - canSurrender
      properties:
        score:
          type: integer
          description: Hand score (Ace counted optimally as 1 or 11).
          example: 18
        cards:
          type: array
          description: Cards in this hand.
          items:
            $ref: '#/components/schemas/Card'
        bet:
          type: integer
          description: Bet amount for this hand.
          example: 100
        stood:
          type: boolean
          description: Whether the player has stood on this hand.
        doubled:
          type: boolean
          description: Whether the player doubled down on this hand.
        busted:
          type: boolean
          description: Whether this hand has busted (score > 21).
        isBlackJack:
          type: boolean
          description: Whether this hand is a natural blackjack (2 cards, score 21).
        canSplit:
          type: boolean
          description: Whether this hand can be split (2 cards of equal BJ value).
        surrendered:
          type: boolean
          description: Whether the player surrendered this hand.
        canSurrender:
          type: boolean
          description: Whether the player can surrender this hand (exactly 2 cards, action phase, not yet stood/busted/surrendered).

    BlackJackResponse:
      type: object
      required:
        - dealer
        - player
        - phase
        - currentHandIdx
        - insuranceBet
        - insuranceAvailable
        - message
        - hintEnabled
        - suggestedAction
        - deckCount
        - dealerHitsSoft17
        - countingEnabled
        - cpuPlayerCount
        - runningCount
        - trueCount
        - perfectPairsBet
        - twentyOnePlus3Bet
        - doubleAfterSplit
      properties:
        dealer:
          $ref: '#/components/schemas/BlackJackPlayer'
        player:
          $ref: '#/components/schemas/BlackJackPlayer'
        hands:
          type: array
          description: |
            Player hands.  Contains 1–3 initial hands (based on `handCount` in bet);
            may grow further after splits (up to 4 per initial hand, 8 total).
            Not present in bet phase.
          items:
            $ref: '#/components/schemas/BlackJackHand'
        phase:
          type: integer
          description: |
            Current game phase:
            - `1` — Bet phase
            - `2` — Deal phase
            - `3` — Insurance phase
            - `4` — Action phase
            - `5` — End phase
            - `6` — Early Surrender phase (only when surrender rule = Early)
          enum: [1, 2, 3, 4, 5, 6]
          example: 4
        currentHandIdx:
          type: integer
          description: |
            Index of the currently active hand (0-based).
            Relevant when player has split into multiple hands.
          example: 0
        insuranceBet:
          type: integer
          description: Amount of insurance bet placed (0 if none).
          example: 0
        insuranceAvailable:
          type: boolean
          description: Whether insurance is available (dealer shows Ace).
        message:
          type: string
          description: |
            Game result message.  One of:
            - `""` — game in progress
            - `"It is a draw."` — tie
            - `"You are the winner."` — player wins
            - `"It is your loss."` — player loses
            - `"bye."` — session ended by quit command
            - `"param error."` — invalid request
            - `"error."` — internal error
          example: You are the winner.
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        hintEnabled:
          type: boolean
          description: Whether the basic strategy hint is currently enabled.
          example: false
        suggestedAction:
          type: integer
          description: |
            Basic strategy suggested action for the current hand (0 when hint is disabled or no suggestion).
            - `0` — None
            - `1` — Hit
            - `2` — Stand
            - `3` — Double Down (else Hit)
            - `4` — Split
            - `5` — Surrender
            - `6` — Decline Insurance
            - `7` — Double Down (else Stand)
          enum: [0, 1, 2, 3, 4, 5, 6, 7]
          example: 1
        deckCount:
          type: integer
          description: |
            Number of decks in the shoe.  Valid values: 1, 2, 4, 6, 8.
          enum: [1, 2, 4, 6, 8]
          example: 1
        dealerHitsSoft17:
          type: boolean
          description: Whether the dealer hits on soft 17 (H17).  False means dealer stands on soft 17 (S17).
          example: false
        countingEnabled:
          type: boolean
          description: Whether card counting display is currently enabled.
          example: false
        cpuPlayerCount:
          type: integer
          description: Number of CPU players at the table (0–3).
          minimum: 0
          maximum: 3
          example: 0
        doubleAfterSplit:
          type: boolean
          description: Whether double-after-split (DAS) is currently allowed.
          example: true
        countingSystem:
          type: integer
          description: |
            Current card counting system (0=Hi-Lo, 1=KO, 2=Zen Count, 3=Omega II).
          minimum: 0
          maximum: 3
          example: 0
        deckPenetration:
          type: integer
          description: |
            Current deck penetration rate in percent (50 or 75).
            Controls when the shoe is reshuffled based on remaining cards.
          enum: [50, 75]
          example: 75
        runningCount:
          type: integer
          description: |
            Running count based on the active counting system.  Only meaningful when `countingEnabled` is true.
            Positive values favor the player; negative values favor the dealer.
          example: 0
        trueCount:
          type: number
          description: |
            True count (running count divided by estimated remaining decks).
            Only meaningful when `countingEnabled` is true.  Returns 0 for unbalanced systems (KO).
          example: 0.0
        cpuPlayers:
          type: array
          description: |
            CPU player seats.  Empty array when `cpuPlayerCount` is 0.
            Each entry contains the CPU player's chip count and hands.
          items:
            $ref: '#/components/schemas/BlackJackCpuSeat'
        perfectPairsBet:
          type: integer
          description: Current Perfect Pairs side bet amount (0 if no side bet placed).
          example: 0
        twentyOnePlus3Bet:
          type: integer
          description: Current poker-hand side bet amount (0 if no side bet placed).
          example: 0
        sideBetResults:
          type: array
          description: |
            Side bet evaluation results.  Present after dealing when side bets were placed.
            Each entry describes one side bet outcome (win or loss).
          items:
            $ref: '#/components/schemas/BlackJackSideBetResult'
        multiHandCount:
          type: integer
          description: |
            Number of initial hands dealt this round (1–3).
            0 means the default single hand.  Only meaningful after a bet has been placed.
          minimum: 0
          maximum: 3
          example: 1
        surrenderRule:
          type: integer
          description: |
            Current surrender rule:
            - `0` — Late surrender (default; surrender allowed after dealer BJ check)
            - `1` — Early surrender (surrender allowed before dealer BJ check)
            - `2` — No surrender (surrender disabled)
          minimum: 0
          maximum: 2
          example: 0

    BlackJackSideBetResult:
      type: object
      description: Result of a single side bet evaluation
      required:
        - betType
        - resultType
        - resultName
        - betAmount
        - payout
      properties:
        betType:
          type: integer
          description: |
            Side bet type:
            - `1` — Perfect Pairs
            - `2` — Poker Hand Bonus
          enum: [1, 2]
          example: 1
        resultType:
          type: integer
          description: |
            Result type.  For Perfect Pairs:
            - `0` — None (no pair)
            - `1` — Perfect Pair (same suit, same value; 25:1)
            - `2` — Colored Pair (same color, same value; 12:1)
            - `3` — Mixed Pair (different color, same value; 6:1)

            For the poker-hand side bet:
            - `0` — None (no hand)
            - `1` — Suited Trips (same suit, same value; 100:1)
            - `2` — Straight Flush (same suit, consecutive; 40:1)
            - `3` — Three of a Kind (same value, different suits; 30:1)
            - `4` — Straight (consecutive values; 10:1)
            - `5` — Flush (same suit; 5:1)
          example: 1
        resultName:
          type: string
          description: Human-readable name of the result (e.g., "Perfect Pair", "Straight Flush").
          example: Perfect Pair
        betAmount:
          type: integer
          description: Amount wagered on this side bet.
          example: 10
        payout:
          type: integer
          description: Payout amount (0 if lost; betAmount × multiplier if won).
          example: 250

    BlackJackCpuSeat:
      type: object
      description: A CPU player seat in a multi-player BlackJack game
      required:
        - chips
        - hands
        - insuranceBet
      properties:
        chips:
          type: integer
          description: Current chip count for this CPU player.
          example: 1000
        hands:
          type: array
          description: Hands for this CPU player (one hand normally; multiple after split).
          items:
            $ref: '#/components/schemas/BlackJackHand'
        insuranceBet:
          type: integer
          description: |
            Amount of insurance bet placed by this CPU player (0 if none).
            CPUs take insurance when card counting is enabled and the count >= 3.
          example: 0

    # -------------------------------------------------------------------------
    # Poker
    # -------------------------------------------------------------------------
    PokerRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `exchange` (`e`),
            `stand` (`s`), `bet` (`b`), `call` (`c`), `raise` (`ra`),
            `check` (`ck`), `fold` (`f`), `allin` (`a`), `odds` (`o`), `quit` (`q`).
          enum: [r, reset, e, exchange, s, stand, b, bet, c, call, ra, raise, ck, check, f, fold, a, allin, o, odds, log, q, quit]
          example: bet
        indices:
          type: array
          description: |
            Zero-based indices of cards to exchange (for the `exchange` command)
            or to calculate draw odds for (for the `odds` command).
            Omit or send an empty array to keep all cards.
          items:
            type: integer
            minimum: 0
            maximum: 4
          example: [0, 2]
        amount:
          type: integer
          description: |
            Bet/raise amount (for the `bet` and `raise` commands).
            Minimum bet is 10.
          minimum: 10
          example: 20
        cpuCount:
          type: integer
          description: Number of CPU players (used with `reset` command)
          minimum: 1
          maximum: 3
          example: 2
        jokerCount:
          type: integer
          description: Number of joker cards (used with `reset` command)
          minimum: 0
          maximum: 2
          example: 1
        bettingLimit:
          type: integer
          description: |
            Betting limit type (used with `reset` command).
            0 = Fixed (default), 1 = Pot Limit, 2 = No Limit.
          minimum: 0
          maximum: 2
          example: 0
        isLowball:
          type: boolean
          description: |
            Enable 2-7 Lowball mode (used with `reset` command).
            When true, the lowest hand wins. Ace always counts as 14,
            straights and flushes count against the player, and jokers
            are forced to rank 0. Best hand is 2-3-4-5-7 off-suit.
          example: false
        cpuMetaAI:
          type: boolean
          description: |
            When true, CPUs adaptively adjust bluff and call behavior based on
            session-scoped game history (Meta-AI). Defaults to false.
          default: false
          example: false
        humanPlayMs:
          type: integer
          description: Human play hesitation time in milliseconds (0=not measured)
          example: 1200
        sessionId:
          type: string
          description: Unique session identifier (max 256 characters)
          maxLength: 256
          example: my-session-001

    PokerPlayerData:
      type: object
      required:
        - id
        - isHuman
        - cards
        - chips
        - currentBet
        - folded
        - allIn
        - handRank
        - handName
        - exchangeCount
        - playStyleName
      properties:
        id:
          type: integer
          description: Player index (0 = human)
        isHuman:
          type: boolean
        cards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
          description: Player's cards. CPU cards hidden (empty) until End phase.
        chips:
          type: integer
          minimum: 0
        currentBet:
          type: integer
          minimum: 0
        folded:
          type: boolean
        allIn:
          type: boolean
        handRank:
          type: integer
          description: "0=HighCard..9=RoyalFlush, 10=FiveOfAKind (joker only)"
          minimum: 0
          maximum: 10
        handName:
          type: string
        exchangeCount:
          type: integer
          minimum: -1
        playStyleName:
          type: string
          description: "CPU play style name (Conservative/Balanced/Aggressive/Bluffer)"

    PokerCpuAction:
      type: object
      properties:
        playerIdx:
          type: integer
        action:
          type: integer
          description: "0=Fold,1=Check,2=Call,3=Bet,4=Raise,5=AllIn"
        amount:
          type: integer

    PokerCpuExchange:
      type: object
      properties:
        playerIdx:
          type: integer
        exchangeCount:
          type: integer

    PokerResult:
      type: object
      properties:
        playerIdx:
          type: integer
        handRank:
          type: integer
        handName:
          type: string
        kickers:
          type: string
          description: Kicker card values formatted as display string (e.g., "A, Q, 10"). Empty for hands without kickers.
        wonAmount:
          type: integer

    PokerSidePot:
      type: object
      properties:
        amount:
          type: integer
        eligiblePlayers:
          type: array
          items:
            type: integer

    PokerOdds:
      type: object
      description: Draw odds for a single hand rank
      required:
        - handRank
        - handName
        - probability
        - count
        - total
      properties:
        handRank:
          type: integer
          description: "Hand rank index (0=HighCard..10=FiveOfAKind)"
          minimum: 0
          maximum: 10
        handName:
          type: string
          description: Human-readable hand name (e.g. "Flush", "Full House")
        probability:
          type: number
          format: double
          description: Probability of achieving this hand (0.0 to 1.0)
          minimum: 0
          maximum: 1
        count:
          type: integer
          description: Number of combinations that result in this hand
          minimum: 0
        total:
          type: integer
          description: Total number of combinations evaluated
          minimum: 0

    PokerResponse:
      type: object
      required:
        - players
        - pot
        - sidePots
        - dealerIdx
        - currentTurn
        - phase
        - gameEndFlag
        - lastBet
        - minRaise
        - ante
        - jokerCount
        - roundResults
        - cpuActions
        - cpuExchanges
        - message
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/PokerPlayerData'
        pot:
          type: integer
          minimum: 0
        sidePots:
          type: array
          items:
            $ref: '#/components/schemas/PokerSidePot'
        dealerIdx:
          type: integer
        currentTurn:
          type: integer
        phase:
          type: integer
          description: |
            Current game phase:
            - `0` — Init (no cards dealt yet)
            - `1` — Deal (first betting round)
            - `2` — Exchange (player can exchange cards)
            - `3` — SecondBet (second betting round)
            - `4` — End (showdown; all hands visible)
          enum: [0, 1, 2, 3, 4]
        gameEndFlag:
          type: boolean
        lastBet:
          type: integer
        minRaise:
          type: integer
        ante:
          type: integer
        jokerCount:
          type: integer
        bettingLimit:
          type: integer
          description: |
            Betting limit type: 0 = Fixed, 1 = Pot Limit, 2 = No Limit.
          enum: [0, 1, 2]
        raiseCount:
          type: integer
          description: Number of raises in the current betting round.
          minimum: 0
        maxBetAmount:
          type: integer
          description: |
            Maximum allowed bet/raise amount for Pot Limit mode.
            0 means no limit (Fixed or No Limit mode).
          minimum: 0
        isLowball:
          type: boolean
          description: Whether 2-7 Lowball mode is active.
        roundResults:
          type: array
          items:
            $ref: '#/components/schemas/PokerResult'
        cpuActions:
          type: array
          items:
            $ref: '#/components/schemas/PokerCpuAction'
        cpuExchanges:
          type: array
          items:
            $ref: '#/components/schemas/PokerCpuExchange'
        odds:
          type: array
          description: |
            Draw odds for each hand rank.  Only present when the `odds` command
            is used during the exchange phase.
          items:
            $ref: '#/components/schemas/PokerOdds'
        message:
          type: string
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Old Maid
    # -------------------------------------------------------------------------
    OldMaidRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `draw` (`d`),
            `shuffle` (`s`), `reorder`, `quit` (`q`).
          enum: [r, reset, d, draw, s, shuffle, reorder, log, q, quit]
          example: draw
        drawIdx:
          type: integer
          description: |
            Zero-based index of the card to draw from the target player's hand.
            Omit (or send `null`) for random selection.
          nullable: true
          minimum: 0
          example: 1
        reorderIndices:
          type: array
          description: |
            Permutation of indices for reordering the human player's hand.
            Required when command is `reorder`. Must be a valid permutation of
            `[0, 1, ..., handSize-1]`.
          nullable: true
          items:
            type: integer
            minimum: 0
          example: [2, 0, 1]
        mode:
          type: integer
          description: Game mode (0=Normal/ババ抜き, 1=JijiNuki/ジジ抜き)
          default: 0
          example: 0
        cpuPlacementStrategy:
          type: boolean
          description: When true, CPUs place the odd card at the edge of their hand
          default: false
          example: false
        cpuMemoryAI:
          type: boolean
          description: When true, CPUs remember draw positions and adjust selection strategy
          default: false
          example: false
        cpuHesitationEnabled:
          type: boolean
          description: When true, CPU actions include a hesitation delay (ms) that varies based on draw outcome
          default: false
          example: false
        cpuMetaAI:
          type: boolean
          description: When true, CPUs adaptively adjust their strategy based on session-scoped game history (Meta-AI)
          default: false
          example: false
        sessionId:
          type: string
          description: Unique session identifier (max 256 characters)
          maxLength: 256
          example: my-session-001

    OldMaidPlayer:
      type: object
      required:
        - id
        - isHuman
        - isFinished
        - cardCount
        - cards
      properties:
        id:
          type: integer
          description: Player index (0 = human, 1–3 = CPU)
          example: 0
        isHuman:
          type: boolean
          description: Whether this is the human player
          example: true
        isFinished:
          type: boolean
          description: Whether this player has finished (all cards matched and discarded)
          example: false
        cardCount:
          type: integer
          description: Number of cards remaining in hand
          example: 13
        cards:
          type: array
          description: Cards in hand.  CPU players' cards are always an empty array.
          items:
            $ref: '#/components/schemas/Card'

    OldMaidCpuAction:
      type: object
      description: Record of a CPU player's draw action during their turn
      required:
        - drawPlayerIdx
        - drawFromIdx
        - drawnCard
        - discardedPairs
      properties:
        drawPlayerIdx:
          type: integer
          description: Index of the CPU player who performed the draw
          example: 1
        drawFromIdx:
          type: integer
          description: Index of the player the card was drawn from
          example: 0
        drawnCard:
          $ref: '#/components/schemas/Card'
        discardedPairs:
          type: integer
          description: Number of pairs discarded as a result of this draw
          example: 1
        hesitationMs:
          type: integer
          description: CPU hesitation delay in milliseconds (0 when feature is disabled)
          example: 750

    OldMaidDrawHistoryEntry:
      type: object
      description: A single draw event in the game-wide draw history (card info omitted for fairness)
      required:
        - drawPlayerIdx
        - drawFromIdx
        - discardedPairs
        - drawerFinished
        - targetFinished
      properties:
        drawPlayerIdx:
          type: integer
          description: Index of the player who drew a card
          example: 0
        drawFromIdx:
          type: integer
          description: Index of the player the card was drawn from
          example: 1
        discardedPairs:
          type: integer
          description: Number of pairs discarded as a result of this draw
          example: 0
        drawerFinished:
          type: boolean
          description: Whether the drawing player finished (0 cards) after this draw
          example: false
        targetFinished:
          type: boolean
          description: Whether the target player finished (0 cards) after this draw
          example: true

    OldMaidResponse:
      type: object
      required:
        - players
        - currentTurn
        - nextDrawTargetIdx
        - gameEndFlag
        - loserIdx
        - lastDrawPlayerIdx
        - lastDrawFromIdx
        - lastDrawCard
        - lastDiscardedPairs
        - hasDrawn
        - cpuActions
        - message
        - cpuHighlightedCardIdx
        - mode
      properties:
        players:
          type: array
          description: State of all 4 players
          items:
            $ref: '#/components/schemas/OldMaidPlayer'
        currentTurn:
          type: integer
          description: Index of the player whose turn it is
          example: 0
        nextDrawTargetIdx:
          type: integer
          description: Index of the player the human must draw from on their next turn
          example: 1
        gameEndFlag:
          type: boolean
          description: Whether the game has ended
          example: false
        loserIdx:
          type: integer
          description: Index of the losing player (-1 while game is in progress)
          example: -1
        lastDrawPlayerIdx:
          type: integer
          description: Index of the player who last drew a card
          example: 0
        lastDrawFromIdx:
          type: integer
          description: Index of the player the last card was drawn from
          example: 1
        lastDrawCard:
          allOf:
            - $ref: '#/components/schemas/Card'
          nullable: true
          description: The card drawn most recently (null before any draw)
        lastDiscardedPairs:
          type: integer
          description: Number of pairs discarded after the last draw
          example: 0
        hasDrawn:
          type: boolean
          description: Whether the human player has drawn this turn
          example: false
        cpuActions:
          type: array
          description: List of CPU actions that occurred since the last human action
          items:
            $ref: '#/components/schemas/OldMaidCpuAction'
        drawHistory:
          type: array
          description: Full draw history throughout the entire game (card info omitted for fairness)
          items:
            $ref: '#/components/schemas/OldMaidDrawHistoryEntry'
        message:
          type: string
          description: |
            Game result or status message.  Empty while the game is in progress.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        cpuHighlightedCardIdx:
          type: integer
          description: Index of the highlighted card for the target CPU player (-1 if none)
          example: -1
        removedCard:
          allOf:
            - $ref: '#/components/schemas/Card'
          nullable: true
          description: The removed card (only revealed at game end in JijiNuki mode)
        mode:
          type: integer
          description: Current game mode (0=Normal/ババ抜き, 1=JijiNuki/ジジ抜き)
          example: 0
        metaAI:
          type: object
          nullable: true
          description: Meta-AI state for the current session (null when Meta-AI is disabled)
          properties:
            enabled:
              type: boolean
              description: Whether Meta-AI is enabled
              example: true
            gamesPlayed:
              type: integer
              description: Number of games played in the current session
              example: 3
            edgePickRate:
              type: number
              format: float
              description: Rate at which the human player picks edge cards (0.0–1.0)
              example: 0.45

    # -------------------------------------------------------------------------
    # Daifugo
    # -------------------------------------------------------------------------
    DaifugoRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `play` (`p`),
            `sort`, `quit` (`q`).
          enum: [r, reset, p, play, sort, log, q, quit]
          example: play
        indices:
          type: array
          description: |
            Zero-based indices of cards to play (for the `play` command).
            Send an empty array to pass.
          items:
            type: integer
            minimum: 0
          maxItems: 13
          example: [0, 1]
        sessionId:
          type: string
          description: Unique session identifier (max 256 characters)
          maxLength: 256
          example: my-session-001
        config:
          allOf:
            - $ref: '#/components/schemas/DaifugoConfig'
          nullable: true
          description: |
            Optional rule configuration for the `reset` command.
            When present, overrides the current session's rule settings.
            Omit (or set to null) to reuse the previous configuration.
        sortMode:
          type: integer
          nullable: true
          description: |
            Sort mode for the `sort` command.
            0 = by strength (default), 1 = by suit, 2 = by number.
          enum: [0, 1, 2]
          example: 0

    DaifugoPlayer:
      type: object
      required:
        - id
        - isHuman
        - isFinished
        - rank
        - cardCount
        - cards
      properties:
        id:
          type: integer
          description: Player index (0 = human, 1–3 = CPU)
          example: 0
        isHuman:
          type: boolean
          description: Whether this is the human player
          example: true
        isFinished:
          type: boolean
          description: Whether this player has played all their cards
          example: false
        rank:
          type: integer
          description: |
            Final rank once the game ends (-1 while game is in progress).
            1 = 大富豪, 2 = 富豪, 3 = 平民, 4 = 大貧民.
          example: -1
        cardCount:
          type: integer
          description: Number of cards remaining in hand
          example: 8
        cards:
          type: array
          description: Cards in hand (only populated for the human player)
          items:
            $ref: '#/components/schemas/Card'
        illegalFinishPenalty:
          type: boolean
          description: Whether this player received an illegal finish penalty (反則上がり)
          example: false

    DaifugoAction:
      type: object
      description: Record of a player's action (play or pass)
      required:
        - playerIdx
        - playedCards
      properties:
        playerIdx:
          type: integer
          description: Index of the player who acted
          example: 1
        playedCards:
          type: array
          description: Cards played.  Null indicates a pass.
          nullable: true
          items:
            $ref: '#/components/schemas/Card'

    DaifugoConfig:
      type: object
      properties:
        jokerCount:
          type: integer
          description: Number of jokers in the deck
          example: 2
        eightCutEnabled:
          type: boolean
          description: Whether 8切り (eight-cut) rule is enabled
          example: true
        suitLockMode:
          type: integer
          description: "Suit lock mode: 0=none, 1=partial, 2=full"
          enum: [0, 1, 2]
          example: 1
        elevenBackEnabled:
          type: boolean
          description: Whether 11バック (eleven-back) rule is enabled
          example: true
        sequenceEnabled:
          type: boolean
          description: Whether 階段 (sequence/straight) rule is enabled
          example: true
        cardExchangeEnabled:
          type: boolean
          description: Whether カード交換 (card exchange) based on previous ranks is enabled
          example: true
        blindExchangeEnabled:
          type: boolean
          description: Whether ブラインドカード交換 (blind card exchange — upper-rank player gives random cards instead of weakest) rule is enabled (default false)
          example: false
        fiveSkipEnabled:
          type: boolean
          description: Whether 5飛び (five-skip) rule is enabled (default false)
          example: false
        fiveSkipCount:
          type: integer
          description: Number of players to skip per 5 card
          minimum: 1
          maximum: 5
          example: 1
        sevenPassEnabled:
          type: boolean
          description: Whether 7渡し (seven-pass) rule is enabled (default false)
          example: false
        tenDiscardEnabled:
          type: boolean
          description: Whether 10捨て (ten-discard) rule is enabled (default false)
          example: false
        spadeThreeEnabled:
          type: boolean
          description: Whether スペ3返し (spade-three counter) rule is enabled (default false)
          example: false
        capitalFallEnabled:
          type: boolean
          description: Whether 都落ち (capital-fall) rule is enabled (default false)
          example: false
        nineReverseEnabled:
          type: boolean
          description: Whether 9リバース (nine-reverse) rule is enabled (default false)
          example: false
        coupDetatEnabled:
          type: boolean
          description: Whether クーデター (coup d'état — 3 nines = revolution) rule is enabled (default false)
          example: false
        numberLockEnabled:
          type: boolean
          description: Whether 激シバ (number lock) rule is enabled (default false)
          example: false
        queenBomberEnabled:
          type: boolean
          description: Whether Qボンバー (queen bomber) rule is enabled (default false)
          example: false
        sandstormEnabled:
          type: boolean
          description: Whether 砂嵐 (sandstorm — 3 non-joker 3s clear the table like 8-cut) rule is enabled (default false)
          example: false
        emperorEnabled:
          type: boolean
          description: Whether エンペラー (emperor — 4 consecutive cards of all different suits on clear table triggers revolution + table clear) rule is enabled (default false)
          example: false
        sequenceRevolutionEnabled:
          type: boolean
          description: Whether 階段革命 (sequence revolution — 4+ card sequences trigger revolution) rule is enabled (default false)
          example: false
        sequenceLockEnabled:
          type: boolean
          description: Whether 階段縛り (sequence lock — playing a sequence on a sequence locks to sequences; persists across table clears) rule is enabled (default false)
          example: false
        illegalFinishEnabled:
          type: boolean
          description: Whether 反則上がり (illegal finish — finishing with 8-cut, joker, or revolution is penalized) rule is enabled (default false)
          example: false
        cpuDifficulty:
          type: integer
          description: |
            CPU difficulty level.
            0 = Normal (default, balanced AI with card preservation),
            1 = Easy (simple greedy play),
            2 = Hard (strategic AI that adapts to opponent hand sizes)
          enum: [0, 1, 2]
          example: 0

    DaifugoExchangeAction:
      type: object
      properties:
        fromPlayerIdx:
          type: integer
          description: Index of the player who gave cards
          example: 3
        toPlayerIdx:
          type: integer
          description: Index of the player who received cards
          example: 0
        cards:
          type: array
          description: Cards that were exchanged
          items:
            $ref: '#/components/schemas/Card'

    DaifugoResponse:
      type: object
      required:
        - players
        - currentTurn
        - tableCards
        - lastPlayPlayerIdx
        - gameEndFlag
        - revolutionActive
        - elevenBackActive
        - suitLocked
        - lockedSuit
        - tableIsSequence
        - config
        - exchangeActions
        - cpuActions
        - humanAction
        - message
        - pendingAction
        - pendingActionTarget
        - reverseDirection
        - numberLocked
        - sequenceLocked
        - sortMode
      properties:
        players:
          type: array
          description: State of all 4 players
          items:
            $ref: '#/components/schemas/DaifugoPlayer'
        currentTurn:
          type: integer
          description: Index of the player whose turn it is
          example: 0
        tableCards:
          type: array
          description: Cards currently on the table (last played set)
          items:
            $ref: '#/components/schemas/Card'
        lastPlayPlayerIdx:
          type: integer
          description: Index of the player who last played cards
          example: 0
        gameEndFlag:
          type: boolean
          description: Whether the game has ended
          example: false
        revolutionActive:
          type: boolean
          description: Whether revolution is active (strength order reversed)
          example: false
        elevenBackActive:
          type: boolean
          description: Whether 11-back is active (temporary strength reversal)
          example: false
        suitLocked:
          type: boolean
          description: Whether suit lock is active
          example: false
        lockedSuit:
          type: string
          description: The locked suit name (e.g. SPADE, HEART) when suitLocked is true
          example: ""
        tableIsSequence:
          type: boolean
          description: Whether the current table play is a sequence (階段)
          example: false
        config:
          $ref: '#/components/schemas/DaifugoConfig'
        exchangeActions:
          type: array
          description: Card exchange actions that occurred at game start (based on previous rankings)
          items:
            $ref: '#/components/schemas/DaifugoExchangeAction'
        cpuActions:
          type: array
          description: CPU actions that occurred after the human's last action
          items:
            $ref: '#/components/schemas/DaifugoAction'
        humanAction:
          allOf:
            - $ref: '#/components/schemas/DaifugoAction'
          nullable: true
          description: The human player's most recent action (null before first play)
        message:
          type: string
          description: |
            Game result or status message.  Empty while the game is in progress.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        pendingAction:
          type: string
          description: |
            Pending action that the current player must resolve before turn advances.
            `none` = no pending action; `sevenPass` = must give a card (7渡し);
            `tenDiscard` = must discard a card (10捨て);
            `queenBomber` = must discard a card from target player (Qボンバー).
          enum: [none, sevenPass, tenDiscard, queenBomber]
          example: none
        pendingActionTarget:
          type: integer
          description: |
            For `sevenPass`, the index of the player to give the card to.
            -1 when no pending action.
          example: -1
        reverseDirection:
          type: boolean
          description: Whether turn direction is reversed (9リバース)
          example: false
        numberLocked:
          type: boolean
          description: Whether number lock (連番縛り / 激シバ) is active
          example: false
        sequenceLocked:
          type: boolean
          description: Whether sequence lock (階段縛り) is active — only sequences or emperor can be played when table is clear
          example: false
        sortMode:
          type: integer
          description: |
            Current sort mode for the human player's hand.
            0 = by strength, 1 = by suit, 2 = by number.
          example: 0

    # -------------------------------------------------------------------------
    # Dou Dizhu (斗地主)
    # -------------------------------------------------------------------------
    DoudizhuConfig:
      type: object
      required:
        - cpuDifficulty
      properties:
        cpuDifficulty:
          type: integer
          description: CPU difficulty (0=Normal, 1=Easy, 2=Hard)
          enum: [0, 1, 2]
          example: 0

    DoudizhuRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `bid`, `play` (`p`),
            `log` (`l`).
          enum: [r, reset, bid, p, play, l, log]
          example: play
        indices:
          type: array
          description: |
            Zero-based indices of cards to play (for the `play` command).
            Send an empty array to pass.
          items:
            type: integer
            minimum: 0
          maxItems: 20
          example: [0, 1]
        bidValue:
          type: integer
          nullable: true
          description: |
            Bid value for the `bid` command. 0 = pass, 1-3 = bid amount.
          enum: [0, 1, 2, 3]
          example: 2
        sessionId:
          type: string
          description: Unique session identifier (max 256 characters)
          maxLength: 256
          example: my-session-001
        config:
          allOf:
            - $ref: '#/components/schemas/DoudizhuConfig'
          nullable: true
          description: |
            Optional configuration for the `reset` command.
            Omit (or set to null) to reuse the previous configuration.

    DoudizhuPlayer:
      type: object
      required:
        - id
        - isHuman
        - isFinished
        - isLandlord
        - cardCount
        - cards
      properties:
        id:
          type: integer
          example: 0
        isHuman:
          type: boolean
        isFinished:
          type: boolean
        isLandlord:
          type: boolean
        cardCount:
          type: integer
          example: 17
        cards:
          type: array
          description: Visible cards (only populated for the human player)
          items:
            $ref: '#/components/schemas/Card'

    DoudizhuAction:
      type: object
      required:
        - playerIdx
        - playedCards
        - bidValue
      properties:
        playerIdx:
          type: integer
        playedCards:
          type: array
          nullable: true
          description: Cards played (null = pass)
          items:
            $ref: '#/components/schemas/Card'
        bidValue:
          type: integer
          description: Bid value (bidding phase only)

    DoudizhuResponse:
      type: object
      required:
        - players
        - phase
        - currentTurn
        - tableCards
        - tableCombo
        - kittyCards
        - landlordIdx
        - baseBid
        - highestBid
        - bombCount
        - scores
        - gameEndFlag
        - config
        - cpuActions
        - humanAction
        - message
      properties:
        players:
          type: array
          description: State of all 3 players
          items:
            $ref: '#/components/schemas/DoudizhuPlayer'
        phase:
          type: string
          description: Current phase
          enum: [bid, play, end]
        currentTurn:
          type: integer
        tableCards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        tableCombo:
          type: string
          description: Combination type on the table
        kittyCards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        landlordIdx:
          type: integer
          description: Landlord player index (-1 if not yet decided)
        baseBid:
          type: integer
        highestBid:
          type: integer
        bombCount:
          type: integer
        scores:
          type: array
          items:
            type: integer
        gameEndFlag:
          type: boolean
        config:
          $ref: '#/components/schemas/DoudizhuConfig'
        cpuActions:
          type: array
          items:
            $ref: '#/components/schemas/DoudizhuAction'
        humanAction:
          allOf:
            - $ref: '#/components/schemas/DoudizhuAction'
          nullable: true
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    # -------------------------------------------------------------------------
    # Truco
    # -------------------------------------------------------------------------
    TrucoConfig:
      type: object
      required:
        - cpuDifficulty
        - matchTarget
      properties:
        cpuDifficulty:
          type: integer
          description: CPU difficulty (0=Normal)
          enum: [0]
          example: 0
        matchTarget:
          type: integer
          description: Points needed to win the match (default 15)
          minimum: 1
          maximum: 60
          example: 15

    TrucoRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `play` (`p`),
            `truco` (`t`), `accept` (`a`), `decline` (`d`), `next` (`n`),
            `hint` (`h`), `log` (`l`).
          enum: [r, reset, p, play, t, truco, a, accept, d, decline, n, next, h, hint, l, log]
          example: play
        cardIndex:
          type: integer
          nullable: true
          description: Zero-based hand position to play (for the `play` command).
          minimum: 0
          example: 0
        sessionId:
          type: string
          description: Unique session identifier (max 256 characters)
          maxLength: 256
          example: my-session-001
        config:
          allOf:
            - $ref: '#/components/schemas/TrucoConfig'
          nullable: true
          description: |
            Optional configuration for the `reset` command.
            Omit (or set to null) to reuse the previous configuration.

    TrucoPlayer:
      type: object
      required:
        - id
        - isHuman
        - cardCount
        - cards
        - trickCount
      properties:
        id:
          type: integer
          example: 0
        isHuman:
          type: boolean
        cardCount:
          type: integer
          example: 3
        cards:
          type: array
          description: Visible cards (only populated for the human player)
          items:
            $ref: '#/components/schemas/Card'
        trickCount:
          type: integer
          description: Bazas (tricks) won in the current hand

    TrucoTrickCard:
      type: object
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
        card:
          $ref: '#/components/schemas/Card'

    TrucoHint:
      type: object
      required:
        - action
        - reason
      properties:
        action:
          type: string
          description: Suggested action (play / call / accept / decline)
        cardIndex:
          type: integer
          nullable: true
        reason:
          type: string

    TrucoResponse:
      type: object
      required:
        - players
        - phase
        - handNumber
        - trickNumber
        - currentPlayerIdx
        - responderIdx
        - currentTrick
        - trickResults
        - leadPlayerIdx
        - manoIdx
        - dealerIdx
        - handStake
        - acceptedLevel
        - pendingLevel
        - trucoCallerIdx
        - canDeclareTruco
        - matchTarget
        - matchPoints
        - handWinnerIdx
        - gameEndFlag
        - winnerIdx
        - config
        - message
      properties:
        players:
          type: array
          description: State of both players
          items:
            $ref: '#/components/schemas/TrucoPlayer'
        phase:
          type: integer
          description: Phase (0=Play, 1=Respond, 2=TrickEnd, 3=HandEnd, 4=GameEnd)
          enum: [0, 1, 2, 3, 4]
        handNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        responderIdx:
          type: integer
          description: Player who must respond to a pending call (-1 when none)
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/TrucoTrickCard'
        trickResults:
          type: array
          description: 'Outcome of each completed baza this hand: 0/1 = winner, -1 = parda (tie)'
          items:
            type: integer
        leadPlayerIdx:
          type: integer
        manoIdx:
          type: integer
          description: Elder hand (non-dealer); decides all-parda hands
        dealerIdx:
          type: integer
        handStake:
          type: integer
          description: Current agreed stake for the hand (1..4)
        acceptedLevel:
          type: integer
          description: Accepted betting level (0=none, 1=Truco, 2=Retruco, 3=Vale Cuatro)
          enum: [0, 1, 2, 3]
        pendingLevel:
          type: integer
          description: Proposed level while awaiting a response (0 otherwise)
        trucoCallerIdx:
          type: integer
          description: Index of the player whose call is pending (-1 otherwise)
        canDeclareTruco:
          type: boolean
        matchTarget:
          type: integer
        matchPoints:
          type: array
          description: Cumulative match points [p0, p1]
          items:
            type: integer
        handWinnerIdx:
          type: integer
          description: Winner of the most recent hand (-1 = unresolved)
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: Match winner (-1 = unfinished)
        hint:
          allOf:
            - $ref: '#/components/schemas/TrucoHint'
          nullable: true
        config:
          $ref: '#/components/schemas/TrucoConfig'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    # -------------------------------------------------------------------------
    # Big Two
    # -------------------------------------------------------------------------
    BigTwoRequest:
      type: object
      required:
        - command
      properties:
        command:
          type: string
          enum: [reset, r, play, p]
        indices:
          type: array
          items:
            type: integer
        config:
          $ref: '#/components/schemas/BigTwoConfig'
        sessionId:
          type: string
    BigTwoConfig:
      type: object
      properties:
        cpuDifficulty:
          type: integer
          enum: [0, 1, 2]
    BigTwoResponse:
      type: object
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/BigTwoPlayer'
        currentTurn:
          type: integer
        tableCards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        tablePlayType:
          type: integer
        lastPlayPlayerIdx:
          type: integer
        gameEndFlag:
          type: boolean
        cpuActions:
          type: array
          items:
            $ref: '#/components/schemas/BigTwoAction'
        humanAction:
          $ref: '#/components/schemas/BigTwoAction'
        config:
          $ref: '#/components/schemas/BigTwoConfig'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    BigTwoPlayer:
      type: object
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        isFinished:
          type: boolean
        rank:
          type: integer
        cardCount:
          type: integer
        cards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
    BigTwoAction:
      type: object
      properties:
        playerIdx:
          type: integer
        playedCards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
          nullable: true

    # -------------------------------------------------------------------------
    # Tien Len
    # -------------------------------------------------------------------------
    TienLenRequest:
      type: object
      required:
        - command
      properties:
        command:
          type: string
          enum: [reset, r, play, p]
        indices:
          type: array
          items:
            type: integer
        config:
          $ref: '#/components/schemas/TienLenConfig'
        sessionId:
          type: string
    TienLenConfig:
      type: object
      properties:
        cpuDifficulty:
          type: integer
          enum: [0, 1, 2]
    TienLenResponse:
      type: object
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/TienLenPlayer'
        currentTurn:
          type: integer
        tableCards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        tablePlayType:
          type: integer
        lastPlayPlayerIdx:
          type: integer
        gameEndFlag:
          type: boolean
        cpuActions:
          type: array
          items:
            $ref: '#/components/schemas/TienLenAction'
        humanAction:
          $ref: '#/components/schemas/TienLenAction'
        config:
          $ref: '#/components/schemas/TienLenConfig'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    TienLenPlayer:
      type: object
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        isFinished:
          type: boolean
        rank:
          type: integer
        cardCount:
          type: integer
        cards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
    TienLenAction:
      type: object
      properties:
        playerIdx:
          type: integer
        playedCards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
          nullable: true

    # -------------------------------------------------------------------------
    # Sevens
    # -------------------------------------------------------------------------
    SevensRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `play` (`p`),
            `joker` (`j`), `quit` (`q`).
          enum: [r, reset, p, play, j, joker, log, q, quit]
          example: play
        index:
          type: integer
          description: |
            Zero-based index of the card to play (for the `play` and `joker` commands).
            Use `-1` to pass (play command only).
          default: -1
          example: 3
        jokerTargetSuit:
          type: integer
          description: |
            Target suit for joker placement (for the `joker` command).
            1 = SPADE, 2 = CLOVER, 3 = HEART, 4 = DIAMOND.
          default: 0
          example: 1
        jokerTargetValue:
          type: integer
          description: |
            Target value for joker placement (for the `joker` command).
            Range: 1–13.
          default: 0
          example: 6
        tunnelEnabled:
          type: boolean
          description: |
            Enable tunnel rule (A↔K circular). Only used with `reset` command.
          example: false
        tunnelSkipWidth:
          type: integer
          description: |
            Tunnel skip width (0 = disabled, 2–12 = add ±N skip connections).
            When tunnelEnabled is true, skip connections wrap around (A↔K circular).
            Only used with `reset` command.
          minimum: 0
          maximum: 12
          default: 0
          example: 0
        jokerCount:
          type: integer
          description: |
            Number of jokers in the deck (0–2). Only used with `reset` command.
          minimum: 0
          maximum: 2
          example: 0
        cpuStrategy:
          type: integer
          description: |
            CPU strategy mode (0 = simple, 1 = strategic, 2 = harassment). Only used with `reset` command.
          enum: [0, 1, 2]
          example: 0
        maxPasses:
          type: integer
          description: |
            Maximum number of passes per player (0 = unlimited). Only used with `reset` command.
          minimum: 0
          default: 5
          example: 5
        noJokerFinish:
          type: boolean
          description: |
            Ban finishing the game by playing a joker as the last card. Only used with `reset` command.
          example: false
        jokerReclaim:
          type: boolean
          description: |
            Enable joker reclaim rule (playing a real card on a joker-occupied position returns the joker to the player's hand). Only used with `reset` command.
          example: false
        endStop:
          type: boolean
          description: |
            Enable end stop rule (片側ストップ). When A is placed, the high side (8–K) is blocked; when K is placed, the low side (A–6) is blocked. Only used with `reset` command.
          example: false
        jokerConsecutiveBanned:
          type: boolean
          description: |
            Enable joker consecutive banned rule (ジョーカー連続禁止). When enabled, a player who played a joker on their last turn cannot play a joker on the current turn. Playing a normal card or passing resets the restriction. Only used with `reset` command.
          example: false
        sessionId:
          type: string
          description: Unique session identifier (max 256 characters)
          maxLength: 256
          example: my-session-001

    SevensPlayer:
      type: object
      required:
        - id
        - isHuman
        - isFinished
        - rank
        - cardCount
        - passesUsed
        - maxPasses
        - cards
      properties:
        id:
          type: integer
          description: Player index (0 = human, 1–3 = CPU)
          example: 0
        isHuman:
          type: boolean
          description: Whether this is the human player
          example: true
        isFinished:
          type: boolean
          description: Whether this player has played all their cards
          example: false
        rank:
          type: integer
          description: |
            Final rank once the player finishes (-1 while still playing).
          example: -1
        cardCount:
          type: integer
          description: Number of cards remaining in hand
          example: 5
        passesUsed:
          type: integer
          description: Number of passes the player has used
          example: 0
        maxPasses:
          type: integer
          description: Maximum number of passes allowed
          example: 3
        cards:
          type: array
          description: Cards in hand (only populated for the human player)
          items:
            $ref: '#/components/schemas/Card'
        lastPlayedJoker:
          type: boolean
          description: Whether this player played a joker on their last turn (only meaningful for the human player when jokerConsecutiveBanned is enabled)
          example: false

    SevensAction:
      type: object
      description: Record of a player's action (play or pass)
      required:
        - playerIdx
        - playedCard
        - targetSuit
        - targetValue
      properties:
        playerIdx:
          type: integer
          description: Index of the player who acted
          example: 1
        playedCard:
          allOf:
            - $ref: '#/components/schemas/Card'
          nullable: true
          description: The card played.  Null indicates a pass.
        targetSuit:
          type: integer
          description: |
            Target suit for joker placement (0 for non-joker plays).
          example: 0
        targetValue:
          type: integer
          description: |
            Target value for joker placement (0 for non-joker plays).
          example: 0
        forcedPass:
          type: boolean
          description: |
            Whether the pass was forced (player had no playable cards).
            Only meaningful when `playedCard` is null.
          example: false

    SevensConfig:
      type: object
      description: Game configuration for optional rules
      required:
        - tunnelEnabled
        - tunnelSkipWidth
        - jokerCount
        - cpuStrategy
        - maxPasses
        - noJokerFinish
      properties:
        tunnelEnabled:
          type: boolean
          description: Whether the tunnel rule (A↔K circular) is enabled
          example: false
        tunnelSkipWidth:
          type: integer
          description: Tunnel skip width (0 = disabled, 2–12 = ±N skip connections added)
          example: 0
        jokerCount:
          type: integer
          description: Number of joker cards in the deck
          example: 0
        cpuStrategy:
          type: integer
          description: CPU strategy mode (0 = simple, 1 = strategic, 2 = harassment)
          enum: [0, 1, 2]
          example: 0
        maxPasses:
          type: integer
          description: Maximum number of passes per player (0 = unlimited)
          example: 5
        noJokerFinish:
          type: boolean
          description: Whether finishing the game with a joker as the last card is banned
          example: false
        jokerReclaimEnabled:
          type: boolean
          description: Whether ジョーカー回収 (joker reclaim — playing a real card on a joker-occupied position returns the joker to the player's hand) is enabled (default false)
          example: false
        endStopEnabled:
          type: boolean
          description: Whether 片側ストップ (end stop — A placed blocks high side 8–K, K placed blocks low side A–6) is enabled (default false)
          example: false
        jokerConsecutiveBanned:
          type: boolean
          description: Whether ジョーカー連続禁止 (joker consecutive banned — a player who played a joker last turn cannot play a joker this turn) is enabled (default false)
          example: false

    SevensResponse:
      type: object
      required:
        - players
        - currentTurn
        - tableMinVals
        - tableMaxVals
        - tablePlaced
        - config
        - gameEndFlag
        - cpuActions
        - humanAction
        - message
      properties:
        players:
          type: array
          description: State of all 4 players
          items:
            $ref: '#/components/schemas/SevensPlayer'
        currentTurn:
          type: integer
          description: Index of the player whose turn it is
          example: 0
        tableMinVals:
          type: array
          description: |
            Minimum card value currently placed on the board per suit.
            Array of 5 integers indexed by suit (0 = unused/no suit,
            1 = SPADE, 2 = CLOVER, 3 = HEART, 4 = DIAMOND).
            Derived from `tablePlaced` bitmask for backward compatibility.
          items:
            type: integer
          minItems: 5
          maxItems: 5
          example: [0, 6, 7, 7, 7]
        tableMaxVals:
          type: array
          description: |
            Maximum card value currently placed on the board per suit.
            Uses the same indexing as `tableMinVals`.
            Derived from `tablePlaced` bitmask for backward compatibility.
          items:
            type: integer
          minItems: 5
          maxItems: 5
          example: [0, 8, 7, 7, 7]
        tablePlaced:
          type: array
          description: |
            Bitmask per suit indicating which card values are placed on the board.
            Array of 5 integers indexed by suit. Bit i set means value i is placed.
            Example: 128 (binary 10000000) = only value 7 is placed.
          items:
            type: integer
          minItems: 5
          maxItems: 5
          example: [0, 192, 128, 128, 128]
        config:
          $ref: '#/components/schemas/SevensConfig'
        gameEndFlag:
          type: boolean
          description: Whether the game has ended
          example: false
        cpuActions:
          type: array
          description: CPU actions that occurred after the human's last action
          items:
            $ref: '#/components/schemas/SevensAction'
        humanAction:
          allOf:
            - $ref: '#/components/schemas/SevensAction'
          nullable: true
          description: The human player's most recent action (null before first play)
        message:
          type: string
          description: |
            Game result or status message.  Empty while the game is in progress.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.


    # Doubt
    DoubtRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: Game command
          enum: [reset, r, play, p, doubt, d, skip, s, log, quit, q]
          example: play
        cardIndices:
          type: array
          description: Hand indices of cards to play (required for `play` command)
          items:
            type: integer
          example: [0, 2]
        claimedValue:
          type: integer
          description: Declared card value 1–13 (required for `play` command)
          minimum: 1
          maximum: 13
          example: 3
        doubterIndices:
          type: array
          description: |
            Player indices declaring doubt. Used only for the `doubt` command: include 0 (human)
            if the human is doubting, plus any CPU doubters. Ignored for the `skip` command
            (the server determines CPU doubters from its own state).
          items:
            type: integer
          example: [0, 2]
        doubtWindowSec:
          type: integer
          description: |
            Duration of the doubt window in seconds (used with the `reset` command).
            Must be at least 1. Defaults to 10 when omitted.
          minimum: 1
          example: 5
        cpuMemoryLevel:
          type: integer
          description: |
            CPU memory retention level (used with the `reset` command).
            0 = Easy (~30% retention), 1 = Normal (~70% retention), 2 = Hard (100% retention).
            Defaults to 1 (Normal) when omitted.
          minimum: 0
          maximum: 2
          example: 1
        penaltyDrawLimit:
          type: integer
          description: |
            Maximum number of cards the loser picks up after a doubt resolution
            (used with the `reset` command). 0 = unlimited (loser takes all table cards).
            Remaining cards are discarded from the game. Defaults to 0 when omitted.
          minimum: 0
          example: 5
        cpuHesitationEnabled:
          type: boolean
          description: |
            When true, CPU actions include a hesitation delay (ms) that varies based on
            whether the CPU is bluffing. Bluff → 60% fast (300-500ms), 40% slow (1200-1800ms);
            honest → medium (600-1000ms). Defaults to false.
          default: false
          example: false
        cpuMetaAI:
          type: boolean
          description: |
            When true, CPUs adaptively adjust bluff and doubt behavior based on
            session-scoped game history (Meta-AI). Defaults to false.
          default: false
          example: false
        humanPlayMs:
          type: integer
          description: Human play hesitation time in milliseconds (0=not measured)
          example: 1200
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    DoubtPlayer:
      type: object
      description: A player in the Doubt game
      required:
        - id
        - isHuman
        - isFinished
        - cardCount
        - cards
      properties:
        id:
          type: integer
          description: Player index (0 = human, 1–3 = CPU)
          example: 0
        isHuman:
          type: boolean
          description: Whether this player is the human
          example: true
        isFinished:
          type: boolean
          description: Whether this player has played all cards (won)
          example: false
        cardCount:
          type: integer
          description: Number of cards in hand
          example: 5
        cards:
          type: array
          description: Cards in hand (populated only for the human player)
          items:
            $ref: '#/components/schemas/Card'

    DoubtAction:
      type: object
      description: An action (play) taken by a player
      required:
        - playerIdx
        - claimedValue
        - cardCount
        - isBluff
      properties:
        playerIdx:
          type: integer
          description: Index of the player who took the action
          example: 1
        claimedValue:
          type: integer
          description: The card value the player declared
          example: 3
        cardCount:
          type: integer
          description: Number of cards played
          example: 2
        isBluff:
          type: boolean
          description: Whether the play was a bluff (always false until revealed)
          example: false
        hasTell:
          type: boolean
          description: Whether the CPU showed a visible "tell" (nervousness indicator) during this play
          example: false
        hesitationMs:
          type: integer
          description: CPU hesitation delay in milliseconds (0 when feature is disabled)
          example: 750

    DoubtDoubtResult:
      type: object
      description: Result of a doubt resolution
      required:
        - doubterIdx
        - cardPlayerIdx
        - wasLying
        - loserIdx
        - cardCount
        - discardedCount
        - revealedCards
      properties:
        doubterIdx:
          type: integer
          description: Index of the player who called doubt
          example: 0
        cardPlayerIdx:
          type: integer
          description: Index of the player whose cards were challenged
          example: 1
        wasLying:
          type: boolean
          description: Whether the challenged player was bluffing
          example: true
        loserIdx:
          type: integer
          description: Index of the player who must pick up the table cards
          example: 1
        cardCount:
          type: integer
          description: Number of cards the loser picks up
          example: 4
        discardedCount:
          type: integer
          description: Number of cards discarded from the game (when penaltyDrawLimit is active and table exceeds the limit)
          example: 0
        revealedCards:
          type: array
          description: Cards that were revealed during the doubt resolution
          items:
            $ref: '#/components/schemas/Card'

    DoubtResponse:
      type: object
      description: Current state of the Doubt game
      required:
        - players
        - currentTurn
        - phase
        - tableCardCount
        - cpuDoubters
        - cpuActions
        - gameEndFlag
        - winnerIdx
        - message
        - doubtWindowSec
        - penaltyDrawLimit
      properties:
        players:
          type: array
          description: All players in turn order
          items:
            $ref: '#/components/schemas/DoubtPlayer'
        currentTurn:
          type: integer
          description: Index of the player whose turn it is
          example: 0
        phase:
          type: integer
          description: "Game phase: 0 = Play, 1 = Doubt window, 2 = Game end"
          enum: [0, 1, 2]
          example: 0
        tableCardCount:
          type: integer
          description: Total number of cards on the table
          example: 0
        lastAction:
          allOf:
            - $ref: '#/components/schemas/DoubtAction'
          nullable: true
          description: The most recent play action (null until first play)
        cpuDoubters:
          type: array
          description: Indices of CPU players who have decided to call doubt
          items:
            type: integer
          example: []
        cpuActions:
          type: array
          description: CPU play actions that occurred after the human's last action
          items:
            $ref: '#/components/schemas/DoubtAction'
        humanAction:
          allOf:
            - $ref: '#/components/schemas/DoubtAction'
          nullable: true
          description: The human player's most recent play action (null before first play)
        lastDoubtResult:
          allOf:
            - $ref: '#/components/schemas/DoubtDoubtResult'
          nullable: true
          description: Result of the most recent doubt resolution (null if no doubt yet)
        gameEndFlag:
          type: boolean
          description: Whether the game has ended
          example: false
        winnerIdx:
          type: integer
          description: Index of the winner (-1 while game is in progress)
          example: -1
        message:
          type: string
          description: |
            Game result or status message.  Empty while the game is in progress.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        doubtWindowSec:
          type: integer
          description: Duration of the doubt window in seconds (reflects current game config)
          example: 10
        penaltyDrawLimit:
          type: integer
          description: Maximum number of cards the loser picks up after a doubt resolution (0 = unlimited)
          example: 0
        metaAI:
          type: object
          nullable: true
          description: Meta-AI state for the current session (null when Meta-AI is disabled)
          properties:
            enabled:
              type: boolean
              description: Whether Meta-AI is enabled
              example: true
            gamesPlayed:
              type: integer
              description: Number of games played in the current session
              example: 5
            bluffRate:
              type: number
              format: float
              description: Rate at which the human player bluffs (0.0–1.0)
              example: 0.35
            doubtAccuracy:
              type: number
              format: float
              description: Accuracy of the human player's doubt calls (0.0–1.0)
              example: 0.60
            hesitationMean:
              type: number
              format: float
              description: Average human hesitation time in milliseconds
              example: 950.0

    # -------------------------------------------------------------------------
    # Texas Hold'em
    # -------------------------------------------------------------------------
    HoldemRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `fold` (`f`),
            `check` (`ck`), `call` (`c`), `bet` (`b`), `raise` (`ra`),
            `allin` (`a`), `rebuy` (`rb`), `skiprebuy` (`sr`),
            `addon` (`ad`), `skipaddon` (`sa`), `muck` (`m`),
            `show` (`sh`), `quit` (`q`).
          enum: [r, reset, f, fold, ck, check, c, call, b, bet, ra, raise, a, allin, rb, rebuy, sr, skiprebuy, ad, addon, sa, skipaddon, m, muck, sh, show, log, q, quit]
          example: bet
        amount:
          type: integer
          description: |
            Bet/raise amount (for the `bet` and `raise` commands).
            Ignored for other commands.
          minimum: 1
          example: 40
        smallBlind:
          type: integer
          description: |
            Small blind amount (for the `reset` command).
            Defaults to 5 when omitted.
            If only `smallBlind` is specified and it is >= the default `bigBlind`,
            `bigBlind` is auto-adjusted to `smallBlind * 2`.
          minimum: 1
          example: 5
        bigBlind:
          type: integer
          description: |
            Big blind amount (for the `reset` command).
            Defaults to 10 when omitted.
            If only `bigBlind` is specified and the default `smallBlind` is >= `bigBlind`,
            `smallBlind` is auto-adjusted to `bigBlind / 2` (minimum 1).
          minimum: 1
          example: 10
        tournamentMode:
          type: boolean
          description: |
            Enable tournament mode with blind escalation (for the `reset` command).
            Defaults to false when omitted.
          example: false
        blindLevelHands:
          type: integer
          description: |
            Number of hands before blinds increase in tournament mode (for the `reset` command).
            Defaults to 10 when omitted. Must be >= 1.
          minimum: 1
          example: 10
        blindMultiplier:
          type: integer
          description: |
            Blind multiplier as a percentage when blinds increase (for the `reset` command).
            Defaults to 200 (= 2x). Must be >= 101.
          minimum: 101
          example: 200
        bettingLimit:
          type: integer
          description: |
            Betting limit type (used with `reset` command).
            0 = Fixed (default), 1 = Pot Limit, 2 = No Limit.
          minimum: 0
          maximum: 2
          example: 0
        tableSize:
          type: integer
          description: |
            Table size (used with `reset` command).
            4 = 4-max (default), 6 = 6-max, 9 = 9-max.
          enum: [4, 6, 9]
          example: 4
        rebuyEnabled:
          type: boolean
          description: |
            Enable rebuy (used with `reset` command).
            Defaults to false when omitted.
          example: false
        rebuyMaxCount:
          type: integer
          description: |
            Maximum number of rebuys per player (used with `reset` command).
            Defaults to 3 when omitted.
          minimum: 1
          example: 3
        rebuyChips:
          type: integer
          description: |
            Chips granted per rebuy (used with `reset` command).
            Defaults to 1000 when omitted.
          minimum: 1
          example: 1000
        rebuyPeriodHands:
          type: integer
          description: |
            Number of hands during which rebuy is available (used with `reset` command).
            Defaults to 10 when omitted.
          minimum: 1
          example: 10
        addonEnabled:
          type: boolean
          description: |
            Enable addon (used with `reset` command).
            Defaults to false when omitted.
          example: false
        addonChips:
          type: integer
          description: |
            Chips granted by addon (used with `reset` command).
            Defaults to 1500 when omitted.
          minimum: 1
          example: 1500
        addonAfterHand:
          type: integer
          description: |
            Hand number after which addon becomes available (used with `reset` command).
            Defaults to 10 when omitted.
          minimum: 1
          example: 10
        cpuMetaAI:
          type: boolean
          description: |
            When true, CPUs adaptively adjust bluff and call behavior based on
            session-scoped game history (Meta-AI). Defaults to false.
          default: false
          example: false
        humanPlayMs:
          type: integer
          description: Human play hesitation time in milliseconds (0=not measured)
          example: 1200
        sessionId:
          type: string
          description: Unique session identifier (max 256 characters)
          maxLength: 256
          example: my-session-001

    HoldemPlayer:
      type: object
      required:
        - id
        - isHuman
        - cards
        - chips
        - currentBet
        - folded
        - allIn
        - handRank
        - handName
        - bestHand
        - playStyleName
      properties:
        id:
          type: integer
          description: Player index (0 = human, 1+ = CPU)
          example: 0
        isHuman:
          type: boolean
          description: Whether this is the human player
          example: true
        cards:
          type: array
          description: |
            Hole cards.  Human player's cards are always visible.
            CPU cards are hidden (empty array) until showdown.
          items:
            $ref: '#/components/schemas/Card'
        chips:
          type: integer
          description: Current chip count
          minimum: 0
          example: 990
        currentBet:
          type: integer
          description: Current round bet amount
          minimum: 0
          example: 10
        folded:
          type: boolean
          description: Whether the player has folded
          example: false
        allIn:
          type: boolean
          description: Whether the player is all-in
          example: false
        handRank:
          type: integer
          description: |
            Numeric hand rank (only populated at showdown for non-folded players):
            0 = High Card, 1 = One Pair, 2 = Two Pair, 3 = Three of a Kind,
            4 = Straight, 5 = Flush, 6 = Full House, 7 = Four of a Kind,
            8 = Straight Flush, 9 = Royal Flush.
          minimum: 0
          maximum: 9
          example: 0
        handName:
          type: string
          description: Human-readable hand name (empty until showdown)
          example: ""
        bestHand:
          type: array
          description: Best 5-card hand (only populated at showdown)
          items:
            $ref: '#/components/schemas/Card'
        lowBestHand:
          type: array
          description: |
            Omaha Hi-Lo only. Best 5-card low hand when qualified at
            showdown (5 distinct ranks all ≤ 8, Ace = 1). Absent for
            non-Hi-Lo games or unqualified players.
          items:
            $ref: '#/components/schemas/Card'
        lowQualifies:
          type: boolean
          description: |
            Omaha Hi-Lo only. True when the player has a qualifying low
            hand at showdown.
        playStyleName:
          type: string
          description: |
            CPU play style name: TAG (Tight-Aggressive), LAP (Loose-Passive),
            TAP (Tight-Passive), LAG (Loose-Aggressive).
            Empty for human player.
          example: TAG
        totalHands:
          type: integer
          description: Total number of hands played (for HUD stats)
          minimum: 0
          example: 10
        vpip:
          type: integer
          description: VPIP percentage (Voluntarily Put $ In Pot) — preflop call/bet/raise/allin rate
          minimum: 0
          maximum: 100
          example: 50
        pfr:
          type: integer
          description: PFR percentage (Pre-Flop Raise) — preflop bet/raise/allin rate
          minimum: 0
          maximum: 100
          example: 25
        threeBet:
          type: integer
          description: 3Bet percentage — preflop re-raise rate when facing a raise
          minimum: 0
          maximum: 100
          example: 10
        af:
          type: string
          description: "Aggression Factor (postflop bets+raises / calls). Values: decimal (e.g. \"2.5\"), \"∞\" (bets but no calls), \"-\" (no postflop actions)"
          example: "2.5"

    HoldemCpuAction:
      type: object
      description: Record of a CPU player's betting action
      required:
        - playerIdx
        - action
        - amount
      properties:
        playerIdx:
          type: integer
          description: Index of the CPU player who acted
          example: 1
        action:
          type: integer
          description: |
            Action taken: 0 = Fold, 1 = Check, 2 = Call,
            3 = Bet, 4 = Raise, 5 = All-in.
          enum: [0, 1, 2, 3, 4, 5]
          example: 2
        amount:
          type: integer
          description: Amount bet/raised (0 for fold/check/call/all-in)
          example: 0

    HoldemResult:
      type: object
      description: Result for a player at showdown
      required:
        - playerIdx
        - handRank
        - handName
        - bestHand
        - wonAmount
      properties:
        playerIdx:
          type: integer
          description: Index of the player
          example: 0
        handRank:
          type: integer
          description: Numeric hand rank
          example: 1
        handName:
          type: string
          description: Hand name (e.g., "One Pair")
          example: One Pair
        kickers:
          type: string
          description: Kicker card values formatted as display string (e.g., "A, Q, 10"). Empty for hands without kickers.
        bestHand:
          type: array
          description: Best 5-card hand
          items:
            $ref: '#/components/schemas/Card'
        wonAmount:
          type: integer
          description: Chips won from the pot
          example: 40
        mucked:
          type: boolean
          description: Whether the player mucked (hid) their hand at showdown
          example: false
        lowQualifies:
          type: boolean
          description: |
            Omaha Hi-Lo only. True when the player has a qualifying low hand
            (5 distinct ranks all ≤ 8, Ace = 1). Absent for non-Hi-Lo games.
        lowBestHand:
          type: array
          description: |
            Omaha Hi-Lo only. Best 5-card low hand when qualified. Absent
            otherwise.
          items:
            $ref: '#/components/schemas/Card'
        lowKickers:
          type: string
          description: Omaha Hi-Lo only. Low-side kicker display string.
        hiWonAmount:
          type: integer
          description: |
            Omaha Hi-Lo only. Chips won from the high half of split pots.
            `wonAmount` is the total (hi + lo). Absent for non-Hi-Lo games.
        lowWonAmount:
          type: integer
          description: |
            Omaha Hi-Lo only. Chips won from the low half of split pots.

    HoldemSidePot:
      type: object
      description: A side pot created when a player goes all-in
      required:
        - amount
        - eligiblePlayers
      properties:
        amount:
          type: integer
          description: Total chips in this side pot
          example: 100
        eligiblePlayers:
          type: array
          description: Indices of players eligible to win this pot
          items:
            type: integer
          example: [0, 1, 2]

    HoldemResponse:
      type: object
      required:
        - players
        - communityCards
        - pot
        - sidePots
        - dealerIdx
        - currentTurn
        - phase
        - gameEndFlag
        - lastBet
        - minRaise
        - roundResults
        - cpuActions
        - message
      properties:
        players:
          type: array
          description: State of all players (4/6/9 depending on table size)
          items:
            $ref: '#/components/schemas/HoldemPlayer'
        communityCards:
          type: array
          description: Community cards on the board (0/3/4/5 cards based on phase)
          items:
            $ref: '#/components/schemas/Card'
        pot:
          type: integer
          description: Current main pot size
          minimum: 0
          example: 30
        sidePots:
          type: array
          description: Side pots (created when players go all-in with different amounts)
          items:
            $ref: '#/components/schemas/HoldemSidePot'
        dealerIdx:
          type: integer
          description: Index of the dealer for this round
          example: 0
        currentTurn:
          type: integer
          description: Index of the player whose turn it is
          example: 0
        phase:
          type: integer
          description: |
            Current game phase:
            - `0` — Init
            - `1` — PreFlop
            - `2` — Flop
            - `3` — Turn
            - `4` — River
            - `5` — Showdown
            - `6` — End
            - `7` — Rebuy/Addon
          enum: [0, 1, 2, 3, 4, 5, 6, 7]
          example: 1
        gameEndFlag:
          type: boolean
          description: Whether the round has ended
          example: false
        lastBet:
          type: integer
          description: The current highest bet in this betting round
          minimum: 0
          example: 10
        minRaise:
          type: integer
          description: Minimum raise amount
          minimum: 0
          example: 20
        bettingLimit:
          type: integer
          description: |
            Betting limit type: 0 = Fixed, 1 = Pot Limit, 2 = No Limit.
          enum: [0, 1, 2]
        raiseCount:
          type: integer
          description: Number of raises in the current betting round.
          minimum: 0
        maxBetAmount:
          type: integer
          description: |
            Maximum allowed bet/raise amount for Pot Limit mode.
            0 means no limit (Fixed or No Limit mode).
          minimum: 0
        roundResults:
          type: array
          description: Results for each non-folded player at showdown
          items:
            $ref: '#/components/schemas/HoldemResult'
        cpuActions:
          type: array
          description: CPU actions that occurred since the last human action
          items:
            $ref: '#/components/schemas/HoldemCpuAction'
        message:
          type: string
          description: |
            Game result or status message.  One of:
            - `""` — game in progress
            - `"You are the winner."` — player wins
            - `"You lose."` — player loses
            - `"You folded."` — player folded
            - `"Game over."` — game ended (no results)
            - `"Unsupported command."` — unknown command
            - `"bye."` — session ended by quit command
            - `"param error."` — invalid request
            - `"error."` — internal error
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        handCount:
          type: integer
          description: Number of hands played (for tournament mode display)
          minimum: 0
          example: 0
        smallBlind:
          type: integer
          description: Current small blind amount
          minimum: 1
          example: 5
        bigBlind:
          type: integer
          description: Current big blind amount
          minimum: 2
          example: 10
        tournamentMode:
          type: boolean
          description: Whether tournament mode (blind escalation) is active
          example: false
        blindLevelHands:
          type: integer
          description: Number of hands before blinds increase
          minimum: 1
          example: 10
        blindMultiplier:
          type: integer
          description: Blind multiplier percentage (e.g. 200 = 2x)
          minimum: 101
          example: 200
        tableSize:
          type: integer
          description: Current table size (4/6/9)
          enum: [4, 6, 9]
          example: 4
        rebuyPhaseType:
          type: integer
          description: |
            Type of rebuy phase: 1 = rebuy, 2 = addon.
            Only present when phase is 7 (Rebuy/Addon).
          enum: [1, 2]
        rebuyChips:
          type: integer
          description: Chips available for current rebuy
          minimum: 0
        rebuyMaxCount:
          type: integer
          description: Maximum rebuy count setting
          minimum: 0
        rebuyCounts:
          type: array
          description: Rebuy count per player (same order as players array)
          items:
            type: integer
            minimum: 0
        addonChips:
          type: integer
          description: Chips available for addon
          minimum: 0
        muckAvailable:
          type: boolean
          description: Whether the human player can muck (hide) their hand at showdown (available when human lost)
          example: false
        isHiLo:
          type: boolean
          description: |
            Omaha Hi-Lo only. True when this game is configured as
            Omaha 8 or Better (split-pot variant). Absent for non-Hi-Lo
            games.
        equity:
          $ref: '#/components/schemas/HoldemEquity'
        potOdds:
          type: number
          format: double
          description: Pot odds percentage (0-100). Only present during PreFlop-River phases.
          example: 33.33

    HoldemEquity:
      type: object
      description: Equity (win probability) calculated via Monte Carlo simulation. Only present during PreFlop-River phases when human is active.
      required:
        - winProbability
        - handOdds
      properties:
        winProbability:
          type: number
          format: double
          description: Win probability (0.0 - 1.0)
          minimum: 0
          maximum: 1
          example: 0.75
        handOdds:
          type: array
          items:
            $ref: '#/components/schemas/HoldemHandOdds'

    HoldemHandOdds:
      type: object
      required:
        - handRank
        - handName
        - probability
      properties:
        handRank:
          type: integer
          description: Hand rank (0=HighCard, 1=OnePair, ..., 9=RoyalFlush)
          minimum: 0
          example: 1
        handName:
          type: string
          description: Hand rank name
          example: "One Pair"
        probability:
          type: number
          format: double
          description: Probability of achieving this hand rank (0.0 - 1.0)
          minimum: 0
          maximum: 1
          example: 0.5

    # -------------------------------------------------------------------------
    # Action Log (common across all games)
    # -------------------------------------------------------------------------
    ActionLogEntry:
      type: object
      required:
        - turnNumber
        - playerIdx
        - actionType
        - detail
      properties:
        turnNumber:
          type: integer
          description: Sequential turn number starting from 1
          minimum: 1
          example: 1
        playerIdx:
          type: integer
          description: Index of the player who performed the action (0 = human, 1+ = CPU)
          example: 0
        actionType:
          type: string
          description: Type of action (e.g., "hit", "stand", "bet", "play", "draw", "doubt", "fold")
          example: hit
        detail:
          type: string
          description: Human-readable description of the action
          example: "Player 0 hit and drew ♠A"
        cards:
          type: array
          description: Cards involved in the action (optional; revealed even if originally hidden)
          items:
            $ref: '#/components/schemas/Card'

    ActionLogResponse:
      type: object
      required:
        - entries
      properties:
        entries:
          type: array
          description: |
            Cumulative action log (棋譜) for the current game session.
            All hidden cards are revealed in the log entries.
          items:
            $ref: '#/components/schemas/ActionLogEntry'

    # -------------------------------------------------------------------------
    # Hearts
    # -------------------------------------------------------------------------
    HeartsRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `pass`,
            `play` (`p`), `next` (`n`), `nextround` (`nr`),
            `setdifficulty` (`sd`), `setlimit` (`sl`),
            `log` (`l`), `quit` (`q`).
          enum: [reset, r, pass, play, p, next, n, nextround, nr, setdifficulty, sd, setlimit, sl, log, l, quit, q]
          example: play
        cardIndices:
          type: array
          description: |
            Hand indices of the 3 cards to pass (required for `pass` command).
            Must contain exactly 3 indices.
          items:
            type: integer
          example: [0, 3, 7]
        cardIndex:
          type: integer
          description: Hand index of the card to play (required for `play` command)
          example: 2
        config:
          type: object
          description: Game configuration options (used with `reset`, `setdifficulty`, `setlimit` commands)
          properties:
            difficulty:
              type: integer
              description: "CPU difficulty level: 0 = Easy (random), 1 = Normal (point avoidance), 2 = Hard (strategic). Defaults to 1."
              minimum: 0
              maximum: 2
              example: 1
            pointLimit:
              type: integer
              description: "Point limit to end the game. Defaults to 100."
              minimum: 1
              example: 100
            omnibusJD:
              type: boolean
              description: "Omnibus Hearts variant: J♦ awards -10 points (beneficial). Defaults to false."
              example: false
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    HeartsPlayer:
      type: object
      description: A player in the Hearts game
      required:
        - id
        - isHuman
        - score
        - roundScore
        - cards
        - cardCount
      properties:
        id:
          type: integer
          description: Player index (0 = human, 1–3 = CPU)
          example: 0
        isHuman:
          type: boolean
          description: Whether this player is the human
          example: true
        score:
          type: integer
          description: Cumulative score across all rounds
          example: 15
        roundScore:
          type: integer
          description: Score accumulated in the current round
          example: 3
        cards:
          type: array
          description: Cards in hand (populated only for the human player)
          items:
            $ref: '#/components/schemas/Card'
        cardCount:
          type: integer
          description: Number of cards in hand
          example: 13

    HeartsTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
          example: 1
        card:
          $ref: '#/components/schemas/Card'

    HeartsResponse:
      type: object
      description: Current state of the Hearts game
      required:
        - players
        - phase
        - roundNumber
        - trickNumber
        - currentPlayerIdx
        - currentTrick
        - heartsBroken
        - passDirection
        - gameEndFlag
        - winnerIdx
        - message
      properties:
        players:
          type: array
          description: All players in seat order
          items:
            $ref: '#/components/schemas/HeartsPlayer'
        phase:
          type: integer
          description: "Game phase: 0 = Pass, 1 = Play, 2 = TrickEnd, 3 = RoundEnd, 4 = GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 1
        roundNumber:
          type: integer
          description: Current round number (1-based)
          example: 1
        trickNumber:
          type: integer
          description: Current trick number within the round (1-based)
          example: 1
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
          example: 0
        currentTrick:
          type: array
          description: Cards played in the current trick so far
          items:
            $ref: '#/components/schemas/HeartsTrickCard'
        heartsBroken:
          type: boolean
          description: Whether hearts have been broken this round
          example: false
        passDirection:
          type: integer
          description: "Pass direction for the current round: 0 = left, 1 = right, 2 = across, 3 = none"
          enum: [0, 1, 2, 3]
          example: 0
        gameEndFlag:
          type: boolean
          description: Whether the game has ended
          example: false
        winnerIdx:
          type: integer
          description: Index of the winner (-1 while game is in progress)
          example: -1
        message:
          type: string
          description: Game result or status message. Empty while the game is in progress.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        config:
          type: object
          description: Current game configuration
          properties:
            difficulty:
              type: integer
              description: "CPU difficulty level: 0 = Easy, 1 = Normal, 2 = Hard"
              example: 1
            pointLimit:
              type: integer
              description: Point limit to end the game
              example: 100
            omnibusJD:
              type: boolean
              description: "Omnibus Hearts variant: J♦ awards -10 points"
              example: false
        hint:
          type: object
          nullable: true
          description: Recommended play hint (only present when hint command is used)
          properties:
            cardIndices:
              type: array
              items:
                type: integer
              description: Recommended card indices (1 for play, 3 for pass)
            reason:
              type: string
              description: Machine-readable reason key for the hint

    # -------------------------------------------------------------------------
    # Memory
    # -------------------------------------------------------------------------
    MemoryRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `flip` (`f`),
            `next` (`n`), `log` (`l`).
          enum: [reset, r, flip, f, next, n, log, l]
          example: flip
        position:
          type: integer
          description: Board position of the card to flip (required for `flip` command, 0-based index)
          example: 5
        config:
          type: object
          description: Game configuration options (used with `reset` command)
          properties:
            cpuDifficulty:
              type: integer
              description: "CPU difficulty level: 0 = Easy (random), 1 = Normal (partial memory), 2 = Hard (perfect memory). Defaults to 1."
              minimum: 0
              maximum: 2
              example: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    MemoryPlayer:
      type: object
      description: A player in the Memory game
      required:
        - id
        - isHuman
        - pairCount
      properties:
        id:
          type: integer
          description: Player index (0 = human, 1–3 = CPU)
          example: 0
        isHuman:
          type: boolean
          description: Whether this player is the human
          example: true
        pairCount:
          type: integer
          description: Number of pairs collected
          example: 3

    MemoryBoardCard:
      type: object
      description: A card on the Memory board
      required:
        - faceUp
        - taken
      properties:
        faceUp:
          type: boolean
          description: Whether the card is currently face-up
          example: false
        taken:
          type: boolean
          description: Whether the card has been taken as part of a pair
          example: false
        card:
          description: Card details (only present when face-up)
          $ref: '#/components/schemas/Card'

    MemoryResponse:
      type: object
      description: Current state of the Memory game
      required:
        - players
        - phase
        - board
        - currentPlayerIdx
        - firstFlipPos
        - secondFlipPos
        - lastMatchResult
        - turnNumber
        - gameEndFlag
        - winnerIdx
        - message
        - config
      properties:
        players:
          type: array
          description: All players in seat order
          items:
            $ref: '#/components/schemas/MemoryPlayer'
        phase:
          type: integer
          description: "Game phase: 0 = Flip1, 1 = Flip2, 2 = Result, 3 = GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        board:
          type: array
          description: All 52 card positions on the board
          items:
            $ref: '#/components/schemas/MemoryBoardCard'
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
          example: 0
        firstFlipPos:
          type: integer
          description: Board position of the first flipped card (-1 if none)
          example: -1
        secondFlipPos:
          type: integer
          description: Board position of the second flipped card (-1 if none)
          example: -1
        lastMatchResult:
          type: boolean
          description: Whether the last flip pair was a match
          example: false
        turnNumber:
          type: integer
          description: Current turn number (incremented after each pair flip)
          example: 0
        gameEndFlag:
          type: boolean
          description: Whether the game has ended
          example: false
        winnerIdx:
          type: integer
          description: Index of the winner (-1 while game is in progress)
          example: -1
        message:
          type: string
          description: Game result or status message. Empty while the game is in progress.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        config:
          type: object
          description: Current game configuration
          required:
            - cpuDifficulty
          properties:
            cpuDifficulty:
              type: integer
              description: "CPU difficulty level: 0 = Easy, 1 = Normal, 2 = Hard"
              example: 1

    # -------------------------------------------------------------------------
    # Klondike
    # -------------------------------------------------------------------------
    KlondikeZone:
      type: object
      description: Specifies a zone on the Klondike board for move commands
      required:
        - zone
      properties:
        zone:
          type: string
          description: "Zone name: waste, tableau, or foundation"
          enum: [waste, tableau, foundation]
          example: tableau
        col:
          type: integer
          description: Column index (0-based, required for tableau and foundation zones)
          example: 3
        cardIndex:
          type: integer
          description: Index of the card within the column (for moving multiple cards from tableau)
          example: 0

    KlondikeRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `draw` (`d`),
            `move` (`m`), `giveup` (`g`), `hint` (`h`), `autocomplete` (`ac`),
            `log` (`l`).
          enum: [reset, r, draw, d, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
          example: move
        from:
          description: Source zone for a move command
          $ref: '#/components/schemas/KlondikeZone'
        to:
          description: Destination zone for a move command
          $ref: '#/components/schemas/KlondikeZone'
        config:
          type: object
          description: Game configuration (used with reset command)
          properties:
            drawCount:
              type: integer
              description: "Number of cards to draw from stock (1 or 3, default 1)"
              enum: [1, 3]
              example: 3
            scoringMode:
              type: integer
              description: "Scoring mode: 0 = None, 1 = Vegas"
              enum: [0, 1]
              example: 0
        n:
          type: integer
          description: Number of undos to perform (required when command is undo_n)
          example: 3
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    KlondikeTableauCard:
      type: object
      description: A card in a Klondike tableau column
      required:
        - faceUp
      properties:
        faceUp:
          type: boolean
          description: Whether the card is face-up
          example: true
        card:
          description: Card details (only present when face-up)
          $ref: '#/components/schemas/Card'

    KlondikeResponse:
      type: object
      description: Current state of the Klondike game
      required:
        - tableau
        - stockCount
        - waste
        - foundation
        - phase
        - moveCount
        - drawCount
        - canUndo
        - isStalemate
        - undoToEscape
        - score
        - scoringMode
        - message
      properties:
        tableau:
          type: array
          description: The 7 tableau columns, each containing an array of cards
          items:
            type: array
            items:
              $ref: '#/components/schemas/KlondikeTableauCard'
        stockCount:
          type: integer
          description: Number of cards remaining in the stock pile
          example: 24
        waste:
          type: array
          description: Cards in the waste pile (top card is the last element)
          items:
            $ref: '#/components/schemas/Card'
        foundation:
          type: array
          description: The 4 foundation piles (one per suit), each containing an array of cards
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 0 = Playing, 1 = GameClear, 2 = GameOver"
          enum: [0, 1, 2]
          example: 0
        moveCount:
          type: integer
          description: Total number of moves made in the current game
          example: 0
        drawCount:
          type: integer
          description: "Number of cards drawn per draw action (1 or 3)"
          enum: [1, 3]
          example: 1
        canUndo:
          type: boolean
          description: Whether the undo action is available
          example: false
        isStalemate:
          type: boolean
          description: Whether the game is in a stalemate (no moves can lead to a win)
          example: false
        undoToEscape:
          type: integer
          description: Number of undos needed to reach a non-stalemate state (0 if not stalemate, -1 if no escape)
          example: 0
        score:
          type: integer
          description: "Vegas score: -52 + 5 × foundation cards"
          example: -52
        scoringMode:
          type: integer
          description: "Scoring mode: 0 = None, 1 = Vegas"
          enum: [0, 1]
          example: 0
        message:
          type: string
          description: Game result or status message. Empty while the game is in progress.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        hint:
          type: object
          description: Hint for the next available move (only present when hint command is used)
          properties:
            from:
              $ref: '#/components/schemas/KlondikeZone'
            to:
              $ref: '#/components/schemas/KlondikeZone'

    # -------------------------------------------------------------------------
    # FreeCell
    # -------------------------------------------------------------------------
    FreeCellZone:
      type: object
      description: Specifies a zone on the FreeCell board for move commands
      required:
        - zone
      properties:
        zone:
          type: string
          description: "Zone name: tableau, freecell, or foundation"
          enum: [tableau, freecell, foundation]
          example: tableau
        col:
          type: integer
          description: Column index (0-based, used for tableau and foundation zones)
          example: 3
        cell:
          type: integer
          description: Free cell index (0-based, used for freecell zone)
          example: 0
        cardIndex:
          type: integer
          description: Index of the card within the column (for moving multiple cards from tableau)
          example: 0

    FreeCellRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `move` (`m`),
            `giveup` (`g`), `hint` (`h`), `autocomplete` (`ac`),
            `undo` (`u`), `undo_n`, `log` (`l`).
          enum: [reset, r, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
          example: move
        from:
          description: Source zone for a move command
          $ref: '#/components/schemas/FreeCellZone'
        to:
          description: Destination zone for a move command
          $ref: '#/components/schemas/FreeCellZone'
        n:
          type: integer
          description: Number of undos to perform (required when command is undo_n)
          example: 3
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    FreeCellResponse:
      type: object
      description: Current state of the FreeCell game
      required:
        - tableau
        - freeCells
        - foundation
        - phase
        - moveCount
        - canUndo
        - isStalemate
        - undoToEscape
        - message
      properties:
        tableau:
          type: array
          description: The 8 tableau columns, each containing an array of face-up cards
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        freeCells:
          type: array
          description: The 4 free cells (null or a Card)
          items:
            nullable: true
            $ref: '#/components/schemas/Card'
        foundation:
          type: array
          description: The 4 foundation piles (one per suit), each containing an array of cards
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 0 = Playing, 1 = GameClear, 2 = GameOver"
          enum: [0, 1, 2]
          example: 0
        moveCount:
          type: integer
          description: Total number of moves made in the current game
          example: 0
        canUndo:
          type: boolean
          description: Whether the undo action is available
          example: false
        isStalemate:
          type: boolean
          description: Whether the game is in a stalemate (no moves can lead to a win)
          example: false
        undoToEscape:
          type: integer
          description: Number of undos needed to reach a non-stalemate state (0 if not stalemate, -1 if no escape)
          example: 0
        hint:
          type: object
          description: Hint for the next available move (only present when hint command is used)
          properties:
            from:
              $ref: '#/components/schemas/FreeCellZone'
            to:
              $ref: '#/components/schemas/FreeCellZone'
        message:
          type: string
          description: Game result or status message. Empty while the game is in progress.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Eight Off
    # -------------------------------------------------------------------------
    EightOffZone:
      type: object
      description: Specifies a zone on the Eight Off board for move commands
      required:
        - zone
      properties:
        zone:
          type: string
          description: "Zone name: tableau, freecell, or foundation"
          enum: [tableau, freecell, foundation]
          example: tableau
        col:
          type: integer
          description: Column index (0-based, used for tableau and foundation zones)
          example: 3
        cell:
          type: integer
          description: Free cell index (0-based, 0..7)
          example: 4
        cardIndex:
          type: integer
          description: Index of the card within the column (for moving multiple cards from tableau)
          example: 0

    EightOffRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `move` (`m`),
            `giveup` (`g`), `hint` (`h`), `autocomplete` (`ac`),
            `undo` (`u`), `undo_n`, `log` (`l`).
          enum: [reset, r, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
          example: move
        from:
          description: Source zone for a move command
          $ref: '#/components/schemas/EightOffZone'
        to:
          description: Destination zone for a move command
          $ref: '#/components/schemas/EightOffZone'
        n:
          type: integer
          description: Number of undos to perform (required when command is undo_n)
          example: 3
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    EightOffResponse:
      type: object
      description: Current state of the Eight Off game
      required:
        - tableau
        - freeCells
        - foundation
        - phase
        - moveCount
        - canUndo
        - isStalemate
        - undoToEscape
        - message
      properties:
        tableau:
          type: array
          description: The 8 tableau columns, each containing an array of face-up cards
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        freeCells:
          type: array
          description: The 8 free cells (null or a Card)
          items:
            nullable: true
            $ref: '#/components/schemas/Card'
        foundation:
          type: array
          description: The 4 foundation piles (one per suit), each containing an array of cards
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 0 = Playing, 1 = GameClear, 2 = GameOver"
          enum: [0, 1, 2]
          example: 0
        moveCount:
          type: integer
          description: Total number of moves made in the current game
          example: 0
        canUndo:
          type: boolean
          description: Whether the undo action is available
          example: false
        isStalemate:
          type: boolean
          description: Whether the game is in a stalemate (no moves can lead to a win)
          example: false
        undoToEscape:
          type: integer
          description: Number of undos needed to reach a non-stalemate state (0 if not stalemate, -1 if no escape)
          example: 0
        hint:
          type: object
          description: Hint for the next available move (only present when hint command is used)
          properties:
            from:
              $ref: '#/components/schemas/EightOffZone'
            to:
              $ref: '#/components/schemas/EightOffZone'
        message:
          type: string
          description: Game result or status message. Empty while the game is in progress.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Seahaven Towers
    # -------------------------------------------------------------------------
    SeahavenTowersZone:
      type: object
      description: Specifies a zone on the Seahaven Towers board for move commands
      required:
        - zone
      properties:
        zone:
          type: string
          description: "Zone name: tableau, reserved, or foundation"
          enum: [tableau, reserved, foundation]
          example: tableau
        col:
          type: integer
          description: Column index (0-based, used for tableau and foundation zones)
          example: 3
        cell:
          type: integer
          description: Reserved cell index (0-based, used for reserved zone)
          example: 0
        cardIndex:
          type: integer
          description: Index of the card within the column (for moving multiple cards from tableau)
          example: 0

    SeahavenTowersRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `move` (`m`),
            `giveup` (`g`), `hint` (`h`), `autocomplete` (`ac`),
            `undo` (`u`), `undo_n`, `log` (`l`).
          enum: [reset, r, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
          example: move
        from:
          description: Source zone for a move command
          $ref: '#/components/schemas/SeahavenTowersZone'
        to:
          description: Destination zone for a move command
          $ref: '#/components/schemas/SeahavenTowersZone'
        n:
          type: integer
          description: Number of undos to perform (required when command is undo_n)
          example: 3
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    SeahavenTowersResponse:
      type: object
      description: Current state of the Seahaven Towers game
      required:
        - tableau
        - reservedCells
        - foundation
        - phase
        - moveCount
        - canUndo
        - isStalemate
        - undoToEscape
        - message
      properties:
        tableau:
          type: array
          description: The 10 tableau columns, each containing an array of face-up cards
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        reservedCells:
          type: array
          description: The 2 reserved cells / towers (null or a Card)
          items:
            nullable: true
            $ref: '#/components/schemas/Card'
        foundation:
          type: array
          description: The 4 foundation piles (one per suit), each containing an array of cards
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 0 = Playing, 1 = GameClear, 2 = GameOver"
          enum: [0, 1, 2]
          example: 0
        moveCount:
          type: integer
          description: Total number of moves made in the current game
          example: 0
        canUndo:
          type: boolean
          description: Whether the undo action is available
          example: false
        isStalemate:
          type: boolean
          description: Whether the game is in a stalemate (no moves can lead to a win)
          example: false
        undoToEscape:
          type: integer
          description: Number of undos needed to reach a non-stalemate state (0 if not stalemate, -1 if no escape)
          example: 0
        hint:
          type: object
          description: Hint for the next available move (only present when hint command is used)
          properties:
            from:
              $ref: '#/components/schemas/SeahavenTowersZone'
            to:
              $ref: '#/components/schemas/SeahavenTowersZone'
        message:
          type: string
          description: Game result or status message. Empty while the game is in progress.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Baccarat
    # -------------------------------------------------------------------------
    BaccaratRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `bet` (`b`),
            `log` (`l`), `clearhistory` (`ch`).
          enum: [r, reset, b, bet, log, l, ch, clearhistory]
          example: bet
        amount:
          type: integer
          description: |
            Bet amount.  Required for `bet` command (minimum 10, must be a
            multiple of 10).  Ignored for other commands.
          minimum: 10
          example: 100
        betType:
          type: integer
          description: |
            Bet type.  Required for `bet` command.
            0 = Player, 1 = Banker, 2 = Tie.
          enum: [0, 1, 2]
          example: 0
        playerPairBet:
          type: integer
          description: |
            Side bet amount on Player Pair.  Optional; used with the `bet`
            command.  Ignored for other commands.
          minimum: 0
          example: 50
        bankerPairBet:
          type: integer
          description: |
            Side bet amount on Banker Pair.  Optional; used with the `bet`
            command.  Ignored for other commands.
          minimum: 0
          example: 50
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    BaccaratResponse:
      type: object
      description: Current state of the Baccarat game
      required:
        - playerHand
        - bankerHand
        - playerHandValue
        - bankerHandValue
        - phase
        - chips
        - betAmount
        - betType
        - result
        - payout
        - message
      properties:
        playerHand:
          type: array
          description: Cards in the player's hand
          items:
            $ref: '#/components/schemas/Card'
        bankerHand:
          type: array
          description: Cards in the banker's hand
          items:
            $ref: '#/components/schemas/Card'
        playerHandValue:
          type: integer
          description: "Player hand value (ones digit of sum: 0-9)"
          example: 7
        bankerHandValue:
          type: integer
          description: "Banker hand value (ones digit of sum: 0-9)"
          example: 5
        phase:
          type: integer
          description: "Game phase: 1 = Bet, 2 = End"
          enum: [1, 2]
          example: 1
        chips:
          type: integer
          description: Current chip count
          example: 1000
        betAmount:
          type: integer
          description: Current bet amount
          example: 100
        betType:
          type: integer
          description: "Current bet type: 0 = Player, 1 = Banker, 2 = Tie"
          enum: [0, 1, 2]
          example: 0
        result:
          type: integer
          description: "Game result: 1 = Player wins, -1 = Banker wins, 0 = Tie/not resolved"
          enum: [-1, 0, 1]
          example: 0
        payout:
          type: integer
          description: Payout amount for the current round
          example: 0
        history:
          type: array
          description: |
            Big Road history — an ordered list of round results.
            Each element is an integer: 0 = Player win, 1 = Banker win, 2 = Tie.
          items:
            type: integer
            enum: [0, 1, 2]
          example: [0, 1, 1, 2, 0]
        playerPairBet:
          type: integer
          description: Current Player Pair side bet amount
          example: 0
        bankerPairBet:
          type: integer
          description: Current Banker Pair side bet amount
          example: 0
        sideBetResults:
          type: array
          description: Results of side bets for the current round
          items:
            type: object
            properties:
              betType:
                type: integer
                description: "Side bet type identifier"
              resultType:
                type: integer
                description: "Result type identifier"
              resultName:
                type: string
                description: "Human-readable result name"
              betAmount:
                type: integer
                description: "Amount wagered on this side bet"
              payout:
                type: integer
                description: "Payout amount (0 if lost)"
        message:
          type: string
          description: Game result or error message. Empty during bet phase.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Spades
    # -------------------------------------------------------------------------
    SpadesRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `bid` (`b`),
            `play` (`p`), `next` (`n`), `nextround` (`nr`), `log` (`l`).
          enum: [reset, r, bid, b, play, p, next, n, nextround, nr, log, l]
          example: reset
        bid:
          type: integer
          description: Bid value (0-13).  Required for `bid` command.
          minimum: 0
          maximum: 13
          example: 3
        cardIndex:
          type: integer
          description: Index of the card to play.  Required for `play` command.
          example: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            pointLimit:
              type: integer
              minimum: 1
              maximum: 1000
            nilBonus:
              type: integer
              minimum: 0
              maximum: 500
            bagPenaltyThreshold:
              type: integer
              minimum: 1
              maximum: 100
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    SpadesResponse:
      type: object
      description: Current state of the Spades game
      properties:
        players:
          type: array
          description: Array of player objects
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              bid:
                type: integer
              roundScore:
                type: integer
              cumulativeScore:
                type: integer
              trickCount:
                type: integer
              bags:
                type: integer
        phase:
          type: integer
          description: "Game phase: 0 = Bid, 1 = Play, 2 = TrickEnd, 3 = RoundEnd, 4 = GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
          description: Current round number
        trickNumber:
          type: integer
          description: Current trick number within the round
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        bidPlayerIdx:
          type: integer
          description: Index of the player currently bidding
        currentTrick:
          type: array
          description: Cards played in the current trick
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        spadesBroken:
          type: boolean
          description: Whether spades have been broken (played as a non-lead card)
        gameEndFlag:
          type: boolean
          description: Whether the game has ended
        winnerIdx:
          type: integer
          description: Index of the winning player (-1 if not ended)
        leadPlayerIdx:
          type: integer
          description: Index of the player who leads the current trick
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            pointLimit:
              type: integer
            nilBonus:
              type: integer
            bagPenaltyThreshold:
              type: integer
        message:
          type: string
          description: Game status message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        hint:
          type: object
          nullable: true
          description: Recommended play or bid hint (only present when hint command is used)
          properties:
            cardIndex:
              type: integer
              nullable: true
              description: Recommended card index (null during bid phase)
            bid:
              type: integer
              nullable: true
              description: Recommended bid value (null during play phase)
            reason:
              type: string
              description: Machine-readable reason key for the hint

    # -------------------------------------------------------------------------
    # Call Break
    # -------------------------------------------------------------------------
    CallBreakConfig:
      type: object
      description: Call Break game configuration
      properties:
        cpuDifficulty:
          type: integer
          minimum: 0
          maximum: 2
        maxRounds:
          type: integer
          minimum: 1
          maximum: 50

    CallBreakResponse:
      type: object
      description: |
        Current state of the Call Break game. `roundScore` and
        `cumulativeScore` are int×10 — the wire integer 41 represents 4.1
        points. The frontend divides by 10 for display.
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              bid:
                type: integer
              roundScore:
                type: integer
                description: Round score in int×10 form (display = value/10)
              cumulativeScore:
                type: integer
                description: Cumulative score in int×10 form (display = value/10)
              trickCount:
                type: integer
        phase:
          type: integer
          description: "0 = Bid, 1 = Play, 2 = TrickEnd, 3 = RoundEnd, 4 = GameEnd"
          enum: [0, 1, 2, 3, 4]
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        bidPlayerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        spadesBroken:
          type: boolean
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        validPlayIndices:
          type: array
          description: Indices in the human player's hand legal to play this turn (empty when not the human's play turn).
          items:
            type: integer
        config:
          $ref: '#/components/schemas/CallBreakConfig'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
        hint:
          type: object
          nullable: true
          properties:
            cardIndex:
              type: integer
              nullable: true
            bid:
              type: integer
              nullable: true
            reason:
              type: string

    # -------------------------------------------------------------------------
    # Tarneeb
    # -------------------------------------------------------------------------
    TarneebConfig:
      type: object
      description: Tarneeb game configuration
      properties:
        cpuDifficulty:
          type: integer
          minimum: 0
          maximum: 2
        pointLimit:
          type: integer
          minimum: 1
          maximum: 200
        minBid:
          type: integer
          minimum: 1
          maximum: 13

    TarneebResponse:
      type: object
      description: Current state of the Tarneeb game.
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              team:
                type: integer
                description: "0 or 1"
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              bid:
                type: integer
              roundScore:
                type: integer
              cumulativeScore:
                type: integer
              trickCount:
                type: integer
        teamScores:
          type: array
          items:
            type: integer
        phase:
          type: integer
          description: "0=Bid, 1=TrumpDeclaration, 2=Play, 3=TrickEnd, 4=RoundEnd, 5=GameEnd"
          enum: [0, 1, 2, 3, 4, 5]
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        bidPlayerIdx:
          type: integer
        bidWinnerIdx:
          type: integer
        highestBid:
          type: integer
        trumpSuit:
          type: integer
          description: "0 = not yet declared, 1=♠, 2=♣, 3=♥, 4=♦"
        redealCount:
          type: integer
        dealerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: -1 if undecided
        leadPlayerIdx:
          type: integer
        config:
          $ref: '#/components/schemas/TarneebConfig'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
        hint:
          type: object
          nullable: true
          properties:
            cardIndex:
              type: integer
              nullable: true
            bid:
              type: integer
              nullable: true
            trumpSuit:
              type: integer
              nullable: true
            reason:
              type: string

    # -------------------------------------------------------------------------
    # Briscola
    # -------------------------------------------------------------------------
    BriscolaConfig:
      type: object
      description: Briscola game configuration. v1 only supports Normal difficulty.
      properties:
        cpuDifficulty:
          type: integer
          minimum: 0
          maximum: 0
          description: "0 = Normal (only level supported in v1)"

    BriscolaResponse:
      type: object
      description: Current state of the Briscola game.
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              points:
                type: integer
                description: Cumulative points won in tricks (0-120)
              trickCount:
                type: integer
        phase:
          type: integer
          description: "0=Play, 1=TrickEnd, 2=GameEnd"
          enum: [0, 1, 2]
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        trumpSuit:
          type: integer
          description: "1=♠, 2=♣, 3=♥, 4=♦"
        trumpCard:
          $ref: '#/components/schemas/Card'
          description: Face-up trump card; omitted once the stock is exhausted.
        dealerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        stockRemaining:
          type: integer
          description: Cards left in the stock, counting the face-up trump card.
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: -1 = tie or unfinished, 0 = human, 1 = CPU
        config:
          $ref: '#/components/schemas/BriscolaConfig'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
        hint:
          type: object
          nullable: true
          properties:
            cardIndex:
              type: integer
              nullable: true
            reason:
              type: string

    # -------------------------------------------------------------------------
    # Schnapsen / Sixty-Six
    # -------------------------------------------------------------------------
    SchnapsenConfig:
      type: object
      description: Schnapsen game configuration. v1 only supports Normal difficulty.
      properties:
        cpuDifficulty:
          type: integer
          minimum: 0
          maximum: 0
          description: "0 = Normal (only level supported in v1)"

    SchnapsenResponse:
      type: object
      description: Current state of the Schnapsen game.
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              points:
                type: integer
                description: Cumulative card points + marriage bonuses (0-120+)
              trickCount:
                type: integer
        phase:
          type: integer
          description: "0=Play, 1=TrickEnd, 2=GameEnd"
          enum: [0, 1, 2]
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        trumpSuit:
          type: integer
          description: "1=♠, 2=♣, 3=♥, 4=♦"
        trumpCard:
          $ref: '#/components/schemas/Card'
          description: Face-up trump upcard; omitted once the stock is exhausted.
        dealerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        stockRemaining:
          type: integer
          description: Cards left in the stock (excludes the face-up trump upcard).
        isEndgame:
          type: boolean
          description: True once the stock is exhausted and must-follow rules apply (phase 2).
        validPlays:
          type: array
          items:
            type: integer
          description: Indices in the human hand that are legal to play right now.
        marriagePlays:
          type: array
          items:
            type: integer
          description: Indices in the human hand that can start a marriage declaration.
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: -1 = unfinished, 0 = human, 1 = CPU
        config:
          $ref: '#/components/schemas/SchnapsenConfig'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
        hint:
          type: object
          nullable: true
          properties:
            cardIndex:
              type: integer
              nullable: true
            reason:
              type: string
            isMarriage:
              type: boolean

    # -------------------------------------------------------------------------
    # Gaps / Montana
    # -------------------------------------------------------------------------
    GapsMoveZone:
      type: object
      description: A grid cell position used as a Gaps move source/target.
      required:
        - zone
      properties:
        zone:
          type: string
          enum: [grid]
        row:
          type: integer
          minimum: 0
          maximum: 3
          description: Grid row index (0..3).
        col:
          type: integer
          minimum: 0
          maximum: 12
          description: Grid column index (0..12).

    GapsResponse:
      type: object
      description: Current state of the Gaps / Montana game.
      properties:
        grid:
          type: array
          description: 4x13 grid; `null` cells are gaps.
          items:
            type: array
            items:
              oneOf:
                - $ref: '#/components/schemas/Card'
                - type: 'null'
        redealsUsed:
          type: integer
          description: Number of redeals already used (0..3).
        redealsRemaining:
          type: integer
          description: Number of redeals left (3 - redealsUsed).
        phase:
          type: integer
          description: "0=Playing, 1=GameClear, 2=GameOver"
          enum: [0, 1, 2]
        moveCount:
          type: integer
        canUndo:
          type: boolean
        isStalemate:
          type: boolean
        undoToEscape:
          type: integer
          description: Number of undos required to escape stalemate (0 if not stalemate, -1 if unreachable).
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
        hint:
          type: object
          nullable: true
          description: Suggested next move from the backend, if any.
          properties:
            fromRow:
              type: integer
            fromCol:
              type: integer
            toRow:
              type: integer
            toCol:
              type: integer

    # -------------------------------------------------------------------------
    # Two Ten Jack
    # -------------------------------------------------------------------------
    TwoTenJackRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `declare` (`d`),
            `play` (`p`), `next` (`n`), `nextround` (`nr`), `log` (`l`).
          enum: [reset, r, declare, d, play, p, next, n, nextround, nr, log, l]
          example: reset
        trumpSuit:
          type: integer
          description: "Trump suit to declare (1=♠, 2=♣, 3=♥, 4=♦). Required for `declare`."
          minimum: 1
          maximum: 4
          example: 1
        cardIndex:
          type: integer
          description: Index of the card to play.  Required for `play` command.
          example: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            pointLimit:
              type: integer
              minimum: 1
              maximum: 1000
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    TwoTenJackResponse:
      type: object
      description: Current state of the Two Ten Jack game
      properties:
        players:
          type: array
          description: Array of player objects (4 seats; teams are 0,2 vs 1,3)
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              roundScore:
                type: integer
              cumulativeScore:
                type: integer
              trickCount:
                type: integer
              capturedPoints:
                type: integer
        phase:
          type: integer
          description: "Game phase: 0 = Declare, 1 = Play, 2 = TrickEnd, 3 = RoundEnd, 4 = GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        declarerIdx:
          type: integer
          description: Index of the declarer for the current round
        trumpSuit:
          type: integer
          description: "Declared trump suit (1=♠, 2=♣, 3=♥, 4=♦; -1 if not yet declared)"
        currentTrick:
          type: array
          description: Cards played in the current trick
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: "Winning team index (0 = seats 0,2; 1 = seats 1,3; -1 if not ended)"
        leadPlayerIdx:
          type: integer
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            pointLimit:
              type: integer
        message:
          type: string
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        hint:
          type: object
          nullable: true
          description: Recommended play or trump suit hint (only present when hint command is used)
          properties:
            cardIndex:
              type: integer
              nullable: true
              description: Recommended card index (null during declare phase)
            trumpSuit:
              type: integer
              nullable: true
              description: Recommended trump suit (null during play phase)
            reason:
              type: string
              description: Machine-readable reason key for the hint

    # -------------------------------------------------------------------------
    # Crazy Eights
    # -------------------------------------------------------------------------
    CrazyEightsRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `play` (`p`),
            `draw` (`d`), `suit` (`s`), `nextround` (`nr`), `log` (`l`).
          enum: [reset, r, play, p, draw, d, suit, s, nextround, nr, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Index of the card to play.  Required for `play` command.
          example: 2
        suit:
          type: integer
          description: "Suit choice after playing an 8 (1=♠, 2=♣, 3=♥, 4=♦).  Required for `suit` command."
          minimum: 1
          maximum: 4
          example: 3
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            pointLimit:
              type: integer
              minimum: 1
              maximum: 1000
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    CrazyEightsResponse:
      type: object
      description: Current state of the Crazy Eights game
      properties:
        players:
          type: array
          description: Array of player objects
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              roundScore:
                type: integer
              cumulativeScore:
                type: integer
        phase:
          type: integer
          description: "Game phase: 0 = Play, 1 = ChooseSuit, 2 = RoundEnd, 3 = GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
          description: Current round number
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        discardTop:
          description: The top card of the discard pile
          $ref: '#/components/schemas/Card'
        drawPileCount:
          type: integer
          description: Number of cards remaining in the draw pile
        chosenSuit:
          type: integer
          description: "Suit chosen after playing an 8 (0 if not applicable; 1=♠, 2=♣, 3=♥, 4=♦)"
        gameEndFlag:
          type: boolean
          description: Whether the match has ended
        winnerIdx:
          type: integer
          description: Index of the match winner (-1 if not ended)
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            pointLimit:
              type: integer
        message:
          type: string
          description: Game status message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Macau
    # -------------------------------------------------------------------------
    MacauRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `play` (`p`),
            `draw` (`d`), `suit` (`s`), `declare` (`dc`), `skipdeclare` (`sk`),
            `nextround` (`nr`), `log` (`l`).
          enum: [reset, r, play, p, draw, d, suit, s, declare, dc, skipdeclare, sk, nextround, nr, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Index of the card to play.  Required for `play` command.
          example: 2
        suit:
          type: integer
          description: "Suit choice after playing an 8 (1=♠, 2=♣, 3=♥, 4=♦).  Required for `suit` command."
          minimum: 1
          maximum: 4
          example: 3
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            pointLimit:
              type: integer
              minimum: 1
              maximum: 1000
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    MacauResponse:
      type: object
      description: Current state of the Macau game
      properties:
        players:
          type: array
          description: Array of player objects
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              roundScore:
                type: integer
              cumulativeScore:
                type: integer
              hasDeclared:
                type: boolean
                description: Whether the player has declared "Macau!" this turn
        phase:
          type: integer
          description: "Game phase: 0 = Play, 1 = ChooseSuit, 2 = MustDeclare, 3 = RoundEnd, 4 = GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
          description: Current round number
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        discardTop:
          description: The top card of the discard pile
          $ref: '#/components/schemas/Card'
        drawPileCount:
          type: integer
          description: Number of cards remaining in the draw pile
        chosenSuit:
          type: integer
          description: "Suit chosen after playing an 8 (0 if not applicable; 1=♠, 2=♣, 3=♥, 4=♦)"
        penaltyDrawCount:
          type: integer
          description: Accumulated draw-two penalty (0 when no penalty is active)
        direction:
          type: integer
          description: "Play direction: 1 = clockwise, -1 = counter-clockwise (reversed by Jacks)"
        gameEndFlag:
          type: boolean
          description: Whether the match has ended
        winnerIdx:
          type: integer
          description: Index of the match winner (-1 if not ended)
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            pointLimit:
              type: integer
        message:
          type: string
          description: Game status message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Mao
    # -------------------------------------------------------------------------
    MaoRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `play` (`p`),
            `draw` (`d`), `suit` (`s`), `declare` (`dc`), `skipdeclare` (`sk`),
            `declareword` (`dw`), `nextround` (`nr`), `log` (`l`).
          enum: [reset, r, play, p, draw, d, suit, s, declare, dc, skipdeclare, sk, declareword, dw, nextround, nr, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Index of the card to play.  Required for `play` command.
          example: 2
        suit:
          type: integer
          description: "Suit choice after playing an 8 (1=♠, 2=♣, 3=♥, 4=♦).  Required for `suit` command."
          minimum: 1
          maximum: 4
          example: 3
        word:
          type: string
          description: The compliance utterance for the hidden rule.  Required for `declareword`.
          example: mao
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            pointLimit:
              type: integer
              minimum: 1
              maximum: 1000
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    MaoResponse:
      type: object
      description: Current state of the Mao game
      properties:
        players:
          type: array
          description: Array of player objects
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              roundScore:
                type: integer
              cumulativeScore:
                type: integer
              hasDeclared:
                type: boolean
                description: Whether the player has declared "Mao!" this turn
        phase:
          type: integer
          description: "Game phase: 0 = Play, 1 = ChooseSuit, 2 = MustDeclare, 3 = RoundEnd, 4 = GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
          description: Current round number
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        discardTop:
          description: The top card of the discard pile
          $ref: '#/components/schemas/Card'
        drawPileCount:
          type: integer
          description: Number of cards remaining in the draw pile
        chosenSuit:
          type: integer
          description: "Suit chosen after playing an 8 (0 if not applicable; 1=♠, 2=♣, 3=♥, 4=♦)"
        penaltyDrawCount:
          type: integer
          description: Accumulated draw-two penalty (0 when no penalty is active)
        direction:
          type: integer
          description: "Play direction: 1 = clockwise, -1 = counter-clockwise"
        gameEndFlag:
          type: boolean
          description: Whether the match has ended
        winnerIdx:
          type: integer
          description: Index of the match winner (-1 if not ended)
        awaitingWord:
          type: boolean
          description: Hidden-rule signal — the player may need to say a word (the game does not reveal which)
        correctCount:
          type: integer
          description: Number of times the player has correctly complied with the hidden rule
        hintUnlocked:
          type: boolean
          description: Whether a vague rule hint has been unlocked (true after 3 correct compliances)
        ruleHint:
          type: string
          description: A vague hint about the hidden rule.  Populated only when `hintUnlocked` is true.
        rulePenalty:
          type: boolean
          description: Hidden-rule signal — the player's last action incurred a penalty (no reason given)
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            pointLimit:
              type: integer
        message:
          type: string
          description: Game status message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Page One
    # -------------------------------------------------------------------------
    PageOneRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `play` (`p`),
            `draw` (`d`), `declare` (`dc`), `skip` (`sk`),
            `nextround` (`nr`), `log` (`l`).
          enum: [reset, r, play, p, draw, d, declare, dc, skip, sk, nextround, nr, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Index of the card to play.  Required for `play` command.
          example: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            pointLimit:
              type: integer
              minimum: 1
              maximum: 1000
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    PageOneResponse:
      type: object
      description: Current state of the Page One game
      properties:
        players:
          type: array
          description: Array of player objects
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              roundScore:
                type: integer
              cumulativeScore:
                type: integer
              hasDeclared:
                type: boolean
                description: Whether the player has declared "Page One!"
        phase:
          type: integer
          description: "Game phase: 0 = Play, 1 = MustDeclare, 2 = RoundEnd, 3 = GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        discardTop:
          $ref: '#/components/schemas/Card'
        drawPileCount:
          type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            pointLimit:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    # -------------------------------------------------------------------------
    # Gin Rummy
    # -------------------------------------------------------------------------
    SevenBridgeRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Seven Bridge command. Accepted values: `reset` (`r`),
            `drawstock` (`ds`), `pon`, `chi`, `meld` (`m`),
            `layoff` (`lo`), `discard` (`d`), `nextround` (`nr`), `log` (`l`).
          enum: [reset, r, drawstock, ds, pon, chi, meld, m, layoff, lo, discard, d, nextround, nr, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Index of the card to discard (for `discard` / `layoff`).
          example: 2
        cardIndices:
          type: array
          description: Card indices for `pon`, `chi`, or `meld`.
          items:
            type: integer
          example: [0, 1, 2]
        targetPlayerIdx:
          type: integer
          description: Target player's index for `layoff`.
          example: 1
        meldIdx:
          type: integer
          description: Target meld index within the player for `layoff`.
          example: 0
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            pointLimit:
              type: integer
              minimum: 1
              maximum: 1000
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    SevenBridgeResponse:
      type: object
      description: Current state of the Seven Bridge game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              melds:
                type: array
                description: Melds laid down by this player
                items:
                  type: object
                  properties:
                    cards:
                      type: array
                      items:
                        $ref: '#/components/schemas/Card'
              roundScore:
                type: integer
              cumulativeScore:
                type: integer
        phase:
          type: integer
          description: "Game phase: 0 = Draw, 1 = Play, 2 = RoundEnd, 3 = GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        discardTop:
          $ref: '#/components/schemas/Card'
        drawPileCount:
          type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
        roundWinnerIdx:
          type: integer
          description: Index of the last round winner (-1 for stock-out / not decided)
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            pointLimit:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    GinRummyRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `drawstock` (`ds`),
            `drawdiscard` (`dd`), `discard` (`d`), `knock` (`k`),
            `layoff` (`lo`), `nextround` (`nr`), `log` (`l`).
          enum: [reset, r, drawstock, ds, drawdiscard, dd, discard, d, knock, k, layoff, lo, nextround, nr, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Index of the card to discard or knock with.  Required for `discard` and `knock` commands.
          example: 2
        cardIndices:
          type: array
          description: Card indices for `layoff` command.
          items:
            type: integer
          example: [0, 3]
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            pointLimit:
              type: integer
              minimum: 1
              maximum: 1000
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    GinRummyResponse:
      type: object
      description: Current state of the Gin Rummy game
      properties:
        players:
          type: array
          description: Array of player objects
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              roundScore:
                type: integer
              cumulativeScore:
                type: integer
        phase:
          type: integer
          description: "Game phase: 0 = Draw, 1 = Discard, 2 = Layoff, 3 = RoundEnd, 4 = GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
          description: Current round number
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        discardTop:
          description: The top card of the discard pile
          $ref: '#/components/schemas/Card'
        drawPileCount:
          type: integer
          description: Number of cards remaining in the stock pile
        gameEndFlag:
          type: boolean
          description: Whether the match has ended
        winnerIdx:
          type: integer
          description: Index of the match winner (-1 if not ended)
        knockerIdx:
          type: integer
          description: Index of the player who knocked (-1 if not applicable)
        knockerMelds:
          type: array
          description: "Knocker's melds (array of card groups)"
          items:
            type: object
            properties:
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
        knockerDeadwood:
          type: array
          description: "Knocker's deadwood cards"
          items:
            $ref: '#/components/schemas/Card'
        isGin:
          type: boolean
          description: Whether the knock was a gin (0 deadwood)
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            pointLimit:
              type: integer
        message:
          type: string
          description: Game status message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    ChinchonRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `drawstock` (`ds`),
            `drawdiscard` (`dd`), `discard` (`d`), `knock` (`k`),
            `layoff` (`lo`), `nextround` (`nr`), `log` (`l`).
          enum: [reset, r, drawstock, ds, drawdiscard, dd, discard, d, knock, k, layoff, lo, nextround, nr, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Index of the card to discard or knock with.  Required for `discard` and `knock` commands.
          example: 2
        cardIndices:
          type: array
          description: Card indices for `layoff` command.
          items:
            type: integer
          example: [0, 3]
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            playerCount:
              type: integer
              minimum: 2
              maximum: 4
            knockThreshold:
              type: integer
              minimum: 0
              maximum: 60
            eliminationLimit:
              type: integer
              minimum: 1
              maximum: 1000
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    ChinchonResponse:
      type: object
      description: Current state of the Chinchon game
      properties:
        players:
          type: array
          description: Array of player objects
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              roundScore:
                type: integer
              cumulativeScore:
                type: integer
              eliminated:
                type: boolean
        phase:
          type: integer
          description: "Game phase: 0 = Draw, 1 = Discard, 2 = Layoff, 3 = RoundEnd, 4 = GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
          description: Current round number
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        discardTop:
          description: The top card of the discard pile
          $ref: '#/components/schemas/Card'
        drawPileCount:
          type: integer
          description: Number of cards remaining in the stock pile
        gameEndFlag:
          type: boolean
          description: Whether the match has ended
        winnerIdx:
          type: integer
          description: Index of the match winner (-1 if not ended)
        knockerIdx:
          type: integer
          description: Index of the player who knocked (-1 if not applicable)
        knockerMelds:
          type: array
          description: "Knocker's melds (array of card groups), used as layoff targets"
          items:
            type: object
            properties:
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            playerCount:
              type: integer
            knockThreshold:
              type: integer
            eliminationLimit:
              type: integer
        message:
          type: string
          description: Game status message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    ThreeThirteenRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `drawstock` (`ds`),
            `drawdiscard` (`dd`), `discard` (`d`), `knock` (`k`),
            `nextround` (`nr`), `log` (`l`).
          enum: [reset, r, drawstock, ds, drawdiscard, dd, discard, d, knock, k, nextround, nr, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Index of the card to discard or knock with.  Required for `discard` and `knock` commands.
          example: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            playerCount:
              type: integer
              minimum: 2
              maximum: 4
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    ThreeThirteenResponse:
      type: object
      description: Current state of the Three Thirteen game
      properties:
        players:
          type: array
          description: Array of player objects
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              deadwood:
                type: integer
              roundScore:
                type: integer
              cumulativeScore:
                type: integer
        phase:
          type: integer
          description: "Game phase: 0 = Draw, 1 = Discard, 2 = RoundEnd, 3 = GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        round:
          type: integer
          description: Current round number (1 to 11)
        wildRank:
          type: integer
          description: The wild card rank for the current round
        dealCount:
          type: integer
          description: Number of cards dealt to each player this round
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        knockerIdx:
          type: integer
          description: Index of the player who knocked (-1 if not applicable)
        discardTop:
          description: The top card of the discard pile
          $ref: '#/components/schemas/Card'
        drawPileCount:
          type: integer
          description: Number of cards remaining in the stock pile
        gameEndFlag:
          type: boolean
          description: Whether the match has ended
        winnerIdx:
          type: integer
          description: Index of the match winner (-1 if not ended)
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            playerCount:
              type: integer
        message:
          type: string
          description: Game status message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    ConquianRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `drawstock` (`ds`),
            `drawdiscard` (`dd`), `meld` (`m`), `discard` (`d`),
            `nextround` (`nr`), `log` (`l`).
          enum: [reset, r, drawstock, ds, drawdiscard, dd, meld, m, discard, d, nextround, nr, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Index of the card to discard.  Required for the `discard` command.
          example: 2
        meldGroups:
          type: array
          description: Groups of card indices to lay down as melds.  Required for the `meld` command.
          items:
            type: array
            items:
              type: integer
          example: [[0, 1, 2]]
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetWins:
              type: integer
              minimum: 1
              maximum: 100
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    ConquianResponse:
      type: object
      description: Current state of the Conquian game
      properties:
        players:
          type: array
          description: Array of player objects
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              melds:
                type: array
                description: Player's face-up table melds (array of card groups)
                items:
                  type: object
                  properties:
                    cards:
                      type: array
                      items:
                        $ref: '#/components/schemas/Card'
              wins:
                type: integer
                description: Number of rounds this player has won
        phase:
          type: integer
          description: "Game phase: 0 = Draw, 1 = Meld, 2 = RoundEnd, 3 = GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
          description: Current round number
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        discardTop:
          description: The top card of the discard pile
          $ref: '#/components/schemas/Card'
        drawPileCount:
          type: integer
          description: Number of cards remaining in the stock pile
        gameEndFlag:
          type: boolean
          description: Whether the match has ended
        winnerIdx:
          type: integer
          description: Index of the match winner (-1 if not ended or drawn)
        roundWinnerIdx:
          type: integer
          description: Index of the round winner (-1 if not applicable)
        tookDiscard:
          type: boolean
          description: Whether the player drew the discard top and must use it in a meld
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetWins:
              type: integer
        message:
          type: string
          description: Game status message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Tonk types
    # -------------------------------------------------------------------------
    TonkRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `drawstock` (`ds`),
            `drawdiscard` (`dd`), `discard` (`d`), `knock` (`k`),
            `nextround` (`nr`), `log` (`l`).
          enum: [reset, r, drawstock, ds, drawdiscard, dd, discard, d, knock, k, nextround, nr, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Index of the card to discard or knock with. Required for `discard` and `knock` commands.
          example: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            pointLimit:
              type: integer
              minimum: 1
              maximum: 1000
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    TonkResponse:
      type: object
      description: Current state of the Tonk game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              roundScore:
                type: integer
              cumulativeScore:
                type: integer
        phase:
          type: integer
          description: "Game phase: 0 = Draw, 1 = Discard, 2 = RoundEnd, 3 = GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        discardTop:
          $ref: '#/components/schemas/Card'
        drawPileCount:
          type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
        knockerIdx:
          type: integer
        knockerMelds:
          type: array
          items:
            type: object
            properties:
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
        knockerDeadwood:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        opponentMelds:
          type: array
          items:
            type: object
            properties:
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
        opponentDeadwood:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        isTonk:
          type: boolean
          description: Whether the round was won by an instant Tonk on deal (49/50 points)
        isUndercut:
          type: boolean
          description: Whether the opponent undercut the knocker
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            pointLimit:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    # -------------------------------------------------------------------------
    # Thirty-One types
    # -------------------------------------------------------------------------
    ThirtyOneRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `drawstock` (`ds`),
            `drawdiscard` (`dd`), `discard` (`d`), `knock` (`k`),
            `nextround` (`nr`), `log` (`l`).
          enum: [reset, r, drawstock, ds, drawdiscard, dd, discard, d, knock, k, nextround, nr, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Index of the card to discard. Required for the `discard` command.
          example: 1
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            initialLives:
              type: integer
              minimum: 1
              maximum: 10
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    ThirtyOneResponse:
      type: object
      description: Current state of the Thirty-One game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              lives:
                type: integer
              score:
                type: integer
                description: Best single-suit total (revealed for the human always, for CPUs at round end)
              isEliminated:
                type: boolean
        phase:
          type: integer
          description: "Game phase: 0 = Draw, 1 = Discard, 2 = RoundEnd, 3 = GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        discardTop:
          $ref: '#/components/schemas/Card'
        drawPileCount:
          type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
        knockerIdx:
          type: integer
          description: Index of the player who knocked (-1 if none)
        thirtyOneIdx:
          type: integer
          description: Index of the player who reached exactly 31 (-1 if none)
        roundWinnerIdx:
          type: integer
        roundLosers:
          type: array
          items:
            type: integer
          description: Indices of players who lost a life in the most recent round
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            initialLives:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    # -------------------------------------------------------------------------
    # Yaniv types
    # -------------------------------------------------------------------------
    YanivRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `discard` (`d`),
            `yaniv` (`y`), `drawstock` (`ds`), `drawpickup` (`dp`),
            `nextround` (`nr`), `log` (`l`).
          enum: [reset, r, discard, d, yaniv, y, drawstock, ds, drawpickup, dp, nextround, nr, log, l]
          example: reset
        cardIndices:
          type: array
          items:
            type: integer
          description: Indices of the cards to discard. Required for the `discard` command (single card, same-value set, or same-suit run).
          example: [0, 1]
        end:
          type: integer
          description: Which end of the previous discard to take (0 = first, 1 = last). Required for the `drawpickup` command.
          enum: [0, 1]
          example: 0
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            scoreLimit:
              type: integer
              minimum: 50
              maximum: 500
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    YanivResponse:
      type: object
      description: Current state of the Yaniv game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              score:
                type: integer
                description: Cumulative penalty score (lower is better)
              handTotal:
                type: integer
                description: Sum of the hand's card values (revealed for the human always, for CPUs at round end)
              isEliminated:
                type: boolean
        phase:
          type: integer
          description: "Game phase: 0 = Discard, 1 = Draw, 2 = RoundEnd, 3 = GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        pickupCards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
          description: The previous player's discard; only the two end cards may be taken
        drawPileCount:
          type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
        callerIdx:
          type: integer
          description: Index of the player who declared Yaniv (-1 if none)
        asafWinnerIdx:
          type: integer
          description: Index of the player who undercut the caller on Asaf (-1 if none)
        isAsaf:
          type: boolean
        roundScores:
          type: array
          items:
            type: integer
          description: Penalty added to each player in the most recent round
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            scoreLimit:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    TressetteRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `play` (`p`),
            `next` (`n`), `nextround` (`nr`), `hint` (`h`), `log` (`l`).
          enum: [reset, r, play, p, next, n, nextround, nr, hint, h, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Index of the card to play. Required for the `play` command.
          example: 0
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetPoints:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    TressetteResponse:
      type: object
      description: Current state of the Tressette game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              trickCount:
                type: integer
              teamId:
                type: integer
                description: "Team membership: 0 = players 0 & 2, 1 = players 1 & 3"
        phase:
          type: integer
          description: "Game phase: 0 = Play, 1 = TrickEnd, 2 = RoundEnd, 3 = GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        leadPlayerIdx:
          type: integer
        teamScores:
          type: array
          items:
            type: integer
          description: Cumulative points for each team (index 0 = Team A, 1 = Team B)
        teamRoundThirds:
          type: array
          items:
            type: integer
          description: Thirds-of-a-point accumulated by each team in the current round
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (must-follow filtered)
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: Winning team index (-1 if undecided)
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetPoints:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    # -------------------------------------------------------------------------
    # Gong Zhu types
    # -------------------------------------------------------------------------
    GongZhuRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `expose`, `play` (`p`),
            `next` (`n`), `nextround` (`nr`), `hint` (`h`), `log` (`l`).
          enum: [reset, r, expose, play, p, next, n, nextround, nr, hint, h, log, l]
          example: reset
        cardIndices:
          type: array
          items:
            type: integer
          description: Hand indices of point cards to expose. Used by the `expose` command (empty array = expose nothing).
          example: []
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            pointLimit:
              type: integer
              minimum: 1
              maximum: 100000
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    GongZhuResponse:
      type: object
      description: Current state of the Gong Zhu game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              roundScore:
                type: integer
              cumulativeScore:
                type: integer
              trickCount:
                type: integer
        phase:
          type: integer
          description: "Game phase: 0 = Expose, 1 = Play, 2 = TrickEnd, 3 = RoundEnd, 4 = GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        heartsBroken:
          type: boolean
        exposed:
          type: object
          description: Which point cards have been exposed (stakes doubled)
          properties:
            pig:
              type: boolean
            sheep:
              type: boolean
            ace:
              type: boolean
            doubler:
              type: boolean
        exposableIndices:
          type: array
          items:
            type: integer
          description: Hand indices of the human's exposable point cards
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            pointLimit:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    # -------------------------------------------------------------------------
    # Canasta types
    # -------------------------------------------------------------------------
    CanastaRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `drawstock` (`ds`),
            `drawdiscard` (`dd`), `meld` (`m`), `skipmeld` (`sm`),
            `discard` (`d`), `goout` (`go`), `nextround` (`nr`), `log` (`l`).
          enum: [reset, r, drawstock, ds, drawdiscard, dd, meld, m, skipmeld, sm, discard, d, goout, go, nextround, nr, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Index of the card to discard.  Required for `discard` command.
          example: 2
        naturalPairIndices:
          type: array
          description: Indices of two natural cards matching the discard top.  Required for `drawdiscard` command.
          items:
            type: integer
          example: [0, 1]
        meldGroups:
          type: array
          description: Groups of card indices to meld.  Required for `meld` command.
          items:
            type: array
            items:
              type: integer
          example: [[0, 1, 2]]
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            pointLimit:
              type: integer
              minimum: 1
              maximum: 100000
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    CanastaResponse:
      type: object
      description: Current state of the Canasta game
      properties:
        players:
          type: array
          description: Array of player objects
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              melds:
                type: array
                items:
                  type: object
                  properties:
                    cards:
                      type: array
                      items:
                        $ref: '#/components/schemas/Card'
                    isNatural:
                      type: boolean
                    isCanasta:
                      type: boolean
                    rank:
                      type: integer
              red3Count:
                type: integer
              red3s:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              roundScore:
                type: integer
              cumulativeScore:
                type: integer
              hasCanasta:
                type: boolean
              hasInitMeld:
                type: boolean
        phase:
          type: integer
          description: "Game phase: 0 = Draw, 1 = Meld, 2 = Discard, 3 = RoundEnd, 4 = GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
          description: Current round number
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        discardTop:
          description: The top card of the discard pile
          $ref: '#/components/schemas/Card'
        drawPileCount:
          type: integer
          description: Number of cards remaining in the stock pile
        discardPileCount:
          type: integer
          description: Number of cards in the discard pile
        isFrozen:
          type: boolean
          description: Whether the discard pile is frozen
        gameEndFlag:
          type: boolean
          description: Whether the match has ended
        winnerIdx:
          type: integer
          description: Index of the match winner (-1 if not ended)
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            pointLimit:
              type: integer
        message:
          type: string
          description: Game status message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Hand and Foot types
    # -------------------------------------------------------------------------
    HandAndFootRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `drawstock` (`ds`),
            `drawdiscard` (`dd`), `meld` (`m`), `skipmeld` (`sm`),
            `discard` (`d`), `goout` (`go`), `nextround` (`nr`), `log` (`l`).
          enum: [reset, r, drawstock, ds, drawdiscard, dd, meld, m, skipmeld, sm, discard, d, goout, go, nextround, nr, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Index of the card to discard.  Required for `discard` command.
          example: 2
        naturalPairIndices:
          type: array
          description: Indices of two natural cards matching the discard top.  Required for `drawdiscard` command.
          items:
            type: integer
          example: [0, 1]
        meldGroups:
          type: array
          description: Groups of card indices to meld.  Required for `meld` command.
          items:
            type: array
            items:
              type: integer
          example: [[0, 1, 2]]
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            pointLimit:
              type: integer
              minimum: 1
              maximum: 100000
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    HandAndFootResponse:
      type: object
      description: Current state of the Hand and Foot game
      properties:
        players:
          type: array
          description: Array of player objects
          items:
            type: object
            properties:
              id:
                type: integer
              team:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              footCount:
                type: integer
              inFoot:
                type: boolean
              roundScore:
                type: integer
              cumulativeScore:
                type: integer
        teams:
          type: array
          description: Per-team melds and red 3s
          items:
            type: object
            properties:
              team:
                type: integer
              melds:
                type: array
                items:
                  type: object
                  properties:
                    cards:
                      type: array
                      items:
                        $ref: '#/components/schemas/Card'
                    isNatural:
                      type: boolean
                    isCanasta:
                      type: boolean
                    rank:
                      type: integer
              red3Count:
                type: integer
              red3s:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 0 = Draw, 1 = Meld, 2 = Discard, 3 = RoundEnd, 4 = GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
          description: Current round number
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        discardTop:
          description: The top card of the discard pile
          $ref: '#/components/schemas/Card'
        drawPileCount:
          type: integer
          description: Number of cards remaining in the stock pile
        discardPileCount:
          type: integer
          description: Number of cards in the discard pile
        isFrozen:
          type: boolean
          description: Whether the discard pile is frozen
        gameEndFlag:
          type: boolean
          description: Whether the match has ended
        winnerTeam:
          type: integer
          description: Index of the winning team (-1 if not ended)
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            pointLimit:
              type: integer
        message:
          type: string
          description: Game status message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Burraco types
    # -------------------------------------------------------------------------
    BurracoRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `drawstock` (`ds`),
            `drawdiscard` (`dd`), `meld` (`m`), `skipmeld` (`sm`),
            `discard` (`d`), `goout` (`go`), `nextround` (`nr`), `log` (`l`).
          enum: [reset, r, drawstock, ds, drawdiscard, dd, meld, m, skipmeld, sm, discard, d, goout, go, nextround, nr, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Index of the card to discard.  Required for `discard` command.
          example: 2
        naturalPairIndices:
          type: array
          description: Indices of two natural cards matching the discard top.  Required for `drawdiscard` command.
          items:
            type: integer
          example: [0, 1]
        meldGroups:
          type: array
          description: Groups of card indices to meld.  Required for `meld` command.
          items:
            type: array
            items:
              type: integer
          example: [[0, 1, 2]]
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            pointLimit:
              type: integer
              minimum: 1
              maximum: 100000
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    BurracoResponse:
      type: object
      description: Current state of the Burraco game
      properties:
        players:
          type: array
          description: Array of player objects
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              melds:
                type: array
                items:
                  type: object
                  properties:
                    cards:
                      type: array
                      items:
                        $ref: '#/components/schemas/Card'
                    isNatural:
                      type: boolean
                    isBurraco:
                      type: boolean
                    rank:
                      type: integer
              red3Count:
                type: integer
              red3s:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              roundScore:
                type: integer
              cumulativeScore:
                type: integer
              hasBurraco:
                type: boolean
              hasInitMeld:
                type: boolean
              tookPozzetto:
                type: boolean
                description: Whether the player has claimed their pozzetto reserve hand
        phase:
          type: integer
          description: "Game phase: 0 = Draw, 1 = Meld, 2 = Discard, 3 = RoundEnd, 4 = GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
          description: Current round number
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        discardTop:
          description: The top card of the discard pile
          $ref: '#/components/schemas/Card'
        drawPileCount:
          type: integer
          description: Number of cards remaining in the stock pile
        discardPileCount:
          type: integer
          description: Number of cards in the discard pile
        pozzettoCount:
          type: integer
          description: Number of pozzetto (reserve hand) piles still available
        isFrozen:
          type: boolean
          description: Whether the discard pile is frozen
        gameEndFlag:
          type: boolean
          description: Whether the match has ended
        winnerIdx:
          type: integer
          description: Index of the match winner (-1 if not ended)
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            pointLimit:
              type: integer
        message:
          type: string
          description: Game status message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Pinochle types
    # -------------------------------------------------------------------------
    PinochleRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `bid` (`b`),
            `pass` (`pa`), `trump` (`t`), `meld` (`m`), `play` (`p`),
            `next` (`n`), `nextround` (`nr`), `hint` (`h`), `log` (`l`).
          enum: [reset, r, bid, b, pass, pa, trump, t, meld, m, play, p, next, n, nextround, nr, hint, h, log, l]
          example: reset
        bidAmount:
          type: integer
          description: Bid amount.  Required for `bid` command.  Minimum 20.
          example: 25
        suit:
          type: integer
          description: Trump suit (1=Spade, 2=Club, 3=Heart, 4=Diamond).  Required for `trump` command.
          minimum: 1
          maximum: 4
          example: 3
        cardIndex:
          type: integer
          description: Index of the card to play.  Required for `play` command.
          example: 0
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            pointLimit:
              type: integer
              minimum: 1
              maximum: 10000
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    PinochleResponse:
      type: object
      description: Current state of the Pinochle game
      properties:
        players:
          type: array
          description: Array of player objects
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              team:
                type: integer
              trickCount:
                type: integer
              bid:
                type: integer
              hasPassed:
                type: boolean
              meldScore:
                type: integer
              trickPoints:
                type: integer
        phase:
          type: integer
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        bidPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        trumpSuit:
          type: integer
        highestBid:
          type: integer
        highestBidder:
          type: integer
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        teamScores:
          type: array
          items:
            type: integer
          minItems: 2
          maxItems: 2
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
        leadPlayerIdx:
          type: integer
        playerMelds:
          type: array
          items:
            type: array
            items:
              type: object
              properties:
                type:
                  type: integer
                points:
                  type: integer
                cards:
                  type: array
                  items:
                    $ref: '#/components/schemas/Card'
        validPlayIndices:
          type: array
          items:
            type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            pointLimit:
              type: integer

    # -------------------------------------------------------------------------
    # Piquet types
    # -------------------------------------------------------------------------
    PiquetConfig:
      type: object
      description: Piquet configuration (Web request payload)
      properties:
        cpuDifficulty:
          type: integer
          description: 0=Easy, 1=Normal, 2=Hard
        dealsPerPartie:
          type: integer
          minimum: 1
          maximum: 100
          description: Number of deals per partie (default 6)

    PiquetClaim:
      type: object
      description: Point/Sequence/Set claim evidence
      properties:
        length:
          type: integer
        topRank:
          type: integer
        pipTotal:
          type: integer
        suit:
          type: integer
        cards:
          type: array
          items:
            $ref: '#/components/schemas/Card'

    PiquetDeclaration:
      type: object
      description: Result of a single declaration comparison
      properties:
        kind:
          type: integer
          description: 0=Point, 1=Sequence, 2=Set
        elderClaim:
          allOf:
            - $ref: '#/components/schemas/PiquetClaim'
          nullable: true
        youngerClaim:
          allOf:
            - $ref: '#/components/schemas/PiquetClaim'
          nullable: true
        winner:
          type: integer
          description: 0/1 = player index, -1 = tied
        score:
          type: integer
        scoredBy:
          type: integer
        sets:
          type: array
          nullable: true
          items:
            $ref: '#/components/schemas/PiquetClaim'

    PiquetResponse:
      type: object
      description: Current state of the Piquet game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              trickCount:
                type: integer
              declScore:
                type: integer
              trickScore:
                type: integer
              bonusScore:
                type: integer
              roundScore:
                type: integer
              matchScore:
                type: integer
        phase:
          type: integer
          description: "0=Exchange, 1=Declaration, 2=Play, 3=Score, 4=GameEnd"
        dealNumber:
          type: integer
        dealsPerPartie:
          type: integer
        elderIdx:
          type: integer
        youngerIdx:
          type: integer
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        trickNumber:
          type: integer
        tricksWon:
          type: array
          items:
            type: integer
          minItems: 2
          maxItems: 2
        exchangeTurn:
          type: integer
          description: "0=Elder, 1=Younger, 2=Done"
        elderExchangedCnt:
          type: integer
        youngerExchangedCnt:
          type: integer
        elderTalon:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        youngerTalon:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        elderRevealedTalon:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        youngerRevealedTalon:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        carteBlanche:
          type: array
          items:
            type: boolean
          minItems: 2
          maxItems: 2
        declStage:
          type: integer
        declResults:
          type: array
          items:
            $ref: '#/components/schemas/PiquetDeclaration'
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        legalPlayIndices:
          type: array
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "-1 = undetermined or draw"
        hint:
          type: object
          nullable: true
          properties:
            cardIndex:
              type: integer
            discardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
        config:
          $ref: '#/components/schemas/PiquetConfig'

    # -------------------------------------------------------------------------
    # Spider Solitaire types
    # -------------------------------------------------------------------------
    SpiderRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          enum: [reset, deal, move, giveup, hint, autocomplete, undo, undo_n, log]
        n:
          type: integer
          description: Number of undos to perform (required when command is undo_n)
          example: 3
        sessionId:
          type: string
        from:
          $ref: '#/components/schemas/SpiderZone'
        to:
          $ref: '#/components/schemas/SpiderZone'
        config:
          type: object
          properties:
            difficulty:
              type: integer
              enum: [1, 2, 4]
              description: "1=1-suit, 2=2-suit, 4=4-suit"

    SpiderZone:
      type: object
      required:
        - zone
      properties:
        zone:
          type: string
          enum: [tableau]
        col:
          type: integer
        cardIndex:
          type: integer

    SpiderTableauCard:
      type: object
      required:
        - faceUp
      properties:
        card:
          $ref: '#/components/schemas/Card'
        faceUp:
          type: boolean

    SpiderHint:
      type: object
      properties:
        fromCol:
          type: integer
        cardIndex:
          type: integer
        toCol:
          type: integer

    SpiderResponse:
      type: object
      required:
        - tableau
        - stockCount
        - completedSuits
        - phase
        - moveCount
        - canUndo
        - isStalemate
        - undoToEscape
        - score
        - difficulty
        - message
      properties:
        tableau:
          type: array
          items:
            type: array
            items:
              $ref: '#/components/schemas/SpiderTableauCard'
        stockCount:
          type: integer
        completedSuits:
          type: integer
        phase:
          type: integer
          enum: [0, 1, 2]
          description: "0=Playing, 1=GameClear, 2=GameOver"
        moveCount:
          type: integer
        canUndo:
          type: boolean
        isStalemate:
          type: boolean
        undoToEscape:
          type: integer
          description: Number of undos needed to reach a non-stalemate state (0 if not stalemate, -1 if no escape)
          example: 0
        score:
          type: integer
        difficulty:
          type: integer
          enum: [1, 2, 4]
        hint:
          $ref: '#/components/schemas/SpiderHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    # -------------------------------------------------------------------------
    # Spiderette types
    # -------------------------------------------------------------------------
    SpideretteRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          enum: [reset, deal, move, giveup, hint, autocomplete, undo, undo_n, log]
        n:
          type: integer
          description: Number of undos to perform (required when command is undo_n)
          example: 3
        sessionId:
          type: string
        from:
          $ref: '#/components/schemas/SpideretteZone'
        to:
          $ref: '#/components/schemas/SpideretteZone'

    SpideretteZone:
      type: object
      required:
        - zone
      properties:
        zone:
          type: string
          enum: [tableau]
        col:
          type: integer
        cardIndex:
          type: integer

    SpideretteTableauCard:
      type: object
      required:
        - faceUp
      properties:
        card:
          $ref: '#/components/schemas/Card'
        faceUp:
          type: boolean

    SpideretteHint:
      type: object
      properties:
        fromCol:
          type: integer
        cardIndex:
          type: integer
        toCol:
          type: integer

    SpideretteResponse:
      type: object
      required:
        - tableau
        - stockCount
        - completedSuits
        - score
        - phase
        - moveCount
        - canUndo
        - isStalemate
        - message
      properties:
        tableau:
          type: array
          items:
            type: array
            items:
              $ref: '#/components/schemas/SpideretteTableauCard'
        stockCount:
          type: integer
        completedSuits:
          type: integer
          description: Number of suits completed (max 4)
        score:
          type: integer
        phase:
          type: integer
          enum: [0, 1, 2]
          description: "0=Playing, 1=GameClear, 2=GameOver"
        moveCount:
          type: integer
        canUndo:
          type: boolean
        isStalemate:
          type: boolean
        undoToEscape:
          type: integer
          description: Number of undos needed to reach a non-stalemate state (0 if not stalemate, -1 if no escape)
          example: 0
        hint:
          $ref: '#/components/schemas/SpideretteHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    # -------------------------------------------------------------------------
    # Napoleon
    # -------------------------------------------------------------------------
    NapoleonRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `bid` (`b`),
            `trump` (`t`), `exchange` (`e`), `play` (`p`), `next` (`n`),
            `nextround` (`nr`), `hint` (`h`), `log` (`l`),
            `setdifficulty` (`sd`), `setlimit` (`sl`), `setminbid` (`sm`).
          enum: [reset, r, bid, b, trump, t, exchange, e, play, p, next, n, nextround, nr, hint, h, log, l, setdifficulty, sd, setlimit, sl, setminbid, sm]
          example: reset
        bid:
          type: integer
          description: Bid value (number of picture cards, or 0 to pass).  Required for `bid` command.
          minimum: 0
          maximum: 17
          example: 12
        suit:
          type: integer
          description: "Trump suit (1=Spade, 2=Clover, 3=Heart, 4=Diamond).  Required for `trump` command."
          minimum: 1
          maximum: 4
          example: 1
        cardIndex:
          type: integer
          description: Index of the card to play.  Required for `play` command.
          example: 2
        cardIndices:
          type: array
          items:
            type: integer
          description: Indices of cards to exchange with kitty.  Required for `exchange` command.
          example: [0, 3]
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            pointLimit:
              type: integer
              minimum: 1
              maximum: 1000
            minBid:
              type: integer
              minimum: 1
              maximum: 17
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    NapoleonResponse:
      type: object
      description: Current state of the Napoleon game
      properties:
        players:
          type: array
          description: Array of player objects
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              bid:
                type: integer
              picCards:
                type: integer
                description: Number of picture cards captured
              cumulativeScore:
                type: integer
              isNapoleon:
                type: boolean
              isAdjutant:
                type: boolean
        phase:
          type: integer
          description: "Game phase: 0 = Bid, 1 = Trump, 2 = Exchange, 3 = Play, 4 = TrickEnd, 5 = RoundEnd, 6 = GameEnd"
          enum: [0, 1, 2, 3, 4, 5, 6]
          example: 0
        roundNumber:
          type: integer
          description: Current round number
        trickNumber:
          type: integer
          description: Current trick number within the round
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        bidPlayerIdx:
          type: integer
          description: Index of the player currently bidding
        napoleonIdx:
          type: integer
          description: Index of the Napoleon player (-1 if not yet decided)
        adjutantCard:
          $ref: '#/components/schemas/Card'
        trumpSuit:
          type: integer
          description: "Declared trump suit (1=Spade, 2=Clover, 3=Heart, 4=Diamond, 0=not yet declared)"
        currentTrick:
          type: array
          description: Cards played in the current trick
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        kitty:
          type: array
          description: Kitty cards (visible to Napoleon during exchange phase)
          items:
            $ref: '#/components/schemas/Card'
        napoleonBid:
          type: integer
          description: Napoleon's winning bid
        napoleonPicCards:
          type: integer
          description: Total picture cards captured by Napoleon's team
        gameEndFlag:
          type: boolean
          description: Whether the game has ended
        winnerIdx:
          type: integer
          description: Index of the winning player (-1 if not ended)
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            pointLimit:
              type: integer
            minBid:
              type: integer
        message:
          type: string
          description: Game status message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        hint:
          type: object
          nullable: true
          description: Recommended play hint (only present when hint command is used)
          properties:
            cardIndex:
              type: integer
              nullable: true
              description: Recommended card index
            reason:
              type: string
              description: Machine-readable reason key for the hint

    # -------------------------------------------------------------------------
    # Indian Poker
    # -------------------------------------------------------------------------
    IndianPokerRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `fold` (`f`),
            `check` (`ck`), `call` (`c`), `bet` (`b`), `raise` (`ra`),
            `allin` (`a`), `log` (`l`).
          enum: [reset, r, fold, f, check, ck, call, c, bet, b, raise, ra, allin, a, log, l]
          example: reset
        amount:
          type: integer
          description: Bet/raise amount.  Required for `bet` and `raise` commands.
          minimum: 1
          example: 50
        humanPlayMs:
          type: integer
          description: Time in milliseconds the human took to decide (used for Meta-AI hesitation tracking).
          example: 3000
        ante:
          type: integer
          description: Ante amount (only used with `reset`).
          minimum: 1
          example: 10
        bettingLimit:
          type: integer
          description: "Betting limit type (0=Fixed, 1=Pot Limit, 2=No Limit).  Only used with `reset`."
          minimum: 0
          maximum: 2
          example: 2
        cpuMetaAI:
          type: boolean
          description: "Whether CPU Meta-AI is enabled.  Only used with `reset`."
          example: true
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    IndianPokerResponse:
      type: object
      description: Current state of the Indian Poker game
      properties:
        players:
          type: array
          description: Array of player objects
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              card:
                $ref: '#/components/schemas/Card'
                nullable: true
                description: "Player's card (null for human during betting, visible for CPUs)"
              chips:
                type: integer
              currentBet:
                type: integer
              folded:
                type: boolean
              allIn:
                type: boolean
              playStyleName:
                type: string
        pot:
          type: integer
          description: Current pot size
        sidePots:
          type: array
          description: Side pots (when players are all-in with different amounts)
          items:
            type: object
            properties:
              amount:
                type: integer
              eligiblePlayers:
                type: array
                items:
                  type: integer
        dealerIdx:
          type: integer
          description: Index of the current dealer
        currentTurn:
          type: integer
          description: Index of the player whose turn it is
        phase:
          type: integer
          description: "Game phase: 0 = Init, 1 = Ante, 2 = Betting, 3 = Showdown, 4 = End"
          enum: [0, 1, 2, 3, 4]
          example: 0
        gameEndFlag:
          type: boolean
          description: Whether the game has ended
        lastBet:
          type: integer
          description: The last bet amount in the current round
        minRaise:
          type: integer
          description: Minimum raise amount
        raiseCount:
          type: integer
          description: Number of raises in the current round
        maxBetAmount:
          type: integer
          description: Maximum allowed bet amount (based on betting limit)
        bettingLimit:
          type: integer
          description: "Current betting limit (0=Fixed, 1=Pot Limit, 2=No Limit)"
        ante:
          type: integer
          description: Current ante amount
        handCount:
          type: integer
          description: Number of hands played
        roundResults:
          type: array
          description: Results from the showdown
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
              cardRank:
                type: integer
                description: "Card rank (2-14, Ace=14)"
              wonAmount:
                type: integer
        cpuActions:
          type: array
          description: CPU actions taken during the betting round
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              action:
                type: integer
              amount:
                type: integer
        message:
          type: string
          description: Game status message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    VideoPokerRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `bet` (`b`),
            `hold` (`h`), `log` (`l`).
          enum: [reset, r, bet, b, hold, h, log, l]
          example: reset
        amount:
          type: integer
          description: Bet amount (1-5 coins).  Required for `bet` command.
          minimum: 1
          maximum: 5
          example: 1
        indices:
          type: array
          description: Card positions to hold (0-4).  Required for `hold` command.
          items:
            type: integer
            minimum: 0
            maximum: 4
          example: [0, 2, 4]
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    VideoPokerResponse:
      type: object
      description: Current state of the Video Poker game
      properties:
        hand:
          type: array
          description: The player's 5-card hand
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 1 = Bet, 2 = Draw, 3 = Result"
          enum: [1, 2, 3]
          example: 1
        chips:
          type: integer
          description: Current chip count
          example: 100
        betAmount:
          type: integer
          description: Current bet amount (1-5 coins)
          example: 1
        result:
          type: integer
          description: "Round result: 1 = win, 0 = loss/push, -1 = not resolved"
          enum: [-1, 0, 1]
          example: 0
        payout:
          type: integer
          description: Payout amount for the current round
          example: 0
        handRank:
          type: integer
          description: "Hand rank code (0 = no qualifying hand, higher = better hand)"
          example: 0
        handName:
          type: string
          description: Human-readable name of the evaluated hand rank
          example: ""
        heldIndices:
          type: array
          description: Indices of held cards (0-4)
          items:
            type: integer
          example: []
        message:
          type: string
          description: Game status message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Euchre
    # -------------------------------------------------------------------------
    EuchreRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `orderup` (`o`),
            `orderupalone` (`oa`), `pass` (`pa`), `calltrump` (`c`),
            `calltrumpalone` (`ca`), `discard` (`d`), `play` (`p`),
            `next` (`n`), `nextround` (`nr`), `hint` (`h`), `log` (`l`).
          enum: [reset, r, orderup, o, orderupalone, oa, pass, pa, calltrump, c, calltrumpalone, ca, discard, d, play, p, next, n, nextround, nr, hint, h, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Index of the card to play or discard.  Required for `play` and `discard` commands.
          example: 2
        suit:
          type: integer
          description: "Trump suit to call (1=SPADE, 2=CLOVER, 3=HEART, 4=DIAMOND).  Required for `calltrump` and `calltrumpalone` commands."
          minimum: 1
          maximum: 4
          example: 3
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            pointLimit:
              type: integer
              minimum: 1
              maximum: 100
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    EuchreResponse:
      type: object
      description: Current state of the Euchre game
      properties:
        players:
          type: array
          description: Array of player objects
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              isDealer:
                type: boolean
              isAlone:
                type: boolean
              trickCount:
                type: integer
        teams:
          type: array
          description: Array of team objects (2 teams)
          items:
            type: object
            properties:
              score:
                type: integer
              roundScore:
                type: integer
              isMaker:
                type: boolean
        phase:
          type: integer
          description: "Game phase: 0 = PickUp, 1 = CallTrump, 2 = Discard, 3 = Play, 4 = TrickEnd, 5 = RoundEnd, 6 = GameEnd"
          enum: [0, 1, 2, 3, 4, 5, 6]
          example: 0
        roundNumber:
          type: integer
          description: Current round number
        trickNumber:
          type: integer
          description: Current trick number within the round
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        dealerIdx:
          type: integer
          description: Index of the dealer
        turnedUpCard:
          $ref: '#/components/schemas/Card'
        trumpSuit:
          type: integer
          description: "Trump suit (1=SPADE, 2=CLOVER, 3=HEART, 4=DIAMOND, 0=not yet decided)"
        currentTrick:
          type: array
          description: Cards played in the current trick
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        gameEndFlag:
          type: boolean
          description: Whether the game has ended
        winnerTeamIdx:
          type: integer
          description: Index of the winning team (-1 if not ended)
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            pointLimit:
              type: integer
        message:
          type: string
          description: Game status message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        hint:
          type: object
          nullable: true
          description: Recommended play hint (only present when hint command is used)
          properties:
            cardIndex:
              type: integer
              nullable: true
              description: Recommended card index
            reason:
              type: string
              description: Machine-readable reason key for the hint

    # -------------------------------------------------------------------------
    # Pyramid
    # -------------------------------------------------------------------------
    PyramidCard:
      type: object
      description: A card position in the Pyramid layout
      required:
        - zone
      properties:
        zone:
          type: string
          description: "Zone name: pyramid or waste"
          enum: [pyramid, waste]
          example: pyramid
        row:
          type: integer
          description: Row index (0-based, required for pyramid zone)
          example: 6
        col:
          type: integer
          description: Column index (0-based, required for pyramid zone)
          example: 0

    PyramidRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `draw` (`d`),
            `remove` (`rm`), `giveup` (`g`), `hint` (`h`), `undo` (`u`),
            `undo_n`, `log` (`l`).
          enum: [reset, r, draw, d, remove, rm, giveup, g, hint, h, undo, u, undo_n, log, l]
          example: remove
        card1:
          description: First card for a remove command
          $ref: '#/components/schemas/PyramidCard'
        card2:
          description: Second card for a remove command (pair removal)
          $ref: '#/components/schemas/PyramidCard'
        n:
          type: integer
          description: Number of undos to perform (required when command is undo_n)
          example: 3
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    PyramidOutputCard:
      type: object
      description: A card in the Pyramid layout with its state
      properties:
        card:
          description: Card details (null when removed)
          $ref: '#/components/schemas/Card'
        removed:
          type: boolean
          description: Whether the card has been removed
          example: false
        exposed:
          type: boolean
          description: Whether the card is exposed (can be selected)
          example: true

    PyramidResponse:
      type: object
      description: Current state of the Pyramid game
      required:
        - pyramid
        - stockCount
        - waste
        - phase
        - moveCount
        - canUndo
        - isStalemate
        - undoToEscape
        - message
      properties:
        pyramid:
          type: array
          description: The 7 pyramid rows, each containing an array of cards (row i has i+1 cards)
          items:
            type: array
            items:
              $ref: '#/components/schemas/PyramidOutputCard'
        stockCount:
          type: integer
          description: Number of cards remaining in the stock pile
          example: 24
        waste:
          type: array
          description: Cards in the waste pile (top card is the last element)
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 0 = Playing, 1 = GameClear, 2 = GameOver"
          enum: [0, 1, 2]
          example: 0
        moveCount:
          type: integer
          description: Total number of moves made in the current game
          example: 0
        canUndo:
          type: boolean
          description: Whether the undo action is available
          example: false
        isStalemate:
          type: boolean
          description: Whether the game is in a stalemate (no moves can lead to a win)
          example: false
        undoToEscape:
          type: integer
          description: Number of undos needed to reach a non-stalemate state (0 if not stalemate, -1 if no escape)
          example: 0
        message:
          type: string
          description: Game result or status message. Empty while the game is in progress.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        hint:
          type: object
          description: Hint for the next available move (only present when hint command is used)
          properties:
            type:
              type: string
              description: "Hint type: pair, king, waste_pair, waste_king"
              enum: [pair, king, waste_pair, waste_king]
            row1:
              type: integer
              description: Row of first card (-1 for waste operations)
            col1:
              type: integer
              description: Column of first card (-1 for waste operations)
            row2:
              type: integer
              description: Row of second card (-1 for king/waste operations)
            col2:
              type: integer
              description: Column of second card (-1 for king/waste operations)

    TriPeaksRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `draw` (`d`),
            `remove` (`rm`), `giveup` (`g`), `hint` (`h`), `undo` (`u`),
            `undo_n`, `log` (`l`).
          enum: [reset, r, draw, d, remove, rm, giveup, g, hint, h, undo, u, undo_n, log, l]
          example: remove
        row:
          type: integer
          description: Row index of the tableau card to remove (0-based)
          example: 3
        col:
          type: integer
          description: Column index of the tableau card to remove (0-based)
          example: 0
        n:
          type: integer
          description: Number of undos to perform (required when command is undo_n)
          example: 3
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    TriPeaksOutputCard:
      type: object
      description: A card in the TriPeaks tableau with its state
      properties:
        card:
          description: Card details (null when removed)
          $ref: '#/components/schemas/Card'
        removed:
          type: boolean
          description: Whether the card has been removed
          example: false
        exposed:
          type: boolean
          description: Whether the card is exposed (can be selected)
          example: true

    TriPeaksResponse:
      type: object
      description: Current state of the TriPeaks game
      required:
        - tableau
        - stockCount
        - waste
        - phase
        - moveCount
        - canUndo
        - isStalemate
        - undoToEscape
        - message
      properties:
        tableau:
          type: array
          description: The tableau rows, each containing an array of cards (three overlapping peaks)
          items:
            type: array
            items:
              $ref: '#/components/schemas/TriPeaksOutputCard'
        stockCount:
          type: integer
          description: Number of cards remaining in the stock pile
          example: 23
        waste:
          type: array
          description: Cards in the waste pile (top card is the last element)
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 0 = Playing, 1 = GameClear, 2 = GameOver"
          enum: [0, 1, 2]
          example: 0
        moveCount:
          type: integer
          description: Total number of moves made in the current game
          example: 0
        canUndo:
          type: boolean
          description: Whether the undo action is available
          example: false
        isStalemate:
          type: boolean
          description: Whether the game is in a stalemate (no moves can lead to a win)
          example: false
        undoToEscape:
          type: integer
          description: Number of undos needed to reach a non-stalemate state (0 if not stalemate, -1 if no escape)
          example: 0
        message:
          type: string
          description: Game result or status message. Empty while the game is in progress.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        hint:
          type: object
          description: Hint for the next available move (only present when hint command is used)
          properties:
            row:
              type: integer
              description: Row of the card to remove
            col:
              type: integer
              description: Column of the card to remove

    CribbageRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `discard` (`d`),
            `peg` (`p`), `go`, `shownext` (`sn`), `nextround` (`nr`),
            `setdifficulty` (`sd`), `setlimit` (`sl`), `log` (`l`).
          enum: [reset, r, discard, d, peg, p, go, shownext, sn, nextround, nr, setdifficulty, sd, setlimit, sl, log, l]
          example: discard
        cardIndex:
          type: integer
          description: Card index for peg command
          example: 0
        cardIndices:
          type: array
          items:
            type: integer
          description: Card indices for discard command (exactly 2 indices)
          example: [4, 5]
        config:
          type: object
          description: Game configuration (applied on reset)
          properties:
            cpuDifficulty:
              type: integer
              description: "CPU difficulty: 0 = Easy, 1 = Normal, 2 = Hard"
              enum: [0, 1, 2]
              example: 1
            pointLimit:
              type: integer
              description: Point limit to win (default 121)
              example: 121
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    CribbagePlayer:
      type: object
      description: A player in the Cribbage game
      required:
        - id
        - isHuman
        - cardCount
        - cards
        - roundScore
        - cumulativeScore
      properties:
        id:
          type: integer
          description: Player index (0 = human, 1 = CPU)
          example: 0
        isHuman:
          type: boolean
          description: Whether this player is human
          example: true
        cardCount:
          type: integer
          description: Number of cards in hand
          example: 6
        cards:
          type: array
          description: Player's hand cards (visible to human player; hidden for CPU)
          items:
            $ref: '#/components/schemas/Card'
        roundScore:
          type: integer
          description: Points scored in the current round
          example: 0
        cumulativeScore:
          type: integer
          description: Cumulative score across all rounds
          example: 0

    CribbageScoreDetail:
      type: object
      description: Breakdown of a hand/crib score
      properties:
        fifteens:
          type: integer
          description: Points from combinations summing to 15
          example: 4
        pairs:
          type: integer
          description: Points from pairs
          example: 2
        runs:
          type: integer
          description: Points from runs
          example: 3
        flush:
          type: integer
          description: Points from flush
          example: 0
        nobs:
          type: integer
          description: Points from nobs (Jack of starter suit)
          example: 1
        total:
          type: integer
          description: Total points for this hand/crib
          example: 10

    CribbageResponse:
      type: object
      description: Current state of the Cribbage game
      required:
        - players
        - phase
        - roundNumber
        - currentPlayerIdx
        - dealerIdx
        - crib
        - pegCount
        - pegPlayedCards
        - showPhaseStep
        - handScoreDetails
        - gameEndFlag
        - winnerIdx
        - message
      properties:
        players:
          type: array
          description: Array of 2 players (index 0 = human, index 1 = CPU)
          items:
            $ref: '#/components/schemas/CribbagePlayer'
        phase:
          type: integer
          description: "Game phase: 0 = Discard, 1 = Cut, 2 = Pegging, 3 = Show, 4 = RoundEnd, 5 = GameEnd"
          enum: [0, 1, 2, 3, 4, 5]
          example: 0
        roundNumber:
          type: integer
          description: Current round number
          example: 1
        currentPlayerIdx:
          type: integer
          description: Index of the current player
          example: 0
        dealerIdx:
          type: integer
          description: Index of the dealer for this round
          example: 1
        crib:
          type: array
          description: Cards in the crib (visible only during Show/RoundEnd/GameEnd phases)
          items:
            $ref: '#/components/schemas/Card'
        starter:
          description: The starter (cut) card (null before cut phase)
          $ref: '#/components/schemas/Card'
        pegCount:
          type: integer
          description: Current pegging count (0-31)
          example: 0
        pegPlayedCards:
          type: array
          description: Cards played in the current pegging sequence
          items:
            $ref: '#/components/schemas/Card'
        showPhaseStep:
          type: integer
          description: "Show phase step: 0 = non-dealer hand, 1 = dealer hand, 2 = crib"
          enum: [0, 1, 2]
          example: 0
        handScoreDetails:
          type: array
          description: Score details for show phase [non-dealer hand, dealer hand, crib]
          items:
            $ref: '#/components/schemas/CribbageScoreDetail'
        gameEndFlag:
          type: boolean
          description: Whether the game has ended
          example: false
        winnerIdx:
          type: integer
          description: Index of the winner (-1 if game not ended)
          example: -1
        message:
          type: string
          description: Game status or result message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        config:
          type: object
          description: Current game configuration
          properties:
            cpuDifficulty:
              type: integer
              description: "CPU difficulty: 0 = Easy, 1 = Normal, 2 = Hard"
              example: 1
            pointLimit:
              type: integer
              description: Point limit to win
              example: 121

    # -------------------------------------------------------------------------
    # Three Card Poker
    # -------------------------------------------------------------------------
    ThreeCardRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `bet` (`b`),
            `play` (`p`), `fold` (`f`), `log` (`l`).
          enum: [r, reset, b, bet, p, play, f, fold, log, l]
          example: bet
        amount:
          type: integer
          description: |
            Ante bet amount.  Required for `bet` command (minimum 10, must be a
            multiple of 10).  Ignored for other commands.
          minimum: 10
          example: 100
        pairPlusBet:
          type: integer
          description: |
            Pair Plus side bet amount.  Optional; used with the `bet`
            command.  0 or omitted means no side bet.
          minimum: 0
          example: 50
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    ThreeCardResponse:
      type: object
      description: Current state of the Three Card Poker game
      required:
        - playerHand
        - dealerHand
        - phase
        - chips
        - anteBet
        - pairPlusBet
        - playBet
        - result
        - antePayout
        - playPayout
        - anteBonusPayout
        - pairPlusPayout
        - totalPayout
        - dealerQualified
        - playerHandRank
        - dealerHandRank
        - message
      properties:
        playerHand:
          type: array
          description: Cards in the player's hand (3 cards)
          items:
            $ref: '#/components/schemas/Card'
        dealerHand:
          type: array
          description: Cards in the dealer's hand (3 cards; hidden until End phase)
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 1 = Bet, 2 = Action, 3 = End"
          enum: [1, 2, 3]
          example: 1
        chips:
          type: integer
          description: Current chip count
          example: 1000
        anteBet:
          type: integer
          description: Current Ante bet amount
          example: 100
        pairPlusBet:
          type: integer
          description: Current Pair Plus side bet amount
          example: 50
        playBet:
          type: integer
          description: Current Play bet amount (equal to Ante when played)
          example: 0
        result:
          type: integer
          description: "Game result: 1 = Player wins, -1 = Dealer wins, 0 = Push/not resolved"
          enum: [-1, 0, 1]
          example: 0
        antePayout:
          type: integer
          description: Payout from the Ante bet
          example: 0
        playPayout:
          type: integer
          description: Payout from the Play bet
          example: 0
        anteBonusPayout:
          type: integer
          description: Ante Bonus payout (Straight Flush 5:1, Three of a Kind 4:1, Straight 1:1)
          example: 0
        pairPlusPayout:
          type: integer
          description: Pair Plus side bet payout
          example: 0
        totalPayout:
          type: integer
          description: Total payout for the current round (sum of all payouts)
          example: 0
        dealerQualified:
          type: boolean
          description: Whether the dealer qualified (Q-high or better)
          example: false
        playerHandRank:
          type: integer
          description: Player's hand rank (1=High Card, 2=Pair, 3=Flush, 4=Straight, 5=Three of a Kind, 6=Straight Flush)
          example: 2
        dealerHandRank:
          type: integer
          description: Dealer's hand rank (1=High Card, 2=Pair, 3=Flush, 4=Straight, 5=Three of a Kind, 6=Straight Flush)
          example: 1
        message:
          type: string
          description: Game result or error message. Empty during bet/action phase.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Four Card Poker
    # -------------------------------------------------------------------------
    FourCardPokerRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `bet` (`b`),
            `play` (`p`), `fold` (`f`), `log` (`l`).
          enum: [r, reset, b, bet, p, play, f, fold, log, l]
          example: bet
        amount:
          type: integer
          description: Ante bet amount. Required for `bet` (minimum 10, multiples of 10).
          minimum: 10
          example: 100
        acesUpBet:
          type: integer
          description: Aces Up sidebet amount. Optional; used with `bet`. 0 / omitted = no sidebet.
          minimum: 0
          example: 50
        playMultiplier:
          type: integer
          description: Play bet multiplier of the Ante. Required for `play`; 1, 2, or 3. Defaults to 1.
          minimum: 1
          maximum: 3
          example: 2
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters).
          example: my-session-001

    FourCardPokerResponse:
      type: object
      description: Current state of the Four Card Poker game.
      required:
        - playerHand
        - dealerHand
        - playerBest
        - dealerBest
        - phase
        - chips
        - anteBet
        - acesUpBet
        - playBet
        - playMultiplier
        - result
        - antePayout
        - playPayout
        - anteBonusPayout
        - acesUpPayout
        - totalPayout
        - playerHandRank
        - dealerHandRank
        - message
      properties:
        playerHand:
          type: array
          description: Player's 5 cards.
          items:
            $ref: '#/components/schemas/Card'
        dealerHand:
          type: array
          description: Dealer's 6 cards. During the action phase only the upcard is exposed.
          items:
            $ref: '#/components/schemas/Card'
        playerBest:
          type: array
          description: Player's best 4-card subset (populated at End phase).
          items:
            $ref: '#/components/schemas/Card'
        dealerBest:
          type: array
          description: Dealer's best 4-card subset (populated at End phase).
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 1 = Bet, 2 = Action, 3 = End"
          enum: [1, 2, 3]
          example: 1
        chips:
          type: integer
          description: Current chip count.
          example: 1000
        anteBet:
          type: integer
          description: Ante bet amount.
          example: 100
        acesUpBet:
          type: integer
          description: Aces Up sidebet amount.
          example: 50
        playBet:
          type: integer
          description: Play bet amount (anteBet * playMultiplier when Play was chosen).
          example: 0
        playMultiplier:
          type: integer
          description: Play multiplier chosen by the player (1, 2, or 3).
          enum: [0, 1, 2, 3]
          example: 0
        result:
          type: integer
          description: "Game result: 1 = Player wins, -1 = Dealer wins, 0 = Push/not resolved"
          enum: [-1, 0, 1]
          example: 0
        antePayout:
          type: integer
          example: 0
        playPayout:
          type: integer
          example: 0
        anteBonusPayout:
          type: integer
          description: Automatic Ante Bonus payout (Three of a Kind 2:1, Straight Flush 20:1, Four of a Kind 25:1).
          example: 0
        acesUpPayout:
          type: integer
          description: Aces Up sidebet payout.
          example: 0
        totalPayout:
          type: integer
          example: 0
        playerHandRank:
          type: integer
          description: Player's 4-card hand rank (1=High Card, 2=Pair, 3=Two Pair, 4=Straight, 5=Flush, 6=Three of a Kind, 7=Straight Flush, 8=Four of a Kind).
          example: 0
        dealerHandRank:
          type: integer
          description: Dealer's 4-card hand rank (same scale as `playerHandRank`).
          example: 0
        message:
          type: string
          description: Game result or error message. Empty during bet/action phase.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Caribbean Stud Poker
    # -------------------------------------------------------------------------
    CaribbeanStudRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `bet` (`b`),
            `play` (`p`), `fold` (`f`), `log` (`l`).
          enum: [r, reset, b, bet, p, play, f, fold, log, l]
          example: bet
        amount:
          type: integer
          description: |
            Ante bet amount. Required for `bet` command (minimum 10, must be a
            multiple of 10). Ignored for other commands.
          minimum: 10
          example: 100
        jackpotBet:
          type: integer
          description: |
            Jackpot side bet amount. Optional; used with the `bet` command.
            0 or omitted means no side bet.
          minimum: 0
          example: 10
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    CaribbeanStudResponse:
      type: object
      description: Current state of the Caribbean Stud Poker game
      required:
        - playerHand
        - dealerHand
        - phase
        - chips
        - anteBet
        - jackpotBet
        - playBet
        - result
        - antePayout
        - playPayout
        - jackpotPayout
        - totalPayout
        - dealerQualified
        - playerHandRank
        - dealerHandRank
        - message
      properties:
        playerHand:
          type: array
          description: Cards in the player's hand (5 cards)
          items:
            $ref: '#/components/schemas/Card'
        dealerHand:
          type: array
          description: Cards in the dealer's hand (5 cards; hidden until End phase)
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 1 = Bet, 2 = Action, 3 = End"
          enum: [1, 2, 3]
          example: 1
        chips:
          type: integer
          description: Current chip count
          example: 1000
        anteBet:
          type: integer
          description: Current Ante bet amount
          example: 100
        jackpotBet:
          type: integer
          description: Current Jackpot side bet amount
          example: 10
        playBet:
          type: integer
          description: Current Play bet amount (2x Ante on Call)
          example: 0
        result:
          type: integer
          description: "Game result: 1 = Player wins, -1 = Dealer wins, 0 = Push/not resolved"
          enum: [-1, 0, 1]
          example: 0
        antePayout:
          type: integer
          description: Payout from the Ante bet
          example: 0
        playPayout:
          type: integer
          description: Payout from the Play bet
          example: 0
        jackpotPayout:
          type: integer
          description: Jackpot side bet payout
          example: 0
        totalPayout:
          type: integer
          description: Total payout for the current round (sum of all payouts)
          example: 0
        dealerQualified:
          type: boolean
          description: Whether the dealer qualified (Ace-King high or any pair)
          example: false
        playerHandRank:
          type: integer
          description: Player's 5-card hand rank (0=High Card, 1=One Pair, 2=Two Pair, 3=Three of a Kind, 4=Straight, 5=Flush, 6=Full House, 7=Four of a Kind, 8=Straight Flush, 9=Royal Flush)
          example: 1
        dealerHandRank:
          type: integer
          description: Dealer's 5-card hand rank (same encoding as `playerHandRank`)
          example: 1
        message:
          type: string
          description: Game result or error message. Empty during bet/action phase.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Texas Hold'em Bonus Poker
    # -------------------------------------------------------------------------
    TexasHoldemBonusRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `bet` (`b`),
            `play` (`p`), `fold` (`f`), `check` (`c`), `raise` (`ra`),
            `log` (`l`).
          enum: [r, reset, b, bet, p, play, f, fold, c, check, ra, raise, log, l]
          example: bet
        amount:
          type: integer
          description: |
            Ante bet amount. Required for `bet` command (minimum 10, must be a
            multiple of 10). Ignored for other commands.
          minimum: 10
          example: 100
        bonusBet:
          type: integer
          description: |
            Bonus side bet amount. Optional; used with the `bet` command.
            0 or omitted means no side bet.
          minimum: 0
          example: 10
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    TexasHoldemBonusResponse:
      type: object
      description: Current state of the Texas Hold'em Bonus Poker game
      required:
        - playerHand
        - dealerHand
        - community
        - phase
        - chips
        - anteBet
        - bonusBet
        - flopBet
        - turnBet
        - riverBet
        - totalPlayBet
        - result
        - antePayout
        - playPayout
        - bonusPayout
        - totalPayout
        - playerHandRank
        - dealerHandRank
        - message
      properties:
        playerHand:
          type: array
          description: Player's two hole cards
          items:
            $ref: '#/components/schemas/Card'
        dealerHand:
          type: array
          description: Dealer's two hole cards (masked until showdown)
          items:
            $ref: '#/components/schemas/Card'
        community:
          type: array
          description: Community cards (length grows from 0 to 5 as the streets advance)
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 1=Bet, 2=PreFlop, 3=Flop, 4=Turn, 5=End"
          enum: [1, 2, 3, 4, 5]
          example: 1
        chips:
          type: integer
          description: Current chip count
          example: 1000
        anteBet:
          type: integer
          description: Ante bet amount
          example: 100
        bonusBet:
          type: integer
          description: Bonus side bet amount
          example: 10
        flopBet:
          type: integer
          description: Flop bet amount (2x Ante placed pre-flop)
          example: 0
        turnBet:
          type: integer
          description: Turn bet amount (1x Ante placed after the flop)
          example: 0
        riverBet:
          type: integer
          description: River bet amount (1x Ante placed after the turn)
          example: 0
        totalPlayBet:
          type: integer
          description: Sum of flop / turn / river bets
          example: 0
        result:
          type: integer
          description: "Game result: 1=Player wins, -1=Dealer wins, 0=Push/not resolved"
          enum: [-1, 0, 1]
          example: 0
        antePayout:
          type: integer
          description: Payout from the Ante bet (includes Ante Bonus when applicable)
          example: 0
        playPayout:
          type: integer
          description: Combined payout from the flop / turn / river bets
          example: 0
        bonusPayout:
          type: integer
          description: Bonus side bet payout
          example: 0
        totalPayout:
          type: integer
          description: Total payout for the round (sum of all payouts)
          example: 0
        playerHandRank:
          type: integer
          description: Player's best 5-card hand rank (0=High Card .. 9=Royal Flush)
          example: 0
        dealerHandRank:
          type: integer
          description: Dealer's best 5-card hand rank (same encoding as `playerHandRank`)
          example: 0
        message:
          type: string
          description: Game result or error message. Empty during pre-resolution phases.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Casino Hold'em
    # -------------------------------------------------------------------------
    CasinoHoldemRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `bet` (`b`),
            `call` (`c`), `fold` (`f`), `log` (`l`).
          enum: [r, reset, b, bet, c, call, f, fold, log, l]
          example: bet
        amount:
          type: integer
          description: |
            Ante bet amount. Required for `bet` command (minimum 10, must be a
            multiple of 10). Ignored for other commands.
          minimum: 10
          example: 100
        bonusBet:
          type: integer
          description: |
            AA Bonus side bet amount. Optional; used with the `bet` command.
            0 or omitted means no side bet.
          minimum: 0
          example: 10
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    CasinoHoldemResponse:
      type: object
      description: Current state of the Casino Hold'em game
      required:
        - playerHand
        - dealerHand
        - community
        - phase
        - chips
        - anteBet
        - bonusBet
        - callBet
        - result
        - dealerQualify
        - antePayout
        - callPayout
        - bonusPayout
        - totalPayout
        - playerHandRank
        - dealerHandRank
        - message
      properties:
        playerHand:
          type: array
          description: Player's two hole cards
          items:
            $ref: '#/components/schemas/Card'
        dealerHand:
          type: array
          description: Dealer's two hole cards (masked until showdown after a Call)
          items:
            $ref: '#/components/schemas/Card'
        community:
          type: array
          description: Community cards (3 after the flop, 5 at showdown)
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 1=Bet, 2=Flop, 3=End"
          enum: [1, 2, 3]
          example: 1
        chips:
          type: integer
          description: Current chip count
          example: 1000
        anteBet:
          type: integer
          description: Ante bet amount
          example: 100
        bonusBet:
          type: integer
          description: AA Bonus side bet amount
          example: 10
        callBet:
          type: integer
          description: Call bet amount (2x Ante after a Call). 0 if folded.
          example: 0
        result:
          type: integer
          description: "Game result: 1=Player wins, -1=Dealer wins, 0=Push/not resolved"
          enum: [-1, 0, 1]
          example: 0
        dealerQualify:
          type: boolean
          description: Whether the dealer qualified (Pair of Fours or better)
          example: false
        antePayout:
          type: integer
          description: Payout from the Ante bet (per the Ante pay table)
          example: 0
        callPayout:
          type: integer
          description: Payout from the Call bet (1:1 on win, push on dealer no-qualify)
          example: 0
        bonusPayout:
          type: integer
          description: AA Bonus side bet payout
          example: 0
        totalPayout:
          type: integer
          description: Total payout for the round (sum of all payouts)
          example: 0
        playerHandRank:
          type: integer
          description: Player's best 5-card hand rank (0=High Card .. 9=Royal Flush)
          example: 0
        dealerHandRank:
          type: integer
          description: Dealer's best 5-card hand rank (same encoding as `playerHandRank`)
          example: 0
        message:
          type: string
          description: Game result or error message. Empty during pre-resolution phases.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Oh Hell
    # -------------------------------------------------------------------------
    OhHellRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `bid` (`b`),
            `play` (`p`), `next` (`n`), `nextround` (`nr`), `hint` (`h`),
            `log` (`l`).
          enum: [reset, r, bid, b, play, p, next, n, nextround, nr, hint, h, log, l]
          example: reset
        bid:
          type: integer
          description: Bid value (0 to current hand size).  Required for `bid` command.
          minimum: 0
          example: 2
        cardIndex:
          type: integer
          description: Index of the card to play.  Required for `play` command.
          example: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            maxHandSize:
              type: integer
              minimum: 1
              maximum: 13
            scoringVariant:
              type: integer
              minimum: 0
              maximum: 1
            roundDirection:
              type: integer
              minimum: 0
              maximum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    OhHellResponse:
      type: object
      description: Current state of the Oh Hell game
      properties:
        players:
          type: array
          description: Array of player objects
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              bid:
                type: integer
              roundScore:
                type: integer
              cumulativeScore:
                type: integer
              trickCount:
                type: integer
        phase:
          type: integer
          description: "Game phase: 0 = Bid, 1 = Play, 2 = TrickEnd, 3 = RoundEnd, 4 = GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
          description: Current round number
        totalRounds:
          type: integer
          description: Total number of rounds in the game
        handSize:
          type: integer
          description: Number of cards dealt to each player this round
        trickNumber:
          type: integer
          description: Current trick number within the round
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        bidPlayerIdx:
          type: integer
          description: Index of the player currently bidding
        dealerIdx:
          type: integer
          description: Index of the dealer for the current round
        currentTrick:
          type: array
          description: Cards played in the current trick
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        trumpCard:
          nullable: true
          description: The card determining the trump suit (null if no trump)
          allOf:
            - $ref: '#/components/schemas/Card'
        trumpSuit:
          type: integer
          description: "Trump suit, same enum as contractSuit (0=not determined, 1=Clubs, 2=Diamonds, 3=Hearts, 4=Spades, 5=NoTrump)"
        gameEndFlag:
          type: boolean
          description: Whether the game has ended
        winnerIdx:
          type: integer
          description: Index of the winning player (-1 if not ended)
        leadPlayerIdx:
          type: integer
          description: Index of the player who leads the current trick
        restrictedBid:
          type: integer
          description: The bid value the dealer cannot choose (-1 if no restriction)
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            maxHandSize:
              type: integer
            scoringVariant:
              type: integer
            roundDirection:
              type: integer
        message:
          type: string
          description: Game status message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        hint:
          type: object
          nullable: true
          description: Recommended play or bid hint (only present when hint command is used)
          properties:
            cardIndex:
              type: integer
              nullable: true
              description: Recommended card index (null during bid phase)
            bid:
              type: integer
              nullable: true
              description: Recommended bid value (null during play phase)
            reason:
              type: string
              description: Machine-readable reason key for the hint

    # -------------------------------------------------------------------------
    # Ninety-Nine
    # -------------------------------------------------------------------------
    NinetyNineRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `bid` (`b`),
            `play` (`p`), `next` (`n`), `nextround` (`nr`), `hint` (`h`),
            `log` (`l`).
          enum: [reset, r, bid, b, play, p, next, n, nextround, nr, hint, h, log, l]
          example: reset
        buryIndices:
          type: array
          description: Indices of the 3 cards to bury (encodes the bid).  Required for `bid` command.
          items:
            type: integer
          example: [0, 1, 2]
        cardIndex:
          type: integer
          description: Index of the card to play.  Required for `play` command.
          example: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetScore:
              type: integer
              minimum: 10
              maximum: 1000
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    NinetyNineResponse:
      type: object
      description: Current state of the Ninety-Nine game
      properties:
        players:
          type: array
          description: Array of player objects
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              bid:
                type: integer
                description: Declared trick count from buried suits (-1 if not yet declared)
              roundScore:
                type: integer
              cumulativeScore:
                type: integer
              trickCount:
                type: integer
              buriedCount:
                type: integer
                description: Number of cards this player has buried this deal
        phase:
          type: integer
          description: "Game phase: 0 = Bid, 1 = Play, 2 = TrickEnd, 3 = RoundEnd, 4 = GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        dealNumber:
          type: integer
          description: Current deal number
        targetScore:
          type: integer
          description: Cumulative score required to win the game
        handSize:
          type: integer
          description: Number of cards in hand during play (9)
        trickNumber:
          type: integer
          description: Current trick number within the deal
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        bidPlayerIdx:
          type: integer
          description: Index of the player currently bidding
        dealerIdx:
          type: integer
          description: Index of the dealer for the current deal
        currentTrick:
          type: array
          description: Cards played in the current trick
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        trumpSuit:
          type: integer
          description: "Trump suit for the deal (1=Spade, 2=Clover, 3=Heart, 4=Diamond)"
        gameEndFlag:
          type: boolean
          description: Whether the game has ended
        winnerIdx:
          type: integer
          description: Index of the winning player (-1 if not ended)
        leadPlayerIdx:
          type: integer
          description: Index of the player who leads the current trick
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetScore:
              type: integer
        message:
          type: string
          description: Game status message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        hint:
          type: object
          nullable: true
          description: Recommended play or bury hint (only present when hint command is used)
          properties:
            cardIndex:
              type: integer
              nullable: true
              description: Recommended card index (null during bid phase)
            buryIndices:
              type: array
              nullable: true
              description: Recommended 3 card indices to bury (null during play phase)
              items:
                type: integer
            reason:
              type: string
              description: Machine-readable reason key for the hint

    # -------------------------------------------------------------------------
    # Contract Bridge
    # -------------------------------------------------------------------------
    BridgeRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `bid` (`b`),
            `play` (`p`), `next` (`n`), `nextround` (`nr`), `hint` (`h`),
            `log` (`l`).
          enum: [reset, r, bid, b, play, p, next, n, nextround, nr, hint, h, log, l]
          example: reset
        bidType:
          type: integer
          description: |
            Bid type.  Required for `bid` command.
            0 = Pass, 1 = Normal bid (requires bidLevel + bidSuit),
            2 = Double, 3 = Redouble.
          minimum: 0
          maximum: 3
          example: 1
        bidLevel:
          type: integer
          description: "Bid level (1-7).  Required when bidType = 1."
          minimum: 1
          maximum: 7
          example: 1
        bidSuit:
          type: integer
          description: "Bid suit (1=Clubs, 2=Diamonds, 3=Hearts, 4=Spades, 5=NoTrump).  Required when bidType = 1."
          minimum: 1
          maximum: 5
          example: 5
        cardIndex:
          type: integer
          description: Index of the card to play.  Required for `play` command.
          example: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    BridgeResponse:
      type: object
      description: Current state of the Contract Bridge game
      properties:
        players:
          type: array
          description: Array of player objects
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              trickCount:
                type: integer
        teams:
          type: array
          description: Array of team objects (2 teams)
          items:
            type: object
            properties:
              belowLine:
                type: integer
                description: Points below the line (towards game)
              aboveLine:
                type: integer
                description: Points above the line (bonuses/penalties)
              gamesWon:
                type: integer
                description: Number of games won in the rubber
              vulnerable:
                type: boolean
                description: Whether the team is vulnerable
        phase:
          type: integer
          description: "Game phase: 0 = Bid, 1 = Play, 2 = TrickEnd, 3 = RoundEnd, 4 = GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
          description: Current round (deal) number
        trickNumber:
          type: integer
          description: Current trick number within the round
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        dealerIdx:
          type: integer
          description: Index of the dealer
        declarerIdx:
          type: integer
          description: Index of the declarer (-1 if not yet determined)
        dummyIdx:
          type: integer
          description: Index of the dummy (-1 if not yet determined)
        contractLevel:
          type: integer
          description: Contract level (1-7, 0 if not yet determined)
        contractSuit:
          type: integer
          description: "Contract suit (1=Clubs, 2=Diamonds, 3=Hearts, 4=Spades, 5=NoTrump, 0=not yet determined)"
        contractDoubled:
          type: integer
          description: "Double state (0=none, 1=doubled, 2=redoubled)"
        trumpSuit:
          type: integer
          description: "Trump suit (1=Spade, 2=Clover, 3=Heart, 4=Diamond, -1=NoTrump, 0=not yet determined)"
        currentTrick:
          type: array
          description: Cards played in the current trick
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        bidHistory:
          type: array
          description: Auction bid history
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              bidType:
                type: integer
              level:
                type: integer
              suit:
                type: integer
        gameEndFlag:
          type: boolean
          description: Whether the game (rubber) has ended
        winnerTeamIdx:
          type: integer
          description: Index of the winning team (-1 if not ended)
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
        message:
          type: string
          description: Game status message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        hint:
          type: object
          nullable: true
          description: Recommended play or bid hint (only present when hint command is used)
          properties:
            cardIndex:
              type: integer
              nullable: true
              description: Recommended card index (null during bid phase)
            bidType:
              type: integer
              nullable: true
              description: Recommended bid type (null during play phase)
            bidLevel:
              type: integer
              nullable: true
              description: Recommended bid level (null during play phase)
            bidSuit:
              type: integer
              nullable: true
              description: Recommended bid suit (null during play phase)
            reason:
              type: string
              description: Machine-readable reason key for the hint

    # -------------------------------------------------------------------------
    # Pineapple Poker
    # -------------------------------------------------------------------------
    PineappleRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `fold` (`f`),
            `check` (`ck`), `call` (`c`), `bet` (`b`), `raise` (`ra`),
            `allin` (`a`), `discard` (`d`), `rebuy` (`rb`), `skiprebuy` (`sr`),
            `addon` (`ad`), `skipaddon` (`sa`), `muck` (`m`),
            `show` (`sh`), `quit` (`q`).
          enum: [r, reset, f, fold, ck, check, c, call, b, bet, ra, raise, a, allin, d, discard, rb, rebuy, sr, skiprebuy, ad, addon, sa, skipaddon, m, muck, sh, show, log, q, quit]
          example: bet
        amount:
          type: integer
          description: |
            Bet/raise amount (for the `bet` and `raise` commands).
            Ignored for other commands.
          minimum: 1
          example: 40
        cardIdx:
          type: integer
          description: |
            Index of the hole card to discard (for the `discard` command).
            Must be 0, 1, or 2 (index into the 3 hole cards).
          minimum: 0
          maximum: 2
          example: 1
        smallBlind:
          type: integer
          description: |
            Small blind amount (for the `reset` command).
            Defaults to 5 when omitted.
          minimum: 1
          example: 5
        bigBlind:
          type: integer
          description: |
            Big blind amount (for the `reset` command).
            Defaults to 10 when omitted.
          minimum: 1
          example: 10
        tournamentMode:
          type: boolean
          description: |
            Enable tournament mode with blind escalation (for the `reset` command).
            Defaults to false when omitted.
          example: false
        blindLevelHands:
          type: integer
          description: |
            Number of hands before blinds increase in tournament mode.
            Defaults to 10.
          minimum: 1
          example: 10
        blindMultiplier:
          type: integer
          description: |
            Blind multiplier as a percentage when blinds increase.
            Defaults to 200 (= 2x).
          minimum: 101
          example: 200
        bettingLimit:
          type: integer
          description: |
            Betting limit type (used with `reset` command).
            0 = Fixed (default), 1 = Pot Limit, 2 = No Limit.
          minimum: 0
          maximum: 2
          example: 0
        tableSize:
          type: integer
          description: |
            Table size (used with `reset` command).
            4 = 4-max (default), 6 = 6-max, 9 = 9-max.
          enum: [4, 6, 9]
          example: 4
        rebuyEnabled:
          type: boolean
          description: |
            Enable rebuy (used with `reset` command).
            Defaults to false when omitted.
          example: false
        addonEnabled:
          type: boolean
          description: |
            Enable addon (used with `reset` command).
            Defaults to false when omitted.
          example: false
        isDiscardPhase:
          type: boolean
          description: |
            Indicates whether the client is in the discard phase.
            Used internally to track discard state.
          example: false
        discardDone:
          type: boolean
          description: |
            Indicates whether the discard has been completed.
            Used internally to track discard completion.
          example: false
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    PineappleResponse:
      type: object
      required:
        - players
        - communityCards
        - pot
        - sidePots
        - dealerIdx
        - currentTurn
        - phase
        - gameEndFlag
        - lastBet
        - minRaise
        - roundResults
        - cpuActions
        - message
      properties:
        players:
          type: array
          description: State of all players (4/6/9 depending on table size)
          items:
            $ref: '#/components/schemas/HoldemPlayer'
        communityCards:
          type: array
          description: Community cards on the board (0/3/4/5 cards based on phase)
          items:
            $ref: '#/components/schemas/Card'
        pot:
          type: integer
          description: Current main pot size
          minimum: 0
          example: 30
        sidePots:
          type: array
          description: Side pots (created when players go all-in with different amounts)
          items:
            $ref: '#/components/schemas/HoldemSidePot'
        dealerIdx:
          type: integer
          description: Index of the dealer for this round
          example: 0
        currentTurn:
          type: integer
          description: Index of the player whose turn it is
          example: 0
        phase:
          type: integer
          description: |
            Current game phase:
            - `0` — Init
            - `1` — PreFlop
            - `2` — Flop
            - `3` — Turn
            - `4` — River
            - `5` — Showdown
            - `6` — End
            - `7` — Rebuy/Addon
            - `8` — Discard
          enum: [0, 1, 2, 3, 4, 5, 6, 7, 8]
          example: 1
        gameEndFlag:
          type: boolean
          description: Whether the round has ended
          example: false
        lastBet:
          type: integer
          description: The current highest bet in this betting round
          minimum: 0
          example: 10
        minRaise:
          type: integer
          description: Minimum raise amount
          minimum: 0
          example: 20
        bettingLimit:
          type: integer
          description: |
            Betting limit type: 0 = Fixed, 1 = Pot Limit, 2 = No Limit.
          enum: [0, 1, 2]
        raiseCount:
          type: integer
          description: Number of raises in the current betting round.
          minimum: 0
        maxBetAmount:
          type: integer
          description: |
            Maximum allowed bet/raise amount for Pot Limit mode.
            0 means no limit (Fixed or No Limit mode).
          minimum: 0
        roundResults:
          type: array
          description: Results for each non-folded player at showdown
          items:
            $ref: '#/components/schemas/HoldemResult'
        cpuActions:
          type: array
          description: CPU actions that occurred since the last human action
          items:
            $ref: '#/components/schemas/HoldemCpuAction'
        isDiscardPhase:
          type: boolean
          description: Whether the game is currently in the discard phase (phase=8)
          example: false
        discardDone:
          type: boolean
          description: Whether the human player has completed discarding
          example: false
        message:
          type: string
          description: |
            Game result or status message.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        handCount:
          type: integer
          description: Number of hands played (for tournament mode display)
          minimum: 0
          example: 0
        smallBlind:
          type: integer
          description: Current small blind amount
          minimum: 1
          example: 5
        bigBlind:
          type: integer
          description: Current big blind amount
          minimum: 2
          example: 10
        tournamentMode:
          type: boolean
          description: Whether tournament mode (blind escalation) is active
          example: false
        tableSize:
          type: integer
          description: Current table size (4/6/9)
          enum: [4, 6, 9]
          example: 4
        muckAvailable:
          type: boolean
          description: Whether the human player can muck (hide) their hand at showdown
          example: false
        equity:
          $ref: '#/components/schemas/HoldemEquity'
        potOdds:
          type: number
          format: double
          description: Pot odds percentage (0-100).
          example: 33.33

    # -------------------------------------------------------------------------
    # Speed
    # -------------------------------------------------------------------------
    SpeedRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: Game command
          enum: [reset, r, play, p, flip, f, hint, h, log, l, quit, q]
          example: play
        cardIndex:
          type: integer
          description: Hand index of the card to play (required for `play` command)
          minimum: 0
          example: 2
        pileIndex:
          type: integer
          description: Center pile index (0 or 1) to play onto (required for `play` command)
          minimum: 0
          maximum: 1
          example: 0
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    SpeedResponse:
      type: object
      description: Current state of the Speed game
      required:
        - players
        - centerPiles
        - phase
        - gameEndFlag
        - winnerIdx
        - config
        - message
      properties:
        players:
          type: array
          description: Both players (index 0 = human, index 1 = CPU)
          items:
            type: object
            properties:
              id:
                type: integer
                description: Player index
              isHuman:
                type: boolean
                description: Whether this player is human
              cardCount:
                type: integer
                description: Number of cards in hand
              cards:
                type: array
                description: Cards in hand (only populated for human player)
                items:
                  $ref: '#/components/schemas/Card'
              drawPileSize:
                type: integer
                description: Number of cards remaining in the draw pile
        centerPiles:
          type: array
          description: The two center piles (top card of each)
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 0 = Play, 1 = Stuck, 2 = GameEnd"
          enum: [0, 1, 2]
          example: 0
        gameEndFlag:
          type: boolean
          description: Whether the game has ended
        winnerIdx:
          type: integer
          description: Index of the winning player (-1 if game is not over)
          example: -1
        cpuActions:
          type: array
          description: CPU play actions that occurred since the last human action
          items:
            type: object
            properties:
              cardIndex:
                type: integer
              pileIndex:
                type: integer
        hint:
          type: object
          description: Hint for the human player (present when hint command is used)
          properties:
            cardIndex:
              type: integer
            pileIndex:
              type: integer
            found:
              type: boolean
        config:
          type: object
          description: Current game configuration
          properties:
            cpuDifficulty:
              type: integer
              description: "CPU difficulty: 0 = Easy, 1 = Normal, 2 = Hard"
        message:
          type: string
          description: Game status or result message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Go Fish
    # -------------------------------------------------------------------------
    GoFishRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: Game command
          enum: [reset, r, ask, log, l]
          example: ask
        targetIdx:
          type: integer
          description: Target player index to ask (required for `ask` command)
          minimum: 0
          maximum: 3
          example: 1
        rank:
          type: integer
          description: Card rank to ask for (1=Ace, 2-10, 11=J, 12=Q, 13=K; required for `ask` command)
          minimum: 1
          maximum: 13
          example: 7
        config:
          type: object
          description: Game configuration (for `reset` command)
          properties:
            cpuDifficulty:
              type: integer
              description: CPU difficulty (0=Easy, 1=Normal, 2=Hard)
              minimum: 0
              maximum: 2
              example: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    GoFishResponse:
      type: object
      description: Current state of the Go Fish game
      required:
        - players
        - phase
        - gameEndFlag
        - winnerIdx
        - stockCount
        - config
        - message
      properties:
        players:
          type: array
          description: All 4 players (index 0 = human, 1-3 = CPU)
          items:
            type: object
            properties:
              id:
                type: integer
                description: Player index
              isHuman:
                type: boolean
                description: Whether this player is human
              cardCount:
                type: integer
                description: Number of cards in hand
              cards:
                type: array
                description: Cards in hand (only populated for human player)
                items:
                  $ref: '#/components/schemas/Card'
              bookCount:
                type: integer
                description: Number of completed books
              books:
                type: array
                description: Completed books (array of 4-card groups)
                items:
                  type: array
                  items:
                    $ref: '#/components/schemas/Card'
              isFinished:
                type: boolean
                description: Whether this player has finished (no cards and no more draws)
        phase:
          type: integer
          description: "Game phase: 0 = Play, 1 = GameEnd"
          enum: [0, 1]
          example: 0
        gameEndFlag:
          type: boolean
          description: Whether the game has ended
        winnerIdx:
          type: integer
          description: Index of the winning player (-1 if game is not over)
          example: -1
        currentTurn:
          type: integer
          description: Index of the player whose turn it is
          example: 0
        stockCount:
          type: integer
          description: Number of cards remaining in the stock pile
          example: 32
        humanAction:
          type: object
          description: The result of the human player's last ask action
          properties:
            askPlayerIdx:
              type: integer
            askTargetIdx:
              type: integer
            askRank:
              type: integer
            success:
              type: boolean
            cardsReceived:
              type: integer
            bookFormed:
              type: boolean
            bookRank:
              type: integer
        cpuActions:
          type: array
          description: CPU actions that occurred since the last human action
          items:
            type: object
            properties:
              askPlayerIdx:
                type: integer
              askTargetIdx:
                type: integer
              askRank:
                type: integer
              success:
                type: boolean
              cardsReceived:
                type: integer
              bookFormed:
                type: boolean
              bookRank:
                type: integer
        config:
          type: object
          description: Current game configuration
          properties:
            cpuDifficulty:
              type: integer
              description: "CPU difficulty: 0 = Easy, 1 = Normal, 2 = Hard"
            cpuMetaAI:
              type: boolean
              description: Whether Meta-AI is enabled
        message:
          type: string
          description: Game status or result message
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Seven Card Stud
    # -------------------------------------------------------------------------
    SevenCardStudRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `fold` (`f`),
            `check` (`ck`), `call` (`c`), `bet` (`b`), `raise` (`ra`),
            `allin` (`a`), `rebuy` (`rb`), `skiprebuy` (`sr`),
            `addon` (`ad`), `skipaddon` (`sa`), `muck` (`m`),
            `show` (`sh`), `log` (`l`), `quit` (`q`).
          enum: [r, reset, f, fold, ck, check, c, call, b, bet, ra, raise, a, allin, rb, rebuy, sr, skiprebuy, ad, addon, sa, skipaddon, m, muck, sh, show, log, l, q, quit]
          example: bet
        amount:
          type: integer
          description: |
            Bet/raise amount (for the `bet` and `raise` commands).
            Ignored for other commands.
          minimum: 1
          example: 40
        humanPlayMs:
          type: integer
          description: Human play hesitation time in milliseconds (0=not measured)
          example: 1200
        ante:
          type: integer
          description: |
            Ante amount (for the `reset` command).
            Defaults to 2 when omitted.
          minimum: 1
          example: 2
        bringIn:
          type: integer
          description: |
            Bring-in amount (for the `reset` command).
            Defaults to 5 when omitted.
          minimum: 1
          example: 5
        smallBet:
          type: integer
          description: |
            Small bet amount used on Third/Fourth Street (for the `reset` command).
            Defaults to 10 when omitted.
          minimum: 1
          example: 10
        bigBet:
          type: integer
          description: |
            Big bet amount used on Fifth/Sixth/Seventh Street (for the `reset` command).
            Defaults to 20 when omitted.
          minimum: 1
          example: 20
        bettingLimit:
          type: integer
          description: |
            Betting limit type (used with `reset` command).
            0 = Fixed (default), 1 = Pot Limit, 2 = No Limit.
          minimum: 0
          maximum: 2
          example: 0
        tableSize:
          type: integer
          description: |
            Table size (used with `reset` command).
            2 through 7 players. Defaults to 4.
          minimum: 2
          maximum: 7
          example: 4
        tournamentMode:
          type: boolean
          description: |
            Enable tournament mode with ante escalation (for the `reset` command).
            Defaults to false when omitted.
          example: false
        anteLevelHands:
          type: integer
          description: |
            Number of hands before ante increases in tournament mode (for the `reset` command).
            Defaults to 10 when omitted. Must be >= 1.
          minimum: 1
          example: 10
        anteMultiplier:
          type: integer
          description: |
            Ante multiplier as a percentage when ante increases (for the `reset` command).
            Defaults to 200 (= 2x). Must be >= 101.
          minimum: 101
          example: 200
        rebuyEnabled:
          type: boolean
          description: |
            Enable rebuy (used with `reset` command).
            Defaults to false when omitted.
          example: false
        rebuyMaxCount:
          type: integer
          description: |
            Maximum number of rebuys per player (used with `reset` command).
            Defaults to 3 when omitted.
          minimum: 1
          example: 3
        rebuyChips:
          type: integer
          description: |
            Chips granted per rebuy (used with `reset` command).
            Defaults to 1000 when omitted.
          minimum: 1
          example: 1000
        rebuyPeriodHands:
          type: integer
          description: |
            Number of hands during which rebuy is available (used with `reset` command).
            Defaults to 10 when omitted.
          minimum: 1
          example: 10
        addonEnabled:
          type: boolean
          description: |
            Enable addon (used with `reset` command).
            Defaults to false when omitted.
          example: false
        addonChips:
          type: integer
          description: |
            Chips granted by addon (used with `reset` command).
            Defaults to 1500 when omitted.
          minimum: 1
          example: 1500
        addonAfterHand:
          type: integer
          description: |
            Hand number after which addon becomes available (used with `reset` command).
            Defaults to 10 when omitted.
          minimum: 1
          example: 10
        cpuMetaAI:
          type: boolean
          description: |
            When true, CPUs adaptively adjust bluff and call behavior based on
            session-scoped game history (Meta-AI). Defaults to false.
          default: false
          example: false
        profile:
          type: object
          description: Human player profile for Meta-AI (sent back by the client to persist across sessions)
        sessionId:
          type: string
          description: Unique session identifier (max 256 characters)
          maxLength: 256
          example: my-session-001

    SevenCardStudPlayer:
      type: object
      required:
        - id
        - isHuman
        - holeCards
        - doorCards
        - chips
        - currentBet
        - folded
        - allIn
        - handRank
        - handName
        - bestHand
        - playStyleName
      properties:
        id:
          type: integer
          description: Player index (0 = human, 1+ = CPU)
          example: 0
        isHuman:
          type: boolean
          description: Whether this is the human player
          example: true
        holeCards:
          type: array
          description: |
            Face-down hole cards (2 initially, 3 after Seventh Street).
            Human player's cards are always visible.
            CPU cards are hidden (empty array) until showdown.
          items:
            $ref: '#/components/schemas/Card'
        doorCards:
          type: array
          description: |
            Face-up door cards (1 on Third Street, up to 4 by Sixth Street).
            Visible to all players.
          items:
            $ref: '#/components/schemas/Card'
        chips:
          type: integer
          description: Current chip count
          minimum: 0
          example: 990
        currentBet:
          type: integer
          description: Current round bet amount
          minimum: 0
          example: 10
        folded:
          type: boolean
          description: Whether the player has folded
          example: false
        allIn:
          type: boolean
          description: Whether the player is all-in
          example: false
        handRank:
          type: integer
          description: |
            Numeric hand rank (only populated at showdown for non-folded players):
            0 = High Card, 1 = One Pair, 2 = Two Pair, 3 = Three of a Kind,
            4 = Straight, 5 = Flush, 6 = Full House, 7 = Four of a Kind,
            8 = Straight Flush, 9 = Royal Flush.
          minimum: 0
          maximum: 9
          example: 0
        handName:
          type: string
          description: Human-readable hand name (empty until showdown)
          example: ""
        bestHand:
          type: array
          description: Best 5-card hand (only populated at showdown)
          items:
            $ref: '#/components/schemas/Card'
        lowBestHand:
          type: array
          description: |
            Omaha Hi-Lo only. Best 5-card low hand when qualified at
            showdown (5 distinct ranks all ≤ 8, Ace = 1). Absent for
            non-Hi-Lo games or unqualified players.
          items:
            $ref: '#/components/schemas/Card'
        lowQualifies:
          type: boolean
          description: |
            Omaha Hi-Lo only. True when the player has a qualifying low
            hand at showdown.
        playStyleName:
          type: string
          description: |
            CPU play style name: TAG (Tight-Aggressive), LAP (Loose-Passive),
            TAP (Tight-Passive), LAG (Loose-Aggressive), GTO (Game Theory Optimal).
            Empty for human player.
          example: TAG
        totalHands:
          type: integer
          description: Total number of hands played (for HUD stats)
          minimum: 0
          example: 10
        vpip:
          type: integer
          description: VPIP percentage (Voluntarily Put $ In Pot)
          minimum: 0
          maximum: 100
          example: 50
        pfr:
          type: integer
          description: PFR percentage (Pre-Flop Raise)
          minimum: 0
          maximum: 100
          example: 25
        threeBet:
          type: integer
          description: 3Bet percentage
          minimum: 0
          maximum: 100
          example: 10
        af:
          type: string
          description: "Aggression Factor (postflop bets+raises / calls). Values: decimal (e.g. \"2.5\"), \"inf\" (bets but no calls), \"-\" (no actions)"
          example: "2.5"

    SevenCardStudCpuAction:
      type: object
      description: Record of a CPU player's betting action
      required:
        - playerIdx
        - action
        - amount
      properties:
        playerIdx:
          type: integer
          description: Index of the CPU player who acted
          example: 1
        action:
          type: integer
          description: |
            Action taken: 0 = Fold, 1 = Check, 2 = Call,
            3 = Bet, 4 = Raise, 5 = All-in.
          enum: [0, 1, 2, 3, 4, 5]
          example: 2
        amount:
          type: integer
          description: Amount bet/raised (0 for fold/check/call/all-in)
          example: 0

    SevenCardStudResult:
      type: object
      description: Result for a player at showdown
      required:
        - playerIdx
        - handRank
        - handName
        - bestHand
        - wonAmount
      properties:
        playerIdx:
          type: integer
          description: Index of the player
          example: 0
        handRank:
          type: integer
          description: Numeric hand rank
          example: 1
        handName:
          type: string
          description: Hand name (e.g., "One Pair")
          example: One Pair
        kickers:
          type: string
          description: Kicker card values formatted as display string
        bestHand:
          type: array
          description: Best 5-card hand
          items:
            $ref: '#/components/schemas/Card'
        wonAmount:
          type: integer
        lowQualifies:
          type: boolean
          description: |
            Hi-Lo only. Whether this seat made a qualifying eight-or-better low.
        lowBestHand:
          type: array
          description: Hi-Lo only. The five cards forming the low.
          items:
            $ref: '#/components/schemas/Card'
        wonLow:
          type: integer
          description: |
            Hi-Lo only. Chips won as the low half. `wonAmount` is the high and
            low winnings combined.
          description: Chips won from the pot
          example: 40
        mucked:
          type: boolean
          description: Whether the player mucked (hid) their hand at showdown
          example: false

    SevenCardStudSidePot:
      type: object
      description: A side pot created when a player goes all-in
      required:
        - amount
        - eligiblePlayers
      properties:
        amount:
          type: integer
          description: Total chips in this side pot
          example: 100
        eligiblePlayers:
          type: array
          description: Indices of players eligible to win this pot
          items:
            type: integer
          example: [0, 1, 2]

    SevenCardStudResponse:
      type: object
      required:
        - players
        - pot
        - sidePots
        - dealerIdx
        - currentTurn
        - phase
        - gameEndFlag
        - lastBet
        - minRaise
        - roundResults
        - cpuActions
        - message
      properties:
        players:
          type: array
          description: State of all players (2-7 depending on table size)
          items:
            $ref: '#/components/schemas/SevenCardStudPlayer'
        communityCard:
          nullable: true
          description: Used as a shared community card only when the deck runs out of cards, otherwise null
        pot:
          type: integer
          description: Current main pot size
          minimum: 0
          example: 30
        sidePots:
          type: array
          description: Side pots (created when players go all-in with different amounts)
          items:
            $ref: '#/components/schemas/SevenCardStudSidePot'
        dealerIdx:
          type: integer
          description: Index of the dealer for this round
          example: 0
        currentTurn:
          type: integer
          description: Index of the player whose turn it is
          example: 0
        phase:
          type: integer
          description: |
            Current game phase:
            - `0` — Init
            - `1` — Third Street
            - `2` — Fourth Street
            - `3` — Fifth Street
            - `4` — Sixth Street
            - `5` — Seventh Street
            - `6` — Showdown
            - `7` — End
            - `8` — Rebuy/Addon
          enum: [0, 1, 2, 3, 4, 5, 6, 7, 8]
          example: 1
        isHiLo:
          type: boolean
          description: |
            Whether this session is the Hi-Lo (8 or Better) split. Sent so the
            client renders the low column without inferring the variant from the
            route.
        gameEndFlag:
          type: boolean
          description: Whether the round has ended
          example: false
        lastBet:
          type: integer
          description: The current highest bet in this betting round
          minimum: 0
          example: 10
        minRaise:
          type: integer
          description: Minimum raise amount
          minimum: 0
          example: 20
        bettingLimit:
          type: integer
          description: |
            Betting limit type: 0 = Fixed, 1 = Pot Limit, 2 = No Limit.
          enum: [0, 1, 2]
        raiseCount:
          type: integer
          description: Number of raises in the current betting round.
          minimum: 0
        maxBetAmount:
          type: integer
          description: |
            Maximum allowed bet/raise amount for Pot Limit mode.
            0 means no limit (Fixed or No Limit mode).
          minimum: 0
        roundResults:
          type: array
          description: Results for each non-folded player at showdown
          items:
            $ref: '#/components/schemas/SevenCardStudResult'
        cpuActions:
          type: array
          description: CPU actions that occurred since the last human action
          items:
            $ref: '#/components/schemas/SevenCardStudCpuAction'
        message:
          type: string
          description: |
            Game result or status message.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        handCount:
          type: integer
          description: Number of hands played (for tournament mode display)
          minimum: 0
          example: 0
        ante:
          type: integer
          description: Current ante amount
          minimum: 1
          example: 2
        bringIn:
          type: integer
          description: Current bring-in amount
          minimum: 1
          example: 5
        smallBet:
          type: integer
          description: Small bet amount (Third/Fourth Street)
          minimum: 1
          example: 10
        bigBet:
          type: integer
          description: Big bet amount (Fifth/Sixth/Seventh Street)
          minimum: 1
          example: 20
        tournamentMode:
          type: boolean
          description: Whether tournament mode (ante escalation) is active
          example: false
        anteLevelHands:
          type: integer
          description: Number of hands before ante increases
          minimum: 1
          example: 10
        anteMultiplier:
          type: integer
          description: Ante multiplier percentage (e.g. 200 = 2x)
          minimum: 101
          example: 200
        tableSize:
          type: integer
          description: Current table size (2-7)
          minimum: 2
          maximum: 7
          example: 4
        bringInPlayerIdx:
          type: integer
          description: Index of the player required to post the bring-in (lowest door card)
          example: 2
        rebuyAvailable:
          type: boolean
          description: Whether rebuy is available in the current phase
          example: false
        addonAvailable:
          type: boolean
          description: Whether addon is available in the current phase
          example: false
        rebuyPhaseType:
          type: integer
          description: |
            Type of rebuy phase: 1 = rebuy, 2 = addon.
            Only present when phase is 8 (Rebuy/Addon).
          enum: [1, 2]
        rebuyChips:
          type: integer
          description: Chips available for current rebuy
          minimum: 0
        rebuyMaxCount:
          type: integer
          description: Maximum rebuy count setting
          minimum: 0
        rebuyCounts:
          type: array
          description: Rebuy count per player (same order as players array)
          items:
            type: integer
            minimum: 0
        addonChips:
          type: integer
          description: Chips available for addon
          minimum: 0
        muckAvailable:
          type: boolean
          description: Whether the human player can muck (hide) their hand at showdown

    # -------------------------------------------------------------------------
    # Pai Gow Poker
    # -------------------------------------------------------------------------
    PaiGowRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command.  Accepted values: `reset` (`r`), `bet` (`b`),
            `set` (`s`), `log` (`l`).
          enum: [r, reset, b, bet, s, set, log, l]
          example: bet
        amount:
          type: integer
          description: |
            Bet amount.  Required for `bet` command (minimum 10, must be a
            multiple of 10).  Ignored for other commands.
          minimum: 10
          example: 100
        low0:
          type: integer
          description: |
            Index of the first card to place in the low hand (0-6).
            Required for the `set` command.
          minimum: 0
          maximum: 6
          example: 2
        low1:
          type: integer
          description: |
            Index of the second card to place in the low hand (0-6).
            Required for the `set` command.
          minimum: 0
          maximum: 6
          example: 5
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    PaiGowResponse:
      type: object
      description: Current state of the Pai Gow Poker game
      required:
        - playerCards
        - dealerCards
        - playerHighHand
        - playerLowHand
        - dealerHighHand
        - dealerLowHand
        - phase
        - chips
        - bet
        - result
        - highHandResult
        - lowHandResult
        - payout
        - commission
        - playerHighRank
        - playerLowRank
        - dealerHighRank
        - dealerLowRank
        - message
      properties:
        playerCards:
          type: array
          description: Player's 7 dealt cards
          items:
            $ref: '#/components/schemas/Card'
        dealerCards:
          type: array
          description: Dealer's 7 dealt cards (hidden until End phase)
          items:
            $ref: '#/components/schemas/Card'
        playerHighHand:
          type: array
          description: Player's 5-card high hand
          items:
            $ref: '#/components/schemas/Card'
        playerLowHand:
          type: array
          description: Player's 2-card low hand
          items:
            $ref: '#/components/schemas/Card'
        dealerHighHand:
          type: array
          description: Dealer's 5-card high hand
          items:
            $ref: '#/components/schemas/Card'
        dealerLowHand:
          type: array
          description: Dealer's 2-card low hand
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 1 = Bet, 2 = Set, 3 = End"
          enum: [1, 2, 3]
          example: 1
        chips:
          type: integer
          description: Current chip count
          example: 1000
        bet:
          type: integer
          description: Current bet amount
          example: 100
        result:
          type: integer
          description: "Overall game result: 1 = Player wins, -1 = Dealer wins, 0 = Push/not resolved"
          enum: [-1, 0, 1]
          example: 0
        highHandResult:
          type: integer
          description: "High hand comparison result: 1 = Player wins, -1 = Dealer wins, 0 = Tie"
          enum: [-1, 0, 1]
          example: 0
        lowHandResult:
          type: integer
          description: "Low hand comparison result: 1 = Player wins, -1 = Dealer wins, 0 = Tie"
          enum: [-1, 0, 1]
          example: 0
        payout:
          type: integer
          description: Payout for the current round (after commission)
          example: 0
        commission:
          type: integer
          description: Commission amount (5% of bet on player win)
          example: 0
        playerHighRank:
          type: string
          description: Player's high hand rank name (e.g. "Flush", "Two Pair")
          example: ""
        playerLowRank:
          type: string
          description: Player's low hand rank name (e.g. "Pair", "High Card")
          example: ""
        dealerHighRank:
          type: string
          description: Dealer's high hand rank name
          example: ""
        dealerLowRank:
          type: string
          description: Dealer's low hand rank name
          example: ""
        message:
          type: string
          description: Game result or error message. Empty during bet/set phase.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Let It Ride
    # -------------------------------------------------------------------------
    LetItRideRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `bet` (`b`),
            `pull` (`p`), `letitride` (`l`), `log` (`lg`).
          enum: [r, reset, b, bet, p, pull, l, letitride, log, lg]
          example: bet
        amount:
          type: integer
          description: |
            Bet amount per spot. Required for `bet` command (minimum 10, must be
            a multiple of 10). The same amount is placed on all 3 spots.
            Ignored for other commands.
          minimum: 10
          example: 100
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    LetItRideResponse:
      type: object
      description: Current state of the Let It Ride game
      required:
        - playerHand
        - communityCards
        - phase
        - chips
        - betAmount
        - bet1Active
        - bet2Active
        - bet3Active
        - result
        - handRank
        - bet1Payout
        - bet2Payout
        - bet3Payout
        - totalPayout
        - message
      properties:
        playerHand:
          type: array
          description: Cards in the player's hand (3 cards)
          items:
            $ref: '#/components/schemas/Card'
        communityCards:
          type: array
          description: Community cards (2 cards; revealed progressively)
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 1 = Bet, 2 = Decision1, 3 = Decision2, 4 = End"
          enum: [1, 2, 3, 4]
          example: 1
        chips:
          type: integer
          description: Current chip count
          example: 1000
        betAmount:
          type: integer
          description: Bet amount per spot
          example: 100
        bet1Active:
          type: boolean
          description: Whether Bet 1 is still active (can be pulled in the first decision phase)
          example: true
        bet2Active:
          type: boolean
          description: Whether Bet 2 is still active (can be pulled in the second decision phase)
          example: true
        bet3Active:
          type: boolean
          description: Whether Bet 3 is still active (always active, cannot be pulled)
          example: true
        result:
          type: integer
          description: "Game result: 1 = Player wins, -1 = Player loses, 0 = Not resolved"
          enum: [-1, 0, 1]
          example: 0
        handRank:
          type: integer
          description: Player's 5-card hand rank (0=High Card, 1=One Pair, 2=Two Pair, 3=Three of a Kind, 4=Straight, 5=Flush, 6=Full House, 7=Four of a Kind, 8=Straight Flush, 9=Royal Flush)
          example: 0
        bet1Payout:
          type: integer
          description: Payout from Bet 1 (0 if pulled)
          example: 0
        bet2Payout:
          type: integer
          description: Payout from Bet 2 (0 if pulled)
          example: 0
        bet3Payout:
          type: integer
          description: Payout from Bet 3
          example: 0
        totalPayout:
          type: integer
          description: Total payout for the current round (sum of all active bet payouts)
          example: 0
        message:
          type: string
          description: Game result or error message. Empty during bet/decision phase.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Poker Squares
    # -------------------------------------------------------------------------
    PokerSquaresRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `place` (`p`),
            `undo` (`u`), `giveup` (`g`), `log` (`l`), `hint` (`h`).
          enum: [r, reset, p, place, u, undo, g, giveup, log, l, h, hint]
          example: place
        row:
          type: integer
          description: Row index (0-4) for the `place` command.
          minimum: 0
          maximum: 4
          example: 2
        col:
          type: integer
          description: Column index (0-4) for the `place` command.
          minimum: 0
          maximum: 4
          example: 3
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    PokerSquaresOutputCard:
      type: object
      description: A single cell in the 5x5 Poker Squares board (empty when `card` is null).
      properties:
        card:
          description: Card placed on this cell, or null if the cell is empty.
          $ref: '#/components/schemas/Card'

    PokerSquaresResponse:
      type: object
      description: Current state of the Poker Squares game
      required:
        - board
        - placedCount
        - phase
        - canUndo
        - rowScores
        - colScores
        - totalScore
        - message
      properties:
        board:
          type: array
          description: 5x5 grid of cells (row-major). Each cell contains the placed card or null.
          items:
            type: array
            items:
              $ref: '#/components/schemas/PokerSquaresOutputCard'
        currentCard:
          description: The current card that must be placed next. Omitted when the game is complete.
          $ref: '#/components/schemas/Card'
        placedCount:
          type: integer
          description: Number of cards already placed on the board (0-25).
          minimum: 0
          maximum: 25
          example: 0
        phase:
          type: integer
          description: "Game phase: 0 = Playing, 1 = Complete"
          enum: [0, 1]
          example: 0
        canUndo:
          type: boolean
          description: Whether the undo action is available
          example: false
        rowScores:
          type: array
          description: Score of each row (5 entries, one per row). Only rows with all 5 cards placed contribute to the total.
          items:
            type: integer
          example: [0, 0, 0, 0, 0]
        colScores:
          type: array
          description: Score of each column (5 entries, one per column). Only fully-filled columns contribute to the total.
          items:
            type: integer
          example: [0, 0, 0, 0, 0]
        totalScore:
          type: integer
          description: Sum of all 10 row + column hand scores (American scoring system).
          example: 0
        message:
          type: string
          description: Game result or status message. Empty while the game is in progress.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
        hint:
          description: |
            Server-side synergy-aware suggestion for the current card. Omitted when
            no suggestion is available (game complete, or no current card).
          $ref: '#/components/schemas/PokerSquaresOutputHint'

    PokerSquaresOutputHint:
      type: object
      description: Suggested cell for the current card, scored by row/column synergy.
      required:
        - row
        - col
        - score
        - synergy
      properties:
        row:
          type: integer
          description: Row index (0-4) of the suggested cell.
          minimum: 0
          maximum: 4
          example: 2
        col:
          type: integer
          description: Column index (0-4) of the suggested cell.
          minimum: 0
          maximum: 4
          example: 3
        score:
          type: integer
          description: Combined row + column score the placement would create.
          example: 8
        synergy:
          type: boolean
          description: Whether the score is positive, i.e. the placement builds on cards already on the board.
          example: true

    # -------------------------------------------------------------------------
    # Red Dog (レッドドッグ)
    # -------------------------------------------------------------------------
    RedDogRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          enum: [reset, r, bet, b, raise, stay, s, log]
          description: Game command to execute.
        amount:
          type: integer
          description: Bet or raise amount (required for `bet` and `raise` commands).
          example: 100
        sessionId:
          type: string
          description: Session identifier for game state persistence.

    RedDogResponse:
      type: object
      properties:
        initialCards:
          type: array
          description: The initial 2 dealt cards.
          items:
            $ref: '#/components/schemas/Card'
        thirdCard:
          description: Third card (present after raise/stay or pair resolution).
          oneOf:
            - $ref: '#/components/schemas/Card'
            - type: "null"
        phase:
          type: integer
          description: "Current game phase: 1=Bet, 2=InitialDealt, 3=SpreadDecision, 4=PairThird, 5=End."
          example: 1
        chips:
          type: integer
          description: Player's chip balance.
          example: 1000
        ante:
          type: integer
          description: Ante bet amount.
          example: 100
        raise:
          type: integer
          description: Raise amount (0 if not raised).
          example: 0
        spread:
          type: integer
          description: "Spread value (|rank2 - rank1| - 1). 0 for pairs and consecutive ranks."
          example: 4
        result:
          type: integer
          description: "Game result: 1=Win, 0=Draw/Push, -1=Lose."
          example: 0
        totalPayout:
          type: integer
          description: Total payout returned to the player.
          example: 0
        message:
          type: string
          description: Human-readable status or result message.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code for i18n.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template.

    # -------------------------------------------------------------------------
    # Casino War (カジノウォー)
    # -------------------------------------------------------------------------
    CasinoWarRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          enum: [reset, r, bet, b, surrender, war, log]
          description: Game command to execute.
        amount:
          type: integer
          description: Bet amount (required for `bet` command). Must be a multiple of 10, min 10, max 10000.
          example: 100
        sessionId:
          type: string
          description: Session identifier for game state persistence.

    CasinoWarResponse:
      type: object
      properties:
        playerCard:
          description: Player's initial card (present after `bet`).
          oneOf:
            - $ref: '#/components/schemas/Card'
            - type: "null"
        dealerCard:
          description: Dealer's initial card (present after `bet`).
          oneOf:
            - $ref: '#/components/schemas/Card'
            - type: "null"
        playerWarCard:
          description: Player's war card (present after `war`).
          oneOf:
            - $ref: '#/components/schemas/Card'
            - type: "null"
        dealerWarCard:
          description: Dealer's war card (present after `war`).
          oneOf:
            - $ref: '#/components/schemas/Card'
            - type: "null"
        burnCards:
          type: array
          description: Three burn cards drawn face-down before the war cards (empty until `war` is invoked).
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Current game phase: 1=Bet, 2=InitialDealt, 3=TieDecision, 4=WarDealt, 5=End."
          example: 1
        chips:
          type: integer
          description: Player's chip balance.
          example: 1000
        ante:
          type: integer
          description: Ante bet amount.
          example: 100
        warBet:
          type: integer
          description: War bet amount (0 if no war was declared).
          example: 0
        result:
          type: integer
          description: "Game result: 1=Win, 0=Draw/Push, -1=Lose."
          example: 0
        totalPayout:
          type: integer
          description: Total payout returned to the player.
          example: 0
        message:
          type: string
          description: Human-readable status or result message.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code for i18n.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template.

    # -------------------------------------------------------------------------
    # Dragon Tiger (ドラゴンタイガー)
    # -------------------------------------------------------------------------
    DragonTigerRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          enum: [reset, r, bet, b, clear, log]
          description: Game command to execute.
        amount:
          type: integer
          description: Bet amount (required for `bet` command). Must be a multiple of 10, min 10, max 10000.
          example: 100
        betType:
          type: integer
          enum: [0, 1, 2]
          description: "Bet target (required for `bet` command): 0=Dragon, 1=Tiger, 2=Tie."
          example: 0
        sessionId:
          type: string
          description: Session identifier for game state persistence.

    DragonTigerResponse:
      type: object
      properties:
        dragonCard:
          description: Card dealt to the Dragon slot (present after `bet`).
          oneOf:
            - $ref: '#/components/schemas/Card'
            - type: "null"
        tigerCard:
          description: Card dealt to the Tiger slot (present after `bet`).
          oneOf:
            - $ref: '#/components/schemas/Card'
            - type: "null"
        phase:
          type: integer
          description: "Current game phase: 1=Bet, 2=End."
          example: 1
        chips:
          type: integer
          description: Player's chip balance.
          example: 1000
        betAmount:
          type: integer
          description: Bet amount placed in the current round.
          example: 0
        betType:
          type: integer
          description: "Bet target placed in the current round: 0=Dragon, 1=Tiger, 2=Tie."
          example: 0
        result:
          type: integer
          description: "Round result (domain.GameResult): 1=Dragon wins, 2=Tiger wins, 3=Tie."
          example: 0
        payout:
          type: integer
          description: Payout returned to the player for the round (includes stake on a win, half-stake refund on a Dragon/Tiger tie).
          example: 0
        history:
          type: array
          description: "Big Road history (most recent appended). Values: 0=Dragon win, 1=Tiger win, 2=Tie."
          items:
            type: integer
        message:
          type: string
          description: Human-readable status or result message.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code for i18n.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template.

    # -------------------------------------------------------------------------
    # Blackjack Switch (ブラックジャック・スイッチ)
    # -------------------------------------------------------------------------
    BlackJackSwitchRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          enum: [reset, r, bet, b, switch, sw, keep, k, hit, h, stand, s, doubledown, dd, log, l]
          description: Game command to execute.
        amount:
          type: integer
          description: Bet amount per hand (required for `bet`). Total cost is amount * 2.
          example: 100
        sessionId:
          type: string
          description: Session identifier for game state persistence.

    BlackJackSwitchHand:
      type: object
      properties:
        cards:
          type: array
          description: Cards in the hand. Null entries are face-down placeholders.
          items:
            oneOf:
              - $ref: '#/components/schemas/Card'
              - type: "null"
        score:
          type: integer
          description: Best blackjack score for this hand (Aces 1 or 11).
        bet:
          type: integer
          description: Bet amount on this hand (doubles after a successful double-down).
        stood:
          type: boolean
        doubled:
          type: boolean
        busted:
          type: boolean
        isBJ:
          type: boolean
          description: True if the hand is a 2-card 21 (natural). Pays 1:1 in this variant.
        result:
          type: integer
          description: "Per-hand domain.GameResult: 1=Win, 0=Draw, -1=Lose. Set after End phase."
        payout:
          type: integer
          description: Per-hand payout including stake return.

    BlackJackSwitchResponse:
      type: object
      properties:
        hands:
          type: array
          description: Always two hands; the player may switch their second cards in the Switch phase.
          items:
            $ref: '#/components/schemas/BlackJackSwitchHand'
        dealerCards:
          type: array
          description: Dealer's cards. The hole card (index 1) is null until the round ends.
          items:
            oneOf:
              - $ref: '#/components/schemas/Card'
              - type: "null"
        dealerScore:
          type: integer
          description: Visible dealer score (up-card only mid-round; full score at end).
        phase:
          type: integer
          description: "Current game phase: 1=Bet, 2=Switch, 3=Action, 4=End."
        currentHandIdx:
          type: integer
          description: Index of the player hand currently acting (during Action phase).
        chips:
          type: integer
          description: Player's chip balance.
        switched:
          type: boolean
          description: True when the player exercised the switch this round.
        dealerPushed22:
          type: boolean
          description: True when the dealer ended on 22 (push rule, not natural BJ).
        overallResult:
          type: integer
          description: "Aggregate of hand results: 1=net win, 0=draw, -1=net loss."
        totalPayout:
          type: integer
          description: Sum of per-hand payouts.
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    # -------------------------------------------------------------------------
    # Pitch / Setback (ピッチ / セットバック)
    # -------------------------------------------------------------------------
    PitchRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `bid` (`b`),
            `play` (`p`), `next` (`n`), `nextround` (`nr`), `hint` (`h`), `log` (`l`).
          enum: [reset, r, bid, b, play, p, next, n, nextround, nr, hint, h, log, l]
          example: reset
        bid:
          type: integer
          description: Bid value (0=pass, 2-4). Required for `bid` command.
          minimum: 0
          maximum: 4
          example: 3
        cardIndex:
          type: integer
          description: Index of the card to play. Required for `play` command.
          example: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            pointLimit:
              type: integer
              minimum: 1
              maximum: 1000
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters).
          example: my-session-001

    PitchResponse:
      type: object
      description: Current state of the Pitch game
      properties:
        players:
          type: array
          description: Array of player objects
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              bid:
                type: integer
                description: "Player's bid (-1=not bid, 0=pass, 2-4)."
              roundScore:
                type: integer
              cumulativeScore:
                type: integer
              trickCount:
                type: integer
        phase:
          type: integer
          description: "Game phase: 0=Bid, 1=Play, 2=TrickEnd, 3=RoundEnd, 4=GameEnd."
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        dealerIdx:
          type: integer
        currentPlayerIdx:
          type: integer
        bidPlayerIdx:
          type: integer
        currentBid:
          type: integer
          description: Current highest bid (0=no bid yet).
        bidWinnerIdx:
          type: integer
          description: Index of the winning bidder (-1=not yet decided).
        trumpSuit:
          type: integer
          description: "Trump suit: 0=unset, 1=Spades, 2=Clubs, 3=Hearts, 4=Diamonds."
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        validPlayIndices:
          type: array
          items:
            type: integer
          description: Indices of cards in the human player's hand that are legal to play.
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            pointLimit:
              type: integer
        hint:
          type: object
          properties:
            cardIndex:
              type: integer
            bid:
              type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    AllFoursResponse:
      type: object
      description: Current state of the All Fours (Seven Up) game
      properties:
        players:
          type: array
          description: Array of player objects (0=non-dealer/human, 1=dealer/CPU)
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              roundScore:
                type: integer
              cumulativeScore:
                type: integer
              trickCount:
                type: integer
        phase:
          type: integer
          description: "Game phase: 0=Beg, 1=Gift, 2=Play, 3=TrickEnd, 4=RoundEnd, 5=GameEnd."
          enum: [0, 1, 2, 3, 4, 5]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        dealerIdx:
          type: integer
        nonDealerIdx:
          type: integer
        currentPlayerIdx:
          type: integer
        trumpSuit:
          type: integer
          description: "Trump suit: 0=unset, 1=Spades, 2=Clubs, 3=Hearts, 4=Diamonds."
        turnUp:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Card'
          description: The turn-up card that set the provisional trump, or null.
        runCount:
          type: integer
          description: Number of "run the cards" attempts this deal.
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        validPlayIndices:
          type: array
          items:
            type: integer
          description: Indices of cards in the human player's hand that are legal to play.
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            pointLimit:
              type: integer
        hint:
          type: object
          properties:
            cardIndex:
              type: integer
            beg:
              type: boolean
            run:
              type: boolean
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    PrsiResponse:
      type: object
      description: Current state of the Prší (Czech Crazy Eights) game
      properties:
        players:
          type: array
          description: Array of 4 player objects (0=human, 1-3=CPU)
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 0=Play, 1=GameEnd."
          enum: [0, 1]
          example: 0
        currentPlayerIdx:
          type: integer
        discardTop:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Card'
          description: Top card of the discard pile, or null.
        drawPileCount:
          type: integer
        penaltyDrawCount:
          type: integer
          description: Accumulated 7-stack penalty the current player must take or stack onto (0 = none).
        pendingSkips:
          type: integer
          description: Accumulated skip count from Aces/Unders pending on the next turn advance.
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    CalabresellaResponse:
      type: object
      description: Current state of the Calabresella (Terziglio) game
      properties:
        players:
          type: array
          description: Array of 3 player objects (0=human, 1-2=CPU)
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              trickCount:
                type: integer
              score:
                type: integer
                description: Cumulative match score for this player
              isSoloist:
                type: boolean
                description: Whether this player is the soloist for the current deal
              roundThirds:
                type: integer
                description: Thirds-of-a-point captured by this player in the current round
        phase:
          type: integer
          description: "Game phase: 0=Bid, 1=Monte, 2=Play, 3=TrickEnd, 4=RoundEnd, 5=GameEnd"
          enum: [0, 1, 2, 3, 4, 5]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        currentBidderIdx:
          type: integer
          description: Index of the player whose turn it is to bid (during the auction)
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        forehandIdx:
          type: integer
        soloistIdx:
          type: integer
          description: Index of the soloist for the current deal (-1 until decided)
        winningBid:
          type: integer
          description: The winning declaration (0=pass, 1=chiamo, 2=solo)
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        playerScores:
          type: array
          items:
            type: integer
          description: Cumulative match points for each of the 3 players
        roundThirds:
          type: array
          items:
            type: integer
          description: Thirds-of-a-point captured by each of the 3 players in the current round
        lastTrickWinner:
          type: integer
          description: Index of the player who won the most recent trick (-1 if none)
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (must-follow filtered)
        gameEndFlag:
          type: boolean
        winnerPlayer:
          type: integer
          description: Winning player index (-1 if undecided)
        isHumanTurn:
          type: boolean
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetPoints:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    CinchResponse:
      type: object
      description: Current state of the Cinch (Double Pedro / High Five) game
      properties:
        players:
          type: array
          description: Array of 4 player objects (0=human, 1-3=CPU)
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              trickCount:
                type: integer
              bid:
                type: integer
                description: This player's bid for the current deal (0=pass, 1-14)
              totalScore:
                type: integer
                description: Cumulative match score for this player
        phase:
          type: integer
          description: "Game phase: 0=Bid, 1=NameTrump, 2=Play, 3=TrickEnd, 4=RoundEnd, 5=GameEnd"
          enum: [0, 1, 2, 3, 4, 5]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        totalTricks:
          type: integer
          description: Total tricks per deal (9)
        dealerIdx:
          type: integer
        currentTurn:
          type: integer
          description: Index of the player whose turn it is
        bidPlayerIdx:
          type: integer
          description: Index of the player whose turn it is to bid (during the auction)
        currentBid:
          type: integer
          description: The current highest bid (0 if none yet)
        bidWinnerIdx:
          type: integer
          description: Index of the high bidder (-1 until decided)
        trumpSuit:
          type: integer
          description: "Trump suit: 0=unset, 1=♠, 2=♣, 3=♥, 4=♦"
          enum: [0, 1, 2, 3, 4]
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        lastTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        lastTrickWinner:
          type: integer
          description: Index of the player who won the most recent trick (-1 if none)
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (must-follow filtered)
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: Winning player index (-1 if undecided)
        roundWinners:
          type: array
          items:
            type: integer
          description: Player indices that reached the target this round
        lastDealDetail:
          type: object
          description: Scoring breakdown for the most recently completed deal
          properties:
            trumpSuit:
              type: integer
            bidderIdx:
              type: integer
            bid:
              type: integer
            setBack:
              type: boolean
              description: Whether the bidder failed to make the bid and was set back
            points:
              type: object
              additionalProperties:
                type: integer
              description: Points captured per player this deal
            gained:
              type: object
              additionalProperties:
                type: integer
              description: Net score change per player this deal
        isHumanTurn:
          type: boolean
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            bid:
              type: integer
            trumpSuit:
              type: integer
            reason:
              type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            pointLimit:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    LooResponse:
      type: object
      description: Current state of the Loo (Lanterloo) game
      properties:
        players:
          type: array
          description: Array of 4 player objects (0=human, 1-3=CPU)
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              trickCount:
                type: integer
              playing:
                type: boolean
                description: Whether this player is participating in the current deal
              chips:
                type: integer
                description: Cumulative chip total for this player
        phase:
          type: integer
          description: "Game phase: 0=Decide, 1=Play, 2=TrickEnd, 3=RoundEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        totalTricks:
          type: integer
          description: Total tricks per deal (5)
        dealerIdx:
          type: integer
        currentTurn:
          type: integer
          description: Index of the player whose turn it is
        decidePlayerIdx:
          type: integer
          description: Index of the player deciding play/pass (during the decide phase)
        trumpSuit:
          type: integer
          description: "Trump suit: 0=unset, 1=♠, 2=♣, 3=♥, 4=♦"
          enum: [0, 1, 2, 3, 4]
        turnUp:
          $ref: '#/components/schemas/Card'
        pot:
          type: integer
          description: Current pot size (carried over between deals)
        potStart:
          type: integer
          description: Pot size at the start of the current deal
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        lastTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        lastTrickWinner:
          type: integer
          description: Index of the winner of the last completed trick (-1 if none)
        playableIndices:
          type: array
          items:
            type: integer
        gameEndFlag:
          type: boolean
        lastDealDetail:
          type: object
          description: Settlement breakdown of the most recent deal
          properties:
            potStart:
              type: integer
            trumpSuit:
              type: integer
            playing:
              type: array
              items:
                type: boolean
            tricks:
              type: object
              additionalProperties:
                type: integer
            gained:
              type: object
              additionalProperties:
                type: integer
            looed:
              type: array
              items:
                type: integer
            potCarry:
              type: integer
        isHumanTurn:
          type: boolean
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            decision:
              type: boolean
            reason:
              type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            ante:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    BasraResponse:
      type: object
      description: Current state of the Basra (Bastra) game
      properties:
        players:
          type: array
          description: Array of 4 player objects (0=human, 1-3=CPU)
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              capturedCount:
                type: integer
                description: Number of cards this player has captured so far
              basraCount:
                type: integer
                description: Number of Basra bonuses this player has scored
              score:
                type: integer
                description: Running / final score for this player
        phase:
          type: integer
          description: "Game phase: 0=Play, 1=GameEnd"
          enum: [0, 1]
          example: 0
        roundNumber:
          type: integer
        currentTurn:
          type: integer
          description: Index of the player whose turn it is
        tableCards:
          type: array
          description: Cards currently face-up on the table
          items:
            $ref: '#/components/schemas/Card'
        lastCaptureIdx:
          type: integer
          description: Index of the last player who captured (-1 if none)
        remainingDeck:
          type: integer
          description: Number of cards left in the stock
        playableIndices:
          type: array
          items:
            type: integer
        captureOptions:
          type: object
          description: Maps a hand card index (as string) to the table-card indices it can capture
          additionalProperties:
            type: array
            items:
              type: integer
        winners:
          type: array
          description: Indices of the winning player(s) at game end
          items:
            type: integer
        gameEndFlag:
          type: boolean
        lastDealDetail:
          type: object
          nullable: true
          description: Scoring breakdown of the finished game (present at GameEnd)
          properties:
            cards:
              type: object
              additionalProperties:
                type: integer
            aces:
              type: object
              additionalProperties:
                type: integer
            basras:
              type: object
              additionalProperties:
                type: integer
            hasSevenDiamonds:
              type: integer
              description: Index of the player holding 7♦ (-1 if none)
            hasTenDiamonds:
              type: integer
              description: Index of the player holding 10♦ (-1 if none)
            mostCards:
              type: integer
              description: Index of the player with the most captured cards (-1 if tied)
            gained:
              type: object
              additionalProperties:
                type: integer
        isHumanTurn:
          type: boolean
        hint:
          type: object
          nullable: true
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            tableIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    TablanetResponse:
      type: object
      description: Current state of the Tablanet (Tablić) game
      properties:
        players:
          type: array
          description: Array of 4 player objects (0=human, 1-3=CPU)
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              capturedCount:
                type: integer
                description: Number of cards this player has captured so far
              tablaCount:
                type: integer
                description: Number of Tabla bonuses this player has scored
              score:
                type: integer
                description: Running / final score for this player
        phase:
          type: integer
          description: "Game phase: 0=Play, 1=GameEnd"
          enum: [0, 1]
          example: 0
        roundNumber:
          type: integer
        currentTurn:
          type: integer
          description: Index of the player whose turn it is
        tableCards:
          type: array
          description: Cards currently face-up on the table
          items:
            $ref: '#/components/schemas/Card'
        lastCaptureIdx:
          type: integer
          description: Index of the last player who captured (-1 if none)
        remainingDeck:
          type: integer
          description: Number of cards left in the stock
        playableIndices:
          type: array
          items:
            type: integer
        captureOptions:
          type: object
          description: Maps a hand card index (as string) to the table-card indices it can capture
          additionalProperties:
            type: array
            items:
              type: integer
        winners:
          type: array
          description: Indices of the winning player(s) at game end
          items:
            type: integer
        gameEndFlag:
          type: boolean
        lastDealDetail:
          type: object
          nullable: true
          description: Scoring breakdown of the finished game (present at GameEnd)
          properties:
            cards:
              type: object
              additionalProperties:
                type: integer
            aces:
              type: object
              additionalProperties:
                type: integer
            jacks:
              type: object
              additionalProperties:
                type: integer
            tablas:
              type: object
              additionalProperties:
                type: integer
            hasTenDiamonds:
              type: integer
              description: Index of the player holding 10♦ (-1 if none)
            hasTwoClubs:
              type: integer
              description: Index of the player holding 2♣ (-1 if none)
            mostCards:
              type: integer
              description: Index of the player with the most captured cards (-1 if tied)
            gained:
              type: object
              additionalProperties:
                type: integer
        isHumanTurn:
          type: boolean
        hint:
          type: object
          nullable: true
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            tableIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    TrenteEtQuaranteResponse:
      type: object
      description: Current state of the Trente et Quarante (Rouge et Noir) game
      properties:
        phase:
          type: integer
          description: "Game phase: 0=Bet, 1=Result"
          enum: [0, 1]
          example: 0
        roundNumber:
          type: integer
          description: Number of rounds resolved so far
        chips:
          type: integer
          description: Player's remaining chip stack
        currentBet:
          type: integer
          description: "Selected bet: 0=Noir, 1=Rouge, 2=Couleur, 3=Inverse"
          enum: [0, 1, 2, 3]
        stake:
          type: integer
          description: Amount wagered on the current round
        noirRow:
          type: array
          description: Cards dealt to the Noir (black) row, summed until the total reaches 31+
          items:
            $ref: '#/components/schemas/Card'
        rougeRow:
          type: array
          description: Cards dealt to the Rouge (red) row, summed until the total reaches 31+
          items:
            $ref: '#/components/schemas/Card'
        noirTotal:
          type: integer
          description: Pip total of the Noir row (31-40 once complete)
        rougeTotal:
          type: integer
          description: Pip total of the Rouge row (31-40 once complete)
        winningRow:
          type: integer
          description: "Winning row (the lower total): 0=Noir, 1=Rouge; -1 for none (push/refait)"
          enum: [-1, 0, 1]
        firstCardRed:
          type: boolean
          description: Whether the first card dealt (Noir row's first card) is red — drives Couleur/Inverse display
        refait:
          type: boolean
          description: True when both rows tie at 31 (a "Refait" — half the stake goes to the house)
        result:
          type: integer
          description: "Round result from the player's perspective: 1=win, 0=push, -1=lose"
          enum: [-1, 0, 1]
        payout:
          type: integer
          description: "Gross chips returned to the stack this round (0=lose, stake/2=refait, stake=push, stake*2=win)"
        remainingDeck:
          type: integer
          description: Number of cards remaining in the shoe
        gameEndFlag:
          type: boolean
          description: True once the round has resolved
        hint:
          type: object
          description: Educational hint offered during the bet phase
          properties:
            bet:
              type: integer
              description: "Suggested bet type: 0=Noir, 1=Rouge, 2=Couleur, 3=Inverse"
              enum: [0, 1, 2, 3]
            reason:
              type: string
              description: i18n reason key for the suggestion
        config:
          type: object
          description: Local-rule configuration
          properties:
            defaultBet:
              type: integer
              description: "Bet type pre-selected at the start of each round: 0=Noir, 1=Rouge, 2=Couleur, 3=Inverse"
              enum: [0, 1, 2, 3]
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    KingResponse:
      type: object
      description: Current state of the King game (mirrors KingWebOutput)
      properties:
        players:
          type: array
          description: Array of 4 player objects (0=human, 1-3=CPU)
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              trickCount:
                type: integer
              totalScore:
                type: integer
                description: Cumulative match score for this player (negative = penalty)
        phase:
          type: string
          description: "Game phase"
          enum: [selectContract, play, dealEnd, gameEnd]
          example: selectContract
        dealNumber:
          type: integer
          description: Current deal number (0-indexed, 0-6)
        totalDeals:
          type: integer
          description: Total deals in a match (7 = one pass through all contracts)
        dealerIdx:
          type: integer
          description: Dealer (親) index for the current deal
        currentTurn:
          type: integer
        currentContract:
          type: integer
          description: "Active contract for the current deal (-1 until chosen): 0=No Tricks, 1=No Hearts, 2=No Queens, 3=No King of Hearts, 4=No Last Two Tricks, 5=No Men, 6=King (Trump)"
        trumpSuit:
          type: integer
          description: "Trump suit for the King (Trump) contract (-1=none for the six negative contracts): 1=♠, 2=♣, 3=♥, 4=♦"
        trickNumber:
          type: integer
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        lastTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        lastTrickWinner:
          type: integer
          description: Index of the player who won the most recent trick (-1 if none)
        usedContracts:
          type: array
          items:
            type: boolean
          description: Which of the 7 contracts have already been played this match
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (must-follow filtered)
        gameEndFlag:
          type: boolean
        roundWinners:
          type: array
          items:
            type: integer
          description: Highest-scoring player indices at match end
        lastDealDetail:
          type: object
          nullable: true
          properties:
            contract:
              type: integer
            trumpSuit:
              type: integer
            dealerIdx:
              type: integer
            gained:
              type: object
              additionalProperties:
                type: integer
        isHumanTurn:
          type: boolean
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    PresidentRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: President command. Accepted values. `reset` (`r`), `play` (`p`), `log` (`l`).
          enum: [reset, r, play, p, log, l]
          example: reset
        indices:
          type: array
          description: Card indices to play (empty / omitted = pass). Used with `play`.
          items:
            type: integer
          example: [0, 1]
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            revolutionEnabled:
              type: boolean
            cardExchangeEnabled:
              type: boolean
            passFieldFlushEnabled:
              type: boolean
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    PresidentResponse:
      type: object
      description: Current state of the President (Scum) game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id: { type: integer }
              isHuman: { type: boolean }
              isFinished: { type: boolean }
              rank: { type: integer }
              cardCount: { type: integer }
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
        currentTurn:
          type: integer
        tableCards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        lastPlayPlayerIdx:
          type: integer
        gameEndFlag:
          type: boolean
        revolutionActive:
          type: boolean
        config:
          type: object
          properties:
            revolutionEnabled: { type: boolean }
            cardExchangeEnabled: { type: boolean }
            passFieldFlushEnabled: { type: boolean }
            cpuDifficulty: { type: integer }
        exchangeActions:
          type: array
          items:
            type: object
            properties:
              fromPlayerIdx: { type: integer }
              toPlayerIdx: { type: integer }
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
        cpuActions:
          type: array
          items:
            type: object
            properties:
              playerIdx: { type: integer }
              playedCards:
                type: array
                nullable: true
                items:
                  $ref: '#/components/schemas/Card'
        humanAction:
          type: object
          nullable: true
          properties:
            playerIdx: { type: integer }
            playedCards:
              type: array
              nullable: true
              items:
                $ref: '#/components/schemas/Card'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    CassinoRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: Cassino command. Accepted values. `reset` (`r`), `take` (`t`), `build` (`b`), `trail` (`tr`), `next` (`n`), `log` (`l`).
          enum: [reset, r, take, t, build, b, trail, tr, next, n, log, l]
          example: take
        handIndex:
          type: integer
          description: Hand card index for take / build / trail.
          example: 0
        tableIndices:
          type: array
          description: Table card indices selected for take / build.
          items:
            type: integer
          example: [0, 1]
        buildIndices:
          type: array
          description: Build indices selected for take.
          items:
            type: integer
        declaredValue:
          type: integer
          description: Build declared value (2..10) when command is `build`.
          minimum: 2
          maximum: 10
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            targetScore:
              type: integer
              minimum: 1
              maximum: 999
            multiBuildEnabled:
              type: boolean
            sweepBonusEnabled:
              type: boolean
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    CassinoResponse:
      type: object
      description: Current state of the Cassino game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id: { type: integer }
              isHuman: { type: boolean }
              cardCount: { type: integer }
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              capturedCount: { type: integer }
              sweepCount: { type: integer }
              totalScore: { type: integer }
        currentTurn:
          type: integer
        tableCards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        builds:
          type: array
          items:
            type: object
            properties:
              ownerIdx: { type: integer }
              value: { type: integer }
              groups:
                type: array
                items:
                  type: array
                  items:
                    $ref: '#/components/schemas/Card'
              isMulti: { type: boolean }
        lastCaptureIdx:
          type: integer
        gameEndFlag:
          type: boolean
        phase:
          type: string
          description: Current game phase (`dealing`, `playerTurn`, `roundEnd`, `gameEnd`).
        config:
          type: object
          properties:
            targetScore: { type: integer }
            multiBuildEnabled: { type: boolean }
            sweepBonusEnabled: { type: boolean }
            cpuDifficulty: { type: integer }
        cpuActions:
          type: array
          items:
            type: object
            properties:
              playerIdx: { type: integer }
              type: { type: string }
              playedCard:
                nullable: true
                $ref: '#/components/schemas/Card'
              capturedCards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              buildValue: { type: integer }
              isSweep: { type: boolean }
        humanAction:
          type: object
          nullable: true
          properties:
            playerIdx: { type: integer }
            type: { type: string }
            playedCard:
              nullable: true
              $ref: '#/components/schemas/Card'
            capturedCards:
              type: array
              items:
                $ref: '#/components/schemas/Card'
            buildValue: { type: integer }
            isSweep: { type: boolean }
        remainingDeck:
          type: integer
        packsDealt:
          type: integer
        roundWinners:
          type: array
          items:
            type: integer
        lastRoundDetail:
          type: object
          nullable: true
          properties:
            cards:
              type: object
              additionalProperties:
                type: integer
            spades:
              type: object
              additionalProperties:
                type: integer
            aces:
              type: object
              additionalProperties:
                type: integer
            hasBigCasino: { type: integer }
            hasLittleCasino: { type: integer }
            sweeps:
              type: object
              additionalProperties:
                type: integer
            gained:
              type: object
              additionalProperties:
                type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    ScopaRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: Scopa command. Accepted values. `reset` (`r`), `next` (`n`), `play` (`p`), `log` (`l`).
          enum: [reset, r, next, n, play, p, log, l]
          example: play
        handIndex:
          type: integer
          description: Hand card index to play (for the `play` command).
          example: 0
        tableIndices:
          type: array
          description: >
            Table card indices to capture with the played card (for the `play`
            command). When a single exact match exists it is forced.
          items:
            type: integer
          example: [1, 3]
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            targetScore:
              type: integer
              description: Score required to win the match.
              example: 11
            cpuDifficulty:
              type: integer
              description: CPU difficulty (0=easy, 1=normal, 2=hard).
              minimum: 0
              maximum: 2
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    ScopaResponse:
      type: object
      description: Current state of the Scopa game
      properties:
        players:
          type: array
          description: All players in the game (1 human + 1 CPU).
          items:
            type: object
            properties:
              id: { type: integer }
              isHuman: { type: boolean }
              cardCount: { type: integer }
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              capturedCount: { type: integer }
              scopaCount: { type: integer }
              totalScore: { type: integer }
        currentTurn:
          type: integer
        tableCards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        lastCaptureIdx:
          type: integer
        gameEndFlag:
          type: boolean
        phase:
          type: string
          description: Current game phase.
        config:
          type: object
          properties:
            targetScore: { type: integer }
            cpuDifficulty: { type: integer }
        cpuActions:
          type: array
          description: Actions taken by the CPU since the last human action.
          items:
            type: object
            properties:
              playerIdx: { type: integer }
              playedCard:
                nullable: true
                $ref: '#/components/schemas/Card'
              capturedCards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              isScopa: { type: boolean }
        humanAction:
          type: object
          nullable: true
          properties:
            playerIdx: { type: integer }
            playedCard:
              nullable: true
              $ref: '#/components/schemas/Card'
            capturedCards:
              type: array
              items:
                $ref: '#/components/schemas/Card'
            isScopa: { type: boolean }
        remainingDeck:
          type: integer
        packsDealt:
          type: integer
        roundWinners:
          type: array
          items:
            type: integer
        lastRoundDetail:
          type: object
          nullable: true
          description: Per-point scoring breakdown of the last completed round.
          properties:
            cards:
              type: object
              additionalProperties:
                type: integer
            diamonds:
              type: object
              additionalProperties:
                type: integer
            sevens:
              type: object
              additionalProperties:
                type: integer
            hasSetteBello: { type: integer }
            scopas:
              type: object
              additionalProperties:
                type: integer
            gained:
              type: object
              additionalProperties:
                type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    ScoponeRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: Scopone command. Accepted values. `reset` (`r`), `next` (`n`), `play` (`p`), `setdifficulty` (`sd`), `settarget` (`st`), `log` (`l`).
          enum: [reset, r, next, n, play, p, setdifficulty, sd, settarget, st, log, l]
          example: play
        handIndex:
          type: integer
          description: Hand card index to play (for the `play` command).
          example: 0
        tableIndices:
          type: array
          description: >
            Table card indices to capture with the played card (for the `play`
            command). When a single exact match exists it is forced.
          items:
            type: integer
          example: [1, 3]
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            targetScore:
              type: integer
              description: Score required to win the match.
              example: 11
            cpuDifficulty:
              type: integer
              description: CPU difficulty (0=easy, 1=normal, 2=hard).
              minimum: 0
              maximum: 2
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    ScoponeResponse:
      type: object
      description: Current state of the Scopone game
      properties:
        players:
          type: array
          description: All players in the game (1 human + 3 CPU). Seats 0+2 vs 1+3.
          items:
            type: object
            properties:
              id: { type: integer }
              isHuman: { type: boolean }
              team: { type: integer }
              handCount: { type: integer }
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              capturedCount: { type: integer }
              scopaCount: { type: integer }
        currentTurn:
          type: integer
        tableCards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        teamScores:
          type: array
          description: Cumulative score for each team (index = team number).
          items:
            type: integer
        winnerTeam:
          type: integer
          description: Winning team index, or -1 if the match is not over.
        gameEndFlag:
          type: boolean
        phase:
          type: string
          description: Current game phase (playerTurn, roundEnd, gameEnd).
        config:
          type: object
          properties:
            targetScore: { type: integer }
            cpuDifficulty: { type: integer }
        cpuActions:
          type: array
          description: Actions taken by the CPUs since the last human action.
          items:
            type: object
            properties:
              playerIdx: { type: integer }
              playedCard:
                nullable: true
                $ref: '#/components/schemas/Card'
              capturedCards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              isScopa: { type: boolean }
        humanAction:
          type: object
          nullable: true
          properties:
            playerIdx: { type: integer }
            playedCard:
              nullable: true
              $ref: '#/components/schemas/Card'
            capturedCards:
              type: array
              items:
                $ref: '#/components/schemas/Card'
            isScopa: { type: boolean }
        lastRoundDetail:
          type: object
          nullable: true
          description: Per-point scoring breakdown of the last completed round (keyed by team).
          properties:
            cards:
              type: object
              additionalProperties:
                type: integer
            diamonds:
              type: object
              additionalProperties:
                type: integer
            sevens:
              type: object
              additionalProperties:
                type: integer
            hasSetteBello: { type: integer }
            scopas:
              type: object
              additionalProperties:
                type: integer
            gained:
              type: object
              additionalProperties:
                type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    EscobaRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: Escoba command. Accepted values. `reset` (`r`), `next` (`n`), `play` (`p`), `setdifficulty` (`sd`), `settarget` (`st`), `log` (`l`).
          enum: [reset, r, next, n, play, p, setdifficulty, sd, settarget, st, log, l]
          example: play
        handIndex:
          type: integer
          description: Hand card index to play (for the `play` command).
          example: 0
        tableIndices:
          type: array
          description: >
            Table card indices to capture with the played card (for the `play`
            command). The played card plus the selected table cards must sum to 15.
          items:
            type: integer
          example: [1, 3]
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            targetScore:
              type: integer
              description: Score required to win the match.
              example: 10
            cpuDifficulty:
              type: integer
              description: CPU difficulty (0=easy, 1=normal, 2=hard).
              minimum: 0
              maximum: 2
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    EscobaResponse:
      type: object
      description: Current state of the Escoba game
      properties:
        players:
          type: array
          description: All players in the game (1 human + 3 CPU); free-for-all, no teams.
          items:
            type: object
            properties:
              id: { type: integer }
              isHuman: { type: boolean }
              handCount: { type: integer }
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              capturedCount: { type: integer }
              escobaCount: { type: integer }
              score: { type: integer }
        currentTurn:
          type: integer
        tableCards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        stockRemaining:
          type: integer
          description: Number of cards left in the stock to be re-dealt.
        winnerIdx:
          type: integer
          description: Winning player index, or -1 if the match is not over.
        gameEndFlag:
          type: boolean
        phase:
          type: string
          description: Current game phase (playerTurn, roundEnd, gameEnd).
        config:
          type: object
          properties:
            targetScore: { type: integer }
            cpuDifficulty: { type: integer }
        cpuActions:
          type: array
          description: Actions taken by the CPUs since the last human action.
          items:
            type: object
            properties:
              playerIdx: { type: integer }
              playedCard:
                nullable: true
                $ref: '#/components/schemas/Card'
              capturedCards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              isEscoba: { type: boolean }
        humanAction:
          type: object
          nullable: true
          properties:
            playerIdx: { type: integer }
            playedCard:
              nullable: true
              $ref: '#/components/schemas/Card'
            capturedCards:
              type: array
              items:
                $ref: '#/components/schemas/Card'
            isEscoba: { type: boolean }
        lastRoundDetail:
          type: object
          nullable: true
          description: Per-point scoring breakdown of the last completed round (per-player arrays indexed by player).
          properties:
            cards:
              type: array
              items:
                type: integer
            espadas:
              type: array
              items:
                type: integer
            sevens:
              type: array
              items:
                type: integer
            oros:
              type: array
              items:
                type: integer
            escobas:
              type: array
              items:
                type: integer
            aceEspada:
              type: integer
              description: Player index that captured the A♠, or -1.
            seteEspada:
              type: integer
              description: Player index that captured the 7♠, or -1.
            gained:
              type: array
              items:
                type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    BarbuRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: Barbu command. Accepted values. `reset` (`r`), `next` (`n`), `contract` (`c`), `play` (`p`), `log` (`l`).
          enum: [reset, r, next, n, contract, c, play, p, log, l]
          example: play
        contract:
          type: integer
          description: Contract id 0-6 (for the `contract` command).
          minimum: 0
          maximum: 6
          example: 5
        trumpSuit:
          type: integer
          description: Trump suit 1-4 (only for the Trumps contract; -1 otherwise).
          example: 1
        handIndex:
          type: integer
          description: Hand card index to play (-1 to pass in Dominoes).
          example: 0
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              description: CPU difficulty (0=easy, 1=normal, 2=hard).
              minimum: 0
              maximum: 2
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    BarbuResponse:
      type: object
      description: Current state of the Barbu game
      properties:
        players:
          type: array
          description: All players in the game (1 human + 3 CPU).
          items:
            type: object
            properties:
              id: { type: integer }
              isHuman: { type: boolean }
              cardCount: { type: integer }
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              trickCount: { type: integer }
              dominoRank: { type: integer }
              totalScore: { type: integer }
        phase:
          type: string
          description: Current game phase (selectContract, play, dealEnd, gameEnd).
        dealNumber: { type: integer }
        totalDeals: { type: integer }
        dealerIdx: { type: integer }
        currentTurn: { type: integer }
        currentContract:
          type: integer
          description: Selected contract id 0-6, or -1 when not yet chosen.
        trumpSuit:
          type: integer
          description: Trump suit 1-4 for the Trumps contract, or -1.
        trickNumber: { type: integer }
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx: { type: integer }
              card:
                $ref: '#/components/schemas/Card'
        lastTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx: { type: integer }
              card:
                $ref: '#/components/schemas/Card'
        lastTrickWinner: { type: integer }
        tablePlaced:
          type: array
          description: Dominoes layout per suit as bitmasks (index 1-4).
          items:
            type: integer
        dominoPlayable:
          type: array
          description: Playable hand indices for the human in Dominoes.
          items:
            type: integer
        usedContracts:
          type: array
          description: Which of the 7 contracts the current dealer has used.
          items:
            type: boolean
        gameEndFlag:
          type: boolean
        config:
          type: object
          properties:
            cpuDifficulty: { type: integer }
        roundWinners:
          type: array
          items:
            type: integer
        lastDealDetail:
          type: object
          nullable: true
          description: Per-player scoring breakdown of the last completed deal.
          properties:
            contract: { type: integer }
            trumpSuit: { type: integer }
            dealerIdx: { type: integer }
            gained:
              type: object
              additionalProperties:
                type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    # -------------------------------------------------------------------------
    # Calculation
    # -------------------------------------------------------------------------
    CalculationMoveZone:
      type: object
      description: Source or target zone for a Calculation card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: Zone identifier (stock, waste, foundation)
          enum: [stock, waste, foundation]
        idx:
          type: integer
          description: Pile index (required for waste/foundation; ignored for stock)
    SirTommyMoveZone:
      type: object
      description: Source or target zone for a SirTommy card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: Zone identifier (stock, waste, foundation)
          enum: [stock, waste, foundation]
        idx:
          type: integer
          description: Pile index (required for waste/foundation; ignored for stock)
    CalculationHint:
      type: object
      description: Suggested best move for Calculation
      properties:
        fromZone:
          type: string
          enum: [stock, waste]
        wasteIdx:
          type: integer
          description: Waste pile index (-1 if fromZone is `stock`)
        foundationIdx:
          type: integer
          description: Target foundation index (0..3)
    CalculationResponse:
      type: object
      description: Response payload for /calculation/exec
      properties:
        foundations:
          type: array
          description: Four foundation piles indexed 0..3 with step sizes +1, +2, +3, +4 respectively.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        wastes:
          type: array
          description: Four waste piles indexed 0..3.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        stockCount:
          type: integer
        stockTop:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "0=Playing, 1=GameClear, 2=GameOver"
        moveCount:
          type: integer
        canUndo:
          type: boolean
        isStalemate:
          type: boolean
        undoToEscape:
          type: integer
          description: Number of undos needed to escape stalemate (0 = not stalemate, -1 = unreachable)
        hint:
          $ref: '#/components/schemas/CalculationHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    # -------------------------------------------------------------------------
    # Spite and Malice
    # -------------------------------------------------------------------------
    SirTommyResponse:
      type: object
      description: Response payload for /sirtommy/exec
      properties:
        foundations:
          type: array
          description: Four foundation piles indexed 0..3 with step sizes +1, +2, +3, +4 respectively.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        wastes:
          type: array
          description: Four waste piles indexed 0..3.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        stockCount:
          type: integer
        stockTop:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "0=Playing, 1=GameClear, 2=GameOver"
        moveCount:
          type: integer
        canUndo:
          type: boolean
        isStalemate:
          type: boolean
        undoToEscape:
          type: integer
          description: Number of undos needed to escape stalemate (0 = not stalemate, -1 = unreachable)
        hint:
          $ref: '#/components/schemas/SirTommyHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    FourSeasonsMoveZone:
      type: object
      description: Source or target zone for a Four Seasons card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: Zone identifier (waste, tableau, foundation)
          enum: [waste, tableau, foundation]
        idx:
          type: integer
          description: Pile index (0..4 for the cross, 0..3 for the corners; omitted for the waste)
    ColoradoMoveZone:
      type: object
      description: Source or target zone for a Colorado card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: Zone identifier (waste, tableau, foundation, stock)
          enum: [waste, tableau, foundation, stock]
        idx:
          type: integer
          description: Tableau pile index (0..19); omitted for the waste, the stock, and for a foundation (the server picks the pile)
    CribbageSquaresScore:
      type: object
      description: One hand's cribbage score breakdown
      properties:
        fifteens:
          type: integer
        pairs:
          type: integer
        runs:
          type: integer
        flush:
          type: integer
        nobs:
          type: integer
        total:
          type: integer
    DiplomatMoveZone:
      type: object
      description: Source or target zone for a Diplomat card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: Zone identifier (tableau, waste, foundation)
          enum: [tableau, waste, foundation]
        col:
          type: integer
          description: Column index (0..7); omitted for the waste and for a foundation (the server picks the pile)
    RoyalCotillionMoveZone:
      type: object
      description: Source or target zone for a Royal Cotillion card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: Zone identifier (tableau, reserve, waste, stock, foundation)
          enum: [tableau, reserve, waste, stock, foundation]
        col:
          type: integer
          description: Tableau slot (0..15) or reserve pile (0..3); omitted for the waste, the stock, and for a foundation (the server picks the pile)
    CrazyQuiltMoveZone:
      type: object
      description: Source or target zone for a Crazy Quilt card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: Zone identifier (quilt, waste, foundation)
          enum: [quilt, waste, foundation]
        col:
          type: integer
          description: Quilt cell index (0..63, row*8+col); omitted for the waste and for a foundation (the server picks the pile)
    CrazyQuiltHint:
      type: object
      description: A suggested move in Crazy Quilt
      properties:
        fromZone:
          type: string
          description: "quilt, waste or stock"
        fromIdx:
          type: integer
          description: Quilt cell index, or -1 when the source is the waste or the stock
        toZone:
          type: string
          description: "foundation or waste"
        toIdx:
          type: integer
          description: Destination index, or -1 when the destination is the waste
    CrazyQuiltResponse:
      type: object
      description: Response payload for /crazyquilt/exec
      properties:
        quilt:
          type: array
          description: The 64 cells in row-major order. A cell whose card has been taken is null.
          items:
            $ref: '#/components/schemas/Card'
        available:
          type: array
          description: Per-cell availability. True when one of the card's SHORT sides is exposed, which depends on whether the cell is laid vertically or horizontally. Computed by the server so the rule lives in one place.
          items:
            type: boolean
        foundation:
          type: array
          description: The eight foundations, seeded with an Ace or a King of each suit before the deal.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        foundationAscending:
          type: array
          description: Per-foundation direction; true builds up from the Ace, false down from the King.
          items:
            type: boolean
        stockCount:
          type: integer
        redealsLeft:
          type: integer
          description: Redeals remaining. The waste is turned over without shuffling.
        waste:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "0=Playing, 1=GameClear, 2=GameOver"
        moveCount:
          type: integer
        canUndo:
          type: boolean
        isStalemate:
          type: boolean
        undoToEscape:
          type: integer
        hint:
          $ref: '#/components/schemas/CrazyQuiltHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    TarabishTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    TarabishHint:
      type: object
      description: >-
        A suggestion. During bidding it advises whether to take trump and
        carries no cardIndex; during play it names a card.
      properties:
        cardIndex:
          type: integer
          description: Index into your hand. Absent during bidding.
        reason:
          type: string
          description: >-
            tarabishTakeTrump / tarabishPassTrump while bidding;
            tarabishWinTrick or tarabishFeedPartner (your partner is winning --
            throw points on it) during play.
    TarabishPlayer:
      type: object
      description: One seat at a Tarabish table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        team:
          type: integer
          description: "0 or 1. Seats 0+2 are one partnership, 1+3 the other."
        cardCount:
          type: integer
          description: Hand size. The only hand information exposed for the CPU.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        meldPoints:
          type: integer
          description: Meld total computed from the deal (runs plus bella).
        runLen:
          type: integer
          description: Longest run declared, or 0.
        hasBella:
          type: boolean
          description: Holds the trump K and Q.
        trickCount:
          type: integer
    TarabishResponse:
      type: object
      description: Response payload for /tarabish/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/TarabishPlayer'
        phase:
          type: integer
          description: "0=Bid, 1=Play, 2=RoundEnd, 3=GameEnd"
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        trumpSuit:
          type: integer
        upCard:
          allOf:
            - $ref: '#/components/schemas/Card'
          description: The card turned to propose trump. It joins the dealer's hand once trump is settled.
        trumpTakerIdx:
          type: integer
          description: "Seat that took trump, or -1 before it is settled"
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        scores:
          type: array
          description: Running total per team, index 0 and 1.
          items:
            type: integer
        roundPoints:
          type: array
          description: Points taken this round per team, index 0 and 1.
          items:
            type: integer
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/TarabishTrickCard'
        validPlays:
          type: array
          description: Hand indices you may legally play. Following suit is compulsory.
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: "-1 until decided, and -1 on a tie"
        hint:
          $ref: '#/components/schemas/TarabishHint'
        config:
          type: object
          properties:
            target:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    BalootTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    BalootHint:
      type: object
      description: >-
        A suggestion. While declaring it advises a mode and carries no
        cardIndex, naming the suit when it recommends Hokom; during play it
        names a card.
      properties:
        cardIndex:
          type: integer
          description: Index into your hand. Absent while declaring.
        reason:
          type: string
          description: >-
            balootDeclareSun / balootDeclareHokom / balootPassDeclare while
            declaring; balootWinTrick or balootFeedPartner (your partner is
            winning -- throw points on it) during play.
        suit:
          type: integer
          description: Trump suit to declare when reason is balootDeclareHokom; 0 otherwise.
    BalootPlayer:
      type: object
      description: One seat at a Baloot table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        team:
          type: integer
          description: "0 or 1. Seats 0+2 are one partnership, 1+3 the other."
        cardCount:
          type: integer
          description: Hand size. The only hand information exposed for the CPU.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        hasBaloot:
          type: boolean
          description: Holds the trump K and Q -- worth 20, and possible in Hokom only.
        declared:
          type: boolean
          description: Has already declared or passed this round.
        trickCount:
          type: integer
    BalootResponse:
      type: object
      description: Response payload for /baloot/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/BalootPlayer'
        phase:
          type: integer
          description: "0=Declare, 1=Play, 2=RoundEnd, 3=GameEnd"
        mode:
          type: integer
          description: "0=undeclared, 1=Sun, 2=Hokom. The mode decides the rank order itself."
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        trumpSuit:
          type: integer
          description: Meaningful in Hokom only; 0 under Sun, which has no trump.
        declarerIdx:
          type: integer
          description: "Seat that declared the mode, or -1 before it is settled"
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        scores:
          type: array
          description: Running total per team, index 0 and 1.
          items:
            type: integer
        roundPoints:
          type: array
          description: Points taken this round per team, index 0 and 1.
          items:
            type: integer
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/BalootTrickCard'
        validPlays:
          type: array
          description: Hand indices you may legally play. Following suit is compulsory.
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: "-1 until decided, and -1 on a tie"
        hint:
          $ref: '#/components/schemas/BalootHint'
        config:
          type: object
          properties:
            target:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    EstimationTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    EstimationHint:
      type: object
      description: >-
        A suggestion. While choosing trump or calling it carries no cardIndex
        and puts the recommended suit or number in value; during play it names
        a card.
      properties:
        cardIndex:
          type: integer
          description: Index into your hand. Absent before play begins.
        reason:
          type: string
          description: >-
            estimationSelectTrump / estimationBid / estimationDashCall /
            estimationAvoidRestricted before play; estimationWinTrick or
            estimationDuck (you already have your call) during play.
        value:
          type: integer
          description: Suit to make trump, or the number to call. 0 during play.
    EstimationPlayer:
      type: object
      description: One seat at an Estimation table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
          description: Hand size. The only hand information exposed for the CPU.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        bid:
          type: integer
          description: "Tricks called, or -1 before this seat has called"
        callType:
          type: integer
          description: "0=normal, 1=Dash Call (0), 2=Risk (the highest call)"
        trickCount:
          type: integer
        roundScore:
          type: integer
          description: Change from the round just scored.
        totalScore:
          type: integer
    EstimationResponse:
      type: object
      description: Response payload for /estimation/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/EstimationPlayer'
        phase:
          type: integer
          description: "0=Trump, 1=Bid, 2=Play, 3=RoundEnd, 4=GameEnd"
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        trumpSuit:
          type: integer
          description: 0 until the dealer has chosen.
        restrictedBid:
          type: integer
          description: >-
            The one call the last bidder may not make, because it would bring
            the total to 13; -1 when nobody is under that restriction.
        currentPlayerIdx:
          type: integer
        bidPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/EstimationTrickCard'
        validPlays:
          type: array
          description: Hand indices you may legally play. Following suit is compulsory.
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "-1 until decided, and -1 on a tie"
        hint:
          $ref: '#/components/schemas/EstimationHint'
        config:
          type: object
          properties:
            rounds:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    IsraeliWhistTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    IsraeliWhistHint:
      type: object
      description: >-
        A suggestion. During the auction and the calling round it carries no
        cardIndex and puts the recommended number in value (and the suit in
        suit when it recommends a bid); during play it names a card.
      properties:
        cardIndex:
          type: integer
          description: Index into your hand. Absent before play begins.
        reason:
          type: string
          description: >-
            israeliwhistAuctionBid / israeliwhistAuctionPass in the auction;
            israeliwhistBid / israeliwhistMeetQuota /
            israeliwhistAvoidRestricted when calling; israeliwhistWinTrick or
            israeliwhistDuck during play.
        value:
          type: integer
          description: Tricks to bid or call. 0 during play.
        suit:
          type: integer
          description: Suit to bid in the auction; 0 otherwise.
    IsraeliWhistPlayer:
      type: object
      description: One seat at an Israeli Whist table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
          description: Hand size. The only hand information exposed for the CPU.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        auctionBid:
          type: integer
          description: "Tricks called in the auction, or -1 when this seat never bid"
        auctionSuit:
          type: integer
          description: Suit offered in the auction, or 0.
        passed:
          type: boolean
          description: Dropped out of the auction. Final -- a passed seat cannot bid again.
        bid:
          type: integer
          description: "Target called in the second round, or -1 before this seat has called"
        trickCount:
          type: integer
        roundScore:
          type: integer
          description: Change from the round just scored.
        totalScore:
          type: integer
    IsraeliWhistResponse:
      type: object
      description: Response payload for /israeliwhist/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/IsraeliWhistPlayer'
        phase:
          type: integer
          description: "0=Auction, 1=Bid, 2=Play, 3=RoundEnd, 4=GameEnd"
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        trumpSuit:
          type: integer
          description: 0 until the auction closes.
        declarerIdx:
          type: integer
          description: "Seat that won the auction, or -1 before it closes"
        highBid:
          type: integer
          description: Highest auction call so far; becomes the declarer's quota.
        highSuit:
          type: integer
          description: Suit of the highest auction call.
        minimumBid:
          type: integer
          description: Lowest call you may make -- the quota when you are the declarer, else 0.
        restrictedBid:
          type: integer
          description: >-
            The one call the last bidder may not make, because it would bring
            the total to 13; -1 when nobody is under that restriction.
        currentPlayerIdx:
          type: integer
        auctionPlayerIdx:
          type: integer
        bidPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/IsraeliWhistTrickCard'
        validPlays:
          type: array
          description: Hand indices you may legally play. Following suit is compulsory.
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "-1 until decided, and -1 on a tie"
        hint:
          $ref: '#/components/schemas/IsraeliWhistHint'
        config:
          type: object
          properties:
            rounds:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    HokmTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    HokmHint:
      type: object
      description: >-
        A suggestion. While trump is undeclared it carries no cardIndex and
        puts the recommended suit in suit; during play it names a card.
      properties:
        cardIndex:
          type: integer
          description: Index into your hand. Absent while trump is undeclared.
        reason:
          type: string
          description: >-
            hokmDeclareTrump before play; hokmWinTrick or hokmSaveCards (your
            partner is winning -- keep your high cards) during play.
        suit:
          type: integer
          description: Suit to make trump; 0 during play.
    HokmPlayer:
      type: object
      description: One seat at a Hokm table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        team:
          type: integer
          description: "0 or 1. Seats 0+2 are one partnership, 1+3 the other."
        cardCount:
          type: integer
          description: Hand size. The only hand information exposed for the CPU.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        isHakem:
          type: boolean
          description: Declares trump from their first five cards; keeps the role while winning.
        trickCount:
          type: integer
    HokmResponse:
      type: object
      description: Response payload for /hokm/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/HokmPlayer'
        phase:
          type: integer
          description: "0=Trump, 1=Play, 2=HandEnd, 3=GameEnd"
        handNumber:
          type: integer
        trickNumber:
          type: integer
        trumpSuit:
          type: integer
          description: 0 until the hakem has declared.
        hakemIdx:
          type: integer
        scores:
          type: array
          description: Hand points per team, index 0 and 1.
          items:
            type: integer
        teamTricks:
          type: array
          description: >-
            Tricks taken this hand per team. The hand ends the moment one side
            reaches tricksToWin, so this -- not trickNumber -- is the progress.
          items:
            type: integer
        tricksToWin:
          type: integer
          description: Tricks that take the hand (7).
        lastHandKot:
          type: boolean
          description: The previous hand was a Kot (the losers took no trick), worth 2.
        lastHandWinner:
          type: integer
          description: "Team that took the previous hand, or -1 before there is one"
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/HokmTrickCard'
        validPlays:
          type: array
          description: Hand indices you may legally play. Following suit is compulsory.
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: "-1 until decided, and -1 on a tie"
        hint:
          $ref: '#/components/schemas/HokmHint'
        config:
          type: object
          properties:
            target:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    ShelemTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    ShelemHint:
      type: object
      description: >-
        A suggestion. While bidding or discarding it carries no cardIndex and
        puts the recommended points in value (or the trump suit in suit);
        during play it names a card.
      properties:
        cardIndex:
          type: integer
          description: Index into your hand. Absent before play begins.
        reason:
          type: string
          description: >-
            shelemBid / shelemPass while bidding; shelemDiscard while sorting
            the widow; shelemWinTrick or shelemFeedPartner during play.
        value:
          type: integer
          description: Points to bid. 0 otherwise.
        suit:
          type: integer
          description: Suit to make trump when discarding. 0 otherwise.
    ShelemPlayer:
      type: object
      description: One seat at a Shelem table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        team:
          type: integer
          description: "0 or 1. Seats 0+2 are one partnership, 1+3 the other."
        cardCount:
          type: integer
          description: Hand size. The only hand information exposed for the CPU.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        bid:
          type: integer
          description: "Points bid, or -1 when this seat never bid"
        passed:
          type: boolean
          description: Dropped out of the bidding. Final -- a passed seat cannot bid again.
        declaredShelem:
          type: boolean
          description: Claimed every trick instead of naming a number.
        trickCount:
          type: integer
    ShelemResponse:
      type: object
      description: Response payload for /shelem/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/ShelemPlayer'
        phase:
          type: integer
          description: "0=Bid, 1=Discard, 2=Play, 3=RoundEnd, 4=GameEnd"
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        trumpSuit:
          type: integer
          description: 0 until the declarer has named it.
        declarerIdx:
          type: integer
          description: "Seat that won the bidding, or -1 before it settles"
        contract:
          type: integer
          description: Points contracted for.
        shelemBid:
          type: boolean
          description: The contract is Shelem -- every trick -- rather than a points total.
        minBid:
          type: integer
          description: Lowest bid that would beat the standing one; already clamped to the maximum.
        widowSize:
          type: integer
          description: Cards still face down; 0 once the declarer has taken them.
        discardCount:
          type: integer
          description: How many cards the declarer must discard (4).
        scores:
          type: array
          description: Running total per team, index 0 and 1.
          items:
            type: integer
        roundPoints:
          type: array
          description: Card points taken this round per team, index 0 and 1.
          items:
            type: integer
        teamTricks:
          type: array
          description: Tricks taken this round per team, index 0 and 1.
          items:
            type: integer
        currentPlayerIdx:
          type: integer
        bidPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/ShelemTrickCard'
        validPlays:
          type: array
          description: Hand indices you may legally play. Following suit is compulsory.
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: "-1 until decided, and -1 on a tie"
        hint:
          $ref: '#/components/schemas/ShelemHint'
        config:
          type: object
          properties:
            target:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    MendikotTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    MendikotHint:
      type: object
      description: A suggestion naming a card to play.
      properties:
        cardIndex:
          type: integer
          description: Index into your hand.
        reason:
          type: string
          description: >-
            mendikotChaseTen when a ten is already on the table,
            mendikotFeedPartner when your partner has the trick, or
            mendikotDuck when you cannot win it.
    MendikotPlayer:
      type: object
      description: One seat at a Mendikot table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        team:
          type: integer
          description: "0 or 1. Seats 0+2 are one partnership, 1+3 the other."
        cardCount:
          type: integer
          description: Hand size. The only hand information exposed for the CPU.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        tens:
          type: integer
          description: Tens captured by this seat. The four tens decide the hand.
        trickCount:
          type: integer
    MendikotResponse:
      type: object
      description: Response payload for /mendikot/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/MendikotPlayer'
        phase:
          type: integer
          description: "0=Play, 1=HandEnd, 2=GameEnd. There is no trump phase."
        handNumber:
          type: integer
        trickNumber:
          type: integer
        trumpSuit:
          type: integer
          description: >-
            0 until somebody fails to follow suit -- the card they play sets
            it. There is no separate trump-choosing step.
        trumpChooserIdx:
          type: integer
          description: "Seat whose unfollowable card set trump, or -1 while undecided"
        tensInDeck:
          type: integer
          description: How many tens exist in the deck (4). The hand is a race for these.
        teamTens:
          type: array
          description: Tens captured this hand per team, index 0 and 1.
          items:
            type: integer
        teamTricks:
          type: array
          description: Tricks taken this hand per team. Only decides a two-apiece split.
          items:
            type: integer
        scores:
          type: array
          description: Hand points per team, index 0 and 1.
          items:
            type: integer
        lastHandWinner:
          type: integer
          description: "Team that took the previous hand, or -1 before the first one ends"
        lastHandKind:
          type: string
          description: >-
            How the previous hand was decided: tens, tricks, mendikot (all four
            tens, 2 points) or whitewash (every trick, 3 points). Empty before
            the first hand ends.
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/MendikotTrickCard'
        validPlays:
          type: array
          description: Hand indices you may legally play. Following suit is compulsory.
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: "-1 until decided, and -1 on a tie"
        hint:
          $ref: '#/components/schemas/MendikotHint'
        config:
          type: object
          properties:
            target:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    BhabhiTrickCard:
      type: object
      description: A card sitting on the table
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    BhabhiHint:
      type: object
      description: A suggestion naming a card to play.
      properties:
        cardIndex:
          type: integer
          description: Index into your hand.
        reason:
          type: string
          description: >-
            bhabhiLead when starting a trick, bhabhiDuck when you can follow,
            or bhabhiDumpHigh when you cannot and take the pile regardless.
    BhabhiPlayer:
      type: object
      description: One seat at a Bhabhi table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
          description: Hand size. The only hand information exposed for the CPU.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        rank:
          type: integer
          description: >-
            Order in which this seat emptied its hand, or -1 while still
            holding cards. This is not a ranking by skill -- everyone who gets
            out is safe, and only the last one holding cards loses.
        pickups:
          type: integer
          description: How many times this seat has taken the pile into hand.
    BhabhiResponse:
      type: object
      description: Response payload for /bhabhi/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/BhabhiPlayer'
        phase:
          type: integer
          description: "0=Play, 1=GameEnd. There are no hands to split the game into."
        trickNumber:
          type: integer
          description: Tricks closed so far, by either a completed trick or a pickup.
        leadSuit:
          type: integer
          description: 0 before anyone has led. Following this suit is compulsory.
        pile:
          type: array
          description: Cards on the table. Whoever cannot follow takes all of them.
          items:
            $ref: '#/components/schemas/BhabhiTrickCard'
        lastPickupIdx:
          type: integer
          description: "Seat that last took the pile, or -1 before the first pickup"
        lastPickupSize:
          type: integer
          description: How many cards that pickup was worth.
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        validPlays:
          type: array
          description: Hand indices you may legally play. Following suit is compulsory.
          items:
            type: integer
        aliveCount:
          type: integer
          description: How many seats still hold cards.
        gameEndFlag:
          type: boolean
        bhabhiIdx:
          type: integer
          description: "The loser, or -1 until decided. This game names a loser, not a winner."
        stalemate:
          type: boolean
          description: >-
            The game was called as deadlocked rather than played out. Cards
            only leave play on a trick everyone followed, so a distribution
            where somebody is always void can circulate forever; at
            stalemateTricks the seat holding the most cards becomes the Bhabhi.
        stalemateTricks:
          type: integer
          description: The trick count at which a deadlock is called.
        hint:
          $ref: '#/components/schemas/BhabhiHint'
        config:
          type: object
          properties:
            playerCnt:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    TeenDoPaanchTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    TeenDoPaanchHint:
      type: object
      description: >-
        A suggestion. While choosing trump it carries no cardIndex and puts the
        recommended suit in suit; during play it names a card.
      properties:
        cardIndex:
          type: integer
          description: Index into your hand. Absent before play begins.
        reason:
          type: string
          description: >-
            teendopaanchSelectTrump before play; teendopaanchWinTrick or
            teendopaanchDuck (you already have your target) during play.
        suit:
          type: integer
          description: Suit to make trump. 0 during play.
    TeenDoPaanchPlayer:
      type: object
      description: One seat at a 3-2-5 table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
          description: Hand size. The only hand information exposed for the CPU.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        target:
          type: integer
          description: >-
            Tricks this seat owes this round: 3, 2 or 5. Assigned, not bid --
            there is no bidding phase, and the targets rotate between rounds.
        trickCount:
          type: integer
        met:
          type: integer
          description: Rounds in which this seat made its target. The game is decided on this.
    TeenDoPaanchResponse:
      type: object
      description: Response payload for /teendopaanch/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/TeenDoPaanchPlayer'
        phase:
          type: integer
          description: "0=Trump, 1=Play, 2=RoundEnd, 3=GameEnd"
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        trumpSuit:
          type: integer
          description: 0 until the five-target seat has chosen.
        fivePlayerIdx:
          type: integer
          description: Seat that owes 5 tricks. That seat, and only it, names trump.
        lastExchange:
          type: integer
          description: >-
            How many cards changed hands for last round's shortfall. Surplus
            tricks buy an opponent's best cards; nothing else shows this.
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/TeenDoPaanchTrickCard'
        validPlays:
          type: array
          description: Hand indices you may legally play. Following suit is compulsory.
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "-1 until decided, and -1 on a tie"
        hint:
          $ref: '#/components/schemas/TeenDoPaanchHint'
        config:
          type: object
          properties:
            rounds:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    HasenpfefferTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    HasenpfefferHint:
      type: object
      description: >-
        A suggestion. While bidding it carries no cardIndex and puts the
        recommended number in value; while discarding it names both a card and
        the suit to make trump; during play it names a card.
      properties:
        cardIndex:
          type: integer
          description: Index into your hand. Absent while bidding.
        reason:
          type: string
          description: >-
            hasenpfefferBid / hasenpfefferPass / hasenpfefferMustBid while
            bidding; hasenpfefferDiscard while sorting the blind;
            hasenpfefferWinTrick or hasenpfefferFeedPartner during play.
        value:
          type: integer
          description: Tricks to bid. 0 otherwise.
        suit:
          type: integer
          description: Suit to make trump when discarding. 0 otherwise.
    HasenpfefferPlayer:
      type: object
      description: One seat at a Hasenpfeffer table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        team:
          type: integer
          description: "0 or 1. Seats 0+2 are one partnership, 1+3 the other."
        cardCount:
          type: integer
          description: Hand size. The only hand information exposed for the CPU.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        bid:
          type: integer
          description: "Tricks bid, 0 if this seat passed, or -1 before it answered"
        trickCount:
          type: integer
    HasenpfefferResponse:
      type: object
      description: Response payload for /hasenpfeffer/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/HasenpfefferPlayer'
        phase:
          type: integer
          description: "0=Bid, 1=Discard, 2=Play, 3=HandEnd, 4=GameEnd"
        handNumber:
          type: integer
        trickNumber:
          type: integer
        trumpSuit:
          type: integer
          description: 0 until the declarer names it along with the discard.
        declarerIdx:
          type: integer
          description: "Seat that won the bidding, or -1 while the auction runs"
        contract:
          type: integer
          description: Tricks contracted for.
        minBid:
          type: integer
          description: >-
            Lowest bid that would beat the standing one. 0 means nobody can bid
            any more -- the maximum is standing, so passing is the only move.
        mustBid:
          type: boolean
          description: >-
            You are the dealer and everybody else passed, so you cannot pass.
            Bidding is compulsory; a hand never gets thrown in.
        blindSize:
          type: integer
          description: Cards still face down; 0 once the declarer has taken the blind.
        scores:
          type: array
          description: Running total per team, index 0 and 1.
          items:
            type: integer
        teamTricks:
          type: array
          description: Tricks taken this hand per team, index 0 and 1.
          items:
            type: integer
        lastHandEuchred:
          type: boolean
          description: The declaring side failed its contract in the hand just scored.
        lastHandTricks:
          type: integer
          description: Tricks the declaring side took in the hand just scored.
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/HasenpfefferTrickCard'
        validPlays:
          type: array
          description: Hand indices you may legally play. Following suit is compulsory.
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: "-1 until decided, and -1 on a tie"
        hint:
          $ref: '#/components/schemas/HasenpfefferHint'
        config:
          type: object
          properties:
            target:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    SergeantMajorTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    SergeantMajorHint:
      type: object
      description: >-
        A suggestion. While choosing trump it names a suit; while discarding it
        names several cards in indices; during play it names one card.
      properties:
        cardIndex:
          type: integer
          description: Index into your hand. Absent outside the play phase.
        reason:
          type: string
          description: >-
            sergeantmajorSelectTrump before play; sergeantmajorDiscard while
            sorting the kitty; sergeantmajorWinTrick or sergeantmajorPressOn
            (target already met) during play.
        suit:
          type: integer
          description: Suit to make trump. 0 otherwise.
        indices:
          type: array
          description: Cards to discard. Empty outside the discard phase.
          items:
            type: integer
    SergeantMajorPlayer:
      type: object
      description: One seat at a Sergeant Major table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
          description: Hand size. The only hand information exposed for the CPU.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        target:
          type: integer
          description: >-
            Tricks this seat owes this round: 8, 5 or 3. Fixed by seat, never
            bid -- the dealer owes 8, the seat to their left 5, the next 3.
        trickCount:
          type: integer
        score:
          type: integer
          description: Running total of (tricks - target). The game is decided on this.
    SergeantMajorResponse:
      type: object
      description: Response payload for /sergeantmajor/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/SergeantMajorPlayer'
        phase:
          type: integer
          description: "0=Trump, 1=Discard, 2=Play, 3=RoundEnd, 4=GameEnd"
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        trumpSuit:
          type: integer
          description: 0 until the dealer has chosen.
        kittySize:
          type: integer
          description: >-
            Cards still face down. 52 does not divide by three, so the four
            left over go to the dealer, who discards discardCount after naming
            trump.
        discardCount:
          type: integer
          description: How many cards the dealer must discard (4).
        lastExchange:
          type: integer
          description: >-
            How many cards changed hands for last round's shortfall. Falling
            short of your target costs your best cards; nothing else shows it.
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
          description: The dealer. This seat owes 8 and names trump.
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/SergeantMajorTrickCard'
        validPlays:
          type: array
          description: Hand indices you may legally play. Following suit is compulsory.
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "-1 until decided, and -1 on a tie"
        hint:
          $ref: '#/components/schemas/SergeantMajorHint'
        config:
          type: object
          properties:
            rounds:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    HoneymoonBridgeTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    HoneymoonBridgeHint:
      type: object
      description: >-
        A suggestion. During the auction it names a contract in level/suit and
        carries no cardIndex; while playing it names a card.
      properties:
        cardIndex:
          type: integer
          description: Index into your hand. Absent during the auction.
        reason:
          type: string
          description: >-
            honeymoonbridgeDraw in the draw phase (those tricks do not score);
            honeymoonbridgeBid or honeymoonbridgePass during the auction;
            honeymoonbridgeWinTrick while playing the contract.
        level:
          type: integer
          description: Level to bid, 0 to pass. 0 outside the auction.
        suit:
          type: integer
          description: Denomination to bid; 0 is no-trump.
    HoneymoonBridgePlayer:
      type: object
      description: One of the two seats at a Honeymoon Bridge table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
          description: Hand size. The only hand information exposed for the CPU.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        bidLevel:
          type: integer
          description: "This seat's latest bid level, or 0 for a pass"
        bidSuit:
          type: integer
          description: "This seat's latest bid denomination; 0 is no-trump"
        trickCount:
          type: integer
        score:
          type: integer
    HoneymoonBridgeResponse:
      type: object
      description: Response payload for /honeymoonbridge/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/HoneymoonBridgePlayer'
        phase:
          type: integer
          description: "0=Draw, 1=Bid, 2=Play, 3=RoundEnd, 4=GameEnd"
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        stockSize:
          type: integer
          description: >-
            Cards left in the stock: 26 at the deal, two fewer after each
            draw-phase trick, and 0 from the auction onwards.
        trumpSuit:
          type: integer
          description: 0 means no-trump -- during the draw phase and for a no-trump contract.
        declarerIdx:
          type: integer
          description: "-1 until a contract is bought"
        contractLevel:
          type: integer
          description: 0 until a contract is bought.
        requiredTricks:
          type: integer
          description: Tricks the contract needs (6 + contractLevel). 0 before the auction.
        minBidLevel:
          type: integer
          description: >-
            The lowest bid that still outbids the table, so a client need not
            rederive the auction's order (no-trump above diamonds). 0 when
            nothing can outbid the table -- pass is the only move -- and 0
            outside the auction.
        minBidSuit:
          type: integer
          description: Denomination that goes with minBidLevel; 0 is no-trump.
        lastMade:
          type: boolean
          description: Whether the last deal's contract was made.
        lastTricks:
          type: integer
          description: Tricks the declarer took in the last deal.
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/HoneymoonBridgeTrickCard'
        validPlays:
          type: array
          description: Hand indices you may legally play. Following suit is compulsory.
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "-1 until decided"
        hint:
          $ref: '#/components/schemas/HoneymoonBridgeHint'
        config:
          type: object
          properties:
            target:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    MinibridgeTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    MinibridgeHint:
      type: object
      description: >-
        A suggestion. While choosing the contract it names one in level/suit
        and carries no cardIndex; while playing it names a card.
      properties:
        cardIndex:
          type: integer
          description: Index into the hand being controlled. Absent before play begins.
        reason:
          type: string
          description: >-
            minibridgeContract before play; minibridgeWinTrick while playing
            your own hand, minibridgeDummy when the card named is in the
            dummy's.
        level:
          type: integer
          description: Level to choose. 0 outside the contract phase.
        suit:
          type: integer
          description: Denomination to choose; 0 is no-trump.
    MinibridgePlayer:
      type: object
      description: One seat at a Minibridge table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
          description: Hand size. Reported for every seat; the cards themselves are not.
        cards:
          type: array
          description: >-
            Populated for the human player, and for the dummy once the contract
            is set. Empty otherwise.
          items:
            $ref: '#/components/schemas/Card'
        hcp:
          type: integer
          description: >-
            High-card points this seat announced. **With no auction this is the
            game's only public information**, and the four seats always add up
            to exactly 40.
        team:
          type: integer
          description: "0 or 1. Seats 0 and 2 are one pair, 1 and 3 the other."
        trickCount:
          type: integer
    MinibridgeResponse:
      type: object
      description: Response payload for /minibridge/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/MinibridgePlayer'
        phase:
          type: integer
          description: "0=Contract, 1=Play, 2=RoundEnd, 3=GameEnd"
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        contractLevel:
          type: integer
          description: 0 until the declarer has chosen.
        contractSuit:
          type: integer
          description: 0 means no-trump, which is a choice rather than "unset".
        requiredTricks:
          type: integer
          description: Tricks the contract needs (6 + contractLevel). 0 before it is chosen.
        declarerIdx:
          type: integer
          description: Decided from the announced HCP as soon as the cards are dealt.
        dummyIdx:
          type: integer
          description: The declarer's partner. Their hand goes face up once the contract is set.
        dummyHand:
          type: array
          description: The dummy's hand. Empty until the contract is chosen.
          items:
            $ref: '#/components/schemas/Card'
        lastMade:
          type: boolean
          description: Whether the last deal's contract was made.
        lastTricks:
          type: integer
          description: Tricks the declaring side took in the last deal.
        teamScores:
          type: array
          description: Running totals, indexed by team.
          items:
            type: integer
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/MinibridgeTrickCard'
        validPlays:
          type: array
          description: >-
            Hand indices you may legally play, **for the seat you are
            controlling** -- the dummy's when it is the dummy's turn and you
            are the declarer. Following suit is compulsory.
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: "-1 until decided, and -1 on a tie"
        hint:
          $ref: '#/components/schemas/MinibridgeHint'
        config:
          type: object
          properties:
            rounds:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    PasurHint:
      type: object
      description: >-
        A suggestion. table names the table indices to take, and is empty when
        the advice is to lay the card down instead.
      properties:
        cardIndex:
          type: integer
          description: Index into your hand.
        reason:
          type: string
          description: pasurSoor, pasurCapture, or pasurTrail.
        table:
          type: array
          description: Table indices to take. Empty for a lay-down.
          items:
            type: integer
    PasurPlayer:
      type: object
      description: One seat at a Pasur table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
          description: Hand size. The only hand information exposed for the CPU.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        capturedCount:
          type: integer
          description: Cards taken so far, across both the normal and the soor pile.
        soors:
          type: integer
          description: How many times this seat emptied the table. Those captures score double.
        score:
          type: integer
    PasurResponse:
      type: object
      description: Response payload for /pasur/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/PasurPlayer'
        phase:
          type: integer
          description: "0=Play, 1=GameEnd"
        table:
          type: array
          description: The cards face up on the table. Their positions are the `table` indices.
          items:
            $ref: '#/components/schemas/Card'
        captureOptions:
          type: array
          description: >-
            Per hand card, the sets of table indices it may take -- so a client
            never has to rebuild "which subsets add to 11", which would drift
            from the server. An empty array for a card means it can only be
            laid down.
          items:
            type: array
            items:
              type: array
              items:
                type: integer
        deckRemaining:
          type: integer
        packsDealt:
          type: integer
        lastCaptureIdx:
          type: integer
          description: >-
            The seat that captured last, or -1. **Cards left on the table at
            the end go to them.**
        currentPlayerIdx:
          type: integer
        gameEndFlag:
          type: boolean
        winners:
          type: array
          description: >-
            Seats on the highest score. More than one entry means a tie; empty
            until the game ends.
          items:
            type: integer
        hint:
          $ref: '#/components/schemas/PasurHint'
        config:
          type: object
          properties:
            playerCnt:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    SnapHint:
      type: object
      description: A suggestion -- whether to call right now, and why.
      properties:
        snap:
          type: boolean
          description: true when a call would be correct this instant.
        reason:
          type: string
          description: snapDeclare, snapStep, or snapWait.
    SnapPlayer:
      type: object
      description: One seat at a Snap table. Stocks are face down -- only the count is public.
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        stockSize:
          type: integer
    SnapResponse:
      type: object
      description: Response payload for /snap/exec
      properties:
        phase:
          type: integer
          description: "0=Play, 1=GameEnd"
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "-1 until decided, and -1 when play simply could not continue"
        currentTurnIdx:
          type: integer
        isHumanTurn:
          type: boolean
        snapAvailable:
          type: boolean
          description: >-
            Whether a call would be correct right now. **True only when the top
            two cards match** -- a single card showing can never be snapped.
        centerPileSize:
          type: integer
        topCard:
          $ref: '#/components/schemas/Card'
        players:
          type: array
          items:
            $ref: '#/components/schemas/SnapPlayer'
        playerCnt:
          type: integer
        cpuDifficulty:
          type: integer
          description: "0=easy, 1=normal, 2=hard"
        pendingKind:
          type: integer
          description: "0=none, 1=a CPU has booked a call, 2=a CPU will turn a card"
        pendingDeadlineMs:
          type: integer
          format: int64
          description: >-
            When the booked action fires. A human who calls before this wins
            the pile.
        lastEventKind:
          type: integer
          description: "0=none, 1=step, 2=correct call, 3=wrong call, 4=out of stock"
        lastEventPlayerIdx:
          type: integer
        hint:
          $ref: '#/components/schemas/SnapHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    RollingStoneTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    RollingStoneHint:
      type: object
      description: A suggestion. Carries no cardIndex when the only move is to pick up.
      properties:
        cardIndex:
          type: integer
          description: Index into your hand.
        reason:
          type: string
          description: rollingstoneLead, rollingstoneFollow, or rollingstonePickUp.
    RollingStonePlayer:
      type: object
      description: One seat at a Rolling Stone table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
          description: Hand size. **Fewer is better** -- running out first is how you win.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        pickups:
          type: integer
          description: How many tricks this seat has been forced to take into hand.
        finishedAt:
          type: integer
          description: Finishing place, or 0 while still holding cards.
    RollingStoneResponse:
      type: object
      description: Response payload for /rollingstone/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/RollingStonePlayer'
        phase:
          type: integer
          description: "0=Play, 1=GameEnd"
        mustPickUp:
          type: boolean
          description: >-
            Whether you cannot follow and must take the trick into your hand.
            **Read this before validPlays** -- an empty validPlays also means
            "not your turn", and the two call for opposite things.
        validPlays:
          type: array
          description: Hand indices you may legally play. Following suit is compulsory.
          items:
            type: integer
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/RollingStoneTrickCard'
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        trickNumber:
          type: integer
          description: Tricks resolved so far. Pickups count too -- they end a trick as well.
        lastPickupIdx:
          type: integer
          description: The seat that just took a trick into hand, or -1.
        finishedCnt:
          type: integer
        deckSize:
          type: integer
          description: "Cards in this deal: players x 8, so 32 / 40 / 48."
        discarded:
          type: integer
          description: Cards out of play. **Only a completed trick removes any.**
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: >-
            -1 until decided. A winner still holding cards means the 500-trick
            stalemate was reached rather than anyone running out.
        hint:
          $ref: '#/components/schemas/RollingStoneHint'
        config:
          type: object
          properties:
            playerCnt:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    GoofspielHint:
      type: object
      description: A suggestion.
      properties:
        cardIndex:
          type: integer
          description: Index into your bid hand.
        reason:
          type: string
          description: goofspielMatch, goofspielHighPrize, goofspielLowPrize, or goofspielCarried.
    GoofspielPlayer:
      type: object
      description: One seat at a Goofspiel table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
          description: Bid cards not yet spent.
        cards:
          type: array
          description: >-
            The remaining bid cards. **Populated for every seat, CPUs included**
            -- spent cards are public, so a remainder is just a suit minus what
            has been played.
          items:
            $ref: '#/components/schemas/Card'
        score:
          type: integer
        hasBid:
          type: boolean
          description: Whether this seat has committed a bid this round.
        revealedBid:
          allOf:
            - $ref: '#/components/schemas/Card'
          description: The bid, present only after the reveal.
    BotifarraTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    ColourWhistTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    CrazyFourPokerResponse:
      type: object
      description: Response payload for /crazyfourpoker/exec
      properties:
        phase:
          type: integer
          description: "0=Bet, 1=Decide, 2=Result"
        playerHand:
          type: array
          description: Always an array. Five cards; four of them play.
          items:
            $ref: '#/components/schemas/Card'
        dealerHand:
          type: array
          description: Always an array. Empty until the showdown.
          items:
            $ref: '#/components/schemas/Card'
        playerBest:
          type: array
          description: Always an array.
          items:
            $ref: '#/components/schemas/Card'
        dealerBest:
          type: array
          description: Always an array. Empty until the showdown.
          items:
            $ref: '#/components/schemas/Card'
        playerHandRank:
          type: integer
          description: "1=High Card .. 8=Four of a Kind"
        dealerHandRank:
          type: integer
          description: Zero until the showdown.
        hasAcesOrBetter:
          type: boolean
        maxMultiplier:
          type: integer
          description: 1, or 3 with a pair of aces or better. Do not re-derive client-side.
        playerQualifies:
          type: boolean
          description: Whether the player's own four is king high or better. Server-computed so clients need not re-derive it.
        dealerQualifies:
          type: boolean
        anteBet:
          type: integer
        superBet:
          type: integer
          description: Always equal to the ante.
        queensUpBet:
          type: integer
        playBet:
          type: integer
        playMultiplier:
          type: integer
        result:
          type: integer
          description: "0=none, 1=fold, 2=win, 3=lose, 4=push, 5=dealer did not qualify"
        payout:
          type: integer
          description: Total returned this round, including returned stakes.
        chips:
          type: integer
        minTotalWager:
          type: integer
        roundNumber:
          type: integer
        remainingCards:
          type: integer
        gameEndFlag:
          type: boolean
        config:
          type: object
          properties:
            initialChips:
              type: integer
            defaultAnte:
              type: integer
        message:
          type: string
        messageCode:
          type: string
    ChemindeFerResponse:
      type: object
      description: Response payload for /chemindefer/exec
      properties:
        players:
          type: array
          description: Always an array. Six seats; the bank travels between them.
          items:
            type: object
            properties:
              id:
                type: integer
              name:
                type: string
              isHuman:
                type: boolean
              chips:
                type: integer
              bet:
                type: integer
              isBanker:
                type: boolean
              isRepresentative:
                type: boolean
        phase:
          type: integer
          description: "0=Stake, 1=Bet, 2=PunterDraw, 3=BankerDraw, 4=RoundEnd"
        bankerIdx:
          type: integer
        betTurn:
          type: integer
          description: Seat due to bet, or -1 once betting has closed.
        stake:
          type: integer
        remainingStake:
          type: integer
        totalBet:
          type: integer
        stakeMin:
          type: integer
        stakeMax:
          type: integer
        betMin:
          type: integer
        betMax:
          type: integer
          description: Zero once betting has closed.
        representativeIdx:
          type: integer
          description: Highest bettor, who decides the punter side. -1 until the deal.
        punterMayChoose:
          type: boolean
          description: True only at a punter total of 5.
        bankerHand:
          type: array
          description: Always an array.
          items:
            $ref: '#/components/schemas/Card'
        punterHand:
          type: array
          description: Always an array.
          items:
            $ref: '#/components/schemas/Card'
        bankerTotal:
          type: integer
        punterTotal:
          type: integer
        punterDrew:
          type: boolean
        result:
          type: integer
          description: "0=undecided, 1=banker, 2=punters, 3=tie"
        roundNumber:
          type: integer
        remainingCards:
          type: integer
        isHumanTurn:
          type: boolean
        gameEndFlag:
          type: boolean
        config:
          type: object
          properties:
            rounds:
              type: integer
            initialChips:
              type: integer
        message:
          type: string
        messageCode:
          type: string
    ColourWhistResponse:
      type: object
      description: Response payload for /colourwhist/exec
      properties:
        players:
          type: array
          description: Always an array. Only the human seat carries its cards.
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              trickCount:
                type: integer
              score:
                type: integer
                description: Running score. Negative values are normal - scoring is zero-sum.
              isDeclarerSide:
                type: boolean
                description: Sides are set by the contract, not by seat.
              hasPassed:
                type: boolean
        phase:
          type: integer
          description: "0=Bid, 1=Call, 2=Play, 3=RoundEnd, 4=GameEnd"
        validPlays:
          type: array
          items:
            type: integer
        dealerIdx:
          type: integer
        contract:
          type: integer
          description: "0=none, 1=Samen, 2=Alleen, 3=Miserie, 4=Troel"
        declarerIdx:
          type: integer
        partnerIdx:
          type: integer
          description: -1 until known. Troel names it at deal time; Samen hides it until the called card is played.
        calledCard:
          allOf:
            - $ref: '#/components/schemas/Card'
          description: The card called to find a partner. Samen only - troel is dealt, not called.
        trumpSuit:
          type: integer
          description: "1..4, or -1 for no trump (Miserie)"
        troelForced:
          type: boolean
          description: True when three aces in one hand forced the contract without an auction.
        currentTurn:
          type: integer
        isHumanTurn:
          type: boolean
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/ColourWhistTrickCard'
        lastTrick:
          type: array
          items:
            $ref: '#/components/schemas/ColourWhistTrickCard'
        lastTrickWinner:
          type: integer
        trickCount:
          type: integer
        declarerTricks:
          type: integer
        roundNumber:
          type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
        config:
          type: object
          properties:
            rounds:
              type: integer
        message:
          type: string
        messageCode:
          type: string
    RikkenTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    RikkenResponse:
      type: object
      description: Response payload for /rikken/exec
      properties:
        players:
          type: array
          description: Always an array. Only the human seat carries its cards.
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              trickCount:
                type: integer
              score:
                type: integer
                description: Running score. Negative values are normal - scoring is zero-sum.
              isDeclarerSide:
                type: boolean
                description: Sides are set by the contract, not by seat.
              hasPassed:
                type: boolean
        phase:
          type: integer
          description: "0=Bid, 1=Call, 2=Play, 3=RoundEnd, 4=GameEnd"
        validPlays:
          type: array
          items:
            type: integer
        dealerIdx:
          type: integer
        contract:
          type: integer
          description: "0=none, 1=Rik, 2=Misere, 3=Solo, 4=Open Misere"
        declarerIdx:
          type: integer
          description: -1 until the auction resolves.
        partnerIdx:
          type: integer
          description: -1 until the called card is played.
        calledCard:
          allOf:
            - $ref: '#/components/schemas/Card'
          description: The ace called to find a partner. Rik only.
        trumpSuit:
          type: integer
          description: "1..4, or -1 for no trump (the Misere contracts)"
        currentTurn:
          type: integer
        isHumanTurn:
          type: boolean
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/RikkenTrickCard'
        lastTrick:
          type: array
          items:
            $ref: '#/components/schemas/RikkenTrickCard'
        lastTrickWinner:
          type: integer
        trickCount:
          type: integer
        declarerTricks:
          type: integer
        roundNumber:
          type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
        config:
          type: object
          properties:
            rounds:
              type: integer
        message:
          type: string
        messageCode:
          type: string
    BotifarraResponse:
      type: object
      description: Response payload for /botifarra/exec
      properties:
        players:
          type: array
          description: Always an array. Only the human seat carries its cards.
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              team:
                type: integer
                description: "0 = seats 0 and 2, 1 = seats 1 and 3"
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              trickCount:
                type: integer
        phase:
          type: integer
          description: "0=Declare, 1=Delegated, 2=Double, 3=Play, 4=RoundEnd, 5=GameEnd"
        validPlays:
          type: array
          description: Hand indices you may play. Often a strict subset - you must win the trick if you can.
          items:
            type: integer
        dealerIdx:
          type: integer
        declarerIdx:
          type: integer
          description: -1 until trump is named.
        trumpSuit:
          type: integer
          description: "1=spades, 2=clubs, 3=hearts, 4=diamonds, -1=no trump"
        multiplier:
          type: integer
          description: "1, 2 (contrar) or 4 (recontrar)"
        currentTurn:
          type: integer
        isHumanTurn:
          type: boolean
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/BotifarraTrickCard'
        lastTrick:
          type: array
          items:
            $ref: '#/components/schemas/BotifarraTrickCard'
        lastTrickWinner:
          type: integer
        trickCount:
          type: integer
        roundPoints:
          type: array
          description: Points taken this round per team. They sum to 72 once the round is dealt out.
          items:
            type: integer
        scores:
          type: array
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: -1 until decided.
        config:
          type: object
          properties:
            targetScore:
              type: integer
            allowDoubling:
              type: boolean
        message:
          type: string
        messageCode:
          type: string
    AndarBaharResponse:
      type: object
      description: Response payload for /andarbahar/exec
      properties:
        joker:
          allOf:
            - $ref: '#/components/schemas/Card'
          description: The reference card. Its colour decides which column is dealt first.
        andarCards:
          type: array
          description: Cards dealt to Andar. Always an array, never null.
          items:
            $ref: '#/components/schemas/Card'
        baharCards:
          type: array
          description: Cards dealt to Bahar. Always an array, never null.
          items:
            $ref: '#/components/schemas/Card'
        firstColumn:
          type: integer
          description: "Column dealt first, and the one paying 0.9:1. 0=Andar, 1=Bahar"
        dealtCount:
          type: integer
          description: Cards dealt before the round was decided.
        phase:
          type: integer
          description: "1=Bet, 2=End"
        chips:
          type: integer
        betAmount:
          type: integer
        betTarget:
          type: integer
          description: "0=Andar, 1=Bahar"
        sideAmount:
          type: integer
        sideBand:
          type: integer
          description: -1 when no side bet was placed.
        winner:
          type: integer
          description: Column that matched the joker. -1 until decided.
        result:
          type: integer
        payout:
          type: integer
          description: Total returned to the player, stake included.
        history:
          type: array
          description: Winning column per round. Always an array, never null.
          items:
            type: integer
        message:
          type: string
        messageCode:
          type: string
    GoofspielResponse:
      type: object
      description: Response payload for /goofspiel/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/GoofspielPlayer'
        phase:
          type: integer
          description: "0=Bid, 1=Reveal, 2=GameEnd"
        validPlays:
          type: array
          description: Hand indices you may bid. Empty once you have committed this round.
          items:
            type: integer
        currentPrize:
          allOf:
            - $ref: '#/components/schemas/Card'
          description: The prize on the table, absent once the round is settled.
        carriedPrizes:
          type: array
          description: Prizes carried over by earlier ties.
          items:
            $ref: '#/components/schemas/Card'
        prizeValue:
          type: integer
          description: Points at stake this round, **including any carry-over**.
        prizeRemaining:
          type: integer
        lastWinnerIdx:
          type: integer
          description: The seat that won the last round, or -1 when it was a tie.
        lastGained:
          type: integer
          description: Points moved last round. 0 on a tie.
        roundNumber:
          type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: -1 until decided.
        hint:
          $ref: '#/components/schemas/GoofspielHint'
        config:
          type: object
          properties:
            playerCnt:
              type: integer
            tieRule:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    CucumberTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    CucumberHint:
      type: object
      description: A suggestion.
      properties:
        cardIndex:
          type: integer
          description: Index into your hand.
        reason:
          type: string
          description: cucumberLead, cucumberBeat, or cucumberForced.
    CucumberPlayer:
      type: object
      description: One seat at a Cucumber table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        penalty:
          type: integer
          description: Penalty points. **Fewer is better** -- this is the only score.
    CucumberResponse:
      type: object
      description: Response payload for /cucumber/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/CucumberPlayer'
        phase:
          type: integer
          description: "0=Play, 1=RoundEnd, 2=GameEnd"
        validPlays:
          type: array
          description: >-
            Hand indices you may legally play. You must beat highestInTrick if
            you can; if you cannot, this holds exactly your lowest card.
          items:
            type: integer
        highestInTrick:
          type: integer
          description: The rank to beat, or 0 while nobody has led. Suits are irrelevant.
        forced:
          type: boolean
          description: >-
            Whether your lowest card is the only legal play. **This is not the
            same as validPlays having one entry** -- a hand can hold exactly one
            card that does beat the trick.
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/CucumberTrickCard'
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        trickNumber:
          type: integer
        roundNumber:
          type: integer
        lastTrickWinnerIdx:
          type: integer
          description: The seat that took the last trick of the previous round, or -1.
        lastPenalty:
          type: integer
          description: Penalty scored for that last trick.
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: -1 until decided.
        hint:
          $ref: '#/components/schemas/CucumberHint'
        config:
          type: object
          properties:
            playerCnt:
              type: integer
            targetScore:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    StealingBundlesHint:
      type: object
      description: A suggestion. victimIdx is -1 unless the advice is to steal.
      properties:
        cardIndex:
          type: integer
          description: Index into your hand.
        victimIdx:
          type: integer
          description: Seat to steal from, or -1.
        reason:
          type: string
          description: stealingbundlesSteal, stealingbundlesTake, or stealingbundlesTrail.
    StealingBundlesPlayer:
      type: object
      description: One seat at a Stealing Bundles table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        bundleSize:
          type: integer
          description: Cards won so far. **This is the score.**
        bundleTop:
          allOf:
            - $ref: '#/components/schemas/Card'
          description: >-
            The top card of this seat's bundle, absent while it is empty.
            **Public for every seat** -- it is the one thing opponents aim at.
    StealingBundlesResponse:
      type: object
      description: Response payload for /stealingbundles/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/StealingBundlesPlayer'
        phase:
          type: integer
          description: "0=Play, 1=GameEnd"
        tableCards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        tableMatches:
          type: object
          description: Hand index (as a string key) to the table positions it can capture.
          additionalProperties:
            type: array
            items:
              type: integer
        stealTargets:
          type: object
          description: Hand index (as a string key) to the seats whose bundle it can steal.
          additionalProperties:
            type: array
            items:
              type: integer
        canCapture:
          type: boolean
          description: >-
            Whether any capture is available. **You may only trail when this is
            false** -- taking is compulsory when something can be taken.
        deckRemaining:
          type: integer
        lastCaptureIdx:
          type: integer
          description: The seat that captured last, or -1. It receives whatever is left on the table.
        currentPlayerIdx:
          type: integer
        turnNumber:
          type: integer
        packsDealt:
          type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: -1 until decided.
        hint:
          $ref: '#/components/schemas/StealingBundlesHint'
        config:
          type: object
          properties:
            playerCnt:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    PigHint:
      type: object
      description: A suggestion. Carries no cardIndex while a signal is out.
      properties:
        cardIndex:
          type: integer
          description: Index into your hand.
        reason:
          type: string
          description: pigSignal, pigDiscardOdd, or pigNoSingleton.
    PigPlayer:
      type: object
      description: One seat at a Pig table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
          description: Hand size. **Always 4** -- everyone passes and receives at the same time.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        letters:
          type: integer
          description: Letters collected. Three puts you out.
        letterWord:
          type: string
          description: 'The letters themselves: "", "P", "PI", or "PIG".'
        eliminated:
          type: boolean
        hasSignalled:
          type: boolean
          description: Whether this seat has noticed the signal in the current round.
        noticedOrder:
          type: integer
          description: The order in which this seat noticed (1-based, 0 = not yet).
        hasChosenPass:
          type: boolean
          description: Whether this seat has chosen its card. Everyone passes at once.
    PigResponse:
      type: object
      description: Response payload for /pig/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/PigPlayer'
        phase:
          type: integer
          description: "0=Pass, 1=Signal, 2=RoundEnd, 3=GameEnd"
        validPlays:
          type: array
          description: Hand indices you may pass. Empty once you have chosen this round.
          items:
            type: integer
        signallerIdx:
          type: integer
          description: >-
            The seat that put a hand to its nose, or -1. **Nothing on the board
            shows this** -- a signal is silent, so this field is the only way a
            client can surface it.
        noticedCnt:
          type: integer
          description: How many seats have noticed, including the signaller.
        roundLoserIdx:
          type: integer
          description: The seat that took a letter at the end of the last round, or -1.
        roundNumber:
          type: integer
        passCount:
          type: integer
          description: Passes made this round.
        deckSize:
          type: integer
          description: "Cards in play: players x 4, so 12 / 16 / 20 / 24."
        currentPlayerIdx:
          type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: -1 until decided.
        hint:
          $ref: '#/components/schemas/PigHint'
        config:
          type: object
          properties:
            playerCnt:
              type: integer
            cpuDifficulty:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    LingerLongerTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    LingerLongerHint:
      type: object
      description: A suggestion. Always names a card; there is only ever one kind of move.
      properties:
        cardIndex:
          type: integer
          description: Index into your hand.
        reason:
          type: string
          description: lingerlongerWinTrick, lingerlongerNoStock, or lingerlongerDuck.
    LingerLongerPlayer:
      type: object
      description: One seat at a Linger Longer table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
          description: Hand size. **More is better** -- running out is how you go out.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        tricksWon:
          type: integer
          description: Tricks won. **Not a score** -- it is the number of refills earned.
        eliminatedAt:
          type: integer
          description: Elimination order, or 0 while still holding cards.
    LingerLongerResponse:
      type: object
      description: Response payload for /lingerlonger/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/LingerLongerPlayer'
        phase:
          type: integer
          description: "0=Play, 1=GameEnd"
        validPlays:
          type: array
          description: Hand indices you may legally play. Following suit is compulsory.
          items:
            type: integer
        stockSize:
          type: integer
          description: >-
            Cards left in the stock. **At 0 nobody can refill any more**, so
            from here every trick strictly shrinks the table.
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/LingerLongerTrickCard'
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        trickNumber:
          type: integer
          description: Tricks resolved so far.
        lastDrawIdx:
          type: integer
          description: The seat that just drew a replacement, or -1.
        eliminatedCnt:
          type: integer
          description: How many seats have run out so far.
        discarded:
          type: integer
          description: Cards out of play. **Only a completed trick removes any.**
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: -1 until decided.
        hint:
          $ref: '#/components/schemas/LingerLongerHint'
        config:
          type: object
          properties:
            playerCnt:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    RamsTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    RamsHint:
      type: object
      description: >-
        A suggestion. In the decision phase it advises whether to enter and
        carries no cardIndex; during play it names a card.
      properties:
        cardIndex:
          type: integer
          description: Index into your hand. Absent during the decision phase.
        reason:
          type: string
          description: >-
            ramsPlayIn / ramsPassOut in the decision phase; ramsTakeTrick (no
            trick banked yet, so avoid the extra payment) or ramsAlreadySafe
            (one trick banked) during play.
    RamsPlayer:
      type: object
      description: One seat at a Rams table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
          description: Hand size. The only hand information exposed for the CPU.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        chips:
          type: integer
          description: Chips held. HIGHER is better -- this decides the game.
        inRound:
          type: boolean
          description: Entered this round (chose to play rather than drop).
        decided:
          type: boolean
          description: Has chosen play or drop for this round.
        roundTricks:
          type: integer
          description: Tricks taken this round. Zero while inRound costs an extra payment.
        trickCount:
          type: integer
    RamsResponse:
      type: object
      description: Response payload for /rams/exec
      properties:
        players:
          type: array
          description: Length is 3, 4 or 5 depending on the configured table size.
          items:
            $ref: '#/components/schemas/RamsPlayer'
        phase:
          type: integer
          description: "0=Decide, 1=Play, 2=RoundEnd, 3=GameEnd"
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        pot:
          type: integer
          description: Chips in the pot, shared out by tricks taken.
        trumpSuit:
          type: integer
          description: Suit of the card turned after the deal.
        upCard:
          allOf:
            - $ref: '#/components/schemas/Card'
          description: The card turned to fix trump.
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        activeCount:
          type: integer
          description: How many players entered this round.
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/RamsTrickCard'
        validPlays:
          type: array
          description: Hand indices you may legally play. Following suit is compulsory.
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "-1 until decided, and -1 on a tie"
        hint:
          $ref: '#/components/schemas/RamsHint'
        config:
          type: object
          properties:
            playerCnt:
              type: integer
            rounds:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    ReversisTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    ReversisHint:
      type: object
      description: A suggested card to play
      properties:
        cardIndex:
          type: integer
          description: Index into your hand
        reason:
          type: string
          description: >-
            Why that card: reversisAvoidMarked (the Quinola or the A of
            diamonds is on the table), reversisAvoidPoints (plain penalty cards
            are), reversisLeadSafe (leading; play a low card worth nothing), or
            reversisDumpHigh (safe trick -- shed a high card now).
    ReversisPlayer:
      type: object
      description: One seat at a Reversis table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
          description: Hand size. The only hand information exposed for the CPU.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        chips:
          type: integer
          description: Chips held. HIGHER is better -- this decides the game.
        roundPenalty:
          type: integer
          description: Penalty points taken this round. LOWER is better -- it wins the pool.
        trickCount:
          type: integer
        tookQuinola:
          type: boolean
          description: Took the J of hearts (+5 penalty, 5 chips to the pool).
        tookDiamondAce:
          type: boolean
          description: Took the A of diamonds (+5 penalty, 5 chips to the pool).
    ReversisResponse:
      type: object
      description: Response payload for /reversis/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/ReversisPlayer'
        phase:
          type: integer
          description: "0=Play, 1=RoundEnd, 2=GameEnd"
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        pool:
          type: integer
          description: Chips in the pool, taken whole by the fewest penalty points.
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/ReversisTrickCard'
        validPlays:
          type: array
          description: Hand indices you may legally play. Following suit is compulsory.
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "-1 until decided, and -1 on a tie"
        hint:
          $ref: '#/components/schemas/ReversisHint'
        config:
          type: object
          properties:
            rounds:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    PolignacTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    PolignacHint:
      type: object
      description: A suggested card to play
      properties:
        cardIndex:
          type: integer
          description: Index into your hand
        reason:
          type: string
          description: >-
            Why that card: polignacAvoidJack (a jack is on the table),
            polignacDumpJack (safe trick -- shed a jack now), polignacLeadSafe
            (leading; play a low non-jack), or polignacBlockCapot (take this
            trick to break a declared capot).
    PolignacPlayer:
      type: object
      description: One seat at a Polignac table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
          description: Hand size. The only hand information exposed for the CPU.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        score:
          type: integer
          description: Running penalty total across rounds. LOWER is better.
        roundPenalty:
          type: integer
          description: Penalty taken in the current round.
        trickCount:
          type: integer
        declaredCapot:
          type: boolean
          description: Declared capot this round.
    PolignacResponse:
      type: object
      description: Response payload for /polignac/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/PolignacPlayer'
        phase:
          type: integer
          description: "0=Declare, 1=Play, 2=RoundEnd, 3=GameEnd"
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        capotIdx:
          type: integer
          description: "Seat that declared capot, or -1 when nobody did"
        capotTricks:
          type: integer
          description: Tricks the capot declarer has taken so far.
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/PolignacTrickCard'
        validPlays:
          type: array
          description: Hand indices you may legally play. Following suit is compulsory.
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "-1 until decided, and -1 on a tie"
        hint:
          $ref: '#/components/schemas/PolignacHint'
        config:
          type: object
          properties:
            rounds:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    SlobberhannesTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    SlobberhannesHint:
      type: object
      description: A suggested card to play
      properties:
        cardIndex:
          type: integer
          description: Index into your hand
        reason:
          type: string
          description: >-
            Why that card: slobberhannesAvoid (this trick carries a penalty),
            slobberhannesDump (safe trick -- shed a high card now), or
            slobberhannesLeadLow (leading; play low and see what happens).
    SlobberhannesPlayer:
      type: object
      description: One seat at a Slobberhannes table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
          description: Hand size. The only hand information exposed for the CPU.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        score:
          type: integer
          description: Running total across rounds. Penalties are negative, the clean-round bonus positive.
        trickCount:
          type: integer
        tookFirstTrick:
          type: boolean
          description: Took this round's first trick (-1).
        tookLastTrick:
          type: boolean
          description: Took this round's last trick (-1).
        tookQueen:
          type: boolean
          description: Took the trick holding the Q of clubs (-1).
    SlobberhannesResponse:
      type: object
      description: Response payload for /slobberhannes/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/SlobberhannesPlayer'
        phase:
          type: integer
          description: "0=Play, 1=RoundEnd, 2=GameEnd"
        roundNumber:
          type: integer
        trickNumber:
          type: integer
          description: 0-based. Tricks 0 and 7 carry the position penalties.
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/SlobberhannesTrickCard'
        validPlays:
          type: array
          description: Hand indices you may legally play. Following suit is compulsory.
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "-1 until decided, and -1 on a tie"
        hint:
          $ref: '#/components/schemas/SlobberhannesHint'
        config:
          type: object
          properties:
            rounds:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    GermanWhistTrickCard:
      type: object
      description: A card played in the current trick
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
          description: Index of the player who played this card
        card:
          $ref: '#/components/schemas/Card'
    GermanWhistHint:
      type: object
      description: A suggested card to play in German Whist
      properties:
        cardIndex:
          type: integer
          description: Index into your hand
        reason:
          type: string
          description: >-
            Why that card. The aim inverts between the halves:
            germanWhistTakeUpCard (win this trick, the face-up card is worth
            having), germanWhistDuck (lose on purpose, it is not), or
            germanWhistWinTrick (second half -- every trick scores).
    GermanWhistPlayer:
      type: object
      description: One seat at a German Whist table
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
          description: Hand size. The only hand information exposed for the CPU.
        cards:
          type: array
          description: Populated for the human player only; empty for the CPU.
          items:
            $ref: '#/components/schemas/Card'
        trickCount:
          type: integer
          description: Total tricks taken, both halves.
        scoringTricks:
          type: integer
          description: Tricks taken in the SECOND half only -- the ones that decide the game.
    GermanWhistResponse:
      type: object
      description: Response payload for /germanwhist/exec
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/GermanWhistPlayer'
        phase:
          type: integer
          description: "0=Draw (first 13 tricks, no score), 1=Scoring (second 13), 2=GameEnd"
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/GermanWhistTrickCard'
        trumpSuit:
          type: integer
          description: Suit of the first face-up card; trump for the whole hand.
        upCard:
          allOf:
            - $ref: '#/components/schemas/Card'
          description: The face-up card the first half is played for. Absent once the stock is exhausted.
        stockCount:
          type: integer
        validPlays:
          type: array
          description: Hand indices you may legally play. Following suit is compulsory.
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: "-1 until decided"
        hint:
          $ref: '#/components/schemas/GermanWhistHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    RoyalCotillionHint:
      type: object
      description: A suggested move in Royal Cotillion
      properties:
        fromZone:
          type: string
          description: "tableau, reserve, waste or stock"
        fromIdx:
          type: integer
          description: Slot or reserve index, or -1 when the source is the waste or the stock
        toZone:
          type: string
          description: "foundation, tableau or waste"
        toIdx:
          type: integer
          description: Destination index, or -1 when the destination is the waste
    RoyalCotillionResponse:
      type: object
      description: Response payload for /royalcotillion/exec
      properties:
        tableau:
          type: array
          description: The sixteen slots, one card each. An empty slot is null.
          items:
            $ref: '#/components/schemas/Card'
        reserve:
          type: array
          description: The four reserve piles. Only the last card of each is playable, and an emptied pile is never refilled.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        foundation:
          type: array
          description: The eight foundations, each building by twos and wrapping through all thirteen ranks.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        foundationOdd:
          type: array
          description: Per-foundation series; true starts from the Ace, false from the deuce.
          items:
            type: boolean
        stockCount:
          type: integer
        waste:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "0=Playing, 1=GameClear, 2=GameOver"
        moveCount:
          type: integer
        canUndo:
          type: boolean
        isStalemate:
          type: boolean
        undoToEscape:
          type: integer
        hint:
          $ref: '#/components/schemas/RoyalCotillionHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    DiplomatHint:
      type: object
      description: A suggested move in Diplomat
      properties:
        fromZone:
          type: string
          description: "tableau, waste or stock"
        fromIdx:
          type: integer
          description: Column index, or -1 when the source is the waste or the stock
        toZone:
          type: string
          description: "foundation, tableau or waste"
        toIdx:
          type: integer
          description: Destination index, or -1 when the destination is the waste
    DiplomatResponse:
      type: object
      description: Response payload for /diplomat/exec
      properties:
        tableau:
          type: array
          description: The eight columns. Only the last card of each is playable.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        foundation:
          type: array
          description: The eight foundations, each building up in suit from the Ace.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        stockCount:
          type: integer
        waste:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "0=Playing, 1=GameClear, 2=GameOver"
        moveCount:
          type: integer
        canUndo:
          type: boolean
        isStalemate:
          type: boolean
        undoToEscape:
          type: integer
        hint:
          $ref: '#/components/schemas/DiplomatHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    CribbageSquaresHint:
      type: object
      description: The cell the server suggests for the card in hand
      properties:
        row:
          type: integer
          description: Suggested row (0..3)
        col:
          type: integer
          description: Suggested column (0..3)
        score:
          type: integer
          description: Points the placement adds to its row and column
        synergy:
          type: boolean
          description: True when the placement scores anything at all
    CribbageSquaresResponse:
      type: object
      description: Response payload for /cribbagesquares/exec
      properties:
        board:
          type: array
          description: The 4x4 grid, row-major. Each cell holds a card or null.
          items:
            type: array
            items:
              type: object
              properties:
                card:
                  $ref: '#/components/schemas/Card'
        currentCard:
          $ref: '#/components/schemas/Card'
        starter:
          allOf:
            - $ref: '#/components/schemas/Card'
          description: The 17th card. Null until all 16 cells are filled.
        placedCount:
          type: integer
        phase:
          type: integer
          description: "0=Playing, 1=Complete"
        canUndo:
          type: boolean
        rowScores:
          type: array
          items:
            type: integer
        colScores:
          type: array
          items:
            type: integer
        rowDetails:
          type: array
          items:
            $ref: '#/components/schemas/CribbageSquaresScore'
        colDetails:
          type: array
          items:
            $ref: '#/components/schemas/CribbageSquaresScore'
        totalScore:
          type: integer
        winScore:
          type: integer
          description: The total needed to clear the game (61).
        isWin:
          type: boolean
        hint:
          $ref: '#/components/schemas/CribbageSquaresHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    ColoradoHint:
      type: object
      description: A suggested move in Colorado
      properties:
        fromZone:
          type: string
          description: "tableau, waste or stock"
        fromIdx:
          type: integer
          description: Tableau pile index, or -1 when the source is the waste or the stock
        toZone:
          type: string
          description: "foundation, tableau or waste"
        toIdx:
          type: integer
          description: Destination index, or -1 when the destination is the waste
    ColoradoResponse:
      type: object
      description: Response payload for /colorado/exec
      properties:
        tableau:
          type: array
          description: The twenty tableau piles. Only the last card of each is playable, and only to a foundation.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        foundation:
          type: array
          description: The eight foundations. Indices 0-3 build up in suit from the Ace, 4-7 build down in suit from the King.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        foundationAscending:
          type: array
          description: Per-foundation build direction; true builds up from the Ace, false builds down from the King.
          items:
            type: boolean
        stockCount:
          type: integer
        waste:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "0=Playing, 1=GameClear, 2=GameOver"
        moveCount:
          type: integer
        canUndo:
          type: boolean
        hint:
          $ref: '#/components/schemas/ColoradoHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
    FourSeasonsHint:
      type: object
      description: Recommended move for Four Seasons
      properties:
        fromZone:
          type: string
          enum: [waste, tableau]
        fromIdx:
          type: integer
          description: Cross pile index, or -1 when the source is the waste
        toZone:
          type: string
          enum: [foundation]
        toIdx:
          type: integer
          description: Target corner index (0..3)
    FourSeasonsResponse:
      type: object
      description: Response payload for /fourseasons/exec
      properties:
        tableau:
          type: array
          description: The five cross piles. Only the last card of each is playable.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        foundation:
          type: array
          description: The four corner foundations, each building up in suit from baseRank.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        stockCount:
          type: integer
        waste:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        baseRank:
          type: integer
          description: The rank the foundations start from, fixed by the first card of this deal (1..13).
        phase:
          type: integer
          description: "0=Playing, 1=GameClear, 2=GameOver"
        moveCount:
          type: integer
        canUndo:
          type: boolean
        hint:
          $ref: '#/components/schemas/FourSeasonsHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string


    AuldLangSyneMoveZone:
      type: object
      description: Source or target zone for an Auld Lang Syne card move
      required:
        - zone
      properties:
        zone:
          type: string
          description: Zone identifier (waste, foundation)
          enum: [waste, foundation]
        idx:
          type: integer
          description: Pile index (0..3)
    AuldLangSyneHint:
      type: object
      description: Recommended move for Auld Lang Syne
      properties:
        wasteIdx:
          type: integer
          description: Source waste pile index (0..3)
        foundationIdx:
          type: integer
          description: Target foundation index (0..3)
    AuldLangSyneResponse:
      type: object
      description: Response payload for /auldlangsyne/exec
      properties:
        foundations:
          type: array
          description: Four foundation piles indexed 0..3, each building A to K ignoring suit.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        wastes:
          type: array
          description: Four waste piles indexed 0..3. Only the last element of each is playable.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        stockCount:
          type: integer
          description: Cards left in the stock. Always a multiple of four, so stockCount/4 deals remain.
        phase:
          type: integer
          description: "0=Playing, 1=GameClear, 2=GameOver"
        moveCount:
          type: integer
        canUndo:
          type: boolean
        isStalemate:
          type: boolean
        undoToEscape:
          type: integer
          description: Number of undos needed to escape stalemate (0 = not stalemate, -1 = unreachable)
        hint:
          $ref: '#/components/schemas/AuldLangSyneHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string


    # -------------------------------------------------------------------------
    # Spite and Malice
    # -------------------------------------------------------------------------
    SpiteAndMaliceMoveZone:
      type: object
      description: Source or target zone for a Spite & Malice card move/discard.
      required:
        - zone
      properties:
        zone:
          type: string
          description: Zone identifier
          enum: [hand, goal, side, foundation]
        idx:
          type: integer
          description: Pile or hand index (required for hand/side/foundation; ignored for goal)
    SpiteAndMaliceHint:
      type: object
      description: Recommended move for Spite & Malice
      properties:
        source:
          type: string
          enum: [goal, hand, side]
        index:
          type: integer
          description: Source-pile index (-1 when source is goal)
        foundationIdx:
          type: integer
          description: Target foundation index (0..3) — for `discard=true` this is the target side pile index instead.
        discard:
          type: boolean
          description: If true, the recommendation is to discard `hand[index]` to side[foundationIdx] (ends turn).
    SpiteAndMalicePlayer:
      type: object
      description: One Spite & Malice player's view. Hand entries may be `null`
        when the opponent's cards are hidden.
      properties:
        hand:
          type: array
          items:
            nullable: true
            allOf:
              - $ref: '#/components/schemas/Card'
        goalTop:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Card'
        goalSize:
          type: integer
        sides:
          type: array
          description: Four side piles indexed 0..3.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        isCpu:
          type: boolean
    SpiteAndMaliceResponse:
      type: object
      description: Response payload for /spiteandmalice/exec
      properties:
        phase:
          type: integer
          description: "0=Playing, 1=GameOver"
        current:
          type: integer
          description: Current player index (0=human, 1=cpu)
        players:
          type: array
          minItems: 2
          maxItems: 2
          items:
            $ref: '#/components/schemas/SpiteAndMalicePlayer'
        foundations:
          type: array
          description: Up to 4 shared foundation piles.
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        foundationTops:
          type: array
          description: Effective top value for each foundation (0 if empty).
          items:
            type: integer
        stockSize:
          type: integer
        completedSize:
          type: integer
        moveCount:
          type: integer
        winner:
          type: integer
          description: -1 while playing, 0 if human won, 1 if CPU won
        goalSize:
          type: integer
          description: Goal pile size at game start (configurable)
        cpuDifficulty:
          type: integer
          description: 0=easy, 1=normal, 2=hard
        hint:
          $ref: '#/components/schemas/SpiteAndMaliceHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    MonteCarloRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `remove` (`m`, `move`),
            `deal` (`d`), `undo` (`u`), `giveup` (`g`), `hint` (`h`), `log` (`l`).
          enum: [r, reset, m, move, remove, d, deal, u, undo, g, giveup, h, hint, log, l]
          example: remove
        fromR:
          type: integer
          description: First-cell row (0-4) for the `remove` command.
          minimum: 0
          maximum: 4
        fromC:
          type: integer
          description: First-cell column (0-4) for the `remove` command.
          minimum: 0
          maximum: 4
        toR:
          type: integer
          description: Second-cell row (0-4) for the `remove` command.
          minimum: 0
          maximum: 4
        toC:
          type: integer
          description: Second-cell column (0-4) for the `remove` command.
          minimum: 0
          maximum: 4
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    MonteCarloOutputCard:
      type: object
      description: A single cell in the 5x5 Monte Carlo board (empty when `card` is null).
      properties:
        card:
          description: Card on this cell, or null if the cell is empty (gap awaiting compression).
          $ref: '#/components/schemas/Card'

    MonteCarloHint:
      type: object
      description: Recommended next move - either a remove pair or a deal call.
      required:
        - action
      properties:
        action:
          type: string
          description: "Recommended action: 'remove' for a pair, 'deal' to compress + refill."
          enum: [remove, deal]
        fromR:
          type: integer
          description: First-cell row for `remove`.
        fromC:
          type: integer
          description: First-cell column for `remove`.
        toR:
          type: integer
          description: Second-cell row for `remove`.
        toC:
          type: integer
          description: Second-cell column for `remove`.

    MonteCarloResponse:
      type: object
      description: Current state of the Monte Carlo Solitaire game
      required:
        - board
        - phase
        - stockCount
        - removedCount
        - dealCount
        - canUndo
        - isStalemate
        - message
      properties:
        board:
          type: array
          description: "5x5 grid of cells (row-major). Empty cells (post-removal, pre-deal) have `card: null`."
          items:
            type: array
            items:
              $ref: '#/components/schemas/MonteCarloOutputCard'
        phase:
          type: integer
          description: "Game phase: 0 = Playing, 1 = GameClear, 2 = GameOver"
          enum: [0, 1, 2]
          example: 0
        stockCount:
          type: integer
          description: Cards remaining in the stock (0-27 after a fresh reset).
          minimum: 0
          maximum: 52
        removedCount:
          type: integer
          description: Cards removed from the board so far (must reach 52 to win).
          minimum: 0
          maximum: 52
        dealCount:
          type: integer
          description: Number of times the player has called `deal`.
          minimum: 0
        canUndo:
          type: boolean
          description: Whether the undo action is available.
        isStalemate:
          type: boolean
          description: True when no remove pair exists and `deal` cannot change the board.
        hint:
          $ref: '#/components/schemas/MonteCarloHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    ContractRummyRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: Game command. Accepted values include `reset`, `drawstock`, `drawdiscard`, `meldcontract`, `meldextra`, `layoff`, `discard`, `nextround`, `log`, plus their short aliases.
          enum: [r, reset, ds, drawstock, dd, drawdiscard, mc, meldcontract, me, meldextra, lo, layoff, d, discard, nr, nextround, log, l]
          example: drawstock
        cardIndex:
          type: integer
          description: Index in the human player's hand (used by `discard` and `layoff`).
        cardIndices:
          type: array
          description: Indices in the human player's hand (used by `meldextra`).
          items:
            type: integer
        indicesPerSlot:
          type: array
          description: Outer index = contract slot; inner array = card indices to fill that slot. Used by `meldcontract`.
          items:
            type: array
            items:
              type: integer
        targetPlayerIdx:
          type: integer
          description: Target player index for `layoff`.
        meldIdx:
          type: integer
          description: Target meld index on the target player for `layoff`.
        config:
          type: object
          description: Optional config to apply on `reset`.
          properties:
            cpuDifficulty:
              type: integer
              description: 0=Easy, 1=Normal, 2=Hard.
            failContractPenalty:
              type: integer
              description: Extra penalty added when a player ends a round without meeting the contract.
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    ContractRummyContractSlot:
      type: object
      description: One slot of the round contract.
      properties:
        kind:
          type: integer
          description: 0=set (same rank), 1=run (same suit consecutive).
        size:
          type: integer
          description: Number of cards required to fill this slot.

    ContractRummyMeld:
      type: object
      description: A meld laid on the table.
      properties:
        cards:
          type: array
          items:
            $ref: '#/components/schemas/Card'

    ContractRummyPlayerOutput:
      type: object
      description: Player state for Contract Rummy.
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
        cards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        melds:
          type: array
          items:
            $ref: '#/components/schemas/ContractRummyMeld'
        contractMet:
          type: boolean
        roundScore:
          type: integer
        cumulativeScore:
          type: integer

    ContractRummyResponse:
      type: object
      description: Current state of the Contract Rummy game.
      required:
        - players
        - phase
        - roundNumber
        - totalRounds
        - currentPlayerIdx
        - drawPileCount
        - gameEndFlag
        - winnerIdx
        - roundWinnerIdx
        - contractSlots
        - config
        - message
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/ContractRummyPlayerOutput'
        phase:
          type: integer
          description: 0=Draw, 1=Play, 2=RoundEnd, 3=GameEnd.
        roundNumber:
          type: integer
        totalRounds:
          type: integer
        currentPlayerIdx:
          type: integer
        discardTop:
          $ref: '#/components/schemas/Card'
        drawPileCount:
          type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
        roundWinnerIdx:
          type: integer
        contractSlots:
          type: array
          items:
            $ref: '#/components/schemas/ContractRummyContractSlot'
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            failContractPenalty:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    # -------------------------------------------------------------------------
    # Kalooki
    # -------------------------------------------------------------------------
    KalookiRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: Game command. Accepted values include `reset`, `drawstock`, `drawdiscard`, `meld`, `layoff`, `discard`, `nextround`, `log`, plus their short aliases.
          enum: [r, reset, ds, drawstock, dd, drawdiscard, m, meld, lo, layoff, d, discard, nr, nextround, log]
          example: drawstock
        cardIndex:
          type: integer
          description: Index in the human player's hand (used by `discard` and `layoff`).
        meldGroups:
          type: array
          description: Outer index = one meld group; inner array = card indices in the human player's hand forming that meld. Used by `meld`.
          items:
            type: array
            items:
              type: integer
        targetPlayerIdx:
          type: integer
          description: Target player index for `layoff` (may be any player after opening).
        meldIdx:
          type: integer
          description: Target meld index on the target player for `layoff`.
        config:
          type: object
          description: Optional config to apply on `reset`.
          properties:
            playerCount:
              type: integer
              description: Total number of players (2-4).
            openingPoints:
              type: integer
              description: Minimum point total required to open with the first meld(s) (default 51).
            cpuDifficulty:
              type: integer
              description: 0=Easy, 1=Normal, 2=Hard.
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    KalookiMeld:
      type: object
      description: A meld laid on the table.
      properties:
        cards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        hasJoker:
          type: boolean
          description: Whether the meld contains a joker (joker melds score 1.5x).

    KalookiPlayerOutput:
      type: object
      description: Player state for Kalooki.
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
        cards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        melds:
          type: array
          items:
            $ref: '#/components/schemas/KalookiMeld'
        opened:
          type: boolean
          description: Whether the player has met the opening threshold.
        roundScore:
          type: integer
        cumulativeScore:
          type: integer

    KalookiResponse:
      type: object
      description: Current state of the Kalooki game.
      required:
        - players
        - phase
        - currentPlayerIdx
        - drawPileCount
        - gameEndFlag
        - winnerIdx
        - roundWinnerIdx
        - config
        - message
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/KalookiPlayerOutput'
        phase:
          type: integer
          description: 0=Draw, 1=Meld, 2=RoundEnd, 3=GameEnd.
        roundNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        discardTop:
          $ref: '#/components/schemas/Card'
        drawPileCount:
          type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
        roundWinnerIdx:
          type: integer
        openingPoints:
          type: integer
          description: Minimum point total required to open (default 51).
        config:
          type: object
          properties:
            playerCount:
              type: integer
            openingPoints:
              type: integer
            cpuDifficulty:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    # -------------------------------------------------------------------------
    # Ultimate Texas Hold'em
    # -------------------------------------------------------------------------
    UltimateTexasHoldemRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `bet` (`b`),
            `play` (`p`), `check` (`c`), `fold` (`f`), `log` (`l`).
          enum: [r, reset, b, bet, p, play, c, check, f, fold, log, l]
          example: bet
        amount:
          type: integer
          description: |
            Ante bet amount. Required for `bet` command (minimum 10, must be a
            multiple of 10). The Blind is set to the same value automatically.
          minimum: 10
          example: 100
        tripsBet:
          type: integer
          description: |
            Trips side bet amount. Optional; used with the `bet` command. 0 or
            omitted means no side bet.
          minimum: 0
          example: 20
        multiplier:
          type: integer
          description: |
            Play bet multiplier (in units of Ante). Used with the `play`
            command. Pre-flop accepts 3 or 4, flop accepts 2, river accepts 1.
          enum: [1, 2, 3, 4]
          example: 4
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    UltimateTexasHoldemResponse:
      type: object
      description: Current state of the Ultimate Texas Hold'em game
      required:
        - playerHand
        - dealerHand
        - community
        - phase
        - chips
        - anteBet
        - blindBet
        - tripsBet
        - playBet
        - folded
        - result
        - dealerQualified
        - antePayout
        - blindPayout
        - playPayout
        - tripsPayout
        - totalPayout
        - playerHandRank
        - dealerHandRank
        - message
      properties:
        playerHand:
          type: array
          description: Player's two hole cards
          items:
            $ref: '#/components/schemas/Card'
        dealerHand:
          type: array
          description: Dealer's two hole cards (masked until showdown)
          items:
            $ref: '#/components/schemas/Card'
        community:
          type: array
          description: Community cards (length grows from 0 to 5 as streets advance)
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 1=Bet, 2=PreFlop, 3=Flop, 4=River, 5=End"
          enum: [1, 2, 3, 4, 5]
          example: 1
        chips:
          type: integer
          description: Current chip count
          example: 1000
        anteBet:
          type: integer
          description: Ante bet amount
          example: 100
        blindBet:
          type: integer
          description: Blind bet amount (always equal to anteBet)
          example: 100
        tripsBet:
          type: integer
          description: Trips side bet amount
          example: 0
        playBet:
          type: integer
          description: Play bet amount (0 / 1x / 2x / 3x / 4x Ante)
          example: 0
        folded:
          type: boolean
          description: Whether the player folded at the river
          example: false
        result:
          type: integer
          description: "Game result: 1=Player wins, -1=Dealer wins, 0=Push/not resolved"
          enum: [-1, 0, 1]
          example: 0
        dealerQualified:
          type: boolean
          description: Whether the dealer qualified (pair or better)
          example: false
        antePayout:
          type: integer
          description: Ante payout (returned amount + profit)
          example: 0
        blindPayout:
          type: integer
          description: Blind payout (returned amount + profit per paytable, only when player wins with Straight+)
          example: 0
        playPayout:
          type: integer
          description: Play bet payout (returned amount + profit)
          example: 0
        tripsPayout:
          type: integer
          description: Trips side bet payout (returned amount + profit per paytable)
          example: 0
        totalPayout:
          type: integer
          description: Sum of antePayout + blindPayout + playPayout + tripsPayout
          example: 0
        playerHandRank:
          type: integer
          description: Player's best 5-card hand rank
          example: 0
        dealerHandRank:
          type: integer
          description: Dealer's best 5-card hand rank
          example: 0
        message:
          type: string
          description: Human-readable status message (empty when no event)
          example: ''
        messageCode:
          type: string
          description: i18n code for the message
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Mississippi Stud
    # -------------------------------------------------------------------------
    MississippiStudRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `bet` (`b`),
            `play` (`p`), `fold` (`f`), `log` (`l`).
          enum: [r, reset, b, bet, p, play, f, fold, log, l]
          example: bet
        amount:
          type: integer
          description: Ante amount. Required for `bet` (minimum 10). Ignored for other commands.
          minimum: 10
          example: 100
        multiplier:
          type: integer
          description: Street bet multiplier (1, 2, or 3). Required for `play`. Ignored for other commands.
          enum: [1, 2, 3]
          example: 3
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    MississippiStudResponse:
      type: object
      description: Current state of the Mississippi Stud game
      required:
        - playerHand
        - communityCards
        - communityRevealed
        - phase
        - chips
        - anteAmount
        - streetMultipliers
        - folded
        - totalBet
        - result
        - handRank
        - payoutMultiplier
        - antePayout
        - streetPayouts
        - totalPayout
        - message
      properties:
        playerHand:
          type: array
          description: The player's hole cards (2 cards once dealt)
          items:
            $ref: '#/components/schemas/Card'
        communityCards:
          type: array
          description: |
            Community cards (3 cards). Each card is masked (`{design: '', value: 0}`)
            until its street is revealed; on game end every card is revealed.
          items:
            $ref: '#/components/schemas/Card'
        communityRevealed:
          type: array
          description: Per-card reveal state for `communityCards` (length 3)
          items:
            type: boolean
        phase:
          type: integer
          description: 'Game phase: 1 = Ante, 2 = 3rd Street, 3 = 4th Street, 4 = 5th Street, 5 = End'
          enum: [1, 2, 3, 4, 5]
          example: 1
        chips:
          type: integer
          description: Current chip balance
          example: 1000
        anteAmount:
          type: integer
          description: Ante placed for the current round (0 before bet)
          example: 100
        streetMultipliers:
          type: array
          description: 3rd / 4th / 5th street bet multipliers (length 3, values 0/1/2/3)
          items:
            type: integer
            enum: [0, 1, 2, 3]
        folded:
          type: boolean
          description: True if the player folded during the round
          example: false
        totalBet:
          type: integer
          description: Total chips committed to the round (ante + all street bets)
          example: 100
        result:
          type: integer
          description: 'Game result: 1 = Player wins, -1 = Player loses, 0 = Push / not resolved'
          enum: [-1, 0, 1]
          example: 0
        handRank:
          type: integer
          description: |
            Final 5-card hand rank
            (0 = High Card, 1 = One Pair, 2 = Two Pair, 3 = Three of a Kind,
            4 = Straight, 5 = Flush, 6 = Full House, 7 = Four of a Kind,
            8 = Straight Flush, 9 = Royal Flush). 0 until resolution.
          example: 0
        payoutMultiplier:
          type: integer
          description: Applied paytable multiplier (-1 = push, 0 = loss, positive = win)
          example: 0
        antePayout:
          type: integer
          description: Payout from the ante portion
          example: 0
        streetPayouts:
          type: array
          description: Payouts from each street bet (length 3)
          items:
            type: integer
        totalPayout:
          type: integer
          description: Total payout returned to the player
          example: 0
        message:
          type: string
          description: Human-readable status message
          example: ''
        messageCode:
          type: string
          description: i18n code for the message
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # Belote
    # -------------------------------------------------------------------------
    BeloteRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `orderup` (`o`),
            `pass` (`pa`), `calltrump` (`c`), `play` (`p`), `next` (`n`),
            `nextround` (`nr`), `hint` (`h`), `log` (`l`).
          enum: [r, reset, o, orderup, pa, pass, c, calltrump, p, play, n, next, nr, nextround, h, hint, l, log]
          example: orderup
        suit:
          type: integer
          description: Trump suit for `calltrump` (1=Spade, 2=Clover, 3=Heart, 4=Diamond). Must differ from the face-up card's suit.
          enum: [1, 2, 3, 4]
          example: 2
        cardIndex:
          type: integer
          description: Index into the player's hand for `play`. Required for `play`.
          minimum: 0
          example: 0
        config:
          $ref: '#/components/schemas/BeloteConfig'
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    BeloteConfig:
      type: object
      description: Belote game configuration
      properties:
        cpuDifficulty:
          type: integer
          description: 'CPU difficulty: 0=Easy, 1=Normal, 2=Hard'
          enum: [0, 1, 2]
          example: 1
        targetScore:
          type: integer
          description: Score required to win the game (default 1000)
          minimum: 1
          example: 1000
        dixDeDer:
          type: integer
          description: Bonus points awarded to the winner of the last trick (default 10)
          minimum: 0
          example: 10
        enableBeloteRebelote:
          type: boolean
          description: Enable +20 bonus for K+Q of trumps in one hand
          example: true

    BelotePlayer:
      type: object
      description: One Belote player's public state
      required:
        - id
        - isHuman
        - cardCount
        - cards
        - team
        - trickCount
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
        cards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        team:
          type: integer
          enum: [0, 1]
        trickCount:
          type: integer

    BeloteTrickCard:
      type: object
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
        card:
          $ref: '#/components/schemas/Card'

    BeloteHint:
      type: object
      required:
        - reason
      properties:
        cardIndex:
          type: integer
        orderUp:
          type: boolean
        suit:
          type: integer
        reason:
          type: string

    BeloteResponse:
      type: object
      description: Current state of the Belote game
      required:
        - players
        - phase
        - roundNumber
        - trickNumber
        - currentPlayerIdx
        - bidPlayerIdx
        - dealerIdx
        - trumpSuit
        - makerTeam
        - makerPlayerIdx
        - currentTrick
        - teamScores
        - roundPoints
        - roundBeloteBonus
        - gameEndFlag
        - winnerTeam
        - leadPlayerIdx
        - message
        - config
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/BelotePlayer'
        phase:
          type: integer
          description: 'Game phase: 0=BidPickUp, 1=BidCallTrump, 2=Play, 3=TrickEnd, 4=RoundEnd, 5=GameEnd'
          enum: [0, 1, 2, 3, 4, 5]
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        bidPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        trumpSuit:
          type: integer
          enum: [0, 1, 2, 3, 4]
        faceUpCard:
          nullable: true
          allOf:
            - $ref: '#/components/schemas/Card'
        makerTeam:
          type: integer
        makerPlayerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/BeloteTrickCard'
        teamScores:
          type: array
          items:
            type: integer
        roundPoints:
          type: array
          items:
            type: integer
        roundBeloteBonus:
          type: array
          items:
            type: integer
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
        leadPlayerIdx:
          type: integer
        hint:
          $ref: '#/components/schemas/BeloteHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
        config:
          $ref: '#/components/schemas/BeloteConfig'

    # -------------------------------------------------------------------------
    # Mighty
    # -------------------------------------------------------------------------
    MightyRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `bid` (`b`),
            `trump` (`t`), `exchange` (`e`), `play` (`p`),
            `jokerlead` (`jl`), `next` (`n`), `nextround` (`nr`),
            `hint` (`h`), `log` (`l`).
          enum: [reset, r, bid, b, trump, t, exchange, e, play, p, jokerlead, jl, next, n, nextround, nr, hint, h, log, l]
          example: reset
        bid:
          type: integer
          description: |
            Bid value (target number of point cards, or 0 to pass). Required for `bid`.
            The effective floor is `config.minBid` (default 13) and is raised by
            `config.noTrumpExtra` when `noTrump=true`. Maximum 20 (all point cards).
          minimum: 0
          maximum: 20
          example: 14
        noTrump:
          type: boolean
          description: When `true`, the bid is placed on the No-Trump axis (bid floor +`config.noTrumpExtra`; round score is doubled).
          example: false
        cardIndex:
          type: integer
          description: Index into the declarer's hand. Required for `play` and `jokerlead`.
          minimum: 0
          example: 2
        trumpSuit:
          type: integer
          description: "Declared trump suit (−1 = No-Trump, 1 = Spade, 2 = Clover, 3 = Heart, 4 = Diamond). Required for `trump`."
          enum: [-1, 1, 2, 3, 4]
          example: 1
        partnerSuit:
          type: integer
          description: "Partner-card suit (0 = Joker, 1 = Spade, 2 = Clover, 3 = Heart, 4 = Diamond). Required for `trump`."
          enum: [0, 1, 2, 3, 4]
          example: 3
        partnerValue:
          type: integer
          description: Partner-card rank (1 = Ace, 11 = J, 12 = Q, 13 = K). Required for `trump`.
          minimum: 1
          maximum: 13
          example: 1
        discardIndices:
          type: array
          description: Indices of the 3 cards to discard during kitty exchange (declarer's hand after picking up the kitty). Required for `exchange`; must have length 3.
          minItems: 3
          maxItems: 3
          items:
            type: integer
            minimum: 0
          example: [0, 3, 9]
        jokerLeadSuit:
          type: integer
          description: Demand suit when leading the Joker (1 = Spade, 2 = Clover, 3 = Heart, 4 = Diamond). Required for `jokerlead`.
          enum: [1, 2, 3, 4]
          example: 3
        config:
          $ref: '#/components/schemas/MightyConfig'
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    MightyConfig:
      type: object
      description: Mighty game configuration
      properties:
        cpuDifficulty:
          type: integer
          description: 'CPU difficulty: 0=Easy, 1=Normal, 2=Hard'
          enum: [0, 1, 2]
          example: 1
        minBid:
          type: integer
          description: Minimum bid amount (default 13)
          minimum: 1
          maximum: 20
          example: 13
        noTrumpExtra:
          type: integer
          description: Extra bid amount required when bidding No-Trump (default 2)
          minimum: 0
          maximum: 19
          example: 2
        pointLimit:
          type: integer
          description: Cumulative score needed to end the game (default 100)
          minimum: 1
          example: 100

    MightyPlayer:
      type: object
      description: One Mighty player's public state
      required:
        - id
        - isHuman
        - cardCount
        - cards
        - bid
        - bidNoTrump
        - isDeclarer
        - isPartner
        - partnerRevealed
        - pointCards
        - roundScore
        - cumulativeScore
        - trickCount
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
        cards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        bid:
          type: integer
          description: Player's current bid value (0 = pass).
        bidNoTrump:
          type: boolean
          description: Whether the player's current bid is on the No-Trump axis.
        isDeclarer:
          type: boolean
        isPartner:
          type: boolean
          description: Whether the player is the secret partner (only revealed after `partnerRevealed` becomes true).
        partnerRevealed:
          type: boolean
          description: Whether the partner identity has been revealed during the round.
        pointCards:
          type: integer
          description: Number of point cards captured this round.
        roundScore:
          type: integer
        cumulativeScore:
          type: integer
        trickCount:
          type: integer

    MightyTrickCard:
      type: object
      required:
        - playerIdx
        - card
      properties:
        playerIdx:
          type: integer
        card:
          $ref: '#/components/schemas/Card'
        isJokerLead:
          type: boolean
          description: Whether this card is a Joker played as the lead under the Joker-lead rule.
        leadDemandSuit:
          type: integer
          description: Demand suit the Joker lead asked followers to follow (1-4).

    MightyHint:
      type: object
      required:
        - reason
      properties:
        cardIndex:
          type: integer
          nullable: true
        bid:
          type: integer
          nullable: true
        bidNoTrump:
          type: boolean
          nullable: true
        trumpSuit:
          type: integer
          nullable: true
        partnerSuit:
          type: integer
          nullable: true
        partnerValue:
          type: integer
          nullable: true
        discardIndices:
          type: array
          items:
            type: integer
          nullable: true
        jokerLeadSuit:
          type: integer
          nullable: true
        reason:
          type: string
          description: Machine-readable reason key for the hint.

    MightyResponse:
      type: object
      description: Current state of the Mighty game
      required:
        - players
        - phase
        - currentPlayerIdx
        - declarerIdx
        - partnerIdx
        - partnerRevealed
        - trumpSuit
        - highestBid
        - highestBidder
        - winningBidNoTrump
        - currentTrick
        - gameEndFlag
        - winnerTeam
        - leadPlayerIdx
        - message
        - config
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/MightyPlayer'
        phase:
          type: integer
          description: 'Game phase: 0=Bid, 1=TrumpAndFriend, 2=KittyExchange, 3=Play, 4=TrickEnd, 5=RoundEnd, 6=GameEnd'
          enum: [0, 1, 2, 3, 4, 5, 6]
        currentPlayerIdx:
          type: integer
        declarerIdx:
          type: integer
          description: Index of the declarer (-1 if not yet decided)
        partnerIdx:
          type: integer
          description: Index of the secret partner (-1 if not yet bound or self-friend)
        partnerRevealed:
          type: boolean
          description: Whether the secret partner has been revealed (set when the partner card is played).
        trumpSuit:
          type: integer
          description: "Declared trump suit (−1 = No-Trump, 0 = not yet declared, 1−4 = Spade/Clover/Heart/Diamond)."
          enum: [-1, 0, 1, 2, 3, 4]
        partnerCard:
          nullable: true
          description: The partner card named by the declarer (suit + rank).
          allOf:
            - $ref: '#/components/schemas/Card'
        highestBid:
          type: integer
          description: Highest bid placed during the bid phase (0 if no bid yet).
        highestBidder:
          type: integer
          description: Index of the highest bidder (-1 if no bid yet).
        winningBidNoTrump:
          type: boolean
          description: Whether the winning bid is on the No-Trump axis.
        currentTrick:
          type: array
          items:
            $ref: '#/components/schemas/MightyTrickCard'
        kitty:
          type: array
          description: Kitty cards (visible to the declarer during the KittyExchange phase).
          items:
            $ref: '#/components/schemas/Card'
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: 'Winning team after RoundEnd / GameEnd (0 = declarer team, 1 = defenders, -1 = undecided).'
        leadPlayerIdx:
          type: integer
          description: Index of the player who leads the current trick.
        hint:
          $ref: '#/components/schemas/MightyHint'
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
        config:
          $ref: '#/components/schemas/MightyConfig'

    # -------------------------------------------------------------------------
    # Oasis Poker
    # -------------------------------------------------------------------------
    OasisPokerRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `bet` (`b`),
            `exchange` (`e`), `stand` (`s`), `play` (`p`), `fold` (`f`),
            `log` (`l`).
          enum: [r, reset, b, bet, e, exchange, s, stand, p, play, f, fold, log, l]
          example: exchange
        amount:
          type: integer
          description: |
            Ante bet amount. Required for `bet` command (minimum 10, must be a
            multiple of 10). Ignored for other commands.
          minimum: 10
          example: 100
        jackpotBet:
          type: integer
          description: |
            Jackpot side bet amount. Optional; used with the `bet` command.
            0 or omitted means no side bet.
          minimum: 0
          example: 10
        indices:
          type: array
          description: |
            Card indices to exchange (0..4, max 5 cards, no duplicates).
            Used with the `exchange` command. Empty array is equivalent to `stand`.
          items:
            type: integer
            minimum: 0
            maximum: 4
          example: [0, 2]
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    OasisPokerResponse:
      type: object
      description: Current state of the Oasis Poker game
      required:
        - playerHand
        - dealerHand
        - phase
        - chips
        - anteBet
        - jackpotBet
        - exchangeCount
        - exchangeFee
        - playBet
        - result
        - antePayout
        - playPayout
        - jackpotPayout
        - totalPayout
        - dealerQualified
        - playerHandRank
        - dealerHandRank
        - message
      properties:
        playerHand:
          type: array
          description: Cards in the player's hand (5 cards)
          items:
            $ref: '#/components/schemas/Card'
        dealerHand:
          type: array
          description: Cards in the dealer's hand (5 cards; the 2nd-5th cards are hidden until End phase)
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 1 = Bet, 2 = Exchange, 3 = Action, 4 = End"
          enum: [1, 2, 3, 4]
          example: 1
        chips:
          type: integer
          description: Current chip count
          example: 1000
        anteBet:
          type: integer
          description: Current Ante bet amount
          example: 100
        jackpotBet:
          type: integer
          description: Current Jackpot side bet amount
          example: 10
        exchangeCount:
          type: integer
          description: Number of cards exchanged this round
          minimum: 0
          maximum: 5
          example: 2
        exchangeFee:
          type: integer
          description: Total exchange fee collected (ante × exchangeCount)
          minimum: 0
          example: 200
        playBet:
          type: integer
          description: Current Play bet amount (2x Ante on Call)
          example: 0
        result:
          type: integer
          description: "Game result: 1 = Player wins, -1 = Dealer wins, 0 = Push/not resolved"
          enum: [-1, 0, 1]
          example: 0
        antePayout:
          type: integer
          description: Payout from the Ante bet
          example: 0
        playPayout:
          type: integer
          description: Payout from the Play bet
          example: 0
        jackpotPayout:
          type: integer
          description: Jackpot side bet payout
          example: 0
        totalPayout:
          type: integer
          description: Total payout for the current round (sum of all payouts)
          example: 0
        dealerQualified:
          type: boolean
          description: Whether the dealer qualified (Ace-King high or any pair)
          example: false
        playerHandRank:
          type: integer
          description: Player's 5-card hand rank (0=High Card, 1=One Pair, 2=Two Pair, 3=Three of a Kind, 4=Straight, 5=Flush, 6=Full House, 7=Four of a Kind, 8=Straight Flush, 9=Royal Flush)
          example: 1
        dealerHandRank:
          type: integer
          description: Dealer's 5-card hand rank (same encoding as `playerHandRank`)
          example: 1
        message:
          type: string
          description: Game result or error message. Empty during bet/exchange/action phases.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    # -------------------------------------------------------------------------
    # RussianPoker
    # -------------------------------------------------------------------------
    RussianPokerRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `bet` (`b`),
            `exchange` (`e`), `buy6th` (`6`), `select` (`sel`), `play` (`p`),
            `fold` (`f`), `force` (`fe`), `decline` (`d`), `log` (`l`).
          enum: [r, reset, b, bet, e, exchange, '6', buy6th, sel, select, p, play, f, fold, force, fe, d, decline, log, l]
          example: bet
        amount:
          type: integer
          description: Ante bet amount. Required for `bet` command.
          minimum: 10
          example: 100
        indices:
          type: array
          description: Card indices to exchange (0..4).
          items:
            type: integer
            minimum: 0
            maximum: 4
          example: [0, 2]
        discardIndex:
          type: integer
          description: Index of the card to discard from a 6-card hand (0..5). Used with `select`.
          minimum: 0
          maximum: 5
          example: 3
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    RussianPokerResponse:
      type: object
      description: Current state of the Russian Poker game
      required:
        - playerHand
        - dealerHand
        - phase
        - chips
        - anteBet
        - exchangeCount
        - exchangeFee
        - bought6th
        - buy6thFee
        - forceExchanged
        - forceExchangeFee
        - playBet
        - result
        - antePayout
        - playPayout
        - totalPayout
        - dealerQualified
        - playerHandRank
        - dealerHandRank
        - message
      properties:
        playerHand:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        dealerHand:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 1=Bet, 2=Action, 3=Select, 4=PostAction, 5=ForceQualify, 6=End"
          enum: [1, 2, 3, 4, 5, 6]
        chips:
          type: integer
        anteBet:
          type: integer
        exchangeCount:
          type: integer
        exchangeFee:
          type: integer
        bought6th:
          type: boolean
        buy6thFee:
          type: integer
        forceExchanged:
          type: boolean
        forceExchangeFee:
          type: integer
        playBet:
          type: integer
        result:
          type: integer
          enum: [-1, 0, 1]
        antePayout:
          type: integer
        playPayout:
          type: integer
        totalPayout:
          type: integer
        dealerQualified:
          type: boolean
        playerHandRank:
          type: integer
        dealerHandRank:
          type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string

    # -------------------------------------------------------------------------
    # HighCardFlush
    # -------------------------------------------------------------------------
    HighCardFlushResponse:
      type: object
      description: Current state of the High Card Flush game returned after every command.
      properties:
        playerHand:
          type: array
          description: Player's 7 cards (only populated from the Action phase onwards).
          items:
            $ref: '#/components/schemas/Card'
        dealerHand:
          type: array
          description: Dealer's 7 cards (revealed only at the End phase).
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Current phase. 1=Bet, 2=Action, 3=End"
          enum: [1, 2, 3]
          example: 1
        chips:
          type: integer
          description: Player's current chip balance.
          example: 1000
        anteBet:
          type: integer
          description: Ante bet placed this round.
          example: 100
        flushBonusBet:
          type: integer
          description: Optional Flush Bonus side bet (0 = no bet).
          example: 0
        straightFlushBet:
          type: integer
          description: Optional Straight Flush Bonus side bet (0 = no bet).
          example: 0
        raiseBet:
          type: integer
          description: Raise bet placed during the Action phase (0 = not raised yet or folded).
          example: 0
        result:
          type: integer
          description: "Game result. -1=dealer win, 0=push, 1=player win"
          enum: [-1, 0, 1]
          example: 0
        antePayout:
          type: integer
          description: Ante payout including the returned stake (0 = forfeited).
        raisePayout:
          type: integer
          description: Raise payout including the returned stake (0 = forfeited).
        flushBonusPayout:
          type: integer
          description: Flush Bonus payout including the returned stake (0 = no win).
        straightFlushPayout:
          type: integer
          description: Straight Flush Bonus payout including the returned stake (0 = no win).
        totalPayout:
          type: integer
          description: Sum of all payouts.
        dealerQualified:
          type: boolean
          description: True when the dealer's best flush is 3+ cards with high card 9 or better.
        playerFlushLen:
          type: integer
          description: Length of the player's longest same-suit subset.
        dealerFlushLen:
          type: integer
          description: Length of the dealer's longest same-suit subset (revealed at End phase).
        playerStraightFlushLen:
          type: integer
          description: Length of the player's longest straight flush (0 if none).
        maxRaiseMultiplier:
          type: integer
          description: Maximum raise multiplier permitted by the player's flush length (1, 2, or 3).
        message:
          type: string
          description: Game result or error message.
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    Rummy500Request:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          enum: [reset, r, drawstock, ds, drawdiscard, dd, meld, m, layoff, lo, discard, d, nextround, nr, log, l, quit, q]
          description: Game command alias.
        sessionId:
          type: string
          description: Stable identifier for the active game session.
        cardIndex:
          type: integer
          description: Card index in the human hand (for `discard` and the card argument of `layoff`).
        cardIndices:
          type: array
          items:
            type: integer
          description: Card indices for `meld` (3+ entries describing the meld).
        discardIdx:
          type: integer
          description: Discard pile index for `drawdiscard`. Omit or pass `-1` to draw the top of the pile.
        meldOwner:
          type: integer
          description: Player index that owns the meld being laid off onto.
        meldIdx:
          type: integer
          description: Index of the target meld within `meldOwner`'s laid melds.
        config:
          $ref: '#/components/schemas/Rummy500ConfigInput'

    Rummy500ConfigInput:
      type: object
      properties:
        cpuDifficulty:
          type: integer
          minimum: 0
          maximum: 2
          description: 0=Easy, 1=Normal, 2=Hard.
        pointLimit:
          type: integer
          minimum: 1
          maximum: 10000
          description: Target cumulative score; first to reach it wins.

    Rummy500Response:
      type: object
      properties:
        players:
          type: array
          items:
            $ref: '#/components/schemas/Rummy500PlayerData'
        phase:
          type: integer
          enum: [0, 1, 2, 3]
          description: 0=Draw, 1=Play, 2=RoundEnd, 3=GameEnd.
        roundNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        discardPile:
          type: array
          items:
            $ref: '#/components/schemas/Card'
          description: Discard pile in order (index 0 = oldest). Drawing accepts an index and takes idx + all cards above.
        drawPileCount:
          type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: -1 until the game ends.
        roundEnderIdx:
          type: integer
          description: Player index that ended the round, or -1 if the stock ran out.
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties:
            type: string
        config:
          $ref: '#/components/schemas/Rummy500ConfigOutput'

    Rummy500ConfigOutput:
      type: object
      properties:
        cpuDifficulty:
          type: integer
        pointLimit:
          type: integer

    Rummy500PlayerData:
      type: object
      properties:
        id:
          type: integer
        isHuman:
          type: boolean
        cardCount:
          type: integer
        cards:
          type: array
          items:
            $ref: '#/components/schemas/Card'
        roundScore:
          type: integer
        cumulativeScore:
          type: integer
        laidMelds:
          type: array
          items:
            $ref: '#/components/schemas/Rummy500Meld'

    Rummy500Meld:
      type: object
      properties:
        cards:
          type: array
          items:
            $ref: '#/components/schemas/Card'

    # -------------------------------------------------------------------------
    # Penguin
    # -------------------------------------------------------------------------
    PenguinZone:
      type: object
      description: Specifies a zone on the Penguin board for move commands
      required:
        - zone
      properties:
        zone:
          type: string
          description: "Zone name: tableau, freecell, or foundation"
          enum: [tableau, freecell, foundation]
          example: tableau
        col:
          type: integer
          description: Column index (0-based, used for tableau and foundation zones)
          example: 3
        cell:
          type: integer
          description: Free cell index (0-based, 0..6)
          example: 4
        cardIndex:
          type: integer
          description: Index of the card within the column (for moving multiple cards from tableau)
          example: 0

    PenguinRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `move` (`m`),
            `giveup` (`g`), `hint` (`h`), `autocomplete` (`ac`),
            `undo` (`u`), `undo_n`, `log` (`l`).
          enum: [reset, r, move, m, giveup, g, hint, h, autocomplete, ac, undo, u, undo_n, log, l]
          example: move
        from:
          description: Source zone for a move command
          $ref: '#/components/schemas/PenguinZone'
        to:
          description: Destination zone for a move command
          $ref: '#/components/schemas/PenguinZone'
        n:
          type: integer
          description: Number of undos to perform (required when command is undo_n)
          example: 3
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    PenguinResponse:
      type: object
      description: Current state of the Penguin game
      required:
        - tableau
        - freeCells
        - foundation
        - baseRank
        - phase
        - moveCount
        - canUndo
        - isStalemate
        - message
      properties:
        tableau:
          type: array
          description: The 7 tableau columns, each containing an array of face-up cards
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        freeCells:
          type: array
          description: The 7 free cells (null or a Card)
          items:
            nullable: true
            $ref: '#/components/schemas/Card'
        foundation:
          type: array
          description: The 4 foundation piles (one per suit), each containing an array of cards
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        baseRank:
          type: integer
          description: The base rank for foundations (1-13, determined by the first dealt card)
          example: 5
        phase:
          type: integer
          description: "Game phase: 0 = Playing, 1 = GameClear, 2 = GameOver"
          enum: [0, 1, 2]
          example: 0
        moveCount:
          type: integer
          description: Total number of moves made in the current game
          example: 0
        canUndo:
          type: boolean
          description: Whether the undo action is available
          example: false
        isStalemate:
          type: boolean
          description: Whether the game is in a stalemate (no moves can lead to a win)
          example: false
        undoToEscape:
          type: integer
          description: Number of undos needed to reach a non-stalemate state (0 if not stalemate, -1 if no escape)
          example: 0
        hint:
          type: object
          description: Hint for the next available move (only present when hint command is used)
          properties:
            fromZone:
              type: string
            fromCol:
              type: integer
            cardIndex:
              type: integer
            toZone:
              type: string
            toCol:
              type: integer
        message:
          type: string
          description: Game result or status message. Empty while the game is in progress.
          example: ""
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    BourreRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          enum: [reset, r, decide, draw, play, p, next, log, l]
        sessionId:
          type: string
        decide:
          type: boolean
          description: For the `decide` command — true to play, false to fold.
        indices:
          type: array
          description: For the `draw` command — indices of the cards to discard.
          items:
            type: integer
        cardIndex:
          type: integer
          description: For the `play` command — index of the card to play.
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
              enum: [0, 1, 2]

    BourreResponse:
      type: object
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              isFinished:
                type: boolean
              folded:
                type: boolean
              decided:
                type: boolean
              drawn:
                type: boolean
              bourreed:
                type: boolean
              chips:
                type: integer
              tricks:
                type: integer
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  type: object
                  properties:
                    design:
                      type: string
                    value:
                      type: integer
        phase:
          type: string
          enum: [decide, draw, play, roundEnd, gameEnd, unknown]
        currentPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        pot:
          type: integer
        carryPot:
          type: integer
        trumpSuit:
          type: string
        trumpCard:
          type: object
          nullable: true
          properties:
            design:
              type: string
            value:
              type: integer
        trickNumber:
          type: integer
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                type: object
                properties:
                  design:
                    type: string
                  value:
                    type: integer
        lastTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                type: object
                properties:
                  design:
                    type: string
                  value:
                    type: integer
        lastTrickWinner:
          type: integer
        leadPlayerIdx:
          type: integer
        handNumber:
          type: integer
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
        validPlays:
          type: array
          items:
            type: integer
        results:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              tricks:
                type: integer
              wonAmount:
                type: integer
              bourreed:
                type: boolean
              folded:
                type: boolean
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties: true

    # -------------------------------------------------------------------------
    # Sheepshead types
    # -------------------------------------------------------------------------
    SheepsheadRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `pick` (`p`), `pass`,
            `bury` (`b`), `call` (`c`), `play`, `next` (`n`), `nextround` (`nr`),
            `setdifficulty` (`sd`), `setbasechips` (`sb`), `hint` (`h`), `log` (`l`).
          enum: [reset, r, pick, p, pass, bury, b, call, c, play, next, n, nextround, nr, setdifficulty, sd, setbasechips, sb, hint, h, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        cardIndices:
          type: array
          items:
            type: integer
          description: Hand indices of two cards to bury. Required for the `bury` command.
          example: [0, 1]
        suit:
          type: integer
          description: Fail-suit to call (1=♠, 2=♣, 3=♥). Required for the `call` command.
          minimum: 1
          maximum: 3
          example: 1
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        amount:
          type: integer
          description: Base chip stake for `setbasechips`. Must be a positive integer.
          minimum: 1
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            baseChips:
              type: integer
              minimum: 1
            targetChips:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    SheepsheadResponse:
      type: object
      description: Current state of the Sheepshead game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              trickCount:
                type: integer
              chips:
                type: integer
                description: Current chip count for this player
        phase:
          type: integer
          description: "Game phase: 0=Pick, 1=Bury, 2=Call, 3=Play, 4=TrickEnd, 5=RoundEnd, 6=GameEnd"
          enum: [0, 1, 2, 3, 4, 5, 6]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        blindCount:
          type: integer
          description: Number of cards currently in the blind (2 before pick, 0 after bury)
        buried:
          type: array
          description: The 2 buried cards (visible only after round end)
          items:
            $ref: '#/components/schemas/Card'
        pickerIdx:
          type: integer
          description: Index of the player who picked up the blind (-1 if not yet decided)
        partnerIdx:
          type: integer
          description: Index of the secret partner (-1 until revealed)
        calledSuit:
          type: integer
          description: "Fail-suit called by the picker (1=♠, 2=♣, 3=♥, 0=none)"
        partnerRevealed:
          type: boolean
          description: True once the partner has played the called Ace
        passCount:
          type: integer
          description: Number of players who have passed the blind in the current round
        callableSuits:
          type: array
          items:
            type: integer
          description: Fail-suit indices the picker may legally call (suits not in the picker's hand)
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (must-follow filtered)
        roundPickerPoints:
          type: integer
          description: Card points accumulated by the picker team this round (including buried)
        roundMultiplier:
          type: integer
          description: "Chip stake multiplier for this round: 1=normal, 2=Schneider, 3=Schwarz"
        roundPickerWon:
          type: boolean
          description: True if the picker team won the round (available at RoundEnd/GameEnd)
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: Index of the player who reached the target chip count (-1 if undecided)
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        messageParams:
          type: object
          additionalProperties: true
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            baseChips:
              type: integer
            targetChips:
              type: integer

    # -------------------------------------------------------------------------
    # Doppelkopf types
    # -------------------------------------------------------------------------
    DoppelkopfRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `play`, `announce` (`a`),
            `next` (`n`), `nextround` (`nr`), `setdifficulty` (`sd`),
            `setbasechips` (`sb`), `log` (`l`).
          enum: [reset, r, play, announce, a, next, n, nextround, nr, setdifficulty, sd, setbasechips, sb, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        amount:
          type: integer
          description: Base chip stake for `setbasechips`. Must be a positive integer.
          minimum: 1
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            baseChips:
              type: integer
              minimum: 1
            targetChips:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    DoppelkopfResponse:
      type: object
      description: Current state of the Doppelkopf game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              trickCount:
                type: integer
              chips:
                type: integer
                description: Current chip count for this player
        phase:
          type: integer
          description: "Game phase: 0=Play, 1=TrickEnd, 2=RoundEnd, 3=GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        reTeam:
          type: array
          items:
            type: integer
          description: Indices of Re team members (revealed at round end; partial during play if Re announced)
        soloRe:
          type: boolean
          description: True if one player holds both Q♣ (solo Re vs 3 Kontra)
        teamsRevealed:
          type: boolean
          description: True once both teams are fully public (end of round)
        reAnnounced:
          type: boolean
          description: True if Re has been announced this round
        kontraAnnounced:
          type: boolean
          description: True if Kontra has been announced this round
        canAnnounce:
          type: boolean
          description: True if the human player may still announce during this trick (trick 1 only)
        youAreRe:
          type: boolean
          description: True if the human player is on the Re team
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (must-follow filtered)
        roundRePoints:
          type: integer
          description: Card points accumulated by the Re team this round
        roundReWon:
          type: boolean
          description: True if the Re team won the round (available at RoundEnd/GameEnd)
        roundGamePoints:
          type: integer
          description: Game points awarded to the winning side this round (before chip settlement)
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: Index of the player who reached the target chip count (-1 if undecided)
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            baseChips:
              type: integer
            targetChips:
              type: integer

    # -------------------------------------------------------------------------
    # Mus types
    # -------------------------------------------------------------------------
    MusRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `mus` (`m`), `corte` (`c`, `cut`),
            `discard` (`d`), `paso`, `envido` (`e`), `ordago`, `quiero`, `noquiero` (`nq`),
            `next` (`n`), `setdifficulty` (`sd`), `log` (`l`).
          enum: [reset, r, mus, m, corte, c, cut, discard, d, paso, envido, e, ordago, quiero, noquiero, nq, next, n, setdifficulty, sd, log, l]
          example: reset
        cardIndices:
          type: array
          items:
            type: integer
          description: Hand indices of cards to discard. Required for the `discard` command (0-4 cards).
          example: [0, 2]
        amount:
          type: integer
          description: Bet amount in amarrakos for `envido`. Must be a positive integer.
          minimum: 1
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetAmarrakos:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    MusResponse:
      type: object
      description: Current state of the Mus game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              teamId:
                type: integer
                description: "Team index: 0 = seats 0&2, 1 = seats 1&3"
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 0=Mus, 1=Discard, 2=Grande, 3=Chica, 4=Pares, 5=Juego, 6=Showdown, 7=GameEnd"
          enum: [0, 1, 2, 3, 4, 5, 6, 7]
          example: 0
        roundNumber:
          type: integer
        manoIdx:
          type: integer
          description: Seat index of the current Mano (first player in this round)
        betTeam:
          type: integer
          description: "Team index that placed the current pending bet (-1 = none)"
        pendingStake:
          type: integer
          description: Current bet amount on the table (0 when no active bet)
        lastBettorTeam:
          type: integer
          description: Team index of the last team to bet or raise (-1 = none)
        musTurn:
          type: integer
          description: Seat index of the player whose Mus/Corte declaration is pending
        discardTurn:
          type: integer
          description: Seat index of the player who should discard next (-1 = not in Discard phase)
        musCycle:
          type: integer
          description: Number of completed Mus exchange cycles so far (0-3)
        amarrakos:
          type: array
          items:
            type: integer
          description: Amarrakos score for each team [team0, team1]
        results:
          type: array
          description: Per-betting-round showdown results (available at Showdown/GameEnd)
          items:
            type: object
            properties:
              roundName:
                type: string
                description: "Name of the betting round (grande, chica, pares, juego, punto)"
              winnerTeam:
                type: integer
              pointsAwarded:
                type: integer
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: Index of the winning team (-1 if undecided)
        humanTeam:
          type: integer
          description: Team index of the human player (always 0)
        isHumanTurn:
          type: boolean
        canPaso:
          type: boolean
          description: True when the human may play Paso
        canEnvido:
          type: boolean
          description: True when the human may play Envido
        canOrdago:
          type: boolean
          description: True when the human may play Ordago
        canQuiero:
          type: boolean
          description: True when the human may play Quiero (a bet is pending)
        canNoQuiero:
          type: boolean
          description: True when the human may play No Quiero (a bet is pending)
        hint:
          type: object
          properties:
            action:
              type: string
              description: "Suggested action (e.g. paso, envido, ordago, quiero, noquiero)"
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetAmarrakos:
              type: integer

    # -------------------------------------------------------------------------
    # Tute types
    # -------------------------------------------------------------------------
    TuteRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `play`, `marriage` (`m`),
            `tute`, `next` (`n`), `nextround` (`nr`), `setdifficulty` (`sd`), `log` (`l`).
          enum: [reset, r, play, marriage, m, tute, next, n, nextround, nr, setdifficulty, sd, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        suit:
          type: integer
          description: |
            Suit to use for the `marriage` command. 1=♠ (Spades), 2=♣ (Clubs),
            3=♥ (Hearts), 4=♦ (Diamonds).
          minimum: 1
          maximum: 4
          example: 3
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetPoints:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    TuteResponse:
      type: object
      description: Current state of the Tute game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              trickCount:
                type: integer
              teamId:
                type: integer
                description: "Team membership: 0 = seats 0 & 2, 1 = seats 1 & 3"
        phase:
          type: integer
          description: "Game phase: 0=Play, 1=TrickEnd, 2=RoundEnd, 3=GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        trumpSuit:
          type: integer
          description: "Current trump suit: 1=♠, 2=♣, 3=♥, 4=♦"
          minimum: 1
          maximum: 4
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        declaredSuits:
          type: array
          items:
            type: integer
          description: Suit indices for which a cante has already been declared this round
        teamScores:
          type: array
          items:
            type: integer
          description: Cumulative points for each team across rounds (index 0 = Team A seats 0&2, 1 = Team B seats 1&3)
        roundTeamPoints:
          type: array
          items:
            type: integer
          description: Card points accumulated by each team in the current round (before last-trick bonus)
        canDeclareMarriage:
          type: boolean
          description: True if the human player is on lead and holds a Rey+Caballo pair not yet declared
        canDeclareTute:
          type: boolean
          description: True if the human player holds all four Reys or all four Caballos
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (must-follow filtered)
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: Index of the winning team (-1 if undecided)
        isHumanTurn:
          type: boolean
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetPoints:
              type: integer

    SuecaRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `play` (`p`),
            `next` (`n`), `nextround` (`nr`), `setdifficulty` (`sd`), `log` (`l`).
          enum: [reset, r, play, p, next, n, nextround, nr, setdifficulty, sd, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetGamePoints:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    SuecaResponse:
      type: object
      description: Current state of the Sueca game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              trickCount:
                type: integer
              teamGamePoints:
                type: integer
                description: Cumulative game points of this player's team
        phase:
          type: integer
          description: "Game phase: 0=Play, 1=TrickEnd, 2=RoundEnd, 3=GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        trumpSuit:
          type: integer
          description: "Current trump suit: 1=♠, 2=♣, 3=♥, 4=♦"
          minimum: 1
          maximum: 4
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        teamGamePoints:
          type: array
          items:
            type: integer
          description: Cumulative game points per team (index 0 = Team A seats 0&2, 1 = Team B seats 1&3)
        roundCardPoints:
          type: array
          items:
            type: integer
          description: Card points accumulated by each team in the current round
        roundWinnerTeam:
          type: integer
          description: Index of the team that won the most card points in the last round (-1 if draw or not yet resolved)
        roundGamePoints:
          type: integer
          description: Game points awarded to the round winner (1, 2, or 4; 0 on a 60-60 draw)
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (must-follow filtered)
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: Index of the winning team (-1 if undecided)
        isHumanTurn:
          type: boolean
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetGamePoints:
              type: integer

    KlaverjasRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `play` (`p`),
            `next` (`n`), `nextround` (`nr`), `setdifficulty` (`sd`), `log` (`l`).
          enum: [reset, r, play, p, next, n, nextround, nr, setdifficulty, sd, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetScore:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    KlaverjasResponse:
      type: object
      description: Current state of the Klaverjas game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              trickCount:
                type: integer
              teamScore:
                type: integer
                description: Cumulative score of this player's team
        phase:
          type: integer
          description: "Game phase: 0=Play, 1=TrickEnd, 2=RoundEnd, 3=GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        trumpSuit:
          type: integer
          description: "Current trump suit: 1=♠, 2=♣, 3=♥, 4=♦"
          minimum: 1
          maximum: 4
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        teamScores:
          type: array
          items:
            type: integer
          description: Cumulative scores per team (index 0 = Team A seats 0&2, 1 = Team B seats 1&3)
        roundCardPoints:
          type: array
          items:
            type: integer
          description: Card points accumulated by each team in the current round
        roundRoem:
          type: array
          items:
            type: integer
          description: Roem (meld) points awarded to each team at the start of the current round
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (Klaverjas obligation filtered)
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: Index of the winning team (-1 if undecided)
        isHumanTurn:
          type: boolean
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetScore:
              type: integer

    ManilleRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `play` (`p`),
            `next` (`n`), `nextround` (`nr`), `setdifficulty` (`sd`), `log` (`l`).
          enum: [reset, r, play, p, next, n, nextround, nr, setdifficulty, sd, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetScore:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    ManilleResponse:
      type: object
      description: Current state of the Manille game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              trickCount:
                type: integer
              teamScore:
                type: integer
                description: Cumulative score of this player's team
        phase:
          type: integer
          description: "Game phase: 0=Play, 1=TrickEnd, 2=RoundEnd, 3=GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        trumpSuit:
          type: integer
          description: "Current trump suit: 1=♠, 2=♣, 3=♥, 4=♦"
          minimum: 1
          maximum: 4
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        teamScores:
          type: array
          items:
            type: integer
          description: Cumulative scores per team (index 0 = Team A seats 0&2, 1 = Team B seats 1&3)
        roundCardPoints:
          type: array
          items:
            type: integer
          description: Card points accumulated by each team in the current round
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (Manille follow rules filtered)
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: Index of the winning team (-1 if undecided)
        isHumanTurn:
          type: boolean
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetScore:
              type: integer

    MariasRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `play`,
            `next` (`n`), `nextround` (`nr`), `setdifficulty` (`sd`), `log` (`l`).
          enum: [reset, r, play, next, n, nextround, nr, setdifficulty, sd, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetScore:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    MariasResponse:
      type: object
      description: Current state of the Mariáš game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              isSoloist:
                type: boolean
                description: True if this player is the Soloist for the current round
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              score:
                type: integer
                description: Cumulative game points for this player
        phase:
          type: integer
          description: "Game phase: 0=Play, 1=TrickEnd, 2=RoundEnd, 3=GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        soloistIdx:
          type: integer
          description: Index of the Soloist player for the current round
        trumpSuit:
          type: integer
          description: "Current trump suit: 1=♠, 2=♣, 3=♥, 4=♦"
          minimum: 1
          maximum: 4
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        playerScores:
          type: array
          items:
            type: integer
          description: Cumulative game points per player (index = player seat)
        roundCardPoints:
          type: array
          items:
            type: integer
          description: Card points accumulated by each player in the current round
        roundMarriage:
          type: array
          items:
            type: integer
          description: Marriage bonus points per player auto-detected at deal for the current round
        lastTrickWinner:
          type: integer
          description: Index of the player who won the last (10th) trick (-1 if not yet completed)
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (Mariáš follow rules filtered)
        gameEndFlag:
          type: boolean
        winnerPlayer:
          type: integer
          description: Index of the winning player (-1 if undecided)
        isHumanTurn:
          type: boolean
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetScore:
              type: integer

    SedmaRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `play` (`p`),
            `next` (`n`), `nextround` (`nr`), `setdifficulty` (`sd`), `log` (`l`).
          enum: [reset, r, play, p, next, n, nextround, nr, setdifficulty, sd, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetScore:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    SedmaResponse:
      type: object
      description: Current state of the Sedma game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              trickCount:
                type: integer
              teamScore:
                type: integer
                description: Cumulative score of this player's team
        phase:
          type: integer
          description: "Game phase: 0=Play, 1=TrickEnd, 2=RoundEnd, 3=GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        teamScores:
          type: array
          items:
            type: integer
          description: Cumulative scores per team (index 0 = Team A seats 0&2, 1 = Team B seats 1&3)
        roundCardPoints:
          type: array
          items:
            type: integer
          description: Card points accumulated by each team in the current round
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (all indices, as Sedma has no follow obligation)
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: Index of the winning team (-1 if undecided)
        isHumanTurn:
          type: boolean
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetScore:
              type: integer

    SoloWhistRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `bid`, `pass`,
            `play`, `next` (`n`), `nextround` (`nr`), `setdifficulty` (`sd`), `log` (`l`).
          enum: [reset, r, bid, pass, play, next, n, nextround, nr, setdifficulty, sd, log, l]
          example: reset
        bidType:
          type: integer
          description: Bid contract type for the `bid` command. 0=Pass, 1=Solo, 2=Misère, 3=Abundance.
          minimum: 0
          maximum: 3
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetScore:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    SoloWhistResponse:
      type: object
      description: Current state of the Solo Whist game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              isDeclarer:
                type: boolean
                description: True if this player is the Declarer for the current round
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              score:
                type: integer
                description: Cumulative game points for this player
        phase:
          type: integer
          description: "Game phase: 0=Bid, 1=Play, 2=TrickEnd, 3=RoundEnd, 4=GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        declarerIdx:
          type: integer
          description: Index of the Declarer player for the current round (-1 if not yet determined)
        contract:
          type: integer
          description: "Current contract: 0=None/Pass, 1=Solo, 2=Misère, 3=Abundance"
          enum: [0, 1, 2, 3]
        trumpSuit:
          type: integer
          description: "Current trump suit: 1=Spades, 2=Clubs, 3=Hearts, 4=Diamonds (0 for Misère / no trump)"
          minimum: 0
          maximum: 4
        bids:
          type: array
          items:
            type: integer
          description: Bid placed by each player in seat order (0=Pass, 1=Solo, 2=Misère, 3=Abundance; -1=not yet bid)
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        playerScores:
          type: array
          items:
            type: integer
          description: Cumulative game points per player (index = player seat)
        roundTricks:
          type: array
          items:
            type: integer
          description: Number of tricks won by each player in the current round (index = player seat)
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (must-follow filtered)
        gameEndFlag:
          type: boolean
        winnerPlayer:
          type: integer
          description: Index of the winning player (-1 if undecided)
        isHumanTurn:
          type: boolean
        isHumanBidTurn:
          type: boolean
          description: True when it is the human's turn to bid
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetScore:
              type: integer

    CatchTenResponse:
      type: object
      description: Current state of the Catch the Ten (Scotch Whist) game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              roundScore:
                type: integer
                description: Honor points captured this deal
              cumulativeScore:
                type: integer
              trickCount:
                type: integer
              team:
                type: integer
                description: "Partnership index (0 = human side, 1 = opponents)"
        phase:
          type: integer
          description: "Game phase: 0=Play, 1=TrickEnd, 2=RoundEnd, 3=GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        trumpSuit:
          type: integer
          description: "Trump suit: 1=Spades, 2=Clubs, 3=Hearts, 4=Diamonds"
          minimum: 1
          maximum: 4
        dealerIdx:
          type: integer
        teamScores:
          type: array
          items:
            type: integer
          description: Cumulative honor points per team [team0, team1]
          minItems: 2
          maxItems: 2
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: "Winning team (0 or 1); -1 if undecided, -2 if the game ended in a draw"
        leadPlayerIdx:
          type: integer
        hint:
          type: object
          properties:
            cardIndex:
              type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            pointLimit:
              type: integer

    KnockoutWhistRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `play`,
            `next` (`n`), `nextround` (`nr`), `setdifficulty` (`sd`), `log` (`l`).
          enum: [reset, r, play, next, n, nextround, nr, setdifficulty, sd, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    KnockoutWhistResponse:
      type: object
      description: Current state of the Knockout Whist game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              eliminated:
                type: boolean
                description: True if this player has been knocked out
              dogbones:
                type: integer
                description: Number of Dogbone tokens remaining for this player
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              roundTricks:
                type: integer
                description: Number of tricks won by this player in the current round
        phase:
          type: integer
          description: "Game phase: 0=Play, 1=TrickEnd, 2=RoundEnd, 3=GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
          description: Current round number (starts at 1)
        handSize:
          type: integer
          description: Number of cards dealt this round (= 8 - roundNumber)
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        trumpSuit:
          type: integer
          description: "Current trump suit: 1=Spades, 2=Clubs, 3=Hearts, 4=Diamonds"
          minimum: 1
          maximum: 4
        roundWinnerIdx:
          type: integer
          description: Index of the player who won the most tricks last round (-1 if round 1)
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        activeCount:
          type: integer
          description: Number of players still in the game (not eliminated)
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (must-follow filtered)
        gameEndFlag:
          type: boolean
        winnerPlayer:
          type: integer
          description: Index of the winning player (-1 if undecided)
        isHumanTurn:
          type: boolean
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer

    NapRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `bid`, `pass`,
            `play`, `next` (`n`), `nextround` (`nr`), `setdifficulty` (`sd`), `log` (`l`).
          enum: [reset, r, bid, pass, play, next, n, nextround, nr, setdifficulty, sd, log, l]
          example: reset
        bidType:
          type: integer
          description: Bid contract type for the `bid` command. 0=Pass, 2=Two, 3=Three, 4=Four, 5=Nap.
          enum: [0, 2, 3, 4, 5]
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetScore:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    NapResponse:
      type: object
      description: Current state of the Nap game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              isDeclarer:
                type: boolean
                description: True if this player is the Declarer for the current round
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              score:
                type: integer
                description: Cumulative chip count for this player
        phase:
          type: integer
          description: "Game phase: 0=Bid, 1=Play, 2=TrickEnd, 3=RoundEnd, 4=GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        declarerIdx:
          type: integer
          description: Index of the Declarer player for the current round (-1 if not yet determined)
        contract:
          type: integer
          description: "Current contract bid: 0=None/Pass, 2=Two, 3=Three, 4=Four, 5=Nap"
          enum: [0, 2, 3, 4, 5]
        trumpSuit:
          type: integer
          description: "Current trump suit: 1=Spades, 2=Clubs, 3=Hearts, 4=Diamonds (0 if not yet determined)"
          minimum: 0
          maximum: 4
        bids:
          type: array
          items:
            type: integer
          description: Bid placed by each player in seat order (0=Pass, 2=Two, 3=Three, 4=Four, 5=Nap; -1=not yet bid)
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        playerScores:
          type: array
          items:
            type: integer
          description: Cumulative chip count per player (index = player seat)
        roundTricks:
          type: array
          items:
            type: integer
          description: Number of tricks won by each player in the current round (index = player seat)
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (must-follow filtered)
        gameEndFlag:
          type: boolean
        winnerPlayer:
          type: integer
          description: Index of the winning player (-1 if undecided)
        isHumanTurn:
          type: boolean
        isHumanBidTurn:
          type: boolean
          description: True when it is the human's turn to bid
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetScore:
              type: integer

    PreferenceRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `bid`, `pass`,
            `play`, `next` (`n`), `nextround` (`nr`), `setdifficulty` (`sd`), `log` (`l`).
          enum: [reset, r, bid, pass, play, next, n, nextround, nr, setdifficulty, sd, log, l]
          example: reset
        bidType:
          type: integer
          description: Bid contract type for the `bid` command. 0=Pass, 1=Six, 2=Misere, 3=Seven, 4=Eight.
          minimum: 0
          maximum: 4
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetScore:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    GanjifaRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`),
            `play`, `next` (`n`), `nextround` (`nr`), `setdifficulty` (`sd`), `log` (`l`).
          enum: [reset, r, play, next, n, nextround, nr, setdifficulty, sd, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetRounds:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001
    ViraRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `bid`, `pass`,
            `play`, `next` (`n`), `nextround` (`nr`), `setdifficulty` (`sd`), `log` (`l`).
          enum: [reset, r, bid, pass, play, next, n, nextround, nr, setdifficulty, sd, log, l]
          example: reset
        bidType:
          type: integer
          description: Bid contract type for the `bid` command. 0=Pass, 1=Gask, 2=Solo, 3=Misere, 4=Vira.
          minimum: 0
          maximum: 4
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetScore:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001
    SpoilFiveRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `play`,
            `next` (`n`), `nextround` (`nr`), `setdifficulty` (`sd`), `log` (`l`).
          enum: [reset, r, play, next, n, nextround, nr, setdifficulty, sd, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetScore:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    SpoilFiveResponse:
      type: object
      description: Current state of the Spoil Five game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              score:
                type: integer
                description: Accumulated pot winnings for this player
              roundTricks:
                type: integer
                description: Number of tricks won by this player in the current round
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 0=Play, 1=TrickEnd, 2=RoundEnd, 3=GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
          description: Current round number (starts at 1)
        trickNumber:
          type: integer
          description: Current trick number within the round (1-5)
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        leadPlayerIdx:
          type: integer
          description: Index of the player who led the current trick
        dealerIdx:
          type: integer
          description: Index of the current dealer
        trumpSuit:
          type: integer
          description: "Current trump suit: 1=Spades, 2=Clubs, 3=Hearts, 4=Diamonds"
          minimum: 1
          maximum: 4
        pot:
          type: integer
          description: Current pot total (accumulates on spoil)
        roundWinnerIdx:
          type: integer
          description: Index of the player who won 3 tricks this round (-1 if spoiled or in progress)
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (must-follow and reneging rules applied)
        gameEndFlag:
          type: boolean
        winnerPlayer:
          type: integer
          description: Index of the winning player (-1 if undecided)
        isHumanTurn:
          type: boolean
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetScore:
              type: integer

    FortyFivesRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `bid`, `pass`,
            `play`, `next` (`n`), `nextround` (`nr`), `setdifficulty` (`sd`), `log` (`l`).
          enum: [reset, r, bid, pass, play, next, n, nextround, nr, setdifficulty, sd, log, l]
          example: reset
        bidValue:
          type: integer
          description: Bid value for the `bid` command. 0=Pass, 15, 20, or 25.
          enum: [0, 15, 20, 25]
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetScore:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    FortyFivesResponse:
      type: object
      description: Current state of the Auction Forty-Fives game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              isDeclarer:
                type: boolean
                description: True if this player placed the winning bid for the current round
              teamScore:
                type: integer
                description: Cumulative team score for the team this player belongs to
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 0=Bid, 1=Play, 2=TrickEnd, 3=RoundEnd, 4=GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
          description: Current round number (starts at 1)
        trickNumber:
          type: integer
          description: Current trick number within the round (1-5)
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        leadPlayerIdx:
          type: integer
          description: Index of the player who led the current trick
        dealerIdx:
          type: integer
          description: Index of the current dealer
        declarerIdx:
          type: integer
          description: Index of the player who placed the winning bid (-1 if not yet determined)
        contract:
          type: integer
          description: "Bid value of the winning bid: 0=None/Pass, 15, 20, or 25"
          enum: [0, 15, 20, 25]
        trumpSuit:
          type: integer
          description: "Current trump suit: 1=Spades, 2=Clubs, 3=Hearts, 4=Diamonds (0 if not yet determined)"
          minimum: 0
          maximum: 4
        bids:
          type: array
          items:
            type: integer
          description: Bid placed by each player in seat order (0=Pass, 15, 20, 25; -1=not yet bid)
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        teamScores:
          type: array
          items:
            type: integer
          description: Cumulative team scores — index 0 is teamA (seats 0&2), index 1 is teamB (seats 1&3)
        roundTeamPoints:
          type: array
          items:
            type: integer
          description: Trick points won by each team in the current round — index 0 is teamA, index 1 is teamB
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (must-follow and reneging rules applied)
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: Index of the winning team (0=teamA, 1=teamB; -1 if undecided)
        isHumanTurn:
          type: boolean
        isHumanBidTurn:
          type: boolean
          description: True when it is the human's turn to bid
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetScore:
              type: integer

    TwentyNineRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `bid`, `pass`,
            `play`, `next` (`n`), `nextround` (`nr`), `setdifficulty` (`sd`), `log` (`l`).
          enum: [reset, r, bid, pass, play, next, n, nextround, nr, setdifficulty, sd, log, l]
          example: reset
        bidValue:
          type: integer
          description: Bid value for the `bid` command. 0=Pass, 16, 20, 24, or 28.
          enum: [0, 16, 20, 24, 28]
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetScore:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    TwentyNineResponse:
      type: object
      description: Current state of the Twenty-Nine game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              isDeclarer:
                type: boolean
                description: True if this player placed the winning bid for the current round
              teamScore:
                type: integer
                description: Cumulative game points for the team this player belongs to
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 0=Bid, 1=Play, 2=TrickEnd, 3=RoundEnd, 4=GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
          description: Current round number (starts at 1)
        trickNumber:
          type: integer
          description: Current trick number within the round (1-8)
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        leadPlayerIdx:
          type: integer
          description: Index of the player who led the current trick
        dealerIdx:
          type: integer
          description: Index of the current dealer
        declarerIdx:
          type: integer
          description: Index of the player who placed the winning bid (-1 if not yet determined)
        contract:
          type: integer
          description: "Bid value of the winning bid: 0=None/Pass, 16, 20, 24, or 28"
          enum: [0, 16, 20, 24, 28]
        trumpSuit:
          type: integer
          description: "Current trump suit: 1=Spades, 2=Clubs, 3=Hearts, 4=Diamonds (0 if not yet determined)"
          minimum: 0
          maximum: 4
        trumpRevealed:
          type: boolean
          description: True once the hidden trump suit has been revealed during play
        bids:
          type: array
          items:
            type: integer
          description: Bid placed by each player in seat order (0=Pass, 16, 20, 24, 28; -1=not yet bid)
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        teamScores:
          type: array
          items:
            type: integer
          description: Cumulative game points — index 0 is teamA (seats 0&2), index 1 is teamB (seats 1&3)
        roundTeamPoints:
          type: array
          items:
            type: integer
          description: Card points won by each team in the current round — index 0 is teamA, index 1 is teamB
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (must-follow rule applied)
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: Index of the winning team (0=teamA, 1=teamB; -1 if undecided)
        isHumanTurn:
          type: boolean
        isHumanBidTurn:
          type: boolean
          description: True when it is the human's turn to bid
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetScore:
              type: integer

    CourtPieceRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `trump` (`t`), `play` (`p`),
            `next` (`n`), `nextround` (`nr`), `hint` (`h`), `setdifficulty` (`sd`),
            `setlimit` (`sl`), `log` (`l`).
          enum: [reset, r, trump, t, play, p, next, n, nextround, nr, hint, h, setdifficulty, sd, setlimit, sl, log, l]
          example: reset
        trumpSuit:
          type: integer
          description: Trump suit for the `trump` command. 1=Spades, 2=Clubs, 3=Hearts, 4=Diamonds.
          enum: [1, 2, 3, 4]
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        limit:
          type: integer
          description: Match point limit for `setlimit`.
          minimum: 1
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetScore:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    CourtPieceResponse:
      type: object
      description: Current state of the Court Piece game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              isCaller:
                type: boolean
                description: True if this player is the caller (Hakim) for the current round
              teamScore:
                type: integer
                description: Cumulative match points for the team this player belongs to
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 0=Trump, 1=Play, 2=TrickEnd, 3=RoundEnd, 4=GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
          description: Current round number (starts at 1)
        trickNumber:
          type: integer
          description: Current trick number within the round (1-13)
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        leadPlayerIdx:
          type: integer
          description: Index of the player who led the current trick
        callerIdx:
          type: integer
          description: Index of the caller (Hakim) for the current round
        trumpSuit:
          type: integer
          description: "Current trump suit: 1=Spades, 2=Clubs, 3=Hearts, 4=Diamonds (0 if not yet declared)"
          minimum: 0
          maximum: 4
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        teamScores:
          type: array
          items:
            type: integer
          description: Cumulative match points — index 0 is teamA (seats 0&2), index 1 is teamB (seats 1&3)
        roundTrickCounts:
          type: array
          items:
            type: integer
          description: Tricks won by each team in the current round — index 0 is teamA, index 1 is teamB
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (must-follow rule applied)
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: Index of the winning team (0=teamA, 1=teamB; -1 if undecided)
        isHumanTurn:
          type: boolean
        isHumanTrumpTurn:
          type: boolean
          description: True when it is the human caller's turn to declare trump
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetScore:
              type: integer

    BeziqueRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `play` (`p`), `meld` (`m`),
            `skip` (`s`), `next` (`n`, `nextround`), `hint` (`h`), `setdifficulty` (`sd`),
            `settarget` (`st`), `log` (`l`).
          enum: [reset, r, play, p, meld, m, skip, s, next, n, nextround, hint, h, setdifficulty, sd, settarget, st, log, l]
          example: reset
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        meldIndex:
          type: integer
          description: List index of the meld to declare. Required for the `meld` command.
          example: 0
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        target:
          type: integer
          description: Target score for `settarget`.
          minimum: 1
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetScore:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    BeziqueResponse:
      type: object
      description: Current state of the Bezique game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              score:
                type: integer
                description: Cumulative match score for this player
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 0=Play, 1=Meld, 2=TrickEnd, 3=RoundEnd, 4=GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
          description: Current deal number (starts at 1)
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        leadPlayerIdx:
          type: integer
          description: Index of the player who led the current trick
        trumpSuit:
          type: integer
          description: "Trump suit: 1=Spades, 2=Clubs, 3=Hearts, 4=Diamonds (0 if not yet set)"
          minimum: 0
          maximum: 4
        stockCount:
          type: integer
          description: Number of cards remaining in the stock (0 in Phase 2)
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        scores:
          type: array
          items:
            type: integer
          description: Cumulative match scores — index 0 is the human, index 1 is the CPU
        availableMelds:
          type: array
          description: Melds the trick winner may declare (Meld phase only)
          items:
            type: object
            properties:
              name:
                type: string
                description: Meld name (e.g. Marriage, Royal Marriage, Bezique, Four Aces)
              points:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (must-follow rule applied in Phase 2)
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: Index of the winning player (0=human, 1=CPU; -1 if undecided)
        isHumanTurn:
          type: boolean
        isHumanMeldTurn:
          type: boolean
          description: True when it is the human's turn to declare a meld
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetScore:
              type: integer

    EcarteRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `propose` (`pr`), `stand` (`st`),
            `accept` (`a`), `refuse` (`rf`), `discard` (`d`), `play` (`p`),
            `next` (`n`, `nextround`), `hint` (`h`), `setdifficulty` (`sd`),
            `settarget` (`tg`), `log` (`l`).
          enum: [reset, r, propose, pr, stand, st, accept, a, refuse, rf, discard, d, play, p, next, n, nextround, hint, h, setdifficulty, sd, settarget, tg, log, l]
          example: reset
        accept:
          type: boolean
          description: Dealer's response to a proposed exchange. True accepts, false refuses (used with the `accept`/`refuse` commands).
          example: true
        cardIndex:
          type: integer
          description: Hand index of the card to play. Required for the `play` command.
          example: 0
        discardIndices:
          type: array
          items:
            type: integer
          description: Hand indices of the cards to discard and redraw. Used with the `discard` command.
          example: [3, 4]
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        target:
          type: integer
          description: Target score for `settarget`.
          minimum: 1
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetScore:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    EcarteResponse:
      type: object
      description: Current state of the Écarté game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              score:
                type: integer
                description: Cumulative match score for this player
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 0=Exchange, 1=Play, 2=RoundEnd, 3=GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        negStep:
          type: integer
          description: "Exchange negotiation step: 0=elder to propose/stand, 1=dealer to accept/refuse, 2=both to discard/redraw"
        roundNumber:
          type: integer
          description: Current deal number (starts at 1)
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        leadPlayerIdx:
          type: integer
          description: Index of the player who led the current trick
        dealerIdx:
          type: integer
          description: Index of the dealer for the current deal (the elder is the other player)
        trumpSuit:
          type: integer
          description: "Trump suit: 1=Spades, 2=Clubs, 3=Hearts, 4=Diamonds (0 if not yet set)"
          minimum: 0
          maximum: 4
        trumpCard:
          $ref: '#/components/schemas/Card'
        stockRemaining:
          type: integer
          description: Number of cards remaining in the stock
        refusalByDealer:
          type: boolean
          description: True when the dealer refused the elder's exchange proposal this deal (arms the refusal penalty)
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        dealPoints:
          type: array
          items:
            type: integer
          description: Tricks taken so far this deal — index 0 is the human, index 1 is the CPU
        matchScore:
          type: array
          items:
            type: integer
          description: Cumulative match scores — index 0 is the human, index 1 is the CPU
        validPlays:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (must-follow rule applied in the Play phase)
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: Index of the winning player (0=human, 1=CPU; -1 if undecided)
        isHumanTurn:
          type: boolean
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetScore:
              type: integer

    PreferenceResponse:
      type: object
      description: Current state of the Preference game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              isDeclarer:
                type: boolean
                description: True if this player is the Declarer for the current round
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              score:
                type: integer
                description: Cumulative game points for this player
        phase:
          type: integer
          description: "Game phase: 0=Bid, 1=Play, 2=TrickEnd, 3=RoundEnd, 4=GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        declarerIdx:
          type: integer
          description: Index of the Declarer player for the current round (-1 if not yet determined)
        contract:
          type: integer
          description: "Game contract: 0=None/Pass, 1=Six, 2=Misere, 3=Seven, 4=Eight"
          enum: [0, 1, 2, 3, 4]
        trumpSuit:
          type: integer
          description: "Current trump suit: 1=Spades, 2=Clubs, 3=Hearts, 4=Diamonds (0 for Misere / no trump)"
          minimum: 0
          maximum: 4
        bids:
          type: array
          items:
            type: integer
          description: Bid placed by each player in seat order (0=Pass, 1=Six, 2=Misere, 3=Seven, 4=Eight; -1=not yet bid)
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        playerScores:
          type: array
          items:
            type: integer
          description: Cumulative game points per player (index = player seat)
        roundTricks:
          type: array
          items:
            type: integer
          description: Number of tricks won by each player in the current round (index = player seat)
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (must-follow filtered)
        gameEndFlag:
          type: boolean
        winnerPlayer:
          type: integer
          description: Index of the winning player (-1 if undecided)
        isHumanTurn:
          type: boolean
        isHumanBidTurn:
          type: boolean
          description: True when it is the human's turn to bid
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetScore:
              type: integer

    GanjifaResponse:
      type: object
      description: Current state of the Ganjifa game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              trickCount:
                type: integer
                description: Tricks won by this player in the current round
              score:
                type: integer
                description: Cumulative tricks won by this player across the match
        phase:
          type: integer
          description: "Game phase: 0=Play, 1=TrickEnd, 2=RoundEnd, 3=GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        trumpSuit:
          type: integer
          description: >
            Trump suit as a Ganjifa design (1-8), auto-declared each round as the dealer's
            longest suit. **Designs 1-4 are the strong group and 5-8 the weak group**, and
            the rank order inverts between them: in a strong suit the 12 is the highest
            card, in a weak suit the 1 is. A client that ranks by raw value gets the
            opposite answer for half the deck.
          minimum: 0
          maximum: 8
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        playerScores:
          type: array
          items:
            type: integer
          description: Cumulative tricks won per player across the match (index = player seat)
        roundTricks:
          type: array
          items:
            type: integer
          description: Number of tricks won by each player in the current round (index = player seat)
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (must-follow filtered)
        gameEndFlag:
          type: boolean
        winnerPlayer:
          type: integer
          description: Index of the winning player (-1 if undecided, and also -1 on a tie)
        isHumanTurn:
          type: boolean
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetRounds:
              type: integer
    ViraResponse:
      type: object
      description: Current state of the Vira game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              isDeclarer:
                type: boolean
                description: True if this player is the Declarer for the current round
              cardCount:
                type: integer
              cards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              score:
                type: integer
                description: Cumulative game points for this player
        phase:
          type: integer
          description: "Game phase: 0=Bid, 1=Play, 2=TrickEnd, 3=RoundEnd, 4=GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
        trickNumber:
          type: integer
        currentPlayerIdx:
          type: integer
        leadPlayerIdx:
          type: integer
        dealerIdx:
          type: integer
        declarerIdx:
          type: integer
          description: Index of the Declarer player for the current round (-1 if not yet determined)
        contract:
          type: integer
          description: "Game contract: 0=None/Pass, 1=Six, 2=Misere, 3=Seven, 4=Eight"
          enum: [0, 1, 2, 3, 4]
        trumpSuit:
          type: integer
          description: "Current trump suit: 1=Spades, 2=Clubs, 3=Hearts, 4=Diamonds (0 for Misere / no trump)"
          minimum: 0
          maximum: 4
        bids:
          type: array
          items:
            type: integer
          description: Bid placed by each player in seat order (0=Pass, 1=Six, 2=Misere, 3=Seven, 4=Eight; -1=not yet bid)
        currentTrick:
          type: array
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              card:
                $ref: '#/components/schemas/Card'
        playerScores:
          type: array
          items:
            type: integer
          description: Cumulative game points per player (index = player seat)
        roundTricks:
          type: array
          items:
            type: integer
          description: Number of tricks won by each player in the current round (index = player seat)
        playableIndices:
          type: array
          items:
            type: integer
          description: Hand indices the human may legally play (must-follow filtered)
        gameEndFlag:
          type: boolean
        winnerPlayer:
          type: integer
          description: Index of the winning player (-1 if undecided)
        isHumanTurn:
          type: boolean
        isHumanBidTurn:
          type: boolean
          description: True when it is the human's turn to bid
        hint:
          type: object
          properties:
            cardIndices:
              type: array
              items:
                type: integer
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            targetScore:
              type: integer
    ThreeCardBragRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `see` (`s`), `bet` (`b`),
            `raise` (`rs`), `fold` (`f`), `show` (`sh`), `next` (`n`, `nextround`),
            `hint` (`h`), `setdifficulty` (`sd`), `setante` (`sa`), `setchips` (`sc`),
            `log` (`l`).
          enum: [reset, r, see, s, bet, b, raise, rs, fold, f, show, sh, next, n, nextround, hint, h, setdifficulty, sd, setante, sa, setchips, sc, log, l]
          example: bet
        raiseStake:
          type: integer
          description: Amount to raise the stake by. Required for the `raise` command.
          minimum: 1
          example: 10
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        ante:
          type: integer
          description: Ante paid into the pot at the start of each deal, for `setante`.
          minimum: 1
        startingChips:
          type: integer
          description: Number of chips each player starts with, for `setchips`.
          minimum: 1
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            ante:
              type: integer
              minimum: 1
            startingChips:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    ThreeCardBragResponse:
      type: object
      description: Current state of the Three Card Brag game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              chips:
                type: integer
                description: Chips this player currently holds
              seen:
                type: boolean
                description: True if the player has looked at their hand (Seen); false if still Blind
              folded:
                type: boolean
                description: True if the player has folded out of the current deal
              out:
                type: boolean
                description: True if the player has been eliminated from the match (out of chips)
              cardCount:
                type: integer
              cards:
                type: array
                description: The player's 3 cards (revealed only for the human when Seen, or at showdown)
                items:
                  $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 0=Betting, 1=Showdown, 2=RoundEnd, 3=GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
          description: Current deal number (starts at 1)
        pot:
          type: integer
          description: Total chips currently in the pot
        stake:
          type: integer
          description: Current per-action stake (a Blind player pays this, a Seen player pays double)
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        dealerIdx:
          type: integer
          description: Index of the dealer for the current deal
        isShowdown:
          type: boolean
          description: True when a showdown is being resolved this deal
        canShow:
          type: boolean
          description: True when the human (Seen, two players remaining) may call a show
        roundWinnerIdx:
          type: integer
          description: Index of the player who won the current/last deal (-1 if undecided)
        matchWinnerIdx:
          type: integer
          description: Index of the match winner (last player with chips; -1 if undecided)
        validActions:
          type: array
          items:
            type: string
          description: Action commands the human may legally issue this turn (e.g. see, bet, raise, fold, show)
        gameEndFlag:
          type: boolean
        isHumanTurn:
          type: boolean
        hint:
          type: object
          properties:
            action:
              type: string
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            ante:
              type: integer
            startingChips:
              type: integer

    TeenPattiRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `see` (`s`), `bet` (`b`),
            `raise` (`rs`), `fold` (`f`), `show` (`sh`), `sideshow` (`ss`),
            `accept` (`ac`), `decline` (`dc`), `next` (`n`, `nextround`), `hint` (`h`),
            `setdifficulty` (`sd`), `setante` (`sa`), `setchips` (`sc`), `log` (`l`).
          enum: [reset, r, see, s, bet, b, raise, rs, fold, f, show, sh, sideshow, ss, accept, ac, decline, dc, next, n, nextround, hint, h, setdifficulty, sd, setante, sa, setchips, sc, log, l]
          example: bet
        raiseStake:
          type: integer
          description: Amount to raise the stake by. Required for the `raise` command.
          minimum: 1
          example: 10
        accept:
          type: boolean
          description: Response to a side show request (true to accept, false to decline). Used with `accept` / `decline`.
        difficulty:
          type: integer
          description: CPU difficulty level for `setdifficulty`. 0=Easy, 1=Normal, 2=Hard.
          minimum: 0
          maximum: 2
        ante:
          type: integer
          description: Ante (boot) paid into the pot at the start of each deal, for `setante`.
          minimum: 1
        startingChips:
          type: integer
          description: Number of chips each player starts with, for `setchips`.
          minimum: 1
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            ante:
              type: integer
              minimum: 1
            startingChips:
              type: integer
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    TeenPattiResponse:
      type: object
      description: Current state of the Teen Patti game
      properties:
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              chips:
                type: integer
                description: Chips this player currently holds
              seen:
                type: boolean
                description: True if the player has looked at their hand (Seen); false if still Blind
              folded:
                type: boolean
                description: True if the player has folded out of the current deal
              out:
                type: boolean
                description: True if the player has been eliminated from the match (out of chips)
              cardCount:
                type: integer
              cards:
                type: array
                description: The player's 3 cards (revealed only for the human when Seen, or at showdown)
                items:
                  $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 0=Betting, 1=SideShow, 2=Showdown, 3=RoundEnd, 4=GameEnd"
          enum: [0, 1, 2, 3, 4]
          example: 0
        roundNumber:
          type: integer
          description: Current deal number (starts at 1)
        pot:
          type: integer
          description: Total chips currently in the pot
        stake:
          type: integer
          description: Current per-action stake (a Blind player pays this, a Seen player pays double)
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        dealerIdx:
          type: integer
          description: Index of the dealer for the current deal
        isShowdown:
          type: boolean
          description: True when a showdown is being resolved this deal
        canShow:
          type: boolean
          description: True when the human (Seen, two players remaining) may call a show
        canRequestSideShow:
          type: boolean
          description: True when the human (Seen, three or more players remaining) may request a side show against the previous Seen player
        sideShowRequester:
          type: integer
          description: Index of the player who requested the pending side show (-1 if none)
        sideShowTarget:
          type: integer
          description: Index of the player being asked to accept or decline the pending side show (-1 if none)
        roundWinnerIdx:
          type: integer
          description: Index of the player who won the current/last deal (-1 if undecided)
        matchWinnerIdx:
          type: integer
          description: Index of the match winner (last player with chips; -1 if undecided)
        validActions:
          type: array
          items:
            type: string
          description: Action commands the human may legally issue this turn (e.g. see, bet, raise, fold, sideshow, accept, decline, show)
        gameEndFlag:
          type: boolean
        isHumanTurn:
          type: boolean
        hint:
          type: object
          properties:
            action:
              type: string
            reason:
              type: string
        message:
          type: string
        messageCode:
          type: string
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
            ante:
              type: integer
            startingChips:
              type: integer

    SpoonsRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `pass` (`p`), `grab` (`g`),
            `next` (`n`), `log` (`l`).
          enum: [reset, r, pass, p, grab, g, next, n, log, l]
          example: pass
        cardIndex:
          type: integer
          description: Index of the card to pass to the next player. Required for the `pass` command.
          minimum: 0
          example: 2
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    SpoonsResponse:
      type: object
      description: Current state of the Spoons game
      properties:
        phase:
          type: integer
          description: "Game phase: 0=Pass, 1=Grab, 2=RoundEnd, 3=GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: Index of the winning player (-1 until the game ends)
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        feederIdx:
          type: integer
          description: Index of the feeder who draws from the draw pile this round
        isHumanTurn:
          type: boolean
        spoonsRemaining:
          type: integer
          description: Spoons still on the table to be grabbed
        grabWindowOpen:
          type: boolean
          description: True when the grab window is open (race to grab a spoon)
        firstGrabberIdx:
          type: integer
          description: Index of the first player to grab this round (-1 until one grabs)
        roundLoserIdx:
          type: integer
          description: Index of the player who missed out this round (-1 until decided)
        roundNumber:
          type: integer
          description: Current round number (starts at 1)
        drawPileSize:
          type: integer
          description: Cards remaining in the feeder's draw pile
        players:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              isHuman:
                type: boolean
              handSize:
                type: integer
              hand:
                type: array
                description: The player's cards (populated only for the human)
                items:
                  $ref: '#/components/schemas/Card'
              letters:
                type: integer
                description: Number of S-P-O-O-N-S letters collected (0-6)
              eliminated:
                type: boolean
              hasSpoon:
                type: boolean
        cpuDifficulty:
          type: integer
        message:
          type: string
        messageCode:
          type: string

    KempsRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `swap` (`s`), `pass` (`p`),
            `signal` (`sig`), `kemps` (`k`), `counter` (`c`), `next` (`n`), `log` (`l`).
          enum: [reset, r, swap, s, pass, p, signal, sig, kemps, k, counter, c, next, n, log, l]
          example: swap
        handIndex:
          type: integer
          description: Index of the hand card to swap. Required for the `swap` command.
          minimum: 0
          example: 1
        fieldIndex:
          type: integer
          description: Index of the field card to swap with. Required for the `swap` command.
          minimum: 0
          example: 2
        signalType:
          type: integer
          description: Secret signal type to set (0=Sound, 1=Blink). Required for the `signal` command.
          minimum: 0
          maximum: 1
          example: 1
        targetSeat:
          type: integer
          description: Opponent seat to call Counter-Kemps against. Required for the `counter` command.
          minimum: 0
          example: 1
        config:
          type: object
          description: Optional game configuration (only used with `reset`).
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
            targetScore:
              type: integer
              description: Team score required to win (default 5)
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    KempsResponse:
      type: object
      description: Current state of the Kemps game
      properties:
        phase:
          type: integer
          description: "Game phase: 0=Exchange, 1=Declare, 2=RoundEnd, 3=GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        gameEndFlag:
          type: boolean
        winnerTeam:
          type: integer
          description: Winning team (0 or 1), or -1 until the game ends
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        isHumanTurn:
          type: boolean
        teamScores:
          type: array
          description: Team scores indexed by team number (Team A = index 0)
          items:
            type: integer
        field:
          type: array
          description: The shared field of cards available to swap with
          items:
            $ref: '#/components/schemas/Card'
        signalType:
          type: integer
          description: The human secret signal type (0=Sound, 1=Blink)
        partnerSignaling:
          type: boolean
          description: Whether the human partner is currently signaling (human-only cue)
        opponentSignaling:
          type: boolean
          description: Whether an opponent may be signaling (vague human-only cue)
        fourHolderIdx:
          type: integer
          description: Seat index that completed four of a kind, or -1
        roundResult:
          type: integer
          description: "Round result code: 0=none, 1=Kemps, 2=Counter, 3=CounterFail, 4=Miss"
        roundWinnerTeam:
          type: integer
          description: Team that won the most recent round, or -1
        roundNumber:
          type: integer
          description: Current round number (starts at 1)
        players:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              isHuman:
                type: boolean
              team:
                type: integer
                description: Team number (0=Team A, 1=Team B)
              handSize:
                type: integer
              hand:
                type: array
                description: The player's cards (populated only for the human)
                items:
                  $ref: '#/components/schemas/Card'
              hasFourOfAKind:
                type: boolean
                description: Whether this player holds four of a kind (human only)
        cpuDifficulty:
          type: integer
        targetScore:
          type: integer
          description: Team score required to win (default 5)
        message:
          type: string
        messageCode:
          type: string

    CuckooRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `keep` (`k`), `swap` (`s`),
            `refuse` (`rf`), `accept` (`ac`), `nextround` (`nr`), `setdifficulty` (`sd`),
            `log` (`l`).
          enum: [reset, r, keep, k, swap, s, refuse, rf, accept, ac, nextround, nr, setdifficulty, sd, log, l]
          example: swap
        config:
          type: object
          description: Optional game configuration (used with `reset` and `setdifficulty`).
          properties:
            cpuDifficulty:
              type: integer
              description: CPU difficulty (0 Easy, 1 Normal, 2 Hard)
              minimum: 0
              maximum: 2
            initialLives:
              type: integer
              description: Lives each player starts with (default 3)
              minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    CuckooResponse:
      type: object
      description: Current state of the Cuckoo game
      properties:
        phase:
          type: integer
          description: "Game phase: 0=Turn, 1=Refuse, 2=RoundEnd, 3=GameEnd"
          enum: [0, 1, 2, 3]
          example: 0
        roundNumber:
          type: integer
          description: Current round number (starts at 1)
        currentPlayerIdx:
          type: integer
          description: Index of the player whose turn it is
        dealerIdx:
          type: integer
          description: Index of the dealer (swaps with the stock instead of a neighbour)
        stockCount:
          type: integer
          description: Number of cards remaining in the stock
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: Index of the winning player, or -1 until the game ends
        pendingSwapFrom:
          type: integer
          description: Seat initiating a pending swap during the Refuse phase, or -1
        pendingSwapTo:
          type: integer
          description: Seat receiving a pending swap during the Refuse phase, or -1
        roundLowest:
          type: integer
          description: Rank value of the lowest card in the scored round, or -1
        roundLosers:
          type: array
          description: Seat indices that lost a life in the most recent round
          items:
            type: integer
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              card:
                description: The player's card (populated only when visible to the human)
                $ref: '#/components/schemas/Card'
              lives:
                type: integer
                description: Remaining lives
              isEliminated:
                type: boolean
              kingRevealed:
                type: boolean
                description: Whether this player revealed a King to refuse a swap
              isCurrentTurn:
                type: boolean
        config:
          type: object
          properties:
            cpuDifficulty:
              type: integer
              description: CPU difficulty (0 Easy, 1 Normal, 2 Hard)
            initialLives:
              type: integer
              description: Lives each player starts with (default 3)
        message:
          type: string
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    PishtiRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `play` (`p`), `next` (`n`),
            `setdifficulty` (`sd`), `setplayers` (`sp`), `log` (`l`).
          enum: [reset, r, play, p, next, n, setdifficulty, sd, setplayers, sp, log, l]
          example: play
        handIndex:
          type: integer
          description: Index of the hand card to play onto the pile (used with `play`).
          minimum: 0
        config:
          type: object
          description: Optional game configuration (used with `reset`, `setdifficulty`, `setplayers`).
          properties:
            playerCnt:
              type: integer
              description: Number of players (2-4, default 4)
              minimum: 2
              maximum: 4
            cpuDifficulty:
              type: integer
              description: CPU difficulty (0 Easy, 1 Normal, 2 Hard)
              minimum: 0
              maximum: 2
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    PishtiResponse:
      type: object
      description: Current state of the Pişti game
      properties:
        phase:
          type: string
          description: "Game phase: play, roundEnd, gameEnd"
          enum: [play, roundEnd, gameEnd]
          example: play
        currentTurn:
          type: integer
          description: Index of the player whose turn it is
        pile:
          type: array
          description: All cards currently on the central pile, bottom to top
          items:
            $ref: '#/components/schemas/Card'
        pileTop:
          description: The top card of the pile, or null when the pile is empty
          $ref: '#/components/schemas/Card'
        pileCount:
          type: integer
          description: Number of cards on the pile
        lastCaptureIdx:
          type: integer
          description: Seat index of the most recent capturer, or -1
        gameEndFlag:
          type: boolean
        remainingDeck:
          type: integer
          description: Number of cards remaining in the stock
        winners:
          type: array
          description: Winning seat indices (may tie), empty until the game ends
          items:
            type: integer
        finalScores:
          type: array
          description: Final scores indexed by seat, empty until the game ends
          items:
            type: integer
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              cardCount:
                type: integer
                description: Number of cards currently in hand
              cards:
                type: array
                description: The player's hand cards (populated only for the human)
                items:
                  $ref: '#/components/schemas/Card'
              capturedCount:
                type: integer
                description: Total number of cards captured so far
              pistiBonus:
                type: integer
                description: Accumulated Pişti bonus points
              finalScore:
                type: integer
                description: Final score (populated once the game ends)
        config:
          type: object
          properties:
            playerCnt:
              type: integer
              description: Number of players (2-4)
            cpuDifficulty:
              type: integer
              description: CPU difficulty (0 Easy, 1 Normal, 2 Hard)
        message:
          type: string
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    CuarentaRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `play` (`p`), `next` (`n`),
            `setdifficulty` (`sd`), `log` (`l`).
          enum: [reset, r, play, p, next, n, setdifficulty, sd, log, l]
          example: play
        handIndex:
          type: integer
          description: Index of the hand card to play onto the table (used with `play`).
          minimum: 0
        config:
          type: object
          description: Optional game configuration (used with `reset`, `setdifficulty`).
          properties:
            targetScore:
              type: integer
              description: Score needed to win the match (default 40)
              minimum: 1
            cpuDifficulty:
              type: integer
              description: CPU difficulty (0 Easy, 1 Normal, 2 Hard)
              minimum: 0
              maximum: 2
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    CuarentaResponse:
      type: object
      description: Current state of the Cuarenta game
      properties:
        phase:
          type: integer
          description: "Game phase: 0 Play, 1 RoundEnd, 2 GameEnd"
          enum: [0, 1, 2]
          example: 0
        currentTurn:
          type: integer
          description: Index of the player whose turn it is
        tableCards:
          type: array
          description: All cards currently on the central table
          items:
            $ref: '#/components/schemas/Card'
        lastCaptureIdx:
          type: integer
          description: Seat index of the most recent capturer, or -1
        gameEndFlag:
          type: boolean
        remainingDeck:
          type: integer
          description: Number of cards remaining in the stock
        roundWinners:
          type: array
          description: Winning team indices (may tie), empty until the match ends
          items:
            type: integer
        teamScores:
          type: array
          description: Match scores indexed by team (0 Team A, 1 Team B)
          items:
            type: integer
        players:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              isHuman:
                type: boolean
              team:
                type: integer
                description: Team index (0 Team A, 1 Team B)
              cardCount:
                type: integer
                description: Number of cards currently in hand
              cards:
                type: array
                description: The player's hand cards (populated only for the human)
                items:
                  $ref: '#/components/schemas/Card'
              capturedCount:
                type: integer
                description: Total number of cards captured so far this round
        humanAction:
          type: object
          nullable: true
          description: The human's most recent play action, or null
          properties:
            playerIdx:
              type: integer
            playedCard:
              $ref: '#/components/schemas/Card'
            capturedCards:
              type: array
              items:
                $ref: '#/components/schemas/Card'
            isCaida:
              type: boolean
            isLimpia:
              type: boolean
            rondaBonus:
              type: integer
        cpuActions:
          type: array
          description: CPU play actions since the last human play
          items:
            type: object
            properties:
              playerIdx:
                type: integer
              playedCard:
                $ref: '#/components/schemas/Card'
              capturedCards:
                type: array
                items:
                  $ref: '#/components/schemas/Card'
              isCaida:
                type: boolean
              isLimpia:
                type: boolean
              rondaBonus:
                type: integer
        config:
          type: object
          properties:
            targetScore:
              type: integer
              description: Score needed to win the match (default 40)
            cpuDifficulty:
              type: integer
              description: CPU difficulty (0 Easy, 1 Normal, 2 Hard)
        message:
          type: string
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    FaroRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `bet` (`b`), `clearBet` (`cb`),
            `clearAll` (`ca`), `deal` (`d`), `call`, `next` (`n`), `log` (`l`).
          enum: [reset, r, bet, b, clearBet, cb, clearAll, ca, deal, d, call, next, n, log, l]
          example: bet
        rank:
          type: integer
          description: Layout rank to bet on or clear (1 is A to 13 is K); used with `bet` and `clearBet`.
          minimum: 1
          maximum: 13
        amount:
          type: integer
          description: Chip amount to wager (used with `bet`).
          minimum: 1
        copper:
          type: boolean
          description: When true the bet wagers the rank to lose rather than win (used with `bet`).
        order:
          type: array
          description: Predicted order of the final three ranks (used with `call`); empty to skip the call.
          items:
            type: integer
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    FaroResponse:
      type: object
      description: Current state of the Faro game
      properties:
        phase:
          type: integer
          description: "Game phase: 1 Betting, 2 Turn, 3 Call, 4 RoundEnd, 5 GameEnd"
          enum: [1, 2, 3, 4, 5]
          example: 1
        chips:
          type: integer
          description: The player's remaining bankroll in chips
        bets:
          type: array
          description: Chips currently placed on the layout, one entry per wagered rank
          items:
            type: object
            properties:
              rank:
                type: integer
                description: Rank the chip is placed on (1 is A to 13 is K)
              amount:
                type: integer
                description: Wagered chip amount
              copper:
                type: boolean
                description: When true the bet wagers the rank to lose
        soda:
          allOf:
            - $ref: '#/components/schemas/Card'
          nullable: true
          description: The burned soda card (first card of the deal), or null before the deal
        losingCard:
          allOf:
            - $ref: '#/components/schemas/Card'
          nullable: true
          description: The most recent turn's losing card (first turned), or null
        winningCard:
          allOf:
            - $ref: '#/components/schemas/Card'
          nullable: true
          description: The most recent turn's winning card (second turned), or null
        split:
          type: boolean
          description: True when the last turn was a split (both cards the same rank)
        turnsPlayed:
          type: integer
          description: Number of turns dealt so far this round
        turnsTotal:
          type: integer
          description: Total number of turns in a full round
        remaining:
          type: integer
          description: Number of cards still left in the dealing box
        callCards:
          type: array
          description: The final three cards available to call (populated during the Call phase)
          items:
            $ref: '#/components/schemas/Card'
        callOrder:
          type: array
          description: The player's predicted order for the called cards (rank values), empty when none
          items:
            type: integer
        callWon:
          type: boolean
          description: True when the most recent call was correct (paid 4 to 1)
        totalPayout:
          type: integer
          description: Net chip change for the just-finished round
        gameEndFlag:
          type: boolean
        message:
          type: string
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    OpenFaceChineseRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `place` (`p`), `nextround` (`nr`),
            `setdifficulty` (`sd`), `setplayers` (`sp`), `hint`, `log` (`l`).
          enum: [reset, r, place, p, nextround, nr, setdifficulty, sd, setplayers, sp, hint, log, l]
          example: place
        row:
          type: integer
          description: Row to place the pending card into (0 front, 1 middle, 2 back); used with `place`.
          minimum: 0
          maximum: 2
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    OpenFaceChineseResponse:
      type: object
      description: Current state of the Open Face Chinese Poker (OFC) game
      properties:
        phase:
          type: integer
          description: "Game phase: 0 Placing, 1 RoundEnd, 2 GameEnd"
          enum: [0, 1, 2]
          example: 0
        roundNumber:
          type: integer
          description: 1-based round number
        currentPlayerIdx:
          type: integer
          description: Seat index whose turn it currently is
        dealerIdx:
          type: integer
          description: Seat index of the current dealer
        currentCard:
          allOf:
            - $ref: '#/components/schemas/Card'
          nullable: true
          description: The card the human must place this turn (present only on the human's turn)
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: Winning seat index, or -1 for a draw
        isHumanTurn:
          type: boolean
          description: True when it is the human player's turn to place a card
        hint:
          type: object
          nullable: true
          description: Optional placement hint (present only on a hint request)
          properties:
            row:
              type: integer
              description: Suggested row (0 front, 1 middle, 2 back)
            reason:
              type: string
              description: Human-readable rationale for the suggestion
        players:
          type: array
          description: One entry per player
          items:
            type: object
            properties:
              id:
                type: integer
                description: Seat index (0 is the human)
              isHuman:
                type: boolean
              front:
                type: array
                description: Top row (up to 3 cards)
                items:
                  $ref: '#/components/schemas/Card'
              middle:
                type: array
                description: Middle row (up to 5 cards)
                items:
                  $ref: '#/components/schemas/Card'
              back:
                type: array
                description: Bottom row (up to 5 cards)
                items:
                  $ref: '#/components/schemas/Card'
              pending:
                type: array
                description: The pending card(s) awaiting placement (human only; empty for CPU)
                items:
                  $ref: '#/components/schemas/Card'
              roundScore:
                type: integer
                description: Net points scored in the just-finished round
              royalty:
                type: integer
                description: Royalty bonus points earned this round
              fouled:
                type: boolean
                description: True when the hand is fouled (rows not in non-decreasing strength order)
              fantasyland:
                type: boolean
                description: True when the player qualified for Fantasyland
              totalScore:
                type: integer
                description: Cumulative score across all rounds
        config:
          type: object
          description: Echoed game configuration
          properties:
            cpuDifficulty:
              type: integer
            playerCount:
              type: integer
            targetRounds:
              type: integer
        message:
          type: string
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    RussianBankRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command. Accepted values: `reset` (`r`), `pf` (move source to a foundation),
            `mt` (move source to tableau `toCol`), `d` (discard / end turn), `s` (stop),
            `u` (undo), `hint`, `log` (`l`).
          enum: [reset, r, pf, mt, d, s, u, hint, log, l]
          example: pf
        zone:
          type: integer
          description: Move source zone (0 reserve, 1 waste, 2 tableau); used with `pf` and `mt`.
          minimum: 0
          maximum: 2
        fromOpp:
          type: boolean
          description: True to take the source from the opponent's pile (reserve or waste).
        col:
          type: integer
          description: Source tableau column (used when zone is 2).
          minimum: 0
          maximum: 3
        toCol:
          type: integer
          description: Destination tableau column; used with `mt`.
          minimum: 0
          maximum: 3
        config:
          type: object
          description: Optional game configuration.
          properties:
            cpuDifficulty:
              type: integer
              minimum: 0
              maximum: 2
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    RussianBankResponse:
      type: object
      description: Current state of the Russian Bank (Crapette) game
      properties:
        phase:
          type: integer
          description: "Game phase: 0 Idle, 1 Playing, 2 GameEnd"
          enum: [0, 1, 2]
          example: 1
        currentPlayerIdx:
          type: integer
          description: Seat index whose turn it currently is
        gameEndFlag:
          type: boolean
        winnerIdx:
          type: integer
          description: Winning seat index, or -1 for a draw
        isHumanTurn:
          type: boolean
          description: True when it is the human player's turn
        canCallStop:
          type: boolean
          description: True when the human may call stop on the CPU
        canUndo:
          type: boolean
          description: True when the human's last move can be undone
        moveCount:
          type: integer
          description: Total moves played so far
        tableau:
          type: array
          description: The 4 shared tableau columns (top card is last)
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        foundations:
          type: array
          description: The 8 shared foundations (A-up by suit; top card is last)
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        hint:
          type: object
          nullable: true
          description: Optional move hint (present only on a hint request)
          properties:
            zone:
              type: integer
              description: Source zone (0 reserve, 1 waste, 2 tableau)
            fromOpponent:
              type: boolean
            col:
              type: integer
              description: Source tableau column
            toFoundation:
              type: boolean
              description: True when the destination is a foundation
            toCol:
              type: integer
              description: Destination tableau column when toFoundation is false
        players:
          type: array
          description: One entry per player (index 0 is the human)
          items:
            type: object
            properties:
              id:
                type: integer
                description: Seat index (0 is the human)
              isHuman:
                type: boolean
              reserveCount:
                type: integer
                description: Cards left in the reserve (empty it to win)
              reserveTop:
                allOf:
                  - $ref: '#/components/schemas/Card'
                nullable: true
                description: Top reserve card (face up), if any
              handCount:
                type: integer
                description: Face-down cards left in hand
              wasteCount:
                type: integer
              wasteTop:
                allOf:
                  - $ref: '#/components/schemas/Card'
                nullable: true
                description: Top waste card, if any
              stopPoints:
                type: integer
                description: Times this player caught the opponent with stop
        config:
          type: object
          description: Echoed game configuration
          properties:
            cpuDifficulty:
              type: integer
        message:
          type: string
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    LaBelleLucieRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command: `reset` (`r`), `mf` (fan to fan), `ff` (fan to foundation),
            `rd` (redeal), `ac` (autocomplete), `u` (undo), `undo_n`, `g` (giveup),
            `hint`, `log` (`l`).
          enum: [reset, r, mf, ff, rd, ac, u, undo_n, g, hint, log, l]
          example: mf
        from:
          type: integer
          description: Source fan index; used with `mf` and `ff`.
          minimum: 0
        to:
          type: integer
          description: Destination fan index; used with `mf`.
          minimum: 0
        n:
          type: integer
          description: Number of moves to undo; used with `undo_n`.
          minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    LaBelleLucieResponse:
      type: object
      description: Current state of the La Belle Lucie game
      properties:
        fans:
          type: array
          description: The fans (top card is last); the count varies after a redeal
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        foundation:
          type: array
          description: The 4 foundations (A-up by suit)
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        redealsLeft:
          type: integer
          description: Remaining gather-and-reshuffle redeals (0 to 3)
        phase:
          type: integer
          description: "Game phase: 0 Playing, 1 GameClear, 2 GameOver"
          enum: [0, 1, 2]
          example: 0
        moveCount:
          type: integer
        canUndo:
          type: boolean
        hint:
          type: object
          nullable: true
          description: Optional move hint (present only on a hint request)
          properties:
            fromFan:
              type: integer
            toFan:
              type: integer
            toFoundation:
              type: boolean
        message:
          type: string
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    SimpleSimonRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command: `reset` (`r`), `m` (move), `u` (undo), `undo_n`, `g` (giveup),
            `hint`, `log` (`l`).
          enum: [reset, r, m, u, undo_n, g, hint, log, l]
          example: m
        fromCol:
          type: integer
          description: Source column index; used with `m`.
          minimum: 0
          maximum: 9
        cardIndex:
          type: integer
          description: Index within the source column of the first card to move; used with `m`.
          minimum: 0
        toCol:
          type: integer
          description: Destination column index; used with `m`.
          minimum: 0
          maximum: 9
        n:
          type: integer
          description: Number of moves to undo; used with `undo_n`.
          minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    SimpleSimonResponse:
      type: object
      description: Current state of the Simple Simon game
      properties:
        columns:
          type: array
          description: The 10 tableau columns (top card is last)
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        completedSuits:
          type: integer
          description: Number of complete K-A suits removed (0 to 4)
        phase:
          type: integer
          description: "Game phase: 0 Playing, 1 GameClear, 2 GameOver"
          enum: [0, 1, 2]
          example: 0
        moveCount:
          type: integer
        canUndo:
          type: boolean
        hint:
          type: object
          nullable: true
          description: Optional move hint (present only on a hint request)
          properties:
            fromCol:
              type: integer
            cardIndex:
              type: integer
            toCol:
              type: integer
        message:
          type: string
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    DoubleKlondikeRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command: `reset` (`r`), `d` (draw), `mwt`, `mwf`, `mtt`, `mtf`,
            `ac` (autocomplete), `u` (undo), `undo_n`, `g` (giveup), `hint`, `log` (`l`).
          enum: [reset, r, d, mwt, mwf, mtt, mtf, ac, u, undo_n, g, hint, log, l]
          example: d
        col:
          type: integer
          description: Tableau column index; used with `mwt` and `mtf`.
          minimum: 0
          maximum: 8
        fromCol:
          type: integer
          description: Source column index; used with `mtt`.
          minimum: 0
          maximum: 8
        cardIndex:
          type: integer
          description: Index within the source column of the first card to move; used with `mtt`.
          minimum: 0
        toCol:
          type: integer
          description: Destination column index; used with `mtt`.
          minimum: 0
          maximum: 8
        n:
          type: integer
          description: Number of moves to undo; used with `undo_n`.
          minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    DoubleKlondikeResponse:
      type: object
      description: Current state of the Double Klondike game
      properties:
        tableau:
          type: array
          description: The 9 tableau columns (top card is last); face-down cards hide their value
          items:
            type: array
            items:
              type: object
              properties:
                card:
                  $ref: '#/components/schemas/Card'
                faceUp:
                  type: boolean
        stockCount:
          type: integer
          description: Number of cards left in the stock
        waste:
          type: array
          description: The waste pile (top card is last)
          items:
            $ref: '#/components/schemas/Card'
        foundation:
          type: array
          description: The 8 foundations (two per suit), each built up A to K
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 0 Playing, 1 GameClear, 2 GameOver"
          enum: [0, 1, 2]
          example: 0
        moveCount:
          type: integer
        canUndo:
          type: boolean
        isStalemate:
          type: boolean
        hint:
          type: object
          nullable: true
          description: Optional move hint (present only on a hint request)
          properties:
            fromZone:
              type: string
            fromCol:
              type: integer
            cardIndex:
              type: integer
            toZone:
              type: string
            toCol:
              type: integer
        message:
          type: string
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.

    BlackHoleRequest:
      type: object
      required:
        - command
        - sessionId
      properties:
        command:
          type: string
          description: |
            Game command: `reset` (`r`), `m` / `mb` (play a fan top), `u` (undo),
            `undo_n`, `g` (giveup), `hint`, `log` (`l`).
          enum: [reset, r, m, mb, u, undo_n, g, hint, log, l]
          example: m
        fan:
          type: integer
          description: Source fan index; used with `m`.
          minimum: 0
          maximum: 16
        n:
          type: integer
          description: Number of moves to undo; used with `undo_n`.
          minimum: 1
        sessionId:
          type: string
          description: Client-generated session identifier (max 256 characters)
          example: my-session-001

    BlackHoleResponse:
      type: object
      description: Current state of the Black Hole game
      properties:
        fans:
          type: array
          description: The 17 fans (top card is last)
          items:
            type: array
            items:
              $ref: '#/components/schemas/Card'
        blackHole:
          type: array
          description: The central black hole pile (top card is last)
          items:
            $ref: '#/components/schemas/Card'
        phase:
          type: integer
          description: "Game phase: 0 Playing, 1 GameClear, 2 GameOver"
          enum: [0, 1, 2]
          example: 0
        moveCount:
          type: integer
        canUndo:
          type: boolean
        isStalemate:
          type: boolean
        hint:
          type: object
          nullable: true
          description: Optional move hint (present only on a hint request)
          properties:
            fan:
              type: integer
        message:
          type: string
        messageCode:
          type: string
          description: Machine-readable message code corresponding to the message field.
        messageParams:
          type: object
          additionalProperties:
            type: string
          description: Interpolation parameters for the message template keyed by placeholder name.
