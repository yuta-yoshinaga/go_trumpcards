# マカオ（CUI版）遊び方

## ゲーム概要

4人（プレイヤー1人 + CPU 3人）で遊ぶ、クレイジーエイトの欧州系バリエーションです。捨て札のトップとスートまたはランクが一致するカードを出していき、先に手札を出し切ったプレイヤーがラウンドの勝者です。「マジックカード」（2=ドローツー、7=スキップ、8=ワイルド、J=リバース）によるペナルティの応酬と、残り1枚での「マカオ！」宣言が最大の見どころです。

## 起動方法

```sh
go run ./cmd/trumpcards macau
go run ./cmd/trumpcards --lang en macau  # 英語モード
```

## ルール

### 基本ルール

- 52枚のトランプを使用、4人プレイ
- 各プレイヤーに5枚ずつ配り、1枚を表向きにして捨て札の山を開始、残りは山札
- 捨て札のトップとスートまたはランクが一致するカードを出す
- 出せるカードがなければ山札から1枚ドロー。出せればそのまま続行、出せなければパス
- 山札が空になったら捨て札（トップを除く）をリサイクルして山札にする

### マジックカード

- **2（ドローツー）**: 次のプレイヤーは2枚引く。2を重ねて出すとペナルティが累積し（+2枚）、次へ回せる。重ねられない（または出さない）プレイヤーが累積分をまとめて引き受ける
- **7（スキップ）**: 次のプレイヤーの手番を飛ばす
- **8（ワイルド）**: いつでも出せ、出した後に次のスートを指定できる
- **J（リバース）**: プレイの進行方向を反転する

### マカオ宣言

- 手札が残り1枚になったら「マカオ！」（`dc`）と宣言する必要がある
- 宣言せずにスキップ（`sk`）すると、ペナルティとして2枚引く

### 得点

- ラウンド終了時、他プレイヤーの手札がスコアとして加算される
- 8=50点、J/Q/K=10点、A=1点、その他=額面

### ゲーム終了

- ポイント上限（デフォルト200点）に達したプレイヤーが出たらマッチ終了

### CPU難易度

- **Easy** (0): ランダムにカードを出す（宣言を忘れることがある）
- **Normal** (1): 基本的な戦略（デフォルト）
- **Hard** (2): 戦略的なプレイ（8の温存等）

## ゲームの流れ

```mermaid
flowchart TD
    A[ゲーム開始 - reset] --> B[カード配布 - 各5枚]
    B --> C[捨て札の山に1枚表向き]
    C --> D{プレイヤーの手番?}
    D -->|はい| E{ペナルティ中?}
    D -->|いいえ| F[CPUが自動でプレイ]
    E -->|はい| G{2を持っている?}
    E -->|いいえ| H{出せるカードあり?}
    G -->|はい| I[2を重ねる または 引き受ける]
    G -->|いいえ| J[累積分を引き受けてパス]
    H -->|はい| K[カードを出す]
    H -->|いいえ| L[山札から1枚ドロー]
    I --> M
    K --> N{8を出した?}
    N -->|はい| O[スート選択]
    N -->|いいえ| M{手札が1枚?}
    O --> M
    M -->|はい| P[マカオ宣言 - dc / sk]
    M -->|いいえ| Q{手札が0枚?}
    P --> Q
    L --> D
    J --> D
    F --> D
    Q -->|はい| R[ラウンド終了 - スコア計算]
    Q -->|いいえ| D
    R --> S{ポイント上限到達?}
    S -->|いいえ| B
    S -->|はい| T[マッチ終了 - 結果表示]
    T -->|reset| A
```

## コマンド一覧

| コマンド | 短縮形 | 説明 |
|----------|--------|------|
| `reset` | `r` | ゲームリセット（設定保持） |
| `play <idx>` | `p <idx>` | カードをプレイ（インデックス指定） |
| `draw` | `d` | 山札から1枚ドロー（ペナルティ中は累積分を引き受ける） |
| `suit <1-4>` | `s <1-4>` | スート選択（8を出した後）: 1=♠, 2=♣, 3=♥, 4=♦ |
| `declare` | `dc` | 「マカオ！」と宣言する（残り1枚） |
| `skipdeclare` | `sk` | 宣言せずペナルティを受ける |
| `nextround` | `nr` | ラウンドをスコアリングして次のラウンドへ |
| `setdifficulty <0-2>` | `sd <0-2>` | CPU難易度設定（0=Easy, 1=Normal, 2=Hard） |
| `setlimit <n>` | `sl <n>` | ポイント上限設定 |
| `log` | `l` | 棋譜表示 |
| `quit` | `q` | ゲーム終了 |
| `help` | `?` | ヘルプ表示 |

### コマンド例

- `p 2` → インデックス2のカードを出す
- `d` → 山札から1枚ドロー（ペナルティ中は累積分を引き受ける）
- `s 3` → スートを♥に設定（8を出した後）
- `dc` → 「マカオ！」と宣言

## 画面の見方

```
==========
Macau (マカオ)
==========
ラウンド: 1  山札: 27枚  方向: →
捨て札: HEART 7
ドローペナルティ累積: 2枚 (2を重ねるか、引き受けてください)
You: 累計0点 ラウンド0点 5枚
[0]SPADE 3  [1]CLOVER 2  [2]HEART Q  [3]DIAMOND 5  [4]SPADE J
CPU 1: 累計0点 ラウンド0点 5枚
CPU 2: 累計0点 ラウンド0点 4枚
CPU 3: 累計0点 ラウンド0点 6枚
----------
手番: You
play <idx>・・・カードを出す  draw・・・ドロー
==========
```

- `ラウンド: N`: 現在のラウンド番号
- `山札: N枚`: 山札の残り枚数
- `方向: →/←`: 現在のプレイ進行方向（J=リバースで反転）
- `捨て札`: 捨て札の山の一番上のカード
- `ドローペナルティ累積`: 累積中のドローツー枚数（0枚なら非表示）
- `[0]SPADE 3`...: インデックス付きの手札カード
- `手番`: 現在の手番プレイヤー

## 遊び方のコツ

- 2は手元に温存し、ペナルティを受けそうなときに重ねて相手へ回しましょう
- 7（スキップ）や J（リバース）は、出し切りそうな相手の手番を飛ばす妨害に有効です
- 8（ワイルド）は切り札として温存し、出せるカードがない時に使いましょう
- 残り1枚になったら「マカオ！」宣言（`dc`）を忘れずに。忘れるとペナルティで2枚増えます
- 高得点カード（8=50点、J/Q/K=10点）は早めに処理してリスクを減らしましょう
