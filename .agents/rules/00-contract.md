---
trigger: always_on
---
# Contract Rules

委譲タスク実行時の絶対条件:

- **コミット禁止**: `git commit` を実行しない。
- **未コミット変更の保持**: `git reset` / `git checkout --` / `git stash` / `git restore` / `git push` を実行しない。「このファイルを触るな」は「編集するな」の意味であり「元の状態に戻せ」ではない。
- **一時ファイルの削除**: `patch_*.sh` や `grep_output.txt` などの作業用一時ファイルは作業後に必ず削除する。
- **実装と単体試験の同伴**: 実装を追加・変更したら、同じ変更に単体試験を必ず追加する (必須要件)。
- **事前検証**: 書いたコードは報告前にコンパイル・実行して確かめる。Go は `go build ./...`、フロントは `bun run typecheck`。試験が通ることを主張する前に実際に走らせる。
- **否定対照の復元**: `{false && ...}`, `if (true)`, コメントアウト等を入れたら必ず元に戻し、戻した状態で試験を再実行する。
- **起動失敗の切り分け**: コマンドが起動失敗（モジュール欠落・バイナリ欠落・ディレクトリ違い等）した場合はセットアップエラーとして報告し、検査対象の失敗と混同しない。
- **検索コマンド**: `rg` (ripgrep) は環境にないため `grep -rn` を使用する。
- **作業後の状態報告**: 最後に `git status --porcelain` と `git log --oneline -1` を報告する。
- **指示条件の誤り**: 指示された条件が誤っていると思ったら、勝手に数字や実装を合わせずに理由を付けて報告し停止する。
